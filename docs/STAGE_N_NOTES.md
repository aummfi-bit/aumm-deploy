# Stage N — Notes & Design Freeze

> **Status:** N0 NOTES design freeze (Stage N opens on the post-M tree; no `stage-n` branch cut yet) — N-D0—N-D6 LOCKED from the Stage N pre-flight Opus beat (the on-disk `aumm-site` manifest / sectors / `07a_tokens.md` clone + the `02`/`06` pool profiles + the Stage M config/deploy framework + the M-D7 deferred-pool characterization + the M-D11 composite-RP precedent). Companion to STAGE_N_PLAN.md (lands at N0.3).
>
> **Last update:** 2026-06-22 — N0 pre-flight + design freeze: token-availability survey cleared 16 clean Standard pools + 2 resolvable Non-Standard (`02 ixAetheron` via composite RPs, `06 ixLibertas` as a 7-token USD hub); `{04 ixViatica (fBRZ), 07 ixCambio (aEURS)}` hard-deferred. N-D0—N-D6 LOCKED.
>
> **Mode:** Opus high entry (Stage J/M posture — §13 has no Stage N row). The new `src/rate_provider/` contract surface (N3) is Opus extra-high (novel contract). Per-pool composition + literal authoring are Opus beats; clean-16 config transcription + unit/fork test writing + plan-row housekeeping drop to Sonnet.
>
> **Audience:** Sagix plus any future Claude session that needs the running log of decisions resolved during Stage N implementation and the incidents caught at audit.

---

## How this file is organized

- Design decisions during implementation. N-D0—N-D6 mirror STAGE_N_PLAN.md's N-D Decisions table (lands at N0.3) — all seven LOCKED at N0 from the pre-flight Opus beat. Any new decision resolved during implementation gets the next free N-D* number (N-D7 onward) and is recorded as a new entry — not retro-edited. Matches the C-D* … M-D* convention.
- Findings (N10 onward). Implementation incidents, drift caught at audit, scope-expansion catches, token-availability gotchas. Numbered from N10 to avoid collision with N-D* planning codes (matches the C10 … M10 pattern).
- Cross-reference convention (per CLAUDE.md §5): N-Dn = planning or implementation decision n; Nn (n ≥ 10) = implementation finding n; OQ-N = open question N from FINDINGS.md; F-n = formula n from `11_formulas.md`; §xxix = section in an aumm-site numbered spec; M-Dn / L-Dn / … = prior-stage decisions; Mn / Ln / … = prior-stage findings (carry forward across stages).

---

## Design decisions

The N-D rows below carry the design freeze locked at N0 from a pre-flight Opus beat. Stage N is the final Miliarium config/deployment stage (Stage-M-class): it deploys the Sector-3 (Equity + thematic) pools plus the two resolvable Majors deferred from M (M-D7) via the Stage E framework, binds them into the registry / gauge / emission layer, and adds the first Aureum-owned rate-provider surface (`src/rate_provider/`). Headline constraint (inherited M-D1): all per-pool composition data is sourced from the on-disk `aumm-site` clone (`/Users/janus/code/aumm-site/`), never WebFetch. Entries beyond the N0 freeze (N-D7 onward) attach to specific sub-step locks as decisions are resolved during N1—N7 execution.

| # | Status | Decision | Anchor | Locked at |
| --- | --- | --- | --- | --- |
| N-D0 | LOCKED | Stage N build scope = **18 pools**: the 16 originally-scoped Sector-3 slots `{12 ixStrata, 13 ixForum, 15 ixRegistrum, 16 ixDebitum, 17 ixEquitix, 18 ixInnovix, 19 ixGigantus, 20 ixMagnix, 21 ixNubix, 22 ixMoneta, 23 ixColossix, 24 ixVitalix, 25 ixMedicix, 26 ixMercatura, 27 ixAurix, 28 ixMetallum}` + the two resolvable Majors deferred from M-D7 `{02 ixAetheron, 06 ixLibertas}`. The two hard-blocked M-D7 Majors `{04 ixViatica, 07 ixCambio}` stay deferred — `fBRZ` (ixViatica theme) has no mainnet address (Flux lists none) and `aEURS` (ixCambio yield core, QG-critical) is not in the Aave address book; neither resolves to the E-D17 verified-literal bar. Supersedes STAGES_OVERVIEW's stale "~20 pools" + fictional "ixNovae / ixArtifex / …" pool-name prose (fixed at N1). Of the 16 clean: all Standard 26/26/16/16/16, QG = svZCHF 26% + stable-core 26% = 52% at floor; the `*on` tokenized equities / ETFs are `TokenType.STANDARD` ERC-20 theme legs (address-only, no RP), so they do not gate. | `06_miliarium_manifest.md` xiii (slot table); `07a_tokens.md` (token addresses); M-D2 9-slot Majors set; M-D7 deferral characterization; profiles `02_ixAetheron.md` / `06_ixLibertas.md`; user scope adjudication 2026-06-22 | N0 |
| N-D1 | LOCKED | **`CompositeRateProvider`** — new Aureum-owned `src/rate_provider/CompositeRateProvider.sol` `is IRateProvider`, the Stage-N restoration pattern M-D11 prescribed. Two immutables `(IERC4626 wrapper, IRateProvider underlyingRP)`, zero admin, zero storage; `getRate() = wrapper.previewRedeem(FixedPoint.ONE).mulDown(underlyingRP.getRate())` — Balancer `FixedPoint` math (mirrors the submodule `ERC4626RateProvider` `previewRedeem` + round-down), wrapper-shares → underlying-LST → ETH. Deployed **twice** for ixAetheron (02): `(waEthrETH, Rocket Pool rETH RP 0x1a8F81c256aee9C640e14bB0453ce247ea0DFE6F)` and `(waEthweETH 0x867b0CDC4B39a19945E616c29639b0390b39db3B, Aave weETH oracle 0x87625393534d5C102cADB66D37201dF24cc26d4C)`. Rejected: (a) two bespoke single-purpose RPs (more code, no benefit); (b) plain rETH / weETH fallback (kills the ixAetheron dual-yield thesis per `02_ixAetheron.md`). N4 preflight (NOT an architecture blocker, hard gate before literals land): (i) resolve the real `waEthrETH` Aave-stataToken wrapper address — `07a` lists canonical rETH `0xae78736…`, not the wrapper; (ii) fork-verify both underlying RPs return nonzero — the Aave weETH oracle `0x8762…` is the same stataToken-oracle family as the `0xe1D9…` wstETH oracle that reverted in M-D11, so it may itself be revert-class, in which case the weETH composite rebinds to an EtherFi weETH→ETH source (new N-D, mirroring M-D11). | M-D11 (composite-RP Stage-N restoration); `03_ixCasper.s.sol` L45—46 (`0x8Be2` / `0x72D07D` fWSTETH composite precedent); submodule `ERC4626RateProvider.sol` (`previewRedeem` pattern); `02_ixAetheron.md` (27/27 dual-core thesis); Balancer `FixedPoint.mulDown`; M5.1b RP-revert lesson; user sign-off 2026-06-22 | N0 |
| N-D2 | LOCKED | Missing-ERC4626-RP rule → Aureum-owned generic `src/rate_provider/ERC4626RateProvider.sol` (a ~22-line copy of the Balancer submodule pattern `previewRedeem(FixedPoint.ONE)`, NOT a submodule edit per CLAUDE.md §8c). Deployed for the two QG-critical ERC-4626 tokens whose `07a` RP column is absent / a non-RP link: **scrvUSD** `0x0655977FEb2f289A4aB78af67BAB0d17aAb84367` (ixLibertas 15%) and **sfrxUSD** `0xcf62F905562626CfcDD2261162a51fd02Fc9c5b6` (ixLibertas 14% + **ixMagnix 20** 26% + **ixAurix 27** 26%). One sfrxUSD RP deployment address is reused as a shared constant across all three configs. Without these, scrvUSD / sfrxUSD register `STANDARD` and drop from the `_compute52PctNumerator` count — ixLibertas QG falls to 42% (fail), and the sfrxUSD-core Standard pools lose a 26pp leg. | `07a_tokens.md` L48 (scrvUSD, no RP) / L47 (sfrxUSD, token-link not RP); submodule `ERC4626RateProvider.sol`; `06_ixLibertas.md` (57% QG); `GaugeEligibility._compute52PctNumerator`; CLAUDE.md §8c (no submodule edits); user sign-off 2026-06-22 | N0 |
| N-D3 | LOCKED | ixLibertas (06) `USDC` / `USDT` bind **canonical bare ERC-20**, `TokenType.STANDARD`, `rateProvider = address(0)` — NOT the `07a` "USDC" / "USDT" rows, whose "Ethereum address" cells carry the waEth *wrapper* addresses (`0xD4fa…` / `0x7Bc3…`). The `06_ixLibertas.md` profile specs bare stablecoins (USD Stable 6 / 7, ERC-20). Canonical mainnet: USDC `0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48`, USDT `0xdAC17F958D2ee523a2206206994597C13D831ec7`. Resolves the M-D7 "USDC / USDT bare-vs-wrapped form" deferral reason. | `06_ixLibertas.md` (bare ERC-20 USD Stable 6 / 7); `07a_tokens.md` L63—64 (wrapper-address-in-bare-row gap); M-D7 ixLibertas deferral; canonical USDC / USDT mainnet addresses | N0 |
| N-D4 | LOCKED | ixLibertas (06) `PYUSD` binds bare ERC-20 `0xb51EDdDD8c47856D81C8681EA71404Cec93E92c6`, `TokenType.STANDARD`, no RP — the `07a` PYUSD row's Aave-Core PYUSD RP (`0xdd8AEBC1…`) is ignored (profile specs bare PYUSD, USD Stable 2). ixLibertas final composition (7-token hub, no ixEDEL): scrvUSD 15% (4626, N-D2 RP) / PYUSD 15% (STD) / GHO 14% (4626, RP `0x851b73c4BFd5275D47FFf082F9e8B4997dCCB253`) / sUSDS 14% (4626, RP `0x1195be91e78ab25494c855826ff595eef784d47b`) / sfrxUSD 14% (4626, N-D2 RP) / USDT 14% (STD, N-D3) / USDC 14% (STD, N-D3); QG numerator = scrvUSD + GHO + sUSDS + sfrxUSD = 57% (+5pp over the 52% floor, per FINDINGS F8b). | `06_ixLibertas.md` (PYUSD USD Stable 2 bare); `07a_tokens.md` L41 (PYUSD); FINDINGS F8b (57% QG); pilot RP literals (GHO / sUSDS) | N0 |
| N-D5 | LOCKED | N6 fork-test architecture + assertion scope mirror M-D10: `test/fork/StageNIntegration.t.sol` (`StageNIntegrationFixture` on `StageIIntegrationFixture`, fresh real `MiliariumRegistry(address(this), [1,5,14], [pilots])`, `new DeployIx*().run()` for all 18, `vm.setEnv`-wire, `new DeployStageN().deploy(address(this))`) + `test/fork/DeployStageN.t.sol` (fail-fast governance reverts). Assertion scope = binding-liveness + QG re-assert (`evaluateEligibility`) + Rate-Provider resolve (`getRate() > 0` per the inherited `test_rateProvidersResolve_nonzeroGetRate` gate — now covering the two composite + two ERC4626 instances and the sfrxUSD legs on 20 / 27) + hook attach — NOT β-init emission accrual. `script/DeployStageN.s.sol` (N5) is bind-only per M-D9 (`replaceSlot` + `seedFoundingPool` + `setAuMTContractForPool` over the 18 slots, three fail-fast governance preconditions). | M-D10 (M5 fork architecture); M-D9 (bind-only orchestration); `StageMIntegration.t.sol` `test_rateProvidersResolve_nonzeroGetRate`; `StageIIntegrationFixture`; user M5-depth precedent | N0 |
| N-D6 | LOCKED | The new `src/rate_provider/` surface (`CompositeRateProvider` + `ERC4626RateProvider`) gets a Stage-N whitehat pass at N7 (the WN pass) before the `stage-n-complete` tag — first Aureum-owned RP contract surface, so it carries genuine review weight (rounding direction, `previewRedeem` vs `convertToAssets`, zero-rate / revert propagation from the underlying RP, decimals scaling). Unit tests (N3) + fork liveness (N6) + whitehat (N7) is the three-gate close, mirroring the WM / WL cadence. | CLAUDE.md §1 (small reviewable surface); white-hat per-stage cadence; WM / WL precedent; N3 `src/rate_provider/` | N0 |
| N-D7 | LOCKED | **RP-address threading = config-parameter injection via env var** (plan candidate (c); user-adjudicated). The four Aureum-deployed Rate Providers (sfrxUSD + scrvUSD `ERC4626RateProvider`, 2× ixAetheron `CompositeRateProvider`) have no mainnet address, so they cannot be baked into a `pure` config. Mechanism: (1) `MiliariumPoolDeployer._config()` base mutability widens `pure`→`view` — one-word I13 fix-forward on the tagged Stage-E base; the 14 clean wrappers keep their `pure override` (`pure ⊆ view`), so no clean-config touch and no Stage-E re-tag; (2) the RP-dependent config libraries take the RP address(es) as `config(address …)` parameters — `IxMagnixConfig.config(sfrxUsdRp)` (20), `IxAurixConfig.config(sfrxUsdRp)` (27), `IxLibertasConfig.config(scrvUsdRp, sfrxUsdRp)` (06), `IxAetheronConfig.config(waEthrEthRp, waEthweEthRp)` (02); (3) their `view` wrappers resolve the address(es) from env vars — `SFRXUSD_RATE_PROVIDER` (the single shared instance reused across 06/20/27, honouring N-D2's "shared constant"), `SCRVUSD_RATE_PROVIDER` (06), `WAETHRETH_COMPOSITE_RATE_PROVIDER` + `WAETHWEETH_COMPOSITE_RATE_PROVIDER` (02) — mirroring the existing `AUREUM_WEIGHTED_POOL_FACTORY` / `FEE_ROUTING_HOOK` / `GOVERNANCE_MULTISIG` resolution in `run()` (L37-39); (4) the N6 fixture deploys the four RP instances, `vm.setEnv`s these keys, then calls `new DeployIx*().run()`. The shared `run()` body and its 52%-QG re-assertion (L51-59) are UNCHANGED — the config returns the real non-zero RP address, so the WITH_RATE weight counts toward QG normally. Rejected: (a) CREATE2-deterministic bake — keeps configs pure but the baked constant is sensitive to RP bytecode/metadata-hash drift (silent stale address on any RP/import edit) and needs a CREATE2 factory + predicted==deployed guard; (b) fixture `vm.etch` — test-only cheatcode with no real-deploy analogue, undercuts the N7 WN whitehat review. Forward-compatible: Stage-P/R real deploy sets the same env keys to the real RP addresses, any deploy method. | `MiliariumPoolDeployer` L32 (`_config()` pure) + L37-39 (env pattern) + L51-59 (QG); N-D1 / N-D2 (the four RP instances); `StageMIntegration.t.sol` setUp (fixture env-wire precedent); user adjudication 2026-06-23 | N3.0 |
| N-D8 | LOCKED | **ixAetheron (02) redesign — native-4626 non-Lido ETH-staking basket; supersedes the N-D1 ixAetheron composite leg** (user-adjudicated 2026-06-23). Composition (5-token, ascending-address-sorted at literal-write time): sfrxETH 28% / wOETH 28% / ixEDEL 16% / rETH 14% / weETH 14%; QG numerator = sfrxETH 28 + wOETH 28 = **56%** (+4pp over the 52% floor). Drivers: avoid Aave wrappers + Aave/oracle dependency + KelpDAO, simpler RP, native-4626 / ETH+-vetted cores preferred (Aureum supplies the liquidity, so Reserve depth concerns do not bind — but Reserve's ETH+ token vetting is a useful signal). Why N-D1's Aave path was dropped: waEthrETH (`0x867Cf025B5dA438c4e215c60B59bBB3aFe896Fda`, the Aave Core `STATIC_A_TOKEN` for rETH, resolved from the bgd-labs address-book) and waEthweETH (`0x867b0CDC4B39a19945E616c29639b0390b39db3B`) are the ONLY rETH/weETH route that counts toward the `GaugeEligibility._compute52PctNumerator` gate (admitted-ERC-4626-only — `src/gauge/GaugeEligibility.sol` L337-348), but both require Aave-stata oracles — the M-D11 revert class (the weETH oracle `0x87625393534d5C102cADB66D37201dF24cc26d4C` is the same stataToken-oracle family as the wstETH `0xe1D97bF61901B075E9626c8A2340a7De385861Ef` that reverted in M-D11) plus two Aureum `CompositeRateProvider` instances. Plain rETH/weETH are ERC-20 (not ERC-4626) → `IERC4626.asset()` reverts → add **0** to the gate, so they cannot carry it. Cores resolved: sfrxETH (Frax) + wOETH (Origin), native ERC-4626, fork-verified 2026-06-23 (`asset()` → frxETH `0x5E8422345238F34275888049021821E8E08CAa1f` / OETH `0x856c4Efb76C1D1AE02e20CEB03A2A6a08b0b8dC3`; `previewRedeem(1e18)` = 1.162e18 / 1.161e18; oracle-free), both ETH+-vetted. Token + RP wiring (E-D17 fork-verified literals): (1) sfrxETH `0xac3E018457B222d93114458476f3E3416Abbe38F` — `WITH_RATE`, RP = Aureum `ERC4626RateProvider` (N3.1) env-injected per N-D7 as `SFRXETH_RATE_PROVIDER` (no mainnet RP; `previewRedeem` treats frxETH≈ETH, mirroring N-D2's sfrxUSD→frxUSD≈USD); (2) wOETH `0xDcEe70654261AF21C44c093C300eD3Bb97b78192` — `WITH_RATE`, RP = Aureum `ERC4626RateProvider` env-injected as `WOETH_RATE_PROVIDER` (OETH hard-1:1 ETH); (3) rETH `0xae78736Cd615f374D3085123A210448E74Fc6393` — `WITH_RATE`, RP = `0x1a8F81c256aee9C640e14bB0453ce247ea0DFE6F` mainnet literal (Rocket Pool rETH RP, `getRate` = 1.165e18, oracle-free), plain ERC-20 → 0 to QG; (4) weETH `0xCd5fE23C85820F7B72D0926FC9b05b43E359b7ee` — `WITH_RATE`, RP = weETH **itself** (`0xCd5fE23C85820F7B72D0926FC9b05b43E359b7ee`, satisfies `IRateProvider.getRate` = 1.097e18, oracle-free self-RP), plain ERC-20 → 0 to QG; (5) ixEDEL `0xe4a10951f962e6cB93Cb843a4ef05d2F99DB1F94` — `STANDARD`, no RP. Consequences: supersedes the N-D1 ixAetheron leg (the 2× `CompositeRateProvider`-for-02 design) — the `CompositeRateProvider` contract (N3.2) is NOT removed, retained as tested zero-admin infrastructure with no current consumer (available for a future composite need / the deferred M-D11 ixCasper waEthwstETH restoration); N-D7 ixAetheron env keys change `WAETHRETH_COMPOSITE_RATE_PROVIDER` / `WAETHWEETH_COMPOSITE_RATE_PROVIDER` → `SFRXETH_RATE_PROVIDER` / `WOETH_RATE_PROVIDER` (two `ERC4626RateProvider`, not composites), the N-D7 param-injection-via-env mechanism unchanged (`IxAetheronConfig.config(sfrxEthRp, wOethRp)` with a `view` wrapper reading the two keys); gov tokens RPL/ETHFI dropped (the non-Lido thesis now rides four non-Lido staking protocols — Frax / Origin / Rocket Pool / EtherFi); admission auto-handled at N6 via `_makePoolEligible` per-pool auto-admit (`test/fork/StageGIntegration.t.sol` L382), real-deploy genesis admission a Stage-P/R obligation; amends the `02_ixAetheron.md` profile (waEthrETH/waEthweETH/RPL/ETHFI → this basket) — aumm-site update user-side, deferred. | N-D1 (superseded ixAetheron leg) / N-D2 + N3.1 (`ERC4626RateProvider` reuse) / N-D7 (env-injection mechanism + ixAetheron key change) / M-D11 (Aave-stata-oracle revert class avoided) / `GaugeEligibility.sol` L337-348 (admitted-4626 gate) / `StageGIntegration.t.sol` L382 (`_makePoolEligible` auto-admit) / ETH+ token vetting / fork-verification + user adjudication 2026-06-23 | N4.0 |

---

## Token-availability preflight (N0)

| Tier | Slots | Status | Note |
| --- | --- | --- | --- |
| Clean Standard (16) | 12, 13, 15, 16, 17—26, 27, 28 | BUILD (N2) | QG = svZCHF 26 + stable-core 26 = 52% floor; `*on` / gov theme legs are `TokenType.STANDARD` ERC-20 (address-only) |
| Resolvable Non-Standard (2) | 02 ixAetheron, 06 ixLibertas | BUILD (N4) | 02 needs 2× `CompositeRateProvider` (N-D1); 06 needs scrvUSD / sfrxUSD ERC4626 RP (N-D2) + bare USDC / USDT / PYUSD (N-D3 / N-D4) |
| Hard-deferred (2) | 04 ixViatica, 07 ixCambio | DEFER | fBRZ (no mainnet addr) / aEURS (not listed) — undeployable to the E-D17 bar |
| RP watch | 20 ixMagnix, 27 ixAurix | VERIFY (N6) | sfrxUSD yield-core RP not battle-tested in a built pool — fork-verify per `test_rateProvidersResolve_nonzeroGetRate` |
