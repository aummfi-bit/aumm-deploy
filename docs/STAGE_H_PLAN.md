# Stage H — Emission Distributor

> **Status:** H8 unit-test confirmation pass complete (612/612 green, 22 suites, 19.17s, no code changes). H11 family in progress: H11.0a NOTES H13 Findings entry at `fb2d9b1`. **H-D locks:** H-D1—H-D6 + H-D8—H-D42 LOCKED (39 total). Next: H11.0b — PLAN Completion Log catch-up (this commit); H11.0c — CLAUDE.md §11 resume-anchor refresh; H12 — Stage close.
>
> **Last update:** 2026-05-23 — H8 unit-test confirmation pass 612/612 green (22 suites, 19.17s, no code changes); H11.0a docs/STAGE_H_NOTES.md H13 Findings entry at `fb2d9b1`; H11.0b PLAN Completion Log catch-up this commit.
>
> **Mode:** Sonnet for H11.0b — PLAN Completion Log catch-up (this commit); Opus high for H11.0c — CLAUDE.md §11 resume-anchor refresh; Sonnet for H12 — Stage close fast-forward merge + `stage-h-complete` tag + snapshot push.

## Goal

Stage H delivers the emission distributor: F-0 piecewise bootstrap decay (Months 0—6 then 6—10), F-1 equal split across Miliarium pools for Months 0—10, F-3 linear blend across Months 11—12, and F-7 full CCB-weighted distribution from Year 2 onward, with geometric era halving at BLOCKS_PER_ERA boundaries per the canonical schedule. Minted AuMM authority hands off through IAuMM.setMinter() as a one-shot; the stage also exposes an AuMT-claim interface forward dependency for Stage I and an IIncendiaryRegistry interface forward dependency for Stage L. Stage H tag: stage-h-complete.

## Dependencies

- src/lib/AureumTime.sol (Stage C)
- src/token/AuMM.sol + src/token/IAuMM.sol (Stage C — IAuMM.setMinter one-shot handoff)
- src/fee_router/AureumFeeRoutingHook.sol + der Bodensee deployment (Stage D)
- src/ccb/CCBScore.sol + src/ccb/CCBShare.sol + src/ccb/EMASampler.sol + src/ccb/CCBMultiplier.sol (Stage F)
- src/gauge/GaugeRegistry.sol + src/gauge/IEfficiencyOracle.sol interface (Stage G — IGaugeRegistry.isGaugeApproved consumer; IEfficiencyOracle concrete ships at H2b)
- src/ccb/ITVLOracle.sol interface (Stage F — concrete ships at H2a)

## Sub-step skeleton

```text
H0 — Stage H scaffolding: PLAN/NOTES/overview prep (H0.X family commits)
H1 — Architecture + schedules inventory; lock H-D6 (F-1 share denominator); interface boundary freeze
H2 — In-stage oracles per H-D1 — H2a concrete ITVLOracle; H2b concrete IEfficiencyOracle
H3 — BodenseeBootstrapChannel per H-D2 — isolated F-0 bootstrap rail to der Bodensee
H4 — Pool-scoped emission distributor per H-D4—H-D5 + H-D15—H-D23 — H4.0a NOTES freeze + H4.0b PLAN refresh; H4.1 IEmissionDistributor interface; H4.1.x-bis IEfficiencyOracle in-place recordEmissions + G16 inheritor sweep; H4.2 scaffold; H4.3 constructor; H4.4 governance setters; H4.5 _accrueGlobal + _settlePool + _lpTrancheEmission stub + recordScore; H4.6 recordDeposit + recordWithdrawal; H4.7 claim + IAuMM.mint; H4.8a—H4.8N unit cohort
H5 — Era halving + phased schedule wiring + Stage L Incendiary forward-dep stub + `recordScore` reshape + Harness observation surface (interval-integrated `_lpTrancheIntegral` per H-D26—H-D30 + F-3 α-blend reshape per H-D31—H-D33 + Harness contract per H-D34): H5.0a NOTES H-D26—H-D30 LANDED 841fe24; H5.0b PLAN refresh (H5 family skeleton + H-D26—H-D30 Decisions rows + H4.8.10c/d + H5.0a Completion Log self-rows + status header + Last update + Mode) LANDED 8bf5be5; H5.1a src/emission/EmissionDistributor.sol — `incendiaryRegistry` slot + `IncendiaryRegistrySet` event + `setIncendiaryRegistry(address)` `onlyGovernance` setter per H-D29 LANDED 1291271; H5.1b src/emission/EmissionDistributor.sol — private view `_bootstrapApSum` per H-D27 (bit-for-bit mirror of `BodenseeBootstrapChannel._apSum`) LANDED 0ccb64c; H5.1c src/emission/EmissionDistributor.sol — private view `_phaseAwareBody` three-phase dispatcher per H-D27/H-D28/H-D29 (bootstrap AP + transition `rate × n` + continuous `rate × n − Incendiary_skim`) LANDED 3a1c0fc; H5.1d src/emission/EmissionDistributor.sol — internal view `_lpTrancheIntegral(from, to)` era-splitting cursor walk per H-D26/H-D30 LANDED b0bf0d9; H5.1e src/emission/EmissionDistributor.sol — `_accrueGlobal` body rewrite per H-D26 (`_lpTrancheIntegral(last+1, block.number)` supersedes H-D21 single-block sample) LANDED 7b1babb (H5.1 helpers family complete); H5.2 src/incendiary/IIncendiaryRegistry.sol — Stage L forward-dep interface stub `integratedSkim(uint256 from, uint256 to) external view returns (uint256)` per H-D29 LANDED c4cfe7f (first file under src/incendiary/ — consolidates H7's original IIncendiaryRegistry surface duty); H5.3a NOTES H-D31—H-D33 LOCKED LANDED d71bd9c; H5.3b src/emission/EmissionDistributor.sol — `IMiliariumRegistry` import + 6th typed immutable `_miliariumRegistry` + `f5Total` / `f5Score` storage slots + 7th constructor param `miliariumRegistry_` + ZeroAddress guard per H-D31 LANDED 64d8af7; H5.3c test/unit/EmissionDistributor.t.sol — `MockMiliariumRegistry` + `miliReg` wiring into 8 constructor sites + `RevertWhen_ConstructedWithZeroMiliariumRegistry` + `WiresImmutables` 7th assertion + `InitsStorageSlots` `f5Total==0` per H-D31 / G16 inheritor enumeration LANDED 39b055e; H5.3d src/emission/EmissionDistributor.sol — `_alphaF3(uint256 block_)` private view per H-D32 (linear F-3 α blend with lower clamp 0 at ≤month10EndBlock + upper clamp 1e18 at ≥year1EndBlock + 437_999-block denominator) LANDED 506ca89; H5.3e src/emission/EmissionDistributor.sol — `recordScore` body rewrite to H-D31 10-step flow + H-D33 reshape (`f5Total`/`f5Score` signed-delta + `_alphaF3` snapshot + Miliarium `(1e18-α).mulDown(f5Total/28) + α.mulDown(F5)` + non-Miliarium `α.mulDown(F5)`) LANDED 4eebdef; H5.3f test/unit/EmissionDistributor.t.sol — `recordScore` cohort migration to H-D31/H-D33 (`vm.roll` past `year1EndBlock` + `f5Score`/`f5Total` asserts on 8 existing tests + 2 new bootstrap α=0 tests: Miliarium `f5Total/28` + non-Miliarium zero) LANDED e34616a; H5.3f-fix1+fix2 test/unit/EmissionDistributor.t.sol — rational literal cast `uint256(100e18)/28` (fix1) + 16-test `vm.roll` migration to α=1e18 continuous regime + Accrual `lastAccrualBlock` assertion rebase to `year1EndBlock` (fix2) LANDED 7b75aad (H5.3 family complete; 67/67 tests green); H5.4.0a NOTES H-D34 LOCKED (Harness observation surface) LANDED 73c5979; H5.4.0b PLAN H-D31—H-D34 Decisions rows LANDED ccc29d9; H5.4.0c PLAN status header + Last update + Mode + sub-step skeleton H5 row refresh (this commit); H5.4.0d test/unit/harness/EmissionDistributorHarness.sol — Harness scaffold `is EmissionDistributor` + 8-arg constructor pass-through (aumm, gaugeRegistry, emaSampler, ccbMultiplier, efficiencyOracle, miliariumRegistry, genesisBlock, initialGovernance) + 2 public-view wrappers `extLpTrancheIntegral(uint256 from, uint256 to)` + `extLpTrancheEmission(uint256 block_)` per H-D34; H5.4.0e test/unit/EmissionDistributor.t.sol — setUp retype `distributor` slot from `EmissionDistributor` to `EmissionDistributorHarness` + harness import path per H-D34; H5.4.0f test/unit/EmissionDistributor.t.sol — H5 observation cohort (AP arithmetic identity vs `BodenseeBootstrapChannel._apSum` fixture + era-boundary cursor loop coverage + phase-boundary nesting + continuous-leg Incendiary stub zero-skim + global conservation invariant assertion across phase boundaries); H5.4.0g test/unit/EmissionDistributor.t.sol — `incendiaryRegistry` governance setter cohort (happy path + NotGovernance revert + zero-address deprecation valve acceptance + `IncendiaryRegistrySet` emit); H5.5 close-of-family sweep — Completion Log catch-up (H5.0b—H5.4.0g rows) + STAGE_H_NOTES.md Findings queue updates if any + CLAUDE.md §11 refresh + RB-005-style robustness backport register row if any
H6 — AuMM minter plumbing + AuMT forward-dep in-place extension + real-AuMM integration tests per H-D35 (amended at H6.0c) + H-D36: H6.0a NOTES H-D35 + H-D36 LOCKED LANDED c1ef0c9; H6.0b PLAN sub-step skeleton + Decisions rows + Completion Log backfill + status header LANDED 78578b3; H6.0c NOTES H-D35 amended (in-place extension reframe + G16 sweep required at H6.2a + G-D9 `governanceWeight` preserved per G-DP4 + Stage G `VaultClassRegistry.sol:277` consumer untouched) LANDED 856a0f4; H6.0d PLAN refresh — L31 sub-step skeleton rewrite + L78 H-D35 row rationale rewrite + status header + Completion Log H6.0c/H6.0d rows + H6.0b `<pending>` → `78578b3` backfill (this commit); H6.1 src/token/IAuMT.sol — in-place extension per amended H-D35 (preserve `is IERC20` + G-D9 `governanceWeight(address holder) external view returns (uint256)` from Stage G consumed at `src/gauge/VaultClassRegistry.sol:277` via `auMT = IAuMT(auMT_)` L211; add `mint(address to, uint256 amount) external` + `burn(address from, uint256 amount) external` + `distributor() external view returns (address)` + `pool() external view returns (address)`; NOT a new file — extends existing G-DP4 forward-dep stub); H6.2a test/unit/VaultClassRegistry.t.sol + test/fork/mocks/StageGMocks.sol — G16 inheritor sweep per amended H-D35 (extend BOTH `MockAuMT` inheritors at L29 + L34 respectively with stub bodies for the 4 new functions returning 0 / `address(0)` IN THE SAME COMMIT per `test/fork/mocks/StageGMocks.sol:L10` propagation NOTE; existing `governanceWeight` + 6 IERC20 stubs preserved); H6.2b forge test --match-path test/unit/VaultClassRegistry.t.sol — Stage G veto cohort regression check (existing `vetoProposal` cohort still green under extended IAuMT — no abstract-contract or selector-mismatch regression per G16 discipline); H6.3a test/unit/AuMMDistributorIntegration.t.sol — scaffold per H-D36 (contract `AuMMDistributorIntegrationTest is Test` + real `AuMM` import from `src/token/AuMM.sol` + `EmissionDistributorHarness` import + setUp wiring `IAuMM(address(new AuMM(GENESIS_BLOCK_, address(this))))` per C-D11 two-flag-lock + constructor chain identical to `EmissionDistributorTest` except `aumm` slot typed as real `AuMM`); H6.3b test/unit/AuMMDistributorIntegration.t.sol — setMinter handoff + revert tests (`test_SetMinterHandoff_EmitsMinterSet` happy path + `test_RevertWhen_SetMinterCalledTwice` C-D11 `_minterAdmin` zeroed on first success → second call reverts `NotMinterAdmin`); H6.3c test/unit/AuMMDistributorIntegration.t.sol — claim + halving-boundary tests (`test_ClaimMintsRealAuMM_SinglePool` mints AuMM to USER_1 via real `IAuMM.mint` path + `test_ClaimAcrossHalvingBoundary_UsesRealBlockEmissionRate` `vm.roll` past `AureumTime.firstHalvingBlock(GENESIS_BLOCK_)+1` asserts `_lpTrancheIntegral` cursor walk per H-D30 snapshots `GENESIS_RATE >> eraIndex` halved rate `5e17` wei/block at Era 1 boundary); H6.4 close-of-family sweep — Completion Log catch-up (H6.1—H6.3c rows) + STAGE_H_NOTES.md Findings queue updates if any + CLAUDE.md §11 refresh
H7 — Consolidated into H5.2 per H-D29 — IIncendiaryRegistry forward-dep interface stub lands at src/incendiary/IIncendiaryRegistry.sol within Stage H emission wiring (matches CLAUDE.md repository layout + H-D29 canonical text "Stage L deploys the canonical IIncendiaryRegistry implementation under src/incendiary/"); slot retained for any post-H5 Stage L surface refinement during emission rollout
H8 — Unit tests — schedule math, distributor core, bootstrap channel, oracle adapters
H9 — Fork integration test cohort per H-D37—H-D40 — H9.0a NOTES H-D37—H-D40 LOCKED (mock swap-out matrix + StageHIntegrationFixture inheritance from StageGIntegrationFixture + 4-contract cohort per emission-schedule phase + BodenseeBootstrapChannel.distribute() full IVault.unlock arc against real Balancer V3 Vault + D35 split-form + D36 --threads 1 belt + block-pin posture) LANDED eade88f; H9.0b PLAN sub-step skeleton + H-D37—H-D40 Decisions rows + H6.4e/H6.4f/H9.0a Completion Log catch-up + status header + Last update + Mode refresh (this commit); H9.1 test/fork/StageHIntegration.t.sol — StageHIntegrationFixture is StageGIntegrationFixture scaffold per H-D37 (setUp override: super.setUp() reuses Stage G CREATE3 vault salt + Bodensee BPT init + ixPilot deployment + governance multisig keccak + SwapAndDepositToBodensee hook registration; chain extension TVLOracle + EfficiencyOracle + BodenseeBootstrapChannel + EmissionDistributor + efficiencyOracle.setEmissionsRecorder(address(emissionDistributor)) per H-D23 + emissionDistributor.setAuMTContract(address(this)) per H6.3 precedent; MockMiliariumRegistry isMiliarium(pool)=true for 3 pilots; incendiaryRegistry stays address(0) per H-D29 zero-stub; minter-slot wiring (aumm.setMinter) deferred to each derived contract's setUp override per H-D37 amended at H9.0c (Option A — avoids Bodensee-pool AuMM token conflict between StageHBootstrapPhaseTest needing bootstrapChannel as minter for H-D39 full distribute() arc vs emission-distributor-path tests needing emissionDistributor as minter)); H9.2 test/fork/StageHIntegration.t.sol — StageHBootstrapPhaseTest is StageHIntegrationFixture per H-D38(1) (F-0/F-1 bootstrap arc: accrue() cadence across Bootstrap A 80→50% + Bootstrap B 50→0% legs + H-D11 AP closed-form identity + pendingAccrual + totalDistributed = Σ_accrued invariant); H9.3 test/fork/StageHIntegration.t.sol — StageHBootstrapPhaseTest distribute() arc per H-D39 (vm.prank(GOVERNANCE_MULTISIG); distribute() → real IVault.unlock → _distributeCallback → safeTransfer(vault, pendingAmount) → vault.settle(aumm) → vault.addLiquidity(... DONATION ...); post-assertions Bodensee AuMM reserve += pendingAmount + BPT supply unchanged + Distributed event + pendingAccrual==0 + totalDistributed incremented + transient slots cleared; sad-path pair test_RevertWhen_DistributeCalledByNonGovernance + test_RevertWhen_DistributeWithNoPendingAccrual); H9.4 test/fork/StageHIntegration.t.sol — StageHContinuousPhaseTest is StageHIntegrationFixture per H-D38(2) (F-7 phase: vm.roll past year1EndBlock(GENESIS_BLOCK_) from setUp → α=1e18; 3-pilot recordScore + recordDeposit + claim → assert aumm.balanceOf(to) > 0 + Claimed event + bounded conservation |Σ_i pendingClaim_i − lpTrancheEmission(span)| ≤ N_gauged wei per H-D29 zero-stub); H9.5 test/fork/StageHIntegration.t.sol — StageHHalvingBoundaryTest is StageHIntegrationFixture per H-D38(3) (H-D30 era cursor walk against real Vault state: vm.roll straddle of AureumTime.nthHalvingBlock(GENESIS_BLOCK_, 1) with 20-block window → claim → assert aumm.blockEmissionRate(nthHalvingBlock(g,1)) == 5e17 AND claim amount equals cursor-walked 1e18 × n_pre + 5e17 × n_post per H6.3c arithmetic against real Vault reserve state); H9.6 test/fork/StageHIntegration.t.sol — StageHCrossStackTest is StageHIntegrationFixture per H-D38(4) (full cross-stack wiring: gauge approval via VaultClassRegistry → real EMASampler TVL EMA push via SwapAndDepositToBodensee swap → TVLOracle.tvl(pool) reflecting EMA → EfficiencyOracle.efficiencyInputs(pool) → recordScore F-3/F-7 reshape per H-D33 → recordDeposit → claim → aumm.balanceOf(to) > 0); H9.7 close-of-family sweep — Completion Log catch-up (H9.1—H9.6 rows) + STAGE_H_NOTES.md Findings queue updates if any + CLAUDE.md §11 refresh
H10 — H-D7 lock (Option C) + deployment script + multisig dry-run evidence: H10.0a NOTES H-D7 LOCKED + H-D41 carry-forward LANDED db75c6b; H10.0b PLAN refresh LANDED c4b747a; H10.1 script/DeployStageH.s.sol — Stage H deployment script (AuMM + TVLOracle + EfficiencyOracle + BodenseeBootstrapChannel + EmissionDistributor + post-deploy wiring; no setMinter per H-D7 Option C; MinterNotZero invariant assertion; 191 lines) LANDED 90f9a82 (subsequently revised at H10.1-fix1 per H-D42); H10.2-first-attempt test/fork/DeployStageH.t.sol — multisig dry-run evidence fork test, REVERTED on `call to non-contract address 0x6076…DFFb` (VAULT placeholder) — surfaced AuMM-vs-Bodensee chicken-and-egg per BodenseeBootstrapChannel constructor `_vault.getPoolTokens(BODENSEE_POOL)` AuMM-index lookup (H-D12); H10.0c-fix NOTES H-D42 LOCKED (AuMM as Stage H input; DeployStageH.s.sol consumes AUMM env per DeployDerBodensee.s.sol L39 convention) LANDED 2af6c8d; H10.0d-fix PLAN refresh + H-D42 Decisions row + Completion Log catch-up this commit; H10.1-fix1 script/DeployStageH.s.sol revision per H-D42 (remove `new AuMM` Step 1; add `AuMM(vm.envAddress("AUMM"))` env-read; derive GENESIS_BLOCK from `aumm.GENESIS_BLOCK()`; preserve `MinterNotZero` invariant assertion as input precondition); H10.2-revised test/fork/DeployStageH.t.sol — inherit from StageGIntegrationFixture (provides Vault + AuMM + Bodensee + gauge stack); H10.3 close-of-family sweep (Completion Log catch-up + status header + mode)
H11 — Completion Log + cross-docs (FINDINGS cross-refs as needed)
H12 — Stage close — fast-forward merge, stage-h-complete tag, preserved stage-h snapshot convention
```

## H-D Decisions

| ID | Decision | Status | Rationale |
|----|----------|--------|-----------|
| H-D1 | Oracles in-stage as H2a/H2b | LOCKED | Matches Stage G precedent of single-branch stage with sub-step families inside |
| H-D2 | Separate BodenseeBootstrapChannel contract | LOCKED | Mirrors G-D11 / G-D21 audit-isolation precedent |
| H-D3 | F-7 share weights step-function sampled at BLOCKS_PER_EPOCH boundaries | LOCKED | F-7 spec literal "every block" discretised to epoch cadence — matches F-8 multiplier cadence per F-D24; deviation documented in STAGE_H_NOTES.md |
| H-D4 | Pull / lazy accrual via MasterChef-style accRewardPerScoreUnit accumulator | LOCKED | Spec language "deposit triggers accrual; withdrawal stops it immediately" and gas economics dictate pull model |
| H-D5 | Pool-scoped distributor; no IGaugeRegistry enumeration; incremental totalScore aggregate updated permissionlessly per recordScore(pool) | LOCKED | isGaugeApproved(pool) per score-record call suffices — no G16-class mock sweep needed |
| H-D6 | F-1 share denominator — fixed 1/28 literal vs dynamic 1/N_miliarium | LOCKED | Literal 1/28 per F-1 canonical text — pre-N=28 fraction structurally unminted by design (no treasury buffer, no deferred mint); F-3 equal-split leg inherits same denominator; dynamic-N enumeration rejected per H-D5; no FINDINGS promotion needed — see STAGE_H_NOTES.md H-D6 |
| H-D7 | AuMM.setMinter() handoff timing — Stage H deploy vs Stage K governance migration | LOCKED | Option C — defer setMinter to Stage K; no setMinter call at Stage H deploy; accrue/recordScore/recordDeposit/recordWithdrawal bookkeeping accumulates from GENESIS_BLOCK; distribute()/claim() revert NotMinter pre-Stage-K; H-D41 multi-minter architecture carry-forward to Stage K; H-D42 pivots AuMM from Stage H deployment artifact to Stage H input (DeployStageH.s.sol consumes AUMM env per DeployDerBodensee.s.sol L39 convention; Option C `_minterAdmin = GOVERNANCE_MULTISIG` invariant becomes input precondition asserted via `MinterNotZero` rather than enforced by construction) |
| H-D8 | TVLOracle constellation roster typed-domain set | LOCKED | `{IMiliariumRegistry roster} ∪ {BODENSEE_POOL} ∪ {governanceAddedPools}` per OQ-8 BTC_WRAPPERS precedent; pre-Stage-J collapses to `{BODENSEE_POOL} ∪ {governanceAddedPools}` per F-D9; governance-extensible via `setGovernanceContract`-pivoting pattern per Stage G `GaugeRegistry`; roster append-only at Stage H |
| H-D9 | TVLOracle per-token underlying + cross-asset leg arithmetic | LOCKED | Step 1 reads `IVaultExplorer.getPoolData(pool).balancesLiveScaled18[]`; Step 2 balance-ratio averaging at constellation venues via `_constellationRatio`; per-token wrapper → underlying via governance-curated `tokenToUnderlying` map; Phase 1 direct venues only; OQ-22 L1115 2-hop carry-forward |
| H-D10 | EfficiencyOracle intra-epoch accumulation + EpochEntry ring buffer + svZCHF numéraire | LOCKED | `EpochEntry[3]` ring with epoch-stamp stale detection; `recordFees(pool, token, amountScaled18)` + `recordEmissions(pool, aummAmountScaled18)` convert to svZCHF via `tvlOracle.quoteSvZCHF` (H-D10 v2 per H1.9-bis); `efficiencyInputs` view divides by 3; immutable `ITVLOracle tvlOracle` + `address AuMM` |
| H-D11 | BodenseeBootstrapChannel mint-at-distribute + permissionless accrue accumulator + AP piecewise integration | LOCKED | `accrue()` permissionless bookkeeping (`lastAccrualBlock` + `pendingAccrual` in AuMM scaled18; no `IAuMM.mint`); `distribute()` governance-gated mint + Bodensee DONATION; F-0 piecewise share Bootstrap A 80% → 50% (`[genesisBlock, month6EndBlock]`) + Bootstrap B 50% → 0% (`(month6EndBlock, month10EndBlock]`); per-leg AP sum `((first + last) / 2) × n × rate` O(1); cross-boundary calls split into two AP sums; Era 0 covers all bootstrap per OQ-3 |
| H-D12 | BodenseeBootstrapChannel one-sided AuMM DONATION via Vault.unlock (mirrors G-D11 / G-D21; does NOT reuse SwapAndDepositToBodensee.donate) | LOCKED | `SwapAndDepositToBodensee.donate` rejects non-svZCHF/sUSDS `payToken` per `src/gauge/SwapAndDepositToBodensee.sol` L329 + L362–L364; channel implements own `IVault.unlock` callback path with AuMM as pay token; 10-step distribute flow (compute amount → mint → snapshot reserve → transient slots → unlock → settle → addLiquidity DONATION → clear slots → residual assert); `_aummIndex` introspected at construction via `_vault.getPoolTokens(_bodensee)`; three transient slots (`_EXECUTING_SLOT` + `_PENDING_AMOUNT_SLOT` + `_ORIGINAL_CALLER_SLOT`) via `StorageSlotExtension.calculateSlot` per G-D11 |
| H-D13 | BodenseeBootstrapChannel lifecycle clamp + empty-interval short-circuit + no selfdestruct | LOCKED | `to = min(block.number, month10EndBlock)` clamps accrual past Month 10; `if (from > to) return;` short-circuits empty intervals; no `selfdestruct` / `disable` / `pause` (EIP-6049 / EIP-4758 deprecate `selfdestruct` post-Cancun); contract permanent zero-activity fixture post-window with readable `pendingAccrual` + `totalDistributed`; governance `distribute()` remains live for residual flush |
| H-D14 | BodenseeBootstrapChannel governance-only distribute() + immutable BODENSEE_POOL recipient | LOCKED | `onlyGovernance` gates `distribute()` (H-D11 mint-at-distribute justifies — prevents arbitrary-block caller-chosen price-window force-flush); mutable governance slot via `setGovernanceContract(address)` rejects `address(0)` per TVLOracle precedent; recipient pool immutable `BODENSEE_POOL` (no `setBodenseePool` setter) — structurally stronger than H-D8 roster / H-D10 recorder slots; F-2 Stage L Incendiary fee burn not coupled in-channel (Stage D `AureumProtocolFeeController` continues to route fees) |
| H-D15 | Global `accRewardPerScoreUnit` + per-pool `poolAccDebt` lazy settle (two-tier accumulator topology) | LOCKED | Formalizes H-D4 — solitary global `accRewardPerScoreUnit` + per-pool `poolAccDebt[pool]` snapshots; O(1) per-pool settle without cross-pool sweep; MasterChef / Synthetix lazy-settle; `_accrueGlobal` advances global, `_settlePool` computes `poolAllocation = (acc − poolAccDebt).mulDown(poolScore)` then writes debt; empty-`totalScore` guard prevents division-by-zero; F12 signed-delta colocated at H-D19 score-recording site, not at settle |
| H-D16 | Per-user maps shipped at H4 + governance-settable `auMTContract` recorder slot | LOCKED | `mapping(address => mapping(address => uint256)) userLP` + `userRewardDebt` keyed `(pool, user)`; single-snapshot MasterChef variant — pending = `(poolEffectiveAcc − userRewardDebt) × userLP`; recorder slot `auMTContract` per H-D10 pattern (mutable, zero-address-acceptable as deprecation safety valve); pre-Stage-I posture `auMTContract == address(0)` → `recordDeposit` / `recordWithdrawal` revert `NotAuMTContract`; H1 forward-dep interface boundary holds for Stage I AuMT consumer |
| H-D17 | Permissionless `recordScore(pool)` + `CCBScore` library delegation + EMA read-only + revoked-gauge revert | LOCKED | Confirms H-D5's permissionless `recordScore` vocabulary; revert `NotApproved(pool)` on `!_gaugeRegistry.isGaugeApproved(pool)`; runs `_accrueGlobal` then `_settlePool` before mutating score; `newScore = CCBScore.score(_emaSampler.tvlEMA(pool), _ccbMultiplier.getMultiplier(pool))` via F-D10 library; F-D22 EMA read-only — `tvlEMA` read, never `updateEMA` called; F12 signed-delta middleware applied to `totalScore`; `ScoreUpdated(pool, oldScore, newScore)` emit |
| H-D18 | In-distributor F-3 / F-7 share computation; F-3 α-blend + F-7 Incendiary skim wired at H5 | LOCKED | Share computation locus inside `EmissionDistributor` itself, not a separate resolver contract; H4 close exposes `poolScore` + `totalScore` invariants with implicit `_settlePool` math; H5 wires F-3 α-blend + F-7 Incendiary skim into `_lpTrancheEmission(block.number)` via `IAuMM.blockEmissionRate` + F-0 Bodensee subtraction + H-D6 1/28 literal + F-7 `IIncendiaryRegistry.activeBoostClaims` (H7 forward-dep stub); H4 stub returns full block emission; `CCBShare.shares()` NOT used in runtime (anti-enumeration per H-D5) |
| H-D19 | Absolute `newScore` external ABI + internal F12 signed-delta `totalScore` middleware | LOCKED | `recordScore(address pool)` takes no score param; distributor computes absolute newScore internally per H-D17; F12 pattern — `int256 delta = int256(newScore) − int256(oldScore); totalScore = _applySignedDelta(totalScore, delta);` mirrors Stage F production CCBMultiplier and test-side F13 `_applySignedDelta` helper; SafeCast guards `int256 ↔ uint256` transitions; single underflow-safe site keeps F12 discipline auditable; H-D17 revoked-gauge revert prevents `totalScore` corruption |
| H-D20 | `IAuMM.mint` at claim time + H-D7 deployment timing independent + `recordEmissions` push locus delegated to H-D23 | LOCKED | `claim(address pool, address to)` runs `_accrueGlobal` → `_settlePool(pool)` → `_settleUser(pool, msg.sender)` → `IAuMM.mint(to, amount)` if `amount > 0`; zero-amount short-circuit; mirrors BodenseeBootstrapChannel mint-at-distribute (H-D11) — mint authority is the scarce resource; H-D7 governs WHEN `setMinter(distributor)` fires at deploy (H10) — H4 codes against distributor-as-minter pre-handoff (reverts `NotMinter`); `recordEmissions` push NOT at claim mint per H-D23 (allocation-side, not mint-side) |
| H-D21 | Lazy accrual tick on mutating entry + H4 LP-tranche stub returning full block emission | LOCKED | Every mutating external entry runs `_accrueGlobal()` first; mirrors BodenseeBootstrapChannel lazy-accrual (H-D11); `if (block.number == lastAccrualBlock) return;` empty-interval short-circuit; `accRewardPerScoreUnit += (rate × Δblocks).divDown(totalScore)` when `totalScore > 0`; H4 `_lpTrancheEmission(uint256) internal view returns (uint256)` is a STUB returning `IAuMM.blockEmissionRate(block_)` (full block emission, no F-0/F-3/F-7 phase subtractions); H5 wires schedule logic; no `selfdestruct` / `disable` / `pause` per H-D13 precedent |
| H-D22 | Two-tier storage layout: global + per-pool + per-user tiers + immutables | LOCKED | Global tier — `accRewardPerScoreUnit`, `totalScore`, `lastAccrualBlock`; per-pool tier — `poolScore[pool]`, `poolAccDebt[pool]`, `poolTotalLP[pool]`; per-user tier — `userLP[pool][user]`, `userRewardDebt[pool][user]`; governance slots — mutable `governance` + `auMTContract`; immutables — `AuMM`, `_gaugeRegistry`, `_emaSampler`, `_ccbMultiplier`, `_efficiencyOracle` (in-place extended at H4.1.x-bis sibling sub-step per G16 inheritor enumeration discipline), `GENESIS_BLOCK`; no transient storage at H4 (no Vault.unlock callback) |
| H-D23 | Accrual-time `recordEmissions` push at `_settlePool` boundary + allocation-side F-10 semantics | LOCKED | Push locus at `_settlePool(pool)` (NOT at `IAuMM.mint` claim site) — F-10 efficiency denominator feeds allocation-side data, not mint-side; push site AFTER `_accrueGlobal()` BEFORE `poolAccDebt[pool]` write; amount `poolAllocation = (acc − poolAccDebt).mulDown(poolScore)`; zero-skip when `poolAllocation == 0`; semantic lock — `recordEmissions` feeds allocation-side; Σ allocation ≥ Σ minted; stranded allocation `poolScore > 0 ∧ poolTotalLP == 0` is intentional tournament signal; no double-push; claim does NOT add second push per H-D20; pre-Stage-I `recordScore` is the live mutator keeping F-10 denominator flowing |
| H-D24 | Per-pool `poolAccRewardPerLP` accumulator slot (FixedPoint AuMM-wei-per-LP-unit) + `_settlePool` per-LP-unit accumulation step | LOCKED | Closes per-LP-unit derivation gap H-D16's `(poolEffectiveAccPerLPunit − userRewardDebt) × userLP` formula leaves unresolved against H-D15's settle math — fourth per-pool tier slot extending H-D22 in-place at H4.5g; `_settlePool` body retrofits with `uint256 totalLP = poolTotalLP[pool]; if (totalLP > 0) { poolAccRewardPerLP[pool] += poolAllocation.divDown(totalLP); }` inside the existing H-D23 `poolAllocation > 0` zero-skip, sandwiched between EfficiencyOracle push (H-D23 first) and `poolAccDebt = acc` rebase (last); inner `totalLP > 0` guard prevents divide-by-zero — allocation still pushes to EfficiencyOracle for F-10 accounting but no per-LP-unit accrual (MasterChef stranded-allocation semantic per H-D23); `IEmissionDistributor` extends in-place at H4.5f to add `poolAccRewardPerLP(address)` external view; `userRewardDebt[pool][user]` stores snapshot of `poolAccRewardPerLP[pool]` at user-settle time; `pendingClaim` derives `(poolAccRewardPerLP[pool] − userRewardDebt[pool][user]).mulDown(userLP[pool][user])`; additive — H-D15/H-D16/H-D22/H-D23 bodies remain LOCKED |
| H-D25 | Per-user `pendingBalance` crystallized-claim slot + `recordDeposit` / `recordWithdrawal` settle pattern | LOCKED | Closes per-user crystallized-pending derivation gap H-D16 leaves under H-D20's `IAuMM.mint`-at-`claim`-only lock — Sushi MasterChef V2 auto-claim-at-deposit unavailable, so pre-deposit pending allocation cannot be erased when `userLP` increments and `userRewardDebt` rebases; third per-user tier slot extending H-D22 in-place at H4.6a; `recordDeposit(pool, user, amount)` body per H-D16 / H-D21 / H-D25: revert if `msg.sender != auMTContract`; `_accrueGlobal` → `_settlePool(pool)` → `acc = poolAccRewardPerLP[pool]` → `pending = (acc − userRewardDebt).mulDown(userLP)` (pre-mutation `userLP`) → zero-skip `pendingBalance += pending` crystallization → `userLP += amount` + `poolTotalLP += amount` → `userRewardDebt = acc` rebase → emit `DepositRecorded`; `recordWithdrawal` symmetric (decrement); `claim` settles identically then `amount = pendingBalance + (acc − userRewardDebt).mulDown(userLP)` additive form, zero-skips, zeros `pendingBalance`, rebases `userRewardDebt`, mints, emits; `pendingClaim(pool, user)` binds to same additive form; `IEmissionDistributor` extends in-place at H4.5h to add `pendingBalance(address, address)` external view; additive — H-D15/H-D16/H-D20/H-D22/H-D24 bodies remain LOCKED |
| H-D26 | `_lpTrancheIntegral(from, to)` replaces single-block sampling in `_accrueGlobal` for conservation-correct interval integration | LOCKED | Closes discretization-bias gap H-D21's single-block-sample × deltaBlocks accrual leaves unresolved against F-0 piecewise-linear `bodensee_share(b)` + era-halving piecewise-constant `blockEmissionRate(b)`; replaces `_accrueGlobal`'s snapshot read with `function _lpTrancheIntegral(uint256 from, uint256 to) internal view returns (uint256)` splitting at three boundary classes (F-0 piecewise month6End/month10End + F-3/F-7 phase year1End + era-halving nthHalvingBlock); constant `blockEmissionRate(sub_from)` per sub-interval + per-phase LP-share factor via AP closed-form (H-D27 bootstrap) or `rate × n` (H-D28 transition / H-D29 continuous); `_lpTrancheEmission(block_)` view RETAINED as external view for tests/UI but no longer read in production accrual; H-D21 LOCKED for H4 historical posture; H-D26 supersedes for H5 production |
| H-D27 | Bootstrap leg AP integration mirrors `BodenseeBootstrapChannel._apSum` bit-for-bit; split at month6End per F-0 piecewise boundary | LOCKED | F-0 bootstrap-window sub-interval body per H-D26; for `[sub_from, sub_to]` with `sub_to ≤ month10EndBlock` the `lp_apsum = rate × n − bodensee_apsum` identity enforces F-0 conservation against `BodenseeBootstrapChannel.accrue`; bodensee_apsum matches `_apSum` at `src/emission/BodenseeBootstrapChannel.sol` L218-L226 bit-for-bit `((first + last) × n × rate) / (2 × 1e18)` with Bootstrap A (anchorStart=genesis, anchorEnd=month6End, startShare=8e17, dropShare=3e17) + Bootstrap B (anchorStart=month6End, anchorEnd=month10End, startShare=5e17, dropShare=5e17); cross-month6End sub-intervals split into A-leg + B-leg; `rate = IAuMM.blockEmissionRate(sub_from)` exact within Era 0 enclosure per OQ-3; `rate × n − bodensee_apsum` form preserves literal arithmetic identity (one fewer rounding-mode divergence risk under FixedPoint mulDown/divDown); private `_bootstrapApSum` helper — formula duplicated bit-for-bit per H-D2 channel-distributor separation (no upstream call) |
| H-D28 | Transition leg (Months 11—12, `month10End < block ≤ year1End`) returns `rate × n` with no LP-tranche subtraction | LOCKED | F-3 transition-phase sub-interval body per H-D26; LP-tranche contribution = `rate × (sub_to − sub_from + 1)` with `rate = IAuMM.blockEmissionRate(sub_from)`; per F-0 bootstrap share permanently zero after month10End + per F-7 Incendiary activates only post-year1End; F-3 α-blend formula `share_i(block) = (1 − α) × (1/28) + α × CCB_share_i(block)` governs PER-POOL share assignment within LP tranche NOT global LP-tranche emission per canonical F-3 domain note "During Months 11–12, bodensee_share = 0 — LP tranche equals full block emission before Incendiary"; α-blend lives in `recordScore` middleware per H-D18 at H5.3 not in `_lpTrancheIntegral`; transition leg degenerate (integrand constant — Era 0 encloses year1End per OQ-5 `year1End ≈ 2.628M < 10.5M = BLOCKS_PER_ERA`); H-D28 governs only global LP-tranche integral seen by `_accrueGlobal` |
| H-D29 | Continuous leg (Year 2+, `block > year1End`) F-7 Step 1 Incendiary skim — mutable governance-settable `incendiaryRegistry` slot + interval-form `IIncendiaryRegistry.integratedSkim(from, to)` interface; Stage H stub returns 0 | LOCKED | F-7 continuous-phase sub-interval body + Stage L forward-dep wiring per H-D26; contribution = `rate × n − Incendiary_integral(sub_from, sub_to)`; Stage H ship Incendiary contract does NOT exist — `_lpTrancheIntegral` returns `rate × n` with `Incendiary_integral = 0` forward-dep stub; slot decision (max-security + easy-to-manage): `address public incendiaryRegistry` defaults `address(0)`, `setIncendiaryRegistry(address) onlyGovernance` accepts `address(0)` as deprecation safety valve per H-D10 pattern, NOT one-shot per H-D7 (Incendiary lacks scarcest-resource justification — F-7 reallocation-not-new-inflation invariant bounds rug-pull surface, safety valve preserves LP-safe fallback); interface decision (security + program): interval-form `integratedSkim(from, to) external view returns (uint256)` rather than per-block `claimsAt(block)` — interval form shifts O(deltaBlocks) iteration to registry (epoch-bucketed cumulative sums internal via permissionless `crystallize` registry-side helper) eliminating unbounded-gas vector against distributor + symmetric composition with `_lpTrancheIntegral` `(from, to) → uint256` shape; NO try/catch wrap — reverting registry reverts accrual (conservative against silent skim-suppression past 21M cap); H-D3 scope clarified — `BLOCKS_PER_EPOCH` applies to F-7 Step 2 CCB-weighted POOL share assignment NOT Step 1 per-block skim; defense-in-depth skim cap deferred as optional H5.4 review; H10 deployment defaults `address(0)`; Stage L deploys `IIncendiaryRegistry` under `src/incendiary/` + governance `setIncendiaryRegistry(deployedRegistry)` once via multisig |
| H-D30 | Era-boundary splitting in `_lpTrancheIntegral`: sub-intervals split at `nthHalvingBlock(g, k)` for each spanned k ≥ 1; constant `blockEmissionRate(sub_from)` per sub-interval | LOCKED | `IAuMM.blockEmissionRate(b) = GENESIS_RATE >> eraIndex(b, GENESIS_BLOCK)` piecewise-constant across `BLOCKS_PER_ERA` boundaries per OQ-5 / §xxix; snapshot `rate = blockEmissionRate(to)` once across a halving-spanning interval undercounts LP-tranche by up to 50% across pre-halving portion violating H-D26 conservation invariant; resolution: while-loop cursor splits at every spanned `nthHalvingBlock(g, k)` computing each era-sub-interval with constant `rate = blockEmissionRate(era_sub_from)`; algorithm — `cursor = from; while (cursor ≤ to) { era = eraIndex(GENESIS_BLOCK, cursor); eraEnd = nthHalvingBlock(GENESIS_BLOCK, era + 1) − 1; subTo = min(eraEnd, to); contribution += _phaseAwareBody(cursor, subTo, blockEmissionRate(cursor)); cursor = subTo + 1; }`; `_phaseAwareBody` dispatches to H-D27/H-D28/H-D29 + nests phase splitting inside era loop iteration; Era 0 encloses bootstrap (month10End < 10.5M) + transition (year1End < 10.5M) per OQ-3/OQ-5 — era-splitting effectively no-op gas-wise for first ~3.5 years post-genesis; loop unbounded in principle but bounded in practice (active pools recent `lastAccrualBlock`; dormant O(eras_since_genesis) = O(years/4) over protocol lifetime); H5.4 gas-cost analysis confirms sub-O(eras) iteration count never breaches block gas limits at reasonable era counts |
| H-D31 | `recordScore` H5.3 reshape: storage extensions + `IMiliariumRegistry` 6th immutable + 10-step flow order | LOCKED | Adds `uint256 public f5Total` + `mapping(address => uint256) public f5Score` for raw F-5 signed-delta accounting; repurposes `poolScore[pool]` to hold reshaped effective score per H-D33; sixth constructor immutable `IMiliariumRegistry _miliariumRegistry` with `ZeroAddress` guard; 10-step `recordScore` flow — gauge gate `NotApproved` → `_accrueGlobal` → `_settlePool` → `CCBScore.score` → signed-delta `f5Total` against old `f5Score[pool]` → write `f5Score[pool]` → `_alphaF3(block.number)` snapshot → H-D33 reshape branch on `_miliariumRegistry.isMiliarium(pool)` → signed-delta `totalScore` against old `poolScore[pool]` → write `poolScore[pool]` + emit `ScoreUpdated`; G16 inheritor sweep at H5.3c wires `MockMiliariumRegistry` into all 8 constructor sites in test/unit/EmissionDistributor.t.sol |
| H-D32 | `_alphaF3(uint256 block_) private view returns (uint256)`: linear F-3 α blend factor with endpoint clamping | LOCKED | Linear α evolves 0 → 1e18 across the transition window `(month10EndBlock(GENESIS_BLOCK), year1EndBlock(GENESIS_BLOCK)]` per `11_formulas.md` F-3; lower clamp `block_ ≤ m10` returns 0, upper clamp `block_ ≥ y1` returns 1e18, interior `(block_ - m10 - 1) * 1e18 / (y1 - m10 - 1)` with 437_999-block denominator per OQ-3 + OQ-5 canonical constants; plain integer arithmetic — no FixedPoint library call needed; branchlessly unifies bootstrap (α=0 → H-D33 collapses to F-1 equal-split), F-3 transition (interior α), and continuous (α=1e18 → H-D33 collapses to F-7 Step 4 individual CCB) regimes at the reshape level |
| H-D33 | Effective score reshape formula: Miliarium pool F-3 equal-split blend vs non-Miliarium α-CCB only; Option A — non-Miliarium joins α-CCB leg during F-3 | LOCKED | Miliarium pool reshape `effective_Mil = (1e18 - alpha).mulDown(f5Total / 28) + alpha.mulDown(score_F5_new)`; non-Miliarium pool reshape `effective_non_Mil = alpha.mulDown(score_F5_new)` per Option A; at α=0 collapses to F-1 equal-split (Mil: `f5Total / 28` per H-D6 1/28 literal; non-Mil: 0); at α=1e18 collapses to F-7 Step 4 individual CCB (both: `score_F5_new`); canonical source `10_constitution.md §xxviii` includes non-Miliarium in the F-3 α-CCB leg, superseding `03_theoretical_foundation.md` where the two conflict; global conservation `Σ effective_i = f5Total` modulo 1/28 integer-division floor with 28 Miliarium pools gauged; Q9 deployment-invariant — governance gauges every Miliarium pool at slot creation, `NotApproved` gate at step 1 of `recordScore` prevents ungauged pools reaching the reshape branch |
| H-D34 | Test-side observation surface for `_lpTrancheIntegral` + `_lpTrancheEmission` via Harness contract pattern at `test/unit/harness/EmissionDistributorHarness.sol`; preserves prod ABI minimality | LOCKED | Test-only contract `is EmissionDistributor` with two public-view pass-through wrappers `extLpTrancheIntegral(uint256 from, uint256 to) external view returns (uint256)` + `extLpTrancheEmission(uint256 block_) external view returns (uint256)`; constructor pass-through all eight `EmissionDistributor` arguments (AuMM, gaugeRegistry, emaSampler, ccbMultiplier, efficiencyOracle, miliariumRegistry, genesisBlock, initialGovernance); no additional state, no override of parent behavior — `Harness is-a EmissionDistributor` preserves existing 67-test cohort through inheritance; H5.4.0e setUp retyping `distributor` slot from `EmissionDistributor` to `EmissionDistributorHarness`; preserves prod ABI minimality per Stage K governance handoff + Stage R formal verification constraints; H-D26 NatSpec drift at `src/emission/EmissionDistributor.sol` L303 + L328 (calls `_lpTrancheEmission` "external view for tests / UI" when actual signature is `internal view`) deferred to H5.5 close-of-family polish; G16 inheritor enumeration applies prospectively |
| H-D35 | In-place extension of Stage G `src/token/IAuMT.sol` — Stage I receipt-token consumer-side documentary additions (`mint`, `burn`, `distributor`, `pool`); G-D9 `governanceWeight(address)` preserved verbatim; distributor recorder slot remains bare `address` per H-D16 | LOCKED | Amended at H6.0c (`856a0f4`) — original H6.0a framing as "first file under src/token/" was factually wrong; `src/token/IAuMT.sol` already exists from Stage G per G-DP4 + G-D9 as forward-dep stub for `VaultClassRegistry.vetoProposal` veto-threshold semantic; in-place extension preserves `is IERC20` + `governanceWeight(address holder) external view returns (uint256)` consumed at `src/gauge/VaultClassRegistry.sol:277` via `auMT = IAuMT(auMT_)` L211; adds `mint(address to, uint256 amount) external` + `burn(address from, uint256 amount) external` + `distributor() external view returns (address)` + `pool() external view returns (address)`; G16 inheritor sweep REQUIRED at H6.2a — Stage G shipped two `is IAuMT` inheritors (`test/unit/VaultClassRegistry.t.sol:29` canonical + `test/fork/mocks/StageGMocks.sol:34` mirror with `test/fork/mocks/StageGMocks.sol:L10` propagation NOTE) both extended with stub bodies for the 4 new functions in the same commit (return 0 for `uint256` returns; return `address(0)` for `address` returns); H6.2b `forge test --match-path test/unit/VaultClassRegistry.t.sol` regression-checks Stage G veto cohort; deferred to Stage I — events, errors, setter signatures, qualification-period / withdrawal-reset semantics; H6 ships minimum compile-stable surface plus G-D9 preservation, no Stage I behavior |
| H-D36 | Real-AuMM integration test family at `test/unit/AuMMDistributorIntegration.t.sol` — Scope A integration coverage with 4-test cohort exercising mock/real `setMinter` parity + halving | LOCKED | 4 test names: `test_SetMinterHandoff_EmitsMinterSet`, `test_RevertWhen_SetMinterCalledTwice`, `test_ClaimMintsRealAuMM_SinglePool`, `test_ClaimAcrossHalvingBoundary_UsesRealBlockEmissionRate`; setUp wires `IAuMM(address(new AuMM(GENESIS_BLOCK_, address(this))))` per C-D11 two-flag-lock; `test_RevertWhen_SetMinterCalledTwice`: `_minterAdmin` zeroed on first `setMinter` success → second call reverts `NotMinterAdmin` not `MinterAlreadySet` (C-D11 semantics); `test_ClaimAcrossHalvingBoundary` uses `vm.roll` past `year1EndBlock(GENESIS_BLOCK_)` for α=1e18 continuous regime + `vm.roll` past `AureumTime.firstHalvingBlock(GENESIS_BLOCK_)+1` to assert `_lpTrancheIntegral` cursor walk per H-D30 snapshots `GENESIS_RATE >> eraIndex` halved rate `5e17` wei/block at Era 1 boundary; `distributor` slot typed `EmissionDistributorHarness` per H-D34; out of scope — H9 fork integration, full Stage E pilots, OQ-7 OOG ceiling stress |
| H-D37 | H9 fork-test mock swap-out matrix + `StageHIntegrationFixture is StageGIntegrationFixture` inheritance from Stage G fixture | LOCKED amended at H9.0c | Real-contract roster (deployed in `setUp` from `src/`): Vault scaffold + factory + fee controller + fee routing hook (Stage D — reused via `super.setUp()`); AuMM (C); EMASampler + CCBMultiplier (F); GaugeRegistry + VaultClassRegistry + GaugeEligibility + SwapAndDepositToBodensee (G); TVLOracle (H2a — replaces Stage G's `MockTVLOracle`); EfficiencyOracle (H2b — replaces Stage G's `MockEfficiencyOracle`); BodenseeBootstrapChannel (H3); EmissionDistributor (H4—H6); `MockMiliariumRegistry` retained (F-D9 Stage J forward-dep) with `isMiliarium(pool)=true` for 3 pilots; AuMT direct impersonation via `setAuMTContract(address(this))` per H6.3 precedent — no mock token layer; `incendiaryRegistry` stays `address(0)` per H-D29 zero-stub; pilot roster ixHelvetia/ixEdelweiss/ixAurebit slots 01/05/14; `super.setUp()` inheritance reuses Stage G CREATE3 vault salt + Bodensee BPT init (D32 β-pattern) + ixPilot deployment + `svZCHF`/`sUSDS` env-reads + governance multisig keccak + `SwapAndDepositToBodensee` hook registration; chain extension at setUp override + `efficiencyOracle.setEmissionsRecorder(address(emissionDistributor))` per H-D23 deploy prerequisite + `emissionDistributor.setAuMTContract(address(this))` per H6.3 recorder impersonation precedent; minter-slot wiring (`aumm.setMinter`) deferred to each derived contract's setUp override per Option A amendment at H9.0c — original H9.0a framing of `aumm.setMinter(address(emissionDistributor))` in fixture setUp would have conflicted with `StageHBootstrapPhaseTest` needing `bootstrapChannel` as minter per H-D39 full distribute() arc against the shared Bodensee-pool AuMM token (Bodensee pool tokens include AuMM so a second AuMM instance is not viable); each derived test sets its own minter (StageHBootstrapPhaseTest → bootstrapChannel; StageHContinuousPhaseTest/StageHHalvingBoundaryTest/StageHCrossStackTest → emissionDistributor); exercises Stage H deploy path without locking governance-migration timing (H-D7 OPEN); E10 `deal(adjust=false)` rule applies to any `deal()` call introduced at H9 |
| H-D38 | H9 test cohort breakdown: four derived contracts per emission-schedule phase | LOCKED | (1) `StageHBootstrapPhaseTest` — F-0+F-1 phase (accrue cadence Bootstrap A 80→50% + B 50→0% per H-D11 AP closed-form + `pendingAccrual + totalDistributed = Σ_accrued` invariant + distribute() H-D39 arc + sad-path pair); (2) `StageHContinuousPhaseTest` — F-7 phase (`vm.roll` past `year1EndBlock(GENESIS_BLOCK_)` from setUp so α=1e18; 3-pilot `recordScore` + `recordDeposit` + `claim` → assert `aumm.balanceOf(to) > 0` + `Claimed` event + bounded conservation `\|Σ_i pendingClaim_i − lpTrancheEmission(span)\| ≤ N_gauged wei`; assumes `incendiaryRegistry = address(0)` zero-stub per H-D29; N_gauged=3); (3) `StageHHalvingBoundaryTest` — H-D30 era cursor walk against real Vault state (`vm.roll` straddle `AureumTime.nthHalvingBlock(GENESIS_BLOCK_, 1)` 20-block window → claim → assert `blockEmissionRate(boundary) == 5e17` AND claim amount = cursor-walked `1e18 × n_pre + 5e17 × n_post` per H6.3c arithmetic against real Vault reserve state); (4) `StageHCrossStackTest` — full cross-stack wiring (gauge approval via `VaultClassRegistry` → real `EMASampler` TVL EMA push via `SwapAndDepositToBodensee` swap → `TVLOracle.tvl(pool)` → `EfficiencyOracle.efficiencyInputs(pool)` → `recordScore` F-3/F-7 reshape per H-D33 → `recordDeposit` → `claim` → `aumm.balanceOf(to) > 0`); 12—20 tests across 4 contracts + fixture; 800—1200 lines; `EmissionDistributor` used directly (Harness is test-unit only per H-D34) |
| H-D39 | `BodenseeBootstrapChannel.distribute()` full `IVault.unlock` callback exercise against mainnet Balancer V3 Vault at `0xbA1333...` | LOCKED | `StageHBootstrapPhaseTest.test_Distribute_*` arc — primary H9 validation differentiator vs H3 unit cohort (which used `MockVault`); test arc (a) `vm.roll` bootstrap window (b) `accrue()` populates `pendingAccrual` (c) snapshot Bodensee AuMM reserve via `vault.getPoolTokenInfo(bodenseePool)` (d) `vm.prank(GOVERNANCE_MULTISIG); distribute()` → real `IVault.unlock` → `_distributeCallback` → `IERC20(aumm).safeTransfer(vault, pendingAmount)` → `vault.settle(aumm)` → `vault.addLiquidity(..., AddLiquidityKind.DONATION, ...)`; post-assertions Bodensee AuMM reserve += pendingAmount + BPT supply unchanged (DONATION per D32) + `Distributed(GOVERNANCE_MULTISIG, pendingAmount)` event + `pendingAccrual == 0` + `totalDistributed += pendingAmount` + transient `_distributeCallback` slots cleared; sad-path pair `test_RevertWhen_DistributeCalledByNonGovernance` (`vm.prank(attacker); vm.expectRevert(NotGovernance.selector)`) + `test_RevertWhen_DistributeWithNoPendingAccrual` (fresh genesis + `vm.expectRevert(NoPendingAccrual.selector)`); closes H3.7e `MockVault` validation gap — identical path against byte-identical-to-mainnet Balancer V3 Vault bytecode + real reentrancy guards + real transient storage + real settle accounting + real AuMM reserve tracking; `abi.decode(result, (uint256))` per D22 inner-callback contract |
| H-D40 | H9 fork-test invocation: D35 split-form + D36 `--threads 1` belt + Ankr RPC carry-forward + block-pin posture | LOCKED | Canonical command `forge test --match-path "test/fork/StageHIntegration.t.sol" --fork-url $MAINNET_RPC_URL --threads 1 -vv`; `--match-path` per D35 restricts fork context to H9 file only (bypasses other 6 fork files' Ankr rate-limit exposure; avoids D35 global-fork indefinite-hang pattern at 25+ min on free-tier); `--threads 1` per D36+F-D11 prevents `vm.setEnv` races (H9 introduces no new race surfaces — all Stage H deployments in-process via `new` constructors in `setUp()`, not env-address handoffs); H8 unit-cohort split-form Part A is symmetric counterpart, H9 is Part B; block-pin posture enter without `--fork-block-number` (Ankr serves head; live Bodensee reserve + EMA state); if state-drift flake surfaces across re-runs lock via `vm.envOr("FORK_BLOCK_NUMBER", uint256(0))` env-read in `StageHIntegrationFixture.setUp()` per Stage E `PilotPools.t.sol` + Stage G `StageGIntegration.t.sol` precedent and document pin block in later H9.x sub-step; permanent D36 env-key namespacing fix remains deferred per §11 carry-forward |
| H-D41 | Multi-minter architecture — BodenseeBootstrapChannel.distribute() (H-D11) + EmissionDistributor.claim() (H-D20) both require IAuMM.mint; AuMM C-D11 one-shot minter slot | DEFERRED-TO-STAGE-K | Stage K Option A (EmissionDistributor mintForBootstrap path) or Option B (AuMMMinterRouter); Stage H ships Option C single-minter deferral per H-D7 |
| H-D42 | AuMM deployed as Stage H input rather than Stage H output; DeployStageH.s.sol consumes AUMM env per DeployDerBodensee.s.sol L39 convention | LOCKED | Chicken-and-egg surfaced at H10.2 first execution — revert `call to non-contract address 0x6076…DFFb` (VAULT placeholder) triggered by BodenseeBootstrapChannel constructor `_vault.getPoolTokens(BODENSEE_POOL)` AuMM-index lookup per H-D12; resolution mirrors `script/DeployDerBodensee.s.sol:L39` `address aumm = vm.envAddress("AUMM");` precedent (canonical convention for downstream scripts referencing BODENSEE_POOL); H-D7 Option C invariant `_minterAdmin = GOVERNANCE_MULTISIG` becomes input precondition asserted via `MinterNotZero` rather than enforced by construction; production chain implies separate `script/DeployAuMM.s.sol` micro-script before DeployStageH (deferred to later H-x or Stage K opening); H10.1-fix1 revises script (remove `new AuMM` Step 1; add env-read; derive GENESIS_BLOCK from `aumm.GENESIS_BLOCK()`; preserve `aumm` public slot + `MinterNotZero` assertion); H10.2-revised test inherits StageGIntegrationFixture (provides Vault + AuMM + Bodensee + gauge stack) |

## Completion Log

| Sub-step | Commit | Date | Notes |
|----------|--------|------|-------|
| H0.1 | 86d166c | 2026-05-14 | docs/STAGE_H_PLAN.md scaffold created (header + Goal + Dependencies + Sub-step skeleton + H-D Decisions table H-D1—H-D7 + Completion Log header) |
| H0.2 | 610b07e | 2026-05-14 | docs/STAGE_H_NOTES.md scaffold created (status header + organization rubric + H-D1—H-D5 LOCKED bodies + H-D6/H-D7 OPEN placeholders + findings queue + open questions queue) |
| H0.3 | d8d67d4 | 2026-05-14 | docs/STAGE_H_PLAN.md — Completion Log H0.1 hash fill + H0.2 row added |
| H1.1 | b6f37d2 | 2026-05-14 | docs/STAGE_H_NOTES.md — ## Architecture section (new contracts/interfaces inventory + consumer surface + schedules-by-phase table) |
| H1.2 | 5f36a89 | 2026-05-14 | docs/STAGE_H_NOTES.md — ## Interface boundary section (Produced + Consumed + Forward-dependency stubs) |
| H1.3 | 87b62a7 | 2026-05-14 | docs/STAGE_H_NOTES.md — H-D6 OPEN → LOCKED (1/28 literal per F-1; supply-deflationary by design; H-D6 Open-questions bullet removed) |
| H1.4 | d992f68 | 2026-05-14 | docs/STAGE_H_PLAN.md + docs/STAGE_H_NOTES.md — H-D6 row LOCKED + Completion Log rows H0.3/H1.1—H1.3 + NOTES status header refresh |
| H1.x-bis | 3d39978 | 2026-05-15 | src/ccb/ICCBMultiplier.sol — ICCBMultiplier interface retrofit RB-005 (getMultiplier per OQ-23) |
| H1.5 | 4e25a57 | 2026-05-15 | docs/STAGE_H_NOTES.md — H-D8 LOCKED (constellation enumeration source for OQ-22 cross-asset leg) |
| H1.6 | b0403b5 | 2026-05-15 | docs/STAGE_H_PLAN.md — status header refresh (H-D8 LOCKED) + Completion Log rows H1.4/H1.x-bis/H1.5 |
| H1.7 | 20614e3 | 2026-05-15 | docs/STAGE_H_NOTES.md — H-D9 LOCKED (TVLOracle arithmetic: balancesLiveScaled18 step 1, tokenToUnderlying map, balance-ratio α step 2, Phase 1 direct-venues-only + OQ-22 L1115 carry-forward) |
| H1.8 | 342932e | 2026-05-15 | docs/STAGE_H_PLAN.md — status header refresh (H-D9 LOCKED) + Completion Log rows H1.6/H1.7 |
| H2a.1 | e48a61c | 2026-05-15 | src/emission/TVLOracle.sol — skeleton scaffold (abstract contract, storage layout per H-D8 + H-D9, IVaultExplorer + ITVLOracle imports) |
| H2a.2 | 6f157f7 | 2026-05-15 | src/emission/TVLOracle.sol — constructor (IVaultExplorer + BODENSEE_POOL + governance init, ZeroAddress guard) |
| H2a.3 | 685a81e | 2026-05-15 | src/emission/TVLOracle.sol — tokenToUnderlying seed loop (2 new constructor params, ArrayLengthMismatch, genesis mapping loop) |
| H2a.4 | 321c8c7 | 2026-05-15 | src/emission/TVLOracle.sol — governance setter (NotGovernance error, GovernanceTransferred event, onlyGovernance modifier, setGovernanceContract) |
| H2a.5 | 1db1085 | 2026-05-15 | src/emission/TVLOracle.sol — addConstellationPool (AlreadyAdded error, ConstellationPoolAdded event, IERC20 import, _underlyingToPools incremental append) |
| H2a.6 | 8b69cbb | 2026-05-15 | src/emission/TVLOracle.sol — setTokenUnderlying (TokenUnderlyingSet event, Token underlying management section, reverse-map staleness NatSpec) |
| H2a.7a | e4a9eee | 2026-05-15 | src/emission/TVLOracle.sol — SVZCHF immutable + constructor param (_svzchf, ZeroAddress guard, assignment, @param NatSpec) |
| H2a.7b | cdb94f8 | 2026-05-15 | src/emission/TVLOracle.sol — _constellationRatio helper (H-D9 Step 2 balance-ratio averaging, PoolData import, Internal helpers section) |
| H2a.7c | 00eb759 | 2026-05-15 | src/emission/TVLOracle.sol — concrete tvl() body (H-D9 Step 1+2), abstract keyword removal, title + @dev NatSpec fix |
| H2a.8a | 21d5734 | 2026-05-15 | test/fork/mocks/StageHMocks.sol — MockVaultExplorer (minimal IVaultExplorer stub: setPool, getPoolTokens, getPoolData, LengthMismatch) |
| H2a.8b | 82f6b4f | 2026-05-15 | test/unit/TVLOracle.t.sol — scaffold (imports, TVLOracleTest, setUp deploying TVLOracle + MockVaultExplorer cast, _addr helper; no test functions) |
| H2a.8c | b5da9e2 | 2026-05-15 | test/unit/TVLOracle.t.sol — constructor revert + immutable wiring + seed-map + setGovernanceContract tests (11 tests, 11 pass) |
| H2a.8d | 2b7ca03 | 2026-05-15 | test/unit/TVLOracle.t.sol — addConstellationPool + setTokenUnderlying tests (9 tests; 20 total, 20 pass) |
| H2a.8e1 | 0f281df | 2026-05-15 | test/unit/TVLOracle.t.sol — tvl() helpers (_mapToken, _setComposition, _addVenue) + 4 edge-case tests (empty pool, unmapped, no venue, svZCHF identity; 24 total, 24 pass) |
| H2a.8e2 | 4a7f400 | 2026-05-15 | test/unit/TVLOracle.t.sol — tvl() arithmetic tests: single-venue ratio, two-venue avg, multi-token per-underlying summing, multi-underlying pool sum (4 tests; 28 total, 28 pass) |
| H2a.9a | 95191c7 | 2026-05-15 | docs/STAGE_H_PLAN.md — status header refresh (H2a complete) + Completion Log rows H1.8 + H2a.1—H2a.7c |
| H2a.9b | c283c2c | 2026-05-15 | docs/STAGE_H_PLAN.md — Last update refresh + Completion Log rows H2a.8a—H2a.8e2 + H2a.9a (H2a family closed) |
| H1.9 | e90e561 | 2026-05-15 | docs/STAGE_H_NOTES.md — H-D10 LOCKED (EfficiencyOracle intra-epoch accumulation + EpochEntry ring buffer + epoch-stamp stale detection + view-side SMA simulation) |
| H2a.10a | 50e7768 | 2026-05-15 | src/emission/TVLOracle.sol — quoteSvZCHF external wrapper (forward-prep H2b handoff primitive per H-D10; single-token mirror of inner tvl() conversion; skip-on-zero semantics) |
| H1.9-bis | d30d12f | 2026-05-16 | docs/STAGE_H_NOTES.md — H-D10 v2 amendment (svZCHF numéraire, quoteSvZCHF push signatures, tvlOracle + AuMM constructor immutables, AuMM mapping prerequisite) |
| H2a.10b | d95da68 | 2026-05-16 | test/unit/TVLOracle.t.sol — quoteSvZCHF unit tests (8 cases: unmapped, mapped-but-no-venue, zero amount, svZCHF identity, single-venue ratio, two-venue averaging, linearity in amount, tvl() inner-loop cross-check; 36/36 pass) |
| H2b.1 | 15fcbc9 | 2026-05-16 | src/emission/EfficiencyOracle.sol — abstract scaffold (ITVLOracle + IEfficiencyOracle imports, EpochEntry struct, 3 immutables + 3 governance/recorder slots + 4 accumulator/history mappings, 4 errors, 6 events; no functions yet per H-D10 per H1.9-bis) |
| H2b.2 | 8986abf | 2026-05-16 | src/emission/EfficiencyOracle.sol — constructor body (4 params: ITVLOracle + AuMM + GENESIS_BLOCK + initial governance; 3 ZeroAddress guards on the 3 address params; feeRecorder + emissionsRecorder default to address(0) per H-D10) |
| H2b.3 | 575109f | 2026-05-16 | src/emission/EfficiencyOracle.sol — onlyGovernance modifier + 3 governance setters (setGovernanceContract rejects zero per TVLOracle precedent; setFeeRecorder + setEmissionsRecorder zero-address-acceptable per H-D10 deprecation safety valve; all emit respective events) |
| H2a.10c | aa43252 | 2026-05-16 | src/ccb/ITVLOracle.sol + src/emission/TVLOracle.sol + 3 MockTVLOracle test doubles — ITVLOracle.quoteSvZCHF interface declaration retrofit + `override` on concrete TVLOracle + return-0 stubs in all 3 MockTVLOracle inheritors (G16 inheritor enumeration discipline); unblocks H2b.4 EfficiencyOracle tvlOracle.quoteSvZCHF call |
| H2b.4 | 31fb72e | 2026-05-16 | src/emission/EfficiencyOracle.sol — AureumTime import + onlyFeeRecorder / onlyEmissionsRecorder modifiers + _ensureCurrentEpoch internal helper (epoch rollover with EpochEntry ring write + EpochFinalized emit) + recordFees / recordEmissions external entry points (quoteSvZCHF conversion + skip-on-zero accumulation + FeesRecorded / EmissionsRecorded emits); contract still abstract pending efficiencyInputs at H2b.5 |
| H2b.5 | 7e0f8f0 | 2026-05-16 | src/emission/EfficiencyOracle.sol — efficiencyInputs view (two-tier lookup: live accumulators tier 1 + EpochEntry ring stamp-match tier 2; early-epoch guard currentEpoch < 3 per H-D10; divide-by-3 SMA; abstract keyword removed — contract now concrete IEfficiencyOracle) |
| H2b.6a | 58dfc19 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — scaffold (MockEfficiencyTVLOracle: settable rate → linear quoteSvZCHF; EfficiencyOracleTest: setUp + 5 helpers; no test functions yet) |
| H2b.6b | daada32 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — constructor tests (3 ZeroAddress reverts + zero-genesis acceptance + immutable wiring w/ default-zero recorders) + setGovernanceContract tests (happy path emit + zero-address revert + nonGov revert + post-handoff access shift); 9 tests, all pass |
| H2b.6c | 7fd27df | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — setFeeRecorder + setEmissionsRecorder tests (happy-path slot + emit, zero-address deprecation acceptance, NotGovernance revert); 15 tests total, all pass |
| H2b.6d | 23ea084 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — onlyFeeRecorder + onlyEmissionsRecorder access-control tests (random caller revert, governance-caller orthogonality revert, deprecation safety-valve revert for each recorder); 21 tests total, all pass |
| H2b.6e | 9921cf1 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — recordFees + recordEmissions mechanics tests (happy-path quoteSvZCHF conversion + emit, unmapped-token zero-svZCHF emit, zero-amount zero-svZCHF emit; emit always fires per H-D10 audit-trail-clarity semantic); 27 tests total, all pass |
| H2b.6f | 851fbc2 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — _ensureCurrentEpoch rollover tests (same-epoch accumulator preservation, post-flush reset, multi-epoch jump single-emit via vm.recordLogs, phantom first-activation emit, per-pool isolation); 32 tests total, all pass |
| H2b.6g | 2300204 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — efficiencyInputs early-epoch guard tests (atEpoch0/1/2 return zero, atEpoch2WithEarlierRecordings proves underflow guard fires before loop) + cold-pool tests (coldPoolAtEpoch3 tier-1 default match contributes 0+0, coldPoolAtLargeEpoch all checks miss); 38 tests total, all pass |
| H2b.6h | 8c87471 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — efficiencyInputs tier-1 live-accumulator lookup tests (tier1AtI1/I2/I3 single-epoch SMA division, multipleFeeTokensSameEpoch accumulator aggregation, emissionsOnly numerator stays zero, tier1Stale fourEpochsLater noMatch); 44 tests total, all pass |
| H2b.6i | 98ea933 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — efficiencyInputs tier-2 history-ring lookup tests (tier2AtI1/I2/I3 single ring-slot hit, allThreeSlots distinctPerEpochValues attribution, tier1AndTier2Mixed full coverage, tier2SurvivesDormancy); 50 tests total, all pass |
| H2b.6j | 3e12d73 | 2026-05-16 | test/unit/EfficiencyOracle.t.sol — efficiencyInputs stale-slot + SMA attenuation tests (singleSlotMismatchedStamp, allSlotsStaleAtFarEpoch, ringOverwriteByNewerEpoch, twoActiveOutOfThree dividedBy3NotBy2, integerDivisionFloors, firstActivationPhantomOverwritten); 56 tests total, all pass |
| H3.0a | 5003853 | 2026-05-18 | docs/STAGE_H_NOTES.md — H-D11—H-D14 BodenseeBootstrapChannel design freeze lock (status header + Last update refresh + H-D11 mint-at-distribute + H-D12 Vault.unlock DONATION + H-D13 lifecycle clamp + H-D14 governance-only distribute; 5 micro-sub-steps α through ε bundled in one commit) |
| H3.0b | ca37986 | 2026-05-18 | docs/STAGE_H_PLAN.md — status header + Last update refresh (H-D11—H-D14) + H-D11—H-D14 Decisions rows + H3.0a Completion Log row |
| H3.1 | 0e8703d | 2026-05-18 | src/emission/IBodenseeBootstrapChannel.sol — interface (2 events Accrued + Distributed; accrue + distribute + pendingAccrual + totalDistributed + lastAccrualBlock; H-D11—H-D14 design-freeze NatSpec) |
| H3.1-amend | 3d0f100 | 2026-05-18 | src/emission/IBodenseeBootstrapChannel.sol — extend Distributed event to Distributed(address indexed governance, uint256 amount); gives _ORIGINAL_CALLER_SLOT consumer per H-D12 3-slot design |
| H3.2 | 0dd460a | 2026-05-18 | src/emission/BodenseeBootstrapChannel.sol — abstract scaffold (9 imports + using directives + 5 immutables + 4 storage slots + 3 transient slot constants + 7 errors + GovernanceTransferred event; no functions yet per H2b.1 precedent) |
| H3.3 | cad6554 | 2026-05-18 | src/emission/BodenseeBootstrapChannel.sol — constructor (5 params: IVault + address bodensee + IAuMM + GENESIS_BLOCK + initial governance; 4 ZeroAddress guards; 6 assignments; _aummIndex resolution via getPoolTokens loop with type(uint8).max sentinel + IndexLookupFailed revert; lastAccrualBlock init to GENESIS_BLOCK per H-D11) |
| H3.4 | 17a93fe | 2026-05-18 | src/emission/BodenseeBootstrapChannel.sol — onlyGovernance modifier + setGovernanceContract (ZeroAddress revert + old/new swap + GovernanceTransferred emit; mirrors TVLOracle / EfficiencyOracle H-D14 pattern) |
| H3.5 | 3096e0a | 2026-05-18 | src/emission/BodenseeBootstrapChannel.sol — accrue() external override + _apSum internal pure helper (H-D11 AP piecewise integration: Bootstrap A 8e17→5e17 + Bootstrap B 5e17→0 closed-form ((first+last)*n*rate)/(2*1e18); H-D13 lifecycle clamp min(block.number,month10End) + empty-interval short-circuit; IAuMM.blockEmissionRate snapshotted once; cross-boundary split at month6End; emits Accrued(from, to, contribution)) |
| H3.6a | 306d43f | 2026-05-18 | src/emission/BodenseeBootstrapChannel.sol — _distributeCallback external (H-D12 inner half: OnlyVault gate + CallbackPayloadMismatch sentinel vs _PENDING_AMOUNT_SLOT + pre/postReserve via _currentReserve(_aummIndex) + IERC20(address(AuMM)).safeTransfer + _vault.settle + addLiquidity DONATION with maxAmountsIn[_aummIndex] + BptMintedOnDonation guard + ReserveDeltaMismatch guard + emit Distributed via _ORIGINAL_CALLER_SLOT.tload()) + _currentReserve(uint8) internal view (balancesRaw[idx] via _vault.getPoolTokenInfo per G-D18) + 3 callback-path errors (ReentrancyGuard + BptMintedOnDonation + ReserveDeltaMismatch); mirrors SwapAndDepositToBodensee L390—L450 with BODENSEE_POOL / _aummIndex / IERC20-cast-AuMM substitutions; contract still abstract pending distribute() at H3.6b |
| H3.6a-RB-005 | a2bdb37 | 2026-05-18 | docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-005 register row for 8 forge-lint unsafe-typecast warnings (GaugeEligibility.t.sol L717/L736/L756 + EfficiencyOracle.t.sol L51 + TVLOracle.t.sol L32; Open, deferred to Stage H close-of-stage sweep) |
| H3.6b | 5c93352 | 2026-05-18 | src/emission/BodenseeBootstrapChannel.sol — distribute() external override + remove abstract (H-D12 outer 10-step: NoPendingAccrual guard + pendingAccrual snapshot + zero + totalDistributed increment + IAuMM.mint + ReentrancyGuard tload + 3 transient slot writes + _vault.unlock + 3 slot clears + HelperBalanceNonZero residual assert; H-D14 onlyGovernance; contract becomes concrete) |
| H3.7a | 32e69a1 | 2026-05-18 | test/unit/BodenseeBootstrapChannel.t.sol — scaffold (MockERC20 + MockAuMM is ERC20,IAuMM + MockVault with reentrancyAttack/corruptCallbackAmount/mintBptOnDonation/bumpReserveOnDonation switches; BodenseeBootstrapChannelTest: GENESIS_BLOCK_=1_000_000 + BODENSEE_POOL_ + GOV constants + setUp wiring vault.setTokens/setBalancesRaw/setHelper + aumm.setMinter(address(channel)) + vm.roll(GENESIS_BLOCK_) + 4 helpers _addr/_rollTo/_month6End/_month10End; no test functions per H2b.6a precedent) |
| H3.7b | e2f4df8 | 2026-05-18 | test/unit/BodenseeBootstrapChannel.t.sol — constructor tests (8): test_Constructor_RevertWhen_VaultZero + BodenseeZero + AummZero + GovernanceZero (ZeroAddress.selector) + RevertWhen_AummNotInBodensee (IndexLookupFailed.selector inline badVault with [svZchf,sUsds,other] roster) + WiresImmutables (BODENSEE_POOL/AuMM/GENESIS_BLOCK asserts) + InitsGovernance + InitsLastAccrualBlock |
| H3.7c | 45bfa88 | 2026-05-18 | test/unit/BodenseeBootstrapChannel.t.sol — setGovernanceContract tests (4): HappyPath_EmitsAndUpdates (vm.expectEmit GovernanceTransferred + governance update) + RevertWhen_NewGovernanceZero (ZeroAddress.selector) + RevertWhen_CallerNotGovernance (NotGovernance.selector encodeWithSelector) + PostHandoffAccessShifts (old-gov locked out, new-gov succeeds) |
| H3.7d | 8242d7a | 2026-05-18 | test/unit/BodenseeBootstrapChannel.t.sol — accrue() tests (8) + _expectedApSum helper + IBodenseeBootstrapChannel import (H3.7d-fix folded: Accrued event referenced via interface qualifier IBodenseeBootstrapChannel.Accrued because solc 0.8.26 does not expose inherited interface events via child-contract qualification): EmptyInterval_NoOp + BootstrapAOnly + BootstrapBOnly + BoundarySpanning + PostMonth10Clamp + SecondCallAfterClampNoOp + SequentialAccumulation_BootstrapA + PermissionlessAndEmits |
| H3.7e | 0fda303 | 2026-05-18 | test/unit/BodenseeBootstrapChannel.t.sol — distribute() tests (5): RevertWhen_NoPendingAccrual + RevertWhen_CallerNotGovernance (NotGovernance.selector encodeWithSelector) + HappyPath_MutatesStateAndEmitsDistributed (enableBumpReserveOnDonation + vm.expectEmit Distributed(GOV,amount) + pendingAccrual=0 + totalDistributed assert) + RevertWhen_HelperBalanceNonZero (deal 50e18 pre-existing residual) + RevertWhen_Reentrant (second channel gov=vault; inner NoPendingAccrual bubbles up; direct ReentrancyGuard.selector test via vm.transientStore deferred) |
| H3.7f | e577db7 | 2026-05-18 | test/unit/BodenseeBootstrapChannel.t.sol — _distributeCallback callback-path error tests (4): NotVault + CallbackPayloadMismatch + BptMintedOnDonation + ReserveDeltaMismatch; 29/29 unit cohort green |
| H3.8.1 | 19c0177 | 2026-05-18 | test/unit/GaugeEligibility.t.sol + EfficiencyOracle.t.sol + TVLOracle.t.sol — RB-005 close: GaugeEligibility L717/L736/L756 `address(uint160(0xN+uint160(i)))` → `makeAddr(string.concat("pool",vm.toString(i)))`; EfficiencyOracle L51 + TVLOracle L32 `_addr` helpers repurposed to `makeAddr(vm.toString(seed))` dropping `pure`; forge-lint 8 → 0 unsafe-typecast warnings; 370/370 unit suite green |
| H3.8.2 | 642a174 | 2026-05-18 | docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-005 Open → Closed (19c0177 H3.8.1; 8x unsafe-typecast 0x; 370/370 unit suite green) |
| H3.8.3 | 41cabeb | 2026-05-18 | docs/STAGE_H_PLAN.md — Completion Log rows H3.0b—H3.8.2 + status header refresh |
| H3.8.4 | d549942 | 2026-05-18 | CLAUDE.md §11 — Stage H in-progress resumption anchor (H1—H3 + H3.8 sweep; replaces Stage G snapshot; H4 next) |
| H4.0a | 9fda668 | 2026-05-19 | docs/STAGE_H_NOTES.md — H-D15—H-D23 EmissionDistributor design freeze lock (two-tier accRewardPerScoreUnit + poolAccDebt topology; per-user maps at H4; permissionless recordScore + CCBScore delegation; in-distributor F-3/F-7 share; absolute newScore + F12 signed-delta totalScore; IAuMM.mint at claim; lazy accrual tick + H4 LP-tranche stub; two-tier storage layout; accrual-time recordEmissions push at _settlePool boundary — allocation-side F-10 semantics) |
| H4.0b | de92f57 | 2026-05-19 | docs/STAGE_H_PLAN.md — H-D15—H-D23 Decisions rows + Completion Log rows H3.8.3/H3.8.4/H4.0a + status header + Last update + H4 sub-step skeleton expansion |
| H4.0c | a8b69b2 | 2026-05-19 | docs/STAGE_H_PLAN.md — Completion Log self-row for H4.0b |
| H4.0d | 64702bc | 2026-05-19 | CLAUDE.md §11 — Stage H resumption anchor refresh (H4.0a—H4.0c landed; HEAD a8b69b2; 22 sub-step commits; H-D8—H-D23 LOCKED; Next sub-step H4.1 IEmissionDistributor) |
| H4.1 | faed424 | 2026-05-19 | src/emission/IEmissionDistributor.sol — IEmissionDistributor interface per H-D15—H-D22 (6 events: ScoreUpdated/DepositRecorded/WithdrawalRecorded/Claimed/GovernanceTransferred/AuMTContractSet; 4 errors: NotApproved/NotAuMTContract/NotGovernance/ZeroAddress; 4 state mutators: recordScore/recordDeposit/recordWithdrawal/claim; 2 governance setters: setGovernanceContract/setAuMTContract; 11 views: accRewardPerScoreUnit/totalScore/lastAccrualBlock/poolScore/poolAccDebt/poolTotalLP/userLP/userRewardDebt/pendingClaim/governance/auMTContract) |
| H4.1.x-bis | 5128459 | 2026-05-19 | src/gauge/IEfficiencyOracle.sol — recordEmissions(address,uint256) push entry point per H-D23 (allocation-side F-10 semantics; @dev stale-deferral line refreshed); EfficiencyOracle.sol L223 override keyword; G16 inheritor sweep — empty stubs in test/fork/mocks/StageGMocks.sol + test/unit/GaugeEligibility.t.sol; mirrors H2a.10c quoteSvZCHF retrofit; forge build green |
| H4.2 | 76d0918 | 2026-05-19 | src/emission/EmissionDistributor.sol — abstract scaffold per H-D22 (9 imports: SafeCast+FixedPoint+IEmissionDistributor+IAuMM+IGaugeRegistry+IEMASampler+ICCBMultiplier+IEfficiencyOracle+CCBScore; 3 using: SafeCast uint256/int256 + FixedPoint; 6 public immutables AuMM/_gaugeRegistry/_emaSampler/_ccbMultiplier/_efficiencyOracle/GENESIS_BLOCK; 3 global+3 per-pool+2 per-user+2 governance public override storage slots; 0 functions per H3.2+H2b.1 scaffold precedent; forge build green) |
| H4.3 | a004277 | 2026-05-19 | src/emission/EmissionDistributor.sol — constructor per H-D22/H-D21/H-D16 (7 trailing-underscore params aumm_/gaugeRegistry_/emaSampler_/ccbMultiplier_/efficiencyOracle_/genesisBlock_/initialGovernance_; 6 ZeroAddress guards on address params; 8 assignments wiring 5 interface immutables + GENESIS_BLOCK + governance slot + lastAccrualBlock = genesisBlock_ per H-D21 first-accrual anchor; auMTContract NOT written → defaults to address(0) per H-D16 pre-Stage-I posture; silent governance init mirroring TVLOracle/EfficiencyOracle/BodenseeBootstrapChannel; contract remains abstract pending H4.4—H4.7; forge build green; 80 → 119 lines) |
| H4.4 | 7b43fd3 | 2026-05-19 | src/emission/EmissionDistributor.sol — governance setters per H-D14/H-D16 (onlyGovernance modifier per H3 BodenseeBootstrapChannel L145-L150 precedent; setGovernanceContract external override onlyGovernance + ZeroAddress guard + capture-old/write-new/emit GovernanceTransferred; setAuMTContract external override onlyGovernance — NO ZeroAddress guard per H-D16 deprecation safety valve asymmetry + capture-old/write-new/emit AuMTContractSet; forge build green; 119 → 153 lines) |
| H4.5a | d071c10 | 2026-05-19 | src/emission/EmissionDistributor.sol — _lpTrancheEmission(uint256 block_) internal view STUB per H-D21 (returns AuMM.blockEmissionRate(block_) unchanged; no F-0/F-3/F-7 phase subtractions; intentional over-accrual vs true LP tranche until H5 wires phase-aware schedule; block_ trailing underscore per Solidity reserved-keyword shadow avoidance; section header Emission rate stub H-D21; forge build green; 153 → 165 lines) |
| H4.5b | 4cb8395 | 2026-05-19 | src/emission/EmissionDistributor.sol — _accrueGlobal() internal per H-D15/H-D21 (11-line body: cache last = lastAccrualBlock SLOAD + empty-interval short-circuit if block.number == last + cache total = totalScore SLOAD + empty-totalScore guard if total == 0 writes lastAccrualBlock = block.number + returns + reads rate = _lpTrancheEmission(block.number) once per call after guards + deltaBlocks = block.number - last + accRewardPerScoreUnit += (rate * deltaBlocks).divDown(total) FixedPoint binding + final lastAccrualBlock = block.number write; strict order empty-interval THEN empty-totalScore THEN math; no unchecked blocks; section header Accrual H-D15 / H-D21; forge build green; 165 → 185 lines) |
| H4.5c | 254552e | 2026-05-19 | src/emission/EmissionDistributor.sol — _settlePool(address pool) internal per H-D15/H-D23 (7-line body: cache acc = accRewardPerScoreUnit SLOAD + deltaAcc = acc - poolAccDebt[pool] + poolAllocation = deltaAcc.mulDown(poolScore[pool]) FixedPoint mulDown + zero-skip if poolAllocation > 0 + _efficiencyOracle.recordEmissions(pool, poolAllocation) push BEFORE poolAccDebt write per H-D23 strict order + poolAccDebt[pool] = acc debt rebase; no try/catch recorder-gate revert propagates per H-D23/H-D10; no unchecked blocks; section header Settlement H-D15/H-D23; forge build green; 185 → 202 lines) |
| H4.5d | b988172 | 2026-05-19 | src/emission/EmissionDistributor.sol — _applySignedDelta(uint256 base, int256 delta) internal pure F12 signed-delta helper per H-D19 (extracts CCBMultiplier L272+L276 inlined pattern; single-statement body: return (base.toInt256() + delta).toUint256() using SafeCast already imported; SafeCast revert invariants documented in NatSpec; section header F12 signed-delta helper H-D19; no unchecked blocks; forge build green; 202 → 215 lines) |
| H4.5e | ee5eb99 | 2026-05-19 | src/emission/EmissionDistributor.sol — recordScore(address pool) external override per H-D17 6-step sequence (gauge gate NotApproved(pool) revert + _accrueGlobal + _settlePool per H-D21 lazy tick + CCBScore.score(_emaSampler.tvlEMA + _ccbMultiplier.getMultiplier) F-5 recomputation + H-D19 signed-delta totalScore middleware via _applySignedDelta + poolScore[pool] = newScore write + ScoreUpdated emit; permissionless; no modifier beyond gauge gate per H-D17; no unchecked blocks; section header Score producer H-D17; forge build green; 215 → 234 lines) |
| H4.0d | 038d0fb | 2026-05-19 | docs/STAGE_H_NOTES.md — H-D24 Per-pool poolAccRewardPerLP accumulator slot + _settlePool per-LP-unit accumulation step LOCKED (closes per-LP-unit derivation gap between H-D15 poolAllocation math and H-D16 pendingClaim formula; fourth per-pool tier slot extending H-D22 in-place at H4.6; _settlePool retrofit deferred to H4.5g; H-D23 strict order preserved; stranded-allocation skip-on-zero-LP semantic; H-D15/H-D16/H-D22/H-D23 bodies remain LOCKED; additive entry; label collision with 64702bc H4.0d preserved verbatim from commit message — this is the H-D24 NOTES amendment) |
| H4.5f | 65c5311 | 2026-05-19 | src/emission/IEmissionDistributor.sol — poolAccRewardPerLP(address pool) external view + pendingClaim @dev H-D24 binding (fourth per-pool tier view per H-D22 / H-D24 + poolEffectiveAccPerLPunit binds to poolAccRewardPerLP[pool] + mulDown form; G16 confirmed single inheritor EmissionDistributor abstract; forge build green; 218 → 226 lines) |
| H4.5g | fef6944 | 2026-05-19 | src/emission/EmissionDistributor.sol — poolAccRewardPerLP storage slot + _settlePool per-LP-unit retrofit per H-D24 (fourth per-pool tier slot mapping(address => uint256) public override poolAccRewardPerLP between poolAccDebt and poolTotalLP; _settlePool body 7 → 11 lines: inside poolAllocation > 0 zero-skip after H-D23 efficiencyOracle push add totalLP = poolTotalLP[pool] + if (totalLP > 0) poolAccRewardPerLP[pool] += poolAllocation.divDown(totalLP) H-D23/H-D24 strict order preserved; @dev NatSpec extended with H-D24 stranded-allocation sentence; public override auto-getter satisfies H4.5f IEmissionDistributor.poolAccRewardPerLP view; forge build green; 234 → 241 lines) |
| H4.0e | 4b6b49b | 2026-05-20 | docs/STAGE_H_NOTES.md — H-D25 Per-user pendingBalance crystallized-claim slot + recordDeposit/recordWithdrawal settle pattern LOCKED (closes per-user pending derivation gap vs H-D20 mint-at-claim-only + H-D22 tier enumeration; third per-user tier slot extending H-D22 in-place at H4.6; pendingClaim additive form binding crystallized-pending + live-delta; H4.5h interface extension + H4.6 code deferred; additive entry — H-D15/H-D16/H-D20/H-D22/H-D24 remain LOCKED; 221 → 224 lines) |
| H4.5h | 2fd4f14 | 2026-05-20 | src/emission/IEmissionDistributor.sol — pendingBalance(address pool, address user) external view + pendingClaim @dev H-D25 additive form (third per-user tier view per H-D22 / H-D25 + crystallized-pending balance view positioned between userRewardDebt and pendingClaim; pendingClaim @dev derivation line refreshed to pendingBalance[pool][user] + (poolAccRewardPerLP[pool] — userRewardDebt[pool][user]).mulDown(userLP[pool][user]) per H-D16 / H-D24 / H-D25; G16 confirmed single inheritor EmissionDistributor abstract; forge build green; 226 → 236 lines) |
| H4.6a | bf61d6b | 2026-05-20 | src/emission/EmissionDistributor.sol — pendingBalance storage slot + onlyAuMTContract modifier per H-D16 / H-D25 (third per-user tier slot mapping(address => mapping(address => uint256)) public override pendingBalance positioned between userRewardDebt and the Governance section header per H-D22 ordering — auto-getter satisfies H4.5h IEmissionDistributor.pendingBalance external view; new onlyAuMTContract modifier inserted after onlyGovernance per H-D16 recorder-slot gate — body if (msg.sender != auMTContract) revert NotAuMTContract(msg.sender) mirroring onlyGovernance L126-L129 precedent, pre-Stage-I posture with auMTContract == address(0) causes all callers to revert because msg.sender cannot equal zero; no consumers yet at H4.6a; forge build green; 241 → 251 lines) |
| H4.6b | 28c1b1d | 2026-05-20 | src/emission/EmissionDistributor.sol — recordDeposit(address pool, address user, uint256 amount) external override onlyAuMTContract per H-D16 / H-D21 / H-D25 (new Recorder mutators section at L251; body: _accrueGlobal + _settlePool + cache acc=poolAccRewardPerLP[pool] + pending=(acc-userRewardDebt).mulDown(userLP) against pre-deposit userLP + zero-skip if pending>0 pendingBalance+=pending crystallization per H-D25 + userLP+=amount + poolTotalLP+=amount + userRewardDebt=acc rebase + emit DepositRecorded; onlyAuMTContract modifier from H4.6a; abstract stays — recordWithdrawal + claim unimplemented; no unchecked; forge build green; 251 → 274 lines) |
| H4.6c | 2d9537e | 2026-05-20 | src/emission/EmissionDistributor.sol — recordWithdrawal(address pool, address user, uint256 amount) external override onlyAuMTContract per H-D16 / H-D21 / H-D25 (symmetric to recordDeposit — decrement instead of increment; body: _accrueGlobal + _settlePool + cache acc=poolAccRewardPerLP[pool] + pending=(acc-userRewardDebt).mulDown(userLP) against pre-withdrawal userLP + zero-skip if pending>0 pendingBalance+=pending crystallization per H-D25 + userLP-=amount + poolTotalLP-=amount + userRewardDebt=acc rebase + emit WithdrawalRecorded; underflow on over-withdrawal reverts — AuMT recorder responsible; abstract stays — claim unimplemented; no unchecked; forge build green; 274 → 295 lines) |
| H4.7a+b | 13c1ae5 | 2026-05-20 | src/emission/EmissionDistributor.sol — H4.7a claim(address pool, address to) + H4.7b pendingClaim(address pool, address user) bundled per H-D20 / H-D25 (commit message names only H4.7b but diff adds both functions; H4.7a claim sole AuMM.mint site per H-D20: _accrueGlobal + _settlePool + cache acc=poolAccRewardPerLP[pool] + amount = pendingBalance[pool][user] + (acc - userRewardDebt[pool][user]).mulDown(userLP[pool][user]) per H-D25 additive form + zero-amount short-circuit return + pendingBalance=0 + userRewardDebt=acc rebase + AuMM.mint(to, amount) per H-D20 + emit Claimed(pool, user, to, amount); H4.7b pendingClaim external view returns pendingBalance[pool][user] + (poolAccRewardPerLP[pool] - userRewardDebt[pool][user]).mulDown(userLP[pool][user]); uses stored poolAccRewardPerLP — not live-simulated; last unimplemented IEmissionDistributor functions; abstract keyword vestigial after this commit; no unchecked; forge build green; 295 → 329 lines) |
| H4.7c | 7ee706f | 2026-05-20 | src/emission/EmissionDistributor.sol — drop `abstract` keyword from L17 contract declaration (all IEmissionDistributor functions implemented after H4.7a/H4.7b bundled at 13c1ae5; EmissionDistributor fully concrete and deployable; forge build green; 329 lines unchanged) |
| H4.8.1 | 6789fba | 2026-05-20 | test/unit/EmissionDistributor.t.sol — scaffold (5 mocks + EmissionDistributorTest setUp + helpers; no test functions; test cohorts at H4.8.2 onward) |
| H4.8.2 | 55ba482 | 2026-05-20 | test/unit/EmissionDistributor.t.sol — Constructor test cohort (6 ZeroAddress reverts + 2 happy-path inits: WiresImmutables + InitsStorageSlots; 8/8 green; 196 → 300 lines) |
| H4.8.3 | c5b34b6 | 2026-05-20 | test/unit/EmissionDistributor.t.sol — Governance setter test cohort (setGovernanceContract: 2 reverts + UpdatesSlot + EmitsGovernanceTransferred; setAuMTContract: NotGovernance + DefaultsZero + AcceptsZero + UpdatesSlotAndEmits; 16/16 green; 300 → 368 lines) |
| H4.8.4 | e1873cc | 2026-05-20 | test/unit/EmissionDistributor.t.sol — recordScore test cohort (2 NotApproved reverts + 6 happy-path: state-and-emit + permissionless + signed-delta up/down + no-op + two-pool aggregation; 24/24 green; 368 → 462 lines) |
| H4.8.5 | 0bc3f86 | 2026-05-20 | test/unit/EmissionDistributor.t.sol — recordDeposit test cohort (NotAuMTContract + 3 first-deposit state assertions + DepositRecorded emit + zero-amount + multi-user poolTotalLP + crystallization; 32/32 green; 462 → 541 lines) |
| H4.8.6 | 02c2c15 | 2026-05-20 | test/unit/EmissionDistributor.t.sol — recordWithdrawal test cohort (NotAuMTContract + 2 underflow paths + partial/full state writes + WithdrawalRecorded emit + zero-amount + crystallization; 40/40 green; 541 → 624 lines) |
| H4.8.7 | 8900996 | 2026-05-20 | test/unit/EmissionDistributor.t.sol — claim test cohort (8 tests; 2 zero-amount short-circuits + happy-path + delegated-to + drain-pending + emit-Claimed + same-block-noop + zero-address-revert; 48/48 green; 624 → 740 lines) |
| H4.8.8 | eba884d | 2026-05-20 | test/unit/EmissionDistributor.t.sol — pendingClaim view cohort (8 tests: zero-user + post-deposit + crystallized-pending-only + stale-snapshot + fresh-after-touch + drops-after-claim + additive-both-components + pool-isolation; 56/56 green; 740 → 840 lines) |
| H4.8.9 | 5cb7b80 | 2026-05-20 | test/unit/EmissionDistributor.t.sol — accrual + settle math cohort (8 tests: multi-block accumulator + empty-totalScore guard + empty-interval guard + EfficiencyOracle push + zero-allocation skip + per-LP-unit math + poolAccDebt rebase + zero-LP stranded; 64/64 green; 840 → 945 lines) |
| H4.8.10a | dff5875 | 2026-05-20 | docs/STAGE_H_PLAN.md — Completion Log rows H4.0c—H4.7c (22 rows: H4 implementation family backfill; IEmissionDistributor interface + IEfficiencyOracle.recordEmissions retrofit + EmissionDistributor scaffold/constructor/setters/internals/mutators/claim; H4.0d label collision + H4.7a+b bundle noted inline) |
| H4.8.10b | 9a118c2 | 2026-05-20 | docs/STAGE_H_PLAN.md — Completion Log rows H4.8.1—H4.8.9 (9 rows: EmissionDistributor unit test cohort family; scaffold + Constructor + governance setters + recordScore + recordDeposit + recordWithdrawal + claim + pendingClaim view + accrual+settle math; 64/64 green) |
| H4.8.10c | 86e0929 | 2026-05-20 | docs/STAGE_H_PLAN.md — status header refresh (H4 complete + H4.8 unit cohort + H4.8.10a/b landed; H-D1—H-D25 LOCKED) + H-D24/H-D25 Decisions rows + H4.8.10a/b Completion Log self-rows + Mode line tense refresh |
| H4.8.10d | 95e3830 | 2026-05-20 | CLAUDE.md §11 — Stage H in-progress resumption anchor (H4 family complete + H4.8 unit cohort + H-D1—H-D25 LOCKED; H5 next; replaces H3.8.4 snapshot) |
| H5.0a | 841fe24 | 2026-05-20 | docs/STAGE_H_NOTES.md — H-D26—H-D30 LOCKED bodies inserted between L124 and former L125 separator (H-D26 `_lpTrancheIntegral(from, to)` replaces single-block sampling in `_accrueGlobal` for conservation-correct interval integration; H-D27 bootstrap leg AP integration mirrors `BodenseeBootstrapChannel._apSum` bit-for-bit at L218-L226; H-D28 transition leg returns `rate × n` with no LP-tranche subtraction — F-3 α-blend lives in `recordScore` middleware per H-D18; H-D29 continuous-leg F-7 Step 1 Incendiary skim with mutable governance-settable `incendiaryRegistry` slot + interval-form `IIncendiaryRegistry.integratedSkim(from, to)` interface + Stage H zero-stub; H-D30 era-boundary splitting at `nthHalvingBlock(g, k)` with constant `blockEmissionRate(sub_from)` per sub-interval; 5 new LOCKED entries; 224 → 244 lines) |
| H5.0b | 8bf5be5 | 2026-05-20 | docs/STAGE_H_PLAN.md — H5 sub-step skeleton (H5.0a—H5.5 family decomposition + H7 consolidation) + H-D26—H-D30 Decisions rows + H4.8.10c/d + H5.0a Completion Log self-rows + status header refresh (178 → 186 lines) |
| H5.2 | c4cfe7f | 2026-05-20 | src/incendiary/IIncendiaryRegistry.sol — Stage L forward-dep interface stub (integratedSkim per H-D29; first file under src/incendiary/; H7 consolidated) |
| H5.1a | 1291271 | 2026-05-20 | src/emission/EmissionDistributor.sol — incendiaryRegistry slot + IncendiaryRegistrySet event + setIncendiaryRegistry setter per H-D29 (governance-settable address(0) deprecation valve; 329 → 351 lines) |
| H5.1b | 0ccb64c | 2026-05-20 | src/emission/EmissionDistributor.sol — private _bootstrapApSum helper per H-D27 (bit-for-bit mirror of BodenseeBootstrapChannel._apSum; F-0 bootstrap AP closed-form; 351 → 381 lines) |
| H5.1c | 3a1c0fc | 2026-05-20 | src/emission/EmissionDistributor.sol — private view _phaseAwareBody helper per H-D27/H-D28/H-D29 (three-phase dispatcher with cursor walk; bootstrap A/B split at m6 with conservation identity rate*n − _bootstrapApSum; transition rate*n no subtraction; continuous rate*n − skim with address(0) zero-stub per H-D29; AureumTime + IIncendiaryRegistry imports; 381 → 436 lines) |
| H5.1d | b0bf0d9 | 2026-05-20 | src/emission/EmissionDistributor.sol — internal view _lpTrancheIntegral(from, to) era-splitting outer loop per H-D26 / H-D30 (cursor walk with AureumTime.eraIndex + nthHalvingBlock split; constant rate = AuMM.blockEmissionRate(cursor) per era sub-interval; _phaseAwareBody dispatch for inner phase split; supersedes H-D21 single-block sample for H5 production accrual path; _accrueGlobal rewiring deferred to H5.1e; 436 → 461 lines) |
| H5.1e | 7b1babb | 2026-05-20 | src/emission/EmissionDistributor.sol — _accrueGlobal body rewrite per H-D26 (replaces rate=_lpTrancheEmission+deltaBlocks*rate pattern with contribution=_lpTrancheIntegral(last+1,block.number); NatSpec rewritten to H-D26 production accrual path; supersedes H-D21 single-block sample; H5.1 family complete — 461 → 460 lines) |
| H5.3a | d71bd9c | 2026-05-20 | docs/STAGE_H_NOTES.md — H-D31—H-D33 LOCKED (recordScore H5.3 reshape storage extensions + IMiliariumRegistry 6th immutable + 10-step flow order; _alphaF3 linear blend helper; effective score formula Miliarium equal-split blend + non-Miliarium α-CCB Option A per §xxviii; 244 → 256 lines) |
| H5.3b | 64d8af7 | 2026-05-20 | src/emission/EmissionDistributor.sol — IMiliariumRegistry import + 6th typed immutable _miliariumRegistry + f5Total / f5Score storage slots + 7th constructor param miliariumRegistry_ + ZeroAddress guard + immutable assignment per H-D31 (460 → 474 lines) |
| H5.3c | 39b055e | 2026-05-20 | test/unit/EmissionDistributor.t.sol — MockMiliariumRegistry + miliReg wiring into 8 constructor sites + test_RevertWhen_ConstructedWithZeroMiliariumRegistry + WiresImmutables 7th assertion + InitsStorageSlots f5Total==0 per H-D31 (945 → 995 lines) |
| H5.3d | 506ca89 | 2026-05-20 | src/emission/EmissionDistributor.sol — _alphaF3(uint256 block_) private view helper per H-D32 (linear F-3 α blend across transition window; lower clamp 0 at ≤month10EndBlock; upper clamp 1e18 at ≥year1EndBlock; 437_999-block denominator; 474 → 496 lines) |
| H5.3e | 4eebdef | 2026-05-20 | src/emission/EmissionDistributor.sol — recordScore body rewrite to H-D31 10-step flow + H-D33 F-3 α-blend reshape (f5Total/f5Score signed-delta + _alphaF3 snapshot + Miliarium (1e18-α).mulDown(f5Total/28)+α.mulDown(F5) + non-Miliarium α.mulDown(F5); 496 → 504 lines) |
| H5.3f | e34616a | 2026-05-21 | test/unit/EmissionDistributor.t.sol — recordScore cohort migration to H-D31/H-D33 (vm.roll past year1EndBlock + f5Score/f5Total asserts on 8 existing tests; 2 new bootstrap α=0 tests: Miliarium f5Total/28 + non-Miliarium zero; AureumTime import; 995 → 1045 lines) |
| H5.3f-fix1+fix2 | 7b75aad | 2026-05-21 | test/unit/EmissionDistributor.t.sol — rational literal cast uint256(100e18)/28 (fix1) + 16-test vm.roll migration to α=1e18 continuous regime + Accrual lastAccrualBlock assertion rebase to year1EndBlock (fix2); 67/67 tests pass (1045 → 1061 lines) |
| H5.4.0a | 73c5979 | 2026-05-21 | docs/STAGE_H_NOTES.md — H-D34 LOCKED (test-side observation surface for _lpTrancheIntegral + _lpTrancheEmission via Harness contract at test/unit/harness/EmissionDistributorHarness.sol; preserves prod ABI minimality; NatSpec drift note; G16 prospective inheritor enumeration; 256 → 260 lines) |
| H5.4.0b | ccc29d9 | 2026-05-21 | docs/STAGE_H_PLAN.md — H-D31—H-D34 Decisions rows added to table (H5.3a NOTES freeze H-D31/H-D32/H-D33 backfill + H5.4.0a NOTES freeze H-D34 backfill; 186 → 190 lines) |
| H5.4.0c | e69d45a | 2026-05-21 | docs/STAGE_H_PLAN.md — status header + Last update + Mode + sub-step skeleton H5 row refresh (HEAD ccc29d9; H-D1—H-D34 LOCKED; 17 commits since H5.0a 841fe24 reflected; H5.4.0d next; 190 lines) |
| H5.4.0d | 5b25585 | 2026-05-21 | test/unit/harness/EmissionDistributorHarness.sol — Harness scaffold is EmissionDistributor + 8-arg constructor pass-through + extLpTrancheIntegral + extLpTrancheEmission wrappers per H-D34 (37 lines; forge build green) |
| H5.4.0e | df2af18 | 2026-05-21 | test/unit/EmissionDistributor.t.sol — distributor slot retype EmissionDistributor → EmissionDistributorHarness + setUp construction retype + harness import per H-D34 (1061 → 1062 lines; 67/67 tests green) |
| H5.4.0f.1 | fac7e95 | 2026-05-21 | test/unit/EmissionDistributor.t.sol — 6 AP arithmetic identity tests for extLpTrancheIntegral over single bootstrap A/B-leg intervals + _apSumFixture closed-form helper mirroring _bootstrapApSum per H-D27 + H-D34; fix1 replaces naive loop with closed-form integer-rounding identity (1062 → 1144 lines; 73/73 tests green) |
| H5.4.0f.2 | 9dbce93 | 2026-05-21 | test/unit/EmissionDistributor.t.sol — 6 era-boundary cursor walk tests for extLpTrancheIntegral (single-era, straddle-constant, straddle-halved via vm.mockCall, exact-era-end, era1-start, three-era-halving) per H-D30 (1144 → 1208 lines; 79/79 tests green) |
| H5.4.0f.3 | 85a0ff1 | 2026-05-21 | test/unit/EmissionDistributor.t.sol — 6 phase-boundary nesting tests for extLpTrancheIntegral (CrossM6 ALeg→BLeg, CrossM10 BLeg→transition, CrossY1 transition→continuous, CrossM6AndM10 ALeg→transition, CrossM10AndY1 BLeg→continuous, FullSweep all 4 phases) per H-D27+H-D28+H-D29 (1208 → 1291 lines; 85/85 tests green) |
| H5.4.0f.4 | 620d92d | 2026-05-21 | test/unit/EmissionDistributor.t.sol — 5 continuous-phase Incendiary skim tests (IIncendiaryRegistry import + DefaultRegistry_NoCall + NonZeroRegistry_ZeroSkim + NonZeroRegistry_PositiveSkim_Subtracts + RevertWhen_IncendiaryRegistryReverts + RegistryRebindToZero_DeprecationValve; 90/90 green; 1291 → 1368 lines) |
| H5.4.0f.5 | 8f1fe3e | 2026-05-21 | test/unit/EmissionDistributor.t.sol — 6 H-D26 conservation invariant tests (ALegInterior + BLegInterior + TransitionInterior + ContinuousDefaultRegistry + ContinuousWithSkim + FullSweep; LP+bootstrap+skim=rate×n property; 96/96 green; 1368 → 1451 lines) |
| H5.4.0g | 5009a0c | 2026-05-21 | test/unit/EmissionDistributor.t.sol — 4 setIncendiaryRegistry governance setter tests (RevertWhen_NotGovernance + AcceptsZeroAddress + UpdatesSlot + EmitsIncendiaryRegistrySet; 100/100 green; 1451 → 1482 lines); fix IEmissionDistributor.sol — move IncendiaryRegistrySet event from concrete contract to interface per G16 (EmissionDistributor.sol 504 → 498 lines; IEmissionDistributor.sol → 244 lines) |
| H5.5a | b7e9cc2 | 2026-05-21 | docs/STAGE_H_PLAN.md — Completion Log backfill 25 rows H5.0b—H5.4.0g (190 → 215 lines) |
| H5.5b | a9e4b06 | 2026-05-21 | docs/STAGE_H_PLAN.md — status header + Last update + Mode refresh (H5 family complete; 100/100 tests; H5.5c CLAUDE.md §11 next; 215 lines) |
| H5.5c | c78e6f7 | 2026-05-21 | CLAUDE.md — §11 resume-anchor refresh (Stage H position roll-up H1—H5 complete + H6 next-step framing) |
| H6.0a | c1ef0c9 | 2026-05-21 | docs/STAGE_H_NOTES.md — H-D35 (IAuMT forward-dep interface stub at src/token/IAuMT.sol) + H-D36 (real-AuMM integration test family at test/unit/AuMMDistributorIntegration.t.sol, 4-test cohort) LOCKED (260 → 268 lines) |
| H6.0b | 78578b3 | 2026-05-21 | docs/STAGE_H_PLAN.md — H6 sub-step skeleton expansion (H6.0a—H6.3 family) + H-D35/H-D36 Decisions rows + H5.5a/b/c + H6.0a Completion Log backfill + status header + Last update + Mode refresh (this commit; 215 → ~225 lines) |
| H6.0c | 856a0f4 | 2026-05-22 | docs/STAGE_H_NOTES.md — H-D35 amended: in-place extension of Stage G IAuMT.sol; G16 sweep required at H6.2a; governanceWeight preserved per G-D9 (268 lines) |
| H6.0d | 66da6d3 | 2026-05-22 | docs/STAGE_H_PLAN.md — H6.0d PLAN refresh: L31 sub-step skeleton rewrite + L78 H-D35 row rationale rewrite + status header + Completion Log H6.0c/H6.0d rows + H6.0b <pending> → 78578b3 backfill (this commit; 222 → 224 lines) |
| H6.1 | 3b2a589 | 2026-05-22 | src/token/IAuMT.sol — in-place extension per amended H-D35: 4 functions added (mint + burn + distributor + pool); preserves `is IERC20` + G-D9 `governanceWeight(address)` consumed at `src/gauge/VaultClassRegistry.sol:277`; NOT a new file (existing G-DP4 forward-dep stub from Stage G); 25 → 74 lines |
| H6.2a | 8ea1cd2 | 2026-05-22 | test/unit/VaultClassRegistry.t.sol + test/fork/mocks/StageGMocks.sol — G16 inheritor sweep per amended H-D35: both `MockAuMT` inheritors extended with stub bodies for 4 new functions in same commit per `test/fork/mocks/StageGMocks.sol:L10` propagation NOTE (return 0 for uint256; return address(0) for address); 578 → 590 + 75 → 86 lines; forge test --match-path test/unit/VaultClassRegistry.t.sol 37/37 green at H6.2b |
| H6.3a | 1b0558b | 2026-05-22 | test/unit/AuMMDistributorIntegration.t.sol — scaffold per H-D36: imports (named-import of 5 mocks from EmissionDistributor.t.sol; MockAuMM excluded) + 6 constants + 7 storage slots + setUp wiring real AuMM via C-D11 two-flag-lock + EmissionDistributorHarness per H-D34 (67 lines) |
| H6.3b | 8a65756 | 2026-05-22 | test/unit/AuMMDistributorIntegration.t.sol — 2 setMinter tests per H-D36: SetMinterHandoff_EmitsMinterSet (fresh AuMM + expectEmit + minter slot assert) + RevertWhen_SetMinterCalledTwice (C-D11: NotMinterAdmin first-failing guard, not MinterAlreadySet); 2/2 green (67 → 82 lines) |
| H6.3c | 63fcd3b | 2026-05-22 | test/unit/AuMMDistributorIntegration.t.sol — 2 claim tests per H-D36: ClaimMintsRealAuMM_SinglePool (real AuMM mint path at year1EndBlock+1 single-pool, 100% LP share → 1e18) + ClaimAcrossHalvingBoundary_UsesRealBlockEmissionRate (H-D30 era cursor walk straddle firstHalvingBlock: 9 Era 0 × 1e18 + 11 Era 1 × 5e17 = 14.5e18, not 20e18 flat-rate); 4/4 green (82 → 113 lines) |
| H6.4a | d1f8d56 | 2026-05-22 | docs/STAGE_H_PLAN.md — Completion Log catch-up: H6.0d hash backfill (<pending> → 66da6d3 + ~226 → 224 lines correction) + 6 new rows (H6.1/H6.2a/H6.3a/H6.3b/H6.3c/H6.4a self-row); 224 → 230 lines |
| H6.4b | 71a6d29 | 2026-05-22 | docs/STAGE_H_PLAN.md — status header + Last update + Mode refresh (H6.4a HEAD d1f8d56; H6 implementation family complete; 612/612 unit green across 19 unit .t.sol files; H6.4c/d remaining; 230 lines) |
| H6.4d | c1b536c | 2026-05-22 | CLAUDE.md — §11 resume-anchor refresh (Stage H position roll-up H1—H6 complete + 612/612 unit + H8 next-step framing) |
| H6.4e | 65fbb4c | 2026-05-22 | docs/STAGE_H_PLAN.md — Completion Log tail (H6.4a hash backfill <pending> → d1f8d56 + H6.4b/H6.4d rows appended) + status header + Last update + Mode refresh (H6.4 sweep complete; H8 next; 230 → 232 lines) |
| H6.4f | ef4b9be | 2026-05-22 | CLAUDE.md — §11 resume-anchor refresh for H6.4e closure (HEAD 71a6d29 → 65fbb4c; 153 → 155 commits; H6.4 sweep 'in flight' → 'complete (H6.0—H6.4f)'; recent commit stack prepend H6.4e + H6.4d, drop H5.5b + H5.5a; L438 Stage H position + sub-step roll-up final element refreshed) |
| H9.0a | eade88f | 2026-05-22 | docs/STAGE_H_NOTES.md — H-D37 (H9 fork-test mock swap-out matrix + StageHIntegrationFixture inheritance from StageGIntegrationFixture) + H-D38 (4-contract cohort per emission-schedule phase) + H-D39 (BodenseeBootstrapChannel.distribute() full IVault.unlock arc against real Balancer V3 Vault) + H-D40 (D35 split-form + D36 --threads 1 belt + Ankr RPC carry-forward + block-pin posture) LOCKED (268 → 284 lines) |
| H9.0b | f4a93b4 | 2026-05-22 | docs/STAGE_H_PLAN.md — H9 sub-step skeleton expansion (H9.0a—H9.7 family) + H-D37—H-D40 Decisions rows + H6.4e/H6.4f/H9.0a Completion Log catch-up + H9.0b self-row + status header + Last update + Mode refresh (this commit; 232 → 240 lines) |
| H9.0c | 7f9a95f | 2026-05-22 | docs/STAGE_H_NOTES.md — H-D37 amended (minter-slot wiring deferred to per-derived-test setUp override per Option A; efficiencyOracle.setEmissionsRecorder + emissionDistributor.setAuMTContract in fixture setUp; Anchors expansion C-D11/H-D11/H-D20/H-D23; 284 lines unchanged) |
| H9.0d | e099cb6 | 2026-05-22 | docs/STAGE_H_PLAN.md — H9.0d PLAN refresh: L3 status header + L5 Last update + L34 H9.1 sub-step description (minter deferred to per-derived-test setUp) + L80 H-D37 row Status "LOCKED amended at H9.0c" + Rationale rewrite + Completion Log H9.0b hash backfill (<pending> → f4a93b4) + H9.0c row + H9.0d self-row (this commit; 240 → 242 lines) |
| H9.1 | a24db97 | 2026-05-22 | test/fork/StageHIntegration.t.sol — StageHIntegrationFixture is StageGIntegrationFixture scaffold per H-D37 (TVLOracle + EMASampler + CCBMultiplier + EfficiencyOracle + BodenseeBootstrapChannel + EmissionDistributor + MockMiliariumRegistry wiring; H-D23 setEmissionsRecorder + H6.3 setAuMTContract post-construction wiring; aumm.setMinter deferred to derived contracts per H-D37 amended at H9.0c Option A; incendiaryRegistry slot default per H-D29 zero-stub; commit msg label collision noted inline — tagged "H9.0a" in git but actual plan ref is H9.1 per STAGE_H_PLAN.md L34; 0 → 101 lines) |
| H9.2 | 8fff3cc | 2026-05-22 | test/fork/StageHIntegration.t.sol — StageHBootstrapPhaseTest accrue() cohort per H-D38(1) (4/4 green against real Ankr fork: Bootstrap A leg + Bootstrap B leg + boundary-spanning split + conservation invariant; AureumTime import added; _expectedApSum helper H-D27 bit-for-bit mirror of test/unit/BodenseeBootstrapChannel.t.sol:L333-L348; setUp override: super.setUp() + aumm.setMinter(address(bootstrapChannel)) per H-D37 amended Option A; 101 → 208 lines) |
| H9.3 | 3c35616 | 2026-05-22 | test/fork/StageHIntegration.t.sol — StageHBootstrapPhaseTest distribute() cohort per H-D39 (3 tests: test_Distribute_RealVaultDonationArc_MutatesReserveAndEmitsDistributed full IVault.unlock → _distributeCallback → safeTransfer → settle → DONATION arc against real Balancer V3 Vault + test_RevertWhen_DistributeCalledByNonGovernance + test_RevertWhen_DistributeWithNoPendingAccrual; §12 ambiguity-gate catch — fixture constructs bootstrapChannel with address(this) as governance but H-D39 expects vm.prank(GOVERNANCE_MULTISIG), resolution via H-D14 mutable governance slot bridged in derived setUp.setGovernanceContract; 208 → 257 lines) |
| H9.4 | a3f5e65 | 2026-05-23 | test/fork/StageHIntegration.t.sol — StageHContinuousPhaseTest is StageHIntegrationFixture per H-D38(2) (F-7 phase: 1 test test_Claim_F7Phase_ThreePilotsMint_BoundedConservation; Strategy A white-box vm.store on EMASampler slot 0 = tvlEMA seed bypass; 3-pilot recordScore → recordDeposit → claim arc; tvlEMA=1e18 + span=300 + 3 pilots → 100e18 exact per pilot; bounded conservation diff=0 ≤ N_gauged=3 wei per H-D38(2); 257 → 327 lines) |
| H9.5 | 505a507 | 2026-05-23 | test/fork/StageHIntegration.t.sol — StageHHalvingBoundaryTest is StageHIntegrationFixture per H-D38(3) (1 test test_ClaimAcrossHalvingBoundary_UsesRealBlockEmissionRate; single-pilot 20-block straddle of AureumTime.firstHalvingBlock(GENESIS_BLOCK_) mirrors H6.3c unit identity 9 × 1e18 + 11 × 5e17 = 14.5e18 in fork context against real AuMM bytecode; Strategy A founding-seed via gaugeRegistry.seedFoundingPool + slot 0 EMA vm.store; H9.5-fix1 inline corrective — × U+00D7 in assertEq message string replaced with ASCII x to clear Solidity 0.8.26 "Invalid character in string" compile error; 327 → 366 lines) |
| H9.6 | 00932cb | 2026-05-23 | test/fork/StageHIntegration.t.sol — StageHCrossStackTest is StageHIntegrationFixture per H-D38(4) (1 test test_CrossStack_GaugeApprovalToClaim_MintsAuMM; real gauge approval chain via VaultClassRegistry + GaugeEligibility + ANTI_SPAM_FEE SwapAndDepositToBodensee side-effect; Strategy B TVLOracle seeding via setTokenUnderlying self-map of pilot tokens to svZCHF — _constellationRatio identity yields tvl(pilot) = Σ balancesLiveScaled18; F-D15 EMASampler cold-start seed via emaSampler.updateEMA — no vm.store bypass; α=1e18 continuous regime via vm.roll past year1EndBlock; binary positive-balance assertion per H-D38(4); 366 → 414 lines) |
| H9.7a | ed5dc38 | 2026-05-23 | docs/STAGE_H_PLAN.md — Completion Log catch-up: H9.0d hash backfill <pending> → e099cb6 + 6 new rows (H9.1 with commit-msg label-collision note + H9.2/H9.3/H9.4/H9.5 with H9.5-fix1 inline note + H9.6) + H9.7a self-row (this commit; 242 → 249 lines) |
| H9.7b | dd21f3f | 2026-05-23 | docs/STAGE_H_NOTES.md — Findings queue H10/H11/H12: fork-test governance-wiring setUp override + Strategy A/B TVL seeding criterion + Solidity 0.8.26 non-ASCII string literal restriction (284 → 318 lines) |
| H9.7c | 9db4187 | 2026-05-23 | docs/STAGE_H_PLAN.md — status header + mode refresh + H9.7a hash backfill <pending> → ed5dc38 (249 lines unchanged) |
| H9.7d | aecb57b | 2026-05-23 | CLAUDE.md — §11 resume-anchor refresh (H9 fork integration test family complete 10/10; H9.7 sweep in flight; H8 reordered post-H9; 169 commits; H-D1—H-D6+H-D8—H-D40 37 locks; H10/H11/H12 Findings queue) |
| H9.7e | 2b51d89 | 2026-05-23 | docs/STAGE_H_PLAN.md — Completion Log tail: H9.7b/H9.7c/H9.7d rows + H9.7e self-row (this commit; 249 → 253 lines) |
| H10.0a | db75c6b | 2026-05-23 | docs/STAGE_H_NOTES.md — H-D7 LOCKED Option C (defer setMinter to Stage K) + H-D41 Multi-minter architecture carry-forward (318 → 322 lines) |
| H10.0b | c4b747a | 2026-05-23 | docs/STAGE_H_PLAN.md — H-D7 row OPEN→LOCKED + H-D41 row + H10 sub-step skeleton expansion + status header + Last update + Mode refresh (this commit; 253 → 256 lines) |
| H10.1 | 90f9a82 | 2026-05-23 | script/DeployStageH.s.sol — Stage H deployment script (AuMM + TVLOracle + EfficiencyOracle + BodenseeBootstrapChannel + EmissionDistributor; deployer→GOVERNANCE_MULTISIG handoff; no setMinter per H-D7 Option C; MinterNotZero invariant assertion; 191 lines); subsequently revised at H10.1-fix1 per H-D42 |
| H10.0c-fix | 2af6c8d | 2026-05-23 | docs/STAGE_H_NOTES.md — H-D42 LOCKED (AuMM as Stage H input per H10.2 first-execution revert at 90f9a82; DeployStageH.s.sol consumes AUMM env per DeployDerBodensee.s.sol L39 convention; GENESIS_BLOCK derives from aumm.GENESIS_BLOCK(); 322 → 326 lines) |
| H10.0d-fix | 11bf56d | 2026-05-23 | docs/STAGE_H_PLAN.md — L3 status header + L5 Last update + L7 Mode + L35 H10 skeleton refresh + L50 H-D7 cross-ref to H-D42 + H-D42 Decisions row + H10.0b hash backfill (<pending> → c4b747a) + H10.1/H10.0c-fix/H10.0d-fix Completion Log rows (this commit; 256 → 260 lines) |
| H10.1-fix1 | c04ee0d | 2026-05-23 | script/DeployStageH.s.sol — H-D42 revision: AuMM consumed via AUMM env var (not deployed in-script); genesisBlock derives from aumm.GENESIS_BLOCK(); MinterNotZero invariant reframed as input-precondition check; NatSpec env-list drops GENESIS_BLOCK, adds AUMM (191 → 224 lines) |
| H10.2-revised | 2112f09 | 2026-05-23 | test/fork/DeployStageH.t.sol — fork-test validates DeployStageH.s.sol (H10.1-fix1) via StageGIntegrationFixture inheritance: real Vault + AuMM + Bodensee; 11 assertions (H-D7 Option C minter slot + Step 7 handoffs + Step 6 wiring + BODENSEE_POOL + GENESIS_BLOCK derivation) + positive setMinter proof; 1/1 fork green (219 lines) |
| H6.2a-fix | 8da816e | 2026-05-23 | test/fork/mocks/StageGMocks.sol — MockAuMT distributor()/pool() view → pure (both return address(0) constants; Solc 0.8.26 Warning(2018) cleanup surfaced at H10.2-revised forge build; -2 warnings) |
| H10.3 | e19b737 | 2026-05-23 | docs/STAGE_H_PLAN.md — H10.0d-fix hash backfill (<pending> → 11bf56d; 256 → 260 lines correction) + H10.1-fix1/H10.2-revised/H6.2a-fix Completion Log rows + status header + Last update + Mode refresh (this commit; 260 → 264 lines) |
| H8 | n/a | 2026-05-23 | unit-test confirmation pass (reordered post-H9): `forge test --no-match-path "test/fork/**"`; 612/612 green across 22 suites (19.17s); no code changes — pre-existing clean baseline carried through H9/H10; no commit |
| H11.0a | fb2d9b1 | 2026-05-23 | docs/STAGE_H_NOTES.md — H13 Findings entry: deploy-script fork tests cannot stub env vars whose receivers make external calls in their constructors; StageGIntegrationFixture inheritance is the resolution (H10.2 first-attempt revert + H-D42 chicken-and-egg; 318 → 338 lines) |
| H11.0b | <pending> | 2026-05-23 | docs/STAGE_H_PLAN.md — H10.3 hash backfill (<pending> → e19b737) + H8/H11.0a Completion Log rows + H11.0b self-row + status header + Last update + Mode refresh (this commit; 264 → 267 lines) |
