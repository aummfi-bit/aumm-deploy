# Aureum White-Hat Output — Findings Ledger

The single, append-only record of every white-hat finding across all passes.
One row per finding. Driven by `AUREUM_WHITEHAT_PROCESS.md`; every step's
attestation either adds rows here or records "no finding" in the coverage table
below.

Append-only: never delete a row. A finding that turns out to be a false
positive is marked `False-positive` in **Status**, with the reason in
**Explanation** — the row stays as a record that the surface was checked.

---

## Column legend

| Column | Meaning |
|---|---|
| **ID** | `F-NN`, monotonic across the whole project (never reused). |
| **Date** | ISO date the finding was raised (`YYYY-MM-DD`). |
| **Stage/Step** | The white-hat step that surfaced it, e.g. `WI.3`. "Stage that figured it out." |
| **File** | Contract path, e.g. `src/token/AuMM.sol`. |
| **Routine** | Function / modifier / library routine where the issue lives. |
| **Surface** | Catalog ID `S0`–`S12` (see process doc §7). |
| **Severity** | `Critical` / `High` / `Medium` / `Low` / `Info`. |
| **Confidence** | `Confirmed` (full PoC) / `Suspected` (needs PoC) / `Theoretical`. |
| **Issue** | One-line statement of the problem. |
| **Explanation** | The attack path / mechanism, and — once resolved — the resolution reasoning. |
| **Status** | `Open` / `Confirmed` / `Fixed` / `Accepted-risk` / `False-positive`. |
| **Fix** | Commit hash of the fix (or `—`), plus byte-identity note if the fix touches the substrate seam. |
| **PoC** | Path to the Foundry test that proves/kills it, e.g. `test/whitehat/F07_emaDonation.t.sol`. |

### Status lifecycle
```
Open ─▶ Confirmed ─▶ Fixed
  │          │
  └─▶ False-positive   └─▶ Accepted-risk (documented, no code change)
```
A `Suspected` finding stays **Open** until a PoC moves it to **Confirmed** or
**False-positive**. Nothing reaches **Fixed** without both a PoC and a fix
commit.

---

## Findings table

| ID | Date | Stage/Step | File | Routine | Surface | Sev | Conf | Issue | Explanation | Status | Fix | PoC |
|----|------|-----------|------|---------|---------|-----|------|-------|-------------|--------|-----|-----|
| F-01 | 2026-06-11 | WK.10 | `src/governance/AureumGovernance.sol` | `_voteSucceeded` / `_createProposal` / `castVote` | S9 | Medium | Confirmed | Quorum denominator is the propose-time `VOTING_WEIGHT.totalSupply()` snapshot (L201/L204→L289) while `forVotes`/`againstVotes` accrue at vote-time (`poke`+`governanceWeight`, L257-258); turnout can exceed 100% of snapshot, so the 20% quorum (`QUORUM_BPS`) gates against a stale, attacker-shrinkable base, not true eligible weight. | Root cause: `VotingWeight._totalQualifiedWeight` is a lazy poke-accumulator (VotingWeight L95-96/L115-120) excluding never-poked holders; the proposer is not poked in `_createProposal` (L196-206) but self-pokes in `castVote` (L257), so their weight is absent from the denominator yet present in the numerator. Effective quorum = 20% of poked weight; at `snapshot==0` it is vacuous. A single qualified holder unilaterally passes gauge revoke / fee change (simple majority) and composition swap (2/3). Bounded by the 14-day qualification cliff (VotingWeight L137, no flash weight), the non-refundable 1,000 svZCHF deposit, the ~16-day vote+timelock contest window (one larger honest against-vote defeats it), permissionless defensive poking, and the WK.8 bounded blast radius (no theft, reversible). Sharpest in early/thin-participation life. Fix: read the quorum denominator at tally-time `endBlock` (all self-poked voters then included → turnout ≤ 100%), or Governor-style historical weight checkpoints. WK.13 intent: a DEVIATION from the documented `vetoSupport ≤ totalSupply` invariant (VotingWeight L13/L21), not an I-D17-intended reuse — I-D17 deferred the semantics to Stage K (STAGE_I_NOTES L179) and the original consumer `vetoProposal` reads `totalSupply()` live (VaultClassRegistry L279); AureumGovernance's propose-time snapshot is the divergence → CODE FIX (not Accepted-risk). RESOLVED (e6d9b3d) — `_voteSucceeded` reads the quorum denominator LIVE at tally via `VOTING_WEIGHT.totalSupply()` (L294), the WK.13 `vetoProposal`-mirroring alignment: by `endBlock` every voter has self-poked so the accumulator subsumes the numerator → turnout ≤ 100%; `snapshotTotalSupply` retained for the `ProposalCreated` event only. Regression `test_F01_liveTallySupplyDefeatsStaleQuorumGaming` models the accumulator growing 20× post-propose (snapshot 10_000e18 → live 200_000e18) — a 10_000e18 minority vote the stale snapshot passed is now Defeated; F01 suite 5/5. No substrate seam. | Fixed | `e6d9b3d` (no substrate seam) | `test/whitehat/F01_quorumSnapshotTiming.t.sol` (5/5) |
| F-02 | 2026-06-11 | WK.12 | `src/governance/VotingWeight.sol` | `_positionPower` / `poke` | S9 | Medium | Confirmed | F-9 per-position power curve `base.powDown(e)` (e=0.25 Era0 / ⅓ Era1+, L149-152) is subadditive, so splitting one LP position across N addresses multiplies summed `governanceWeight` by ≈N^(1-e) (N^¾ Era0 / N^⅔ Era1+) at only gas+time cost — defeating the F-9 anti-plutocracy dampening and amplifying governance capture (compounds F-01). | Concavity x^e (e<1, f(0)=0) is subadditive: Σf(pieces) ≥ f(Σ); value conserved across split (`tvl·Σlp_i/totalLP = tvl·lp/totalLP`, L142) but summed power amplified; cliff (L137) / on-ramp (L144-147) / gauge-gate (L133) identical across pieces so they cancel; concavity applied per-(pool,holder) summed per holder (poke L110-112), no cross-address aggregation (impossible on-chain w/o identity). PoC: 16-split → ≈8×, 81-split → ≈27× the weight of one consolidated position of identical total LP. Bounded by per-address gas, 14-day cliff (real non-flash capital), value/base dust floor (L143/L148); no on-chain Sybil resistance; bounded blast radius (no theft, reversible). Fix: apply the root to pool aggregate value distributed pro-rata by LP share (split-invariant), or accept the known concave-voting Sybil tradeoff. Intent (F-9 spec acceptance, `11_formulas.md` F-9 / I-D7) → WH-F + spec check. RESOLVED (a5a3a1b + aumm-site 50e21a5) — spec-first per the §12 ambiguity-gate: canonical F-9 rewritten to the pool-aggregate root (aumm-site `11_formulas.md` section swap + 04_tokenomics/10_constitution/12_glossary/17_faq consistency sweep, 50e21a5), then `VotingWeight._positionPower` rewritten to `tvlEMA(pool)^era-root × (userLP/poolTotalLP) × min(time,ON_RAMP)/ON_RAMP` — the holder leg is linear, so wallet-splitting is weight-invariant by construction (rounding-down sums split pieces to consolidated-minus-dust: 16-split −12 wei, 81-split −88 wei observed). Subadditivity migrates to the gauge-gated pool axis (≤ roster size, requires real diversified LP — incentive-aligned, spec-accepted residual). Spec also locks: time factor linear outside the root; share denominator = total LP (forfeited weight vanishes). PoC flipped from proving the ≈8×/≈27× gain to proving invariance (`test_F02_Split16/81_WeightInvariant_NeverGains` + baseline, 3/3 green); unit recalibration 2 literals (32/32); F04 ratio suite stable (4/4); regression 778/778 unit + 94/94 fork. | Fixed | `a5a3a1b` + aumm-site spec `50e21a5` (no substrate seam) | `test/whitehat/F02_concaveSplitSybil.t.sol` (3/3) |
| F-03 | 2026-06-12 | WK.17 | `script/DeployStageK.s.sol` | `_wire` | S10 | Low | Confirmed | The K7 deploy `_wire` (8 wiring calls, L147-167) never invokes `tvlOracle.setMiliariumRegistry(miliariumRegistry)`, so the K6 Miliarium enumeration leg stays permanently dormant in production and the deployer-pinned one-shot `registrySetter` is never sealed — a deviation from K-D8 "registry bound post-deploy at K7". | K6 (WK.15) added `setMiliariumRegistry` (one-shot self-seal, `registrySetter`-gated, `TVLOracle.sol` L193) + the Leg-2 dense enumeration in `_constellationRatio` (L226-234), guarded by `registryBound = address(miliariumRegistry) != address(0)` (L211). K-D8 specifies binding "post-deploy at K7" and `TVLOracle.sol` L50 NatSpec says bound "at the Stage K (K7) handoff", but the materialized K14 wiring inventory enumerates 8 calls (setVotingWeight / addAuthorizedDonator / 2× setMintRouter / setMinter / 2× setGovernanceContract / setAuthorizer) and `_wire` implements exactly those 8 — `setMiliariumRegistry` is in neither; grep confirms ZERO `tvlOracle.setMiliariumRegistry` calls across `script/` and the K fork tests. Run as-is, `tvlOracle.miliariumRegistry()` stays `address(0)`, Leg 2 is skipped (L226 "dormant until the registry is bound"), and `_constellationRatio` runs the pre-K6 reverse-map leg only → K6 valuation densification is inert and gauged Miliarium pools under-weight their LPs in `VotingWeight.tvl(pool)`. Security corollary: `registrySetter` (L54, deployer-pinned, self-zeros only on first bind) stays unsealed — a lingering one-shot privileged setter; deployer-key compromise before any bind lets an attacker bind a malicious `IMiliariumRegistry` skewing `_constellationRatio` → `tvl()` → governance weight + CCB emission (compounds the WK.15 SUSPECTED spot-tvl item; the K6 seal was the intended closure). Bounded — no value-theft path, valuation-accuracy/liveness only, recoverable while the deployer holds the unsealed setter, and `addConstellationPool` roster population is itself deferred so launch `tvl()` is approximate regardless. Disposition routed (build-line decision, F-01/WK.13 pattern): CODE FIX = add a 9th `_wire` call `tvlOracle.setMiliariumRegistry(IMiliariumRegistry(env MILIARIUM_REGISTRY))` (registrySetter-gated not governance-gated, order-independent of the handoff; needs K governor == the Stage-H TVLOracle deployer, which holds in the single-GOVERNANCE_MULTISIG chain) + a deploy-replay regression asserting `tvlOracle.miliariumRegistry() == MILIARIUM_REGISTRY` post-deploy; ALTERNATIVELY a documented conscious deferral pairing the bind with the deferred `addConstellationPool` roster-population stage. RESOLVED (1e84bfb) — `_wire` gains a 9th call `tvlOracle.setMiliariumRegistry(IMiliariumRegistry(env MILIARIUM_REGISTRY))` after the gauge+Miliarium handoff, before `setAuthorizer` (L166-167): the K6 Leg-2 enumeration binds (`registryBound==true`) and the one-shot `registrySetter` self-seals on deploy. Regression `test_F03_tvlOracleMiliariumRegistryBoundAndSealed` asserts `miliariumRegistry()==MILIARIUM_REGISTRY` + `registrySetter()==address(0)` post-deploy; `DeployStageK` fork suite 6/6. Script + fork test only, no src contract change. | Fixed | `1e84bfb` (no substrate seam) | `test/fork/DeployStageK.t.sol` (6/6) |
| F-04 | 2026-06-12 | WK.18 | `src/governance/VotingWeight.sol` | `_positionPower` / `poke` (consuming `TVLOracle.tvl`) | S7 | Low | Confirmed | `_positionPower` consumes `ORACLE.tvl(pool)` as an atomic SPOT read (L142; `TVLOracle.tvl` L272-283 = live `balancesLiveScaled18 × _constellationRatio`, no oracle-side EMA/TWAP), so a same-tx spot-tvl pump inflates `governanceWeight` with zero change to the hook-gated recorder position — structurally manipulable, though the F-9 power root caps the gain at the 4th/3rd root of the pump. | `value = ORACLE.tvl(pool) × userLP/poolTotalLP` (L142); the recorder shares (L138/L140) are hook-gated (`recordDeposit`/`recordWithdrawal`, `onlyAuMTContract`) and non-flash-movable (OQ-25 cancels the LP-share leg — a normal add-liquidity raises tvl AND poolTotalLP), but `tvl(pool)` is a SPOT read with the EMA living consumer-side (EMASampler, Stage F) NOT in `tvl()`. So spot-tvl is the lone atomically-manipulable input: an attacker holding a qualified position pumps `tvl(pool)` in the same tx as `poke`/`castVote` (flash-swap skewing a thin constellation venue's `balZ/balU` per `_venueRatio` L247-264, or a one-sided DONATION raising the target's balances), captures inflated `governanceWeight`, unwinds. PoC `test/whitehat/F04_spotTvlManipulation.t.sol` 3/3 green proves BOTH halves on the K3 mock harness with a share-1 qualified position (base == tvl): (1) a pure spot-tvl change with ZERO recorder change moves weight (`assertGt(w16, w1)` — the structural exposure), and (2) the F-9 root self-limits it — 16× spot-tvl → ~2× weight Era0 (`powDown(0.25e18)`, 16^¼=2), 8× → ~2× Era1+ (`ONE/3`, 8^⅓=2), 10000× → ~10× (the quartic ceiling at extreme pump). Economically self-limiting: weight-gain = (tvl-pump)^¼ and every pump costs ≥ the pumped value (DONATION 1:1 permanent; flash-SWAP ≥ round-trip slippage+fees) → quartically sublinear ROI. Residual marginal threat = a THIN-venue oracle pump in a SPARSE constellation roster (few `_constellationRatio` averaging venues + deep target) where the slippage-to-gain ratio improves — bounded but not killed, and COMPOUNDS F-01 (quorum-gaming) + F-02 (Sybil-split) since the prize is governance capture not token profit. Bounded: no theft, no flash weight (14-day cliff), recorder-share honesty, ~16-day vote+timelock public contest. Supersedes the WK.15 "if confirmed → F-03" forward-ref (F-03 taken by the WK.17 deploy-omission). Disposition routed (build-line/WH-H decision, F-01/WK.13 pattern): CODE FIX = oracle-side EMA/TWAP on `TVLOracle.tvl` (the H-D9 spot-vs-EMA design — Stage-H, not K-diff → WH-H) and/or a VotingWeight-side smoothed/checkpointed tvl read; ALTERNATIVELY documented Accepted-risk given the quartic self-limiting + F-01/F-02 contest bounds. Full fork donation/swap sim (R1-blocked: pilots have no svZCHF path on the fork) → Stage-P sweep. RESOLVED (c283371 + 3d80ac8) — VotingWeight._positionPower now reads the 60-day `EMASampler.tvlEMA` (not spot `ORACLE.tvl`) behind an `EMA_MATURITY_BLOCKS = 60 × BLOCKS_PER_DAY` seed-age gate: a pool whose TVL EMA is unseeded (`emaSeedBlock == 0`) or younger than 60 days confers zero governance weight, closing both the atomic spot-pump and the F-D15 cold-start seed-to-spot window; a mature EMA still carries the F-9 quartic bound as defense-in-depth. The fix added `emaSeedBlock` first-seed exposure to EMASampler/IEMASampler (additive, Stage-F fix-forward, I13-class — no re-tag). The F04 PoC was flipped from proving the pump to proving the gate (`test_F04_FreshlySeededEmaPump_ConfersZeroWeight` + 3 more, 4/4 green). No substrate seam. | Fixed | `3d80ac8` (no substrate seam) | `test/whitehat/F04_spotTvlManipulation.t.sol` (4/4) |
| F-05 | 2026-06-14 | WK-R.5 | `src/governance/VotingWeight.sol` consuming `src/ccb/EMASampler.sol` | `_positionPower` (consumer) / `EMASampler.updateEMA` seed path | S7 | Low | Suspected | The F-04 EMA-maturity gate (`VotingWeight` L136-138) checks seed AGE (`block.number - emaSeedBlock >= 432_000`), not value freshness; `lastEMAUpdateBlock` is read by NEITHER consumer (`VotingWeight` grep 0; `CCBMultiplier` body grep 0 — only the L77 NatSpec; `IEMASampler` L16 "reserved for a future stale-EMA guard at Stage H", unimplemented). An inflated EMA — raw seed (`EMASampler` L138-141, smoothing-bypassed) or sustained multi-sample skew — confers quartically-bounded amplified governance weight once the 60-day AGE gate matures. | The seed path assigns `spotTVL` raw (donation / add-liquidity / swap-pumpable via `TVLOracle.tvl` L274-283 live `balancesLiveScaled18`); the consumer gates only on seed age (`VotingWeight` L138), so a stale inflated seed survives to maturity. Permissionless daily re-sampling is a weak mitigation: (59/61)^60 = 0.135, so 13.5% of an inflated seed survives 60 honest daily samples (half-life ~21d ≈ the 60d gate). Sustained-skew converges (steady state of (2·spot+59·old)/61 is EMA→spot; 60 daily pumped samples drive EMA 86.5% toward the pump). Neither variant is covered by the OQ-5a-bis single-block-spike acceptance (`EMASampler` L14-18): the seed path bypasses the cited smoothing, and multi-sample skew converges toward — not away from — the pumped value. Bounded LOW by the ENFORCED F-9 quartic root (`VotingWeight` L158 `ema.powDown(0.25e18)`: 100×→3.16×, 10000×→10× weight), the flash-immune recorded-share leg (OQ-25, L149), the 60-day real-capital commitment, relative-only advantage over other-pool holders (same-pool holders share the inflation), veto = blocking power not theft, and the opposing emission counter-incentive (`CCBMultiplier` L268 deltaIntra penalty on a high EMA). Cross-cutting: the freshness gap is present in both `tvlEMA` consumers; the emission consumer is self-correcting, the governance consumer is one-directional → governance-scoped. Disposition routed: CODE FIX = a `lastEMAUpdateBlock` freshness gate in `_positionPower` (and/or seed-path smoothing/anchor); ALTERNATIVELY documented Accepted-risk given the quartic self-limiting. RESOLVED (`d5cecb3` i + `70eb276` ii) — consumer-side freshness gate in `_positionPower`: `EMA_STALENESS_BLOCKS = AureumTime.BLOCKS_PER_EPOCH` (14 days / one epoch) + `if (block.number - EMA_SAMPLER.lastEMAUpdateBlock(pool) > EMA_STALENESS_BLOCKS) return 0;` after the seed-AGE maturity gate (`VotingWeight` L167) — `EMA_MATURITY_BLOCKS` guards seed AGE, this guards seed FRESHNESS, so a never-refreshed or pumped-then-abandoned seed (incl. the WK-R.6 100%-inflated pre-approval dormant-seed window) is structurally stale → 0 the instant the 60-day clock matures; `lastEMAUpdateBlock == 0` is fail-closed. Chosen over the seed-path smoothing/anchor alternative (minimal fail-closed surface, `EMASampler` byte-untouched). PoC `test/whitehat/F05_emaFreshnessGate.t.sol` (4/4): stale mature→0, fresh mature→weight, strict-`>` boundary pinned; 784 non-fork green. Fix-forward on `stage-l` (I13-class, no Stage-K re-tag). | Fixed | `d5cecb3` + `70eb276` (no substrate seam) | `test/whitehat/F05_emaFreshnessGate.t.sol` (4/4) |
| F-06 | 2026-06-14 | WK-R.8 | `src/governance/AureumGovernance.sol` | `_voteSucceeded` / `state` / `queue` / `execute` (consuming `VotingWeight.totalSupply`) | S9 | Medium | Suspected | F-01's fix made the quorum denominator a LIVE `VOTING_WEIGHT.totalSupply()` read at tally (L294), but `forVotes`/`againstVotes` FREEZE at `endBlock` (castVote window L254) while `state`→`_voteSucceeded` re-reads the denominator live on every `queue`/`execute` (L276); an adversary holding DORMANT (un-poked) matured qualified LP pokes it into `_totalQualifiedWeight` AFTER `endBlock`, inflating the denominator past the frozen turnout's 20% bar → an otherwise-passing proposal griefed to Defeated/Expired. | Frozen-numerator-over-live-denominator asymmetry post-`endBlock`: `castVote` reverts `ProposalNotActive` once `block.number > endBlock` (L254) so `totalVotes` is final, but `_voteSucceeded` reads `VOTING_WEIGHT.totalSupply()` (= `_totalQualifiedWeight`, VotingWeight L97-99) live at every `state()` call (L276) and `poke` permissionlessly grows the accumulator at any block (VotingWeight L109-124). Attack: pre-position matured qualified LP, hold it DORMANT (never poked → absent from the accumulator), then post-`endBlock` poke it in so `totalVotes*10_000 < totalSupply()*2_000` (L294) → `_voteSucceeded` false → `state` short-circuits Defeated BEFORE the eta branches (L276 ahead of L277-279), so even a `queue`d proposal (eta set L315) reverts to Defeated and `execute` reverts `ProposalNotSucceeded` (L330); holding the inflation through `eta+EXECUTION_GRACE_BLOCKS` (L278) Expires it → a passed vote nullified (governance liveness/outcome integrity, no fund theft). Cost gating (→ Medium not High): the SAME real-matured-stake gate as F-02 — `poke` only adds weight for LP clearing `_positionPower`'s gauge-approval (VotingWeight L135) + 60-day-matured EMA (L138) + 14-day cliff (L142) + real recorder LP (L143-146); NOT a flash/spot attack. But the gate raises PRICE not closes the vector: (1) capital is RECOVERABLE (LP kept, unlike the burned proposal deposit L198-199) → opportunity-cost + gas, repeatable; (2) the strict `<` quorum (L294) kills a marginal (~20% turnout) proposal on an infinitesimal bump and a 25%-turnout one on ~25% accumulator inflation; (3) the natural adversary is the incumbent large LP of the challenged pool — exactly who holds dormant qualified stake, and the F-9 sublinear root `tvlEMA^e` (e=1/4 Era0 / 1/3 Era1+, L155-158) concentrates weight in few high-TVL pools where a whale dominates the addable denominator (pool-aggregate weight capped at `tvlEMA^e`, split among LPs by share). Symmetric weaker face — deflate-to-pass: withdraw + `poke` self down to lower the bar for a majority-but-sub-quorum proposal, floored by other holders' weight (cannot fabricate forVotes post-`endBlock`), milder. LINEAGE: NOT a regression — pre-F-01 the propose-time `snapshotTotalSupply` (L201/L204) was immutable so this vector did not exist; F-01's live read (correct for the >100%-turnout bug WK-R.2 re-proved) OPENS this inverse vector as a side effect, causally downstream of the fix, not an F-01 re-open. Confirms the WK-R.4 forward-ref (`state()` live-denominator block-via-pump → WK-R.8). Disposition routed (build-line, F-01 pattern): CODE FIX = snapshot the quorum DENOMINATOR at `endBlock` (pull-based lazy capture on the first post-`endBlock` `state()`, or an explicit `finalizeTally`), freezing numerator AND denominator at the tally boundary — reverting to propose-time `snapshotTotalSupply` reintroduces the F-01 bug, so the endBlock-time capture is the correct alignment; ALTERNATIVELY documented Accepted-risk given the real-stake gate + root-capped cost + ~16-day public contest. RESOLVED (97131f8 iii+iv + 427187a v, after 9b57d9c i + 8fdd476 ii) — Governor-style snapshot voting: a pre-vote `snapshotBlock` (`start + VOTING_DELAY_BLOCKS`) freezes BOTH the numerator (`getPastVotes(voter, snapshotBlock)` in `castVote` L260) and the denominator (`getPastTotalSupply(p.snapshotBlock)` in `_voteSucceeded` L300) at one OZ `Checkpoints.Trace208` block, so a post-`endBlock` `poke` moves only the live accumulator, never the snapshotBlock checkpoint the tally reads — closing the inflation grief AND the symmetric deflate-to-pass face and converting F-01's live read into a structural numerator≤denominator invariant; SUPERSEDES the e6d9b3d live-read denominator (F-01) and the WK-R.8 endBlock-only sketch (`ProposalState.Pending` appended, `poke` dropped from `castVote`). PoC `test/whitehat/F06_postEndBlockDenominatorFreeze.t.sol` (block-aware mock, 2/2) proves both faces; F01 suite re-pointed to the snapshot model (5/5); 780 non-fork green. Fix-forward on `stage-l` (I13-class, no Stage-K re-tag). | Fixed | `97131f8` + `427187a` (no substrate seam) | `test/whitehat/F06_postEndBlockDenominatorFreeze.t.sol` (2/2) |
| F-07 | 2026-06-17 | WL.1 | `src/incendiary/IncendiaryRegistry.sol` | `_maturePrice` (consumed by `_valueInAuMM` / `buyBoost`) | S7 | Low | Confirmed | `_maturePrice` (L332-339) enforced only the F-04 seed-AGE maturity gate (`seedBlock==0`→EMANotMature L334; `age < EMA_MATURITY_BLOCKS`→EMANotMature L336) and NOT sample-FRESHNESS, so a mature-but-stale der-Bodensee price-EMA rail (seasoned ≥60d but `lastSampleBlock` far behind `block.number`) priced every `buyBoost` deposit→entitlement valuation off a stale stored EMA indefinitely — the IncendiaryRegistry sibling of the F-05 `VotingWeight` freshness gap. | `_maturePrice` never read `block.number - rail.lastSampleBlock`; `lastSampleBlock` advances only on permissionless `updateRailEMA` (L317, cadence ≥ BLOCKS_PER_DAY). Once a rail's seed seasons the 60-day `EMA_MATURITY_BLOCKS`, an inflated-or-merely-stale EMA priced `buyBoost` (L268) via `_valueInAuMM` (L411), BEFORE the deposit `safeTransferFrom` payment pull, at a stale rate until someone re-sampled. Unlike F-05's quartically-damped governance weight, the boost valuation is rate-scaled-LINEAR off the EMA, so a stale EMA maps deposit value to entitlement at the stale price directly. Bounded LOW by the 5% anti-gaming haircut, the F-04 maturity gate (no fresh-seed pump), the L-D6 15%-of-epoch aggregate boost cap (per-epoch over-entitlement ceiling), the real svZCHF/sUSDS deposit cost donated to der Bodensee (L-D2 — buyer pays real value), relative-only mispricing (vs current spot, not theft), and permissionless re-sampling. Fix: consumer-side freshness gate mirroring F-05 — `EMA_STALENESS_BLOCKS = AureumTime.BLOCKS_PER_EPOCH` (14d / one epoch, L47) + `if (block.number - rail.lastSampleBlock > EMA_STALENESS_BLOCKS) revert EMAStale(payToken, staleness, EMA_STALENESS_BLOCKS)` (L337-338) after the maturity gate, fail-closed (a never-refreshed seed is structurally stale the instant the 60-day clock matures). Test retrofit (F-07.2) added a fresh re-sample before each pre-existing mature-rail buyBoost/extMaturePrice test + a `test_buyBoost_revert_emaStale` negative. PoC `test/whitehat/F07_emaStalePricing.t.sol` (4/4): deeply-stale-mature reverts, fresh-mature prices, strict-`>` epoch boundary pinned. 825/825 unit + 99/99 fork green. Fix-forward on `main` (Stage-L add-on, I13-class, no re-tag). | Fixed | `0a21b99` + `b96227c` (no substrate seam) | `test/whitehat/F07_emaStalePricing.t.sol` (4/4) |
| F-08 | 2026-06-17 | WM.1 | `src/gauge/VaultClassRegistry.sol` | `vetoProposal` | S9 | Medium | Suspected | `vetoProposal` (L273-284) accumulates `proposal.vetoSupport += votingWeight.governanceWeight(msg.sender)` on every call with NO per-voter dedup — there is no `hasVetoed` mapping anywhere and the `VaultClassProposal` struct (L52-66) carries no per-voter tracking, so one address can call repeatedly to stack its own weight arbitrarily and cross the 10% veto bar (`VETO_THRESHOLD_BPS = 1000`, L37/L279) alone, auto-revoking a vault-class proposal. | `governanceWeight(msg.sender)` (L277) is a pure live read returning the same value each call; `vetoSupport` is monotonic with no idempotency guard, so N calls contribute N×weight and any holder with nonzero qualified weight reaches the 10% bar in a handful of repeated calls, tripping L279-282 (`finalized = revoked = true`) — killing the proposal (and revoking the class if previously admitted). Governance griefing / veto-threshold bypass, not theft (INV-5 — blocking-power, not value extraction); bounded to the vault-class 52%-Quality-Gate admission lever (G-D8/G-D9) and a 14-day-cliff-qualified position, but the per-veto cost is ~gas only and repeatable. Disposition (CODE FIX): add `mapping(uint256 => mapping(address => bool)) hasVetoed` + an `AlreadyVetoed(proposalId)` revert (one veto per address per proposal); a snapshot/checkpointed veto weight (F-06 precedent) is an optional defense-in-depth refinement. RESOLVED (`5cc5f3f`) — `vetoProposal` now dedups one veto per address per proposal: `mapping(uint256 => mapping(address => bool)) public hasVetoed` (L75) + `AlreadyVetoed(proposalId)` (L140), checked at L282 and set at L283 BEFORE the `vetoSupport` accumulation, so a repeat call from the same holder reverts and no single sub-threshold address can stack its own weight across the 10% `VETO_THRESHOLD_BPS` bar; legitimate multi-voter accumulation and an at-or-above-threshold single-holder kill are preserved. Took the dedup-mapping disposition (the snapshot/checkpointed veto-weight refinement noted optional, deferred). PoC `test/whitehat/F08_vetoStacking.t.sol` (4/4) pins the repeat-veto revert + the preserved legitimate paths. Fix-forward on `stage-m` (Stage-G add-on, I13-class, no Stage-G re-tag); 847 unit + 99 fork green. | Fixed | `5cc5f3f` (no substrate seam) | `test/whitehat/F08_vetoStacking.t.sol` (4/4) |
| F-09 | 2026-06-17 | WM.2 | `src/fee_router/AureumFeeRoutingHook.sol` | `onAfterAddLiquidity` / `onAfterRemoveLiquidity` | S8 | Medium | Suspected | The I-D14 recorder dispatch resolves the LP as `lp = IRouterSender(router).getSender()` (L371/L396) under only `onlyVault` + a recorder-unset guard, with NO trusted-router allowlist (grep 0); `getHookFlags` enables `shouldCallAfterAddLiquidity`/`shouldCallAfterRemoveLiquidity` (L296-297) so the Vault fires these for ANY router, and `script/DeployStageI.s.sol:111` binds the hook itself as `auMTContractByPool[pool]` so the hook's `recordDeposit`/`recordWithdrawal` pass `EmissionDistributor.onlyAuMTContract` (L479/L513) — an attacker acting as its own router (the documented production pattern, `test/fork/StageIIntegration.t.sol:102`) supplies an arbitrary `getSender()`, decoupling emission/qualification accounting from BPT custody. | The after-liquidity callbacks receive no BPT recipient, so the only identity signal is the attacker-controlled `router`/`getSender()`/`userData`. The deposit path credits emission accrual + the I-D14 `effectiveQualBlock` governance-qualification clock (`EmissionDistributor` L491-499) to any chosen address holding no BPT; the withdrawal path can target a victim — `recordWithdrawal(pool, victim, amt)` decrements `userLP[pool][victim]` and resets `effectiveQualBlock[pool][victim] = 0` (L524), griefing the victim's qualification. No direct fund theft (value misallocation + griefing), but the most impactful of the WM trio. Architecturally entangled: there is no trusted router today (Aureum's Router deferred to Stage O, D33; every LP is its own router), so a getSender-trust gate has nothing to allowlist yet. Disposition (CODE FIX, chosen): a governance-settable trusted-router allowlist — the hook skips the recorder dispatch for any non-allowlisted router (fail-closed); the Stage O Aureum Router is allowlisted when it ships, recording dormant until then. Fix design to be locked in a §12 ambiguity-gate section before implementation. RESOLVED (`fc023ee`) — the I-D14 recorder dispatch in `onAfterAddLiquidity`/`onAfterRemoveLiquidity` is now gated on a governance-managed `trustedRouter` allowlist: `if (recorder != address(0) && trustedRouter[router])` (L387/L413) skips the `getSender()`-resolved credit for any non-allowlisted (e.g. self-)router — fail-closed, no credit and no revert — so a self-router cannot spoof LP identity into the emission / qualification clock. `setTrustedRouter(router, trusted)` (L299, `governanceModule`-gated, persistent not one-shot) + a `TrustedRouterSet` event are the governance lever; the `trustedRouter` mapping sits in the last storage slot (6) to preserve the pinned C-D11 / I-D16 admin slots 3—5. The Stage O Aureum Router (D33) is allowlisted when it ships; until then the allowlist is empty and the dispatch is dormant. `StageIIntegrationFixture` retrofitted to bind `governanceModule` + self-allowlist its self-router, restoring the StageI/K/L recorder-through-hook assertions. PoC `test/whitehat/F09_routerSpoofRecorderAttribution.t.sol` (7/7): self-router spoof skipped on add+remove, allowlisted router credits the resolved LP, access-control + event + revoke. Fix-forward on `stage-m` (Stage-I add-on, I13-class, no Stage-I re-tag); 847 unit + 99 fork green. | Fixed | `fc023ee` (no substrate seam) | `test/whitehat/F09_routerSpoofRecorderAttribution.t.sol` (7/7) |
| F-10 | 2026-06-17 | WM.3 | `src/emission/EmissionDistributor.sol` | `recordScore` (consuming `EMASampler.tvlEMA`) | S7 | Medium | Suspected | `recordScore` (L450-454) reads `_emaSampler.tvlEMA(pool)` straight into `CCBScore.score(...)` with NO maturity and NO freshness gate (no `EMA_MATURITY_BLOCKS` / `EMA_STALENESS_BLOCKS` / `seedBlock` anywhere in the file), unlike the F-04/F-05 consumer `VotingWeight._positionPower` (L164-167) which gates the same EMASampler on seed-exists + 60-day maturity + 14-day freshness — the EmissionDistributor sibling of F-05/F-07. | An inflated or cold-seeded `tvlEMA` (the F-04 spot-pump-at-seed / F-05 stale-seed class — `EMASampler.updateEMA` raw-seeds spot on cold start, gauge activation permissionless) flows straight into the F-5 score → `poolScore` → `totalScore` emission allocation, diverting AuMM block emission to the attacker's pool. Unlike F-05's quartically-damped governance weight, the emission score is linear in `tvlEMA` (no F-9 root), so the mispricing is undamped → Medium (above the F-05/F-07 Low). Bounded by gauge approval, the 21M cap, and same-pool-LP dilution. Disposition (CODE FIX): mirror the F-05 gate in `recordScore` — treat `tvlEMA(pool)` as 0 when `emaSeedBlock == 0`, `block.number - emaSeedBlock < EMA_MATURITY_BLOCKS`, or `block.number - lastEMAUpdateBlock > EMA_STALENESS_BLOCKS`, so the pool scores 0 until its EMA is seasoned + fresh; `IEMASampler` already exposes both accessors (L19/L25). RESOLVED (`609335e`) — `recordScore` now reads the TVL EMA through `_gatedTvlEMA(pool)` (L456): returns 0 when unseeded (`emaSeedBlock == 0`), immature (`block.number - emaSeedBlock < EMA_MATURITY_BLOCKS`, 60d), or stale (`block.number - lastEMAUpdateBlock > EMA_STALENESS_BLOCKS`, 14d / one epoch), so `CCBScore.score` collapses the pool's F-5 score to 0 until its EMA is seasoned + fresh — the EmissionDistributor mirror of the F-04/F-05 `VotingWeight._positionPower` gate. Degradation is return-0 (not revert), so a stale pool's stored score self-clears on the next permissionless `recordScore`; `IEMASampler` already exposed both accessors. Took the consumer-side gate (EMASampler byte-untouched). PoC `test/whitehat/F10_emaScoreGate.t.sol` (7/7) pins unseeded / immature / stale → 0 and seasoned+fresh → score; `DeployStageK` fork retrofit. Fix-forward on `stage-m` (Stage-H add-on, I13-class, no Stage-H re-tag); 847 unit + 99 fork green. | Fixed | `609335e` (no substrate seam) | `test/whitehat/F10_emaScoreGate.t.sol` (7/7) |
| F-11 | 2026-06-26 | WN | `src/rate_provider/ERC4626RateProvider.sol` + `src/rate_provider/CompositeRateProvider.sol` | `constructor` (both) | S4 | Low | Confirmed | Both Aureum-owned RP constructors assigned the vault immutable with NO decimals check, and `getRate()` returns `previewRedeem(FixedPoint.ONE)` (the composite ×`.mulDown(underlyingRateProvider.getRate())`) with ZERO decimal scaling — a faithful 18-decimal Balancer rate only when the vault's shares AND its underlying asset are both 18-decimal; a non-18-decimal pairing (e.g. the EURS / Aave-stata class deferred at ixCambio per M-D7 / M-D11) silently produces a wrong-scale rate that still clears the deploy-time `getRate() > 0` liveness gate. | `previewRedeem(FixedPoint.ONE)` is an 18-dec fixed-point assets-per-share ONLY if shares and asset are both 18-decimal; for a 6-dec underlying the redemption value is scaled 10^6 not 10^18 — a 10^12 mis-statement of the rate. The deploy-time `getRate() > 0` gate (the gate that caught the N-D9 sfrxUSD zero-rate bug) checks LIVENESS, not SCALE, so a wrong-scale-but-nonzero rate passes silently. Confirmed-but-latent: all five current consumers are 18/18 (scrvUSD/06 N-D2; sfrxETH + wOETH/ixAetheron-02 N-D8; yBOLD inner-leg + ysyBOLD wrapper across 06/20/27 N-D9), so no live bug — the exposure is forward-looking, for the deferred non-18 classes (M-D7 / M-D11 ixCambio). Bounded: fail-closed at construction (a wrong-decimals deploy reverts; never a wrong rate at runtime). Disposition (user-adjudicated): fail-closed constructor guard, over a scale-normalizing `getRate()` or a deploy-script scale assertion. RESOLVED (`ca088eb` WN.2a + `58581b9` WN.2b) — both constructors now check `wrappedToken_`/`wrapper_.decimals() == 18` (else `InvalidShareDecimals(uint8)`) AND `IERC20Metadata(asset()).decimals() == 18` (else `InvalidAssetDecimals(uint8)`) BEFORE assigning the immutable; `getRate()` math is byte-unchanged — a pure deploy-time precondition. The composite's `underlyingRateProvider_` leg is intentionally NOT decimal-checked (an `IRateProvider` returns an 18-dec FixedPoint rate by Balancer convention, not an ERC-4626/ERC-20 with its own decimals). WN.1 (`5a3a40d`) retrofitted the N3.3 unit mocks (`MockPreviewVault` gains `decimals()=18` + `asset()`) so the guard passes; WN.4 = Slither 0.11.4 clean on both contracts (zero results) + manual danger-keyword sweep clean. PoC `test/whitehat/F11_decimalsGuard.t.sol` (6/6): four guard-revert paths (share≠18 / asset≠18 on each contract, pinning the exact `InvalidShareDecimals(6)` / `InvalidAssetDecimals(6)` selectors) + two 18/18 happy-path construction pins. Fix-forward on `stage-m` (Stage-N surface, no re-tag); 11/11 unit + 6/6 PoC green. | Fixed | `ca088eb` (WN.2a, ERC4626) + `58581b9` (WN.2b, Composite) (no substrate seam) | `test/whitehat/F11_decimalsGuard.t.sol` (6/6) |
| F-12 | 2026-06-29 | WO | `src/gauge/GaugeEligibility.sol` | `meetsCompositionQualityGate` | S9 | Low | Confirmed | The Stage-O composition Quality Gate computed the 52% numerator from pool-reported `IWeightedPool.getNormalizedWeights()` but skipped the `isPoolFromFactory` provenance check (O-D2 deliberately narrowed it away alongside the TVL floor); since `AureumFeeRoutingHook.onRegister` (L318-330) accepts any non-Bodensee pool, a hand-rolled non-factory contract carrying the canonical hook could report faked weights and spoof the ≥52% bar. | The hook check (O-D2a) only requires the canonical `feeRoutingHook`, which the permissive `onRegister` lets ANY pool carry; `getPoolTokens` is the Vault's real registered set, but `getNormalizedWeights()` is self-reported by the pool contract — a genuine Aureum-factory `WeightedPool` returns immutable real weights, a hand-rolled contract returns whatever it likes. Attack: deploy a custom contract, register it with the canonical hook + real tokens (e.g. 10% admitted-4626 / 90% junk), implement `getNormalizedWeights()` to claim ≥52% admitted, pass the gate, propose it as a composition replacement. Bounded LOW by the 2/3 tessera-weighted supermajority (harm needs honest voters to approve the spoofed pool TRUSTING the gate's verdict — but that programmatic assurance is exactly the gate's purpose, so a spoofable gate misleads informed voting) + the 1,000-svZCHF non-refundable deposit + composition rarity/scrutiny. Root cause: O-D2 over-applied §xxvii L55 ("no permissionless-activation criteria check is run") to drop factory provenance with the TVL floor — TVL-skip is justified (a fresh replacement has no TVL), factory-skip is not (provenance is what makes the weight read trustworthy). Disposition (user-adjudicated — fix at the gate over the hook-onRegister-restriction or accept-and-document alternatives). RESOLVED (`b7d64a6` WO.1-3) — `meetsCompositionQualityGate` adds `if (!IBasePoolFactory(approvedFactory).isPoolFromFactory(pool)) revert PoolTypeNotWhitelisted(approvedFactory)` after the numerator (L386, mirroring `_checkEligibilityCriteria` L368), so only genuine Aureum-factory weighted pools (whose `getNormalizedWeights` is the immutable real config) clear the gate; it now skips ONLY the TVL floor + anti-spam fee. O4 retrofit (`_wireCompositionPool` sets factory-true; the `…IgnoresTvlFloorAndFactoryWhitelist` narrowness test inverts to `…IgnoresTvlFloor`) + O5 retrofit (both fork candidate-mock helpers add the `isPoolFromFactory` mock). The gate runs in STATICCALL (view) so a malicious candidate cannot reenter-mutate; it guards the only `replaceSlot` chokepoint (`onlyGovernance`, sole caller `AureumGovernance._executeProposal`, always gates first) at both propose (pre-deposit) and execute (pre-mutation). PoC `test/whitehat/F12_compositionGateProvenance.t.sol` (1/1): faked 100%-admitted weights clear the bar with provenance true, the same pool reverts `PoolTypeNotWhitelisted` with provenance false. Pre-tag finding+fix on `stage-o` (Stage-O surface, no prior tag); 872 unit + 124 fork green. | Fixed | `b7d64a6` (WO.1-3) (no substrate seam) | `test/whitehat/F12_compositionGateProvenance.t.sol` (1/1) |
| F-13 | 2026-07-01 | WH-D.2 | `src/fee_router/AureumFeeRoutingHook.sol` | `_swapExactInFeeTokenToSvZchfViaVault` (L487-504) + `_addLiquidityOneSidedToBodenseeViaVault` (L530-563) | S5 | Low | Confirmed | The hook's two internal fee-conversion legs set `limitRaw: 0` (L494) and `minBptAmountOut: 0` (L552) — no slippage bound — so the per-swap protocol fee routed inside `onAfterSwap` (fee-token → svZCHF → one-sided Bodensee add) is sandwichable: a searcher front-runs to skew `params.pool`, the hook accepts any output, Bodensee receives less svZCHF than fair, the back-run captures the spread. | Value leak from the protocol fee stream to MEV; attacker ROI > 0 in principle. Bounded LOW: each event routes a dust-sized amount (≈ 50% × the pool swap fee on a single trade, per token), so realistic extraction is gas-bounded, though systematic across all swaps. The conversion is settlement-embedded (atomic with the user's swap), so the practical form is a sandwich of the victim's trade where the unprotected fee-rider leaks slightly beyond the victim's own slippage protection. Acknowledged in-code (`AureumFeeRoutingHook.sol` L514-517) and pre-flagged as a Stage-Q surface (`STAGE_D_PLAN.md:L703`); the hook holds no oracle to derive a bound. Disposition (user-adjudicated 2026-07-01): LOG as Accepted-risk, deferred to Stage Q — candidate mitigation is a rate-derived `limitRaw`/`minBptAmountOut` bound, or accept the dust-bound rationale. Not a new discovery — an accepted design trade-off logged for coverage completeness + the Stage-Q tier-1 audit and a quantifying sandwich sim. SPLIT re-adjudication (PB-D9, user-adjudicated 2026-07-13): scope re-ground at HEAD — the WH-D pointers predate F-14 / PB2.1, and the shared `_swapFeeAndDeposit` primitive now has four consumers, not one. (a) onAfterSwap (the per-swap path this row was filed against) — ACCEPTED, 0/0 bounds retained: bounding rejected on circularity (spot-derived bounds move with the same-bundle skew; no lagged oracle for arbitrary fee tokens on arbitrary pools), hot-path gas, and an F-14-class liveness regression (a bound tripping inside settlement-embedded `onAfterSwap` reverts the user's swap); quantified on the mainnet fork (PB2.4d2) — `feeRider = 0.1875e18` (0.375% of a 50e18 swap) and the skew round-trip costs 12× the entire fee-rider prize, fee-negative at every realistic size; severity stays S5 Low on this half. (b) routeYieldFee / routeYieldFeeToHook (the PB2.1 controller entry, added post-audit — the 2026-07-01 dust rationale never covered it) — FIXED: routes an epoch (~14 days) of accumulated yield fees in one public-mempool call, non-dust once governance sets the yield fee; caller-supplied `minDepositTokenOut` + `minBptAmountOut` now enforce natively via the Vault (`SwapLimit` / `BptAmountOutBelowMin`), a tripped bound reverting only the route call (PB-D6 stamp-after-success leaves the epoch unburned). (c) routeGovernanceDeposit / routeIncendiaryDeposit — DORMANT (zero callers; Stage K / L route via `SwapAndDepositToBodensee.donate`, module setters unset by design), threaded with the same two params as future-safe interface hygiene, attested not a wiring commitment. | SPLIT — onAfterSwap Accepted-risk (S5 Low) / routeYieldFee Fixed / dormant entries hardened + attested dormant | SPLIT (PB-D9, `6fc8416`): onAfterSwap 0/0 retained (dust; accepted); routeYieldFee + dormant entries bounded via caller-supplied minDepositTokenOut + minBptAmountOut → Vault-native SwapLimit / BptAmountOutBelowMin — `ebf5aaa` (src: interface + impl + controller pass-through) + `69e77c9` + `1e91e2f` (unit) + `6fc1625` (bounded-route fork witness) + `137f1bd` (onAfterSwap sandwich sim) (no substrate seam) | `test/fork/AureumFeeRoutingHook.t.sol` — bounded-route witness (`test_Fork_RouteYieldFee_minDepositTokenOut_revertsWhenTooTight` + `test_Fork_RouteYieldFee_minBptAmountOut_revertsWhenTooTight` + `test_Fork_RouteYieldFee_swapLegSucceedsAtRealisticBound` + `test_Fork_RouteYieldFee_addLegSucceedsAtRealisticBound`, PB2.4d1) + onAfterSwap sandwich sim (`test_Fork_F13_feeRiderPerSwapIsDust` + `test_Fork_F13_skewCostExceedsFeeRiderPrize`, PB2.4d2; feeRider 0.375% of swap, skewCost 12× the prize) + unit bound coverage in `test/unit/AureumFeeRoutingHook.t.sol` + `test/unit/AureumProtocolFeeController.t.sol` (PB2.4c1/c2); 14/14 fork + 901 unit |
| F-14 | 2026-07-01 | WH-G.2 (carried from WH-D.2) | `src/fee_router/AureumFeeRoutingHook.sol` | `onAfterSwap` → `_swapFeeAndDeposit` (the `tokenOut: SV_ZCHF` conversion on `params.pool`) | S5 | High | Confirmed | `onAfterSwap` converts every accrued fee token → svZCHF by swapping on `params.pool`, which the Vault reverts when svZCHF is not a registered token of that pool — reverting the user's swap; svZCHF is a member of 24 of 26 Miliarium pools (the hub-token convention), but `02 ixAetheron` (native-4626 LST basket) and `06 ixLibertas` (7-token USD hub) omit it, so every fee-charging swap on those two bricks (settlement-critical liveness DoS). | Failure path (deterministic — any honest fee-charging swap, no attacker needed): the pool carries the canonical hook (`getHookFlags.shouldCallAfterSwap`; `onRegister` admits any non-Bodensee pool); a user swap accrues the 50% protocol fee (OQ-1); `onAfterSwap` collects it and, for any non-svZCHF/non-ZCHF fee token, swaps `feeToken → svZCHF` on `params.pool`; the Vault reverts tokenOut-not-registered → the user's swap reverts. Original revert PoC `fa1dbbd`. VENUE WALL: a per-fee-token conversion-venue registry is infeasible — 8 of the 12 distinct tokens across the two pools have no svZCHF-liquid venue anywhere in the constellation (scrvUSD/USDC/PYUSD/USDT; sfrxETH/rETH/weETH/wOETH), and the 4 ixAetheron LSTs appear ONLY in ixAetheron (no first hop for a bridge). Disposition (user-adjudicated, P-D12): TWO-PART — (1) HOOK FAIL-SAFE: `onRegister` caches `poolBodenseeDepositToken` (svZCHF preferred, sUSDS fallback, else `address(0)`); `onAfterSwap` skips collect/convert/route with NO revert (fees accrue in the Vault, governance-withdrawable) when the cache is `address(0)` — a universal backstop for every non-svZCHF pool present or future; (2) sUSDS GENERALIZATION: the conversion + one-sided-add parameterize on the deposit token, so `06 ixLibertas` (holds sUSDS at 14%) auto-routes via its own sUSDS leg with ZERO composition change (der Bodensee = [AuMM, sUSDS, svZCHF] accepts either rail). `02 ixAetheron` (no Bodensee token in-pool) stays on the fail-safe; its accrued fees convert OFF-CHAIN (keeper calls the router, swaps LSTs → svZCHF/sUSDS, donates to Bodensee) — a P-bis ops process with a fee-custody item (`withdrawProtocolFees` forces recipient == hook) to resolve there. Alternatives dropped: the svZCHF config-fix (CHF/ETH FX cost breaking the N-D8 native-4626 identity; svZCHF is admitted-4626 so it would have RAISED the QG — dropped on product grounds) and an on-chain ixEDEL 2-hop bridge (multi-hop + bridge registry + cross-pool coupling + doubled F-13 slippage on the crown-jewel hot path for one pool). F-13's unbounded-internal-leg slippage class unchanged (sUSDS legs inherit it → Stage Q). I13-class fix-forward on `stage-p`; hook is `stage-d`-tagged — no re-tag. 874 unit + 7/7 hook fork + 41/41 StageGIntegration + 3/3 PilotPools + 4/4 CCBEngine green. | Fixed | `1af89f3` (P-D12 (1) fail-safe) + `1decb70` (P-D12 (2) sUSDS generalization) + `fdb158c` (skip test) (no substrate seam) | `test/fork/AureumFeeRoutingHook.t.sol` (`test_Fork_F14_sUsdsRailRoutesToBodensee` + `test_Fork_SwapRoutesFeeToBodensee`) + `test/unit/AureumFeeRoutingHook.t.sol` (`test_onAfterSwap_skipsPoolWithNoBodenseeRail`); revert PoC `fa1dbbd` |
| F-15 | 2026-07-01 | WH-G.4 | `src/gauge/VaultClassRegistry.sol` | `vetoProposal` | S9 | Low | Suspected | `vetoProposal` reads `votingWeight.totalSupply()` LIVE as the 10% veto-threshold denominator (L286) — the F-06 live-denominator pattern that AureumGovernance's Governor-style snapshot fix was NOT propagated to; a proposal-defender can poke dormant qualified weight during the ~28-day (201,600-block) veto window to inflate the denominator and suppress a marginal legitimate veto below the 10% `VETO_THRESHOLD_BPS` bar → force a class admission over community objection. | The threshold `(vetoSupport * 10_000) / votingWeight.totalSupply() >= 1000` is evaluated live per call (L286); `vetoSupport` accumulates across vetoers (persistent) while `totalSupply()` (the VotingWeight poke-accumulator) is grown at any block by permissionless `poke`. To suppress a veto the defender must keep `totalSupply` high enough at EVERY veto call that cumulative `vetoSupport` never crosses 10%, so the attack only bites a THIN veto (few vetoers, marginal 10%) — a broad vetoer coalition out-accumulates the defender. Bounded LOW: `governanceWeight` is EMA-mature + 14-day-cliff + gauge-gated (no flash, real matured stake), the 28-day window is a long public contest, and the admitted class is revocable (`revokeVaultClass`, onlyGovernance) + only matters if a pool then uses it AND clears the 52% gate. LINEAGE: the F-06 sibling — F-06 fixed the identical live-denominator gaming in AureumGovernance's quorum via a snapshotBlock checkpoint, but VaultClassRegistry's veto path was not migrated. F-08's dedup fix re-verified holding at WH-G.4 (`hasVetoed` set before the external `governanceWeight` read). Disposition (user-adjudicated 2026-07-01): Accepted-risk, deferred to Stage Q — snapshot-denominator propagation (checkpointed veto weight, mirroring F-06) is the candidate mitigation; the PoC + fix decision belongs to the Stage-Q formal audit given Low severity + the real-stake/28-day/revocable bounds. ACCEPT re-adjudication (PB-D10, user-adjudicated 2026-07-14): the Stage-Q deferral retires (Stage Q narrowed to the audit engagement at the 2026-07-11 restructure) — Accepted-risk stands standalone on the poke-invariant ground truth: `governanceWeight` returns the poked checkpoint `_holderWeight[holder]` and `poke` maintains `_totalQualifiedWeight == sum of _holderWeight[*]` (`VotingWeight.sol` L110-112 / L24), so any weight enterable into the veto numerator is already a denominator summand — the defender's inflation is capped at fully-poked truth, a coalition at or above 10% of true qualified weight cannot be suppressed by any poke strategy, and a coalition clearing 10% only of an under-counted accumulator was never a G-D19 kill under a fully-poked reading of the 10.00% AuMT-weighted bar; the numerator stays open across the 28-day window with vetoers moving last, so poked-in weight is live veto ammunition (the opposite geometry to F-06's frozen turnout — whose fix design already scoped these live reads out per the F-06 "Scope boundary (confirmed)" paragraph). Both snapshot fixes rejected as regression-bearing: the full transplant disenfranchises every holder not poked pre-creation absent a VOTING_DELAY-class pending window; the denominator-only snapshot opens a zero-checkpoint division-by-zero veto bypass where the live read self-heals. Zero-denominator edge attested (PB-D10 (iv)): a zero-weight veto against an empty accumulator panics and fully unwinds; the first nonzero-weight veto auto-kills at ratio 100% — fail-toward-veto. | Accepted-risk (standalone per PB-D10 — S9 Low; live reads retained by design) | — (no code change per PB-D10 `34e4c27`; both snapshot shapes rejected as regression-bearing) | `test/whitehat/F15_vetoDenominatorInflation.t.sol` (PB2.5b `f36155b`, 6/6 — real-VotingWeight poke sim: baseline thin-veto trip, suppression face demonstrated, truth-ceiling kill, recruit-flip, zero-denominator panic + self-heal edges); 907 unit green |
| F-16 | 2026-07-02 | WH-H.2a (escalated from the WH-G.2 dormant-tournament observation) | `src/gauge/GaugeRegistry.sol` + `src/gauge/GaugeEligibility.sol` + `src/emission/EmissionDistributor.sol` + `src/ccb/IGaugeRegistry.sol` | `advanceTournament` / `computeEpochSnapshot` / `recordScore` | S9 | Medium | Confirmed | The canonical F-10 efficiency-tournament emission caps (`08_bootstrap.md` §xxiii, `11_formulas.md` F-10) were deferred wholesale at Stage G (G-D15b) and never wired — `GaugeEligibility.computeEpochSnapshot` had no on-chain caller and assigned no cap tiers, so a pool with a cold-start/flash-manipulable TVL-EMA seed and near-zero real fee revenue could earn an uncapped `poolScore`/`totalScore` emission share once α=1 post-Year-1 (non-Miliarium `effective` score is then purely F-5/CCB-driven, linear in `tvlEMA`). | Root cause: `EMASampler`'s cold-start seed adopts a single un-smoothed `TVLOracle.tvl(pool)` spot (flash-manipulable via `balancesLiveScaled18`, no TWAP); `CCBScore.score` is linear in `tvlEMA`, and the non-Miliarium multiplier is a flat `1e18` (`CCBMultiplier.getMultiplier`), so a spiked seed becomes `poolScore` 1:1 and, post-Year-1, an uncapped emission share — exactly the failure mode `08_bootstrap.md` L131 names verbatim ("a pool with large TVL and large CCB share but minimal fees ranks at the bottom … receives at most 0.1% of emissions"). The designed defense (the F-10 efficiency tournament, ranking pools by `efficiency_ratio = (swap_fee_revenue + yield_fee_revenue) / emissions_received` and hard-capping the bottom cohort) existed only as a dormant `computeEpochSnapshot` (the WH-G.2 observation — no on-chain caller, binary favored-cohort only, distributor never read it) since G-D15b deferred the whole mechanism to Stage H and it was never built. Disposition (user-adjudicated, P-D13, full on-chain enumeration per user directive — all gauged pools in the competition): wired end to end across F16a-h. (1) `GaugeRegistry` gains an `EnumerableSet _activeGauges` (F16b) populated by all four activation paths and a permissionless `advanceTournament()` (F16c) gated by a `BLOCKS_PER_YEAR + 1` month-13 activation boundary (P-D14 (3), identical to the distributor's own gate) plus a `BLOCKS_PER_EPOCH` cadence throttle. (2) `GaugeEligibility.computeEpochSnapshot` assigns floor-percentile cap tiers (F16d, P-D15 (1)): `cap{10,50,100}Count = floor(nRanked × {5,10,15} / 100)`, most-severe-first — 10/50/100/0 bps for bottom-5%/10-5%/15-10%/top-85% — with a post-warmup zero-denominator SKIP (not revert) so one dead gauge cannot brick the permissionless tournament (P-D14 (5)); floor was chosen over the existing favored-cohort ceiling pattern specifically because ceiling caps the ONLY pool at 0.1% when `nRanked==1` (a single-pool catastrophe floor avoids). (3) `IGaugeRegistry.poolEmissionCapBps` delegates to the concrete `GaugeEligibility` mapping (F16e, G16 inheritor sweep across 4 mock files). (4) `EmissionDistributor.recordScore` clamps `effective_new` to `capT.divDown(1e18 - capT).mulDown(totalScore - oldEffective)` post-month-13 when a nonzero cap applies (F16f, P-D16) — the single clamped local flows into the `totalScore` signed-delta, the `poolScore` write, and the `ScoreUpdated` emit, so total/pool bookkeeping cannot disagree; `S_other = totalScore - oldEffective` is structurally non-negative and the clamp is a no-op below the gate or when uncapped. (5) Comprehensive unit coverage (F16g): floor-tier assignment against a 20-pool descending-efficiency set plus the `nRanked=6` no-cap floor-degradation guarantee (`GaugeEligibility.t.sol`, 21/21), and the clamp's exact-value / no-op / under-cap paths against a newly-settable mock (`EmissionDistributor.t.sol`, 115/115). (6) A standalone real-stack end-to-end PoC (F16h, P-D17) — the REAL `GaugeRegistry` + `GaugeEligibility` (20 synthetic gauges admitted via the `onlyGovernance` `seedFoundingPools` eligibility bypass, no mainnet fork needed) drives 4 `advanceTournament()` calls past the 3-epoch warmup, assigns the real bottom-5% tier to a fee-less pool carrying a `1e30`-magnitude flash-spiked seed, and confirms the REAL `recordScore` clamp — reading the cap through the REAL `IGaugeRegistry` delegation — holds that pool's emission share at or below 0.1% (`capT/1e18` exactly) despite its raw uncapped F-5 score exceeding the entire honest-pool total. Deferred, not implemented: the canonical $10K-TVL tournament-ranking floor (a separate exclusion gate needing a live oracle-priced TVL read, off the F-16 attack path since the attacker holds spiked-HIGH TVL) and the favored-cohort top-side anti-concentration caps (an orthogonal axis with undefined deployment values, drops onto the same infrastructure later as a pure addendum). Deploy wiring for `advanceTournament` + the `poolEmissionCapBps` getter deferred to P9. I13-class fix-forward on `stage-p`; `GaugeRegistry`/`GaugeEligibility` (`stage-g`-tagged) and `EmissionDistributor` (`stage-h`-tagged) both untouched — no re-tag. | Fixed | `c087c03` (F16b enumeration) + `5b83e8c`+`0952f2d` (F16c/F16c-b tournament caller) + `0441762`+`0f51efd`+`833077a` (F16d/F16d-b/F16d-c tier assignment) + `18468e6`+`d0cc90e` (F16e/F16e-b interface plumbing) + `d4c6d7d` (F16f clamp) + `9591c36`+`84e6870` (F16g-1/F16g-2 unit tests) + `95e8476` (F16h PoC) (no substrate seam) | `test/unit/GaugeEligibility.t.sol` (floor-tier assignment, 21/21) + `test/unit/EmissionDistributor.t.sol` (clamp mechanics, 115/115) + `test/whitehat/F16_emissionCapTournament.t.sol` (end-to-end, 2/2) |
| F-17 | 2026-07-04 | WH-I.1 | `src/emission/EmissionDistributor.sol` + `src/emission/IEmissionDistributor.sol` + `src/governance/VotingWeight.sol` | `recordDeposit` / `recordWithdrawal` / `claim` / `_syncDown` / `syncPosition` + `VotingWeight._positionPower` | S9 | High | Confirmed | The EmissionDistributor recorder writes `userLP` / `poolTotalLP` / `effectiveQualBlock` only on trusted-router liquidity callbacks, but I-D14 makes the BPT the freely-transferable AuMT receipt and F-09's symmetric `trustedRouter` allowlist SKIPS `recordWithdrawal` on any non-trusted-router removal — so the recorded position desyncs from the live BPT balance into a phantom `userLP > balanceOf` via (1) a non-trusted-router removal (100% capital recovered through the D32 permissionless `Vault.unlock`, `recordWithdrawal` never fires) or (2) a plain ERC-20 BPT transfer. The phantom then monetizes on four seams: capital-free emission (`claim` mints off the stale `userLP` with `balanceOf == 0`), honest-LP dilution (the inflated `poolTotalLP` denominator), skin-less governance (`VotingWeight._positionPower` reads the recorder, never the BPT), and a recipient canonical-exit revert (`recordWithdrawal` underflow for a holder with no recorded stake). Latent until P9 `setTrustedRouter` seats the F-09 allowlist. | Root cause: I-D14's soulbinding argument (BPT = receipt worth the full LP capital, so moving it is irrational) was scoped by I-D14 / OQ-25 to transfer/sell + the voting clock ONLY; it does not cover the recorder's emission/denominator/exit seams — a cross-seam residual of (F-09 symmetric gate x I-D14 transferable BPT x the P9 trusted-router rollout), not a failed F-09 fix. Disposition (user-adjudicated, P-D18 "you move you lose" forfeit-delta — never crystallize/mint on more than live `balanceOf`): downward-only reconciliation via `_syncDown`, which settles FIRST (the phantom-window emission distributes then strands in `poolAccRewardPerLP` per H-D24), then ratchets `userLP` / `poolTotalLP` down to a per-site reference WITHOUT touching `userRewardDebt` — forfeiting the withdrawn delta's pending `(acc − debt) × (userLP − balanceOf)` by construction (never banked into `pendingBalance`) — and resets the §viii clock. Wired at all three settle sites with references chosen to neutralize the post-mint/burn hook-callback timing: `recordDeposit` = `balanceOf − amount` (pre-mint), `recordWithdrawal` = `balanceOf + amount` (pre-burn) plus a `debit = min(amount, userLP)` recipient-liveness clamp (over-withdrawal / no-stake exit no longer underflow-reverts), `claim` + permissionless `syncPosition` = live `balanceOf`. `VotingWeight._positionPower` caps its numerator at `min(userLP, balanceOf)` with the `poolTotalLP` denominator left uncapped — it heals permissionlessly via `syncPosition`, so the cap only ever UNDER-counts. OQ-25 upward asymmetry preserved (`recordDeposit` credits only `bptAmountOut`, never `balanceOf`); honest-mover forfeit accepted (no rational holder moves the receipt). RESOLVED — `_syncDown` + `syncPosition` at `392e036`, VotingWeight cap + full-suite `balanceOf`-read repair at `a9aafa9`, PoC flip at `5f539a4`, fork-fixture restore at `0598a2f`, faithful real-BPT fork test at `5d0a41d`; 885 unit/whitehat + 126 fork green. PROCESS NOTE: the F17c `balanceOf` read broke `RecorderClock.t.sol` + `AuMMDistributorIntegration.t.sol` latently (F17d's narrow `--match-path EmissionDistributor.t.sol` verify missed them); a full-suite run at F17e surfaced 34 unit + 10 fork failures across 9 files — all "credited-but-holdless" test fixtures the fix correctly zeroes — restored via a `balanceOf → 1e30` mock (the receipt invariant proven in the unit suites + PoC, not the fork fixtures, per the user-adjudicated Mock+1-faithful-test call). I13-class fix-forward on `stage-p`; `EmissionDistributor` (`stage-h`) / `VotingWeight` (`stage-k`) untouched — no re-tag. | Fixed | `392e036` (core reconciliation + `IEmissionDistributor.syncPosition` + test retrofit) + `a9aafa9` (VotingWeight cap + WH-I full-suite repair) + `5f539a4` (PoC flip) + `0598a2f` (fork restore) + `5d0a41d` (faithful fork test); PoC `7979dff` (F17b); P-D18 design `a5aa1a4` + `78a68bf` (F17a lock/amend) (no substrate seam) | `test/whitehat/F17_recorderBptDesync.t.sol` (3/3 — phantom via non-trusted removal + plain transfer; claim forfeits emission to ~0) + `test/fork/StageKIntegration.t.sol::test_F17_realBptHolder_weightTracksLiveBalance` (real stack — in-sync holder confers weight, out-of-band BPT move forfeits it) |
| F-18 | 2026-07-05 | WH-P6.1 (cross-seam a) | `src/emission/BodenseeBootstrapChannel.sol` (cross-checked vs `src/emission/EmissionDistributor.sol`) | `accrue` rate snapshot (L206) vs `EmissionDistributor._lpTrancheIntegral` (L357) | S1 | Low | Confirmed | The channel↔distributor mint split (H-D2 / H-D26 / H-D27) requires both contracts to price the F-0 Bodensee bootstrap share at the same per-block rate — the distributor subtracts `bodensee_share(b) × rate(b)` from the LP tranche (`_phaseAwareBody` L310 / L315 via `_bootstrapApSum`, byte-identical to the channel's `_apSum` per H-D27) while the channel mints the same quantity for der Bodensee (`accrue` L199-224 → `distribute` L267) — but they snapshot `rate` on divergent bases: `accrue` L206 reads `blockEmissionRate(block.number)` (the CALL block) while `_lpTrancheIntegral` L357 reads `blockEmissionRate(cursor)` (the INTEGRAND block, per era, H-D30). Within Era 0 (which encloses the whole bootstrap window, `month10End ≈ 2.19M ≪ 10.5M`) both read `GENESIS_RATE`; they diverge only if the channel's first bootstrap-spanning `accrue()` lands at a call-block already in Era ≥ 1 (`block.number ≥ GENESIS_BLOCK + BLOCKS_PER_ERA`, ~4 yr). | Direction: `blockEmissionRate` is monotone-decreasing, so the channel rate ≤ the distributor's subtracted rate — the split UNDER-mints der Bodensee (POL under-funded by the halving factor: ½ in Era 1, ¹⁄₃₂ in Era 5, both PoC-witnessed) and NEVER over-mints, so INV-1 / INV-6 (≤ 21M) is structurally safe and there is no attacker gain (self-inflicted loss-under-operational-neglect only). Reachability: requires total dormancy of the permissionless `accrue()` across the entire Months 0—10 window and into Era 1 — operationally implausible (the bootstrap rail is the core POL mechanism, actively `accrue()` + `distribute()`-operated in-window) but NOT contract-enforced. Why WH-H.1 / WH-L.2 missed it: WH-H.1 (P3 S1) read the channel in isolation and accepted its L197 doc claim "exact within bootstrap" (true for the integrand blocks but silently assuming `block.number` is itself in Era 0), and WH-L.2's cross-seam (a) attestation covered the SKIM / boost leg (continuous-phase `integratedSkim`), not the bootstrap-phase rate basis — the divergence is visible only against the distributor's DIFFERENT, robust per-integrand basis (the P6 cross-seam mandate, P-D6). Disposition (user-adjudicated, P-D19, Fix now): key the channel's rate to the clamped integrand upper bound `to` (`blockEmissionRate(to)`), guaranteed Era 0 (`to = min(block.number, month10End) < GENESIS_BLOCK + BLOCKS_PER_ERA` always), so the rate resolves to `GENESIS_RATE` regardless of the call block, byte-aligning with the distributor's basis. RESOLVED (`c162261`) — 1-token change to a `stage-h`-tagged contract; behavior-preserving in-window (all 34 pre-existing `BodenseeBootstrapChannel.t.sol` tests pass unchanged, their flat `MockAuMM.blockEmissionRate` making the fix a no-op ⇒ zero blast radius); full unit suite 888 green. I13-class fix-forward on `stage-p`; `BodenseeBootstrapChannel` (`stage-h`) untouched — no re-tag. | Fixed | `c162261` (accrue rate → `to` + NatSpec) + P-D19 design lock `015f280` + PoC `a9e58fc` (no substrate seam) | `test/whitehat/F18_channelRateBasis.t.sol` (3/3 — Era-1 & Era-5 `accrue()` yields the same `pendingAccrual` as the in-window Era-0 accrual and equals the distributor's Era-0 subtraction; witnesses the ½ / ¹⁄₃₂ under-mint pre-fix) |
| F-19 | 2026-07-08 | P10.3a (Leg A e2e) | `src/emission/TVLOracle.sol` | `_venueRatio` (guard now L248-L249; the `getPoolData` venue read carries the Vault's `withInitializedPool` modifier), fed every registry pool unconditionally by K-D8 Leg 2 (`_constellationRatio`) | S7 | Low | Confirmed | K-D8's Leg 2 feeds `_venueRatio` every Miliarium-registry pool unconditionally; `_venueRatio` reads `vaultExplorer.getPoolTokens(v)` (fine for any REGISTERED pool) then `vaultExplorer.getPoolData(v)`, which carries the Vault's `withInitializedPool` modifier and reverts `PoolNotInitialized` for a registered-but-uninitialized pool — so ONE uninitialized Miliarium pool bricks EVERY `tvl()` / `quoteSvZCHF` call protocol-wide: EMA seeding/refresh (`EMASampler.updateEMA`), fee-conversion recording (`EfficiencyOracle.recordFees` → `quoteSvZCHF`), and every downstream consumer of a fresh EMA. Surfaced at the FIRST Leg A execution: `StagePEndToEndTest.test_legA_claimMintsThroughFullRecorderChain` reverted `PoolNotInitialized(majorPools[0])` at the EMA-seed step (`updateEMA(pilot 01)` → `tvl(pilot 01)` → `_constellationRatio` Leg 2 → `_venueRatio(ixCasper)`). Production shape is identical to the fixture: M/N `replaceSlot` binds land at deploy while initialization is a separate organic per-pool liquidity event, so the launch window runs bricked until ALL 26 roster pools hold liquidity — the emission machine cannot start (no EMA → no gated score → `totalScore = 0` → zero LP emissions) — and post-launch an O-D2 composition-challenge replacement (gated on token composition, NOT on initialization) re-opens the vector: a passed 2/3 supermajority can insert an uninitialized pool and re-brick the oracle. The eligibility machinery already INTENDS the skip (an uninitialized venue has zero balances and would return `(0, false)`); the read just reverts before eligibility is evaluated. | Why latent through 897 unit + 128 fork tests: the unit `MockVaultExplorer` (`test/fork/mocks/StageHMocks.sol` L14) modeled `setPool` / `getPoolTokens` / `getPoolData` with NO initialization semantics (every mock pool reads as live — the unit suite structurally cannot see the revert); every prior fork fixture binding a real registry (StageJ/K/L on `StageIIntegrationFixture`) seeds the 3 pilots ONLY, all initialized; WH-P6 seam (d) verified Leg-1/Leg-2 dedup EXACTNESS, not venue-read liveness; the orchestrator stack is the FIRST context pairing a full 26-pool registry with the M/N deploy-uninitialized posture — precisely the P-D0b full-system state Leg A exists to exercise. Severity S7 Low: liveness only, no fund loss; the post-launch vector is governance-gated (2/3 supermajority + quorum); `Vault.initialize` is permissionless, so any actor cures with dust liquidity; but the launch-window brick is UNCONDITIONAL. Disposition (user-adjudicated, P-D37): the `isPoolInitialized` guard as the FIRST statement of `_venueRatio` — `if (!vaultExplorer.isPoolInitialized(v)) return (0, false);` — semantically identical to the existing ineligible path (a venue that cannot price does not count toward the average), zero behavior change for initialized venues, protocol-favorable; the selector is live on the production binding (`IVaultExplorer` L131 + `IVaultExtension` L158; `VAULT_EXPLORER` = the Aureum Vault per P-D27). Rejected: try/catch around the venue reads (swallows unrelated read-reverts, masking future bugs); fixture-side init-all-26 + ops mandate (leaves the production `replaceSlot` vector latent and diverges the fixture from the production posture it exists to witness). Witness apparatus: `MockVaultExplorer` gains faithful initialization semantics (`setUninitialized(pool, flag)` opt-out + `isPoolInitialized(pool)` + `getPoolData` reverting `PoolNotInitialized(pool)` when flagged; default-initialized, so all 42 pre-existing `TVLOracle.t.sol` tests pass unchanged — sole consumers grep-enumerated per G16); the fork witness is Leg A itself, held uncommitted until the fix lands (the P-D35 pattern). Amends K-D8 (Leg 2 gains the init guard — second amendment, after P-D35); sibling lineage: F-14 (S5 settlement liveness) + the F-04/F-05/F-07/F-10 EMA gate family (consumers degrade gracefully — here the PRODUCER was the unguarded leg). I13-class fix-forward on `stage-p`; `TVLOracle` (`stage-h`-tagged) untouched — no re-tag. | Fixed | `2181cbf` (the `_venueRatio` guard + `MockVaultExplorer` initialization semantics + PoC, one fix train per P-D37) + P-D37 design lock `7eecb8c` (no substrate seam) | `test/whitehat/F19_uninitializedVenueLiveness.t.sol` (4/4 — an uninitialized venue in the roster leaves `tvl()` live, priced via the remaining venues only (`100e18`, VENUE_A alone); the mock's raw `getPoolData` read reverts `PoolNotInitialized`, proving the guard (not an accident) does the skip; flipping the venue initialized folds it back into the average (`150e18`); `quoteSvZCHF` survives identically) + the fork witness `StagePEndToEndTest.test_legA_claimMintsThroughFullRecorderChain` (P10.3a) |
| F-20 | 2026-07-09 | P10.3c (Leg C3 e2e) | `script/pools/deploy-miliarium-pool.s.sol` (Fix A) + `src/governance/AureumGovernance.sol` (Fix B) | the registration-time `roleAccounts.swapFeeManager` literal (the L67 site, now `address(0)` post-F20b; uniform across all 26 configs per E-D22/OQ-11), consumed by `VaultAdmin.setStaticSwapFeePercentage` (L264) via `_ensureAuthenticatedByExclusiveRole` (L796-803), foreclosing `AureumGovernance._executeProposal` (L362, the FeeChange leg); the Fix-B gate lands in `proposeFeeChange` | S8/S9 | Medium | Confirmed | Every Miliarium pool registers with `swapFeeManager: governanceMultisig`; Balancer's `VaultAdmin.setStaticSwapFeePercentage` authenticates via `_ensureAuthenticatedByExclusiveRole` — a NON-ZERO role is EXCLUSIVE, only that exact address may call and the authorizer is NEVER consulted (`address(0)` defers to the authorizer) — and role accounts are written once at `registerPool` (`VaultExtension.sol` L252) with no setter, immutable for the pool's life. So `AureumGovernance._executeProposal` issues the fee call as `msg.sender = governance` ≠ the manager → `SenderNotAllowed()` at execute, while propose/vote/queue all pass (no propose-time executability gate existed) and the non-refundable `1_000e18` svZCHF bond is already donated at propose — the FeeChange proposal type was structurally inexecutable against the ENTIRE 26-pool roster, a bond trap on a dead path. The K-wire-9 authorizer migration itself is correct: `AureumGovernanceAuthorizer.canPerform` grants `GOVERNANCE_CONTRACT` unconditionally, so with role == 0 the identical call succeeds. Witnessed live: the Leg C3 first fork run failed `[FAIL: SenderNotAllowed()]` at execute on the byte-perfect P-D39 harness. | Why latent through every prior pass: no prior test EXECUTED a FeeChange end-to-end — WK.10/WK.13 audited the quorum arithmetic and deposit lifecycle, K7 wired the migration, and P10.2 wire (13) asserted the STATIC authorizer grant (`canPerform(governance)` true), but the exclusive-role semantic is invisible to a static grant check because a non-zero role means the authorizer is never consulted on that path; Legs C1/C2's execute legs call Aureum-owned registries (`GaugeRegistry` / `MiliariumRegistry`), so Leg C3 is the FIRST path routing a governance execute INTO the Vault. The lever was never dead pre-K — the multisig held it directly as the role account; it died exactly at the K handoff, where authority was supposed to follow the authorizer but the frozen role slot kept bypassing it. §xxix escalation: the role slot being write-once makes the K-D9 retained `swapFeeManager` slice not merely unmigrated but UNMIGRATABLE — §xxix multisig dissolution would have permanently foreclosed fee management on all 26 pools for everyone; Fix A partially resolves the §11 multisig-dissolution carry-forward (the `swapFeeManager` slice now follows the migrating authorizer; the §11 cross-reference lands at the next backfill per P-D40). Severity S8/S9 Medium: no theft; the losses are the proposer's non-refundable `1_000e18` svZCHF bond donated per attempt on a structurally dead proposal type plus governance-liveness foreclosure of the constitutional fee lever. Disposition (user-adjudicated 2026-07-09, P-D40 Fix A+B): (A) `swapFeeManager: address(0)` in the shared deploy base — the exclusive helper then defers to the authorizer, so the Safe keeps the identical lever pre-K via `AureumAuthorizer`, FeeChange works post-K via `AureumGovernanceAuthorizer`, and §xxix dissolution is clean because authority follows the MIGRATING authorizer rather than a frozen role slot; `pauseManager` UNCHANGED (the non-exclusive `_ensureAuthenticatedByRole` L787-793 already falls through to governance); amends E-D22, I13-class fix-forward, E/M/N tags untouched — no mainnet pools exist, so P-bis simply inherits the fix. (B) the `proposeFeeChange` propose-time fail-fast, placed AFTER the existing `InvalidFeeValue` + `InvalidFeeTarget` checks: read `VAULT.getPoolRoleAccounts(targetPool_).swapFeeManager` (IVaultExtension L366) and revert the new `ExclusiveSwapFeeManager(address pool, address manager)` unless the manager is `address(0)` or `address(this)` — bond protection against FUTURE community pools whose creators set their own manager; propose-time ONLY — role accounts are immutable, so no execute-time re-check (contrast the composition gate's execute re-check, which guards a MUTABLE eligibility surface). Blast radius sized at pre-flight (I13): the unit `MockVault` gained `getPoolRoleAccounts` returning the zeroed struct (+ the `PoolRoleAccounts` import); the gate's placement after the existing checks leaves the four negative propose tests reverting before the role read; Fix A flows through the shared deploy base into every `DeployIx*` wrapper, so ALL fork fixtures get role-0 pools; swept — no test anywhere sets fees via the exclusive role (the two hook-scoped inline `swapFeeManager: GOVERNANCE_MULTISIG` pools in `AureumFeeRoutingHook.t.sol` make no fee call). Adjacent doc-precision strike (F20e, doc-only): the 26 per-pool config comments claiming `governanceMultisig` corrected to `address(0)`; `DeployDerBodensee.s.sol` read the sentinel as no-one-can-change — the actual semantic is authorizer-deferral (der Bodensee's fee IS multisig-changeable during A–K via `AureumAuthorizer`, unreachable post-K only because `proposeFeeChange` excludes `BODENSEE_POOL`; the immutability claim holds post-dissolution, not from block 0). Vault substrate citations READ-ONLY (`VaultAdmin.sol` L264 / L787-L803, `VaultExtension.sol` L252) — Vault bytecode untouched, no substrate seam. Sibling lineage: F-19 (the first P10.3 e2e-surfaced finding — the orchestrator stack keeps pairing states no prior fixture did), K-D6 (the proposal-type triad), F-14 (settlement liveness). | Fixed | `2f4dbb2` (F20b Fix A — `deploy-miliarium-pool.s.sol` `swapFeeManager: address(0)`; `pauseManager` unchanged) + `ed32157` (F20c Fix B — the `proposeFeeChange` executability gate + the `MockVault.getPoolRoleAccounts` zeroed-struct retrofit; 45/45 AureumGovernanceTest green) + `3ffd03b` / `dac8c34` (F20e stale-doc sweep — the 26 config comments + the DeployDerBodensee sentinel) + P-D40 design lock `3d1bc0e` (no substrate seam) | `test/whitehat/F20_feeManagerForeclosure.t.sol` (4/4 — an exclusive-manager pool reverts `ExclusiveSwapFeeManager` with the bond FULLY protected: zero svZCHF pulled, zero proposal created, zero donate; `address(0)` and governance-as-manager both pass and donate; the band-check precedes the manager read, pinning the I13 blast-radius contract) + the fork witness `StagePEndToEndTest.test_legC3_feeChangeExecutesThroughMigratedAuthorizer` (P10.3c-1 `8f9cb15`, fork-verified 1/1 post-fix) |
|    |      |           |      |         |         |     |      |       |             |        |     |     |

> Add new rows at the bottom. Keep **ID** monotonic. Pad short tables with the
> empty template row so the columns stay readable in plain text.

---

## Coverage / attestation roll-up

One row per white-hat **pass** (per build stage). This is where "no finding"
lives — it records that a surface was checked and came back clean, so silence is
never mistaken for absence of review. Surfaces are `S0`–`S12`; mark each
`✓` (checked, clean), a finding ID (e.g. `F-07`), `n/a` (contract doesn't expose
it), or `—` (deferred — explain in Notes).

| Pass | Date | Scope (diff base) | Contracts | S1 | S2 | S3 | S4 | S5 | S6 | S7 | S8 | S9 | S10 | S11 | S12 | Notes |
|------|------|-------------------|-----------|----|----|----|----|----|----|----|----|----|-----|-----|-----|-------|
| WK | 2026-06-12 | `stage-j-complete..HEAD` (`63471f9`) | 7 logic: router/distributor/channel/TVLOracle/authorizer/governance/VotingWeight | ✓ | ✓ | ✓ | F-04 | n/a | ✓ | F-04 | ✓ | F-01/F-02 | F-03 | n/a | ✓ | 4 findings (F-01/F-02 S9 Medium; F-03 S10, F-04 S4+S7 Low). All 4 Fixed post-close (F-01 e6d9b3d / F-03 1e84bfb / F-04 c283371+3d80ac8 / F-02 a5a3a1b + aumm-site spec lock 50e21a5). S5 n/a (OQ-1 hook = Stage D); S11 n/a (AureumVaultFactory = Stage B). Back-fill: WH-D / WH-G / WH-H / WH-J + the INV-2 cross-contract harness → Stage-P. See the WK.19 block + findings table for detail. |
| WK-R | 2026-06-14 | `stage-k-complete..stage-k-whitehat-complete` (`63471f9..7f98cc2`) + residual Stage-K | 4 fix-surface: AureumGovernance / VotingWeight / EMASampler / DeployStageK (+TVLOracle setter) | — | — | ✓ | ✓ | n/a | ✓ | F-05 | ✓ | F-06 | ✓ | n/a | ✓ | 2 findings, both Suspected + Open + routed (F-05 S7 Low — EMASampler seed-freshness, age-only maturity gate; F-06 S9 Medium — post-endBlock quorum-denominator inflation griefs a passed proposal, inverse cost of the F-01 live-read). F-01..F-04 re-audited clean + unregressed (R.1/R.2 = F-01/F-02 arithmetic; R.3/R.5 = F-04; R.9 = F-03). INV-5 holds (governance findings = blocking-power/capture, not theft; F-06 burned deposit → griefer net-negative). S1/S2 — not re-scanned (WK-owned; fixes don't touch supply/mint or fee-routing). S5 n/a (OQ-1 hook = Stage D); S11 n/a (Factory = Stage B). S12 ✓ — live Slither 0.11.4 (135 contracts, 31 medium, ZERO high/critical; 3 fix-surface results all pre-existing benign — castVote reentrancy-no-eth, poke + updateEMA incorrect-equality). Disposition: F-05/F-06 PoC-on-fix → Stage-P / WH-H; full-tree Slither differential → Stage-P sweep. |
| WL | 2026-06-17 | `stage-l-complete..HEAD` scheduled Stage-L spot review | 1 fix-surface: IncendiaryRegistry (`_maturePrice` boost-pricing path) | — | — | — | — | n/a | — | F-07 | — | — | — | n/a | — | Targeted Stage-L spot review of the IncendiaryRegistry price-EMA pricing path; 1 finding (F-07, S7 Low — `_maturePrice` freshness gap, IncendiaryRegistry sibling of F-05). Fixed fix-forward on `main` (0a21b99 source+retrofit + b96227c PoC, no re-tag). NOT a full Stage-L S1-S12 sweep — remaining Stage-L surfaces (buyBoost placement / L-D6 cap / FCFS spill, EmissionDistributor boost-delivery leg, deploy wiring) + a formal WL pass deferred to Stage-P. S5 n/a (OQ-1 hook = Stage D); S11 n/a (Factory = Stage B). |
| WN | 2026-06-26 | `stage-m-complete..HEAD` — N7 WN pass over the new `src/rate_provider/` surface (N-D6) | 2: `ERC4626RateProvider` / `CompositeRateProvider` | n/a | n/a | ✓ | F-11 | n/a | ✓ | n/a | ✓ | n/a | — | n/a | ✓ | 1 finding (F-11, S4 Low Confirmed — both RP constructors lacked a decimals guard; `previewRedeem(1e18)` is wrong-scale for any non-18/18 vault, latent until a non-18 consumer ships). Fixed fix-forward on `stage-m` (`ca088eb` WN.2a + `58581b9` WN.2b guards + `5a3a40d` WN.1 mock retrofit + `addad54` WN.3 PoC, no re-tag). S3 ✓ (round-down posture — `previewRedeem`/`convertToAssets` + `mulDown` both floor, rate never over-stated; N3.3). S6 ✓ (view-only `getRate`, zero state writes). S8 ✓ (zero admin, immutable-only, F-11 fail-closed ctor guard, no setters). S1/S2 n/a (no supply/fee surface); S5 n/a (OQ-1 hook = Stage D); S7 n/a (no in-contract EMA); S9 n/a (no gauge/tournament surface); S11 n/a (AureumVaultFactory = Stage B). S10 — (env-injected immutables + deploy-time `getRate() > 0` gate exercised at N6; F-11 hardens the wrong-decimals-address case; full deploy-script S10 sweep → Stage-P). S12 ✓ — live Slither 0.11.4 (138 contracts / 100 detectors / 135 results; ZERO on either RP contract, ZERO high/critical project-wide) + manual danger-keyword sweep clean. NOT a cross-contract consumer sweep — the RP → pool-valuation → CCB/gauge seam → Stage-P. |
| WO | 2026-06-29 | `stage-n-complete..HEAD` — WO pass over the Stage O composition-fitness gate (O1-O5) | `GaugeEligibility.meetsCompositionQualityGate` + `AureumGovernance` propose/execute call sites + `IGaugeRegistry`/`GaugeRegistry` delegation | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | F-12 | n/a | n/a | ✓ | 1 finding (F-12, S9 Low Confirmed — composition gate skipped factory provenance; a non-factory pool with the canonical hook + faked `getNormalizedWeights` could spoof the ≥52% bar). Fixed pre-tag on `stage-o` (`b7d64a6` WO.1-3 gate + O4/O5 retrofits, `e3ab8cf` WO.4 PoC). Threat surface examined, zero other findings: the gate is `view` → STATICCALL (a malicious candidate cannot reenter-mutate); guards the ONLY `replaceSlot` path (`onlyGovernance`, sole caller `AureumGovernance._executeProposal`, always gates first); checked at both propose (pre-deposit fail-fast) and execute (pre-mutation atomic — the O5 mid-vote-de-admission test pins zero state mutation); hook spoofing blocked (`getHooksConfig` is the Vault's immutable registered hook); numerator gaming requires a governance-admitted class (VaultClassRegistry propose-veto). S5 n/a (OQ-1 hook = Stage D; the gate reads `getHooksConfig` but the hook contract is unchanged); S11 n/a (the factory contract is Stage B — F-12 is on the gauge CONSUMING provenance, S9). S12 — the F-12 fix is a one-line `isPoolFromFactory` check identical to the Slither-clean `_checkEligibilityCriteria` L368 (no new detector surface); full-tree Slither differential → Stage-P sweep. |
| WH-D | 2026-07-01 | Stage-D back-fill (full-contract audit of the fee-routing surface; not a diff) | 2: `AureumFeeRoutingHook` / `AureumProtocolFeeController` | n/a | ✓ | ✓ | ✓ | F-13 | ✓ | n/a | ✓ | n/a | — | n/a | — | 3 read-lens steps (S2/S5/S6). 1 finding: F-13 (S5 Low Confirmed Accepted-risk — unbounded internal-leg slippage L494/L552, acknowledged Stage-Q surface). S2 ✓ (WH-D.1 — every reachable outflow → `address(this)`/immutable `FEE_ROUTING_HOOK`; 50% swap-fee pin unconditional incl. `protocolFeeExempt` L602-604 closing the exempt-bypass; all 4 creator entry points revert; creator accrual slot never credited; INV-3 CEI L741-742). S3 ✓ (fee-disaggregation split path dead — creator fees off — live accrual exact, no rounding). S4 ✓ (svZCHF deposit/`balanceOf` balance-sweep donation-safe L464; no share-inflation surface introduced by the hook). S6 ✓ (WH-D.3 — cross-function re-entry fully gated at the L498 token-callback point; no manipulable view; no-external-call-before-write mutators L253-303). Obs-A (controller `collectSwapAggregateFeesForHook` lacks the public path's D-D9 Bodensee guard L263) RESOLVED benign — the sole caller (the hook) never passes Bodensee (`onRegister` L324/L327 refuses attach; `routeYieldFee` L577 reverts). Carry-forward: svZCHF-membership liveness (`onAfterSwap` converts fees via `params.pool` L349, assumes pool contains svZCHF) → WH-G; recorder attribution depth → WH-I; deploy-script S10 sweep → P9; full-tree Slither S12 → Stage-P. S1/S7 n/a (no mint/EMA); S9 n/a (no gauge/tournament surface — the F-13 MEV is S5-classed); S11 n/a (AureumVaultFactory = Stage B). |
| WH-G | 2026-07-01 | Stage-G back-fill (gauge surface; not a diff) | 4: `GaugeRegistry` / `GaugeEligibility` / `VaultClassRegistry` / `SwapAndDepositToBodensee` | n/a | n/a | — | ✓ | F-14 | ✓ | — | ✓ | F-15 | n/a | n/a | — | 5 read-lens steps (WH-G.1 S8 GaugeRegistry / WH-G.2 S9 GaugeEligibility / WH-G.3 S4 GaugeEligibility / WH-G.4 S8 VaultClassRegistry / WH-G.5 S5-S6 SwapAndDepositToBodensee). 2 findings: F-14 (S5 High Confirmed → Fixed — svZCHF-membership settlement-liveness DoS on onAfterSwap, carried from the WH-D.2 carry-forward; P-D12 fail-safe skip + sUSDS deposit-token generalization, `1af89f3`/`1decb70`/`fdb158c`) + F-15 (S9 Low Suspected → Accepted-risk — VaultClassRegistry veto live-denominator, F-06 sibling, → Stage Q). WH-G.1 S8 ✓ (GaugeRegistry — activateGauge fail-closed try/catch returns-before-write, onlyGovernance exact-equality on all 5 privileged fns, terminal Revoked, no one-shot-twice / front-run / delegatecall). WH-G.3 S4 ✓ (F-12 `isPoolFromFactory` re-verified holding at BOTH `_checkEligibilityCriteria` L368 + `meetsCompositionQualityGate` L386; numerator sums immutable normalized weights not balances; the `asset()` probe is class-discriminator-only). WH-G.4 S8 ✓ (F-08 `hasVetoed` dedup holds — set BEFORE the external governanceWeight read; propose-veto-finalize-revoke idempotent + clean window boundary; onlyGovernance revoke). WH-G.5 S5/S6 ✓ (SwapAndDepositToBodensee — transient `_EXECUTING_SLOT` guard + OnlyVault callback + cached-payload match + reserve-delta + residual-zero; pure DONATION mints zero BPT, no rate read — deposit-leg freshness lives in the callers per F-07). S9 OBSERVATION: the F-10 favored-cohort tournament (`computeEpochSnapshot` + EfficiencyOracle) is DORMANT — no production caller (onlyGaugeRegistry-gated but no GaugeRegistry entry point invokes it; nothing reads isFavoredCohort), MOOTING the S9 tournament-gaming vector + the EfficiencyDataUnavailable batch-revert; flagged as an orphaned surface (O-D4 activateBoost sibling) for the P6 dead-surface sweep. Escalated 2026-07-02 to F-16 (Medium; wired end-to-end F16a-h; now Fixed — see the F-16 row in the Findings table above). Adjacent, no finding: GaugeEligibility TVL-floor is a spot read on a permanent isGaugeEligible latch (no ROI — downstream emission EMA-gated per F-04/F-05/F-10 → WH-H); VaultClassRegistry proposeVaultClass external-call-before-state-write benign (Bodensee hookless + svZCHF non-callback + nextProposalId++ atomic); single-step registry setters (2-step = defense-in-depth). S1/S2 n/a (no mint / fee-conservation — SwapAndDeposit is a donation helper); S3/S7 — (rounding not the focus; spot-tvl-floor + EfficiencyOracle EMA → WH-H); S10 n/a (deploy → P9); S11 n/a (AureumVaultFactory = Stage B); S12 — (full-tree Slither → Stage-P). Carry-forwards: recorder attribution → WH-I; F-14 off-chain ixAetheron conversion + fee-custody → P-bis; tournament-dormant + activateBoost → P6. |
| WH-H | 2026-07-03 | Stage-H emission + Stage-F CCB engine back-fill (S1 / S7 / S3; not a diff) — CLOSED 2026-07-03 | H.1: `AuMM` / `AuMMMinterRouter` / `EmissionDistributor` / `BodenseeBootstrapChannel`; H.2: `EMASampler` / `TVLOracle` / `EfficiencyOracle` / `CCBMultiplier` / `CCBScore`; H.3 pending: `CCBMultiplier` / `CCBScore` / `CCBShare`  | ✓ | n/a | ✓ | n/a | n/a | ✓ | ✓ | ✓ | F-16 | n/a | n/a | — | WH-H.1 S1 supply/mint — NO FINDING (crown-jewel step). Exhaustive mint enumeration: the sole `_mint` (`AuMM.sol` L115) is cap-gated (`totalSupply() + amount > MAX_SUPPLY` L114, 21M) + one-shot minter (L98-106); the minter is `AuMMMinterRouter`, a fail-closed 2-caller allowlist (`mintFor` L97 — bootstrap channel + distributor only, no amount policy); exactly 2 `mintFor` call sites project-wide (`EmissionDistributor.claim` L586, `BodenseeBootstrapChannel.distribute` L267). Schedule conservation holds by construction: `_accrueGlobal` L378 calls the H5 production `_lpTrancheIntegral` (not the H4 stub), so the LP tranche subtracts the exact Bodensee AP share during bootstrap (L309 / L314) while the channel independently mints it — LP(b) + bodensee_share(b) = rate(b) per block. The two AP-sum bodies are byte-for-byte identical (`_bootstrapApSum` L278-284 = channel `_apSum` L247-253) over identical B-leg anchors [m6, m10] (dist L314, chan L218) + identical tiling (block m6 → A-leg, m6+1 → B-leg). Halving-era boundary no off-by-one (`nthHalvingBlock(g, era+1) - 1` L354 = era-last-block; rate piecewise-constant, tiled exactly L352-359; `AureumTime` L54 / L89). `totalScore == 0` forgoes the LP tranche (L374-376, under-emission = safe direction); channel lifecycle-clamped to `min(block.number, month10End)` L203 + zeroes pendingAccrual pre-mint L264-267 (cannot over-mint its fixed integral). Reentrancy: claim CEI-correct (state before mintFor, project-owned no-callback callees); distribute 3-slot transient guard + Vault.unlock L268-278. Overflow headroom (AP product <= 3.5e42, far below 2^256). S6 (mint-path reentrancy) + S8 (mint-authority access) checked clean under the S1 lens. ADJACENT OBS (doc-only, benign): dist `_bootstrapApSum` param-doc L263 annotates the B-leg anchorStart as `month6End + 1`, but the code (L314), the sibling `_phaseAwareBody` doc L290, and the channel (L218) all use `m6` — conservation holds (both sides' code agree), but the doc contradicts the code, a maintenance trap (a future edit "fixing" code to the doc on one side only would break the partition); recommend a 1-line doc-fix (L263 `month6End + 1` → `month6End`), no code / behaviour change. S9 = F-16 (escalated WH-G.2 → WH-H.2a, wired end-to-end F16a-h, Fixed — see the findings table). Scoped-out, not silently omitted: Incendiary continuous-phase skim L331 + the L-D25 boost-delivery leg → WH-L.2 cross-seam (a) per the matrix. WH-H.2 S7 TVL-EMA — NO NEW FINDING (re-verifies F-04 / F-05 / F-10 / F-16). EMASampler: 1/day cadence L130-134 + alpha 2/61 L144-147 + F-D15 seed L138-141 (spot 1:1 first call; single-block spike accepted, defended consumer-side per L14-18). 2 direct spot `tvl()` consumers — EMASampler L136 (smooths) + `GaugeEligibility` L381-382 ($10k floor, view eligibility latch, no direct emission, F-16-capped). 3 `tvlEMA` consumers — VotingWeight L164-176 + EmissionDistributor `_gatedTvlEMA` L456-461 both GATED (F-05 maturity L166 / L459 + freshness L167 / L460, F-10) feeding `CCBScore` L475; CCBMultiplier L251 / L264 UNGATED. F-04 spot-tvl in VotingWeight SUPERSEDED (reads gated `tvlEMA` post-F-02; no direct `tvl()` read survives). ADJACENT (documented-safe): (1) CCBMultiplier ungated reads feed only F-8 `updateMultiplier` — sign-only + anti-cyclical (spike own EMA up → -STEP L268) + ±0.05/epoch + clamped [0.75, 1.25] L273-274; sole consumer `getMultiplier` L297-302 × the GATED EMA L475, so a manipulated seed zeroes the base leg (max +25% on a real mature+fresh base, no amplification); (2) EfficiencyOracle reads no TVL / EMA — `recordFees` / `recordEmissions` recorder-gated L210 / L223 + 3-epoch SMA L239-262 (42-day); gaming the F-10 rank needs REAL fee revenue (wash self-defeating). WH-H.3 S3 rounding — NO FINDING. Every division / conversion in the emission + CCB engine rounds DOWN (protocol-favorable — scores / shares understated, user claims <= exact, dust stranded, no over-mint): `CCBScore.score` L24 `mulDown` (the sole live CCB library, EmissionDistributor L475); `CCBMultiplier` L257 / L258 / L263 / L266 / L267 all mul-before-div rounding down, feeding sign-only threshold comparisons (bounded knob [0.75, 1.25], not value transfer); the `recordScore` chain — F-3 α-blend L481 (`f5Total / 28` floor + `mulDown`) + the F-16 clamp L493 (`capT.divDown(1e18 - capT).mulDown(totalScore - oldEffective)`, rounds down so the cap is never exceeded; `totalScore - oldEffective` structurally underflow-safe) + F12 / F13 signed-delta `_applySignedDelta` L476 / L499; the MasterChef accumulator L379 / L393 / L398 (`divDown` / `mulDown`, dust-stranding). No div-before-mul anywhere. S3 sub-lens 're-implemented Vault invariant / weight math' n/a — the CCB engine computes Aureum tokenomic scores, not Balancer pool math (`getNormalizedWeights` reads come from the Vault). ADJACENT (documented-safe): `CCBShare.shares` L41 `divDown` is protocol-favorable but the library has ZERO src consumers (orphaned — the MasterChef accumulator superseded the F-6 normalizer) → P6 dead-surface sweep (O-D4 `activateBoost` + dormant-tournament siblings). Deferred (not code gaps): S3 precision-fuzz + INV-5 round-trip → P6.5 INV harness; full-tree S12 Slither → Stage-P; WK.18 thin-venue `_constellationRatio` pump economics + tournament-ranking-input ROI → Stage-P / Q populated-roster fork sim (spot `tvl()` = OQ-5a-bis / OQ-22-accepted design). WH-H CLOSED 2026-07-03 — 3 read-lens steps (H.1 S1 / H.2 S7 / H.3 S3), 1 finding (F-16 S9 Medium, escalated + Fixed); S1 / S3 / S6 / S7 / S8 ✓, S9 F-16, S2 / S4 / S5 / S10 / S11 n/a, S12 — (Stage-P). |
| WH-I | 2026-07-04 | Stage-I recorder/governance spine back-fill (attribution-integrity lens; not a diff) — CLOSED 2026-07-04 | 3: `EmissionDistributor` (recorder) / `AureumFeeRoutingHook` (dispatch) / `VotingWeight` (reader) — per I-D14 no `AuMT.sol` | n/a | n/a | ✓ | n/a | n/a | ✓ | ✓ | ✓ | F-17 | n/a | n/a | — | WH-I.1 S9 recorder attribution — F-17 (High Confirmed → Fixed): the recorder↔BPT desync — I-D14's transferable BPT + F-09's symmetric `trustedRouter` allowlist (skips `recordWithdrawal` on non-trusted removal) leaves a phantom `userLP > balanceOf` (via non-trusted removal or plain BPT transfer) that monetizes on four seams (capital-free emission, honest-LP dilution, skin-less governance, recipient-exit revert); P-D18 forfeit-delta downward reconciliation (`_syncDown` at all 3 settle sites + permissionless `syncPosition` + `VotingWeight._positionPower` `min(userLP, balanceOf)` read-cap). S8 recorder gate ✓ — the F-09 `onlyAuMTContract(pool)` + `trustedRouter` allowlist holds fail-closed (`recordDeposit` / `recordWithdrawal` admit only the bound recorder; the phantom is a desync of the CREDITED position, not a spoofed caller — F-09's allowlist is the fix's own P9 dependency, not a gap). S3 ✓ (the forfeit-delta rounds protocol-favorable — `_syncDown` reduces `userLP` and forfeits `(acc − debt) × delta` downward, never over-credits; the `min` cap under-counts). Attribution-integrity lens only — host-contract S1 (supply/mint) covered by WH-H.1 / P3, S2 (fee-routing dispatch) by WH-D.1 / P1. S1 / S2 / S4 / S5 / S10 / S11 n/a (host-contract classes, prior passes); S12 — (full-tree Slither → Stage-P). WH-I CLOSED 2026-07-04 — 1 finding (F-17 S9 High, Fixed end-to-end incl. a faithful real-BPT fork witness); the F-16 dormant-tournament + O-D4 `activateBoost` + `CCBShare` dead surfaces still route to the P6 sweep. |
| WH-J | 2026-07-05 | Stage-J registry back-fill (slot registry; not a diff) — CLOSED 2026-07-05 | 1: `MiliariumRegistry` | n/a | n/a | ✓ | n/a | n/a | ✓ | n/a | ✓ | ✓ | — | n/a | — | WH-J.1 S8 slot-mutation access — NO FINDING: the only two mutators (`replaceSlot` L126, `setGovernanceContract` L144) are `onlyGovernance` exact-equality (`msg.sender != governanceContract → NotGovernance` L51); the constructor L66 is deploy-once; `setGovernanceContract` is idempotent-rebindable by design (J-D5 Stage-K handoff, GaugeRegistry mirror), not an ungated window; no unguarded state mutation exists. Post-K `governanceContract == AureumGovernance` (`DeployStageK.s.sol` L166); pre-K the direct `replaceSlot` caller is the trusted Authorizer Safe (the intended `DeployStageM`/`DeployStageN` population path). WH-J.2 S9 roster gaming — NO FINDING: membership is exclusive — the constructor L80 + `replaceSlot` L129 both revert `PoolAlreadyRegistered` via `_isMiliarium`, so a pool occupies ≤ 1 slot and appears exactly once in `_enumerated`; every consumer (`CCBMultiplier` aggregate L249-251, `VotingWeight` weight-sum L138-140, `TVLOracle` Leg-2 L227-229) iterates `[0, count)` and SUMS (order-independent) with no fixed-index cache, so the `_deregister` swap-remove reorder (L165-174) cannot mis-attribute; `_enumerated.length` is monotonic non-decreasing (no `removeSlot`), so `length - 1` L167 never underflows. The F-16 tournament reads a SEPARATE `_activeGauges` EnumerableSet in `GaugeRegistry` — orthogonal to this roster. O-D2 composition gate CONFIRMED firing: `replaceSlot` is reachable only via `AureumGovernance._executeProposal`, which reverts `CompositionQualityGateFailed` (`AureumGovernance.sol` L354) BEFORE the L357 `replaceSlot` call. S3 ✓ (no value arithmetic — 1-based-slot / dense-index bookkeeping only, monotonic length). S6 ✓ (zero external calls — H13-safe, no reentrancy surface). S1/S2/S4/S5/S7/S11 n/a (the registry holds no mint / fee / oracle / hook / factory surface). Cross-seam caveats routed forward, not reopened: (c) governance↔registry `onlyGovernance` [P6.3, confirmed transitively holding here] + (d) roster↔TVLOracle `isMiliarium`-skip dedup [P6.4]. S10 — (`DeployStageJ`/`M`/`N` `replaceSlot` wiring → P9); S12 — (full-tree Slither → Stage-P). WH-J CLOSED 2026-07-05 — 2 read-lens steps (WH-J.1 S8 / WH-J.2 S9), ZERO findings. |
| WH-L | 2026-07-05 | Stage-L incendiary + boost-delivery back-fill (formal WL pass; supersedes the P-D6 F-07 spot review; not a diff) — CLOSED 2026-07-05 | 2: `IncendiaryRegistry` / `EmissionDistributor` (boost-delivery leg, L-D25) | ✓ | n/a | ✓ | ✓ | n/a | ✓ | ✓ | ✓ | ✓ | — | n/a | — | WH-L.1 S9 economic + S4 + S6 — NO FINDING (formal WL pass, supersedes the P-D6 F-07 spot-only review). `buyBoost` (L268) gates in order (L-D20): phase `BoostsNotOpen` post-Year-1 (L270), rail `{svZCHF, sUSDS}` (L271), gauge `isGaugeApproved` (L272), `amount > 0` (L273); valuation `_valueInAuMM` (L411) divides the rate-scaled deposit by the 60-day-mature / 14-day-fresh price EMA via `_maturePrice` (L332) — the F-04/F-05/F-07 gate lineage, F-07 fix RE-VERIFIED HOLDING (`EMAStale` freshness gate L338 + `EMANotMature` maturity gate L334-336). Flash spot manipulation folds ≤ one α=2/61 step/day (`updateRailEMA` L298, daily cadence L302), cannot move the mature EMA; the 5% haircut (L275) + `toScaled18ApplyRateRoundDown` + `divDown` (L415-419) all bias against the buyer. S4 ✓ — `_spotRate` (L358) / `_valueInAuMM` read der Bodensee via `VaultExplorer.getPoolData` with `TokenNotInPool` (L373-374) + `ZeroSpotBalance` (L378-379) guards; index resolution by identity-match, never hardcoded. S6 ✓ — `buyBoost` pull-donate-before-commit (L277-280) carries no reentrancy guard but is doubly closed: the `{svZCHF, sUSDS}` rails are non-callback, and even a hypothetical reentry cannot bypass the cap (`_placeBoost` L486 reads `epochSkimAllocated` fresh, bounded by `capLeft`) or double-spend (each call pays its own non-refundable deposit). The shared 15%-of-epoch cap (`_epochCap` L458, `BOOST_CAP_BPS` L50) guarantees ordinary LPs retain ≥ 85% of every epoch's emission; FCFS walk-forward + non-refundable-deposit economics defeat griefing (a stranded-boost griefer pays real svZCHF/sUSDS for denied boost capacity, never core LP emission). WH-L.2 S1 + cross-seam (a) conservation — NO FINDING: the boost is SKIM-FUNDED, not minted — `_settlePool` (`EmissionDistributor.sol` L404-414) adds `boostIntegral / boostLP` to `poolAccRewardPerLP` with NO `recordEmissions` push (L-D15), and the continuous-phase LP tranche `_phaseAwareBody` L334 subtracts exactly `integratedSkim` (`rate*n - skim`); the L-D23 bound `Σ_pools boostIntegral ≤ integratedSkim` holds by construction — `_placeBoost` L489-490 writes BOTH `epochSkimAllocated[e]` and `epochPoolSkim[e][pool]` by the identical `alloc` (⇒ per-epoch equality), and the floored per-block views (L223 / L244) give floor-of-sum ≥ sum-of-floors, so delivered ≤ skimmed with sub-femto dust stranded protocol-favorably. `rate*n - skim` L334 cannot underflow — the 15% cap holds `skim ≤ ~30% × rate*n` even across a halving straddle. No `_mint` in the boost path; total mint ≤ schedule ≤ 21M (host-contract S1 = WH-H.1). S3 ✓ (haircut / valuation / per-block floor all round down protocol-favorable). S7 ✓ (F-07 re-verified Fixed — the maturity+freshness gate holds). S8 ✓ (`IncendiaryRegistry` is admin-less — all immutables, zero setters; the distributor's `setIncendiaryRegistry` is `onlyGovernance`, WH-H). ADJACENT OBS (→ P6, not a finding): a boost bought for a then-gauged pool still delivers if the gauge is later revoked (`boostIntegral` not re-gated in `_settlePool` per L-D10) — the non-refundable purchase is honored, stays skim-funded / cap-bounded, no conservation break. S2/S5/S11 n/a (no fee-routing / OQ-1-hook / factory surface); S10 — (`setIncendiaryRegistry` deploy wiring → P9); S12 — (full-tree Slither → Stage-P). WH-L CLOSED 2026-07-05 — 3 read-lens steps (WH-L.1 S9/S4/S6 · WH-L.2 S1 conservation · S3/S7/S8 sweep), ZERO findings; F-07 re-verified Fixed. |
| WH-P6 (cross-seam) | 2026-07-06 | Stage-P cross-seam synthesis — the four P-D6 named seams (cross-contract, not a single-contract back-fill or diff) — CLOSED 2026-07-06 | 4 seam pairs: (a) BodenseeBootstrapChannel + EmissionDistributor; (b) AureumFeeRoutingHook + EmissionDistributor recorder; (c) AureumGovernance + GaugeRegistry / MiliariumRegistry / VaultClassRegistry; (d) MiliariumRegistry + TVLOracle | F-18 | n/a | — | n/a | n/a | n/a | ✓ | ✓ | ✓ | — | n/a | — | 4 cross-contract seams (P-D6) — the synthesis a single-contract pass structurally cannot see. (a) channel↔distributor mint split · S1 = F-18 (Low, Fixed — the F-0 Bodensee bootstrap-tranche rate basis: `accrue` L206 keyed to `block.number` (call block) vs the distributor `_lpTrancheIntegral` L357 per-integrand-block basis, diverging to a halving-factor under-mint of der Bodensee if the first bootstrap-spanning `accrue()` lands in Era ≥ 1; keyed to `to`, see the Findings table). (b) hook↔recorder attribution · S9 ✓ — the F-09 fail-closed `trustedRouter` gate (hook L414 / L440) + the F-17 downward `_syncDown` compose cleanly: `userLP` grows ONLY via a trusted-router deposit, and every value-extracting path reconciles against live `balanceOf` (`claim` self-syncs at entry L611, `VotingWeight._positionPower` caps at `min(userLP, balanceOf)`, permissionless `syncPosition` heals the denominator), so a `getSender ≠ to` mismatch self-corrects to a forfeit and can NEVER over-credit — even a misreporting trusted router cannot over-credit (defense-in-depth beyond the F-09 trust model). (c) governance↔registry `onlyGovernance` · S8 ✓ — `AureumGovernance._executeProposal` L350 calls only `GaugeRegistry` (`revokeGauge` / `registerGaugeFromComposition`) + `MiliariumRegistry` (`replaceSlot`), both bound to `AureumGovernance` at K7 (`DeployStageK` L165 / L166) behind exact-equality gates; `VaultClassRegistry` is deliberately OUT of the path (K-D9 — governance stays at the multisig, its propose / veto / finalize lifecycle permissionless, only `revokeVaultClass` `onlyGovernance`); no mid-wiring execution window (the 9-call K7 order is atomic, and voting + timelock gate the first proposal execution until after call (9) migrates the Vault authorizer). (d) roster↔TVLOracle dedup · S7 ✓ — Leg-1's `isMiliarium`-skip (`TVLOracle` L218) is the exact complement of Leg-2 dense-enum ownership (WH-J exclusive membership: `isMiliarium(v) ⟺ v ∈ _enumerated`), so a Miliarium venue present in both legs is skipped in Leg 1 on EVERY occurrence and counted once in Leg 2 — dedup exact under a populated roster. ADJACENT OBS (not a finding — governance-controlled, OQ-22 Phase-1 design-scoped, EMA-damped): `addConstellationPool` L160 double-appends a NON-Miliarium venue holding 2+ same-underlying tokens, so `_constellationRatio` counts its ratio twice; folded into the existing WK.18 thin-venue `_constellationRatio` carry-forward → P / Q populated-roster fork sim. S10 — TWO P9 deploy-wiring invariants surfaced: (b) `setAuMTContractForPool(pool, hook)` MUST land BEFORE `setTrustedRouter(router, true)` (else trusted-router deposits revert `onlyAuMTContract`, fail-safe not corruption); (c) the P9 orchestrator MUST call `VaultClassRegistry.setGovernanceContract(multisig)` — no existing script binds it, else `revokeVaultClass` is permanently unreachable. S1 = F-18; S7 / S8 / S9 ✓; S2 / S4 / S5 / S6 / S11 n/a; S3 / S10 / S12 — (S3 rounding not the cross-seam focus; S10 → P9 wiring checklist; S12 full-tree Slither → Stage-P). CLOSED 2026-07-06 — 1 finding (F-18, S1 Low, Fixed), 3 seams clean, 2 P9 invariants + 1 WK.18 extension. |
| WH-P9 S11 (factory differential) | 2026-07-08 | `src/vault/AureumVaultFactory.sol` (196 L) vs audited upstream `pkg/vault/contracts/VaultFactory.sol` (169 L, submodule pinned `68057fda`) — textual line-by-line differential, not a stage diff (P9.6.2 per P-D33) | 1: `AureumVaultFactory` | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | — | ✓ | — | The S11 differential attestation backing the CLAUDE.md §1 audit-inheritance claim (P9.6.2, P-D33). The +27-line delta (196 vs 169) is FULLY accounted by three buckets; ZERO divergence beyond the documented Option-F2 diff. (1) Option-F2 functional: upstream's per-vault controller — the `deployedProtocolFeeControllers` mapping (up-L27) + `new ProtocolFeeController(IVault(vaultAddress))` + mapping store inside `create()` (up-L115-116) — REMOVED, replaced by the constructor-injected external controller: the `IProtocolFeeController` interface import (L17), the `INITIAL_FEE_CONTROLLER` public immutable (L33), the 9th constructor param + assignment (L80 / L86), and the Vault-creation arg swap `abi.encode(vaultExtension, _authorizer, INITIAL_FEE_CONTROLLER)` (L178, up-L151). The ONLY behavioral divergence in the Vault-creation path is the Vault's third constructor ARGUMENT (external controller instead of factory-deployed). (2) Naming / paths: contract rename (L22) + import re-anchoring `./` → `@balancer-labs/v3-vault/...` (L18-19), resolving to the SAME pinned submodule sources. (3) Comments only: immutable NatSpec (L27-28) + Slither rationales — naming-convention (L29-32), the B6 unindexed-event note (L49-53), encode-packed-collision ×3 + reentrancy-no-eth (L130-133 / L154-157 / L169-175). REMOVED CHECKS: none — every upstream guard retained verbatim: `onlyOwner nonReentrant` (L112), `VaultAlreadyDeployed` (L113-115), the three `InvalidBytecode` creation-code-hash pins (L117-123), pre- and post-deploy `VaultAddressMismatch` (L126-128 / L183-185), `isDeployed` set-last (L189), `VaultCreated` (L187). AUDIT INHERITANCE HOLDS: Vault / VaultExtension / VaultAdmin creation code is caller-supplied and hash-pinned against constructor-baked immutables (L23-25, unchanged), so the factory cannot deploy anything but the hash-pinned audited bytecode — F2 perturbs a constructor arg, never created bytecode. STORAGE-LAYOUT SHIFT (named, benign): deleting the mapping re-indexes the three retained mappings one slot lower (`deployedVaultExtensions` / `deployedVaultAdmins` / `isDeployed`, L35-37; immutables occupy no slots) — the factory is a standalone non-proxy, never delegatecalled, no upgrade path, so upstream layout parity is not load-bearing. ADDRESS PREDICTION UNAFFECTED: dropping the plain-CREATE controller deploy changes only the factory nonce; Create2 (L136 / L160) + CREATE3 (L176) derive from salt + deployer, nonce-independent — no predicted address shifts. ABI DELTA: getter `deployedProtocolFeeControllers(address)` dropped (grep: ZERO consumers project-wide), `INITIAL_FEE_CONTROLLER()` added, constructor 8 → 9 args. Pragma `^0.8.24` + SPDX identical (compiled at the pinned 0.8.26 profile). The §5d checkpoint (differential output isolating the diff lines) is satisfied by this textual differential plus the live fork exercise — `test/fork/DeployAureumVault.t.sol` deploys through this factory against real mainnet state, green in the P9.6.1 split-form run (123/123 fork); an on-fork differential deploy adds nothing since the created bytecode is hash-pinned, not factory-authored. S1-S9 n/a (single-surface differential pass; the factory's operational surface is owner-gated one-shot deploy, audited upstream); S10 — (the P9 deployment-surface roll-up lands as the next row, P9.6.3); S12 — (full-tree Slither → Stage-P close). |
| WH-P9 S10 (deployment surface) | 2026-07-08 | P9 deploy-script surface roll-up — the `DeployStageP` orchestrator + the J/F/G/H/I/M/N/L/K sub-scripts + the `DeployAureumVault` base-layer pre-compute chain; not a diff (P9.6.3 per P-D33) | P9 deploy scripts: `DeployStageP` + `DeployStage{J,F,G,H,I,M,N,L,K}` + `DeployAureumVault` | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | ✓ | n/a | — | The S10 deployment-surface roll-up for the P9 workstream (P9.6.3, P-D33). NO NEW FINDING — the four S10 lens vectors (address-prediction assumptions, deploy-order front-running, `setMinter` front-run, immutables baked with a wrong/influenced address) reviewed across the committed P9 deploy scripts; every vector clean or fail-fast-guarded. (1) IMMUTABLES-BAKED-WITH-WRONG-ADDRESS (the P-D31 composition class, the marquee vector): the base-layer-governor sentinel `DeployStageP._assertBaseLayerGovernor` (L86-89) reads the sole public-immutable ground truth `AureumAuthorizer.GOVERNANCE_MULTISIG()` via the live Vault authorizer and reverts `BaseLayerGovernorMismatch` (L88) BEFORE any orchestration, guarding the three base-layer slots baked at construction (AuMM `_minterAdmin`, the Vault authorizer's `GOVERNANCE_MULTISIG`, the hook's `_emissionRecorderAdmin`); the `DeployStageG` three-class authority split (P9.4b-fix, P-D31) closed the composition bug where the P-D30 'every setter = deployer' model broke under a unified governor — Class A seal-only = deployer (SwapAndDeposit `moduleAdmin` L123 + GaugeEligibility `gaugeRegistrySetter` L149), Class B = GOVERNANCE_MULTISIG (VCR `votingWeightSetter` / `governanceSetter` L143-144 + `GaugeRegistry.governance` L163), Class C `donateAuthorizer` = deployer through the seal then handed to GOVERNANCE_MULTISIG in-script via `setDonateAuthorizer` (L178) — a same-stage authoring-cycle catch fixed fix-forward, recorded as STAGE_P_NOTES P-D31 not an F-NN finding (never a live-script vulnerability past its own P9.4 audit loop). (2) ADDRESS-PREDICTION / CREATE3: the D-D21 / D36 pre-compute chain in `DeployAureumVault` — factory via `vm.computeCreateAddress(deployer, nonce+2)` (L164), Vault via `CREATE3.getDeployed(salt, predictedFactory)` (L171) — both fail-fast (`FactoryAddressMismatch` L203-204, `VaultAddressMismatch` L224-225), so an unexpected intervening CREATE reverts the deploy rather than baking a wrong address; Create2 / CREATE3 are salt + deployer-derived (nonce-independent), the same property attested for the factory in the S11 row above. (3) setMinter FRONT-RUN: AuMM's one-shot `setMinter` (`DeployStageK._wire` L163, wire (5), C-D11) — in the unified-governor fork model the deployer IS the governor (no front-run window), the `_minterAdmin` slot is sentinel-gated (vector 1), and production is per-Safe-batch (P-bis) where the one-shot + Safe multisig closes the window. (4) DEPLOY-ORDER: `DeployStageP._orchestrate` (L91-132) enforces J → F → G → H → setEmissionsRecorder → I → M → N → (seedFoundingPool ×3 + setGovernanceContract) → L → K structurally in call order — M/N before K, L before K, K LAST (`Vault.setAuthorizer` the final protocol mutation, `DeployStageK` L170); the four `_assertPostConditions` (L134-159) fail-fast deploy correctness (GENESIS_BLOCK 4-way L135-144, roster completeness over all 26 pools L147-152, `trustedRouter` structural-unset L154, authorizer migration L156-158); deploy-order front-running is moot for the atomic single-tx orchestrator, and production Safe-batch ordering is a P-bis operational concern. PRIOR + FORWARD S10 ITEMS CLOSED: F-03 (WK.17, S10 Low → Fixed) RE-VERIFIED WIRED — `TVLOracle.setMiliariumRegistry` present as K wire (8) (`DeployStageK` L168, F-03 / K-D8), no longer the WK.17 missing call; WH-P6 (cross-seam) S10 invariant (c) `VaultClassRegistry.setGovernanceContract(multisig)` IMPLEMENTED (`DeployStageP` L126, the P-D31 Class-B `governanceSetter` self-seal target); WH-P6 (b) `setAuMTContractForPool` before `setTrustedRouter` STRUCTURALLY MOOT for P9 — the orchestrator makes NO `setTrustedRouter` call (L154, trustedRouter stays unset = the F-17 / WH-P6 (b) safe default), the production `setTrustedRouter` bind + its ordering constraint move to P-bis / P10 (P-D26 (4)). D36 `vm.setEnv` race is a parallel-fork-TEST-harness concern (mitigated via `--threads 1`, the P9.6.1 run); the orchestrator's own `setEnv` threading (L93-112) is single-tx sequential, D36-race-free by construction (the F-D11 permanent env-key-namespacing fix in `DeployAureumVault.t.sol` stays a deferred carry-forward, not a new finding). S1-S9 n/a (the P9 artifacts are deploy SCRIPTS — the S1-S9 runtime surfaces belong to the deployed contracts, audited in WH-D/G/H/I/J/L); S11 n/a (the `AureumVaultFactory` fork-diff is the preceding row, P9.6.2); S12 — (full-tree Slither → Stage-P close). P9.6.3 CLOSED — the S10 deployment surface reviewed across the P9 scripts, ZERO new findings, F-03 re-verified + the two WH-P6 S10 invariants closed. |
| P10-close (S12 full-tree sweep) | 2026-07-10 | full-tree `slither .` 0.11.4 over `src/` — the S12 static-analysis gate every prior roll-up row deferred to Stage-P (P10.4 / P-D42 companion) | full Aureum `src/` (Slither: 142 total contracts analyzed incl. deps; 155 `src/` findings) | — | — | — | — | — | — | — | — | — | — | — | ✓ | Closes the `S12 → Stage-P close` deferral carried by WK / WK-R / WL / WN / WO / WH-D / WH-G / WH-H / WH-I / WH-J / WH-L / WH-P6 / WH-P9. **ZERO high/critical in `src/`** — the 155 `src/` findings are all benign LOW/MEDIUM/INFORMATIONAL classes (reentrancy-no-eth/benign/events, `block.number` strict-equalities, FixedPoint divide-before-multiply, unused-return, uninitialized-local, naming, dead-code), matching the WK-R (31 medium, benign) / WN baselines; the new P-stage `src/` code (F-16/F-17/F-18/F-19/F-20 + `activateBoost` removal) adds only benign reentrancy/strict-equality hits, each pre-attested in its finding's own review. The 2 project-wide high-severity results are BOTH upstream Balancer-submodule false-positives, OUT OF SCOPE per §1/§8c (byte-identical audited code): `incorrect-shift-in-assembly` ×8 in `WordCodec.sol` (the `1 << bitLength - 1` operator-precedence FP) + `uninitialized-state-variables` in `VaultCommon` / `VaultExtension` / `VaultStorage` (the transient-storage-slot pattern FP). `forge coverage` structurally unavailable (P-D42 — Yul stack-too-deep under the pinned `via_ir` profile); the surfaces-REACHED intent is met by execution evidence (the 877/136 split-form regression + all 20 F-01…F-20 PoC suites + the P6.5 640k-call invariant harness + the P10.2 wiring witness + the P-D36 7-leg e2e roster). **Ledger carries ZERO open findings** — F-01…F-20 = 18 Fixed + F-13 / F-15 Accepted-risk (both S-Low, adjudicated, deferred to Stage Q). S1-S11 n/a — this is the S12 static-analysis closure sweep, not a per-surface contract pass. |
|    |      |                   |           |    |    |    |    |    |    |    |    |    |     |     |     |       |

---

## WK pass — Stage K governance handoff · attestation log
> Per-step white-hat attestations for the WK pass (Stage K). Append one block per step as it closes. The coverage roll-up row above is filled at the WK.19 close-out per process §10/§12. A "no finding" step records an attestation here, not an `F-NN` row — the findings table gains a row only when a real finding lands.
- **Pass:** WK · **Scope:** `git diff stage-j-complete..HEAD` (`63471f9`, Stage K governance handoff) · **Opened:** 2026-06-11
- **Auditor model:** Fable 5 (`claude-fable-5`) — Aureum white-hat debug sprint
- **Status:** CLOSED 2026-06-12 — WK.1–WK.19 complete. 4 findings, all Confirmed + PoC-backed + disposition-routed (none Fixed): F-01 (S9, Medium — quorum propose-time-snapshot vs vote-time numerator; code-fix tally-time denominator) + F-02 (S9, Medium — F-9 concave-split Sybil; spec-level F-9 decision) + F-03 (S10, Low — K7 `_wire` omits `tvlOracle.setMiliariumRegistry`, K6 leg dormant + `registrySetter` unsealed; code-fix wire-(9) + deploy-replay) + F-04 (S4/S7, Low — `VotingWeight` consumes `TVLOracle.tvl` SPOT, atomically pumpable but F-9 quartic-self-limiting; → WH-H oracle EMA/TWAP). Coverage roll-up filled. Back-fill queue: WH-D (swap-fee S2 + OQ-1 hook S5), WH-G/WH-J (registry governance-gate audits), WH-H (INV-2 cross-contract emission-conservation harness + oracle spot-vs-EMA). Stage-P sweep inherits the raised floor. Resolution commits + the `stage-k-complete` tag = build-line decision.
**WK.1 · S1 supply/mint integrity · `src/token/AuMMMinterRouter.sol` — NO FINDING.**
- WK.1 S1: NO FINDING | checked: single mint path (`mintFor` L96-100); cap-not-circumvented (AuMM `mint` L114 ceiling runs on every forward; boundary-tested at cap / cap+1, test L75-87); INV-1 cumulative ceiling (no-burn confirmed AuMM L15); INV-6 two-layer minter gate (AuMM L113 + router L97); reentrancy (router has zero mutable state, AuMM has no transfer hooks); no era/overflow surface in router | could-not-assess: upstream `amount` computation (→WK.3 distributor, WK.5 channel); deploy-time `setMinter` handoff (→WK.17).
- Observation — the cap is an "ever-minted" ceiling only because AuMM has no burn path (no ERC20Burnable, AuMM L15); re-derive INV-1 if any future stage adds burn to AuMM.
- Observation — `mintFor` does not guard `recipient != address(0)`; relies on OZ `_mint` zero-receiver revert downstream (defense-in-depth, not a defect).
- Harness — `test/unit/AuMMMinterRouter.t.sol` 10/10 green.
**WK.2 · S8 access control & privilege · `src/token/AuMMMinterRouter.sol` — NO FINDING.**
- WK.2 S8: NO FINDING | checked: allowlist bypass (exact `msg.sender` equality on two immutables L97 — no `tx.origin`, no signature/permit); immutability (`AUMM`/`BOOTSTRAP_CHANNEL`/`EMISSION_DISTRIBUTOR` `immutable` L27/36/45, ctor-set + zero-checked L77-83, assigned nowhere else in repo); no escalation surface (`mintFor` is the sole function — no fallback/receive/delegatecall/selfdestruct/assembly/owner/admin/setter); governance cannot widen the allowlist or re-point mint (no governance-callable surface); permanent-minter lock (AuMM `_minterAdmin` zeroed L104, router has no re-delegation) | could-not-assess: deploy-baked consumer-address correctness (→WK.17 / S10); consumer-side `setMintRouter` binding (→WK.4 / WK.6).
- Observation (design property, accepted per locked architecture CLAUDE.md §1/§2) — the router is an irrevocable minter with no admin / pause / upgrade / kill-switch, consistent with the no-owner fair-launch design. Consequence: no runtime backstop halts emission if a consumer is later found exploitable, so consumer correctness (WK.3–WK.6) is load-bearing because nothing can revoke the router post-deploy. Not a finding; conscious risk-acceptance.
- Observation (scope boundary) — the router does not constrain `recipient`; recipient authority is fully delegated to the two consumers. Router S8 cleanliness does not extend to recipient-integrity (→WK.4 / WK.6).
**WK.3 · S1 supply/mint integrity · `src/emission/EmissionDistributor.sol` (`claim` mint path via router) — NO FINDING (K-diff surface).**
- WK.3 S1: NO FINDING (K-diff surface) | checked: sole mint site `claim` L535 `mintRouter.mintFor` → router → AuMM cap L114 (router-wiring faithful, cap preserved); CEI effects L532-533 precede interaction L535 (reentrant `claim` self-nullifies to amount=0 via the L528-529 zero-skip); `MintRouterNotSet` guard L534 strictly more restrictive than pre-K; `setMintRouter` one-shot + `onlyGovernance` L224-229 (mis-bind = liveness DoS, not inflation — bounded by AuMM's one-shot minter); differential INV-1/INV-6 preserved vs `stage-j-complete` | could-not-assess: INV-2 schedule conservation (see next line); deploy mintRouter/minter wiring order (→WK.17); hostile-router binding (→WK.4).
- Observation (highest-value carry-forward) — INV-2 (cumulative emission ≤ 21M by schedule construction) is UNAUDITED and UNIMPLEMENTED: no `ReferenceEmission` / `cumulativeAt` harness exists under `test/`, and the F-0/F-3/F-7 schedule math (`_accrueGlobal` L357, `_lpTrancheIntegral` L335, `_phaseAwareBody` L283, `blockEmissionRate`) is Stage-H code outside the K diff, never white-hatted. The 21M cap (AuMM L114) is a hard backstop on every mint, so the failure mode is a claims-revert liveness cliff (`SupplyCapExceeded` at cumulative 21M), not silent inflation. Recommend a dedicated back-fill WH-H pass (or the Stage P sweep) that builds the INV-2 independent reference-emission harness.
- Observation — AuMM.t.sol implements INV-1 (`invariant_totalSupply_neverExceedsCap` L265) and INV-6 (`invariant_mint_revertsFromNonMinter` L269) against `AuMM.mint` in isolation only — not through the distributor accrual flow.
- Harness — `test/unit/EmissionDistributor.t.sol` + `test/unit/AuMMDistributorIntegration.t.sol` 110/110 green (incl. 4 `setMintRouter` one-shot tests + zero-recipient revert).
**WK.4 · S8 access control & privilege · `src/emission/EmissionDistributor.sol` — NO FINDING (K-diff surface).**
- WK.4 S8: NO FINDING (K-diff surface) | checked: K-added `setMintRouter` onlyGovernance + one-shot + ZeroAddress L224-229 (locks mint-routing topology post-bind, adds no new privilege); `claim` permissionless but mints only caller's own accrued (user=msg.sender L526) — no force-mint to arbitrary recipient; `_accrueGlobal` permissionless poke advances accumulators only (no mint); recorder gate `onlyAuMTContract` L176-179 + one-shot governance binding `setAuMTContractForPool` L201-206; emission inflation needs bound recorder + independent gauge approval (`recordScore` L434) + 21M cap (defense-in-depth); uninitialized slots fail-safe to revert; one-shot `setMintRouter` / `setAuMTContractForPool` survive a later-compromised governance | could-not-assess: deploy binding correctness (→WK.17); cross-authority collusion (→Stage P).
- Observation (carry-forward — Stage-H/I, not K-touched, never white-hatted → WH-H/WH-I) — `setGovernanceContract` L188-193 is single-step mutable (no Ownable2Step); a wrong-address handoff bricks every `onlyGovernance` setter irreversibly (deploy-tested → WK.17; consider a two-step transfer).
- Observation (carry-forward) — `setIncendiaryRegistry` L213-217 is mutable + zero-permitted + no try/catch; a hostile or reverting registry DoSes accrual (deliberately conservative per H-D29, but a governance-mutable lever over emission liveness).
- Observation — recorder LP-inflation: the bound AuMT recorder can credit arbitrary `(user, amount)` LP (`recordDeposit` L462); bounded by gauge approval + 21M cap; the residual trust is the one-shot initial binding.
- Harness — S8 access-control tests green within the 110/110 distributor run (`setGovernanceContract` ×3, `setAuMTContractForPool` ×4, `setIncendiaryRegistry` ×3, `setMintRouter` ×4, recorder-gate ×2, `recordScore` gauge-gate ×2).
**WK.5 · S1 supply/mint integrity · `src/emission/BodenseeBootstrapChannel.sol` (bootstrap-flush mint via router) — NO FINDING (K-diff surface).**
- WK.5 S1: NO FINDING (K-diff surface) | checked: sole mint site `distribute` L267 `mintRouter.mintFor` → router → AuMM cap L114 (router-wiring faithful); conservation double-backstop (reserve-delta L307-308 + bptOut==0 L306 + residual balanceOf==0 L276-277 → no stranded mint; atomic rollback on callback failure); reentrancy double-belt (onlyGovernance L261 + CEI pendingAccrual=0 L264 → reentrant `distribute` reverts NoPendingAccrual L263 + `_EXECUTING_SLOT` transient guard L268); AP math bounded + underflow-safe L239-253; single rate snapshot exact (bootstrap window entirely within Era 0); differential INV-1/INV-6 preserved | could-not-assess: INV-2 cross-contract conservation (see observation); real-Vault `distribute` fork path (→WK.18); deploy wiring (→WK.17).
- Observation (highest-value carry-forward — sharper than WK.3) — INV-2 spans TWO mint sources: the channel and the distributor both mint against the same 21M budget, and H-D2 splits them (the distributor's `_lpTrancheIntegral` subtracts the F-0 bodensee share and hands that leg to this channel). Whether the hand-off conserves (no overlap → double-mint, no gap) is a CROSS-CONTRACT seam neither WK.3 nor WK.5 can settle in isolation. Still unaudited, no `ReferenceEmission` harness. The 21M cap bounds total emission (no infinite inflation); a split error would misallocate LP-vs-Bodensee emission against the F-0 spec. Recommend the WH-H back-fill / Stage P sweep build an INV-2 harness summing channel + distributor mints against an independent reference.
- Harness — `test/unit/BodenseeBootstrapChannel.t.sol` 34/34 green (incl. `setMintRouter` ×4, MintRouterNotSet, Reentrant, HelperBalanceNonZero, ReserveDeltaMismatch, BptMintedOnDonation, CallbackPayloadMismatch, NoPendingAccrual, happy-path, 8 accrue/AP tests).
**WK.6 · S2 fee/emission-routing conservation · `src/emission/BodenseeBootstrapChannel.sol` — NO FINDING (K-diff surface).**
- WK.6 S2: NO FINDING (K-diff surface) | checked: destination immutable (`BODENSEE_POOL` L28 + `_aummIndex` L34, ctor-set L137/L155, no setBodenseePool setter anywhere); single egress (only `safeTransfer` L292 → Vault; no other transfer/withdraw/sweep/rescue/call/selfdestruct); single donation target (`addLiquidity` pool=BODENSEE_POOL L298); 1:1 conservation chain pendingAccrual→mint→donate, reserve+=amount L308, no BPT skim L306, residual==0 L276-277 (INV-3 nothing-stuck holds); no creator fee (`totalDistributed` L55 view-only) | could-not-assess: real-Vault DONATION fork mechanics (→WK.18; asserts enforce conservation regardless); swap-fee routing (Stage D, not K-touched) → WH-D back-fill.
- Observation — WK.6 attests the emission-donation channel's conservation only; the protocol's primary S2 surface (the 50% swap-fee → Bodensee path in `AureumProtocolFeeController` / `AureumFeeRoutingHook`, Stage D) is not in the K diff and is a WH-D back-fill item.
- Harness — conservation tests green within the 34/34 channel run (HelperBalanceNonZero, ReserveDeltaMismatch, BptMintedOnDonation, Distribute happy-path).
**WK.7 · S8 access control & privilege · `src/governance/AureumGovernanceAuthorizer.sol` — NO FINDING.**
- WK.7 S8: NO FINDING | checked: canPerform routing (governance=all + NOT time-boxed L66-68; multisig = emergency-set ∩ window L69; default-deny L72); strict `<` window boundary L69 — no off-by-one (END=deploy+2_628_000 L58; `test_canPerform_windowBoundaryStrict` windowEnd-1 allow / windowEnd deny); emergency set {pauseVault, enableRecoveryMode} L75-77, actionIds `keccak256(abi.encodePacked(bytes32(uint160(vault)), selector))` MATCH Balancer `Authentication.sol:47-51` + `VaultAdmin.sol:77` disambiguator; B-strict containment (multisig can pause / enable-recovery but NOT unpause / disable / setAuthorizer / setFee — only governance reverses; worst case temporary DoS not theft; expires 12mo); 5 immutables, no setters, no widening path; ctor zero-checks L52-54; 12/12 tests green | could-not-assess (SILENT-FAILURE → WK.17): the actionId match holds only if deploy passes `vault_` == the real Vault VaultAdmin is bound to; ctor can only zero-check, not verify — a wrong `vault_` silently breaks the multisig's emergency powers with no deploy revert. WK.17 must assert `vault_` == real Vault + fork-test a multisig pauseVault round-trip through the installed authorizer.
- Observation — `where` is ignored in canPerform; acceptable since governance is where-agnostic by design and the multisig is tightly scoped by vault-specific actionIds.
**WK.8 · S8 access control & privilege · `src/governance/AureumGovernance.sol` — NO FINDING.**
- WK.8 S8: NO FINDING | checked: bounded blast radius — `_executeProposal` L337-350 performs ONLY 3 hardcoded param-validated actions (revokeGauge L339 / atomic slot-swap L341-344 / setStaticSwapFeePercentage-in-bounds L348); NO arbitrary-`(target,calldata)` execute (governance holds full Vault authority via the authorizer but its code only ever calls setStaticSwapFee — the rest is latent/inert); timelock unbypassable (eta set only in `queue` L311 = block+2d always future; execute requires Queued L325 + block>=eta L326; queue one-shot self-guarding L308; no eta setter); one-shot execute + CEI (`executed=true` L327 before `_executeProposal` L328 → ProposalAlreadyExecuted L322); entry points gated (propose×3 deposit-gated L198-199 + target-validated L214/225-227/239-240; castVote window + one-vote + CEI L254-257; queue Succeeded-gated); 5 immutables, no setters, no delegatecall/selfdestruct; 43/43 green | could-not-assess: REGISTRY ACCESS GATES — the vote+timelock is the sole path to revokeGauge / replaceSlot / registerGauge / setStaticSwapFee ONLY IF those callees gate to this governance contract; GaugeRegistry / MiliariumSlotRegistry are Stage G/J (not K-touched), so WK.17 must confirm their governance binding points here + a WH-G/WH-J back-fill must audit the gates.
- Observation — tally math → WK.9; reentrancy in execute external calls → WK.11; deposit economics → WK.10; weight integrity → WK.12-14.
**WK.9 · S3 rounding / invariant precision · `src/governance/AureumGovernance.sol` (vote tally, quorum, F-9 dampening) — NO FINDING on arithmetic; one SUSPECTED item raised.**
- WK.9 S3: NO FINDING on tally arithmetic (ZERO division in the entire contract → no rounding risk; quorum L289 / 2-of-3 supermajority L291 / simple majority L293 all cross-multiplied & integer-exact; deposit L179-183 constant lookup; no overflow for any plausible weight scale; F-9 dampening is upstream in `VOTING_WEIGHT.governanceWeight` L258 → WK.12) | SUSPECTED (first candidate finding of the pass; formal F-NN row deferred to WK.10 post-PoC): the quorum denominator is the propose-time snapshot `snapshotTotalSupply = VOTING_WEIGHT.totalSupply()` L201/L289, while vote-weight numerators accrue at vote-time (poke + governanceWeight L257-258) — diverges from the standard same-block Governor snapshot; permissive direction when global voting supply grows over the ~14d (BLOCKS_PER_EPOCH) window; bounded by VotingWeight's 14d qualification cliff (blocks flash-vote) + 6mo on-ramp slope; MAY be intended per I-D17 veto-denominator semantics.
- Routing — exploitability PoC at WK.10 (S9: a proposer picks a low-supply propose block + late-window high-weight vote; quantify the quorum-gaming gain within the cliff/on-ramp limits); intent/spec confirmation at WK.13 (VotingWeight `totalSupply` / I-D17). Resolves to Confirmed (Low-Med quorum-gaming) or Accepted-risk / False-positive.
- Harness — quorum/tally boundary tests green in the 43/43 run (`test_state_quorumBoundaryStrict`, `supermajorityExactPass`, `belowSupermajorityDefeated`, `gauge_tieDefeated`, `fee_simpleMajorityPass`).
**WK.10 · S9 economic/incentive · `src/governance/AureumGovernance.sol` (quorum-snapshot timing) — FINDING F-01 (Confirmed, Medium).**
- WK.10 S9: FINDING F-01 (resolves the WK.9 SUSPECTED item) | checked: propose-time snapshot `VOTING_WEIGHT.totalSupply()` L201 stored L204 vs vote-time numerator `poke`+`governanceWeight` L257-258 feeding quorum L289; proposer NOT poked in `_createProposal` L196-206 (absent from denominator) but self-pokes in `castVote` L257 (present in numerator) → turnout can exceed 100% of snapshot; root cause `VotingWeight._totalQualifiedWeight` lazy poke-accumulator L115-120 excludes never-poked qualified holders; PoC 4/4 green proves all three proposal types pass under a shrunken/zero snapshot | could-not-assess (→WK.13): whether the propose-time snapshot is intended per I-D17 veto-denominator semantics (`VotingWeight.totalSupply` was specced for `VaultClassRegistry.vetoProposal`, not as an AureumGovernance quorum base) — F-01 stands on behavior regardless; resolution (code fix vs Accepted-risk) follows WK.13.
- Quantified — effective quorum = 20% of poked weight, not eligible weight; leverage ρ = poked/eligible is attacker-influenceable down (don't poke honest holders; poke withdrawn holders down) and structurally excludes the attacker's own weight; at ρ→0 (early protocol) quorum is vacuous and a single 1e18 vote carries.
- Bounds (→ Medium, not High) — 14-day qualification cliff (no flash weight, VotingWeight L137); non-refundable 1,000 svZCHF / 1,250 sUSDS deposit; ~16-day vote+timelock public contest (one larger honest against-vote defeats simple majority, ≥half defeats the 2/3 composition); permissionless poking keeps the accumulator honest; WK.8 bounded blast radius (no theft, governance-reversible). Rises toward High under sustained thin participation.
- Fix direction (resolution commit, post-WK.13) — read the quorum denominator at tally-time `endBlock` so all self-poked voters are included (turnout ≤ 100%), or adopt Governor-style historical weight checkpoints.
- Harness — `test/whitehat/F01_quorumSnapshotTiming.t.sol` 4/4 green: turnoutCanExceedSnapshot (forVotes 10_000e18 > snapshot 100e18), minorityAttackerRevokesGauge (200e18 ≈ 0.4% of true weight revokes a gauge), vacuousQuorumWhenAccumulatorEmpty (snapshot 0), singleVoterClearsCompositionSupermajority (100e18 clears 2/3 + executes the slot swap).
**WK.11 · S6 reentrancy & read-only reentrancy · `src/governance/AureumGovernance.sol` — NO FINDING.**
- WK.11 S6: NO FINDING | checked: castVote CEI — `hasVoted[id][msg.sender]=true` L256 set BEFORE the sole external call `VOTING_WEIGHT.poke` L257 → reentrant castVote reverts AlreadyVoted L255; execute CEI — `p.executed=true` L327 set BEFORE `_executeProposal` L328 (all external calls live there) → reentrant execute reverts ProposalAlreadyExecuted L322; queue has zero external calls (eta set + emit only) L307-313; `_createProposal` external calls (`safeTransferFrom` L198 + `BODENSEE_CHANNEL.donate` L199) precede `++proposalCount` L200 + snapshot L201, payToken restricted to SVZCHF/SUSDS immutables (`_depositAmount` reverts otherwise L179-183 — no attacker-controlled token), donate is `onlyAuthorizedDonator` + runs inside `_vault.unlock` (Vault nonReentrant); no receive/fallback/delegatecall/selfdestruct/assembly/low-level-call in the contract; all callees (VOTING_WEIGHT, GAUGE_REGISTRY, SLOT_REGISTRY, VAULT, BODENSEE_CHANNEL) are trusted protocol contracts; read-only reentrancy — the view surface (state/getProposal/hasVoted) reads consistent frozen state during execute (executed=true pre-set, tally final post-endBlock, eta set), and `snapshotTotalSupply` is the poke-accumulator value not a live TVL/price read, so no stale-read window | could-not-assess: whether `GAUGE_REGISTRY.registerGaugeFromComposition(newPool)` L344 callbacks into an attacker-controlled `newPool` during composition execute (GaugeRegistry = Stage G, not K-touched → WH-G; bounded regardless by the executed-flag re-execution block + frozen tally); real-Vault reentrancy of `VAULT.setStaticSwapFeePercentage` L348 + the donate→`_vault.unlock`→`addLiquidity` Bodensee path + der Bodensee add-liquidity hook reachability → WK.18 / WH-D.
- Observation — a benign reentrant `propose*` during the donate `_vault.unlock` callback is possible in principle but harmless: each proposal pays its own non-refundable deposit and gets an independent `++proposalCount` id + own snapshot; no shared-state corruption, no double-spend.
- Observation — the propose-time deposit donation (Vault.unlock + addLiquidity to der Bodensee, L199 → `SwapAndDepositToBodensee` L376/L411) mutates Bodensee TVL before the L201 snapshot read, but `snapshotTotalSupply` is `_totalQualifiedWeight` (poke-accumulator) not a TVL read, so there is no read-only-reentrancy coupling between the donation and the quorum base (orthogonal to F-01).
- Harness — no new PoC; the castVote/execute CEI revert guards are exercised in the K4.6 cohort (`AureumGovernance.t.sol` L401 AlreadyVoted + L603 ProposalAlreadyExecuted) within the 43/43 unit run.
**WK.12 · S9 economic/incentive · `src/governance/VotingWeight.sol` (F-9 weight integrity, gauge-gaming) — FINDING F-02 (Confirmed, Medium); 3 sub-surfaces clean.**
- WK.12 S9: FINDING F-02 + clean(flash/on-ramp/gauge) | checked clean: flash-loan resistance — value is recorder share `tvl·userLP/poolTotalLP` (hook-recorded deposits, not spot BPT, L138-142) + 14-day cliff `timeInPool<QUALIFICATION_PERIOD_BLOCKS→0` L137 → transient capital confers 0 (OQ-25 holds); on-ramp/cliff timing — `cappedTime=min(timeInPool,ON_RAMP)` L144-146 monotonic in held blocks, withdrawal resets `effectiveQualBlock` (no carry) → no time-gaming; gauge gate — unapproved pools confer 0 L133 | FINDING F-02: per-position concave root `base.powDown(e)` L152 (e=0.25 Era0 L149-151) is subadditive → splitting LP across N addresses multiplies summed `governanceWeight` by N^(1-e); PoC `test/whitehat/F02_concaveSplitSybil.t.sol` 3/3 green (16-split ≈8×, 81-split ≈27× a single consolidated position of identical LP) | could-not-assess (→WH-F + spec): whether F-9 Sybil-splittability is an accepted spec tradeoff (`11_formulas.md` F-9 / `03_theoretical_foundation.md` CCB / I-D7); whether the same concave split amplifies CCB/emission weight (EmissionDistributor/CCBMultiplier, Stage F/H, not K-scoped → WH-F/WH-H).
- Mechanism — x^e (e<1, f(0)=0) subadditive: Σf(pieces) ≥ f(Σ); value conserved across split (`tvl·Σlp_i/totalLP`, L142) but summed power amplified by N^(1-e); cliff/on-ramp/gauge identical across pieces (cancel); concavity per-(pool,holder) summed per holder (poke L110-112), no cross-address aggregation possible on-chain w/o identity → free Sybil multiplier on the same real capital.
- Bounds (→ Medium) — per-address gas + N-wallet ops (only real friction, scales with N); 14-day cliff (genuine non-flash capital held); value/base dust floor L143/L148 (very low); no on-chain Sybil resistance; bounded blast radius per WK.8 (no theft, governance-reversible); compounds F-01 (both lower the governance-capture bar).
- Fix direction (resolution, post-WH-F intent) — split-invariant form: apply the concave root to pool aggregate value distributed pro-rata by LP share (`share·poolValue^e`), preserving cross-pool concavity while being within-pool split-invariant; alternatively accept the concave-voting Sybil tradeoff explicitly. Spec-level F-9 change either way.
- Harness — `test/whitehat/F02_concaveSplitSybil.t.sol` 3/3 green: singleConsolidatedPositionWeight (positive baseline), split16xGivesApprox8xWeight (7×–9× bracket = 16^¾), split81xGivesApprox27xWeight (24×–30× bracket = 81^¾).
**WK.13 · S9 intent/spec confirmation · `VotingWeight.totalSupply()` semantics (I-D17) — F-01 intent RESOLVED: deviation, not intended reuse.**
- WK.13 intent: F-01 resolves to a DEVIATION from documented design intent, not an Accepted I-D17 reuse | evidence: (1) I-D17 (STAGE_I_NOTES L179) explicitly DEFERRED the `totalSupply()` veto-denominator semantics to "the Stage K snapshot layer", preserving only the function shape — the snapshot choice is a Stage-K (K4) decision, not an inherited I-D17 constraint; (2) VotingWeight NatSpec L13-14/L21 documents the design invariant `vetoSupport ≤ totalSupply` ("exact veto fraction … by construction"); (3) the original consumer `VaultClassRegistry.vetoProposal` PRESERVES it — reads `governanceWeight(msg.sender)` WITHOUT poke (L277, the checkpoint already summed into the accumulator) + `totalSupply()` LIVE at evaluation (L279), so numerator+denominator share one accumulator → support ≤ totalSupply structurally; (4) AureumGovernance BREAKS it — stores a propose-time `snapshotTotalSupply` (L201/L204) used as a stale denominator (L289) WHILE poke-refreshing the voter at vote-time (L257) above that stale base → forVotes can exceed snapshot (F-01 turnout>100%). The combination (stale stored denominator + refreshed numerator) is the divergence; either alone preserves the invariant.
- Resolution — F-01 stays Confirmed/Open, disposition CODE FIX (not Accepted-risk). Invariant-preserving fix mirroring vetoProposal: compute quorum against `totalSupply()` read LIVE at tally-time `endBlock` (by then every voter has self-poked, so forVotes ≤ totalSupply is restored). VotingWeight has no historical checkpoints, so a propose-time-snapshot-of-both is infeasible; the tally-time live read is the feasible alignment.
- Observation (corroborating) — empty-accumulator handling diverges: vetoProposal's live div-form L279 `(vetoSupport*10_000)/totalSupply()` REVERTS at totalSupply()==0 (fails closed, no spurious veto), while AureumGovernance's stored mul-form L289 `totalVotes*10_000 < snapshot*QUORUM_BPS` PASSES vacuously at snapshot==0 (F-01 `test_vacuousQuorumWhenAccumulatorEmpty`) — the mul-form's permissive direction is the unsafe choice.
- Observation (adjacent, →WH-G) — vetoProposal's L279 division-by-`totalSupply()` reverts (veto DoS, fails closed) on an empty accumulator; Stage-G contract, not K-diff — noted for the WH-G back-fill, not raised as a finding here.
- F-02 intent — NOT resolved here; the F-9 concave-Sybil spec-acceptance question stays routed to WH-F + the canonical spec (`11_formulas.md` F-9), distinct from F-01's totalSupply-snapshot intent. Constitution-level snapshot-voting intent (10_constitution §xxix) not fetched this step; the in-repo documented invariant is authoritative and already violated, which is sufficient to confirm the deviation.
**WK.14 · S3 rounding / invariant-precision · `src/governance/VotingWeight.sol` — NO FINDING.**
- WK.14 S3: NO FINDING | checked: exact-fraction accumulator invariant `_totalQualifiedWeight == Σ _holderWeight[*]` — `poke` adjusts the accumulator by EXACTLY the signed delta `newWeight-oldWeight` (L116-120) so the sum is preserved across re-pokes/withdrawals; no-op short-circuit `newWeight==oldWeight→return` L114 correct; underflow-safe — the subtraction arm `_totalQualifiedWeight -= oldWeight-newWeight` L119 cannot underflow because the invariant guarantees `_totalQualifiedWeight ≥ _holderWeight[holder]=oldWeight` (oldWeight is one summand) → result ≥ newWeight ≥ 0 (NatSpec L100-103); accumulator update is pure integer add/sub (no rounding drift); F12/F13 signed-delta discipline (branch-on-sign, unsigned sub per arm) holds; rounding UNIFORMLY *Down — `value` L142 / `base` L147 / `powDown` L152 round toward zero → a holder's weight is only ever UNDER-stated, never inflated; zero mulUp/divUp/powUp in the file (grep-confirmed) → no mixed-rounding asymmetry → safe for voting (voter can't round own weight up) + veto (consistent rounding preserves `vetoSupport ≤ totalSupply`); per-holder weight = Σ over ≤28 pools of base^e, base bounded by realistic TVL, base^¼ tiny → no uint256 overflow | could-not-assess: ORACLE.tvl precision/manipulation (TVLOracle = Stage H, black-box here → S7 / WH-H); powDown exact rounding bound on extreme inputs (Balancer LogExpMath, trusted audited primitive → differential vs upstream if needed).
- Observation — `value = tvl.mulDown(lp.divDown(totalLP))` L142 is division-before-multiplication (share fraction first, then multiply); minor precision loss vs `(tvl·lp)/totalLP` but always DOWN (conservative) and negligible for realistic values; not a finding.
- Observation — sub-1e-18 LP share rounds `value`/`base` to 0 → 0 weight (dust floor L143/L148); denies dust weight (safe direction, never grants), and bounds the F-02 split-piece from below (a Sybil piece must clear the dust floor to confer any weight).
- Harness — no new PoC; the F12/F13 signed-delta + exact-fraction invariant is directly exercised by the K3 cohort (`VotingWeight.t.sol` test_Poke_TotalSupply_InvariantHolds L339, test_Poke_TopUp_PositiveDelta L356, test_Poke_WithdrawalReset_NegativeDelta L329, test_Poke_Noop_WhenWeightUnchanged L314) within the 29-green unit run.
**WK.15 · S8 + S3 · `src/emission/TVLOracle.sol` K6 re-wire (`setMiliariumRegistry` one-shot + `_constellationRatio` two-leg) — NO FINDING on the re-wire; one SUSPECTED cross-cutting item raised (spot-tvl governance-weight manipulation → WK.18 fork PoC; amends WK.12).**
- WK.15 S8 (setMiliariumRegistry K6.1): NO FINDING | checked: one-shot self-seal — `registrySetter` set to deployer at ctor, zeroed L197 on first bind → re-calls revert `OnlyRegistrySetter` (no caller is address(0)), F-D20 pattern mirroring CCBMultiplier; authority-before-zero — L194 `OnlyRegistrySetter` fires before L195 `ZeroAddress` (unauthorized caller errs regardless of arg, fails closed); zero-checked L195; no re-point — `miliariumRegistry` written only at L196, sealed thereafter; additive — unbound (address(0)) preserves exact pre-K6 behavior | could-not-assess: deploy binds the REAL MiliariumRegistry (ctor/setter zero-checks only, cannot verify identity — a wrong-but-nonzero registry binds+seals silently; same silent-misconfig class as WK.7 vault_) → WK.17.
- WK.15 S3 (_constellationRatio K6.2): NO FINDING | checked: div-by-zero guarded — `count==0 ? 0 : acc/count` L236 + `_venueRatio` divides by balU only when balU>0 L260-261; rounding `(balZ*1e18)/balU` L261 + `acc/count` L236 both round DOWN → tvl understated → conservative for all consumers (harder to clear gauge/voting thresholds); two-leg dedup — Leg 1 skips `isMiliarium(v)` venues L218, Leg 2 owns Miliarium pools L226-234 → counted once; additive — unbound registry skips Leg 2 and Leg-1 skip never fires (exact pre-K6) L211/L218/L226; overflow — `balZ*1e18` / `amount*ratio` bounded by realistic scaled balances ≪ uint256 | could-not-assess: dedup correctness depends on the MiliariumRegistry invariant `isMiliarium(miliariumPoolAt(k))==true ∀k` (Stage J — a dense-list pool that were isMiliarium-false would double-count, skewing the average) → WH-J.
- SUSPECTED (cross-cutting → WK.18 fork PoC) — `TVLOracle.tvl()` L272-283 is a SPOT read of `getPoolData().balancesLiveScaled18` with NO oracle-side EMA/TWAP (the "daily EMA cadence" is consumer-side EMASampler, Stage F, not in tvl()). VotingWeight consumes it ATOMICALLY in the weight path — `value = ORACLE.tvl(pool) × userLP/poolTotalLP` (VotingWeight L142), read at `poke`/`castVote`. The OQ-25 recorder-share design closes the LP-share leg (a normal add-liquidity raises tvl AND recorder poolTotalLP → cancels) but NOT the tvl leg: a flash SWAP (or a DONATION if the pool enables it) moves tvl(pool) without moving recorder poolTotalLP → inflates `value` at castVote, then unwinds. LIKELY self-limiting — the F-9 0.25/⅓ root means doubling weight needs a 16×/8× tvl pump whose AMM slippage may dwarf the gain (root + slippage = implicit manipulation resistance) → may resolve False-positive/Accepted-risk. Confidence Suspected (no PoC this step).
- Amendment + routing — this SHARPENS the WK.12 flash-resistance attestation, which credited only the LP-share leg, not the spot-tvl factor. Exploitability PoC at WK.18 (flash-swap a constellation venue / donate to a Miliarium pool, poke+castVote atomically, measure governanceWeight inflation vs slippage cost); spot-vs-EMA tvl() design is H-D9 Stage-H (not K6-touched) → WH-H; if confirmed → F-03 (Suspected→Confirmed), compounding F-01/F-02.
- Harness — K6 re-wire exercised by the K6.3 cohort (`TVLOracle.t.sol` 42/42 green per §11); no new PoC this step — the SUSPECTED tvl item's PoC is WK.18.
**WK.16 · S12 general Solidity · K-diff contracts (7 logic files) — NO FINDING.**
- WK.16 S12: NO FINDING | checked: danger-keyword sweep across all 7 K-diff logic contracts — ZERO tx.origin / delegatecall / selfdestruct / inline-assembly / unchecked-block / ecrecover / permit-EIP712 / low-level `.call` / `.call{value}` (the two `selfdestruct` + one `assembly` grep hits are NatSpec prose, not code); EIP-1153 transient storage (`BodenseeBootstrapChannel`) — 3-slot Balancer-V3 assembly-helper pattern (G-D14; solc 0.8.26 lacks the `transient` keyword), `tstore`/`tload` opcodes require Cancun = matches locked `evm_version`; slots written L269-271 → nested `_vault.unlock` L272 → cleared L273-275 (+ auto-clear at tx end), no stale-transient leak; callback `_distributeCallback` reads `_PENDING_AMOUNT_SLOT.tload` L289 same-tx, `OnlyVault` L288 + `CallbackPayloadMismatch` L290 block spoofed/corrupted reads; `abi.encodePacked` actionId derivation (`AureumGovernanceAuthorizer` L61-62) — both args fixed-length (`bytes32` vaultDisambiguator + `bytes4` selector = 36B), no ambiguous-boundary collision, matches Balancer `Authentication` (WK.7); decimals — `TVLOracle` consumes `balancesLiveScaled18` (Vault-normalized to 18), no raw-`balanceOf`/decimals mismatch | Slither 0.11.4 (134 contracts, 121 results) — ZERO high/critical detectors on K-diff src: NO arbitrary-send / unchecked-transfer / weak-prng / tx-origin / controlled-delegatecall / suicidal / reentrancy-eth; only low/informational classes (triaged next bullet) | could-not-assess: real-Vault nested-call transient behavior on the live donation path (→WK.18 fork); Stage-H/Stage-J loop callees (→WH-H / WH-J).
- Slither low/informational triage (all NO FINDING) — `reentrancy-no-eth`/`-events`/`-benign` on `EmissionDistributor` (`claim`, `_settlePool`, `record*`) + `AureumGovernance` (`castVote`, `_createProposal`, `execute`) + `BodenseeBootstrapChannel` (`_distributeCallback`): CEI-correct, trusted callees, no ETH at risk — already adjudicated WK.3/WK.4/WK.5/WK.11; `incorrect-equality` (`_accrueGlobal`/`claim`, VotingWeight `poke`/`_positionPower`): intentional `== 0` / no-op guards on control flow (poke no-op L114 per WK.14, claim zero-skip L528-529 per WK.3), not equality on manipulable balances; `unused-return` (`distribute`/`_distributeCallback`/`_currentReserve`): intentional Vault-interaction discards documented in NatSpec L284 (`unlock` empty-bytes return, `settle` credit discarded by design, `getPoolTokenInfo` only `balancesRaw` needed, `addLiquidity` captures+checks `bptOut`); `missing-zero-check` (`EmissionDistributor.setIncendiaryRegistry.newRegistry` L213): zero deliberately permitted per H-D29 = WK.4 carry-forward, not new.
- Observation (cosmetic, →WH-H) — `EmissionDistributor._lpTrancheEmission` L239-241 is dead code (Stage-H H-D21 STUB superseded by `_lpTrancheIntegral`); recommend removal at the WH-H back-fill; no security impact (internal pure, never called).
- Observation (gas, bounded → sharpens WK.15 / →WH-J) — `TVLOracle._constellationRatio` L209-237 + `_venueRatio` L247-264 make external calls inside loops over the Miliarium roster (`miliariumPoolsCount` L227, `getPoolTokens`/`getPoolData` per venue); gas scales with roster size, bounded by the ≤28-pool Miliarium design cap (no unbounded gas-DoS within the constraint). But `tvl()` is consumed ATOMICALLY by `VotingWeight.poke`/`castVote` (VotingWeight L142), so an unenforced cap would let an oversized roster brick governance weight reads — WH-J must confirm the registry enforces the 28-pool cap.
- Harness — Slither 0.11.4 full-project run (`--filter-paths "lib/|test/|script/"`); no new Foundry PoC (S12 static surface, clean).
**WK.17 · S10 deployment / CREATE3 / setMinter · `script/DeployAuMM.s.sol` + `script/DeployStageK.s.sol` + `test/fork/DeployStageK.t.sol` — FINDING F-03 (Confirmed, Low); deploy/wiring otherwise clean.**
- WK.17 S10: FINDING F-03 (TVLOracle Miliarium-registry bind omitted from K7 `_wire`) | checked clean: env-input discipline — both scripts read every address via `vm.envAddress`/`envUint` with NO zero-fallback (DeployAuMM L33-34; DeployStageK L75/L109-136/L154-166), explicit-fail on missing env per the DeployStageJ rule; wiring order load-bearing + correct — `setMintRouter`×2 (steps 3/4) BEFORE the gauge+registry handoff (steps 6/7) per K-D7, `setAuthorizer` LAST (step 8) per OQ-10; every wiring-target setter access-gated (`setVotingWeight` `votingWeightSetter` self-seal VaultClassRegistry L208-213; `addAuthorizedDonator` `onlyDonateAuthorizer` L296; `setMintRouter`×2 `onlyGovernance`+one-shot Channel L186 / Distributor L224; `setMinter` `minterAdmin` one-shot+zero-checked AuMM L98-106 — NOT front-runnable, gated to `_minterAdmin`; `setGovernanceContract`×2 `onlyGovernance` Gauge/Miliarium L144; `setAuthorizer` current-authorizer-gated); AuMM deploy storage-only/H13-safe (no ctor external calls, DeployAuMM L16-18); two-distinct-Bodensee-channels + dual-interface-registry disambiguated per K14 (SWAP_AND_DEPOSIT≠BODENSEE_CHANNEL; MILIARIUM_REGISTRY cast to both IMiliariumRegistry + IMiliariumSlotRegistry); single-governor production precondition stated in NatSpec (cannot enforce, correctly disclosed) | FINDING F-03: `_wire` (L147-167) implements exactly the K14 8-call inventory; `tvlOracle.setMiliariumRegistry` is in neither inventory nor script (grep: ZERO calls in script/ + K fork tests), so the K6 Leg-2 Miliarium enumeration stays dormant (`registryBound==false`, TVLOracle L211/L226) and `registrySetter` (L54) stays unsealed — deviates from K-D8 "registry bound post-deploy at K7" → Low/Confirmed, disposition routed | could-not-assess (carries WK.7) → live `vault.pauseVault()` round-trip: the K7.3 C-assertion checks `canPerform(authorizer.EMERGENCY_ACTION_PAUSE_VAULT(), multisig, vault)` against the authorizer's OWN derived actionId (DeployStageK.t.sol L157-165) — circular, not a behavioral round-trip proving the disambiguator matches the live Vault's `Authentication.getActionId`; formula match was code-verified WK.7, no fork test exercises a real multisig pauseVault through the installed authorizer.
- Observation (test-fidelity, documented) — the K7.3 fork test runs as `governor = address(this)` with a throwaway `AureumAuthorizer` bridge for wire (8) (K15 Option 1), diverging from the production multisig-signed `setAuthorizer`; the divergence is explicitly NatSpec-noted (tier-matching DeployStageH.t.sol L208-218), production fidelity living in the K1 authorizer unit suite + script NatSpec. Acceptable.
- Observation (carries WK.7/WK.8 → WH-G/WH-J) — wires 6/7 point `GaugeRegistry`/`MiliariumRegistry` `governanceContract` at `AureumGovernance`; whether those contracts' `onlyGovernance`-gated actions (`revokeGauge`/`replaceSlot`/`registerGaugeFromComposition`) are the only callers of the slots `AureumGovernance.execute` drives is a Stage-G/J gate question (not K-diff) → WH-G/WH-J back-fill.
- Harness — read-lens on all three K7 files + the 6 wiring-target setter gates + grep of `setMiliariumRegistry` across `script/`+`test/fork/`; F-03 PoC (deploy-replay asserting `tvlOracle.miliariumRegistry()==address(0)` post-`deploy()`) lands with the fix.
**WK.18 · S7 TVL manipulation (spot-tvl → governance weight) · `src/governance/VotingWeight.sol` consuming `TVLOracle.tvl` — FINDING F-04 (Confirmed, Low); resolves the WK.15 SUSPECTED item.**
- WK.18 S7: FINDING F-04 (resolves WK.15 SUSPECTED spot-tvl) | checked: `_positionPower` L142 reads `ORACLE.tvl(pool)` SPOT (TVLOracle.tvl L272-283 = live `balancesLiveScaled18 × _constellationRatio`, no oracle-side EMA/TWAP); recorder shares `lp`/`totalLP` (L138/L140) hook-gated + non-flash-movable (OQ-25 cancels the LP-share leg); spot-tvl is the lone atomically-manipulable factor, captured in-tx with `poke`/`castVote`; F-9 root `base.powDown(0.25e18 Era0 / ONE/3 Era1+)` (L149-152) caps weight-gain at the 4th/3rd root of the pump; PoC 3/3 green proves structural exposure (pure spot pump moves weight, zero recorder change) + quartic ceiling (16×→~2×, 8×→~2×, 10000×→~10×) | could-not-assess (→WH-H + Stage-P): the full economic exploit (cheap thin-venue oracle pump beating the quartic damping in a sparse roster) — R1-blocked on the fork (pilots have no svZCHF path, tvl=0), routed to the Stage-P fork sim with a populated roster; oracle-side spot-vs-EMA fix is H-D9 Stage-H (not K-diff) → WH-H.
- Quantified (PoC) — weight-gain factor = (spot-tvl-pump factor)^exponent: Era0 ¼ root (16× tvl → 2× weight, 10000× → 10×), Era1+ ⅓ root (8× → 2×); a pure spot-tvl change with ZERO recorder-position change moves `governanceWeight` (the structural manipulability); economically ≥1:1 pump cost (DONATION 1:1 permanent, flash-SWAP ≥ round-trip slippage) × quartic damping = sublinear ROI.
- Bounds (→ Low) — no theft / no flash weight (14-day cliff) / recorder-share honesty / ~16-day vote+timelock public contest; quartic self-limiting makes a meaningful pump astronomically costly under deep liquidity. Rises toward marginal-Medium ONLY in a SPARSE-roster + deep-target regime (thin reference venue, few `_constellationRatio` averaging venues), where it COMPOUNDS F-01 (quorum-gaming) + F-02 (Sybil-split) — the prize being governance capture, not token profit.
- Amendment + supersession — this CONFIRMS the WK.15 SUSPECTED spot-tvl item (raised against the TVLOracle K6 re-wire) and SHARPENS the WK.12 flash-resistance attestation (which credited only the LP-share leg). The WK.15 forward-reference "if confirmed → F-03" is SUPERSEDED: F-03 was taken by the WK.17 deploy-omission, so the spot-tvl finding is F-04.
- Harness — `test/whitehat/F04_spotTvlManipulation.t.sol` 3/3 green: SpotTvlPump_MovesWeight_QuarticDampedEra0 (16_000e18 vs 1_000e18 → w16≈2×w1, <3×), SpotTvlPump_CubeRootDampedEra1Plus (8_000e18 → w8≈2×w1 past firstHalving), ExtremeSpotTvlPump_YieldsBoundedWeight_Era0 (10_000e18 → wHuge≈10×w1).
**WK.19 · pass close-out · coverage roll-up + summary — WK pass CLOSED.**
- WK.19: pass CLOSED | 18 audit steps across 7 K-diff logic contracts (`stage-j-complete..HEAD` `63471f9`), severity-ordered S1→S12; coverage roll-up row filled (S1/S2/S3/S6/S8/S12 ✓ clean; S4/S7 F-04; S9 F-01/F-02; S10 F-03; S5 n/a OQ-1 hook=Stage D; S11 n/a AureumVaultFactory=Stage B).
- Findings (4, all Confirmed + PoC'd, none Fixed — dispositions routed per the ledger commit-discipline): F-01 (S9, Medium) quorum denominator = propose-time snapshot vs vote-time numerator → turnout can exceed 100% of a stale base; code-fix = tally-time live denominator (WK.10/WK.13). F-02 (S9, Medium) F-9 per-position concave root is subadditive → N-way split multiplies summed weight ~N^¾ Era0 / N^⅔ Era1+; spec-level F-9 decision (WK.12). F-03 (S10, Low) K7 `_wire` omits `tvlOracle.setMiliariumRegistry` → K6 Miliarium leg dormant + `registrySetter` unsealed; code-fix = wire-(9) + deploy-replay regression (WK.17). F-04 (S4/S7, Low) `VotingWeight` consumes `TVLOracle.tvl` SPOT → atomically pumpable, F-9 quartic root self-limits (16×→~2×); → WH-H oracle EMA/TWAP (WK.18).
- Back-fill queue (raised in-pass, out of K-diff scope) — WH-D: the primary S2 swap-fee → Bodensee path (`AureumProtocolFeeController`/`AureumFeeRoutingHook`, Stage D) + the OQ-1 hook hot path (S5). WH-G/WH-J: the `GaugeRegistry`/`MiliariumRegistry`/`VaultClassRegistry` `onlyGovernance`-gate audits (do the gates the K handoff points at `AureumGovernance` admit only intended callers — WK.8/WK.11/WK.17 routing). WH-H: the INV-2 cross-contract emission-conservation harness (highest-value gap — no `ReferenceEmission`/`cumulativeAt` independent reference; channel + distributor both mint against the same 21M budget, the H-D2 split unaudited; WK.3/WK.5) + the oracle spot-vs-EMA F-04 fix + the F-9 concave-split CCB/emission-weight check.
- Tag-readiness — the white-hat pass is non-blocking by design (process §12); the 4 findings carry routed dispositions, none Fixed. Tagging `stage-k-complete` now (findings routed, fixes at WH-*/Stage-P) vs after landing the two code-fixes (F-01 tally-time denominator, F-03 deploy wire-(9)) is a build-line/user decision — F-03 is the sharpest tag-gate candidate (a deploy-completeness bug that silently inerts a shipped K6 feature).
- Auditor model: Fable 5 (`claude-fable-5`), actor/verifier-independent from the Opus/Sonnet build line per the white-hat cadence.

**Resolution · F-01 + F-02 + F-03 + F-04 — all four findings fixed (2026-06-12, post-close).**
- All four findings are resolved on `stage-k-whitehat` ahead of the `stage-k-complete` tag. The WK.1–WK.19 step blocks + the WK.19 roll-up record the as-of-close state (none Fixed); the findings table above carries the live `Fixed` status with fix hashes.
- **F-01** (`e6d9b3d`) — `AureumGovernance._voteSucceeded` reads the quorum denominator `VOTING_WEIGHT.totalSupply()` LIVE at tally (L294) instead of the propose-time `snapshotTotalSupply`, mirroring the WK.13 `vetoProposal` invariant-preserving form. Regression `test_F01_liveTallySupplyDefeatsStaleQuorumGaming` (accumulator grows 20× post-propose → minority vote Defeated); `F01_quorumSnapshotTiming.t.sol` 5/5.
- **F-03** (`1e84bfb`) — `DeployStageK._wire` gains the 9th call `tvlOracle.setMiliariumRegistry` (after the handoff, before `setAuthorizer`), binding the K6 Leg-2 enumeration and self-sealing `registrySetter` on deploy. Regression `test_F03_tvlOracleMiliariumRegistryBoundAndSealed`; `DeployStageK.t.sol` 6/6.
- **F-04** (`c283371` + `3d80ac8`) — `VotingWeight._positionPower` swaps spot `ORACLE.tvl` for the 60-day `EMASampler.tvlEMA` behind an `EMA_MATURITY_BLOCKS` (60-day) seed-age gate (`emaSeedBlock`), so an unseeded or immature EMA confers zero weight — closing the atomic spot-pump and the F-D15 cold-start residual; a mature EMA keeps the F-9 quartic bound. `emaSeedBlock` is an additive Stage-F fix-forward (I13-class, no re-tag). Regression: `F04_spotTvlManipulation.t.sol` flipped to prove the gate (4/4) + `EMASampler.t.sol` seed-block tests (27/27); full regression 778/778 unit + 94/94 fork.
- **F-02** (`a5a3a1b` + aumm-site `50e21a5`) — the spec-gated pool-aggregate-root fix landed both-sided: canonical F-9 rewritten first (aumm-site `11_formulas.md` + the 04/10/12/17 consistency sweep, `50e21a5`), then `VotingWeight._positionPower` → `tvlEMA^era-root × LP-share × linear timeFactor`. The holder leg is linear, so wallet-splitting is weight-invariant (split sums to consolidated-minus-rounding-dust); subadditivity migrates to the gauge-gated pool axis (≤ roster size, real diversified LP — incentive-aligned, spec-accepted). PoC flipped to fix-proving (`F02_concaveSplitSybil.t.sol` 3/3); unit recalibration 32/32; F04 ratios stable 4/4; regression 778/778 unit + 94/94 fork.
- None of the four fixes touches the Vault substrate seam — `Vault.sol`/`VaultAdmin.sol`/`VaultExtension.sol` byte-identity preserved (CLAUDE.md §1).

---

## WK-R pass — Stage K fix re-audit (second auditor) · attestation log
> Independent second-auditor (Opus) re-audit of the F-01..F-04 fix commits plus the remaining Stage-K surface, hunting regressions / new bugs the WK (Fable 5) pass missed. F-01..F-04 are treated as a FIXED baseline (regression, not re-find); new finding ids continue F-05+, steps use the WK-R.n labels. Append one block per step as it closes. The Coverage roll-up row above is filled at the WK-R.10 close-out per process §10/§12 — a "no finding" step records an attestation here, not an `F-NN` row.
- **Pass:** WK-R · **Scope:** the K-fix delta `git diff stage-k-complete..stage-k-whitehat-complete` (`63471f9..7f98cc2`) + the remaining Stage-K surface; Stage L (`stage-k-whitehat-complete..HEAD`) is OUT of scope (→ future WL) · **Opened:** 2026-06-13
- **Auditor model:** Opus 4.8 (`claude-opus-4-8`) — actor/verifier-independent from the WK Fable 5 pass and from the Opus/Sonnet build line
- **Status:** CLOSED 2026-06-14 — WK-R.1–WK-R.10 complete. 2 findings, both Suspected + Open + disposition-routed (none Fixed): F-05 (S7, Low — EMASampler seed-freshness gap, age-only maturity gate) + F-06 (S9, Medium — post-endBlock quorum-denominator inflation griefs a passed proposal, inverse cost of the F-01 live-read). F-01..F-04 fixes re-audited clean + unregressed. INV-5 holds (governance findings = blocking-power/capture, not theft). Coverage roll-up filled; live Slither delta ZERO high/critical. Disposition: F-05/F-06 PoC-on-fix → Stage-P / WH-H; full-tree Slither differential → Stage-P sweep.
**WK-R.1 · S3 rounding / invariant precision · `src/governance/VotingWeight._positionPower` rewritten arithmetic — NO FINDING.**
- WK-R.1 S3: NO FINDING | checked: uniform round-down (no mulUp/divUp/powUp — grep 0 hits), so weight is only ever understated; `ema^e · share · timeFactor` dimensionally sound; overflow fail-closed; div-by-zero guarded; degenerate-input guards preserve no-junk-into-`powDown`; the F-02 linearization cannot inflate | could-not-assess: LogExpMath internals (trusted upstream); cross-pass magnitude shift → WK-R.10.
**WK-R.2 · S3 rounding / invariant precision · `src/governance/AureumGovernance._voteSucceeded` F-01 live-tally cross-multiply — NO FINDING.**
- WK-R.2 S3: NO FINDING | checked: F-01 is a lone operand swap (`snapshotTotalSupply` → live `totalSupply()`) inside an unchanged exact cross-multiply; zero division; overflow unreachable / fail-closed (same uint256 accumulator read later); dimensional consistency improved | could-not-assess: live-read gameability → WK-R.8; read consistency → WK-R.4. Note: live `totalSupply()` is NOT globally ≥ snapshot — a `poke` can DECREASE weight via the §viii withdrawal-reset (`VotingWeight` L118-121), so the live denominator can fall below the snapshot, making quorum harder (a griefing-direction, not a pass-direction, risk).
**WK-R.3 · S4 ERC-4626 inflation / donation / rate manipulation · `src/governance/VotingWeight` (tvlEMA→weight) donation / first-depositor — NO FINDING (single-tx); 1 SUSPECTED → WK-R.5.**
- WK-R.3 S4: NO FINDING (single-tx) | checked: post-F-04 zero spot reads remain (grep 0); fresh-pool / first-depositor closed by the 60-day maturity gate (2 tests); a one-shot donation moves the EMA ≤2/61 per sample, then the `ema^0.25` cap applies (1 test); the recorder share is hook-gated (donation-immune); no share-mint surface | SUSPECTED → WK-R.5: age-only maturity (no `lastEMAUpdateBlock` freshness), so a stale inflated seed survives if never re-sampled | could-not-assess: concrete OQ-22 oracle internals → Stage-P/WH-H.
**WK-R.4 · S6 reentrancy & read-only reentrancy · `src/governance/AureumGovernance` (castVote / execute / _createProposal CEI + _voteSucceeded live read) — NO FINDING.**
- WK-R.4 S6: NO FINDING | checked: castVote CEI — `hasVoted=true` (L256) set BEFORE the external `VOTING_WEIGHT.poke` (L257), so a reentrant same-(proposal,voter) vote reverts `AlreadyVoted`, and `poke` makes only view calls with no `msg.sender` callback; execute CEI — `executed=true` (L332) set BEFORE `_executeProposal` (L333), re-entry reverts `ProposalAlreadyExecuted`, all routing sinks trusted (`revokeGauge` / `replaceSlot` / `registerGaugeFromComposition` / byte-identical `VAULT.setStaticSwapFeePercentage`); `_createProposal` interaction-before-effect is structurally safe — `proposalId` is allocated at `++proposalCount` (L200) AFTER the L198 `safeTransferFrom` + L199 `donate`, so a reentrant propose only allocates its own id and pays its own deposit; `payToken_` is whitelisted to {SVZCHF, SUSDS} via `_depositAmount` revert-otherwise (standard ERC-20s, no ERC-777 callback); `BODENSEE_CHANNEL.donate` is `onlyAuthorizedDonator` with its own transient `ReentrancyGuard` (`SwapAndDepositToBodensee` L369-371) whose `_vault.unlock` callback touches only the Vault + der-Bodensee; zero external readers of governance tally/state (`getProposal` / `forVotes` / `againstVotes` / `snapshotTotalSupply` / `hasVoted` / `proposalCount` grep 0 cross-contract, `.state(` never called cross-contract — registries gate on `msg.sender == governanceContract`, an address compare not a state read); `_voteSucceeded` live `totalSupply()` (L294) reads settled `VotingWeight` (`_totalQualifiedWeight` mutates only inside the view-only `poke`, never yielding control mid-update) and governance reads NO live Vault balances, so the Balancer unlock-window read-only-reentrancy vector is structurally absent | could-not-assess: `state()` live-denominator non-monotonicity (`Queued`→`Defeated`→`Queued` as the live denominator crosses the 20% quorum bar) is intended F-01 behavior (L287-291) — its economic block-via-denominator-pump exploitability → WK-R.8.
**WK-R.5 · S7 TVL-EMA manipulation · `src/ccb/EMASampler.sol` consumed by `src/governance/VotingWeight._positionPower` — FINDING F-05 (Low, Suspected); resolves the WK-R.3 SUSPECTED stale-seed item.**
- WK-R.5 S7: FINDING F-05 | checked: seed path raw-assigns `spotTVL` (`EMASampler` L138-141), bypassing the α=2/61 smoothing; the consumer gate is seed-AGE only (`VotingWeight` L136-138), not value freshness; `lastEMAUpdateBlock` is unread by both consumers (`VotingWeight` grep 0, `CCBMultiplier` body grep 0, `IEMASampler` L16 reserved-unimplemented); `TVLOracle.tvl` is donation-sensitive (L274-283 live balances); stale-seed CONFIRMED at the consumer layer (existing `test_F04_MatureEmaFlowsAtBoundary` pairs a mature seed-age with an arbitrary EMA value and flows, green) + the decay (59/61)^60 = 0.135 makes the refresh mitigation weak; sustained-skew converges (steady state EMA→spot, 86.5% over 60 daily samples); the OQ-5a-bis single-block acceptance (`EMASampler` L14-18) covers neither the seed path nor multi-sample skew; severity LOW gated by the enforced F-9 quartic (`VotingWeight` L158), the flash-immune recorded share (L149), the 60-day capital commitment, relative-only veto-blocking, and the emission counter-incentive (`CCBMultiplier` L268) | could-not-assess: executed live-`EMASampler`→`VotingWeight` regression + the sampling-race economic cost (→ PoC-on-fix / Stage-P / WH-H); the seed-AUTHORITY / first-`updateEMA` front-run-grief half (→ WK-R.6 / S8); concrete OQ-22 oracle internals (→ Stage-P / WH-H).
- Quantified — F-9 quartic ceiling (`VotingWeight` L158): 100× TVL inflation → 100^¼ = 3.16× weight (13.5× → 1.92× after 60 honest daily re-samples); 10,000× → 10× (1350× → 6.06× re-sampled). Amplification rides only the pool-power leg, scaled by the real recorded share + timeFactor, and is relative to OTHER-pool holders only (same-pool holders share the inflation). Veto = blocking power, not theft.
- Coverage (per-step) — WK-R.5 · S7 · `EMASampler` / `VotingWeight` consumer freshness → FINDING F-05 (Low, Suspected, Open); promotes the WK-R.3 SUSPECTED stale-seed item to a tracked row. The full WK-R pass coverage row lands at the WK-R.10 close-out.
- Harness — sketch only (Ask-mode, no forge run): seed P at an inflated spot → `vm.roll(T0 + 432_000)` with no further `updateEMA` → qualified attacker `poke` reads inflated^¼ weight (CONFIRMS); the honest-daily-re-sample control downgrades but does not kill (0.135 residual); sustained daily pumped sampling drives EMA→spot (CONFIRMS skew). PoC lands with the disposition.
**WK-R.6 · S8 access control & privilege · `src/ccb/EMASampler.updateEMA` seed authority / first-call front-run / grief-seed — NO NEW FINDING; resolves the F-05 seed-authority sub-item, SHARPENS F-05.**
- WK-R.6 S8: NO NEW FINDING (sharpens F-05) | checked: `updateEMA` is permissionless with only a `ZeroOracle` ctor guard (L114) and a once-per-`BLOCKS_PER_DAY` `TooEarly` cadence guard (L133) — no caller gate, no pool-registration / gauge-approval check; `emaSeedBlock` is written exactly once (`EMASampler` L141), by no other writer, never reset, sole reader `VotingWeight` L136; NO gauge/registry contract calls `updateEMA` or re-seeds on approval (grep 0 in `src/gauge`, `src/registry`), so the seed VALUE and the 60-day maturity CLOCK are lifecycle-decoupled from gauge approval; arbitrary-address seeding is inert (consumers gate on `isGaugeApproved` + Miliarium enumeration, so an unregistered seed is never read); per-day first-caller sample-timing on the SMOOTHED path is the OQ-5a-bis-accepted single-block surface (α=2/61-bounded, `EMASampler` L14-18); the grief-LOW-seed direction is the symmetric face of the same root cause | SHARPENS F-05: the seed raw-assigns `spotTVL` into `tvlEMA` STORAGE, which persists unrefreshed — so a 1-block flash-pump seed on a Vault-registered-but-not-yet-gauge-approved (dormant) pool consumes the entire 60-day EMA_MATURITY window with ZERO honest re-samples → the FULL 100% inflated seed (not the F-05 13.5% residual) flows the instant the pool is approved, at near-zero ongoing capital cost, with the attacker's qualifying LP (14-day cliff / 180-day on-ramp) running concurrently; this makes F-05's "permissionless refresh washes out the seed" mitigation largely illusory and raises F-05's likelihood (impact stays bounded — F-9 quartic cap → 100× TVL = 3.16× weight, relative-only veto-blocking, not theft) | could-not-assess: F-05 Low→Medium reconsideration is a disposition call (recommend the code-fix path — re-seed / bind EMA on gauge-approval, or the `lastEMAUpdateBlock` freshness gate — over accepted-risk); executed pre-seed → approval → vote regression → PoC-on-fix / Stage-P / WH-H.
**WK-R.7 · S9 economic / incentive · `src/governance/VotingWeight._positionPower` / `poke` — F-02 split-invariance re-proof + pool-axis Sybil — NO FINDING (F-02 wallet-split fix intact + unregressed; pool-axis subadditivity = F-02 spec-accepted residual).**
- WK-R.7 S9: NO FINDING | checked: wallet-split invariance RE-PROVEN — `_positionPower` L158 applies the F-9 root to the POOL aggregate (`ema.powDown(exponent)`) and the holder leg is LINEAR (`share = lp.divDown(totalLP)` L149, then `mulDown(share)`), so for wallets lp1+lp2=lp in one pool the summed power equals `powDown(ema)·((lp1+lp2)/totalLP)·tf` = consolidated (modulo round-down dust — the F-02 16-split / 81-split dust observations); the F-02 fix (`a5a3a1b`) is intact and UNREGRESSED by F-01 (never touched `_positionPower`) and by F-04 (the spot→`tvlEMA` swap preserved the same pool-aggregate-root / linear-share structure and ADDED per-pool 60d-EMA-maturity friction); the share denominator = total LP (`poolTotalLP`, all LPs) so unqualified/forfeited shares contribute 0 and per-pool summed weight ≤ poolPower·tf (no aggregation overflow); Era1+ further dampens (exponent 1/3 > 1/4, less concave, so N^(2/3) < N^(3/4)) | pool-axis subadditivity CONFIRMED present = exactly F-02's named spec-accepted residual: `poke` sums ONLY over `REGISTRY.miliariumPoolAt(i)` (L111-113) — the Miliarium roster, governance/registry-managed (`MiliariumRegistry`; an attacker CANNOT add pools), so N ≤ roster size and is not attacker-inflatable; each split pool independently requires gauge-approval (L135) + a 60d-matured EMA (L138) + a 14d-cliff-qualified real LP with meaningful share (dilution kills the benefit in established high-TVL roster pools; the N^(3/4) extreme needs dominance in N low-TVL pools) — "real diversified LP, incentive-aligned" per the F-02 disposition | could-not-assess: the F-05 / WK-R.6 inflated-seed can COMBINE multiplicatively with the pool-axis split (both factors individually bounded — quartic cap × roster cap — a combination of tracked findings, not new); executed multi-pool-split regression on a live-roster harness + the dominate-N-small-roster economic break-even (launch roster TVL distribution) → Stage-P / WH-F.
**WK-R.8 · S9 economic / incentive · `src/governance/AureumGovernance._voteSucceeded` / `state` / `queue` / `execute` (F-01 live-denominator inverse-griefing) — FINDING F-06 (Medium, Suspected); the inverse cost of the F-01 live-read fix.**
- WK-R.8 S9: FINDING F-06 | checked: F-01's live denominator `VOTING_WEIGHT.totalSupply()` at `_voteSucceeded` L294 vs the FROZEN numerator `forVotes+againstVotes` (castVote reverts `ProposalNotActive` past `endBlock` L254) — `state`→`_voteSucceeded` re-reads the denominator live on every `queue`/`execute` (L276) and `poke` permissionlessly grows `_totalQualifiedWeight` at any block (VotingWeight L109-124); a DORMANT (un-poked) matured-LP holder pokes in post-`endBlock` to push `totalVotes*10_000 < totalSupply()*2_000` → `_voteSucceeded` false → `state` short-circuits Defeated BEFORE the eta branches (L276 ahead of L277-279), so a `queue`d proposal (eta set L315) reverts to Defeated + `execute` reverts `ProposalNotSucceeded` (L330) + holding through `eta+EXECUTION_GRACE_BLOCKS` (L278) Expires it — a passed vote nullified; cost is the SAME real-matured-stake gate as F-02 (`_positionPower` gauge-approval L135 + 60d-EMA L138 + 14d-cliff L142 + recorder LP L143-146 — not flash/spot) | could-not-assess: executed live-`VotingWeight`→`AureumGovernance` griefing regression + the dominate-the-denominator economic break-even on a live roster → PoC-on-fix / Stage-P.
- Quantified + bounds — the strict `<` quorum (L294) kills a MARGINAL (~20% turnout) proposal on an infinitesimal denominator bump and a 25%-turnout one on ~25% accumulator inflation; addable weight is real-stake-bounded — a pool's aggregate governance weight is capped at `tvlEMA^e` (e=1/4 Era0 / 1/3 Era1+, VotingWeight L155-158) split among its LPs by share, so inflating `_totalQualifiedWeight` ~25% needs dominance of pools summing ~25% of the root-TVL-weighted base, held dormant past the 14d cliff. → Medium not High: no fund theft (liveness/outcome only); capital RECOVERABLE (LP kept, unlike the burned deposit L198-199) → opportunity + gas, repeatable; the ~16-day vote+timelock window is public. Rises toward marginal-High under thin participation + a whale incumbent defending a challenged pool (the F-9 root concentrates weight in few high-TVL pools — the natural adversary IS the natural holder).
- Lineage + disposition — NOT a regression: pre-F-01 the propose-time `snapshotTotalSupply` (L201/L204) was immutable so this vector did not exist; F-01's live read (correct for the >100%-turnout bug WK-R.2 re-proved) OPENS this inverse-griefing vector — a new finding causally downstream of the F-01 fix, not an F-01 re-open. Confirms the WK-R.4 forward-ref (`state()` live-denominator block-via-pump exploitability → WK-R.8). Routed (build-line, F-01 pattern): CODE FIX = snapshot the quorum DENOMINATOR at `endBlock` (pull-based lazy capture on the first post-`endBlock` `state()`, or an explicit `finalizeTally`) so numerator AND denominator freeze together at the tally boundary — reverting to propose-time `snapshotTotalSupply` reintroduces the F-01 bug, so the endBlock-time capture is the correct alignment; ALTERNATIVELY documented Accepted-risk given the real-stake gate + root-capped cost + public contest.
- Coverage (per-step) — WK-R.8 · S9 · `AureumGovernance` F-01 live-denominator inverse-griefing → FINDING F-06 (Medium, Suspected, Open). The full WK-R pass coverage row lands at the WK-R.10 close-out.
- Harness — sketch only (Ask-mode, no forge run): a passing proposal (forVotes clears 20% of the pre-attack accumulator) → `vm.roll(endBlock+1)` → attacker `poke`s a dormant matured position to grow `_totalQualifiedWeight` past 5× turnout → assert `state()==Defeated` and `execute()` reverts `ProposalNotSucceeded`; control = without the poke the same proposal `queue`s + `execute`s. PoC lands with the disposition.
**WK-R.9 · S10 deployment / wiring · `script/DeployStageK._wire` + `src/emission/TVLOracle.setMiliariumRegistry` + `test/fork/DeployStageK.t.sol` (F-03 fix re-audit) — NO FINDING (fix correct + regression-free; converts silent dormancy to a fail-loud wire).**
- WK-R.9 S10: NO FINDING | checked: F-03 fix (`1e84bfb`) is purely additive — the 9th `_wire` call `TVLOracle(env TVL_ORACLE).setMiliariumRegistry(IMiliariumRegistry(env MILIARIUM_REGISTRY))` (`DeployStageK` L167-168), no `src` change; ORDER-INDEPENDENT + correctly placed — wire (8) gates only on `msg.sender == registrySetter` (`TVLOracle` L194) and touches only TVLOracle-internal state, so it neither depends on nor mutates anything wires (1)-(7)/(9) consume, and (8)-before-(9) is safe (`setAuthorizer` migrates Vault authority, orthogonal to `registrySetter`); RIGHT REGISTRY — wire (7) + wire (8) read the SAME `MILIARIUM_REGISTRY` env key (dual-interface J-D2), the same address `_deploy` binds into `VotingWeight.REGISTRY` (L116) + `AureumGovernance.SLOT_REGISTRY` (L122), so the oracle Leg-2 roster (`_constellationRatio` L226-234) cannot diverge from the governance reader; BIND+SEAL intact (unchanged from K6/WK.15 — authority-before-zero L194 then L195, bind-then-self-zero L196 then L197, one-shot); NO REGRESSION — wire (8) is independent state, cannot break another wire; VALID end-to-end test — `test_F03` (`DeployStageK.t.sol` L198-201) is `view` over a `setUp` that runs the full `deploy()` then `_wire()` (L138) and asserts `miliariumRegistry()==realRegistry` (bound to the CORRECT env registry, not merely non-zero) + `registrySetter()==address(0)` (sealed); the inherited oracle enters setUp unbound+unsealed (else wire (8) reverts `OnlyRegistrySetter` + fails setUp — suite 6/6 green), so both assertions pass only if wire (8) executed | could-not-assess: cross-stage oracle IDENTITY — that env `TVL_ORACLE` is the same oracle `EMA_SAMPLER` samples (post-F-04 `VotingWeight` reads `EMASampler.tvlEMA` then `TVLOracle.tvl`; an orphan-but-governor-owned oracle would densify a roster nothing reads) — same silent-misconfig class as the WK.17 `VAULT`/registry-identity could-not-assess (the script reads an env address it can verify non-zero but not by-identity); the Stage-H→K unsealed-`registrySetter` window (a compromised deployer key binds a malicious registry pre-K7 — a pre-existing one-shot residual the fix SHORTENS by sealing at K7).
- Observations — (1) the fix is a STRICT improvement: the original F-03 was a SILENT omission (dormant K6 leg, no error); now a mis-set / unauthorized `TVL_ORACLE` (non-contract, or `registrySetter != governor`) makes wire (8) REVERT and fails the ENTIRE deploy (silent-misconfig → fail-loud, the right direction). (2) Cosmetic NatSpec drift in the fix's own doc — the env-var block (`DeployStageK` L60) annotates `TVL_ORACLE` as "(wire (9) Miliarium-registry bind)" but the actual ordering is wire (8) per the contract-level chain doc (L34-38) + the body comment (L167); (9) is `setAuthorizer`. No functional impact — a stale parenthetical label; recommend "(wire (9)" → "(wire (8)" at L60 in a housekeeping pass. (3) D36 — the test adds `TVL_ORACLE` to the shared `vm.setEnv` surface, covered by the documented `--threads 1` belt (`DeployStageK.t.sol` L45-48); the permanent D36 env-key-namespacing fix stays deferred.
**WK-R.10 · pass close-out · INV-5 round-trip + live Slither delta + coverage roll-up — WK-R pass CLOSED.**
- WK-R.10: pass CLOSED | 9 re-audit steps (R.1–R.9) over the F-01..F-04 fix delta (`stage-k-complete..stage-k-whitehat-complete` `63471f9..7f98cc2`) + residual Stage-K surface, severity-ordered S3→S10; coverage roll-up row filled (S3/S4/S6/S8/S10 ✓ clean; S7 F-05; S9 F-06; S1/S2 — not re-scanned [WK-owned, fixes untouched]; S5 n/a OQ-1 hook = Stage D; S11 n/a Factory = Stage B; S12 ✓ live Slither).
- INV-5 round-trip (every actor net gain ≤ 0) — HOLDS across the WK-R surface: F-01/F-02/F-06 are governance CAPTURE / griefing over the gauge / composition / fee levers, not value extraction ("veto = blocking power, not theft" throughout the ledger); F-06's griefer pays a burned proposal deposit (`AureumGovernance` L198-199) per attempt → direct round-trip net NEGATIVE; F-05's inflated-seed edge is relative veto-weight, not a withdrawable token gain. No F-01..F-04 fix opened a value-extraction path — each REDUCES extractability (F-01 tightened quorum to a live denominator, F-02 linearized the holder leg split-invariant, F-03 sealed the registry setter, F-04 gated on a 60-day EMA).
- Slither delta vs stage-k-complete — CLEAN (live run): Slither 0.11.4, 135 contracts / 57 detectors / 31 medium results, ZERO high/critical (no arbitrary-send / controlled-delegatecall / suicidal / reentrancy-eth / weak-prng / tx-origin) — matches the WK.16 baseline posture (134→135 = the Stage-L IncendiaryRegistry). The 3 results on the fix-touched contracts are all PRE-EXISTING benign classes, none new from the fixes: `AureumGovernance.castVote` reentrancy-no-eth (CEI cleared WK.11/WK-R.4 — hasVoted set before the view-only poke), `VotingWeight.poke` incorrect-equality (newWeight==oldWeight no-op, WK.14), `EMASampler.updateEMA` incorrect-equality (last==0 first-seed sentinel — orthogonal to F-05's value-freshness gap); the F-02 `_positionPower` rewrite introduced NO divide-before-multiply (divDown/mulDown are library calls). The other 28 mediums are out-of-WK-R-scope Stage F/G/H/L classes (CCBMultiplier divide-before-multiply, EmissionDistributor / GaugeRegistry reentrancy-no-eth, branch-init uninitialized-local false-positives, Vault-interaction unused-return) → Stage-P full-tree sweep.
- Findings (2, both Suspected + Open + disposition-routed) — F-05 (S7, Low — `EMASampler` seed path raw-assigns `spotTVL`, the consumer gates seed-AGE not value-freshness; quartic-bounded, sharpened by WK-R.6's pre-approval dormant-seed window) + F-06 (S9, Medium — post-`endBlock` quorum-denominator inflation via dormant-LP `poke` griefs a passed proposal; the inverse cost of the F-01 live-read). Both code-fix-routed (F-05: re-seed/bind EMA on gauge-approval or a `lastEMAUpdateBlock` freshness gate; F-06: snapshot the quorum denominator at `endBlock`); PoC-on-fix → Stage-P / WH-H. The WK-R pass is non-blocking by design (process §12) — `stage-l` carries the two open dispositions forward, no re-tag.
- Auditor model: Opus 4.8 (`claude-opus-4-8`), actor/verifier-independent from the WK Fable 5 pass and the Opus/Sonnet build line.

---

## Fix design — F-06 (Stage-L) · Governor-checkpoint snapshot voting

> Locks the chosen fix for F-06 (row above, Medium, Suspected, Open) BEFORE implementation, per the §12 ambiguity-gate. SUPERSEDES the WK-R.8 disposition sketch ("snapshot the quorum DENOMINATOR at `endBlock`"): that lazy-capture froze only the denominator and left a one-block `endBlock` capture surface; the chosen fix freezes BOTH numerator and denominator at a single pre-vote `snapshotBlock`, closing the F-06 inflation grief AND the WK-R.8 symmetric deflate-to-pass face, and converting F-01's live-read into a structural numerator ≤ denominator invariant. Recorded here, not as a K-D entry — a new K-D would mislabel a Stage-L whitehat fix as pre-tag build-line design. Status stays Open until the code + PoC land; the resolution commit then flips the F-06 row to Fixed with the hash + test path per the two-commit discipline below. Fix-forward on `stage-l`, no Stage-K re-tag (I13-class add-on, F-01..F-04 precedent). OZ `Checkpoints.Trace208` / `upperLookup` / `SafeCast` signatures are confirmed at the first `VotingWeight` `forge build` (G10/G14 compile-probe discipline).

**Design class — Governor-style historical checkpointing.** `VotingWeight` gains per-holder + total weight HISTORY; `AureumGovernance` reads voter weight and the quorum denominator at a frozen `snapshotBlock = startBlock + VOTING_DELAY_BLOCKS`, so post-snapshot `poke`s move neither side.

1. **`VotingWeight` checkpoint history.** Add two OZ `Checkpoints.Trace208` traces (`lib/openzeppelin-contracts/contracts/utils/structs/Checkpoints.sol`, pinned 5.6.1) — one per-holder, one for the running total. `poke` pushes both on every non-no-op delta: key `block.number` → `SafeCast.toUint48`, value the new weight → `SafeCast.toUint208` (reverts on overflow, never truncates). Add `getPastVotes(address holder, uint256 blockNumber)` + `getPastTotalSupply(uint256 blockNumber)` via `upperLookup`, each reverting `FutureLookup(blockNumber)` when `blockNumber >= block.number`. The live `governanceWeight` / `totalSupply` / `poke` surface is UNCHANGED — the veto path keeps live reads (see scope).
   - Bound: `_positionPower` weight is `tvlEMA^(1/4..1/3) · share · timeFactor` ≈ ≤1e21/holder, total ≪ `uint208` max (≈4.1e62) — `SafeCast` is the fail-closed belt, not an expected path.
   - Invariant: the total trace is pushed with the running sum on every `poke`, so its value at any block = Σ holders' checkpointed weight at that block; therefore Σ over voters of `getPastVotes(h, snap)` ≤ `getPastTotalSupply(snap)` — numerator ≤ denominator BY CONSTRUCTION. This is the F-06 closure and the durable F-01 fix.

2. **`AureumGovernance` snapshot + voting delay.** `VOTING_DELAY_BLOCKS = AureumTime.BLOCKS_PER_DAY` (7_200, ~1 day; parity with `EXECUTION_TIMELOCK_BLOCKS = 2 · BLOCKS_PER_DAY`). Struct `Proposal`: REPLACE `snapshotTotalSupply` (uint256) → `snapshotBlock` (uint256), 1-for-1, no slot-layout growth. `_createProposal` sets `snapshotBlock = start + VOTING_DELAY_BLOCKS` and `end = snapshotBlock + VOTING_PERIOD_BLOCKS`, and DROPS the live `VOTING_WEIGHT.totalSupply()` read. `ProposalCreated` emits `snapshotBlock` in place of `snapshotTotalSupply`. Enum `ProposalState`: APPEND `Pending` (new value 6 — preserves Active=0..Expired=5 for existing raw-uint consumers).

3. **`state()` ordering.** `Executed` → `Pending` (`block.number < snapshotBlock`) → `Active` (`snapshotBlock <= block.number <= endBlock`) → post-`endBlock` tally (`_voteSucceeded` → `Defeated` / `Succeeded` / `Queued` / `Expired`). Adds the lower bound at `snapshotBlock` that today's check (`block.number <= endBlock` only) lacks. Non-existent proposals (all-zero fields) keep deriving to `Defeated` (the `block.number < snapshotBlock` test is false at `snapshotBlock == 0`, so they fall through to the tally path).

4. **`castVote` / `_voteSucceeded` snapshot reads.** `castVote`: gate on Active (`block.number < snapshotBlock || block.number > endBlock` → `ProposalNotActive`); REMOVE the `VOTING_WEIGHT.poke(msg.sender)` call; read `weight = VOTING_WEIGHT.getPastVotes(msg.sender, p.snapshotBlock)` (`snapshotBlock < block.number` holds inside Active, so never `FutureLookup`). `hasVoted` CEI guard unchanged. `_voteSucceeded`: denominator reads `VOTING_WEIGHT.getPastTotalSupply(p.snapshotBlock)` in place of live `totalSupply()`.

5. **`IVotingWeight`.** Add `getPastVotes` + `getPastTotalSupply`; keep the existing three members. The `poke` NatSpec ("called on `msg.sender` immediately before reading `governanceWeight`") is now STALE — `castVote` no longer pokes; rewrite that NatSpec to the snapshot semantics in the interface sub-step.

**Load-bearing semantics (bake into Must-match at materialization):**
- **Pre-snapshot `poke` is load-bearing.** A voter's counted weight is the last checkpoint AT OR BEFORE `snapshotBlock`; a `poke` during the Active window updates live state + future checkpoints but NOT this proposal's vote. The ~1-day delay is the establish-weight window; permissionless third-party `poke` (K-D5) still works. This AMENDS K-D6b's "poke-at-vote" semantics, justified by the whitehat fix.
- **+1-day lifecycle.** Propose→close grows from ~14 days (`VOTING_PERIOD_BLOCKS`) to ~15 (delay + period) — intentional Governor-delay extension of K-D6f, not drift.
- **G16 blast radius — exactly three mocks** (`grep -rn "is IVotingWeight"`): `test/unit/AureumGovernance.t.sol`, `test/unit/VaultClassRegistry.t.sol`, `test/fork/mocks/StageGMocks.sol` — each gains stub `getPastVotes` / `getPastTotalSupply`. Re-grep at the interface sub-step per G16.
- **`ProposalCreated` field change** — `snapshotTotalSupply` → `snapshotBlock`; the AureumGovernance test event assert + any off-chain indexer update with it.

**Scope boundary (confirmed).** F-06 stays scoped to `AureumGovernance`. `VaultClassRegistry.vetoProposal` (G-D9, `stage-g-complete`) keeps its LIVE `governanceWeight` / `totalSupply` reads — a different consumer (cumulative accrual over `VETO_WINDOW_BLOCKS`, no `poke`, no post-`endBlock` frozen-numerator geometry), so the F-06 attack does not exist there. The veto path's live-denominator surface is logged as a SEPARATE carry-forward observation (→ a future `F-NN` if tracked), not folded into F-06's blast radius.

**Materialization order (one §8e.1 each, F-06 before F-05):** (i) `VotingWeight` checkpoint history + `getPast*`; (ii) `IVotingWeight` two members + `poke` NatSpec rewrite; (iii) `AureumGovernance` struct / enum / `_createProposal` / `state` / `castVote` / `_voteSucceeded`; (iv) the three `is IVotingWeight` mocks; (v) F-06 PoC `test/whitehat/F06_*.t.sol` (post-snapshot `poke` no longer moves the tally) + regression; (vi) resolution commit flips the F-06 row → Fixed.

**RESOLVED (2026-06-14, Stage-L) — the F-06 findings-table row above is flipped Open→Fixed.** Landed: (i) `VotingWeight` `Checkpoints.Trace208` weight history + `getPastVotes`/`getPastTotalSupply` (`9b57d9c`); (ii) `IVotingWeight` two members + `VotingWeight` override + three `MockVotingWeight` stubs (`8fdd476`); (iii)+(iv) `AureumGovernance` snapshot voting (`ProposalState.Pending` + `VOTING_DELAY_BLOCKS`; numerator `getPastVotes(voter, snapshotBlock)` and denominator `getPastTotalSupply(snapshotBlock)` both frozen at `snapshotBlock`; `poke` dropped from `castVote`) plus the `AureumGovernance.t.sol` snapshot-window re-alignment and the `F01_quorumSnapshotTiming.t.sol` re-point (`97131f8`); (v) the block-aware PoC `test/whitehat/F06_postEndBlockDenominatorFreeze.t.sol` (`427187a`). Execution diverged from the planned split: the (iv) mocks folded into (ii), and (iii)+(iv) committed as one unit. SUPERSESSION — snapshot voting removes F-01's `e6d9b3d` live-`totalSupply()` denominator, so the historical F-01 records (the findings-table F-01 row and the WK.10/WK.13 step blocks) describe the now-superseded pre-F-06 mechanism; they are preserved as the dated record, reconciled here. The F01 suite was re-pointed from bug/live-read demos to snapshot regressions and renamed: `turnoutCanExceedSnapshot` → `test_F01_turnoutBoundedBySnapshotSupply`; `minorityAttackerRevokesGauge` → `test_F01_dustAttackerCannotRevokeGauge`; `singleVoterClearsCompositionSupermajority` → `test_F01_singleVoterCannotClearComposition`; `liveTallySupplyDefeatsStaleQuorumGaming` → `test_F01_snapshotDenominatorDefeatsStaleQuorumGaming`; `vacuousQuorumWhenAccumulatorEmpty` → `test_F06_vacuousQuorumAtZeroSnapshotStillOpen` (the `snapshot==0` vacuous mul-form is NOT closed by F-06 — orthogonal to the denominator-timing axis, still Open per the WK.13 L140 observation, carried to Stage-P). Regression: F06 2/2 + F01 5/5 + `AureumGovernance` 43/43, 780 non-fork tests green.

---

## Fix — F-05 (Stage-L) · EMA freshness gate

**RESOLVED (2026-06-15, Stage-L) — the F-05 findings-table row above is flipped Open→Fixed.** Landed: (i) the `VotingWeight._positionPower` freshness gate + shared-mock retrofit (`d5cecb3`); (ii) the PoC `test/whitehat/F05_emaFreshnessGate.t.sol` (`70eb276`). The fix adds `EMA_STALENESS_BLOCKS = AureumTime.BLOCKS_PER_EPOCH` (100_800, 14 days / one epoch) and the gate `if (block.number - EMA_SAMPLER.lastEMAUpdateBlock(pool) > EMA_STALENESS_BLOCKS) return 0;` immediately after the seed-AGE maturity gate (`VotingWeight` L167, after the `seedBlock == 0` guard at L165). `EMA_MATURITY_BLOCKS` (60 days) now gates seed AGE; the new constant gates seed FRESHNESS — so both faces named in the finding are closed: the F-05 base 13.5%-residual decay path (a stale inflated seed only survives if it is ALSO refreshed within the epoch, but an honest refresh moves the EMA toward spot) AND the WK-R.6-sharpened 100%-inflated pre-approval dormant-seed window (a seed never re-sampled has `block.number - lastEMAUpdateBlock` growing without bound, so it is structurally stale the instant the 60-day clock matures → 0). `lastEMAUpdateBlock == 0` (never updated) is fail-closed stale. CHOSEN PATH — the consumer-side freshness gate over the seed-path smoothing/anchor alternative named in the disposition: it is the minimal fail-closed surface (one external read + one compare in `_positionPower`), leaves `EMASampler` byte-untouched, and is shared by every `poke` caller so both the snapshot-vote and the live-veto consumers inherit it. I13 blast radius — because the gate is reached by all `poke` callers, the shared `MockEMASampler` (`test/unit/VotingWeight.t.sol`) gained a `setLastUpdateBlock` setter, the `_configurePosition` unit helper stamps `block.number`, and the F02 / F04 PoC `setUp`s stamp the pool fresh; all retrofitted in the (i) commit, no test regressed. PoC `test/whitehat/F05_emaFreshnessGate.t.sol` (4/4) pins both faces and the strict-`>` boundary: a deeply-stale mature EMA confers 0, a fresh mature EMA confers weight, delta == EMA_STALENESS_BLOCKS still flows, delta == +1 zeroes. Regression: F05 4/4 + F04 4/4 + F02 3/3 + `VotingWeight` 32/32, 784 non-fork tests green. Fix-forward on `stage-l` (I13-class, no Stage-K re-tag). With F-05 and F-06 both Fixed, the WK-R pass carries ZERO open findings forward — the WK-R.10 close-out attestations (rows above: 'both Suspected + Open + disposition-routed') are preserved as the dated record of the pass and reconciled here.

---

## Fix — F-07 (Stage-L) · IncendiaryRegistry EMA freshness gate

**RESOLVED (2026-06-17, Stage-L) — recorded directly as Fixed.** Unlike F-05/F-06 (recorded Open in the WK-R pass, then later Fixed), F-07 was surfaced and fixed within the same scheduled Stage-L spot review, so the findings-table row above is recorded directly as Fixed; the source fix + test retrofit (`0a21b99`) and the dedicated PoC (`b96227c`) predate this ledger entry. The fix adds `EMA_STALENESS_BLOCKS = AureumTime.BLOCKS_PER_EPOCH` (100_800, 14 days / one epoch, `IncendiaryRegistry` L47) and the gate `if (block.number - rail.lastSampleBlock > EMA_STALENESS_BLOCKS) revert EMAStale(payToken, staleness, EMA_STALENESS_BLOCKS);` (L337-338) immediately after the seed-AGE maturity gate (`seedBlock == 0` → EMANotMature L334; `age < EMA_MATURITY_BLOCKS` → EMANotMature L336). `EMA_MATURITY_BLOCKS` (432_000, 60 days, L44) gates seed AGE; the new constant gates seed FRESHNESS — so a mature-but-stale der-Bodensee price-EMA rail (seasoned ≥60d but never re-sampled, `block.number - lastSampleBlock` growing without bound) is structurally stale the instant the 60-day clock matures and reverts rather than pricing a `buyBoost`. `lastSampleBlock` advances only on permissionless `updateRailEMA`, so the gate is anyone-can-refresh and fail-closed. MIRRORS F-05 (the source NatSpec at L46 labels it the 'F-05 anti-stale-seed floor') — same constant name, same one-epoch threshold, same consumer-side placement — but DIFFERS in degradation: F-05's `VotingWeight` consumer returns 0 weight (a stale position silently does not count), whereas the pricing path REVERTS `EMAStale` (a stale EMA must not silently misprice a purchase; the buy fails closed). DISTINCT ID, not F-05-extended — a different EMA consumer (der-Bodensee PRICE EMA for the boost deposit→entitlement valuation, rate-scaled-LINEAR, vs F-05's TVL EMA for quartically-damped governance weight) in a different contract, so a monotonic F-07 per the ID rule; opens the WL lineage the WK-R scope reserved for Stage L. I13 blast radius — the gate is a fail-fast check inside `_maturePrice`, reached by EVERY caller (`buyBoost` via `_valueInAuMM`, plus the `extMaturePrice` / `extValueInAuMM` harness wrappers), so the F-07.2 retrofit added a fresh `updateRailEMA` re-sample before each pre-existing mature-rail test (the initial buyBoost-only survey missed the harness-wrapper paths, surfaced at unit regression, closed in `_seedAndMature` + `test_extMaturePrice_mature`) plus a new `test_buyBoost_revert_emaStale` negative. PoC `test/whitehat/F07_emaStalePricing.t.sol` (4/4) pins the triad: a deeply-stale mature EMA reverts `EMAStale`, a fresh mature EMA prices, staleness == `EMA_STALENESS_BLOCKS` still flows (strict `>`), staleness == +1 reverts. Regression: 825/825 unit + 99/99 fork green. Fix-forward on `main` (Stage-L add-on, I13-class, no re-tag).

---

## Commit discipline

Follows the project's two-commit pattern:
1. **Finding commit** — adds/updates the row(s) in this ledger.
2. **Resolution commit** — the fix in code, referenced by hash in the **Fix**
   column, plus the row's Status moved to `Fixed`.

The PoC test lands with the fix (a fix without a regression test does not close
a finding). At a stage boundary, the coverage roll-up row for that pass must be
complete before tagging `stage-X-complete`.
