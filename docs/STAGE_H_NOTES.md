# Stage H — Notes & Design Freeze

> **Status:** Implementation in progress. H-D design surface partially locked (H-D1—H-D6 + H-D8—H-D23 LOCKED; H-D7 OPEN — to lock at H10). H1—H3 + H3.8 close-of-family sweep complete; H4 EmissionDistributor scaffold ready to land per the H-D15—H-D23 design freeze. Companion to STAGE_H_PLAN.md.
>
> **Last update:** 2026-05-18 — H-D15—H-D23 EmissionDistributor design freeze lock (H4.0a) — two-tier accumulator topology (global `accRewardPerScoreUnit` + per-pool `poolAccDebt` lazy settle) + per-user maps shipped at H4 + permissionless `recordScore` with `CCBScore` library delegation + in-distributor F-3/F-7 share computation + absolute newScore external ABI with internal F12 signed-delta `totalScore` middleware + `IAuMM.mint` at claim time + lazy accrual tick with H4 LP-tranche stub + two-tier storage layout + accrual-time `recordEmissions` push at `_settlePool` boundary (allocation-side F-10 semantics).
>
> **Mode:** Opus extra-high entry per §13 stage-level defaults (halving math + F12 type-discipline domain). Drops to Opus high after H4. Sonnet beats only for plan-row updates, commit drafts on closed sub-steps, and mechanical Completion Log entries.
>
> **Audience:** Sagix plus any future Claude session that needs the running log of decisions resolved during implementation and the incidents caught at audit.

---

## How this file is organized

- Design decisions during implementation. H-D1—H-D7 mirror STAGE_H_PLAN.md’s H-D Decisions table — H-D1—H-D5 fleshed out as LOCKED at H0.1; H-D6 and H-D7 OPEN as placeholders, to be filled in-place when locked at H1 and H10. Any new decision resolved during implementation (i.e. not pre-seeded in the plan table) gets the next free H-D* number (H-D8 onward) and is recorded as a new entry — not retro-edited into the plan file. Matches the C-D* / D-D* / E-D* / F-D* / G-D* convention.
- Findings (H10 onward). Implementation incidents, drift caught at audit, RPC quirks, env-key surprises, scope-expansion catches, contract-interface gotchas — anything worth a numbered log entry. Numbered from H10 to avoid collision with H-D* planning codes (matches the C10 / D10 / E10 / F10 / G10 pattern).
- Cross-reference convention (per CLAUDE.md §5): H-Dn = planning or implementation decision n; Hn (n ≥ 10) = implementation finding n; OQ-N = open question N from FINDINGS.md; F-n = formula n from `11_formulas.md`; §xxix = section in aummfi-bit/aumm-site numbered spec; G-Dn / F-Dn / E-Dn / D-Dn / C-Dn = prior-stage decisions; Gn / Fn / En / Dn / Cn = prior-stage findings (carry forward across stages).

---

## Design decisions

The H-D rows below replay STAGE_H_PLAN.md:L40-L51 while expanding rationale. Entries H-D1—H-D6, H-D8—H-D14, and H-D15—H-D23 reflect confirmed design locks (H-D1—H-D5 from plan scaffold H0.1; H-D6 locked at H1.3, 2026-05-14; H-D8 added at H1.5, 2026-05-15; H-D9 added at H1.7, 2026-05-15; H-D10 added at H1.9, 2026-05-15, amended H1.9-bis on 2026-05-16; H-D11—H-D14 added at H3.0a, 2026-05-17 — BodenseeBootstrapChannel design freeze per H-D2 audit-isolation precedent; H-D15—H-D23 added at H4.0a, 2026-05-18 — EmissionDistributor design freeze per H-D4 + H-D5 accumulator topology; per the in-stage decision convention recorded in `## How this file is organized` above). Entry H-D7 remains an OPEN placeholder until deployment-timing lock (H-D7 expected at H10 scripting).

### H-D1 — Oracles in-stage as H2a (concrete TVLOracle) + H2b (concrete EfficiencyOracle) — status LOCKED

Concrete oracle implementations arrive as sibling sub-step families inside Stage H: H2a delivers the production-ready `ITVLOracle` implementation; H2b delivers the deterministic on-chain successor to Mock efficiency feeds under `IEfficiencyOracle`. The team rejected a separate pre-mainnet micro-stage devoted solely to oracles because Stage G proved that a single long-lived branch with internal H2a/H2b rows keeps tagging, completion logs, and branch hygiene contiguous without sacrificing scope boundaries. Interfaces are already stabilized—`ITVLOracle` at Stage F milestone F0.2 per F-D11 and FINDINGS-linked G-D25c; `IEfficiencyOracle` at G-D23(ii) with the same FINDINGS scaffolding—plus Mock variants under `test/fork/mocks/`, so substantive remaining work sits in auditable Solidity rather than in interface churn. Embedding that work alongside the emission distributor aligns dependency ordering with GaugeRegistry callers that already anticipate LIVE feeds at Stage H handoff rather than dangling cross-repo promises. Operational risk concentrates in deployment sequencing reviewed once at H2 instead of juggling an extra stage-complete tag prematurely. Canonical TVL-shape discussion remains tracked via FINDINGS OQ-22, while sibling-oracle efficiency semantics tied to GaugeEligibility snapshots remain FINDINGS OQ-G1 complements to G-D23. Primary anchors: F-D11, G-D23, G-D25c, FINDINGS OQ-22, FINDINGS OQ-G1.

### H-D2 — Separate BodenseeBootstrapChannel contract (not inline in distributor) — status LOCKED

F-0 bootstrap emissions funnel through `src/emission/BodenseeBootstrapChannel.sol`, a narrowly scoped ledger that accrues per block toward AuMM earmarked for der Bodensee and exposes a deterministic deposit-and-distribute path marrying one-sided AuMM placements with der Bodensee liquidity while respecting the ancillary F-2 swap-and-buyback machinery. Folding those mechanics into the post-Month-10 emission distributor would interleave unrelated bootstrap accounting with accumulator math meant for gauged pools, increasing failure modes around reserve deltas and treasury routing. Separation mirrors Stage G carve-outs: SwapAndDepositToBodensee stayed outside GaugeRegistry per G-D11, and reserve-delta enforcement remained isolated inside the transient-storage primitive per G-D21. Dedicated contracts keep auditors focused on discrete trust boundaries touching der Bodensee’s bilateral reserves rather than a monolith that evolves every schedule tweak. Operational deploy scripts can instantiate the channel independently of downstream schedule logic, simplifying smoke tests tied solely to Bodensee accounting. Lifecycle clarity—bootstrap rail retires cleanly after Month 10 logic hands off weights to the distributor—reduces regressions tracked through formal reviews. Anchors tying back to rationales: F-0, F-2, G-D11, G-D21.

### H-D3 — F-7 share weights = step function sampled at BLOCKS_PER_EPOCH boundaries — status LOCKED

F-7’s CCB-weighted pool shares recomputed from live score inputs update only once per BLOCKS_PER_EPOCH boundary rather than recomputing on every Ethereum block cited by the textual “every block” reading in `11_formulas.md` F-7. Literal every-block fidelity would amplify gas by forcing synchronized reads across every gauged pool’s TVL lanes and multiplier state on arbitrarily dense swap traffic, collapsing feasibility on mainnet. Instead the refresh cadence aligns with Stage F precedent that pins multiplier evolution to epoch granularity via F-D24, keeping F-8 style multiplier math and F-7 share math co-tenants on BLOCKS_PER_EPOCH (OQ-3 anchors the 100_800 block cadence practitioners already rely on elsewhere). Harmonizing timelines reduces divergence bugs where auditors must reconcile contradictory cadences between boost updates and emitted weights feeding claim math. Divergence from verbatim spec wording stays documented herein until a FINDINGS row records the discrepancy if downstream surfaces ever assume block-perfect weight streams. FINDINGS elevation is warranted if UI dashboards, aggregator heuristics, or governance payloads treat block-frequency F-7 as authoritative; schedule that decision no later than H4 once integration tests illuminate consumer expectations. Anchors referenced during lock: F-7, F-8, F-D24, OQ-3.

### H-D4 — Pull / lazy accrual via MasterChef-style accRewardPerScoreUnit accumulator — status LOCKED

The distributor advances a solitary global `accRewardPerScoreUnit`, FixedPoint-encoded with 18-decimal normalization, ticking forward whenever an emission-bearing block segments accrue protocol mints into the staking ledger. Participant claims dereference snapshots of prior `accRewardPerScoreUnit`, multiplying deltas by persisted pool-specific score totals to reconcile AuMM payouts without iterating every deposit on payout. Matching spec language—“deposit triggers accrual; withdrawal stops it immediately”—means deposit and withdraw mutations refresh per-stake bookkeeping exactly like canonical MasterChef pull semantics instead of spraying push transfers each block across every vault. Attempting naive push accruals would collide with Gas realities once hundreds of gauges exist and would undermine the intra-epoch constancy ensured by H-D3’s stepwise weights. Accumulator bookkeeping must obey F12 and F13 signed-delta safeguards because CCB-derived scores shrink as aggressively as they grow epoch to epoch under tournament losses. Neglecting int/uint discipline while folding negative corrections risks underflow faults identical to Stage F counterexamples that forced explicit signed-add middleware. Operational monitoring therefore treats both boosts and slashes as first-class events feeding the accumulator rather than ephemeral hints. Supporting references: F-7, H-D3, F12, F13.

### H-D5 — Pool-scoped distributor; no IGaugeRegistry enumeration; incremental totalScore aggregate — status LOCKED

Each mutating distributor entry accepts an explicit pool key and consults `IGaugeRegistry.isGaugeApproved(pool)` before mutating aggregates, guaranteeing no hidden enumeration across the upstream registry ABI. GaugeRegistry consciously omits iterators per G-D5 and G-D7, so cloning parallel mirrors inside the emission module would bifurcate truth about which factories remain approved. Instead totalScore evolves via deltas applied alongside every sanctioned score-update path, amortizing bookkeeping to O(1) increments instead of Ω(N) sweeps before claims. Sweep-based reconciliation would resurrect the G16-style mock-registry sweep debt found when interface expansions raced ahead of enumerated consumers in Stage G forks. Liquidity bots and staking front-ends naturally know which pool they target, shifting discovery burdens where context already exists off-chain rather than forcing protocol-level listing APIs. Anchors guarding this rationale: G-D5, G-D7, G16.

### H-D6 — F-1 share denominator (fixed 1/28 vs dynamic 1/N_miliarium) — status LOCKED

H-D6 locks the F-1 equal-split denominator at the literal fixed fraction `1/28` recorded in `11_formulas.md` F-1 and echoed in `04_tokenomics.md` §ix—twenty-eight nominal slots stay constant for the entire Months 0—10 window no matter how many Miliarium pools are live on-chain. For any live count `N < 28`, each block leaves `(28 − N)/28` of `lp_share × block_emission` structurally unminted—never parked in a treasury slot, never deferred for catch-up minting—so the withheld tranche is permanently uncreatable by construction rather than stored for later release. The posture continues Stage C’s Bitcoin-philosophy MAX_SUPPLY = 21M AuMM hard-cap continuity: early windows that trail the notional schedule issue less than the formula’s ceiling allows, compressing effective lifetime supply under the asymptotic cap with no clawback or refill path. Months 11—12 inherit the same literal automatically because F-3’s blend uses `(1 - α(block)) × (1/28)` on the equal-split leg while α linearly ramps, so no separate denominator decision is needed for F-3 once this lock is fixed. Attempting dynamic `N` from `IMiliariumRegistry` enumeration would reintroduce the Ω(N) sweeps H-D5 explicitly forbids and would scramble historical allocations whenever pools arrive or depart between epochs on the BLOCKS_PER_EPOCH cadence referenced from OQ-3. FINDINGS requires no new OQ while implementation matches the canonical `1/28` wording; only a deliberate future departure from `11_formulas.md` F-1 would force a coordinated `aumm-site` §ix amendment plus a numbered open-question row. Anchors closing the record: F-1 as canonical share rule, F-3 as cascade beneficiary, H-D5 as the anti-enumeration guard, MAX_SUPPLY as the Stage C issuance ceiling, `aumm-site` §ix tokenomics as the prose twin, OQ-3 tying epoch discipline to the BLOCKS_PER_EPOCH cadence.

### H-D7 — AuMM.setMinter() handoff timing (Stage H deploy vs Stage K governance migration) — status OPEN

`IAuMM.setMinter()` remains an irreversible one-shot per Stage C precedent, leaving no recovery if the wired minter mishandles accumulator invariants downstream. Selecting whether emission begins during Stage H’s deployment window versus deferring mint authority activation until Stage K governance migration completes determines whether pre-governance timelines may emit AuMM earmarked toward bootstrap ramps or whether every mint awaits post-handoff governors. Earlier wiring favors faster bootstrap ramps scripted with auditor-reviewed bytecode even while governance ballots remain offline, whereas deferral keeps issuance paused until rehearsals finish cleanly at the governance layer. Locks therefore anchor to the H10 deployment sub-step alongside multisig-dry-run evidence. Cross-references: `IAuMM.setMinter`, Stage K governance handoff.

### H-D8 — Constellation enumeration source for OQ-22 cross-asset leg — status LOCKED

H-D8 narrows OQ-22 step 2's "all gauged Aureum pools" prose to a concrete typed-domain set: the constellation = `IMiliariumRegistry`-enumerated Miliarium pool roster (Stage J producer; F-D9 placeholder semantics pre-Stage-J) plus the immutable der Bodensee address plus an append-only governance-extensible roster owned by `TVLOracle` itself. The seed reflects OQ-8's two-part precedent — an enumerable pool set plus a governance-extensible token-of-interest set — translated from "BTC wrappers" to "constellation venues" without touching the non-enumerable `IGaugeRegistry` ABI. `IGaugeRegistry` extension is rejected because G-D5 / G-D7 lock its anti-enumeration posture; H-D5's anti-enumeration guard binds the distributor only, leaving oracle siblings free to mirror deliberate roster state — the TVLOracle's roster lives behind its own audit surface, isolated from accumulator math. Bootstrap horizon pre-Stage-J: `IMiliariumRegistry.isMiliarium(pool)` returns false uniformly per F-D9, so the constellation at H launch collapses to `{ BODENSEE_POOL } ∪ governanceAddedPools`, and tokens whose underlying ≠ svZCHF without a constellation venue resolve to `tvl_contribution = 0` non-revert per OQ-5a-bis EMASampler-compatible spot-read semantics. Governance gate on the additive setter follows the Stage G `GaugeRegistry` pattern — a mutable `governance` slot seeded to the Stage A–K Authorizer Safe at construction, switchable via `setGovernanceContract(address)` to the on-chain governance contract at Stage K migration. The roster is append-only at Stage H — revocation of constellation venues (e.g., a gauge-revoked pool that should drop from the average) is deferred to Stage K alongside broader governance-revocation discipline, so a wrongly-added pool is a known carry-forward concern rather than a Stage H surface. The semantic gap between OQ-22 prose ("all gauged Aureum pools") and operational reality ("Miliarium roster + Bodensee + governance-added pools") is recorded as a known carry-forward — the operationally-correct set is economically equivalent for F-1 / F-3 / F-7 ranking under the H launch composition, but FINDINGS prose alignment (likely a new OQ row or an OQ-22 addendum) is deferred to H11 cross-docs. Anchors closing the record: OQ-22 step 2 (cross-asset leg shape), OQ-8 (BTC_WRAPPERS governance-extensible precedent), G-D5 / G-D7 (`IGaugeRegistry` non-enumerable lock), H-D5 (distributor-only anti-enumeration), F-D9 (Miliarium registry placeholder semantics), Stage J (concrete `IMiliariumRegistry` landing), Stage K (governance handoff via `setGovernanceContract` per Stage G `GaugeRegistry` precedent).

### H-D9 — TVLOracle per-token underlying identification + cross-asset leg arithmetic — status LOCKED

H-D9 instantiates the OQ-22 valuation pipeline as concrete arithmetic for `TVLOracle.tvl(pool)`, locking the sub-decisions left open after OQ-22's pipeline-shape pin. Step 1 (per-token RP-aware unwrap) collapses to a single read of `IVaultExplorer.getPoolData(pool).balancesLiveScaled18[i]` — Balancer V3 natively folds `RP.getRate() × balance × decimalScalingFactor` into that field per `VaultTypes.PoolData` semantics, so the oracle inherits the upstream rate-provider accounting without re-deriving it. Per-token "underlying" identification — needed to match tokens to constellation venues — uses a governance-curated `tokenToUnderlying[address]` map owned by `TVLOracle` itself, seeded at construction with known mappings (svZCHF → ZCHF, sUSDS → USDS, waEthUSDC → USDC, st-EURA → EURA, etc.) and extended via the same `setGovernanceContract`-pivoting `governance` slot already locked in H-D8 (no second governance surface); STANDARD tokens default to a self-map (`tokenToUnderlying[token] = token`) seeded explicitly at construction time; ERC-4626 `asset()` introspection is rejected as a runtime fallback because non-ERC4626 rate providers (cbBTC, future bespoke wrappers) break the assumption — the governance map is OQ-8's `BTC_WRAPPERS` precedent applied one level up, prioritizing predictable audit surface over reflective convenience. Step 2 (underlying-to-svZCHF cross-asset leg) uses balance-ratio averaging at constellation venues — for a constellation pool holding both wrapper-of-U and wrapper-of-svZCHF, the implied U→svZCHF rate equals `balanceSvZCHFLeg18 / balanceULeg18` drawn from the same `PoolData` read, with no pool-type-specific curve math (StableMath Jacobian, weighted ratios) consumed; the contribution-side average is taken across a precomputed reverse-map `underlyingToPools[U]` rebuilt on each `addConstellationPool` call from the H-D8 roster plus the `tokenToUnderlying` state. Accuracy posture: the balance-ratio approximation is exact at AMM parity and degrades smoothly off-parity bounded by arbitrage equilibrium and the constellation-roster averaging, with the downstream daily-cadence EMA per OQ-5a-bis absorbing per-block sampling noise — this is a faithful-but-coarse instantiation of OQ-22's "constellation-spot averaging" prose, matching OQ-8's "averaging dilutes manipulation" framing without requiring per-pool Jacobian math, and is the accepted error budget for Stage H; curve-derived spot price (per-pool-type math) remains an optional post-Stage-H precision tightening reserved for the case where mainnet telemetry shows materially wrong F-7 rankings. Phase boundary: Stage H ships Phase 1 = direct venues only — tokens whose underlying lacks a direct constellation venue with svZCHF resolve to `tvl_contribution = 0` non-revert per H-D8 bootstrap semantics; OQ-22 step 2 prose at `docs/FINDINGS.md` L1115 explicitly allows the 2-hop fallback through ZCHF or sUSDS, but that path is deferred to a future H-x micro-stage or Stage K+ and is called out here as a NOTES carry-forward to prevent auditors from inferring 2-hop is live at Stage H ship. svZCHF dual-role (numéraire AND a WITH_RATE token whose `RP.getRate()` returns ZCHF-per-share rather than 1e18-identity) resolves cleanly under the same arithmetic — step 1 unwraps the svZCHF token contribution to ZCHF-equivalent, step 2 converts ZCHF → svZCHF via the same constellation balance-ratio against svZCHF venues, and the round-trip is mathematically self-consistent. Anchors closing the record: OQ-22 step 1 + step 2 (pipeline shape; FINDINGS L1110–L1116, with L1115 the explicit 2-hop carry-forward anchor), OQ-8 (`BTC_WRAPPERS` governance-extensible precedent; FINDINGS L661–L662, L703), H-D8 (constellation roster + governance gate), F-D9 (Miliarium registry placeholder semantics), OQ-5a-bis (daily EMA cadence absorbing per-block α-approximation noise), Balancer V3 `VaultTypes.PoolData` (`balancesLiveScaled18` semantics), `IVaultExplorer.getPoolData` (single read-only entry point for Step 1), Stage K+ (2-hop path future scope).

### H-D10 — EfficiencyOracle intra-epoch accumulation model + EpochEntry ring buffer — status LOCKED

EfficiencyOracle implements `IEfficiencyOracle.efficiencyInputs(pool)` via intra-epoch accumulation with epoch-stamped ring-buffer history — a concrete Phase 1 instantiation of OQ-G1's 3-epoch SMA formula `(swap_fee_revenue_i + yield_fee_revenue_i) / emissions_received_i`. svZCHF numéraire (H-D10 v2 amendment, H1.9-bis): G-D23(i) requires `numeratorSma` and `denominatorSma` to be expressed in the same unit — Phase 1 denominator-unit lock is svZCHF scaled18 for both legs, achieved by routing all push calls through `ITVLOracle.quoteSvZCHF(token, amountScaled18)`, the external wrapper on `TVLOracle._constellationRatio` committed at H2a.10a. Two authorized push entry points accumulate real-time per-pool data within the current epoch: `recordFees(pool, token, amountScaled18)` gated to a governance-settable `feeRecorder` slot (initially the Stage H governance Safe; wired to the fee routing hook or a fee aggregator in a future upgrade) — converts `amountScaled18` of `token` to svZCHF via `tvlOracle.quoteSvZCHF(token, amountScaled18)` then accumulates the svZCHF equivalent to `_accNumerator[pool]`; tokens unmapped in TVLOracle (`quoteSvZCHF` returns 0) are silently skipped without revert, consistent with TVLOracle's skip-on-zero semantics — and `recordEmissions(pool, aummAmountScaled18)` gated to a governance-settable `emissionsRecorder` slot (wired to `EmissionDistributor` at H4 deploy) — converts AuMM emissions to svZCHF via `tvlOracle.quoteSvZCHF(AuMM, aummAmountScaled18)` then accumulates the svZCHF equivalent to `_accDenominator[pool]`. Two constructor immutables support the quoteSvZCHF calls: `ITVLOracle tvlOracle` (the TVLOracle deployed at H2a, stored as an immutable) and `address AuMM` (the AuMM token address, stored as an immutable, used as the `token` argument to `quoteSvZCHF` in `recordEmissions`). Deploy governance prerequisite: `TVLOracle.tokenToUnderlying[AuMM]` must be seeded at deploy with a constellation venue that prices AuMM in svZCHF; if absent, all `recordEmissions` calls produce `denominatorSma = 0` post-warmup, triggering `EfficiencyDataUnavailable(pool)` reverts in GaugeEligibility — governance rescue is to seed the mapping before the first tournament epoch or call `setEmissionsRecorder(address(0))` to disable stale accumulation. Both entry points trigger `_ensureCurrentEpoch(pool)` before accumulating — if `AureumTime.epochIndex(GENESIS_BLOCK, block.number)` has advanced past `_accEpoch[pool]`, the function writes the accumulated pair `(_accNumerator[pool], _accDenominator[pool])` into the history ring at slot `_accEpoch[pool] % 3` with an `EpochEntry { epoch, numerator, denominator }` struct (epoch stamp enables stale-slot validation in the view function), then resets the accumulators to zero and advances `_accEpoch[pool]` to `currentEpoch`. The ring buffer per pool is `EpochEntry[3]` — size 3 matches `SMOOTHING_EPOCHS = 3` per OQ-4 / FINDINGS L501; epoch `e` writes to slot `e % 3`, overwriting data three epochs prior per ring semantics. Stale-slot detection in `_history[pool][slot]` relies solely on the stored `epoch` field — no zero-fill loop on epoch gaps — so `_ensureCurrentEpoch` is O(1) regardless of dormancy duration. `efficiencyInputs(pool)` is a pure `view` function: for each of the three target epochs (currentEpoch − 1, currentEpoch − 2, currentEpoch − 3), it first checks whether `_accEpoch[pool]` matches the target epoch (in which case the in-progress accumulators supply the value, handling the common case where the epoch boundary has not yet triggered `_ensureCurrentEpoch`); it then falls back to the ring slot with epoch-stamp validation; epochs with no record contribute 0 to both numerator and denominator sums; final returns are `sumNumerator / 3` and `sumDenominator / 3` — always dividing by 3, not by the count of valid epochs, so zeros correctly attenuate dormant-pool SMAs toward zero without inflating the per-epoch average. Early-epoch underflow guard: if `currentEpoch < 3`, `efficiencyInputs` returns `(0, 0)` immediately — GaugeEligibility's warmup check per G-D23(v) (`newEpoch − firstTournamentEpoch[pool] < SMOOTHING_EPOCHS`) skips pools before they accumulate 3 epochs of data regardless. Governance pattern mirrors TVLOracle per H-D8: mutable `governance` slot seeded to the Stage A–K Authorizer Safe at construction, pivotable via `setGovernanceContract(address)` at Stage K; `setFeeRecorder(address)` and `setEmissionsRecorder(address)` both `onlyGovernance`, both accepting zero address to disable the respective recorder slot (a safety valve that blocks stale-fee or stale-emissions accumulation after a feed is deprecated). `GENESIS_BLOCK` immutable: constructor parameter, required for `AureumTime.epochIndex` calls; same precedent as `AuMM.sol`. Known Phase 1 limitation: pools dormant for > 3 epochs present `denominatorSma = 0` to GaugeEligibility post-warmup, causing `EfficiencyDataUnavailable(pool)` reverts; governance rescue path is to call `recordEmissions` as the `emissionsRecorder` or reassign the `emissionsRecorder` slot; no automated resolution; deferred to post-Stage-H if mainnet telemetry reveals material impact. Anchors: OQ-G1 (efficiency metric + SMA shape), FINDINGS L501 (`SMOOTHING_EPOCHS = 3`), G-D23(i) (same-unit requirement for numeratorSma + denominatorSma), G-D23(v) (cold-start warmup, `firstTournamentEpoch` in GaugeEligibility), H-D8 (governance pattern mirror), H2a.10a (`TVLOracle.quoteSvZCHF` svZCHF conversion primitive), `AuMM.sol` `GENESIS_BLOCK` constructor pattern, `AureumTime.epochIndex`.

### H-D11 — BodenseeBootstrapChannel mint-at-distribute + permissionless accrue accumulator + AP piecewise integration — status LOCKED

`BodenseeBootstrapChannel` separates F-0 bootstrap mechanics into two semantically distinct surfaces — a permissionless `accrue()` tick that performs pure bookkeeping and a governance-gated `distribute()` that performs the actual `IAuMM.mint` plus der Bodensee one-sided DONATION. Mint timing locks to `distribute()`: `accrue()` advances `lastAccrualBlock` and bumps a `pendingAccrual` accumulator (denominated in AuMM scaled18 units) without ever invoking `IAuMM.mint`, so anonymous callers can permissionlessly drive the accumulator forward without delegating mint authority and without burning mint capacity on spam. Per-block contribution math integrates the piecewise-linear F-0 share — Bootstrap A spans `[genesisBlock, month6EndBlock]` with `bodensee_share(b) = 0.80 − 0.30 × (b − genesisBlock) / (month6EndBlock − genesisBlock)` (linear 80% → 50%); Bootstrap B spans `(month6EndBlock, month10EndBlock]` with `bodensee_share(b) = 0.50 − 0.50 × (b − month6EndBlock) / (month10EndBlock − month6EndBlock)` (linear 50% → 0%). The per-block emission rate `IAuMM.blockEmissionRate(b)` is snapshot once per `accrue()` call rather than re-read per block — Era 0 fully covers the F-0 bootstrap window since Month 10 end ≈ 2.19M blocks and the first halving lands at 10_512_000 blocks per `BLOCKS_PER_ERA` per OQ-3, so the per-call snapshot is exact within bootstrap while the snapshot pattern keeps the contract era-aware for future revisions. Sum across `[lastAccrualBlock + 1, to]` reduces to an arithmetic progression on each bootstrap leg: `Σ bodensee_share(b) × rate = ((first + last) / 2) × n × rate` where `first = bodensee_share(lastAccrualBlock + 1)`, `last = bodensee_share(to)`, `n = to − lastAccrualBlock`, executing in O(1) regardless of interval length. Cross-boundary `accrue()` calls — intervals spanning `month6EndBlock` — split into two AP sums, one ending at `month6EndBlock` under Bootstrap A coefficients and one starting at `month6EndBlock + 1` under Bootstrap B coefficients, preserving exact piecewise semantics without per-block iteration. `pendingAccrual` and `lastAccrualBlock` are the only mutating state surfaces in `accrue()`; `totalDistributed` mutates only in `distribute()`. Anchors closing the record: F-0 (piecewise bootstrap formula), `IAuMM.blockEmissionRate` (Stage C era halving — snapshotted, not reimplemented), `AureumTime.month6EndBlock` and `AureumTime.month10EndBlock` (Stage C boundary helpers), OQ-3 (`BLOCKS_PER_ERA = 10_512_000` — bootstrap window entirely within Era 0), STAGE_H_NOTES.md L126 (Produced interface boundary — permissionless `accrue` + governance `distribute`).

### H-D12 — BodenseeBootstrapChannel one-sided AuMM DONATION via Vault.unlock — mirrors G-D11 / G-D21 transient-storage + reserve-delta discipline; does NOT reuse SwapAndDepositToBodensee.donate — status LOCKED

`distribute()` executes the AuMM-pay-token one-sided injection into der Bodensee through Balancer V3's `IVault.unlock` callback pattern, mirroring `SwapAndDepositToBodensee`'s G-D11 / G-D21 audit-isolation discipline structurally but keying entirely on AuMM as the pay token — `SwapAndDepositToBodensee.donate(IERC20 payToken, uint256 amount)` rejects any `payToken` other than `_svZchf` or `_sUsds` per `src/gauge/SwapAndDepositToBodensee.sol` L329 + L362–L364 explicit `InvalidPayToken` revert, so the bootstrap channel cannot reuse that helper for AuMM. The distribute flow inside the channel: (1) compute `amount = pendingAccrual`; (2) revert if `amount == 0`; (3) zero `pendingAccrual` and add `amount` to `totalDistributed`; (4) `IAuMM.mint(address(this), amount)` (channel holds mint authority via H-D7-pending wiring); (5) snapshot pre-reserve via `_currentReserve(_aummIndex)` mirroring G-D12 reserve-delta discipline; (6) write transient slots `_EXECUTING = true`, `_PENDING_AMOUNT = amount`, `_ORIGINAL_CALLER = msg.sender`; (7) `_vault.unlock(abi.encodeCall(this._distributeCallback, (amount)))`; inside the callback (`OnlyVault` gate + `CallbackPayloadMismatch` revert on payload mismatch) — (8) `IERC20(address(AuMM)).safeTransfer(address(_vault), amount)` then `_vault.settle(IERC20(address(AuMM)), amount)`; (9) build `maxAmountsIn[_aummIndex] = amount` and call `_vault.addLiquidity({ pool: _bodensee, to: address(this), maxAmountsIn, minBptAmountOut: 0, kind: AddLiquidityKind.DONATION, userData: "" })`; (10) post-unlock clear all three transient slots and assert `IERC20(address(AuMM)).balanceOf(address(this)) == 0` via `HelperBalanceNonZero(residual)` revert. Under Balancer V3 `AddLiquidityKind.DONATION` semantics the BPT conferred to `address(this)` is structurally burnt by the pool (DONATION is the "give without receiving" path), so the channel never holds Bodensee BPT post-call — the post-residual check therefore covers AuMM balance only. `_aummIndex` is resolved at construction by introspecting `_vault.getPoolTokens(_bodensee)` for the AuMM token address and stored as an immutable `uint8` to keep callback gas tight; an `IndexLookupFailed` revert fires at construction if AuMM is not found in der Bodensee's token roster, since a misconfigured deploy is unrecoverable. The transient-storage slot layout uses three immutable slot constants (`_EXECUTING_SLOT`, `_PENDING_AMOUNT_SLOT`, `_ORIGINAL_CALLER_SLOT`) derived via `StorageSlotExtension.calculateSlot` per G-D11 — `_PENDING_PAY_TOKEN_SLOT` is unnecessary versus `SwapAndDepositToBodensee` since pay-token is always AuMM (an immutable rather than a runtime parameter). `_distributeCallback` is `external` for `abi.encodeCall` reachability with the same `OnlyVault` + `CallbackPayloadMismatch` revert pattern as G-D11. The audit boundary stays single-purpose: the channel routes AuMM and only AuMM; svZCHF / sUSDS routing through der Bodensee continues to flow through `SwapAndDepositToBodensee` (G-D12 swap fees, G-D21 governance bonds) — explicit non-coupling avoids auditor inference of shared validation. Anchors closing the record: F-0 (bootstrap destination = der Bodensee), G-D11 (audit-isolation precedent), G-D12 (nine-step SwapAndDeposit pattern as structural template), G-D21 (transient-storage + reserve-delta discipline), `IVault.unlock` + `AddLiquidityKind.DONATION` (Balancer V3 callback + add-liquidity kind), `src/gauge/SwapAndDepositToBodensee.sol` L329 + L362–L364 (explicit AuMM rejection — non-reusable for bootstrap), H-D7 (mint authority handoff timing — OPEN, resolves at H10).

### H-D13 — BodenseeBootstrapChannel lifecycle clamp: to = min(block.number, month10EndBlock) + empty-interval short-circuit + no selfdestruct — status LOCKED

`accrue()` applies a lifecycle clamp — `to = min(block.number, month10EndBlock)` — before computing the AP sum, so calls after the bootstrap window closes still advance `lastAccrualBlock` to `month10EndBlock` (capturing any residual emission) but no longer accumulate further. The empty-interval guard `if (from > to) return;` (where `from = lastAccrualBlock + 1`) short-circuits immediately when `lastAccrualBlock` has already been advanced to `month10EndBlock`, avoiding the AP formula on a degenerate zero-width interval — an inert but harmless no-op otherwise avoided for clarity. `BodenseeBootstrapChannel` has no `selfdestruct`, no `disable`, no `pause` — EIP-6049 / EIP-4758 make `selfdestruct` semantically deprecated on mainnet post-Cancun; the contract becomes a permanent, zero-activity fixture after the bootstrap window closes, with `pendingAccrual` and `totalDistributed` readable on-chain as historical anchors. Governance authority over `distribute()` remains live post-window so any residual `pendingAccrual` accumulated at the boundary tick can still be flushed — the governance slot pivots to the Stage K on-chain governance contract at H-D14's handoff path. Anchors closing the record: F-0 (bootstrap window definition — Month 0 through Month 10), `AureumTime.month10EndBlock` (Stage C boundary helper — the lifecycle clamp upper bound), EIP-6049 / EIP-4758 (selfdestruct semantic deprecation on mainnet post-Cancun), H-D14 (governance-only `distribute()` + Stage K slot handoff).

### H-D14 — BodenseeBootstrapChannel governance-only distribute() + immutable BODENSEE_POOL recipient — stronger than H-D8 / H-D10 mutable slots; F-2 not coupled in-channel — status LOCKED

`distribute()` is gated by an `onlyGovernance` modifier matching the H-D8 / H-D10 governance-slot pattern — the governance slot is mutable via `setGovernanceContract(address)` (rejecting `address(0)` per TVLOracle precedent) so the Stage A–K Authorizer Safe seeded at construction pivots to the Stage K on-chain governance contract at handoff. The recipient pool — der Bodensee — is bound at construction as an immutable `BODENSEE_POOL` rather than a mutable slot: there is no `setBodenseePool` setter, and the `_aummIndex` introspection at construction per H-D12 fixes the AuMM token position in the Bodensee roster permanently. This is structurally stronger than the H-D8 TVLOracle roster (mutable additions/removals via `addPool` / `removePool`) and the H-D10 EfficiencyOracle recorder slots (mutable to `address(0)` for deprecation) — F-0 names der Bodensee specifically as the bootstrap destination, so any future pivot would not be a configuration change but a complete redeployment with a new bootstrap-aware contract. The decision to make `distribute()` governance-gated rather than permissionless or block-cadence-driven follows from H-D11's mint-at-distribute lock: mint authority is the load-bearing scarce resource, and a permissionless distribute would let any caller force-flush `pendingAccrual` at an arbitrary block, donating to der Bodensee at the caller's chosen swap-price window — governance gating gives the protocol explicit timing control over the F-0 → F-1 boundary tick and over any residual flush post-Month-10. F-2 (Stage L Incendiary fee burn) is not coupled in this channel — der Bodensee swap fees and yield fees continue to route through `AureumProtocolFeeController` per Stage D, and the channel never reads or writes fee-router state; the audit boundary stays AuMM-mint-and-donate only. Anchors closing the record: H-D8 (TVLOracle mutable governance slot precedent), H-D10 (EfficiencyOracle mutable recorder slot precedent), H-D11 (mint-at-distribute justifying governance gating), H-D12 (immutable `_aummIndex` introspection at construction), F-0 (der Bodensee as bootstrap destination — pool identity locked at spec level), Stage K (governance handoff path), Stage D `AureumProtocolFeeController` (F-2 routing — not coupled here), STAGE_H_NOTES.md L126 (Produced interface boundary).

### H-D15 — Global `accRewardPerScoreUnit` + per-pool `poolAccDebt` lazy settle (two-tier accumulator topology) — status LOCKED

H-D15 formalizes the two-tier accumulator topology that H-D4 names but does not fully spell out — a solitary global `accRewardPerScoreUnit` (FixedPoint 18-decimal) plus per-pool `poolAccDebt[pool]` snapshots that allow O(1) per-pool settle without iterating the registered-pool universe. The settle pattern is canonical MasterChef / Synthetix: `_accrueGlobal()` advances `accRewardPerScoreUnit += (_lpTrancheEmission(block.number) * Δblocks).divDown(totalScore)` and updates `lastAccrualBlock = block.number`; `_settlePool(pool)` then computes `poolAllocation = (accRewardPerScoreUnit - poolAccDebt[pool]).mulDown(poolScore[pool])`, processes the H-D23 emissionsRecorder push, then writes `poolAccDebt[pool] = accRewardPerScoreUnit`. Pools that are not touched in an interval retain their stale `poolAccDebt` until the next settle — no cross-pool sweep, no Ω(N) update — resolving the apparent tension between H-D4's "solitary global" wording and the system-wide-settle concern that would otherwise force per-pool accumulators. A score change settles at the next mutating entry that touches that pool, while the global accumulator continues advancing for all other pools in parallel; the divergence is bounded by FixedPoint divDown rounding. Empty-`totalScore` guard inside `_accrueGlobal()` — `if (totalScore == 0) { lastAccrualBlock = block.number; return; }` — prevents division-by-zero and ensures no scaled emission accrues when no eligible pools exist (pre-Stage-J or post-revocation-storm conditions). F12 signed-delta arithmetic on `totalScore` is colocated at H-D19's score-recording site, not at `_settlePool`; settle is read-only on `totalScore` and `poolScore`, write-only on `poolAccDebt`. Anchors: H-D4 (locked global accumulator), H-D5 (incremental totalScore + isGaugeApproved gate), H-D11 (BodenseeBootstrapChannel accumulator + AP integration precedent), F-6 (Score normalization), F12 (signed-delta type-discipline domain).

### H-D16 — Per-user maps shipped at H4 + governance-settable `auMTContract` recorder slot — status LOCKED

H-D16 ships full per-user state inside `EmissionDistributor` at H4 close, not deferred to Stage I, so the H1 forward-dep interface boundary at STAGE_H_NOTES.md L141 holds: `IEmissionDistributor`'s `recordDeposit` / `recordWithdrawal` / `claim` ABI is consumable from the Stage I AuMT producer the moment that contract lands. Per-user storage is the single-snapshot MasterChef variant — `mapping(address => mapping(address => uint256)) public userLP` plus `mapping(address => mapping(address => uint256)) public userRewardDebt` keyed on `(pool, user)` — rather than the dual `userAccSnapshot + userPending` shape; the snapshot variant computes pending at claim as `(poolEffectiveAccPerLPunit - userRewardDebt[pool][user]) * userLP[pool][user]` where `poolEffectiveAccPerLPunit` is derived from the pool-level settled accumulator and `poolTotalLP[pool]` at settle time. The recorder slot follows the H-D10 EfficiencyOracle pattern: `address public auMTContract` initialized at construction to `address(0)`, settable via `setAuMTContract(address)` `onlyGovernance` (zero address acceptable as deprecation safety valve). Pre-Stage-I posture is `auMTContract = address(0)` → `recordDeposit` / `recordWithdrawal` revert `NotAuMTContract(msg.sender)`; H4 unit tests mock by setting `auMTContract` to `address(this)` (or `msg.sender` in test scope) for deposit/withdrawal path coverage. The governance handoff pivots on the same mutable `governance` slot as the H-D17 score producer entry and the H-D20 mint authority recipient; the recorder slot itself is independently mutable and zero-address-acceptable per H-D10 deprecation safety valve. Anchors: H-D4 (pull semantics — deposit triggers accrual; withdrawal stops it), STAGE_H_NOTES.md L141 (H1 forward-dep contract), H-D10 (EfficiencyOracle recorder pattern — `feeRecorder` / `emissionsRecorder` analogue), Stage I AuMT (downstream consumer of `recordDeposit` / `recordWithdrawal` / `claim` ABI).

### H-D17 — Permissionless `recordScore(pool)` + `CCBScore` library delegation + EMA read-only + revoked-gauge revert — status LOCKED

H-D17 confirms the score producer model at permissionless writes — H-D5's "incremental totalScore aggregate updated permissionlessly per `recordScore(pool)`" makes this a confirmation of locked-in design rather than a fork. The external function is `recordScore(address pool)` per H-D5 vocabulary; the `poke` name is colloquial-only, not in the ABI. The function (a) reverts immediately if `!_gaugeRegistry.isGaugeApproved(pool)` with `NotApproved(pool)` — prevents `totalScore` corruption from stale recordings on revoked gauges while satisfying H-D5's per-call isGaugeApproved gate; (b) runs `_accrueGlobal()` then `_settlePool(pool)` per the H-D21 lazy accrual tick before mutating score state, so the pool's claim-side allocation is captured against the pre-change score; (c) computes `uint256 newScore = CCBScore.score(_emaSampler.tvlEMA(pool), _ccbMultiplier.getMultiplier(pool))` via library delegation rather than inlined arithmetic, keeping the F-5 formula centralized and respecting OQ-23's `1e18` default for non-Miliarium pools via `getMultiplier`'s contract; (d) applies the H-D19 internal F12 signed-delta middleware to `totalScore`; (e) writes `poolScore[pool] = newScore`; (f) emits `ScoreUpdated(pool, oldScore, newScore)`. EMA refresh boundary is read-only per F-D22 separation: `recordScore` consumes `_emaSampler.tvlEMA(pool)` as a pure read, never silently calling `updateEMA(pool)` — bots wanting fresh EMA refresh it in a prior tx or in the same tx as a separate call. The permissionless posture mirrors BodenseeBootstrapChannel's `accrue()`: anonymous callers drive the accumulator forward without mint authority. Anchors: H-D5 (permissionless `recordScore` + isGaugeApproved gate), F-5 (Score = TVL_EMA × CCB_mult formula), F-D22 (EMA write-vs-read separation — CCBMultiplier reads, never writes EMA), OQ-23 (CCB_mult return-value regimes for non-Miliarium pools), CCBScore (F-D10 library — F-5 score helper).

### H-D18 — In-distributor F-3 / F-7 share computation; F-3 α-blend + F-7 Incendiary skim wired at H5 — status LOCKED

H-D18 locks the share computation locus inside `EmissionDistributor` itself, not in a separate `EmissionScheduleResolver` contract, consistent with H-D5's incremental `totalScore` anti-enumeration posture and the EmissionDistributor inventory row's H4/H5 split. At H4 close, the distributor exposes `poolScore[pool]` and `totalScore` as the underlying invariants; pool-share computation is implicit in the `_settlePool` math (`poolAllocation = Δacc × poolScore`). At H5, F-3 α-blend (Months 11–12) and F-7 Incendiary skim (Year 2+) wire into `_lpTrancheEmission(block.number)` — the per-block LP tranche reads `IAuMM.blockEmissionRate(block.number)`, subtracts the F-0 bodensee share for Months 0–10 (handing the bootstrap leg to `BodenseeBootstrapChannel` per H-D2), applies F-3's `(1 − α(block)) × (1/28) + α(block) × CCB_share` per H-D6's 1/28 literal for the equal-split leg, and subtracts F-7 Step 1 Incendiary skim via `IIncendiaryRegistry.activeBoostClaims` (H7 forward-dep stub). At H4, `_lpTrancheEmission(block.number)` is a stub returning `IAuMM.blockEmissionRate(block.number)` — full block emission, no phase subtractions — so the accumulator-side math is exerciseable in unit tests before H5 schedule wiring lands. `CCBShare.shares()` remains available for test harnesses or any future enumerated path but is NOT used in the runtime H5 distributor: F-1 / F-3 equal-split legs use the H-D6 1/28 literal without iterating a pool vector, and F-7 tournament leg uses `score[pool] / totalScore` via the global aggregate — both avoid the Ω(N) sweep H-D5 explicitly forbids. Anchors: H-D5 (anti-enumeration distributor design), H-D6 (1/28 literal F-1 / F-3 denominator), H-D2 (F-0 bodensee leg in BodenseeBootstrapChannel), F-3 (linear blend), F-7 (full tournament sequence), H7 (IIncendiaryRegistry forward-dep stub), CCBShare (Stage F F-6 library — available but unused at runtime).

### H-D19 — Absolute `newScore` external ABI + internal F12 signed-delta `totalScore` middleware — status LOCKED

H-D19 fixes the score-recording ABI surface at "caller sends absolute, distributor applies signed delta internally" — `recordScore(address pool)` takes no score parameter; the absolute new score is computed inside the distributor per H-D17 from `tvlEMA × CCB_mult`. The F12 signed-delta middleware applies the underflow-safe `totalScore` update at the distributor's single mutation site. Internal pattern: `uint256 oldScore = poolScore[pool]; uint256 newScore = CCBScore.score(...); int256 delta = int256(newScore) - int256(oldScore); totalScore = _applySignedDelta(totalScore, delta); poolScore[pool] = newScore;` — mirrors the production F12 pattern locked at Stage F's `CCBMultiplier` and the test-side F13 `_applySignedDelta(uint256 base, int256 delta) internal pure returns (uint256)` helper. SafeCast guards the `int256 ↔ uint256` transitions at the helper boundary. The single underflow-safe site keeps F12 discipline auditable: every `totalScore` mutation flows through `_applySignedDelta`, and no other write surface exists. Pre-write revoked-gauge revert per H-D17 prevents corruption: a revoked gauge cannot trigger a `totalScore` decrement that would drive the aggregate below the legitimate sum across approved gauges. Read side: `totalScore` is `public uint256` (auto-getter); the FixedPoint scale stays `1e18`-denominated to match `accRewardPerScoreUnit` arithmetic. Anchors: F12 (production type-discipline pattern in CCBMultiplier), F13 (test-side `_applySignedDelta` helper extension per RB-004.0b), H-D5 (incremental `totalScore` aggregate), H-D17 (revoked-gauge revert preventing corruption), SafeCast (OpenZeppelin `int256 ↔ uint256` guards).

### H-D20 — `IAuMM.mint` at claim time + H-D7 deployment timing independent + `recordEmissions` push locus delegated to H-D23 — status LOCKED

H-D20 fixes the AuMM minting site at `claim(address pool, address to)` — the user-facing claim entry runs `_accrueGlobal()` then `_settlePool(pool)` then `_settleUser(pool, msg.sender)` to compute `amount`, then calls `IAuMM.mint(to, amount)` if `amount > 0`, emits `Claimed(pool, msg.sender, to, amount)`, and updates `userRewardDebt[pool][msg.sender]`. Zero-amount short-circuit avoids a wasted external call. The mint pattern mirrors BodenseeBootstrapChannel's mint-at-distribute precedent (H-D11) — mint authority is the load-bearing scarce resource, and consolidating mint to the claim path keeps the AuMM supply invariant aligned with realized user value rather than allocator math (per H-D23's stranded-allocation semantic). H-D7 (`AuMM.setMinter` handoff timing — Stage H deploy vs Stage K governance migration) governs WHEN `setMinter(distributor)` fires at deploy (H10); H4 codes against distributor-as-minter without resolving H-D7 — the distributor's `IAuMM.mint` call reverts `NotMinter` pre-handoff, which is the desired pre-deploy / pre-launch posture. Cross-reference to H-D23: the `recordEmissions` push to EfficiencyOracle is NOT colocated at the `IAuMM.mint` claim site — push happens earlier at `_settlePool` per H-D23 (allocation-side F-10 semantics). Claim itself does NOT add a second `recordEmissions` push; the F-10 denominator is fed exactly once per pool per accrual interval at the `_settlePool` boundary. Anchors: H-D7 (`setMinter` handoff timing — OPEN, locks at H10), H-D11 (BodenseeBootstrapChannel mint-at-action precedent), H-D23 (`recordEmissions` push locus at `_settlePool`, not at claim mint), `IAuMM.mint` (Stage C minter gate), AuMM supply invariant (claim-time mint aligns minted supply with realized claims).

### H-D21 — Lazy accrual tick on mutating entry + H4 LP-tranche stub returning full block emission — status LOCKED

H-D21 fixes the accrual cadence at "every mutating external entry runs `_accrueGlobal()` first, before any state mutation." The pattern mirrors BodenseeBootstrapChannel's H-D11 lazy-accrual model: `recordScore`, `recordDeposit`, `recordWithdrawal`, and `claim` each invoke `_accrueGlobal()` at function entry, ensuring `accRewardPerScoreUnit` is fresh against `block.number` before the caller's intent applies. `_accrueGlobal()` itself: `if (block.number == lastAccrualBlock) return;` (empty-interval short-circuit), then `uint256 rate = _lpTrancheEmission(block.number);` (single read per call) and `accRewardPerScoreUnit += (rate * (block.number - lastAccrualBlock)).divDown(totalScore)` when `totalScore > 0`; otherwise the H-D15 empty-`totalScore` guard short-circuits. H4 ships `_lpTrancheEmission(uint256 block_) internal view returns (uint256)` as a STUB returning `IAuMM.blockEmissionRate(block_)` — the full block emission, no F-0 / F-3 / F-7 phase subtractions; this over-accrues vs the true LP tranche until H5 wires schedule logic that subtracts the F-0 bodensee share (Months 0–10 via H-D2 BodenseeBootstrapChannel), applies F-3 α-blend (Months 11–12), and subtracts F-7 Step 1 Incendiary skim (Year 2+ via H7 IIncendiaryRegistry stub). The stub posture is intentional H4 test-harness limitation, not a semantic lock: unit tests at H4 exercise accumulator + score + claim math in isolation with the full block emission rate; H5 wires the phase-aware emission curve and the H4 unit cohort migrates to the H5 semantics in place. No `selfdestruct` / `disable` / `pause` per H-D13 BodenseeBootstrapChannel precedent — the distributor is a permanent fixture. Anchors: H-D11 (lazy-accrual mint-at-action precedent), H-D4 (per-block segment accrual), H-D18 (H5 phase wiring), H-D15 (empty-`totalScore` guard at `_accrueGlobal`), AureumTime (Stage C era halving via `IAuMM.blockEmissionRate`).

### H-D22 — Two-tier storage layout: global + per-pool + per-user tiers + immutables — status LOCKED

H-D22 fixes the storage layout for `EmissionDistributor`. Global tier — single instances driving the universal accumulator: `uint256 public accRewardPerScoreUnit` (FixedPoint 18-decimal), `uint256 public totalScore` (Σ poolScore over approved gauges), `uint256 public lastAccrualBlock` (most recent `_accrueGlobal()` block). Per-pool tier — keyed on `address pool`: `mapping(address => uint256) public poolScore` (per-pool F-5 score), `mapping(address => uint256) public poolAccDebt` (per-pool snapshot of `accRewardPerScoreUnit` at last `_settlePool`), `mapping(address => uint256) public poolTotalLP` (sum of `userLP[pool][*]` — needed for per-user share computation at settle time). Per-user tier — keyed on `(address pool, address user)`: `mapping(address => mapping(address => uint256)) public userLP` (user's AuMT stake in pool), `mapping(address => mapping(address => uint256)) public userRewardDebt` (per-user single-snapshot variant analogous to `poolAccDebt`, computed against the pool-effective acc-per-LP-unit at settle time). Governance + recorder slots — single instances: `address public governance` (mutable, `setGovernanceContract` pivot per H-D8 / H-D10 / H-D14 precedent), `address public auMTContract` (recorder slot per H-D16, zero-address-acceptable). Immutables — wired at construction: `IAuMM public immutable AuMM`, `IGaugeRegistry public immutable _gaugeRegistry`, `IEMASampler public immutable _emaSampler`, `ICCBMultiplier public immutable _ccbMultiplier`, `IEfficiencyOracle public immutable _efficiencyOracle` (extended-in-place at an H4.1.x-bis sibling sub-step to add the `recordEmissions(address, uint256)` push signature, mirroring the H2a.10c ITVLOracle.quoteSvZCHF retrofit and G16 inheritor-enumeration discipline), `uint256 public immutable GENESIS_BLOCK`. No transient storage at H4 — the BodenseeBootstrapChannel transient-slot pattern is for the `Vault.unlock` callback path which the distributor does not consume. Anchors: H-D15 (global tier), H-D16 (per-user tier + `auMTContract` gate), H-D17 (immutable producer dependencies — `_gaugeRegistry` + `_emaSampler` + `_ccbMultiplier`), H-D23 (`_efficiencyOracle` immutable for `recordEmissions` push), H2a.10c (ITVLOracle.quoteSvZCHF in-place extension precedent), G16 (interface-touching scope discipline — enumerate all inheritors).

### H-D23 — Accrual-time `recordEmissions` push at `_settlePool` boundary + allocation-side F-10 semantics — status LOCKED

H-D23 locks the EfficiencyOracle push locus at `_settlePool(pool)` rather than at the `IAuMM.mint(claimer, amount)` claim site — the F-10 efficiency denominator (`emissions_received_i`) feeds allocation-side data, not mint-side, to capture pool economic earnings independent of LP claim cadence. Push site: inside `_settlePool(pool)`, AFTER `_accrueGlobal()` (so `accRewardPerScoreUnit` is fresh) but BEFORE updating `poolAccDebt[pool]` (so the allocation amount is captured against the pre-settle snapshot). Amount: `uint256 poolAllocation = (accRewardPerScoreUnit - poolAccDebt[pool]).mulDown(poolScore[pool])` — FixedPoint mulDown, AuMM scaled18 unit; passed as `_efficiencyOracle.recordEmissions(pool, poolAllocation)` to the H-D10 push entry. Zero-skip: `if (poolAllocation == 0) skip the external call` — matches H-D10's skip-on-zero spirit and saves the `_ensureCurrentEpoch` round-trip when there is nothing to record. Semantic lock for auditors: `recordEmissions` feeds F-10 allocation-side data, not mint-side; Σ allocation ≥ Σ minted, with equality only when all allocated rewards are eventually claimed; divergence equals stranded allocation when `poolScore > 0` and `poolTotalLP == 0`, which is the intentional tournament signal (pools that capture score without a functioning LP market correctly look low-efficiency in the F-10 ranking). No double-push: `poolAccDebt[pool]` writes immediately after the push, so a subsequent `_settlePool(pool)` in the same interval pushes only the incremental `(Δacc × poolScore)` (typically zero if no intervening accrual). Claim path: `claim` also runs `_settlePool` (pool settle precedes per-user MasterChef math); claim does NOT add a second push at `IAuMM.mint` per H-D20. Recorder gate: the push call lands only when EfficiencyOracle's `emissionsRecorder == address(this)`; pre-deploy / deprecated-recorder posture per H-D10 safety valve produces a revert from `onlyEmissionsRecorder` — distributor does not catch the revert (deploy correctness is governance's responsibility). Rounding: FixedPoint divDown produces bounded drift between allocation sum (small frequent pushes) and mint sum (occasional large mints); no FINDINGS row unless integration tests at H9 show material F-10 rank inversion from the drift. Pre-Stage-I integration gap: `recordScore(pool)` is the only live mutator until AuMT lands at Stage I, and the `_settlePool` push from `recordScore` keeps F-10 denominator data flowing during H4—H9. Anchors: H-D10 (`recordEmissions` producer + skip-on-zero spirit + `onlyEmissionsRecorder` gate + safety valve), H-D15 (settle math), H-D20 (claim-time mint without second push), F-10 (efficiency denominator — `emissions_received_i`), G-D23 (efficiency tournament — allocation-side semantics align with rank stability), OQ-G1 (F-10 SMA formula).

### H-D24 — Per-pool `poolAccRewardPerLP` accumulator slot + `_settlePool` per-LP-unit accumulation step — status LOCKED

H-D24 closes the per-LP-unit derivation gap that H-D16's `(poolEffectiveAccPerLPunit − userRewardDebt[pool][user]) × userLP[pool][user]` formula leaves unresolved against H-D15's settle math and H-D22's storage layout — H-D22's per-pool tier enumeration ships `poolScore`, `poolAccDebt`, and `poolTotalLP` but no per-LP-unit accumulator slot, and H-D15's `_settlePool` math computes `poolAllocation = Δacc × poolScore` and pushes it to EfficiencyOracle per H-D23 but does not derive a per-LP share. Resolution: a fourth per-pool tier slot `mapping(address => uint256) public poolAccRewardPerLP` (FixedPoint 18-decimal AuMM-wei-per-LP-unit accumulator), extending the H-D22 per-pool tier enumeration in-place at H4.5g. `_settlePool(pool)` is extended with a single per-LP-unit accumulation step inside the existing `poolAllocation > 0` zero-skip, sandwiched between the H-D23 EfficiencyOracle push and the `poolAccDebt[pool] = acc` rebase: cache `uint256 totalLP = poolTotalLP[pool]`, then `if (totalLP > 0) { poolAccRewardPerLP[pool] += poolAllocation.divDown(totalLP); }`. The inner `totalLP > 0` guard prevents divide-by-zero when a pool has score but no LP deposits (pre-Stage-I posture or post-withdrawal-storm) — the allocation still pushes to EfficiencyOracle for F-10 gauge accounting per H-D23, but no per-LP-unit accrual occurs and the allocation is effectively stranded from LP distribution (standard MasterChef behaviour — allocations that arrive when LP supply is zero do not retroactively credit later depositors; the F-10 denominator data is still fed via the H-D23 push, preserving the stranded-allocation semantic locked in H-D23 prose at L114). H-D23 strict order is preserved: EfficiencyOracle push FIRST inside the zero-skip, per-LP-unit accumulator increment SECOND inside the same skip with its own inner zero-LP guard, `poolAccDebt[pool] = acc` rebase LAST after the if-block closes. H-D16's `poolEffectiveAccPerLPunit` binds to `poolAccRewardPerLP[pool]` literally: `userRewardDebt[pool][user]` stores a snapshot of `poolAccRewardPerLP[pool]` at user-settle time (H4.6 entry), and `pendingClaim(pool, user)` per IEmissionDistributor L199-L205 derives `(poolAccRewardPerLP[pool] − userRewardDebt[pool][user]).mulDown(userLP[pool][user])` at H4.7. IEmissionDistributor extends in-place at H4.5f to add `function poolAccRewardPerLP(address pool) external view returns (uint256);` to the per-pool view surface, mirroring the H2a.10c interface-extension precedent and G16 inheritor-enumeration discipline (no mock inheritors exist for IEmissionDistributor at H4.0d — Stage I AuMT producer lands later). EmissionDistributor at H4.5g adds the storage slot declaration to the per-pool tier (positioned alphabetically-by-purpose after `poolAccDebt` and before `poolTotalLP` per H-D22 ordering convention) and retrofits the `_settlePool` body with the per-LP-unit accumulation step. The amendment is additive — H-D15 / H-D16 / H-D22 / H-D23 bodies remain LOCKED in their existing form; H-D24 is the canonical resolution and cross-refs them as anchors. Anchors: H-D15 (settle math — extended with per-LP-unit step), H-D16 (`poolEffectiveAccPerLPunit` binding — resolved to `poolAccRewardPerLP[pool]`), H-D22 (per-pool tier — extended in-place with fourth slot), H-D23 (strict order preserved — EfficiencyOracle push BEFORE per-LP-unit accumulation BEFORE `poolAccDebt` rebase), MasterChef standard (stranded-allocation skip-on-zero-LP semantic), FixedPoint divDown (per-LP-unit increment math at 18-decimal scale), H1.9-bis (in-stage design extension precedent — H-D10 v2 svZCHF numéraire amendment), H2a.10c (interface-extension precedent — ITVLOracle.quoteSvZCHF retrofit), G16 (inheritor enumeration discipline).

### H-D25 — Per-user `pendingBalance` crystallized-claim slot + `recordDeposit` / `recordWithdrawal` settle pattern — status LOCKED

H-D25 closes the per-user crystallized-pending-claim derivation gap that H-D16's "settle the user's pending claim against pre-deposit `userLP`" prose leaves unresolved against H-D20's `IAuMM.mint`-at-`claim`-only lock and H-D22's per-user tier enumeration. H-D22's per-user tier ships `userLP` and `userRewardDebt` but no crystallized-pending slot; H-D16's `recordDeposit` semantics ("settle pending against pre-deposit `userLP`" + "update `userRewardDebt` to the new pool-effective accumulator") would otherwise erase the pre-deposit pending allocation when `userLP` increments and `userRewardDebt` rebases to the fresh `poolAccRewardPerLP[pool]` — the Sushi MasterChef V2 standard auto-claim-at-deposit pattern is unavailable under H-D20, which fixes the `IAuMM.mint` site at `claim`. Resolution: a third per-user tier slot `mapping(address => mapping(address => uint256)) public pendingBalance` (FixedPoint 18-decimal AuMM-wei crystallized-pending balance), extending the H-D22 per-user tier in-place at H4.6. `recordDeposit(pool, user, amount)` body per H-D16 / H-D21 / H-D25: revert if `msg.sender != auMTContract`; `_accrueGlobal()`; `_settlePool(pool)`; cache `uint256 acc = poolAccRewardPerLP[pool]`; compute `uint256 pending = (acc - userRewardDebt[pool][user]).mulDown(userLP[pool][user])`; zero-skip `if (pending > 0) { pendingBalance[pool][user] += pending; }`; increment `userLP[pool][user] += amount` and `poolTotalLP[pool] += amount`; rebase `userRewardDebt[pool][user] = acc`; emit `DepositRecorded`. `recordWithdrawal` is symmetric — identical settle / `pendingBalance` crystallization / debt rebase, with `userLP` and `poolTotalLP` decrementing by `amount`. `claim(pool, to)` per H-D20 / H-D25 settles identically against `acc = poolAccRewardPerLP[pool]`, then computes `uint256 amount = pendingBalance[pool][user] + (acc - userRewardDebt[pool][user]).mulDown(userLP[pool][user])` — additive sum of crystallized-pending and live-delta — zero-skips on `amount == 0`, otherwise zeros `pendingBalance[pool][user] = 0`, rebases `userRewardDebt[pool][user] = acc`, and mints `IAuMM.mint(to, amount)` per H-D20; emits `Claimed(pool, msg.sender, to, amount)`. `pendingClaim(pool, user)` view per `IEmissionDistributor` binds to the same additive form: `pendingBalance[pool][user] + (poolAccRewardPerLP[pool] - userRewardDebt[pool][user]).mulDown(userLP[pool][user])`; the `@dev` retrofits in-place at H4.5h with the crystallized-pending term added to the H-D24 live-delta term. The pool-settle ordering invariant (H-D23 strict order) is unaffected — `pendingBalance` writes are per-user, happen AFTER `_settlePool(pool)` returns, and never touch `poolAccDebt[pool]` or `poolAccRewardPerLP[pool]`. `IEmissionDistributor` extends in-place at H4.5h to add `function pendingBalance(address pool, address user) external view returns (uint256);` to the per-user view surface, mirroring the H4.5f / H-D24 interface-extension precedent and G16 inheritor-enumeration discipline (no `IEmissionDistributor` inheritors at H4.5h — Stage I AuMT producer lands later). `EmissionDistributor` at H4.6 adds the storage slot declaration to the per-user tier (positioned after `userRewardDebt` per H-D22 ordering convention) alongside the `recordDeposit` and `recordWithdrawal` implementations; the slot does not require a separate H4.5-style retrofit because the H4.6 sub-step is the only writer and H4.7 `claim` is the only reader and zeroer. The amendment is additive — H-D15 / H-D16 / H-D20 / H-D22 / H-D24 bodies remain LOCKED in their existing form; H-D25 is the canonical resolution and cross-refs them as anchors. Anchors: H-D16 (per-user settle semantics — extended with crystallized-pending step), H-D20 (`IAuMM.mint`-at-`claim`-only — preserved by routing pre-claim allocations to `pendingBalance` rather than to a mint call inside `recordDeposit`), H-D22 (per-user tier — extended in-place with third slot), H-D23 (pool-settle strict order preserved — per-user crystallization happens after `_settlePool` returns), H-D24 (`poolAccRewardPerLP` accumulator — source of `acc` for per-user delta computation), MasterChef V2 standard (`pendingReward` aggregation pattern adapted to a delayed-mint variant per H-D20), FixedPoint `mulDown` (per-user delta math at 18-decimal scale), H2a.10c (interface-extension precedent), H4.5f / H4.5g (in-stage H-D24 extension precedent), G16 (inheritor enumeration discipline).

### H-D26 — `_lpTrancheIntegral(from, to)` replaces single-block sampling in `_accrueGlobal` for conservation-correct interval integration — status LOCKED

H-D26 closes the discretization-bias gap that H-D21's single-block-sample × `deltaBlocks` accrual pattern leaves unresolved against F-0's piecewise-linear `bodensee_share(b)` schedule per `11_formulas.md` F-0 (Bootstrap A: `0.80 − 0.30 × (b − genesis) / (month6End − genesis)` for `b ≤ month6End`; Bootstrap B: `0.50 − 0.50 × (b − month6End) / (month10End − month6End)` for `month6End < b ≤ month10End`; zero thereafter) and against the era-halving piecewise-constant `IAuMM.blockEmissionRate(b) = GENESIS_RATE >> eraIndex(b, GENESIS_BLOCK)` schedule per OQ-5 / §xxix. H-D21's `_accrueGlobal` body (`rate = _lpTrancheEmission(block.number); accRewardPerScoreUnit += (rate * deltaBlocks).divDown(total)`) snapshots both the F-0 LP-share factor and the era rate at `block.number` and applies them as constants across the entire `[lastAccrualBlock + 1, block.number]` interval. During Months 0—10 this systematically undercounts LP allocation by the integrated linear drift (average bias ≈ half the share-drift per call); across era-halving boundaries (`nthHalvingBlock(g, k)` for k ≥ 1) it uses the post-halving rate for the entire interval, biasing by up to 50%. Both violate the conservation invariant `LP_integral(from, to) + Bodensee_apsum(from, to) + Incendiary_integral(from, to) = blockEmissionRate × n` against `BodenseeBootstrapChannel.accrue`'s AP integration at `src/emission/BodenseeBootstrapChannel.sol` L172-L196. Resolution: replace the single-block sample with an interval-integrated helper `function _lpTrancheIntegral(uint256 from, uint256 to) internal view returns (uint256)` returning the conservation-correct LP-tranche emission integrated over `[from, to]` inclusive, split at three classes of boundaries — F-0 piecewise (`month6End`, `month10End`), F-3/F-7 phase (`year1End`), and era-halving (`nthHalvingBlock(g, k)`) — with constant `blockEmissionRate(sub_from)` per sub-interval and per-phase LP-share factor applied via AP closed-form (H-D27 bootstrap) or constant `rate × n` (H-D28 transition, H-D29 continuous). `_accrueGlobal` body rewrites to: `if (block.number == last) return; uint256 total = totalScore; if (total == 0) { lastAccrualBlock = block.number; return; } uint256 contribution = _lpTrancheIntegral(last + 1, block.number); accRewardPerScoreUnit += contribution.divDown(total); lastAccrualBlock = block.number;`. The `_lpTrancheEmission(block_)` view at `src/emission/EmissionDistributor.sol` L174-L176 is RETAINED as an external view for tests / UI / IEmissionDistributor view contract — it returns the single-block LP rate at `block_`; the production accrual path no longer reads it. H-D21 remains LOCKED for H4 historical posture; H-D26 supersedes it for the H5 production accrual path. Anchors: H-D21 (H4 LP-tranche stub — superseded for production), F-0 / F-3 / F-7 (canonical schedules per `11_formulas.md` fetched at H5.0a-pre), `BodenseeBootstrapChannel._apSum` (canonical AP closed-form mirrored bit-for-bit per H-D27), OQ-5 (`BLOCKS_PER_ERA = 10_512_000` halving constant), F12 (signed-delta type discipline carries to per-sub-interval contribution math).

### H-D27 — Bootstrap leg AP integration mirrors `BodenseeBootstrapChannel._apSum` bit-for-bit; split at `month6End` per F-0 piecewise boundary — status LOCKED

H-D27 specifies the F-0 bootstrap-window sub-interval body of `_lpTrancheIntegral` per H-D26. For any sub-interval `[sub_from, sub_to]` with `sub_to ≤ month10EndBlock(GENESIS_BLOCK)`, the F-0 LP-share schedule is `lp_share(b) = 1 − bodensee_share(b)` per `11_formulas.md` F-0 — Bootstrap A: `bodensee_share(b) = 0.80 − 0.30 × (b − genesis) / (month6End − genesis)` for `b ≤ month6End`; Bootstrap B: `bodensee_share(b) = 0.50 − 0.50 × (b − month6End) / (month10End − month6End)` for `month6End < b ≤ month10End`. Conservation against `BodenseeBootstrapChannel.accrue` is enforced via the identity `lp_apsum(from, to) = rate × n − bodensee_apsum(from, to)` where `bodensee_apsum` matches `_apSum` at `src/emission/BodenseeBootstrapChannel.sol` L218-L226 bit-for-bit: `n = to − from + 1; width = anchorEnd − anchorStart; first = startShare − (dropShare × (from − anchorStart)) / width; last = startShare − (dropShare × (to − anchorStart)) / width; bodensee_apsum = ((first + last) × n × rate) / (2 × 1e18)`. Cross-`month6End` sub-intervals split into A-leg `[sub_from, month6End]` (anchorStart = genesis, anchorEnd = month6End, startShare = 8e17, dropShare = 3e17) plus B-leg `[month6End + 1, sub_to]` (anchorStart = month6End, anchorEnd = month10End, startShare = 5e17, dropShare = 5e17). The `rate` snapshot in each sub-interval is `IAuMM.blockEmissionRate(sub_from)` — Era 0 fully encloses the bootstrap window (`month10End ≈ 2.19M < 10.5M = BLOCKS_PER_ERA` per OQ-3), so this snapshot is exact within bootstrap. The H-D26 phase-split at `month10End` ensures the bootstrap leg never overlaps the F-3/F-7 phases. The `rate × n − bodensee_apsum` form preserves the BodenseeBootstrapChannel arithmetic literal identity (conservation by construction; one fewer chance of rounding-mode divergence under FixedPoint mulDown/divDown semantics) over the mathematically-equivalent direct LP-share AP `((first_lp + last_lp) × n × rate) / (2 × 1e18)` form. Implementation in `EmissionDistributor.sol` reimplements the AP closed-form as a private `_bootstrapApSum` helper (not a call to `BodenseeBootstrapChannel` — H-D2's channel-distributor separation lock forbids upstream-call coupling; the formula is duplicated bit-for-bit and unit-tested for arithmetic identity at H5.4 against the BodenseeBootstrapChannel fixture). Anchors: F-0 (canonical schedule per `11_formulas.md` F-0 fetched at H5.0a-pre), `BodenseeBootstrapChannel._apSum` L218-L226 (literal arithmetic source), H-D11 (AP piecewise integration discipline established for the channel — reused here), H-D2 (channel-distributor separation — formula duplicated, not called), OQ-3 (Era 0 spans entire bootstrap window — `rate` snapshot exact), `AureumTime.month6EndBlock` / `month10EndBlock` (boundary helpers at `src/lib/AureumTime.sol` L54-L61).

### H-D28 — Transition leg (Months 11—12, `month10End < block ≤ year1End`) returns `rate × n` with no LP-tranche subtraction — status LOCKED

H-D28 specifies the F-3 transition-phase sub-interval body of `_lpTrancheIntegral` per H-D26. For any sub-interval `[sub_from, sub_to]` with `sub_from > month10EndBlock(GENESIS_BLOCK)` and `sub_to ≤ year1EndBlock(GENESIS_BLOCK)`, the LP-tranche contribution is `rate × (sub_to − sub_from + 1)` where `rate = IAuMM.blockEmissionRate(sub_from)`. No bodensee or Incendiary subtraction applies — per F-0 the bootstrap share is permanently zero after `month10End`, and per F-7 the Incendiary skim activates only post-`year1End`. The F-3 α-blend formula `share_i(block) = (1 − α(block)) × (1/28) + α(block) × CCB_share_i(block)` per `11_formulas.md` F-3 governs PER-POOL share assignment within the LP tranche — it does NOT subtract from the global LP-tranche emission, per the canonical F-3 domain note: "During Months 11–12, bodensee_share = 0 — LP tranche equals full block emission before Incendiary." The α-blend lives in `recordScore` / `poolScore` middleware per H-D18 ("In-distributor F-3 / F-7 share computation; F-3 α-blend + F-7 Incendiary skim wired at H5"), where `poolScore[pool]` is reshaped as the `recordScore` consumer applies the F-3 weighting before the accumulator math. `_lpTrancheIntegral`'s transition leg is therefore a degenerate sub-interval — `rate × n` with no closed-form integration complexity (the integrand is constant, both because `lp_share = 1` per F-3 domain note and because `blockEmissionRate` is constant within Era 0 which still encloses Year 1 end per OQ-5: `year1End ≈ 2.628M < 10.5M = BLOCKS_PER_ERA`). H-D18's per-pool F-3 α-blend reshape lands at H5.3 alongside the F-7 Step 2 CCB share-weight wiring; H-D28 governs only the global LP-tranche emission integral seen by `_accrueGlobal`. Anchors: F-3 (canonical schedule + "bodensee_share = 0" domain note per `11_formulas.md` F-3 fetched at H5.0a-pre), H-D18 (per-pool F-3 α-blend lives in `recordScore` middleware, not in `_lpTrancheIntegral`), H-D6 (1/28 literal supply-deflationary share for the equal-split leg of F-3), OQ-5 (Era 0 encloses Year 1 end — `rate` constant within transition window), `AureumTime.month10EndBlock` / `year1EndBlock` (boundary helpers at `src/lib/AureumTime.sol` L59 / L70).

### H-D29 — Continuous leg (Year 2+, `block > year1End`) F-7 Step 1 Incendiary skim — mutable governance-settable `incendiaryRegistry` slot + interval-form `IIncendiaryRegistry.integratedSkim(from, to)` interface; Stage H stub returns 0 — status LOCKED

H-D29 specifies the F-7 continuous-phase sub-interval body of `_lpTrancheIntegral` per H-D26 and the deployment-ordering + interface-shape decisions for the Stage L forward-dependency wiring. For any sub-interval `[sub_from, sub_to]` with `sub_from > year1EndBlock(GENESIS_BLOCK)`, the LP-tranche contribution per F-7 Step 1 ("Incendiary_total = Σ active Incendiary Boost claims this block" per `11_formulas.md` F-7) is `rate × n − Incendiary_integral(sub_from, sub_to)` where `Incendiary_integral` sums active claims over the interval. At Stage H ship, the Incendiary contract does NOT exist (lands Stage L per CLAUDE.md repository layout `src/incendiary/`) — `_lpTrancheIntegral` returns `rate × n` for the continuous leg with `Incendiary_integral = 0` as a forward-dep stub. Slot-management decision (max-security + easy-to-manage trade-off): `address public incendiaryRegistry` defaults to `address(0)` at deploy, set via `function setIncendiaryRegistry(address newRegistry) external onlyGovernance` accepting `address(0)` as a deprecation safety valve — matches the Stage H precedent for `auMTContract` (H-D16), `feeRecorder` / `emissionsRecorder` (H-D10), and `governance` slots (governance-settable, `address(0)` clears). Diverges from the AuMM.setMinter one-shot precedent (H-D7) because Incendiary lacks the "scarcest resource" justification — mint authority is one-shot to bound rug-pull surface; Incendiary skim recipient is bounded by F-7's "reallocation, not new inflation" invariant per `11_formulas.md` F-7 domain note, and the safety valve preserves LP-safe fallback to zero skim if the registry is bricked or compromised. Marginal one-shot security gain is small relative to the uniform Stage H governance trust model (multisig → Stage K on-chain governance handoff). Emits `IncendiaryRegistrySet(address oldRegistry, address newRegistry)` on set; reverts under `onlyGovernance` modifier; no zero-check on `newRegistry` (zero is the explicit deprecation valve). Interface-shape decision (security + program): `interface IIncendiaryRegistry { function integratedSkim(uint256 from, uint256 to) external view returns (uint256); }` — interval-form returns the pre-integrated F-7 Step 1 sum over `[from, to]` inclusive. Diverges from per-block `claimsAt(uint256 block_)` form because the per-block form would force the distributor to loop O(deltaBlocks) per `_lpTrancheIntegral` call — an unbounded gas vector exploitable by deliberately lagging `lastAccrualBlock` on a high-traffic pool, then triggering accrual to OOG the distributor and brick claims. Interval form shifts the iteration responsibility to the registry, which can ship bucket-cached cumulative sums internally (e.g. epoch-bucketed cumulative sums updated lazily by a permissionless `crystallize(from, to) external` registry-side helper) so `integratedSkim` itself stays O(1) regardless of interval size. The `view` mutability on `integratedSkim` signals read-only to the distributor — the registry's nonpayable cache-update functions are out of the distributor's path. Interval shape also matches `_lpTrancheIntegral`'s `(from, to) → uint256` contract bit-for-bit, symmetric composition. Distributor read site inside the continuous-leg sub-interval body: `address registry = incendiaryRegistry; uint256 skim = 0; if (registry != address(0)) { skim = IIncendiaryRegistry(registry).integratedSkim(sub_from, sub_to); } contribution += rate * n - skim;`. NO try/catch wrap — a reverting registry reverts the entire accrual (governance must clear or fix); conservative posture against silent skim-suppression that would over-credit LPs and push past the 21M cap. H-D3 scope clarification: the BLOCKS_PER_EPOCH step-sampling locked in H-D3 applies to F-7 Step 2 CCB-weighted POOL share assignment, NOT to F-7 Step 1's per-block Incendiary skim — F-7 canonical text says "Step 1 runs every block" for the Incendiary subtraction; only the per-pool weight redistribution (Step 2) samples at epoch boundaries. H-D3 scope-amendment is in-scope for H5.3 (pool-score F-7 wiring); H-D29 governs the global skim subtraction only. Defense-in-depth deferred: skim cap (e.g. `Incendiary_integral ≤ rate × n × FRACTION` for some `FRACTION < 1e18`) deferred as an optional H5 review decision — strict F-7 spec allows up to 100% skim, but a cap protects against malicious registry returns; flag at H5.4 unit cohort design for explicit yea/nay. Stage L coordination: H10 deployment script does NOT need to set `incendiaryRegistry` (default `address(0)` is the deployment value); Stage L deploys the canonical `IIncendiaryRegistry` implementation under `src/incendiary/`, then governance calls `setIncendiaryRegistry(deployedRegistry)` once via the multisig. Anchors: F-7 (canonical schedule + "Incendiary is reallocation, not new inflation" domain note per `11_formulas.md` F-7 fetched at H5.0a-pre), H-D18 (F-7 wiring locus — Incendiary subtraction at distributor level per H-D29; CCB share reshape at recordScore per H-D18), H-D3 (BLOCKS_PER_EPOCH applies to F-7 Step 2 weights, NOT to Step 1 skim — scope clarified), H-D10 (`feeRecorder` / `emissionsRecorder` precedent for mutable governance-settable + `address(0)` deprecation valve), H-D7 (AuMM.setMinter one-shot — divergent precedent; H-D29 declines one-shot for the reasons above), Stage L forward-dep (`src/incendiary/` IIncendiaryRegistry contract lands at Stage L per CLAUDE.md repository layout), `AureumTime.year1EndBlock` (boundary helper at `src/lib/AureumTime.sol` L70).

### H-D30 — Era-boundary splitting in `_lpTrancheIntegral`: sub-intervals split at `nthHalvingBlock(g, k)` for each spanned k ≥ 1; constant `blockEmissionRate(sub_from)` per sub-interval — status LOCKED

H-D30 specifies the era-boundary splitting discipline for `_lpTrancheIntegral` per H-D26. `IAuMM.blockEmissionRate(b) = GENESIS_RATE >> eraIndex(b, GENESIS_BLOCK)` is piecewise-constant across `BLOCKS_PER_ERA` boundaries per OQ-5 / §xxix (`AureumTime.eraIndex` at `src/lib/AureumTime.sol` L46-L49 returns the zero-indexed era; `nthHalvingBlock` at L80-L82 returns each era boundary). Every `nthHalvingBlock(g, k)` for `k ≥ 1` marks a 50% rate step. An accrual interval `[from, to]` spanning one or more halving boundaries that snapshots `rate = blockEmissionRate(to)` once would systematically undercount the LP-tranche allocation by up to 50% across the pre-halving portion — violating the same conservation invariant H-D26 closes for F-0 piecewise drift. Resolution: `_lpTrancheIntegral` splits the interval at every spanned `nthHalvingBlock(g, k)` and computes each era-sub-interval with its own constant `rate = blockEmissionRate(era_sub_from)`. Algorithm: `uint256 cursor = from; uint256 contribution = 0; while (cursor <= to) { uint256 era = AureumTime.eraIndex(GENESIS_BLOCK, cursor); uint256 eraEnd = AureumTime.nthHalvingBlock(GENESIS_BLOCK, era + 1) - 1; uint256 subTo = eraEnd < to ? eraEnd : to; uint256 rate = IAuMM(AuMM).blockEmissionRate(cursor); contribution += _phaseAwareBody(cursor, subTo, rate); cursor = subTo + 1; } return contribution;`. The `_phaseAwareBody(from_, to_, rate_)` helper dispatches to the H-D27 bootstrap leg, H-D28 transition leg, or H-D29 continuous leg based on `from_` vs `month10End` / `year1End` boundaries, further splitting at phase boundaries within the era sub-interval if needed (an era sub-interval that spans `month10End` splits into a bootstrap leg + a transition leg). Era 0 fully encloses the bootstrap window (`month10End ≈ 2.19M < 10.5M = BLOCKS_PER_ERA` per OQ-3) and the transition window (`year1End ≈ 2.628M < 10.5M`), so for the first ~3.5 years post-genesis the era loop has at most 1 iteration per `_lpTrancheIntegral` call and the era-splitting is effectively a no-op gas-cost-wise. From Era 1 onward (post-`firstHalvingBlock(g)` ≈ 4 years), an accrual interval can span at most a small constant number of era boundaries given typical user-activity cadence — Stage I AuMT deposit/withdrawal frequency keeps `lastAccrualBlock` close to `block.number` for active pools, and even fully dormant pools accumulate at most O(eras_since_genesis) sub-intervals = O(years/4) over the protocol lifetime. The loop is unbounded in principle but bounded in practice by user activity; gas-cost analysis at H5.4 unit tests with worst-case multi-era intervals confirms iteration count stays sub-O(eras_since_genesis) under typical mainnet conditions and never breaches block gas limits at reasonable era counts (Era 5 ≈ 20 years post-genesis bounds practical analysis). Era splitting + phase splitting compose: a sub-interval spanning BOTH `month10End` AND `firstHalvingBlock(g)` is impossible at H5 ship (`month10End < firstHalvingBlock` by 8M+ blocks per OQ-3 + OQ-5), but the algorithm handles the composition path correctly — phase splitting nests inside each era sub-interval. Anchors: OQ-5 (`BLOCKS_PER_ERA = 10_512_000` canonical halving constant + Era 0 enclosure of bootstrap + transition windows per OQ-3), AuMM.sol `IAuMM.blockEmissionRate(b) = GENESIS_RATE >> eraIndex(b, GENESIS_BLOCK)` (piecewise-constant schedule source), `AureumTime.eraIndex` / `nthHalvingBlock` (boundary helpers at `src/lib/AureumTime.sol` L46-L49 / L80-L82), H-D26 (interval-splitting parent decision — era-splitting is one of three boundary classes), H-D27 / H-D28 / H-D29 (per-phase sub-interval bodies invoked inside each era loop iteration), F12 (signed-delta type discipline — each sub-interval contribution must be uint256-safe under accumulation).

### H-D31 — `recordScore` H5.3 reshape: storage extensions + `IMiliariumRegistry` 6th immutable + 10-step flow order — status LOCKED

H-D31 specifies the storage additions to `EmissionDistributor.sol` for the H5.3 α-blend wiring and the step-by-step `recordScore` flow order. H4's `recordScore` stored raw F-5 scores directly into `poolScore[pool]`; H5.3 repurposes `poolScore[pool]` as the reshaped effective score (the value driving MasterChef accumulator weight) and adds two storage slots to retain raw F-5 aggregates for anti-enumeration signed-delta accounting. Storage additions: `uint256 public f5Total` — running sum of raw F-5 scores across all gauged pools, updated via signed-delta on every `recordScore` call, initialized to 0; `mapping(address => uint256) public f5Score` — per-pool raw F-5 score (pre-reshape), replacing the pre-reshape role `poolScore[pool]` held in H4, initialized to 0 per pool. `poolScore[pool]` is repurposed at H5.3 to hold the reshaped effective score; it remains the value consumed by `_settlePool` and the `totalScore` signed-delta accounting — no downstream API change, only the semantic meaning of its stored value changes. Immutable addition: `IMiliariumRegistry public immutable _miliariumRegistry` — sixth constructor immutable (after `AuMM`, `_gaugeRegistry`, `_emaSampler`, `_ccbMultiplier`, `_efficiencyOracle`), with a zero-address guard `if (miliariumRegistry_ == address(0)) revert ZeroAddress()`. Pre-Stage-J callers pass a stub registry returning `isMiliarium = false` for all pools; under the stub all gauged pools follow the non-Miliarium branch, so `effective = alpha.mulDown(score_F5_new)` — zero during bootstrap and transition, full F-5 score from Year 2. `recordScore` flow order (ten steps): (1) gauge gate `if (!_gaugeRegistry.isGaugeApproved(pool)) revert NotApproved(pool)`; (2) `_accrueGlobal()` — advance global accumulator to `block.number`; (3) `_settlePool(pool)` — flush per-pool debt before score changes; (4) compute `score_F5_new = CCBScore.score(_emaSampler.tvlEMA(pool), _ccbMultiplier.getMultiplier(pool))`; (5) signed-delta `f5Total` via `_applySignedDelta(f5Total, score_F5_new.toInt256() - f5Score[pool].toInt256())`; (6) write `f5Score[pool] = score_F5_new`; (7) `uint256 alpha = _alphaF3(block.number)`; (8) compute `effective_new` per H-D33 branch on `_miliariumRegistry.isMiliarium(pool)`; (9) signed-delta `totalScore` via `_applySignedDelta(totalScore, effective_new.toInt256() - poolScore[pool].toInt256())` — capture `oldEffective = poolScore[pool]` before this step for the emit; (10) write `poolScore[pool] = effective_new` and emit `ScoreUpdated(pool, oldEffective, effective_new)`. Cross-pool staleness: the `alpha` snapshot at each `recordScore` call differs across pools called at different blocks; accepted per Q2 bot-poke discipline — staleness is bounded by the BLOCKS_PER_EPOCH cadence of upstream F-4/F-8 scoring events and liveness is guaranteed by the permissionless `recordScore` poke surface. H-D3 epoch-step cadence: descriptive only at `recordScore` — no in-distributor epoch-boundary gate; step-function behavior derives from upstream F-4/F-8 cadences. Anchors: H-D18 (per-pool F-3 α-blend lives in `recordScore` middleware — locus decision), H-D32 (`_alphaF3` formula), H-D33 (effective score reshape formula), H-D3 (epoch cadence — descriptive, upstream), F12 (signed-delta type discipline — `_applySignedDelta`), G16 (inheritor enumeration: `_miliariumRegistry` is a new constructor argument; all `MockEmissionDistributor` and test-harness `EmissionDistributor` constructor call sites at H5.3d must wire the new arg).

### H-D32 — `_alphaF3(uint256 block_) private view returns (uint256)`: linear α blend factor with endpoint clamping — status LOCKED

H-D32 specifies the `_alphaF3(uint256 block_) private view returns (uint256)` helper in `EmissionDistributor.sol`. The F-3 blend factor α evolves linearly from 0 to 1e18 across the transition window `(month10EndBlock(GENESIS_BLOCK), year1EndBlock(GENESIS_BLOCK)]` per `11_formulas.md` F-3; outside the window α is clamped. Let `m10 = AureumTime.month10EndBlock(GENESIS_BLOCK)` and `y1 = AureumTime.year1EndBlock(GENESIS_BLOCK)`. Implementation: `if (block_ <= m10) return 0; if (block_ >= y1) return 1e18; return (block_ - m10 - 1) * 1e18 / (y1 - m10 - 1);`. Edge-case verification: α(m10) = 0 (lower clamp — bootstrap regime); α(m10 + 1) = `0 * 1e18 / (y1 - m10 - 1) = 0` (first block of transition window opens at α = 0 — equal-split full weight); α(y1 - 1) < 1e18 (penultimate Year 1 block is fractional); α(y1) = 1e18 (upper clamp — Year 2+ CCB-only regime). The formula unifies all three emission regimes branchlessly at the reshape level: during bootstrap (α = 0) the H-D33 reshape degenerates to `effective_Mil = f5Total / 28`, `effective_non_Mil = 0` — canonical F-1 equal-split; during Year 2+ (α = 1e18) it degenerates to `effective = score_F5_new` — canonical F-7 Step 4. The linear denominator `y1 - m10 - 1 = year1EndBlock(GENESIS_BLOCK) - month10EndBlock(GENESIS_BLOCK) - 1` evaluates to `2_628_000 - 2_190_000 - 1 = 437_999 blocks` per OQ-3 + OQ-5 canonical constants; no division-by-zero risk (`y1 > m10 + 1` by construction). Plain integer arithmetic — `(block_ - m10 - 1)` and `(y1 - m10 - 1)` are both block-count integers; multiplying by `1e18` then integer-dividing by `(y1 - m10 - 1)` yields the FixedPoint 18-decimal result directly; no SafeMath or FixedPoint library call needed. Anchors: F-3 (canonical linear blend per `11_formulas.md` F-3), OQ-3 (`BLOCKS_PER_MONTH = 219_000`, `month10EndBlock ≈ 2_190_000`), OQ-5 (`BLOCKS_PER_YEAR = 2_628_000`, `year1EndBlock ≈ 2_628_000`), `AureumTime.month10EndBlock` / `year1EndBlock` (boundary helpers at `src/lib/AureumTime.sol` L59 / L70), H-D33 (reshape formula consuming α).

### H-D33 — Effective score reshape formula: Miliarium pool F-3 equal-split blend vs non-Miliarium α-CCB only; Option A — non-Miliarium joins α-CCB leg during F-3 — status LOCKED

H-D33 specifies the effective score reshape formula at step 8 of `recordScore` (H-D31 flow order), gated by `_miliariumRegistry.isMiliarium(pool)`. Miliarium pool reshape: `effective_Mil = (1e18 - alpha).mulDown(f5Total / 28) + alpha.mulDown(score_F5_new)`. At α = 0: `effective_Mil = f5Total / 28` — equal-split 1/28 of aggregate raw score; canonical F-1 per H-D6 (1/28 literal supply-deflationary share). At α = 1e18: `effective_Mil = score_F5_new` — pure individual F-5 CCB score; canonical F-7 Step 4. During F-3 (0 < α < 1e18): blended between equal-split baseline and individual CCB weight — canonical F-3. Non-Miliarium gauged pool reshape: `effective_non_Mil = alpha.mulDown(score_F5_new)`. At α = 0: `effective_non_Mil = 0` — zero weight during bootstrap and at the transition-window ramp-up start; consistent with F-1 Miliarium-only equal-split regime. At α = 1e18: `effective_non_Mil = score_F5_new` — full F-5 CCB score from Year 2, identical treatment to Miliarium. During F-3 (0 < α < 1e18): partial CCB weight proportional to α — Option A (ratified per Q3 design decision; canonical source is `10_constitution.md §xxviii` which includes non-Miliarium pools in the F-3 α-CCB leg, superseding the `03_theoretical_foundation.md` reading that excluded non-Miliarium through Month 12; the Constitution is the higher-authority document where the two conflict). Q9 deployment invariant: governance gauges every Miliarium pool at slot creation; the `NotApproved` gate at step 1 of `recordScore` (H-D31) prevents any ungauged pool from reaching the reshape branch; no on-chain `isMiliarium → mustBeGauged` assertion inside the distributor — correctness maintained by deployment discipline. Conservation invariant: at α = 0, `Σ effective_i = Σ_{Mil} (f5Total / 28)`; with exactly 28 Miliarium pools gauged and all `recordScore`'d this collapses to `f5Total` (each gets `f5Total / 28`, 28 pools — conservation holds modulo integer-division floor rounding). At α = 1e18, `Σ effective_i = Σ f5Score[pool] = f5Total` — conservation by construction of the `f5Total` running sum. During F-3, `Σ effective_i = (1 - α) × Σ_{Mil} (f5Total / 28) + α × Σ_{all} f5Score[pool]`; with 28 Miliarium pools gauged this collapses to `(1 - α) × f5Total + α × f5Total = f5Total` — total effective score equals total raw F-5 score at every α (global conservation invariant, modulo integer-division floor at the 1/28 step). Anchors: F-3 + F-7 (canonical share assignment per `11_formulas.md`), `10_constitution.md §xxviii` (Option A canonical source — non-Miliarium joins α-CCB leg during F-3), H-D6 (1/28 literal supply-deflationary share for equal-split leg), F-D9 (Miliarium-only CCB scope), H-D31 (`recordScore` flow order — step 8 is this formula), H-D32 (`_alphaF3` — provides α).

### H-D34 — Test-side observation surface for `_lpTrancheIntegral` + `_lpTrancheEmission` via Harness contract pattern at `test/unit/harness/EmissionDistributorHarness.sol`; preserves prod ABI minimality — status LOCKED

H-D34 specifies the test-side observation surface for `_lpTrancheIntegral` and `_lpTrancheEmission` at H5.4. Both helpers are `internal view` in production per H-D26 (`src/emission/EmissionDistributor.sol` L212 / L308) — minimal-ABI posture per Stage K governance handoff + Stage R formal verification constraints — but the H5.4 unit cohort must validate their numeric outputs directly against expected closed-form values for the AP-identity, era-boundary, phase-boundary, Incendiary-stub, and conservation-partition tests. Resolution: add `test/unit/harness/EmissionDistributorHarness.sol`, a test-only contract `is EmissionDistributor` with two public-view pass-through wrappers — `function extLpTrancheIntegral(uint256 from, uint256 to) external view returns (uint256) { return _lpTrancheIntegral(from, to); }` and `function extLpTrancheEmission(uint256 block_) external view returns (uint256) { return _lpTrancheEmission(block_); }`. Constructor passes through all eight `EmissionDistributor` arguments (`AuMM`, `gaugeRegistry`, `emaSampler`, `ccbMultiplier`, `efficiencyOracle`, `miliariumRegistry`, `genesisBlock`, `initialGovernance`) verbatim. No additional state, no override of parent behavior; `Harness is-a EmissionDistributor` so existing tests' assertions against the parent's public surface remain valid through inheritance. Test-file integration at H5.4.0d: `distributor` slot in `test/unit/EmissionDistributor.t.sol` retypes from `EmissionDistributor` to `EmissionDistributorHarness`; setUp's `new EmissionDistributor(...)` retargets to `new EmissionDistributorHarness(...)`; the eight constructor-revert tests at L229—L319 remain on bare `EmissionDistributor` since they probe parent-constructor surface and the Harness adds no constructor behavior. NatSpec drift: H-D26 prose at `src/emission/EmissionDistributor.sol` L303 + L328 describes `_lpTrancheEmission` as 'RETAINED unchanged per H-D26 as the single-block external view for tests / UI' — the wording presupposed a visibility promotion that did not land at H5.1e; the H-D34 Harness wrapper is the canonical test access path replacing the imprecise prose. NatSpec amendment to clarify 'internal view accessed by tests via the H5.4 Harness wrapper' is OPTIONAL polish deferred to H5.5 close-of-family sweep — the prose drift is benign for audit since `_lpTrancheEmission` and `_lpTrancheIntegral` are not part of the production accrual ABI surface. G16 inheritor enumeration applies prospectively: any future H-D that changes `EmissionDistributor`'s public ABI must update both the parent and the Harness — `grep -rn 'is EmissionDistributor' src/ test/ script/` at the §8e.1 drafting beat enumerates all inheritors before the prompt is written. Anchors: H-D26 (interval-integration parent decision + NatSpec drift origin at L303 + L328), `src/emission/EmissionDistributor.sol` L212 / L308 (current `internal view` declarations on `_lpTrancheEmission` and `_lpTrancheIntegral`), G16 (interface-touching §8e.1 must enumerate all inheritors; applies symmetrically to harness inheritor enumeration as `EmissionDistributor` evolves), `.cursorrules` Cursor operation scope (Harness is a test-only construct under `test/unit/harness/` — Cursor does not touch production visibility at H5.4.0a), Foundry inheritance-based test-harness convention.

### H-D35 — In-place extension of Stage G IAuMT.sol — Stage I receipt-token consumer-side documentary additions (mint, burn, distributor, pool); G-D9 governanceWeight preserved verbatim; distributor recorder slot remains bare address per H-D16 — status LOCKED

H-D35 specifies the in-place extension of the existing `src/token/IAuMT.sol` — landed at Stage G per G-DP4 + G-D9 as a forward-dep stub for `VaultClassRegistry.vetoProposal`'s veto-threshold semantic. Pre-amendment surface (preserved verbatim): `is IERC20` inheritance + `governanceWeight(address holder) external view returns (uint256)` consumed at `src/gauge/VaultClassRegistry.sol:277` via the typed immutable binding `auMT = IAuMT(auMT_)` at L211. H-D35 adds the Stage I receipt-token consumer-side documentary surface required by `EmissionDistributor` H4—H5 recorder semantics: `mint(address to, uint256 amount) external` (called by per-pool LP deposit hook; Stage I AuMT internally calls `EmissionDistributor.recordDeposit`), `burn(address from, uint256 amount) external` (called by per-pool LP withdrawal hook; Stage I AuMT internally calls `EmissionDistributor.recordWithdrawal`), `distributor() external view returns (address)` (bound EmissionDistributor address), `pool() external view returns (address)` (bound per-pool address — AuMT is per-pool per CLAUDE.md §1). Direction of binding — the distributor is the CONSUMER side of the recorder relationship (AuMT calls into the distributor, never the inverse), so the `auMTContract` recorder slot in `EmissionDistributor` remains bare `address` per H-D16; the IAuMT extensions exist as a Stage I documentary obligation surface, not a distributor type assertion. G16 inheritor sweep is REQUIRED at H6.2a — Stage G shipped two `is IAuMT` inheritors that go abstract under the four added function declarations: (i) `test/unit/VaultClassRegistry.t.sol:29` `MockAuMT` (canonical), (ii) `test/fork/mocks/StageGMocks.sol:34` `MockAuMT` (explicit mirror with the `test/fork/mocks/StageGMocks.sol:L10` propagation NOTE requiring any signature change to land in the unit-test embed and the mirror in the same commit). Both `MockAuMT` inheritors extend with zero/stub bodies for the four new functions in the same commit (return 0 for `uint256` returns; return `address(0)` for `address` returns); the existing `governanceWeight` implementations and the six IERC20 stub implementations (`totalSupply`, `balanceOf`, `allowance`, `transfer`, `approve`, `transferFrom`) stay unchanged. `VaultClassRegistry.sol:211` typed binding and `VaultClassRegistry.sol:277` `governanceWeight` call site stay untouched. `forge test --match-path test/unit/VaultClassRegistry.t.sol` at H6.2b regression-checks the pre-existing Stage G veto cohort against the extended IAuMT to confirm no abstract-contract or selector-mismatch regression. Deferred to Stage I — events (`Mint`, `Burn`, `DistributorSet`, `PoolSet`), errors (`NotDistributor`, `ZeroAddress`), setter signatures for the distributor and pool slots (one-shot vs governance pattern not locked at H6), qualification-period / withdrawal-reset semantics per CLAUDE.md §1; H6 ships the minimum compile-stable surface plus G-D9 preservation, no Stage I behavior. Anchors: existing `src/token/IAuMT.sol` (G-DP4 / G-D9 forward-dep stub at Stage G — `governanceWeight(address)` consumer at `src/gauge/VaultClassRegistry.sol:277` via L211 typed binding), IAuMM template at `src/token/IAuMM.sol` (in-place ERC-20 interface pattern), H5.2 `IIncendiaryRegistry` stub precedent at `src/incendiary/IIncendiaryRegistry.sol` (forward-dep convention; precedent applies to surface shape only — IAuMT is not a new file per this H6.0c amendment), CLAUDE.md §1 (AuMT per-pool LP receipt definition), H-D16 (recorder slot bare `address` — confirmed retained), G-DP4 (Stage G forward-dep convention — `IAuMT` original stub), G-D9 (`VaultClassRegistry.vetoProposal` consumer of `governanceWeight`), G15 (scaffold-promise-drift lesson — extending forward-dep interfaces requires inheritor enumeration), G16 (interface-touching §8e.1 inheritor-enumeration discipline — `grep -rn 'is IAuMT' src/ test/ script/`), G-D25c (Stage G mock-unification deferral — MockAuMT remains duplicated across unit + fork mock files per same-commit propagation NOTE).

### H-D36 — Real-AuMM integration test family at test/unit/AuMMDistributorIntegration.t.sol — Scope A integration coverage with 4-test cohort exercising mock/real setMinter parity + halving — status LOCKED

H-D36 specifies the real-AuMM integration test family at `test/unit/AuMMDistributorIntegration.t.sol`, confirming mock/real `setMinter` state-machine parity by constructing a real `AuMM(GENESIS_BLOCK_, address(this))` at `src/token/AuMM.sol` and exercising the constructor-admin → `setMinter(address(distributor))` one-shot handoff against real two-flag-lock semantics (C-D11 — `_minterAdmin` zeroed on success), then driving the full deposit → settle → claim → `IAuMM.mint` flow against real AuMM rather than the `MockAuMM` at `test/unit/EmissionDistributor.t.sol` L19—L56. Test file is new `test/unit/AuMMDistributorIntegration.t.sol` — separate from `test/unit/EmissionDistributor.t.sol` to keep the 100-test MockAuMM-driven unit cohort isolated from real-AuMM-induced setUp shifts; Stage I AuMT integration tests are a natural extension to the same file post-H6. Four-test cohort: `test_SetMinterHandoff_EmitsMinterSet` (construct real AuMM with `minterAdmin_ = address(this)`; expect `MinterSet(address(distributor))` event; call `aumm.setMinter(address(distributor))`; assert `aumm.minter() == address(distributor)`), `test_RevertWhen_SetMinterCalledTwice` (after first `setMinter` succeeds, expect `NotMinterAdmin` revert on second call — `_minterAdmin` was zeroed on first success, so the first-failing guard is `NotMinterAdmin` not `MinterAlreadySet`), `test_ClaimMintsRealAuMM_SinglePool` (gauge approval, `recordScore`, `auMTContract` recorder slot wired, `recordDeposit`, advance blocks past `year1EndBlock(GENESIS_BLOCK_)` into continuous phase α = 1e18 to avoid F-3 reshape complexity, `claim` → assert `aumm.balanceOf(to) > 0` plus `Claimed` event), `test_ClaimAcrossHalvingBoundary_UsesRealBlockEmissionRate` (advance via `vm.roll(AureumTime.firstHalvingBlock(GENESIS_BLOCK_) + 1)`; claim → real `AuMM.blockEmissionRate` returns halved rate `5e17` wei/block; `_lpTrancheIntegral` accrual interval crosses era boundary per H-D30 cursor walk; assert claim amount reflects halved Era 1 rate). Test harness reuse — same `MockGaugeRegistry`, `MockEMASampler`, `MockCCBMultiplier`, `MockTVLOracle`, `MockEfficiencyOracle`, `MockMiliariumRegistry` mocks as `test/unit/EmissionDistributor.t.sol`; distributor slot typed `EmissionDistributorHarness` per H-D34; setUp mirrors L195—L213 pattern with the only swap being `aumm = IAuMM(address(new AuMM(GENESIS_BLOCK_, address(this))))` instead of `aumm = new MockAuMM()`. Separate file justification: (i) keeps the 100-test MockAuMM-driven cohort isolated from real-AuMM-induced setUp shifts, (ii) Stage I AuMT integration tests live in the same file as a natural extension, (iii) makes the integration cohort separately runnable via `forge test --match-path test/unit/AuMMDistributorIntegration.t.sol`. Out of scope at H6 — H9 fork integration with real Bodensee, full Stage E pilots, end-to-end emission accounting, OQ-7 OOG ceiling stress; H6 is the unit-level real-AuMM compile-and-mint pre-pass, not the fork-level emission flow. Anchors: `src/token/AuMM.sol` (constructor L82, `setMinter` L98, `blockEmissionRate` L127, `minter` slot L45), `src/token/IAuMM.sol`, `MockAuMM` divergence sources at `test/unit/EmissionDistributor.t.sol` L19—L56, H-D7 (OPEN — distributor-as-minter handoff timing locks at H10; H6.3 exercises the handoff direction without locking deployment timing), H-D20 (`IAuMM.mint` at claim time — sole production mint call site), H-D34 (Harness pattern — reused for setUp), C-D11 (AuMM two-flag-lock — `minter != 0` AND `_minterAdmin == 0` invariant), `AureumTime.firstHalvingBlock` + `AureumTime.year1EndBlock` (era and phase wiring), b1 + a2 scoping decisions ratified during H6 pre-flight chat-level discussion.

### H-D37 — H9 fork-test mock swap-out matrix + StageHIntegrationFixture inheritance from StageGIntegrationFixture — status LOCKED

H-D37 amended at H9.0c — minter-slot wiring deferred to each derived contract's setUp override per Option A resolution; original H9.0a framing `aumm.setMinter(address(emissionDistributor))` in fixture setUp would have conflicted with `StageHBootstrapPhaseTest` needing `bootstrapChannel` as minter per H-D39 full distribute() arc against the shared Bodensee-pool AuMM token. H-D37 specifies the boundary between real production contracts and test-side mocks/stubs for `test/fork/StageHIntegration.t.sol`. Real-contract roster (deployed in `setUp` from `src/` source): Vault scaffold + `AureumVaultFactory` + `AureumProtocolFeeController` + `AureumFeeRoutingHook` (Stage D — reused verbatim from Stage G fixture via `super.setUp()` inheritance); `AuMM` (Stage C); `EMASampler` + `CCBMultiplier` (Stage F); `GaugeRegistry` + `VaultClassRegistry` + `GaugeEligibility` + `SwapAndDepositToBodensee` (Stage G); `TVLOracle` (H2a — replaces Stage G's `MockTVLOracle`); `EfficiencyOracle` (H2b — replaces Stage G's `MockEfficiencyOracle`); `BodenseeBootstrapChannel` (H3); `EmissionDistributor` (H4—H6). Mock retained: `MockMiliariumRegistry` (Stage J forward-dep per F-D9) — consumed by both `CCBMultiplier` (`_miliariumScore` denominator path) and `EmissionDistributor` (`_miliariumRegistry` 6th immutable per H-D31); configured `isMiliarium(pool) = true` for all 3 pilots so H-D33 reshape exercises the Miliarium branch at every pilot. AuMT — direct impersonation per H6.3 unit-test precedent: `setAuMTContract(address(this))` wires `address(this)` (the test contract) as the recorder so `recordDeposit` / `recordWithdrawal` arrive as `msg.sender == address(this)` without a mock token layer; eliminates Stage I AuMT mock surface entirely. Zero-stub per H-D29: `incendiaryRegistry` stays `address(0)` — F-7 Step 1 Incendiary skim collapses to 0, full CCB-weighted distribution exercises through the H9 fork suite. Pilot roster: 3 pilots — ixHelvetia, ixEdelweiss, ixAurebit (Miliarium slots 01/05/14 per Stage E `test/fork/PilotPools.t.sol`). Fixture inheritance: `StageHIntegrationFixture is StageGIntegrationFixture` — direct single-level inheritance reuses Stage G CREATE3 vault salt, Bodensee BPT `vault.initialize` + per-token `transfer` + `settle` (D32 β-pattern), ixPilot deployment loop, `svZCHF` + `sUSDS` env-reads, governance multisig keccak, and `SwapAndDepositToBodensee` hook registration; `setUp` override calls `super.setUp()` then extends with `TVLOracle` + `EfficiencyOracle` + `BodenseeBootstrapChannel` + `EmissionDistributor` deployments + `efficiencyOracle.setEmissionsRecorder(address(emissionDistributor))` per H-D23 deploy prerequisite + `emissionDistributor.setAuMTContract(address(this))` direct impersonation per H6.3 precedent. `aumm.setMinter(...)` is deferred to each derived contract's `setUp` override per the minter-slot conflict resolution: AuMM exposes a single one-shot `setMinter` slot (C-D11 two-flag-lock — `_minterAdmin` zeroed on first `setMinter` success), but both `BodenseeBootstrapChannel.distribute()` (H-D11 mint-at-distribute) and `EmissionDistributor.claim()` (H-D20 mint-at-claim) require `IAuMM` mint authority over the same Bodensee-shared AuMM token; `StageHBootstrapPhaseTest.setUp()` calls `aumm.setMinter(address(bootstrapChannel))` for the H-D39 full distribute() arc, while `StageHContinuousPhaseTest` / `StageHHalvingBoundaryTest` / `StageHCrossStackTest` each call `aumm.setMinter(address(emissionDistributor))` for the claim path; H-D7 governance-migration timing remains OPEN at H10 (the per-test setMinter exercises both directions of the H-D7 design space without locking the production handoff). No G-D25c mock merge — `StageGMocks.sol` remains under `test/fork/mocks/`; `MockMiliariumRegistry` is the only mock carried into Stage H fixture setUp. E10 `deal(adjust=false)` rule applies to any `deal()` call introduced at H9 (ixEDEL is a Reserve DTF; supply-adjust path arithmetic-wraps per E10). Anchors: `test/fork/StageGIntegration.t.sol` (1225-line Stage G fixture + 8 derived contracts — inheritance pattern + CREATE3 salt + BPT init verbatim reuse), `test/fork/PilotPools.t.sol` (3-pilot deployment precedent), H-D7 (OPEN — `setMinter` timing locks at H10; H9 per-derived-test setMinter exercises both directions of the H-D7 design space), C-D11 (AuMM two-flag-lock — `_minterAdmin` zeroed on first `setMinter` success; underlies the per-test setMinter resolution), H-D11 (BodenseeBootstrapChannel mint-at-distribute — bootstrapChannel needs IAuMM mint authority for H-D39 full arc), H-D20 (EmissionDistributor mint-at-claim — emissionDistributor needs IAuMM mint authority for claim flow), H-D23 (EfficiencyOracle.setEmissionsRecorder deploy prerequisite — wired in fixture setUp), H-D29 (`incendiaryRegistry = address(0)` zero-stub), H-D31 (`_miliariumRegistry` 6th immutable + `MockMiliariumRegistry` fork carry-forward), H-D33 (effective score reshape — Miliarium branch exercises at fork level), D32 (β-pattern `vault.initialize` + `vault.settle` + DONATION `addLiquidity`; no Router, no Permit2), D35 + D36 + H-D40 (invocation belt), E10 (`deal` DTF rule).

### H-D38 — H9 test cohort breakdown: four derived contracts per emission-schedule phase — status LOCKED

H-D38 specifies the four-contract cohort anchored to `StageHIntegrationFixture`. (1) `StageHBootstrapPhaseTest` — F-0 + F-1 phase validation: `accrue()` cadence across Bootstrap A (80→50% AP-decay) and Bootstrap B (50→0%) legs per H-D11 closed-form identity; `distribute()` against real `IVault.unlock` callback (H-D39 full arc); `pendingAccrual + totalDistributed = Σ_accrued` invariant; `test_RevertWhen_DistributeCalledByNonGovernance` + `test_RevertWhen_DistributeWithNoPendingAccrual` sad-path pair (H-D39). (2) `StageHContinuousPhaseTest` — F-7 phase (bypass F-3 transition complexity: `vm.roll` past `AureumTime.year1EndBlock(GENESIS_BLOCK_)` from setUp so α = 1e18): `recordScore` for 3 pilots → `setAuMTContract(address(this))` → `recordDeposit` → `vm.roll` N blocks → `claim` for each pilot → assert `aumm.balanceOf(to) > 0` + `Claimed` event + conservation: `|Σ_i pendingClaim_i − lpTrancheEmission(span)| ≤ N_gauged wei` (bounded not strict equality — each `_settlePool` flooring drops ≤ 1 wei per pool; `N_gauged = 3` here; assumes `incendiaryRegistry = address(0)` zero-stub per H-D29 so no skim reduces the total). (3) `StageHHalvingBoundaryTest` — H-D30 era-cursor walk against real Vault state: `vm.roll` straddle of `AureumTime.nthHalvingBlock(GENESIS_BLOCK_, 1)` with a 20-block window (n_pre + n_post = 20); `recordScore` + `recordDeposit` → accrual interval crosses halving boundary → `claim` → assert `aumm.blockEmissionRate(AureumTime.nthHalvingBlock(GENESIS_BLOCK_, 1)) == 5e17` AND claim amount equals cursor-walked `1e18 × n_pre + 5e17 × n_post` per H6.3c arithmetic; validates H-D30 against real Balancer V3 Vault reserve state (not `MockVault`). (4) `StageHCrossStackTest` — end-to-end cross-stack wiring: gauge approval via `VaultClassRegistry` → real `EMASampler` TVL EMA push via `SwapAndDepositToBodensee` swap → `TVLOracle.tvl(pool)` reflecting EMA → `EfficiencyOracle.efficiencyInputs(pool)` → `recordScore` F-3/F-7 reshape per H-D33 → `recordDeposit` → `claim` → assert `aumm.balanceOf(to) > 0`; validates entire H1—H6 production stack wired together under real Balancer V3 Vault lock semantics. `EmissionDistributor` is used directly in fork suite (not via `EmissionDistributorHarness` — Harness is test-unit only per H-D34). Estimated cohort: 12—20 tests across 4 contracts + fixture; 800—1200 lines. Anchors: H-D39 (full `distribute()` arc + sad-path pair), H-D30 (era-cursor walk — `nthHalvingBlock(g, 1)` straddle arithmetic), H6.3c (halving straddle unit precedent: 9 × 1e18 + 11 × 5e17 = 14.5e18), H-D33 (effective score reshape — F-3 transition vs F-7 full CCB; `StageHContinuousPhaseTest` bypasses F-3 by seeding post-year1), H-D11 (bootstrap AP closed-form — Bootstrap B identity test), H-D34 (Harness is test-unit only — fork suite uses `EmissionDistributor` directly).

### H-D39 — BodenseeBootstrapChannel.distribute() full IVault.unlock callback exercise against mainnet Balancer V3 Vault at 0xbA1333... — status LOCKED

H-D39 specifies the `StageHBootstrapPhaseTest.test_Distribute_*` arc as the primary H9 validation differentiator from the H3 unit cohort. Test arc within `StageHBootstrapPhaseTest`: (a) `vm.roll` to bootstrap window (within first `BLOCKS_PER_ERA` blocks post-genesis); (b) `accrue()` populates `pendingAccrual`; (c) snapshot Bodensee AuMM reserve via `vault.getPoolTokenInfo(bodenseePool)` for the AuMM token index; (d) `vm.prank(GOVERNANCE_MULTISIG); distribute()` fires `IVault.unlock` → `_distributeCallback` → `IERC20(aumm).safeTransfer(address(vault), pendingAmount)` → `vault.settle(aumm)` → `vault.addLiquidity(..., AddLiquidityKind.DONATION, ...)`. Post-distribute assertions: Bodensee AuMM reserve via `vault.getPoolTokenInfo` increased by exactly `pendingAmount`; BPT total supply unchanged (DONATION semantic per D32 β-pattern — DONATION injects tokens into pool reserves without minting BPT); `Distributed(GOVERNANCE_MULTISIG, pendingAmount)` event emitted; `pendingAccrual == 0` post-call; `totalDistributed` incremented by `pendingAmount`; transient `_distributeCallback` entry lock cleared. Sad-path pair: `test_RevertWhen_DistributeCalledByNonGovernance` (`vm.prank(attacker); vm.expectRevert(NotGovernance.selector); distribute()` — access-control gate); `test_RevertWhen_DistributeWithNoPendingAccrual` (fresh fork at genesis block, pre-`accrue()` → `vm.expectRevert(NoPendingAccrual.selector); distribute()` — zero-accrual short-circuit). Validation gap closed: H3.7e unit cohort used `MockVault` for the `_distributeCallback` path; H9 validates the identical path against real Balancer V3 Vault bytecode — byte-identical to mainnet deployment at `0xbA1333333333a1BA1108E8412f11850A5C319bA9`, real reentrancy guards, real transient storage, real settle accounting, real AuMM reserve tracking. `abi.decode(result, (uint256))` on the unlock callback return per D22 (inner callback `returns (uint256)`, not `returns (bytes memory)` with `abi.encode`). Anchors: `src/emission/BodenseeBootstrapChannel.sol` (`distribute()` + `_distributeCallback` — H3 implementation), H-D11 (bootstrap AP closed-form), H3.7e (unit `RevertWhen_Reentrant` via `NoPendingAccrual` bubble-up; direct `vm.transientStore` slot-seeding deferred per §11), D22 (`abi.decode(result, (uint256))` — inner callback return type), D32 (β-pattern `vault.unlock` + `vault.settle` + DONATION `addLiquidity`; no Router, no Permit2), CLAUDE.md §2 (`0xbA1333333333a1BA1108E8412f11850A5C319bA9` — mainnet Balancer V3 Vault, byte-identical to audited source), H-D40 (invocation belt).

### H-D40 — H9 fork-test invocation: D35 split-form + D36 --threads 1 belt + Ankr RPC carry-forward + block-pin posture — status LOCKED

H-D40 locks the canonical forge invocation command and operational posture for `test/fork/StageHIntegration.t.sol`. Canonical command: `forge test --match-path "test/fork/StageHIntegration.t.sol" --fork-url $MAINNET_RPC_URL --threads 1 -vv`. `--match-path` per D35 restricts fork context to the H9 file only — bypasses the other six fork files (`CCBEngine.t.sol`, `PilotPools.t.sol`, `StageGIntegration.t.sol`, `AureumFeeRoutingHook.t.sol`, `SwapAndDepositToBodensee.t.sol`, `DeployAureumVault.t.sol`) that each carry their own Ankr rate-limit exposure; avoids the D35 global-fork hang pattern (indefinite hang at 25+ min on Ankr free-tier when `--fork-url` applied globally). `--threads 1` per D36 + F-D11 prevents `vm.setEnv` races; H9 introduces no new `vm.setEnv` race surfaces — all Stage H deployments (`TVLOracle`, `EfficiencyOracle`, `BodenseeBootstrapChannel`, `EmissionDistributor`) happen in-process via `new` constructors in `setUp()`, not via env-address handoffs between parallel contracts. H8 unit-cohort split-form Part A (`forge test --no-match-path "test/fork/**"` → 612/612 green) is the symmetric counterpart; H9 is Part B. Block-pin posture: enter H9 without `--fork-block-number` (Ankr serves head; live Bodensee reserve + EMA state); if Ankr state-drift flake surfaces across re-runs (e.g. real Bodensee `_currentReserve` snapshot diverges between CI + local runs), lock to a specific block via `vm.envOr("FORK_BLOCK_NUMBER", uint256(0))` env-read in `StageHIntegrationFixture.setUp()` — same pattern as Stage E `PilotPools.t.sol` + Stage G `StageGIntegration.t.sol` — and document the pin block in a later H9.x sub-step rather than at H9.0a entry. Permanent D36 fix (env-key namespacing in `test/fork/DeployAureumVault.t.sol` — `FEE_ROUTING_HOOK_DEPLOY_TEST` vs shared `FEE_ROUTING_HOOK` key) remains deferred per §11 carry-forward. Anchors: D35 (`--match-path` split-form, Ankr global-fork rate-limit), D36 (`--threads 1` race fix, `vm.setEnv` pattern), F-D11 (Foundry parallelism + `vm.setEnv` race risk), `test/fork/PilotPools.t.sol` + `test/fork/StageGIntegration.t.sol` (block-pin posture precedents), CLAUDE.md §11 D35/D36 housekeeping notes.

---

## Architecture

Stage H concentrates new Solidity under `src/emission/`—the emission distributor itself, concrete `TVLOracle` at H2a, concrete `EfficiencyOracle` at H2b, the separate BodenseeBootstrapChannel per H-D2, and the Stage L forward-dependency stub `src/incendiary/IIncendiaryRegistry.sol` slated for H7. AuMM enforces era halving internally through `IAuMM.blockEmissionRate(blockNumber)` returning `GENESIS_RATE >> eraIndex`, which means Stage H consumes (does not reimplement) the halving schedule—and every halving-aware call site in Stage H collapses into a single `IAuMM.blockEmissionRate(block.number)` read. Routine orchestration traverses four schedule phases tied to formulae references: F-0 piecewise bootstrap across Months 0—6 followed by Months 6—10, F-1 equal splitting across twenty-eight nominal Miliarium lanes through Month 10, F-3 linear blending from equal weights toward CCB-weighted shares spanning Months 11—12, and F-7’s full tournament allocations once Year 2 begins. Readers reach backward into Stage F and Stage G solely through immutable, read-only accessors—TVL EMA probes, multiplier reads, roster membership predicates, gauge approval—with mutating workload isolated to accumulator slots inside Stage H contracts plus mediated `IAuMM.mint` calls once deployments satisfy the eventual H-D7 `setMinter` timing gate. Matching H-D5, every guarded entry exposes an explicit `address pool` parameter and predicates on `IGaugeRegistry.isGaugeApproved(pool)`, so enumeration across the upstream registry ABI never enters the distributor design.

### New contracts and interfaces

Files Stage H creates or retrofits. Sub-step landing targets are indicative — exact ordering may shift during execution but the inventory is locked.

| File | Role | Sub-step |
| --- | --- | --- |
| `src/emission/IEmissionDistributor.sol` | Stage H distributor interface — claim entry points for AuMT holders + recordDeposit/recordWithdrawal entry points for the Stage I AuMT contract + accumulator getters | H4 |
| `src/emission/EmissionDistributor.sol` | Pull / lazy-accrual distributor (H-D4) with MasterChef-style `accRewardPerScoreUnit`; pool-scoped state with incremental `totalScore` aggregate (H-D5); epoch-cadence F-7 share weights (H-D3); F-3 blend factor α evolution; F-7 Incendiary-skim integration | H4 + H5 |
| `src/emission/IBodenseeBootstrapChannel.sol` | F-0 bootstrap rail interface — accrual + deposit-and-distribute to der Bodensee, governance one-shot wiring | H3 |
| `src/emission/BodenseeBootstrapChannel.sol` | F-0 piecewise routing (Months 0—6 then 6—10) + one-sided AuMM deposit to der Bodensee via the Vault unlock pattern; audit-isolated per H-D2 mirroring G-D11 / G-D21 precedent | H3 |
| `src/emission/TVLOracle.sol` | Concrete `ITVLOracle` per FINDINGS OQ-22 — svZCHF-denominated TVL via RP-aware unwrap plus constellation-spot averaging | H2a |
| `src/emission/EfficiencyOracle.sol` | Concrete `IEfficiencyOracle` per G-D23 (i) — 3-epoch SMA of `(swap_fee_revenue + yield_fee_revenue) / emissions_received`; consumed by Stage G `GaugeEligibility.computeEpochSnapshot`, not by the H distributor directly | H2b |
| `src/ccb/ICCBMultiplier.sol` | Stage F retrofit interface — Stage F shipped CCBMultiplier without an interface; Stage H needs typed cross-contract access for `getMultiplier(pool)` reads. Robustness backport (RB-005) candidate | H1.x-bis or pre-H micro-stage |
| `src/incendiary/IIncendiaryRegistry.sol` | Stage L forward-dep stub — read-only shape for per-block Incendiary boost claims surface; pre-Stage-L stub returns zero claims | H7 |

### Consumer surface (read-only)

Stage H contracts consume the following from earlier stages — no writes, no state mutation upstream. Forward-dep consumers are listed separately at the end.

- `IAuMM.blockEmissionRate(blockNumber)` (Stage C): per-block AuMM mint budget; internal era halving already applied via `GENESIS_RATE >> eraIndex`. Stage H queries this view — never reimplements halving math.
- `IAuMM.mint(to, amount)` (Stage C): gated by `msg.sender == minter`; Stage H deploys with the `setMinter` one-shot handoff timing locked at H-D7.
- `AureumTime` boundary helpers (Stage C, `src/lib/AureumTime.sol`): `month6EndBlock`, `month10EndBlock`, `month13StartBlock`, `year1EndBlock`, `firstHalvingBlock`, `nthHalvingBlock`, `monthIndex`, `epochIndex`, `eraIndex` — all in place; Stage H phase-boundary checks reduce to library calls against the genesis-block immutable.
- `IMiliariumRegistry.isMiliarium(pool)` plus pool enumeration (Stage F per F-D9; concrete implementation lives at Stage J): identifies the F-1 / F-3 equal-split slots. Pre-Stage-J the placeholder returns `false` for every pool, so F-1 / F-3 mints exclude all pools until Stage J's registry is wired in.
- `IGaugeRegistry.isGaugeApproved(pool)` (Stage G per G-D16a, retained ABI from pre-G-D16a callers): per-call gate on every distributor entry point per H-D5; no enumeration consumed — pool-scoped is the design.
- `IEMASampler.tvlEMA(pool)` (Stage F per F-D5 plus OQ-5a-bis): per-day-sampled svZCHF-denominated TVL EMA with α = 2/61; `TVL_EMA60` input to F-7 Step 3 score.
- `CCBMultiplier.getMultiplier(pool)` (Stage F per OQ-23): per-pool multiplier `CCB_mult` input to F-7 Step 3 score; the 90-day boost gate (F-D17) is deprecated at G-D13, so Stage H consumes the returned value as a plain multiplier without special-casing the boost window.
- `CCBShare.shares(uint256[])` (Stage F per F-D10): F-6 share normalization library — converts per-pool scores into a FixedPoint share vector summing to approximately `FixedPoint.ONE` (modulo `divDown` floor rounding); Stage H feeds Step 3 scores into this lib to obtain Step 4 shares.

`IIncendiaryRegistry.activeBoostClaims(pool, block)` (forward-dep stub at H7): drives F-7 Step 1 Incendiary skim — Stage H ships a zero-claim stub interface at H7 to keep distributor compile-targets stable; Stage L lands the concrete registry that returns nonzero claim amounts per active boost.

### Schedules by phase

Per-block routing rules, organised by phase. Block ranges are inclusive of the lower bound; upper-bound semantics follow `AureumTime` boundary-helper conventions (`monthNEndBlock` returns the last block of month N — see `src/lib/AureumTime.sol`). Halving (era) cadence is orthogonal to phase and applies multiplicatively at every era boundary.

| Phase | Block range | Routing rule | Inputs consumed |
| --- | --- | --- | --- |
| Bootstrap A (F-0 + F-1) | [genesisBlock, month6EndBlock] — Months 0—6 | F-0 piecewise: `bodensee_share(block) = 0.80 − 0.30 × t1` where `t1 = (block − genesisBlock) / (month6EndBlock − genesisBlock)` (linear 80% → 50% as `t1` runs 0 → 1); `lp_share = 1 − bodensee_share`; F-1 equal-split: `emission_to_pool_i(block) = lp_share × block_emission × (1/28)`. Pre-N=28 the `(28 − N)/28` LP-tranche fraction is unminted per H-D6 lock | `IAuMM.blockEmissionRate`, `IMiliariumRegistry` enumeration, `AureumTime.month6EndBlock` |
| Bootstrap B (F-0 + F-1) | (month6EndBlock, month10EndBlock] — Months 6—10 | F-0 piecewise: `bodensee_share(block) = 0.50 − 0.50 × t2` where `t2 = (block − month6EndBlock) / (month10EndBlock − month6EndBlock)` (linear 50% → 0% as `t2` runs 0 → 1); F-1 equal-split unchanged | `IAuMM.blockEmissionRate`, `IMiliariumRegistry` enumeration, `AureumTime.month6EndBlock`, `AureumTime.month10EndBlock` |
| Transition (F-3) | (month10EndBlock, year1EndBlock] — Months 11—12 | F-0: `bodensee_share = 0` permanently; F-3 blend: `share_i(block) = (1 − α(block)) × (1/28) + α(block) × CCB_share_i(block)`; α(block) linear, 0 at the first block of Month 11 and 1 at `year1EndBlock` | `IAuMM.blockEmissionRate`, `IMiliariumRegistry`, `IEMASampler.tvlEMA`, `CCBMultiplier.getMultiplier`, `CCBShare.shares`, `AureumTime.month10EndBlock`, `AureumTime.year1EndBlock` |
| Tournament (F-7) | (year1EndBlock, ∞) — Year 2 onward | F-7 full sequence: EMA update (per-day per Stage F OQ-5a-bis) → Incendiary skim first (`Remaining = block_emission − Incendiary_total`) → CCB scoring `Score(pool_i) = TVL_EMA60(pool_i) × CCB_mult(pool_i)` → share computation `CCB_share(pool_i) = Remaining × Score(pool_i) / Σ Score(eligible)` via `CCBShare.shares` → `Total = CCB_share + Incendiary_claim`. F-7 share weights refresh per `BLOCKS_PER_EPOCH` (H-D3 deviation from spec literal `every block` — locked) | All Stage F consumer interfaces above + `IGaugeRegistry.isGaugeApproved` for eligibility + `IIncendiaryRegistry.activeBoostClaims` once Stage L is live |
| Halving (orthogonal — every era) | Every `BLOCKS_PER_ERA = 10_512_000` blocks since genesis (firstHalvingBlock, secondHalvingBlock, …) | `IAuMM.blockEmissionRate(block) = GENESIS_RATE >> eraIndex`; halves the per-block budget independent of phase. Era 0 rate = `1e18` (1 AuMM/block); Era 1 rate = `0.5e18`; Era N rate = `1e18 >> N`. Stage H consumes `blockEmissionRate` per call and never special-cases the era boundary — the halving is multiplicative on every routing rule above | `IAuMM.blockEmissionRate`, `AureumTime.eraIndex`, `AureumTime.nthHalvingBlock` |

---

## Interface boundary

Stage H sorts interface obligations into three explicit bins—Produced (Stage H authors), Consumed (frozen by prior stages), and Forward-dependency stubs that hold shape until sibling stages compile against them—which together document the full handshake before any H2 Solidity lands. Closing that boundary deliberately before H2 work begins prevents speculative ABI churn that would otherwise ripple through mocks, scripts, and unit harnesses rewritten every week. Whenever post-H1 work uncovers contradictory reality versus this freeze, the CLAUDE.md §12 ambiguity-gate fires first—meaning a fresh H-D* log entry plus deliberate NOTES revision instead of tacit drift across typed domains. Stage G G-DP4’s forward-dependency convention is the precedent: unreachable governance slots stayed explicit placeholders until successors landed, and Stage H repeats that posture for stubs it does not own yet.

### Produced — frozen at H1 close

These interfaces are authoritative once H1 concludes; widening or renaming them afterward requires documenting a fresh H-D* lock and clearing the §12 ambiguity-gate before implementation edits proceed.

| Interface | File | Locked surface | Sub-step |
| --- | --- | --- | --- |
| IEmissionDistributor | `src/emission/IEmissionDistributor.sol` | Per-pool claim entry point for AuMT holders; `recordDeposit` and `recordWithdrawal` entry points callable only by the Stage I AuMT contract per H-D4 pull semantics; views exposing `accRewardPerScoreUnit`, `totalScore`, `pendingClaim`; events for accrual, claim, and score updates | H4 |
| IBodenseeBootstrapChannel | `src/emission/IBodenseeBootstrapChannel.sol` | Permissionless `accrue()` tick for per-block bootstrap mint; governance-callable `distribute()` to der Bodensee via Vault unlock; views for `pendingAccrual`, `totalDistributed`, `lastAccrualBlock`; events for accrual and distribution | H3 |
| ICCBMultiplier (RB-005 retrofit) | `src/ccb/ICCBMultiplier.sol` | `getMultiplier(address pool)` view returning 1e18 / 1.2e18 / `M_i` per OQ-23; interface-only retrofit of the existing Stage F `CCBMultiplier` contract, no contract changes | H1.x-bis or pre-H micro-stage |

### Consumed — frozen by prior stages

Stage H assumes every upstream ABI in this roster remains fixed for its duration—if a predecessor ever widened selectors mid-stream, Stage H would replicate the G16-style inheritor sweep auditors already paid once, so this table is treated as immutable for the life of the stage.

| Interface | File | Frozen at | Surface used |
| --- | --- | --- | --- |
| IAuMM | `src/token/IAuMM.sol` | Stage C `stage-c-complete` | `blockEmissionRate(uint256)`, `mint(address, uint256)`, `setMinter(address)` |
| IGaugeRegistry | `src/ccb/IGaugeRegistry.sol` | Stage G G-D16a (original ABI retained for pre-G-D16a callers) | `isGaugeApproved(address)` — per-call gate per H-D5, no enumeration consumed |
| ITVLOracle | `src/ccb/ITVLOracle.sol` | Stage F F0.2 per F-D11 | `tvl(address)` — svZCHF, 18-decimal fixed-point |
| IEfficiencyOracle | `src/gauge/IEfficiencyOracle.sol` | Stage G G-D23 (ii) | `efficiencyInputs(address)` — consumed by `GaugeEligibility.computeEpochSnapshot`, not by the Stage H distributor directly |
| IMiliariumRegistry | (concrete implementation lands Stage J; pre-Stage-J stub per F-D9) | Stage F F-D9 | `isMiliarium(address)` plus Miliarium pool enumeration |
| IEMASampler | `src/ccb/EMASampler.sol` | Stage F F-D5 plus OQ-5a-bis | `tvlEMA(address)` — per-day-sampled, α = 2/61 |
| CCBShare | `src/ccb/CCBShare.sol` | Stage F F-D10 | `shares(uint256[])` — F-6 share-normalisation library |

### Forward-dependency stubs

Stage G G-DP4 already proved that shipping compile-stable placeholders beats hiding missing dependencies; Stage H copies that recipe so distributor bytecode and tests remain buildable while Incendiary and AuMT contracts stay future work. Stubs therefore return zero values or static empty shapes until their owning stages replace them with real logic.

- `src/incendiary/IIncendiaryRegistry.sol` (H7) — exposes the `activeBoostClaims(address pool, uint256 blockNumber)` view shape; pre-Stage-L stub returns 0 so F-7 Step 1 Incendiary skim collapses to `Remaining = block_emission − 0` and the full CCB-weighted distribution path remains exercisable in Stage H unit and fork tests.
- `src/emission/IEmissionDistributor.sol` is itself a Stage I forward-dep producer — Stage H authors the consumer-facing surface (claim + recordDeposit + recordWithdrawal) that the concrete AuMT contract will plug into when Stage I lands; Stage H freezes that ABI at H1 close to give Stage I a known target. Same direction-of-boundary convention as IAuMT was for Stage G pre-Stage-I.

---

## Findings queue

Entries labeled H10+ accumulate once Stage H exits pure documentation scaffolding and Solidity surfaces stabilize; numbering stays decoupled from H-D planners per the conventions above until incidents surface.

(empty)

---

## Open questions

Documented ambiguity here disappears only after translation into FINDINGS.md rows whenever resolution carries protocol-wide implication; until then NOTES remain provisional scratch space.

- H-D3 F-7 epoch-discretisation — H-D3 documents the design choice; spec-side every-block literal may warrant a new OQ-N row in FINDINGS.md if material to any consumer surface; defer promotion until H4 confirms test exposure.
