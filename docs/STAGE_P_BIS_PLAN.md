# Stage P-bis — Plan & Sub-Step Roadmap

> **Status:** Stage P-bis OPEN — entry freeze committed (`f84fa36` PB1.1 + `986a223` PB1.1a base amendment; PB-D1—PB-D5 locked, PB-D1/PB-D2 + the PB1.1a base user-adjudicated); CLAUDE.md §11 repointed (`db84611`); PB1.2 (this plan) in flight. Three sequenced workstreams per PB-D4: **(A)** open-issue close-out → **(B)** Sepolia deployment → **(C)** `aumm-app` frontend. Companion to STAGE_P_BIS_NOTES.md (the design-decision log); this file is the sub-step sequence + the Completion Log.
>
> **Last update:** 2026-07-11 — PB1 entry sequence complete through the §11 repoint; PB1.2 lands with this file. Branch `stage-p-bis` (cut from `main` @ f72b309 = the CM-series tip, per PB-D3-as-amended). Regression baseline carried from Stage P: 877 unit + 136 fork green. Next: PB2.1 (OQ-20/OQ-21 adjudication).
>
> **Mode:** Opus high entry (§13 stage table) — adjudications, wiring design, deploy-script work, and every non-trivial save audit are Opus beats; drops to Sonnet for read-back verdicts, commit drafts, Completion-Log housekeeping, and deploy-script scaffolding transcription.
>
> **Audience:** Sagix + any future Claude session resuming Stage P-bis. NOTES holds rationale; STAGES_OVERVIEW § "Stage P-bis" holds the canonical workstream roster; this file is the execution spine.

---
## Stage scope

Stage P-bis exists so the Stage-Q external audit receives a frozen, self-audited candidate running live on a public testnet with the `aumm-app` frontend connected — the audit becomes a verification event, not a discovery event (NOTES Framing). Everything pre-audit lands here: the live CLAUDE.md §11 carry-forwards, the ex-Stage-Q pulled-forward items (the P-D9 hevm/Act bundle, the F-13/F-15 dispositions), the Sepolia go-live (PB-D1), and the frontend gate. Workstream-A exit rule: every roster item exits **Fixed, Built, or Explicitly-descoped-with-rationale** — each descope its own PB-D entry, nothing silently carried past the tag (PB-D4). Adjudications precede builds (PB-D5, the §12 ambiguity-gate): PB2.1—PB2.5 lock PB-D entries first; build sub-steps materialize from those outcomes as lettered insertions (PB2.1b, PB2.3b, …).

### Surfaces produced at Stage P-bis
| Artifact | Workstream | Sub-step | Purpose |
| --- | --- | --- | --- |
| PB-D adjudication locks in `STAGE_P_BIS_NOTES.md` | A | PB2.1—PB2.5 | build-vs-descope dispositions (OQ-20/21, OQ-22, pools, F-13, F-15) |
| adjudication-dependent code/test surfaces | A | PB2.*b | drafted per outcome — not pre-committed here |
| governance-handoff rotation script + fork witness | A | PB2.6 | the PB-D2 wiring step (re-settable slice rotations; P-D36-style witness leg) |
| PB-D13 descope lock + RB-006 register row (no code surface) | A | PB2.7 | D36 permanent fix DESCOPED per PB-D13; `--threads 1` promoted to permanent canonical posture |
| direct `ReentrancyGuard.selector` test (roll-plus-accrue mock reentry per PB-D14) | A | PB2.8 | robustness backport (RB-007 created Closed at the landing hash) |
| P-D42 coverage-tooling re-eval attestation (PB-D15 re-affirm) | A | PB2.9 | re-probe reproduces the Yul stack-too-deep on the byte-identical toolchain; execution-evidence waiver re-affirmed |
| WK.18 thin-venue populated-roster fork sim (PB-D16 BUILD) | A | PB2.11 | F-04 surface Fixed (EMA gate); populated-roster positive-control + economics quantifier |
| hevm + Act formal-methods bundle + specs (PB-D17) | A | PB2.12 | P-D9, LAST in A; hevm 0.58.0 local runs + .act specs-as-artifact (proving firm-side); formal/ + RESULTS.md ship in the audit hand-off |
| `CCBMultiplier` all-gauged delta_global rebuild + zero-TVL disposition evidence + CCB formal re-attest (PB-D18) | A | PB2.13 | OQ-23 reopening close-out; the new LAST in A per PB-D18, superseding the PB2.12 row's LAST-in-A framing |
| `test-stubs/` testnet ERC-20s (+ registry) | B | PB3.2 | P8 roster (tier set per the Stage-P Builds list) |
| canonical Router deploy + `setTrustedRouter` binds | B | PB3.3 | P-D26 close-out; seats the F-09 allowlist |
| production `run()` path in `DeployStageP.s.sol` | B | PB3.4 | un-defer `ProductionOrchestrationDeferredToPbis` |
| Sepolia broadcast + explorer verification | B | PB3.5 | the live go-live (user-run, §8b) |
| deployed-addresses artifact | B | PB3.7 | the C-integration contract the frontend consumes |
| F-14 ops + fee-custody process record | B | PB3.10 | ixAetheron off-chain-conversion rails (P-D12) |
| frontend-reads-Sepolia completion evidence | C | PB4.2 | the PB-D4 workstream-C gate record |
| close set (`STAGES_OVERVIEW.md` row, Completion Log, §11, tag) | — | PB5 | PENDING_PBIS two-commit close (PB-D3) |

### Surfaces NOT produced at Stage P-bis
- No mainnet broadcast of any kind (§8c; Stage R).
- No external-audit engagement — Stage Q owns firm selection (OQ-17) and the audit itself.
- No `aumm-app` code in this repo — external repo per OQ-18; this repo tracks the milestone + the artifact (PB-D4).
- No §xxix constitutional amendment — the multisig gap closes via the wiring step (PB-D2).
- No Vault/submodule edits, as always (§8c) — the rotation script and Router work are CALL-layer only.
- Whatever PB2.1—PB2.5 explicitly descope, each recorded as its own PB-D entry.

---
## Decisions

Mirror of `STAGE_P_BIS_NOTES.md` (PB-D1—PB-D5 LOCKED at the entry freeze; this table grows as the PB2 adjudications lock further PB-D entries). The NOTES file holds the rationale; this table is the roadmap index.

| # | Decision (one-line) | Sub-step |
| --- | --- | --- |
| PB-D1 | Sepolia is the live-broadcast target (12s-block parity); verify pass opens workstream B | PB3.1 |
| PB-D2 | §xxix dissolution via the wiring step, no amendment (re-settable rotations + witness; `pauseManager` inert) | PB2.6 |
| PB-D3 | Branch + close mechanics mirror P-D4; base f72b309 (PB1.1a amendment); PENDING_PBIS close | PB5 |
| PB-D4 | Three sequenced workstreams A → B → C; per-item exit Fixed/Built/Descoped; hevm/Act + WK.18 LAST in A | roadmap |
| PB-D5 | Workstream-A adjudication queue runs FIRST, before any build | PB2.1—PB2.5 |
| PB-D6 | OQ-20/OQ-21 BUILD: `routeYieldFeeToHook(pool, token, amount)` + per-pool `BLOCKS_PER_EPOCH` throttle on the controller | PB2.1 |
| PB-D7 | OQ-22 BUILD: 2-hop TVLOracle valuation fallback (governance-append-only hop roster, ZCHF + USDS seeded; direct venues win, no transitive hops) | PB2.2 |
| PB-D8 | Pool restoration SPLIT: ixCasper waEthwstETH restored via Aureum `CompositeRateProvider` (QG 68% → 84%); 04/07 descoped to the Stage-O challenge path; OQ-I10 closed | PB2.3 |
| PB-D9 | F-13 SPLIT: routeYieldFee path Fixed via caller-supplied bounds; onAfterSwap Accepted-risk S5 Low (sandwich-sim evidence); dormant entries threaded + attested | PB2.4 |
| PB-D10 | F-15 ACCEPT: poke-invariant truth ceiling bounds the live veto denominator; both snapshot fixes rejected as regression-bearing | PB2.5 |
| PB-D11 | PB-D2 amendment: `VaultClassRegistry` one-shot-sealed, reroutes to the PB3.4 production bind; PB2.6 narrows to the five-rotation script | PB2.6 |
| PB-D12 | PB-D2 second amendment: rotations pinned to dissolution-time; the v1 freeze witnessed as truth; ossify-vs-extend one consolidated Stage-Q / pre-R input | PB2.6 |
| PB-D13 | PB2.7 DESCOPED: `--threads 1` permanent canonical fork-invocation posture; the D36 branch-(b) parameterization records as RB-006 | PB2.7 |
| PB-D14 | PB2.8 mechanism amendment: `vm.transientStore` absent at pinned forge-std; direct guard witness via roll-plus-accrue mock reentry; channel-only scope | PB2.8 |
| PB-D15 | PB2.9 coverage re-eval: re-probe reproduces the P-D42 Yul stack-too-deep on the byte-identical toolchain (forge `f83bad91` / solc 0.8.26); execution-evidence waiver re-affirmed, re-eval rides to post-audit / newer-toolchain | PB2.9 |
| PB-D16 | PB2.11 WK.18 BUILD: F-04's atomic surface Fixed (60-day EMA gate), so the thin-venue sim is a populated-roster positive-control + sustained-pump economics quantifier, not a bug hunt | PB2.11 |
| PB-D17 | PB2.12 BUILD: hevm 0.58.0 (argotorg, native arm64 + resident z3 4.15.4) runs locally; Act v0.2.0 source-only → .act specs as audit-handoff artifacts, proving attested firm-side; top-level formal/; exit BUILT | PB2.12 |
| PB-D18 | OQ-23 reopened: delta_global re-keys to the all-Active-gauge aggregate (P-D22 shape restored — ctor 2→3-arg + one-shot `setGaugeRegistry` seal); delta_basket withdrawn (damping inherent in F-5/F-6 proportionality, zero spec edits); zero-TVL disposition attached | PB2.13 |
| PB-D19 | GENESIS_BLOCK = broadcast block + 100_800 (one epoch) decouples the emission clock from deploy time; zero code change (one env value — no clamp, cross-contract genesis assertion, no deploy-time accrual); PB3.4 rehearsal runs the future-offset; two optional pre-genesis pins | PB3.4-pre |
| PB-D20 | PB3.2 stub architecture: base-level envOr override resolver in the miliarium base (STUB_ + vm.toString(literal) keys, passthrough default, non-zero-RP fail-fast); Tier D = emitted env-map, no on-chain registry; real ERC4626RateProviders over 18/18 stub 4626s (F-11-inclusive deviation recorded); two templates + named Tier-A instances; permissionless mint | PB3.2 |
| PB-D21 | PB3.2e stub-deploy architecture: `DeployTestnetStubs.s.sol` iterates all 26 config libraries on a mainnet fork — drift-free STUB_ keys derived from the same cfg.* the d1 resolver reads; WITH_RATE → 18-dec stub underlying + StubERC4626 + real RP, STANDARD → real fork decimals, deduped by mainnet address; two RP override paths (STUB_ literals vs the 5 N-D7 named keys); plain CREATE, fork-sample map committed, live map regenerated at PB3.5 | PB3.2 |
| PB-D22 | PB3.3 Router redeploy: permit2 at Balancer's toolbox pin (§8c-approved, interfaces-only — the canonical cross-chain instance is the runtime dependency); `DeployRouter.s.sol` P9-granular with three explicit envAddress reads + the Aureum-branded version constant; the Sepolia hook's one-shot module-aim = the multisig itself (v1 governance has no trusted-router leg; Sepolia instance only — the mainnet one-shot stays the Stage-Q A/B input); the fork witness realizes the P-D26 (3) production-router leg | PB3.3 |
| PB-D23 | PB3.4 production `run()` un-defer: `run()` composes the sub-scripts' env-keyed `run()`s under the real governor (no `GOVERNANCE_MULTISIG` override; dual-path CCB seal — `deploy()` forwards via `f`, `run()` calls the governor directly); F/G/H `run()` normalize off bare `startBroadcast()` to the env-governor convention (zero in-tree callers); the PB-D11 (iii) `VaultClassRegistry` bind materializes post-K in both spines (one fixture assertion, L562); mainnet-fork rehearsal at future genesis with a G10 nesting-probe rung; the PB-D19 (v) pins promoted in | PB3.4 |
| PB-D24 | PB-D23 (i)/(vii) amendment: the composed-`run()` handle-capture mechanism — extend the F/G/H public-storage exposure to J/K/L, assign in `_deploy`, split PB3.4c into c1/c2/c3 | PB3.4c |
| PB-D25 | PB3.4d2 rehearsal harness: standalone mainnet-fork fixture, no `StagePIntegrationFixture` inheritance — the P10 close gate stays byte-identical; real-EOA governor (not `address(orchestrator)`) exercising the `run()`-vs-`deploy()` real-governor distinction; in-process `DeployTestnetStubs` stub replay via one additive `envPairCount`/`envPairAt` getter — drift-free with the e2 map + the PB3.5 broadcast, the roster already covers the der-Bodensee tokens; `GENESIS_BLOCK = block.number + 100_800` per PB-D19; build ladder d2a–d2d | PB3.4d |
| PB-D26 | PB-D20 (i) amendment: the stub override must reorder, not only re-address — an unconditional ascending sort with lockstep weight permutation immediately before `create()`; a production-path defect the rehearsal surfaced, mainnet invariance re-verified via the P10 13/13 close gate | PB3.4d2c |
| PB-D27 | PB3.5 Sepolia go-live: a committed `DECIMALS_` table read fail-fast (no uniform-18, no dual-fork read); a multi-broadcast runbook threading addresses through `.env` rather than `vm.setEnv`; EOA-scoped prediction of the four-deep base-layer address cycle, verified by a Sepolia-fork dry run at the live deployer nonce; the `DeployDerBodensee` stub-RP adaptation plus its first script-driven fork witness; Sepolia keeps the UPSTREAM factory, the real Aureum factory a named Stage-R gate | PB3.5 |
| PB-D28 | Per-step EOA transaction counts defer from rung e to rung h — counts are nonce-invariant and structural, but a written number an operator could act on is exactly the failure mode (iv) exists to prevent, so rung e's runbook carries a structurally complete count table with every cell reading PENDING-h, and h gains the obligation to populate it before any broadcast | PB3.5 |
| PB-D29 | PB-D27 (iv)(2) amendment: the pre-broadcast dry run is two-phase — one upfront full-sequence run is not executable, because simulation never advances the deployer nonce and every step past the vault reads a live predecessor; phase A sources chain-invariant per-step counts (Sepolia simulation where a step runs standalone, mainnet simulation where it needs live predecessors) and computes every projection analytically, phase B re-simulates each step immediately before its own broadcast inside rung i; `forge script` simulation chosen over a fork-test harness, whose CREATE-only nonce advancement would undercount every call-making step | PB3.5h |
| PB-D30 | PB-D29 (v) amendment: the fee-routing hook deploys BEFORE der Bodensee, swapping steps 5 and 6 — the two are independent in both directions (the hook's constructor zero-checks `derBodensee_` then only stores it; `DeployDerBodensee` reads no `FEE_ROUTING_HOOK` key and creates the pool hookless), which reduces phase A to four Sepolia-standalone counts (stubs, vault, weighted-pool factory, AuMM — every constructor chain pure storage, no external call) and retires the mainnet-simulation clause, whose rationale was false for an Aureum-bound factory that exists on no chain until this deployment creates it | PB3.5h |
| PB-D31 | `DeployTestnetStubs` carries no broadcast wrapper — its four CREATEs run in simulation context only, so a live `--broadcast` sends nothing while still emitting a full 67-pair `STUB_` map of codeless addresses; silent through step 4 and fatal at step 5's hook constructor, after step 2 has sealed its immutables. Found by the rung h phase A dry run, proven by contrast (`DeployAureumVault` wrote a dry-run artifact under identical invocation; this script wrote none). Hidden from every prior gate by the same PB-D26 pattern: `StagePRunRehearsal` replays the roster in-process through the `envPairCount` / `envPairAt` getters and never drives the broadcast path. Fix is the `run()` broadcast wrapper, blast radius the rehearsal alone | PB3.5h2 |
| PB-D32 | EIP-170 lock: compiled Vault is 28,304 bytes against the 24,576 limit (mainnet's deployed Vault is 24,538) — two divergences from Balancer's toolchain, hardhat's per-contract `runs: 500` on Vault/VaultExtension (necessary but not sufficient, 24,918 remains at runs 200) and the OpenZeppelin pin (5.0.0 vs our 5.6.1, ~676 bytes unaccounted); decision is to reproduce the audited artifact via a masked byte-oracle against the deployed mainnet runtime rather than merely fit under the limit; mechanism is a project-wide OpenZeppelin 5.0.0 pin plus per-contract `runs = 500`, gated on the i1 oracle match and an advisory review against the real import list (not §2's ERC20-only claim); the Sepolia base layer is dead from nonce 87 — the factory's sealed creationCode hash forces a redeploy cascading through der Bodensee's CREATE3 address | PB3.5i |
| PB-D33 | PB-D32 amendment: EIP-170 cause is `yulDetails.optimizerSteps`, one missing compiler setting worth 782 of 676 needed bytes, not the OpenZeppelin pin (31 bytes, withdrawn) — proven via a Sourcify `exact_match` `stdJsonInput` reproducing mainnet's Vault at 24,538 through solc directly, then via ablation isolating the step sequence as the whole delta; fix is a `foundry.toml` `optimizer_details` block (camelCase keys — snake_case is silently dropped) plus a `vault500` additional compiler profile with per-path `compilation_restrictions` for Vault.sol and VaultExtension.sol; `bytecode_hash` stays `none`, so the committed target is 24,487, not 24,538; blast radius is global (no per-profile `optimizer_details` scoping exists), so PB-D32 (viii)'s in-stage re-verification applies, unmeasured until the i2 artifact-hash diff; also records an unrelated submodule-pin provenance defect, two 2024-12-04 commits missing from our pin, dispositioned separately | PB3.5i1 |
| PB-D34 | PB-D33 amendment: mechanism is a full `[profile.vault]` named profile (not an `additional_compiler_profiles` entry — only full profiles can carry their own `optimizer_details`), pinning solc 0.8.30 since 0.8.26 ICEs on our submodule revision at Balancer's own settings though Balancer's source compiles clean; Vault lands at 24,393, 183 bytes under the 24,576 limit; blast radius measured (not predicted) at 470-artifact baseline: 3 artifacts changed, all downstream of `DeployAureumVault.s.sol`, zero Aureum protocol contracts touched, so PB-D33 (vii)'s in-stage re-verification dissolves; gates 947 unit + 13/13 P10 fork green as regression, not re-validation; user-adjudicated that the 24,393 Vault is explicitly NOT byte parity (24,485 stripped target) and Sepolia needs EIP-170 fit only — the submodule pin bump stays a Stage-R gate with a second motive now on record (restores solc 0.8.26 as well as parity); corrects PB-D33's 928→947 test count and 24,487→24,485 stripped target; a stale 28,304-byte Vault artifact persists under `out/` alongside the deployable one under `out-vault/`; `.env` Sepolia-contamination defect cross-referenced to PB-D35, not absorbed here | PB3.5i3—i4 |
| PB-D35 | PB-D27 (iii) amendment: `.env` populated by PB3.5 phase A (67 `STUB_` pairs + base-layer projections) silently redirected every mainnet fork suite's literals to codeless Sepolia addresses, since PB-D20's resolver activates on key existence alone — undetected for two days because the unit suite binds no mainnet literals and `.env` is gitignored, surfaced only when PB-D34's option-C verification needed the P10 gate; cause is that `.env` was chosen for broadcast-only threading but foundry auto-loads it for `forge test` too; fix is file separation — `.env` stays mainnet-only, the Sepolia overlay moves to an explicitly-sourced `.env.sepolia` — over gating the resolver behind an opt-in flag, rejected because it reopens P10-validated deploy plumbing and its forgotten-flag failure mode is silent-through-step-4-fatal-at-step-5 versus this shape's fail-fast-at-step-2; interim manual restoration (backup at `.env.sepolia-backup`, 197→130 lines) closed P10 to 13/13 but is not the fix; actual split is rung i5c, triggered to land before phase A ever repopulates a Sepolia key; a `STUB_`-resolution fixture guard recorded as a new RB row rather than built | PB3.5i5 |
| PB-D36 | PB-D35 (iv) amendment: the two-file overlay becomes a three-file copy-and-refresh model — `.env.mainnet` and `.env.sepolia` are complete, independently-maintained canonical sets and `.env` is a derived working copy of whichever one is active, so no precedence rule survives and a key omitted from one set can no longer inherit the other chain's value; forced by the seven mainnet-fork fixtures that read `SV_ZCHF` / `SUSDS` raw from the auto-loaded file with no `vm.setEnv` override (only `DeployStageG`, `DeployStageK`, `DeployStageL` and `DerBodenseeScriptWitness` set them), which rules out any explicit-source-only mainnet set short of a source preamble on every fork-test command in the repo; write direction is canonical to `.env` only, `cp` switches posture, and `diff .env .env.mainnet` empty is the honesty check retiring PB-D35 (vii)'s hand-repair drift class; PB-D35 (viii)'s `STUB_`-count pre-run check WITHDRAWN as unsound — it returns zero today while `.env` holds nine stale Sepolia addresses carrying no such prefix — replaced by an `AUMM_ENV_CHAIN` marker failing CLOSED on absence, asserted `mainnet` before a fork run and `sepolia` before a broadcast; the `set -a; source` preamble retires unimplemented, since copy-and-refresh leaves nothing to forget to source; `.env.mainnet` strips fourteen keys (the nine Sepolia deploy outputs plus the five Aureum-deployed rate-provider keys) and the 45-line `DECIMALS_` table, `SV_ZCHF` / `SUSDS` staying at mainnet values for the reason the forcing fact gives; the eight void projections, `FEE_CONTROLLER` and `GENESIS_BLOCK` are removed from `.env.sepolia` rather than carried, which is what actually delivers the step-2 fail-fast PB-D35 (v) claimed and lacked; `DEPLOYER_PRIVATE_KEY` gains an explicit home in both canonical files, since copy-and-refresh destroys a working-copy-only key; ladder i5e1—i5e8, i5e4 gated on a P10 13/13 re-run, trigger inherited unweakened from PB-D35 (viii) | PB3.5i5e |
| PB-D37 | PB-D32 (x) execution: the deployed-bytecode-size gate lands as a named-artifact Solidity test at `test/unit/BytecodeSize.t.sol` riding the unit suite, because neither compiler profile can ever pass an unscoped `forge build --sizes` — `[profile.vault]` carries `VaultMock` at 42,408 bytes (1.73× the 24,576 limit) inside its own `test/` subdirectory, `[profile.default]` carries twenty over-limit artifacts, all `.t.sol` tests or `.s.sol` scripts topping out at 457,403 (18.6×), and forge exposes no per-contract `--sizes` exclusion, so named assertion is forced by measurement rather than chosen on preference; assertions read `vm.getDeployedCode` (deployed code — `DeployAureumVault` L143—L145 correctly reads `vm.getCode`, creation code, which EIP-170 does not bound) with the three vault artifacts named by explicit `out-vault/` path per PB-D34 (x); baseline Vault 24,393 / VaultExtension 20,069 / VaultAdmin 12,405 against 27 concrete Aureum artifacts whose largest is `AureumFeeRoutingHook` at 12,607, making the Vault's 0.74% headroom the only load-bearing number and the Aureum roster a tripwire for the first contract to approach the limit; two points user-adjudicated — the roster is hand-listed with a `vm.readDir` completeness assertion against `src/`'s 45-file count rather than a derived `out/` walk whose test-exclusion logic would itself be load-bearing and fail-open, and the `out-vault/` precondition fails CLOSED naming the rebuild command, matching PB-D36 (iv)'s `AUMM_ENV_CHAIN` posture; PB-D34 (x) refined — `src/vault/AureumVaultFactory.sol` L18—L19 concrete-type imports hold `VaultAdmin`/`VaultExtension` in the default compile graph permanently at 15,304/23,247 against the deployable 12,405/20,069, so those duplicates are continuously REGENERATED rather than stale and `forge clean` never retires them, `Vault.sol` alone being the orphan case; RB-009 (EIP-170 invisible in simulation, plus the dual-`out` hazard) and RB-010 (PB-D35 (ix)'s shared auto-loaded env file, activation on key existence rather than intent) recorded with two new hardening classes, Deployment-limit verification and Environment-scope isolation | PB3.5i6 |
| PB-D38 | PB3.5i8 phase A re-run: resuming onto reused stubs drops `c1` from the section-8 A3/A5/A9 arithmetic — the greenfield branch keeps measured `c1`, the resume branch (stubs at `[0, n0)` already live, step 1 NOT re-broadcast) sets effective `c1 = 0`, so A3 projects the authorizer/fee-controller/vault-factory off `n0` directly, A5 off `n0 + c2` and A9 off `n0 + c2 + c3 + c4`, with A2 becoming confirm-and-skip; `n0 = 90` live, counts re-measured rather than inherited (`c2 = 4`, `c3 = 1`, `c4 = 1`, all agreeing with the record; `c1 = 87` retained as the fresh-deploy step-1 count, not an addend on a resume); the runbook's no-jq count fallback is WITHDRAWN as a double-counter — it also matches every `additionalContracts` entry, returning 8 against the authoritative 4 at step 2, so a step's count is the top-level `transactions` array length and nothing else; the canonical projection set replaces the void one and overlaps it at nonces 91—93 under different role assignments, so no pair may be copied forward; evidence is a three-way pre-use derivation validation (plain CREATE at 87/88/89 against the addresses actually deployed, CREATE3-raw against the recorded old Vault, CREATE3-sender-hashed against the recorded old der Bodensee) plus forge's own step-2 dry-run artifact independently reproducing the CREATE3 proxy and Vault; TRIGGER: the deployer EOA sends nothing between A1 and the step-2 broadcast, or all seven projections void | PB3.5i8 |
| PB-D39 | PB3.5i9 phase 3 blocker: `BODENSEE_SALT` holds salt value 2 and therefore collides with ixAetheron's slot-derived `bytes32(uint256(2))` in the weighted-pool factory's sender-scoped CREATE3 namespace — both are created through that same factory by the same deployer EOA under a bare `vm.startBroadcast()`, and `BasePoolFactory._computeFinalSalt` is `keccak256(abi.encode(msg.sender, block.chainid, salt))`, so `getDeploymentAddress(bytes32(uint256(2)))` called with `--from` the deployer returns der Bodensee live at nonce 97 and `CREATE3.deploy` onto that salt reverts `DeploymentFailed()`; the defect underneath is a category error rather than bad luck — der Bodensee is not a Miliarium Aureum pool, the MA set being the 28 ix-named registry slots (`12_aureum_glossary.md` L64 and L5) against der Bodensee the non-emission-eligible revenue destination (L39), with no third pool category and no pilot or major sub-class, so a non-MA pool was handed a salt inside the 1—28 MA slot space E-D20 established, and both halves sat on disk unread against each other at PB-D38 (v) and `docs/STAGE_P_NOTES.md` L453; the remedy is a second sender for ixAetheron, der Bodensee being immovable with its address sealed as a nonce-91 fee-controller immutable whose correction cascades to a full base-layer restart (deployment record section 4, PB-D27 (iv)), and the salt edit rejected as a mutation to a config library the mainnet deploy reads that would then need un-making, plus a permanent footnote on E-D20's slot-to-salt identity; four constraints — the second key broadcasts `DeployIxAetheron` and nothing else, `MILIARIUM_POOL_02` derives with `--from` that address and never from the canonical deployer whose salt-2 derivation would seed a non-MA pool into the Miliarium registry at `DeployStageP.s.sol` L278, the row records the deviation under §8f with the second sender beside it, and `BODENSEE_SALT` moves outside 1—28 before the first mainnet broadcast as a non-negotiable Stage-R gate on the taxonomy ground rather than to dodge one slot; the rehearsal ran 9/9 green over it because in-process every deployer is its own contract address (`StagePRunRehearsal.t.sol` L141 against L243) while `forge script --broadcast` collapses all of them onto the single signing EOA — the PB-D31 class, in-process identity is not broadcast identity, and the batch derivation caught it only against known live addresses since 26 projections came back 26 distinct; phase-3 record cadence batches the 26 MA rows into one section at phase-3 close, every address being re-derivable from committed config data, with slot 02 the sole per-broadcast exception; TRIGGER: the derivation pass returned der Bodensee's live address in the slot-02 position, one append short of seeding a non-MA pool into the Miliarium registry as a founding pool | PB3.5i9 |
| PB-D40 | PB3.5i9j: the pool env keys carry three prefixes asserting three classes of pool where the protocol has one — the Miliarium Aureum set is a flat 28-slot space (04 and 07 descoped per PB-D8) and der Bodensee is the only other pool and not an MA pool per PB-D39 (ii), the `PILOT_` / `MAJOR_` / `MILIARIUM_` prefixes recording only which stage each pool first landed in, which runbook section 7 already states carries no deploy-time meaning; stating that in prose while the keys themselves keep asserting the taxonomy is the wrong fix, since an operator reads the key in the command they are about to run rather than the paragraph above it; eight keys rename to a uniform `MILIARIUM_POOL_nn` (`PILOT_POOL_01/05/14` and `MAJOR_POOL_03/08/09/10/11`), the target namespace verified free at zero hits so all eight are renames and none a merge into an existing key; atomicity is forced rather than preferred because every consumer reads through `vm.envAddress` with no default, so a half-landed rename aborts on the first unset key instead of degrading — 39 sites across four deploy scripts, 27 across five fork fixtures that set the keys themselves, eight keys in each of `.env.sepolia` and `.env`, nine in the runbook, with nothing built or run between the saves; gate is the fork suites re-run split-form at `--threads 1` per D35 and PB-D13 (P10 at 13/13 plus the StageM, StageI and StageJ file-scoped suites), the unit suite binding none of these keys and serving as control rather than gate; completed-stage docs are NOT renamed — 38 sites across six plan and notes files record what was built under the names then in use, decisively `docs/STAGE_M_PLAN.md` L57 whose checkpoint asserts zero `PILOT_POOL_02/03/07` references and closed M-D3's correction of the synthetic `[2,3,7]` registry genesis seed to the canonical `[1,5,14]`, so the post-rename residual grep is scoped to `script/`, `test/`, the runbook and the two env files and never repo-wide; sequenced before the 24 remaining MA pool broadcasts on hygiene rather than dependency, the pool scripts reading none of these keys and phase 4's orchestrator being the first consumer of one; TRIGGER: the batched broadcast loop drafted for those 24 pools read its addresses out of `PILOT_`- and `MAJOR_`-prefixed keys, putting a taxonomy that does not exist into an operator command for the third time in one session | PB3.5i9j |
| PB-D41 | PB3.8 pool seeding and oracle population: phase 4 leaves the 26 Miliarium pools registered but uninitialized and, independently, unpriced — `AureumWeightedPoolFactory.create` only registers (`script/pools/deploy-miliarium-pool.s.sol` L22-L23 defers liquidity to the fork harness, and `initialize` returns zero hits across `script/`) while the Vault gates swap, add and remove behind `withInitializedPool` at `Vault.sol` L185/L495/L769, and `script/DeployStageF.s.sol` L119 builds the TVLOracle on an empty `tokenToUnderlying` seed that no script ever fills, so `TVLOracle.tvl` L334 skips every token and reports zero TVL however deep a pool is, zeroing CCB score, EMA and emission share; five-part scope — oracle population, initialization of the 26, EMA bootstrap, activity generation, daily keeper — with parts 4 and 5 standing infrastructure running through PB4 rather than deliverables closing at a commit; Leg 2 enumerates Miliarium pools once `miliariumRegistry` is bound so none needs `addConstellationPool`, but `addConstellationPool(derBodensee)` is REQUIRED rather than defensive since `_directRatio` walks only `_underlyingToPools` and the registry and never the immutable `BODENSEE_POOL`, which is write-only across four read sites none of which is a valuation path — the H-D8 NatSpec at `src/emission/TVLOracle.sol` L12 documenting a three-way union against a two-way implementation, corrected comment-only at rung d; mapping convention is WITH_RATE share to underlying stub, STANDARD self-map, AuMM self-map with der Bodensee as venue; ordering is `setTokenUnderlying` then venue and hop wiring then seed then `updateEMA`, load-bearing because a wrong order silently omits venues rather than reverting (`TVLOracle.sol` L198); sizing is a USD 1,000,000 accounting-TVL universe allocated across pools by six-band compression of real-world theme market capitalization, linear proportionality rejected at roughly 1100 to 1 as leaving most pools unswappable, committed as a data table, with within-pool splits taken strictly from `normalizedWeights` per the PB-D26 lockstep lesson; slot 02 `ixAetheron` seeded for swaps and fees but accepted at approximately zero oracle TVL, CCB and emission share, carrying PB-D7's venue wall forward with no invented hop and no script reshape, while slot 06 `ixLibertas` prices at depth 2 through `ixHelvetia` (sUSDS plus svZCHF, itself a Leg 2 venue) so no pool among the 26 depends on der Bodensee; der Bodensee fills by flow rather than LP capital per F-11, takes no share of the budget, and its dust `initialize` defers to PB3.9 behind `GENESIS_BLOCK` because the pool is 40 percent AuMM and AuMM total supply is exactly zero until genesis (`src/token/AuMM.sol` L75 no pre-mine, L112-L113 minter-only, L128 zero below genesis) while both one-sided inflow paths reach `addLiquidity` and so cannot fill an uninitialized pool; TRIGGER: the frontend is the de facto pre-mainnet marketing artifact, and a clean run of every phase then planned would have handed workstream C 26 pools holding nothing, pricing at zero and emitting nothing, the oracle gap invisible from the explorer | PB3.8 |
| PB-D42 | PB3.8e amendment to PB-D41 (iii): the blanket rule mapping every WITH_RATE stub from share to underlying is wrong for exactly one token and wrong silently — `script/DeployStageP.s.sol` L88 and L153 set the `SVZCHF` immutable from `SV_ZCHF`, which is the svZCHF ERC-4626 share rather than any base asset, so mapping that share to a separate ZCHF stub leaves nothing anywhere resolving to `SVZCHF`; two faces of one mechanism follow, `_venueRatio` L315—L320 matching every venue token's resolved underlying against a `quote` that is always `SVZCHF`, so `balQuote` is zero at every venue, every venue returns ineligible and `_directRatio` returns zero for every base, and `_constellationRatio` L234 taking its `1e18` identity only when `underlying == SVZCHF`, so svZCHF never prices as itself either and attempts to price as a foreign underlying through the same dead path — no revert, no event, every `tvl()` returning zero, indistinguishable from the empty-oracle state PB-D41 exists to close; the corrected convention is code-forced rather than chosen, `test/unit/TVLOracle.t.sol` seeding `_mapToken(SVZCHF, SVZCHF)` at L276, L291, L314 and L345, every seeding site in the suite, so svZCHF self-maps while other WITH_RATE wrappers keep share-to-underlying and STANDARD and AuMM keep their self-maps; second finding, the WITH_RATE underlyings are absent from the committed stub map — `test-stubs/DeployTestnetStubs.s.sol` L135—L140 deploys an 18-decimal `StubERC20` underlying and a `StubERC4626` over it but records only the vault under its `STUB_` key, so the underlyings behind 87 WITH_RATE slot occurrences across all 26 configs exist only on chain through `asset()`, and re-running `DeployTestnetStubs` must not be attempted since it CREATEs a fresh roster on every invocation rather than discovering the live one and would orphan the stub addresses the 26 pools already hold; the same derivation feeds `addHopUnderlying` as well as `setTokenUnderlying`, the PB-D7 hop seeds being precisely the `asset()` outputs of the svZCHF and sUSDS shares rather than independent env keys, a wording in PB-D41 (iii) that invites a search for keys that do not exist; rung e therefore adopts derive-commit-then-assert — a read-only `asset()` pass emits the map, the map commits as a reviewable artifact on the PB-D41 (vi) precedent that deployed state stays reproducible from the repo, and rung f re-asserts each entry against a live read before writing so drift between artifact and chain fails closed at wiring time; TRIGGER: drafting rung e required naming what each WITH_RATE stub maps to, which required reading `_venueRatio` rather than the convention, and the convention read alone is self-consistent and wrong — a blanket rule over a typed domain is verified only against every consumer of that domain, and the numeraire is a member of the set the rule quantifies over | PB3.8e |
| PB-D43 | PB3.8e2 hop-roster level rule, correcting PB-D42 (v): a hop entry is an underlying — a value some token RESOLVES TO under `tokenToUnderlying` — never a token address, because every comparison in the path is on resolved underlyings, `_twoHopRatio` calling `_directRatio(hop, SVZCHF)` which reaches `_venueRatio` and accumulates `balBase` only where `tokenToUnderlying[token] == base`, so an entry nothing resolves to accumulates a zero base at every venue, fails the `h2 == 0` gate and is skipped without reverting or emitting; PB-D42 (v) overstated the derivation by naming both the svZCHF and sUSDS `asset()` outputs as hop seeds, when the PB-D42 (iii) self-map short-circuits before the WITH_RATE branch so `asset()` is never called on svZCHF and no ZCHF-side underlying enters the derived map at all, svZCHF itself being present and self-mapped; three bad seeds and one live one — svZCHF is skipped unconditionally at `_twoHopRatio` L287 on every chain since hopping through the numeraire IS the direct path already tried at `_constellationRatio` step 2, raw ZCHF is inert on Sepolia AND mainnet because svZCHF self-maps and no Miliarium config holds raw ZCHF, the sUSDS ERC-4626 share is the live TRAP because the share resolves to its own underlying so nothing resolves to the share and slot 06 `ixLibertas` silently underprices as the genesis roster's only hop consumer, and the sUSDS `asset()` underlying `0xCc72810e4A91D2BDba70B380C9c41327D0E63169` is the single entry rung f seeds; the hop exclusion does NOT ignore svZCHF liquidity, a svZCHF balance in the pool being valued contributing through the `_constellationRatio` L234 identity at `1e18`; permissionless pools are scoped rather than gated — `MiliariumRegistry.SLOT_COUNT` fixed at 28 keeps them out of the Leg 2 enumeration so they become venues only through `addConstellationPool`, while `activateGauge` carries no access modifier beyond its anti-spam fee and `_alphaF3` L444-L448 makes the emission difference purely temporal, zero until month 10, ramping to year 1 and the identical score path thereafter, the oracle requiring only that a genuinely novel underlying be mapped or the pool fails `GaugeEligibility.TVL_FLOOR_SVZCHF` at `TVLFloorNotMet`; all three rosters are governance-curated, append-only and removal-free, so the one-entry hop roster is a genesis-26 property and not a permanent invariant; the level rule is chain-independent and carries to Stage R as RB-012, with the class-admission-versus-oracle-mapping two-authority seam as RB-013; PB-D7 stays intact as the historical disposition, its FINDINGS L1119 "a 2-hop path through ZCHF or sUSDS" phrasing being the origin of both corrected errors; TRIGGER: drafting rung f's `addHopUnderlying` call required naming the seed, and the seed PB-D42 (v) named cannot work | PB3.8e2 |
| PB-D44 | PB3.8e3 artifact form, the half PB-D42 (vi) left open: derive-commit-then-assert fixed the procedure but never the shape of the thing committed, and shape is load-bearing twice over — it decides whether a 120-address transcription is verifiable mechanically rather than by eye, and whether a Sepolia table can be mistaken for a mainnet one; the map commits as the Solidity library `script/config/SepoliaTokenUnderlyings.sol`, a path the e1 script already names in its own header comment at `d763273`, taking the sibling `script/config/GaugeGenesisManifest.sol` skeleton — SPDX identifier, `pragma solidity ^0.8.26`, provenance NatSpec, `library` rather than contract, count constant, `internal pure` accessor over index-aligned arrays — while deliberately refusing its named `address internal constant` layer, because `GaugeGenesisManifest`'s 11 entries are mainnet tokens whose names a reviewer can check against the address independently while these 60 are freshly CREATEd stubs whose only identity is the address itself, so any name would be invented at transcription time and an invented name is precisely the memory oracle the §8e.1 transcription rule bans; the accessor exposes `PAIR_COUNT` at 60 and returns two index-aligned `address[] memory` arrays whose locals are named `t` and `u` against house register, because the audit of 120 hex literals is a `diff` against `test-stubs/pb38e2-derived.log` and every character the library adds to those lines is one more transform the auditor must trust rather than read, leaving leading whitespace as the only difference and pushing meaning into the NatSpec where it costs the diff nothing; the table is Sepolia-bound and therefore an RB-010 shape sitting beside `mainnet-token-decimals.env`, guarded three ways since a file name alone is discipline rather than enforcement — the library name carries the chain, the NatSpec states chain and deriving commit in its first lines, and rung f asserts chain identity before writing anything, PB-D36's `AUMM_ENV_CHAIN` marker already failing CLOSED on absence; regeneration is safe here and unsafe one script over, `DeriveTokenUnderlyings` being read-only and byte-identical across two separate e2 invocations while `DeployTestnetStubs` CREATEs a fresh roster on every invocation and would orphan the stub addresses the 26 pools already hold, a distinction invisible from the two names and so stated plainly as derive freely deploy never, with capture by shell redirect rather than `tee` after a `tee` into a pipe truncated the log on an interrupted run; content closed at 60 pairs, 47 self-mapping and 13 resolving away pairwise-distinct with no duplicate underlying, while rung f's assertion mechanics and the RB-012 membership check on `addHopUnderlying` stay open; TRIGGER: drafting rung e3 required naming the container shape before Cursor could materialize it, and I12 forbids locking a container before its consumers are known — the consumers are rung f and the auditor, the auditor's tool is a diff, and the diff is what chose the field names | PB3.8e3 |
| PB-D45 | PB3.8f wiring-script shape, closing what PB-D44 (vi) deferred plus one hazard no lock had seen: `addHopUnderlying` L188 and `addConstellationPool` L168 each revert `AlreadyAdded` on a duplicate, so a roughly 62-transaction sequence that stalls mid-run cannot be re-invoked and reverts instead of resuming — a live risk on the chain where PB3.5 already produced one partial sequence — while `setTokenUnderlying` is exempt as a plain overwriting setter; chain identity asserts on `block.chainid` equal to 11155111 before any read or send rather than on PB-D36's `AUMM_ENV_CHAIN`, which answers which env file is active rather than which chain is being written to and is answered off-chain where a stale or hand-edited value is indistinguishable from a true one, while `block.chainid` is read from the node actually executing the call, needs no new environment key and fails closed identically inside the rung-h fork rehearsal — pre-flight confirming this establishes a pattern rather than following one, `AUMM_ENV_CHAIN` having zero readers under `script/` and `block.chainid` appearing nowhere in that directory, so the guard is written named for rung g to reuse; execution is two-phase, phase one walking all 60 pairs read-only and accumulating every mismatch rather than reverting on the first, aborting with the total count so an operator sees the whole picture in one run with no state yet changed, phase two writing only on a clean phase one; the per-entry rule is forced by PB-D42's convention rather than chosen, `t != u` marking a WITH_RATE wrapper whose `IERC4626(t).asset()` must equal `u` exactly and `t == u` marking a self-map with nothing to dereference and therefore a structural check, the 47-and-13 split explicitly NOT a proxy for ERC-4626-ness since svZCHF is a genuine share that self-maps by the PB-D42 (iii) exception and a rule reading self-mapped-means-not-a-vault would fail on the numeraire and take the whole run down; both append-only setters are read-before-call against `isHopUnderlying` and `isInGovernanceRoster` and `setTokenUnderlying` skips entries already holding the intended value, making the sequence re-runnable after any interruption and re-sending only what is missing, which on a balance-constrained deployer is the difference between finishing and stalling again; ordering is enforced in code rather than documented, all 60 writes preceding `addConstellationPool(derBodensee)` because L171—L177 appends the pool to `_underlyingToPools` only under tokens already resolving non-zero and skips the rest with no revert and no event exactly as its own L198 and L163 NatSpec warns, and der Bodensee holds AuMM and svZCHF so a roster-first call would index it under nothing and leave a venue that exists on chain but is invisible to `_directRatio`, the same silent-omission class as PB-D42 (iii) and PB-D43 (v); broadcast tranching stays a rung-i operational question, the RB-012 membership guard stays a Stage-R backport rather than being built here, and whether `vaultExplorer.getPoolTokens` behaves on the registered-but-uninitialized der Bodensee is the first thing rung h answers; TRIGGER: drafting rung f required reading the two roster setters rather than trusting the ordering note, and that read surfaced the duplicate-revert making a 62-transaction sequence unresumable | PB3.8f |
| PB-D46 | PB3.8g seeding-script shape, and a blocker that would have survived the rehearsal: every pool initialization in the tree drives `vault.unlock` against a callback on the harness contract itself — `test/fork/StageGIntegration.t.sol` L363, `test/fork/StagePRunRehearsal.t.sol` L464 — which cannot work under `forge script --broadcast`, where `run()` is a simulation emitting EOA transactions and the script contract is never deployed, so the Vault would re-enter an address holding no code and the live run would fail at the first pool with gas already spent on minting and approvals, the PB-D31 shape exactly; initialization therefore routes through `Router.initialize`, which unlocks internally and passes `sender` as `msg.sender` so it broadcasts as an ordinary EOA call, and D32's no-Router-no-Permit2 rule is not reversed but inapplicable, having existed because the mainnet Router is bound to the mainnet Vault while PB3.3 has since deployed an Aureum Router bound to the Aureum Vault; `RouterCommon` L309 pulls through `_permit2.transferFrom` so every token carries the two-step allowance, an ERC-20 approval to canonical Permit2 then a Permit2 allowance to the Router with `uint160` amount and `uint48` expiry, while `minBptAmountOut` is zero and `wethIsEth` false since no leg is native and initialization admits exactly one winner per pool; WITH_RATE legs fund by real `deposit` rather than the permissionless `mint(address,uint256)` at `StubERC4626` L84, because `totalAssets()` L33 reads the vault's own underlying balance so a bare mint is entirely unbacked and `withdraw` L79 performs a real `safeTransfer` and therefore reverts on any unwrap, while valuation would not notice since `previewRedeem` is `pure` at L58 and `getRate()` reads `1e18` either way — which is exactly what makes the shortcut dangerous, invisible to the oracle and visible to a user, against PB-D41's frontend trigger — at a measured rather than assumed cost of roughly 26 extra transactions, about 1.3 million gas or 0.0013 SepETH against a spine near 0.39; amounts derive as allocation times `normalizedWeights` at one dollar per token per PB-D41 (vi) and (vii), which name no price step because there is none, the arithmetic self-consistent since every leg's balance over weight is the same allocation, with token order and weights read from the Vault's registered pool data rather than from the config library per the PB-D26 lockstep lesson and decimals read live per token rather than tabled; the allocation table commits as a slot-keyed library under `script/config/` on the PB-D44 form, carrying the 26 seeded slots 01 through 03, 05, 06 and 08 through 28 with 04 `ixViatica` and 07 `ixCambio` absent per M-D7, and pools resolve through the existing `MILIARIUM_POOL_NN` environment keys built in a loop from those slot numbers rather than through `IMiliariumRegistry`, whose dense `miliariumPoolAt(index)` enumeration carries no slot identity and cannot be joined to a slot-keyed table; the table is authored rather than derived, so its gate is not a diff but the arithmetic identity that the six band totals sum to exactly 1,000,000, asserted in the script; shape is one idempotent script guarded at every step, sufficient balance skipping the mint, sufficient allowance skipping both approvals and `isPoolInitialized` skipping an already-seeded pool, inheriting the PB-D45 (iv) resumability property and composing with forge's own `--resume`; the broadcaster differs from rung f and load-bearingly so, `setTokenUnderlying` being `onlyGovernance` while initialization is permissionless and its caller becomes the pool's first BPT holder, so rung g broadcasts as the deployer EOA rather than the multisig; TRIGGER: sizing rung g required knowing how a pool is actually initialized, and the only pattern present in the tree is one a broadcast cannot execute | PB3.8g |
| PB-D47 | PB3.8h rung-h dry run, `forge script` against live Sepolia without `--broadcast`: 21 of 26 pools seeded in simulation at zero gas — slots 17, 10, 11, 18, 27, 22, 24, 08, 19, 20, 25, 21, 23, 14, 28, 26, 09, 06, 05, 15 and 03 — validating every PB-D46 fix under real state, Router-routed initialization and the Permit2 two-step and backed `deposit` funding and idempotency guards visibly skipping maxed allowances and registered order with lockstep weights and decimals of 6, 8 and 18 including slot 06's seven-token pool, then reverting `FailedCall()` at slot 02 `ixAetheron` with slots 12, 13, 16 and 01 untested behind the halt; the root cause is a NAMESPACE COLLISION rather than a missing contract — `script/pools/configs/02_ixAetheron.s.sol` L45 declares `WEETH_RATE_PROVIDER = WEETH`, correct on mainnet where real weETH implements `getRate()` itself, but `DeployTestnetStubs._processConfig` L117—L129 records `STUB_<token>` as the stub vault at L140 before the provider branch at L122 guards on `stubOf[rp] == address(0)`, finds that slot already occupied by the token's own entry because `rp == token`, and skips its `_record`, so tokens and providers sharing one `STUB_` namespace let a self-providing token shadow itself and `_resolveRateProvider` at `deploy-miliarium-pool.s.sol` L57—L63 resolved the provider to one of the pool's own tokens; terminal for that pool, since `VaultExtension` L217—L221 writes `TokenInfo` once and no rate-provider setter exists in `VaultExtension`, `VaultAdmin`, `Vault` or any vault interface, so `0xD259F35a138383Ac8b545F06C5eBADcA3f6a2890` cannot hold liquidity under any wiring and must be redeployed, while the blast radius is exactly one line across twenty-six configs with weETH present in no other pool; the provider was never missing but unrecorded — `DeployTestnetStubs` L137 builds an `ERC4626RateProvider` for every WITH_RATE stub and discarded weETH's when the script ended, recovered by nonce arithmetic rather than redeployed at `0x4a4EA25a6359852d4e47031Fc1316F3aE13de0a3` with the vault at nonce 13 and the provider at nonce 14, 505 bytes of code, `getRate()` returning exactly `1e18`, and `wrappedToken()` returning the very stub slot 02 registered, that last read being what proves it THE weETH provider rather than merely a working one since every stub is fixed 1:1 and a wrong provider would price identically today and diverge silently later, at zero new contracts; borrowing Balancer's mainnet providers cannot serve a stub chain, their addresses holding no code on Sepolia and their bytecode reading mainnet dependencies sealed in immutables, while mainnet already uses them verbatim from the configs and stub substitution is a Sepolia-only overlay; rate providers are per-token OPTIONAL per `VaultTypes.sol` L199—L200 and `VaultExtension` L223—L233, mandatory here only because the config declares weETH WITH_RATE, and the STANDARD alternative — numerically identical at a 1:1 stub and still clear of the script's bar at 70% against 84% — is REJECTED because it would edit a config file mainnet also reads to work around a testnet defect, the very thing the deployment record praises avoiding on the salt collision; the fixes are provider keys moved to their own namespace, retiring the collision class rather than patching the guard that exposed it, with `DeployTestnetStubs` fixed forward-only and never re-run per PB-D42, plus a `getRate()` probe in `_resolveRateProvider` failing loudly when a resolved provider cannot answer, the generalizable guard that carries to Stage R; the redeploy needs a third funded sender since `BasePoolFactory._computeFinalSalt` hashes `msg.sender` and slot 02's salt is occupied for the second EOA `0xbF7BE08923CB549721CDba6516e0Cc9ae0f68A8e` exactly as it was for the canonical deployer under `BODENSEE_SALT`, leaving E-D20's slot-derived salt unmutated, and the dead pool is labelled abandoned-in-place per section 8f; TRIGGER: the same gap that left the WITH_RATE underlyings unrecorded, which PB-D42 healed through a live `asset()` probe, also swallowed a rate provider that no on-chain call from a token can reveal, making the deployment record's own unversioned-87-addresses open item cost something for the first time | PB3.8h |
| PB-D48 | PB3.8h9: the first live-Sepolia dry run of phase 4 found a production-path defect no test could see — `script/DeployStageP.s.sol` L178 inside `_orchestrateProduction()` read `vm.setEnv("GAUGE_REGISTRY_PLACEHOLDER", vm.toString(address(this)))`, and `forge script` rejects `address(this)` in a script contract because under `--broadcast` the script is ephemeral and never deployed, so its address is meaningless; this is the third instance of the class after PB-D31 and PB-D46, invisible to `StagePRunRehearsal` for the same reason each time — a Foundry test contract is deployed and has a real address, so `address(this)` is valid there and invalid only under `forge script`; the fix is the in-scope `governor` already declared from `GOVERNANCE_MULTISIG`, since `CCBMultiplier`'s constructor only zero-checks the three registry slots and never calls them, and the slot is sealed to the real `GaugeRegistry` once the G stack lands; with the fix the dry run completes at 114 transactions, 16 CREATEs and 36,934,592 gas, settling runbook section 5 row 9 which has read PENDING-h since PB-D28; phases 4, 5 and 6 have never been broadcast at all — `TVL_ORACLE` is absent because the oracle does not yet exist, which is what blocks rung f and rung i, the latter sitting downstream of phase 4 per PB-D41 (xi); the orchestration carries no idempotency of any kind across its 114 transactions, so a mid-run stall cannot be re-invoked without orphaning the first set, with the `--slow` plus `forge script --resume` posture left open | PB3.8h9 |
| PB-D49 | PB3.8h9f: `forge script --resume` does not re-simulate, per its own `--help` text on the pinned 1.6.0-v1.7.0 build — it "does not simulate the script again" and "expects nonces to have remained the same" — which retires the `vm.setEnv` threading hazard PB-D48 left UNVERIFIED, since none of the thirteen production-path `setEnv` writes in `_orchestrateProduction()` are ever re-derived on a resume and the frozen broadcast artifact at `broadcast/DeployStageP.s.sol/11155111/dry-run/run-latest.json` already carries all 114 transactions — 98 CALL and 16 CREATE — each with an explicit recorded nonce, so a CREATE's address stays correct as a pure function of sender and nonce; `--slow` is locked as the other half of the posture rather than an optional flag, since it is what gives a stalled run a clean confirmed-versus-unsent boundary for `--resume` to be specified against, at a cost of roughly 23 minutes across 114 confirmed-sequential transactions; nonce reconciliation before any resume is locked MANDATORY rather than advisory — resume is forbidden until the live deployer nonce read from chain equals the next pending transaction's recorded nonce in the artifact, or until a mismatch has been explicitly reconciled and recorded — because section 8f's no-receipts partial-broadcast artifact, the same shape that made nonces 90 to 92 recoverable only by a manual chain read and produced `script/CreateAureumVault.s.sol` as a bespoke recovery, is precisely what causes that mismatch; the nine-way decomposition alternative through the sub-scripts' own standalone `run()` entries was held open while re-simulation was still a live possibility and is now REJECTED on PB-D13's ground, since resume being replay-based removes the only reason to prefer finer restart granularity over the P10-validated composed path; zero code change, `DeployStageP.s.sol` and every sub-script untouched | PB3.8h9f |
| PB-D50 | PB3.8i2: the live seeded constellation disproves H-D9's exactness lemma — with all 26 pools capitalised and no swap yet executed, `tvl()` sums to 1,271,468 svZCHF against roughly 987,000 accounting, and ixHelvetia closes it without remainder since exactly nine venues hold both sUSDS and svZCHF, `_venueRatio` returns 80/20 equals 4 at slot 01 and 26/26 equals 1 at each of the eight Standard venues, the unweighted `_directRatio` mean is 4/3, and 2,800 times 4/3 plus 11,200 reproduces the on-chain reading to the repeating digit; the code is faithful to its lock, since H-D9 specifies `balanceSvZCHFLeg18 / balanceULeg18` with no pool-type-specific curve math, weighted ratios, consumed, so the defect is the lock's accuracy lemma rather than any drift — a weighted pool at parity satisfies `bal_quote / bal_base = P* times (w_quote / w_base)`, making a bare balance ratio parity-exact if and only if the two weights are equal, which no venue in the constellation satisfies; the bias is therefore a fixed multiplicative constant per venue rather than off-parity degradation, invisible under live trading while remaining exactly the same size, and roster averaging dilutes manipulation but not a bias shared by every venue of the same shape; H-D9's own escape clause, reserved for materially wrong F-7 rankings, has fired pre-mainnet on slots 11 and 17 reading 33.75 percent apart at identical 70,000 dollar allocations; disposition is FIX with H-D9 amended rather than silently patched, since accept would ship a LOCKED decision whose central claim is disproved with reproducible Sepolia evidence; the fix is the minimal weight term and explicitly not StableMath, leaving the token map, cross-venue averaging, two-hop fallback and EMA cadence untouched; it introduces a trust surface the current arithmetic lacks, because normalized weights are pool-self-reported rather than Vault-authoritative and F-12 already records that provenance is what makes them trustworthy, so the factory-gate-versus-equal-weight-fallback question is a typed-domain blocker settled in notes before code; cost is PB-D13-shaped, reopening audited emission source pre-audit and amending a Stage-H LOCKED decision with a re-gate across unit, fork and formal surfaces | PB3.8i2 |
| PB-D51 | PB3.8i2: Balancer normalized weights are shares of pool VALUE, so funding to declared weights requires `balance_i = weight_i * V / P_i`, and PB-D46 (iv)'s allocation-times-weight rule is that formula with every price silently set to one; the lock overturns nothing, because PB-D46 (iv) is correct and correctly scoped — it derives the internal consistency itself, every leg's `balance_i / weight_i` equalling the same allocation so all legs price at parity, and closes with the only coherent choice for stubs carrying no feed, true because the stubs are fabricated 1:1 placeholders where a flat one-dollar fiat is not an approximation of a real price but the only price that exists; the gap is a missing boundary rather than a mistaken derivation, since nothing states the convention is testnet-only and the load-bearing sentence, it names no price step because there is none, is true of a stub chain and false in general while sitting in the lock a Stage-R author would consult as the authority on capitalisation; the failure mode if it crosses unremarked is the PB-D26 class PB-D46 (iv) itself invokes as its reason for reading weights from the Vault, since an 80/20 WETH-and-stable pool funded by token count puts nearly all its value in one leg and registers cleanly while being economically a different pool; Stage R must therefore fund by value against a price source named in advance, with per-leg values asserted to reproduce the declared weights as a post-condition rather than inherited from the allocation table's own sum; `TVLOracle` is disqualified as that source while PB-D50 is open, since funding legs through the same biased estimator yields pools that look correct to the oracle and are wrong in fact, and the circularity of pricing venues through the oracle that prices them wants its own adjudication even after PB-D50 lands; nothing on Sepolia requires remediation and re-seeding would move from one arbitrary fiat to another, so this is a Stage-R gate with no P-bis work item; independence from PB-D50 is explicit, since neither explains the other's Sepolia face and no single disposition may read as closing both | PB3.8i2 |
| PB-D52 | PB3.8i4: the PB-D50 (ix) provenance question resolves as ONE factory-provenance gate ordered BEFORE the weight read, the two candidates that clause offered being not alternatives but covers of different halves — provenance addresses trust, since any contract can implement `getNormalizedWeights()` and lie, which is the F-12 proof-of-concept standing at `test/whitehat/F12_compositionGateProvenance.t.sol` L11-L16, while an equal-weight fallback addresses type safety, since a StableSwap or bespoke venue does not implement the function at all — and the asymmetry is decisive, a fallback never firing against a hostile pool that answers successfully while a correctly ordered gate makes the fallback unreachable; `_venueRatio` therefore calls `IBasePoolFactory(approvedFactory).isPoolFromFactory(v)` first and reads `getNormalizedWeights()` only on a venue that passed, with no `try` / `catch`, no equal-weight default and no second code path to audit; the failure mode is SKIP rather than revert, a rejected venue returning `(0, false)` and dropping out of `_directRatio`'s cross-venue mean exactly as the F-19 / P-D37 uninitialized-venue guard at `src/emission/TVLOracle.sol` L309 already does, because `tvl()` is read by `EMASampler`, `GaugeEligibility` and `EmissionDistributor` so one mis-curated governance roster entry must degrade a single venue's contribution rather than brick valuation protocol-wide — the opposite disposition from `GaugeEligibility.meetsCompositionQualityGate`'s `PoolTypeNotWhitelisted` revert, and legible from the difference between an admission decision on one candidate, which must fail loud, and a mean over a roster, which must fail local; the ordering is deliberately INVERTED relative to that precedent, which reads weights at L371 and L389 and checks provenance only afterwards at L373 and L391, a diagnostics wart rather than a security defect since both paths revert and nothing unsafe is admitted, recorded here as a candidate register row against `GaugeEligibility` with no change to that contract in scope and explicitly not to be copied into the oracle, where the same ordering would additionally defeat the gate by making the unsupported call before the gate could prevent it; `approvedFactory` is a constructor immutable seeded from the same `WEIGHTED_POOL_FACTORY` environment value `script/DeployStageG.s.sol` L127 already passes to `GaugeEligibility`, so no new governance surface, no new setter and no new one-shot seal is introduced, a governance-settable slot rejected because its only argument is avoiding a constructor change and the constructor is re-run regardless, and satisfiability verified rather than assumed since `script/DeployDerBodensee.s.sol` L124 creates der Bodensee through that same factory as do the Miliarium pools, so every venue in the live roster passes the gate today; the fix's true cost is named as a ten-contract cascade rather than left inside PB-D50 (x)'s reopens-TVLOracle phrasing, there being no proxy and no in-place patch, so the changed bytecode forces a new address held immutable by `EfficiencyOracle` L24, `EMASampler` L57 and `GaugeEligibility` L32, those three held immutable in turn by `EmissionDistributor` L36 and L42, `CCBMultiplier` L72, `VotingWeight` L47, L53 and L59 and `GaugeRegistry` L41, and that ring held immutable by `AureumGovernance` L42 and L48 and by `IncendiaryRegistry` L84 — the oracle plus nine, drawn from the sixteen contracts deployed in phase 4, with rung f's 62 wiring transactions to re-run against the new address and rewiring no escape, since `CCBMultiplier.setGaugeRegistry` L176 is the PB-D18 one-shot seal and has already fired on Sepolia; disposition on that cascade is BUILD THE FIX AND PROVE IT ON A SEPOLIA FORK WITHOUT RE-BROADCASTING PHASE 4, the test forking Sepolia at a block after the rung-i2 seeding, deploying the corrected oracle in-process, re-binding the oracle's own governance state and nothing else — the 26 pools are not re-capitalised, no seeding transaction is replayed, and rung i2 stands exactly as broadcast — then reading `tvl()` across all 26 pools for PB-D50 (xi)'s ixHelvetia at exactly 14,000 and constellation near 987,000, evidence stronger than a live re-read because pool state is held fixed and only the estimator varies; live Sepolia therefore keeps the biased oracle until Stage R, stated in the deployment record and CLAUDE.md section 11 rather than left implicit and extending section 13's existing disclosure rather than opening a new one, while mainnet inherits no Sepolia address so Stage R deploys the corrected oracle fresh and the testnet's staleness costs the mainnet path nothing; an optional live redeploy rung is named but NOT scheduled, justified only if workstream C binds on truthful end-to-end numbers and never a precondition for closing the fix; PB-D50's FIX disposition is unrevisited and PB-D51 is untouched and still closes independently, this lock resolving the clause (ix) blocker and pricing the clause (x) cost and nothing else | PB3.8i4 |
| PB-D53 | PB3.8i7: the `_venueRatio` build shape, settling four typed-domain questions PB-D50 and PB-D52 leave open before any code lands — the weight term AGGREGATES in lockstep with the balance sums, `wBase` and `wQuote` being the sums of normalized weights over the tokens whose resolved underlying is base and quote respectively, because a weighted pool's parity relation is per-value-share and the share carried by a summed balance is the sum of its legs' weights, so pairing a summed balance with a single leg's weight is a type error rather than a bounded approximation; the branch that makes this observable is STRUCTURALLY DEAD on the live constellation, since `script/config/SepoliaTokenUnderlyings.sol` carries 60 pairs whose 60 underlyings are pairwise distinct with none of the 13 wrapper underlyings itself a mapped pool token, so no venue holds two tokens resolving to one underlying and each accumulation at `src/emission/TVLOracle.sol` L317 and L319 executes at most once per venue; the consequence is that a single-leg implementation is observationally identical to the correct one on every artefact this stage can produce — all 26 venues, the PB-D52 (viii) Sepolia-fork evidence test, and the ixHelvetia-at-exactly-14,000 and constellation-near-987,000 readings PB-D50 (xi) names as the fix's own evidence — so a wrong build passes the entire re-gate and is confirmed by it, PB-D50 (v)'s hiding mechanism recurring one level down with a one-token-per-underlying roster standing where the equal-weight mock stood, and the re-gate therefore carries a PURPOSE-BUILT unit venue holding two tokens on one underlying at deliberately unequal weights, asserting a value the single-leg reading cannot produce; the branch is latent rather than dead in the protocol's own terms, since `setTokenUnderlying` is a plain overwriting setter per PB-D45 (iv) and governance maps a second token onto an existing underlying at any time with no redeploy, no code change and no event marking the moment it goes live; both new guards SKIP rather than revert on PB-D52 (iii)'s pricing-mean-fails-local reasoning — a weight array whose length disagrees with the token array, close to unreachable behind the provenance gate but costing one comparison against an out-of-bounds panic, and a zero `wBase` or `wQuote`, load-bearing because Solidity division by zero is a `Panic(0x12)` that would brick `tvl()` through arithmetic rather than through a `revert` statement and so escape any reading of the revert surface — both folding into the existing eligibility test at L322; the return becomes `(balQuote * wBase * 1e18) / (balBase * wQuote)`, PB-D50 (viii)'s form with the multiplications ordered ahead of the division per the convention `script/SeedMiliariumPoolsSepolia.s.sol` L58 already states, headroom checked rather than assumed at a numerator bounded by 1e66 against a `uint256` ceiling near 1.16e77, and the divide-first ordering rejected on precision; `approvedFactory` enters as a constructor immutable seeded from the same `WEIGHTED_POOL_FACTORY` value `script/DeployStageG.s.sol` L127 passes to `GaugeEligibility`, positioned after `_svzchf` and before `_initialGovernance` with a `ZeroAddress` check matching the four already present, and ripples to ELEVEN construction sites because Solidity constructor arguments are positional — `script/DeployStageF.s.sol` L121 plus ten test sites across `test/unit/TVLOracle.t.sol`, `test/fork/StageHIntegration.t.sol`, `test/fork/StageIIntegration.t.sol` and `test/whitehat/F19_uninitializedVenueLiveness.t.sol` — sized at pre-flight per I13 rather than discovered at the first failing compile; gate order inside the function is the existing `isPoolInitialized` skip at L309, then the new `isPoolFromFactory` skip, then the pool reads, then `getNormalizedWeights`, so the provenance gate precedes every read it protects per PB-D52 (ii); unchanged are the token map and its PB-D42 self-map convention, `_directRatio`'s unweighted cross-venue mean, `_twoHopRatio` and the PB-D43 hop roster, `_constellationRatio`'s svZCHF identity, `tvl()`'s summation shape, the EMA cadence and every governance setter, with the two NatSpec blocks stating the old return form at L300 and L304 corrected in the same save as the code they describe | PB3.8i7 |
| PB-D54 | PB3.8i9 pre-flight: the unit suite prices venues that do not exist, so the PB3.8i8 provenance gate and weight read collapse a surface PB-D53 (vii)'s eleven constructor sites do not describe — `test/unit/TVLOracle.t.sol` builds every venue as a codeless `makeAddr` address registered into `MockVaultExplorer`, which answers `getPoolTokens`, `getPoolData` and `isPoolInitialized` on the venue's behalf, so no contract exists at any venue address, and that was invisible while `_venueRatio` read only through the explorer; the corrected function addresses the venue directly twice, `IWeightedPool(v).getNormalizedWeights()` reverting against an address holding no code and `IBasePoolFactory(approvedFactory).isPoolFromFactory(v)` returning false for an address no factory created, so every venue contributes nothing and the gate converts the revert into a quiet skip, turning nineteen concrete `tvl()` assertions across eleven venue-setup sites into assertions against zero; the general shape is that a mock answering ON BEHALF OF an object lets a whole test surface run without the object existing, and holds exactly until the code under test asks the object something itself; mock venues therefore become real contracts at EQUAL weights, which preserves all nineteen assertions without recomputing one of them, since those assertions were always implicitly equal-weight having been written against a bare balance ratio, while recomputing them against arbitrary unequal weights would change what each test asserts and prove nothing; preserving equal-weight mocks alone would rebuild PB-D50 (v)'s own blindness around the fix rather than around the defect, so unequal-weight cases are added in the same breath — the equal-weight cases prove no regression and only the unequal-weight cases prove the fix; two doubles land beside `MockVaultExplorer` in `test/fork/mocks/StageHMocks.sol`, a settable-weights weighted-pool double and a settable-membership factory double, followed by five tests: an unequal-weight two-token venue, the PB-D53 (iii) discriminator holding two tokens that resolve to ONE underlying at unequal weights which the genesis roster provably cannot supply, an unprovenanced-venue skip, a short-weight-array skip and a zero-weight-leg skip; ladder i9a the doubles, i9b the eleven-site thread, i9c the venue rebuild, i9d the five tests and i9e the unit re-gate, with the three non-unit construction sites NOT sized here and inspected at their own rung, since `StageHIntegration` and `StageIIntegration` construct no `MockVaultExplorer` at all while `F19_uninitializedVenueLiveness` constructs four | PB3.8i9 |
| PB-D55 | PB3.8i9b3 pre-flight: der Bodensee and the Miliarium pools are created by DIFFERENT factories wherever the two factory environment keys diverge, so the PB-D52 (ii) provenance gate exempts `BODENSEE_POOL` — `test/fork/StageGIntegration.t.sol` L153 creates `bodenseePool` through the upstream `wpf` while L165 sets `AUREUM_WEIGHTED_POOL_FACTORY` to the separately constructed `awpf` and L171 through L189 create the pilot pools through `MiliariumPoolDeployer` subclasses reading that key at `script/pools/deploy-miliarium-pool.s.sol` L91, so no single `approvedFactory` vouches for both and passing either one silently drops the other set from the pricing mean; PB-D52 (v) verified the opposite because it checked live Sepolia, where both keys resolve to `0xC60E426294a06Fb95a6C1BB7A06219f794AAde8F` since `DeployAureumWeightedPoolFactory` constructs an upstream instance per RB-008 and PB-D27 keeps Sepolia upstream — the third finding this stage resting on a coincidence local to the current chain, after PB-D51's every-price-equal-to-one and PB-D53 (iii)'s one-token-per-underlying, and the RB-015 degenerate-parameter class applied to an address rather than to a number; creating der Bodensee through the Aureum factory is arithmetically VIABLE and rejected on taxonomy, since `AureumWeightedPoolFactory` L92 to L100 requires `MIN_ERC4626_WEIGHT` of `52e16` while `DeployDerBodensee` gives AuMM `4e17` as a `STANDARD` zero-provider slot against two `3e17` `WITH_RATE` slots for a sum of `6e17`, eight points clear, but that factory enforces a Miliarium quality bar and PB-D39 (ii) settled that der Bodensee is not a Miliarium pool, so routing it there would subject an F-11-fixed forty-percent-AuMM composition to a gauge-eligibility constraint and make the eight-point margin a silent coupling — PB-D39's category error in a second register, the slot salt there and the creating factory here; a governance-curated factory set is rejected as pre-audit surface growth on the PB-D13 ground PB-D50 (x) already books, and a second immutable as hard-coding a two-factory world into the constructor; disposition is one condition with `v != BODENSEE_POOL` as the LEFT operand so the factory call short-circuits, on the ground PB-D52 (vi) already established for the same pool at `IncendiaryRegistry` L360, that a zero-checked constructor immutable carries structural provenance and a gate there would assert what the deployment already guarantees, leaving the governance-extensible roster that is the actual threat model fully gated; three Stage-R gates now sit on der Bodensee's creation and interact rather than merely coexist, the `BODENSEE_SALT` taxonomy per PB-D39 (v), the environment-key split per PB-D27 (ix) and RB-008, and this factory choice, so Stage R adjudicates them together as one der-Bodensee-creation decision; live Sepolia needs no action, because one factory created all twenty-seven venues there and the exemption never fires | PB3.8i9b3 |
| PB-D56 | PB3.8i9e: the unit re-gate is green and PB-D54 (iv)'s nineteen-preserved claim is corrected by measurement to eighteen preserved and one corrected — the first build since the PB3.8i8 constructor change compiles clean and the Part-A unit suite runs 957 tests across 57 suites with zero failures and zero skips, reconciling as 947 at the PB-D34 gate plus four for `test/unit/BytecodeSize.t.sol` at PB3.5i6 plus exactly six from this ladder, one constructor guard at i9b2 and two discriminators at i9d2 and three skip guards at i9d3, with `test/unit/TVLOracle.t.sol` at 61 of 61 having moved 55 to 61, `test/whitehat/F19_uninitializedVenueLiveness.t.sol` at 4 of 4, and the PB-D37 bytecode-size gate at 4 of 4 confirming `TVLOracle` still clears EIP-170 after gaining an immutable; the single moved assertion is `test_tvl_venueMultiTokenSummingPerUnderlying`, corrected from `100e18` to `200e18` at PB3.8i9d1, and the pre-flight that found it was mechanical rather than lucky since only two arrays in that file are built at length three, one of them the constructor seed test, so exactly one venue could carry an asymmetric leg count and that venue is the one that moved while every other holds one base leg against one quote leg where the weight term is exactly one; the correction is POSITIVE EVIDENCE rather than a weakening of the claim, and this is the part worth carrying forward, because under the equal-thirds weights the rebuilt helper assigns a single-leg implementation taking the first matching token's weight rather than the sum would yield `wBase` equal to `wQuote` in that same venue, leave the ratio at `2e18` and pass the original `100e18` assertion silently, whereas the correct lockstep implementation yields `wBase` of `666666666666666666` against `wQuote` of `333333333333333333`, exactly two, and forces `200e18` — so the obligation to move exactly one assertion, in exactly the venue carrying two base legs, to exactly the value the parity relation predicts, is what the plausible wrong build would never have produced; the five PB-D54 (vi) tests all pass, `test_tvl_venueUnequalWeights_appliesWeightTerm` proving the term applies at `400e18` where the pre-fix bare ratio gives `100e18`, `test_tvl_venueAggregatesWeightsInLockstep` separating correct aggregation at `400e18` from the single-leg reading at `250e18` as the PB-D53 (iii) discriminator the genesis roster cannot supply, and the three skip guards each holding a venue that demonstrably prices at `400e18` before changing exactly one thing so a passing zero proves the guard fired rather than an unrelated absence, the zero-weight case targeting the QUOTE leg deliberately since the divisor is `balBase` times `wQuote` and that is the input which panics at `0x12` without the PB-D53 (v) guard; scope is the unit surface only, with the fork re-gate, the `formal/` surfaces asserting the old ratio per PB-D50 (x), the PB-D52 (viii) Sepolia-fork evidence test and the PB-D52 (ix) record statements all still open and unmeasured | PB3.8i9e |
| PB-D57 | PB3.8i9g: the provenance gate is too narrow by one factory, proven by the mainnet-fork suite — `test_Fork_TwoHop_UsdcPricesViaUsdsHop_PropagatesToEMA` failed at `0 <= 0` because the two-hop fixture's `hopVenue` is created by the canonical upstream `WeightedPoolFactory` while the oracle trusted only the Aureum factory, so `isPoolFromFactory` returned false, `_venueRatio` skipped the venue at the PB-D52 (ii) gate, and the `h1` leg returned zero with no revert and no event. The unit suite could not see it because `_setComposition` vouches for every mock venue by construction, so it proves the gate FIRES but never that it fires ONLY when it should. The generalization matters more than the fixture: a constellation venue is a PRICING SOURCE rather than an Aureum-owned asset, so admission and pricing are different decisions — `GaugeEligibility` is right to demand the Aureum factory for a Miliarium pool, while pricing asks only whether the venue is a genuine weighted pool reporting honest weights, and F-12's threat is a hand-rolled contract returning a fabricated vector rather than a pool from Balancer's own canonical factory, making PB-D52 (ii) under-specified rather than wrong. Mechanism: two constructor immutables and a disjunctive gate, a venue passing when either factory claims it, with no setter, no governance surface and no one-shot seal. PB-D55's `BODENSEE_POOL` exemption RETIRES into the general rule rather than being kept beside it, since der Bodensee is created from the canonical factory in both the fixture and the real deploy script, and keeping both would leave two mechanisms answering one question with the special case hiding the next instance. Rejected: a governance-curated factory set, on the PB-D13 ground, reopening only if a third factory appears; exempting governance-roster venues, as gating the wrong half; and editing the fixture to create `hopVenue` through the Aureum factory, which would fix the evidence rather than the design, the same anti-pattern PB-D47 declined | PB3.8i9g |
| PB-D58 | PB3.8i9j: the PB-D57 re-gate is green at every level and the two-hop mainnet-fork failure it was written to close is closed — the unit count lands at 959 rather than the 957 PB-D57 (vii) predicted, the two extra being a seventh negative-construction test and a disjunction-acceptance test both sized after the lock was written, while every other prediction held exactly: `StageHIntegration` reads 12 of 12 with `test_Fork_TwoHop_UsdcPricesViaUsdsHop_PropagatesToEMA` now passing, `StageIIntegration` holds at 4 of 4, and the P10 close gate holds at 13 of 13 unmoved. The P10 result is the direct confirmation of PB-D57 (iv)'s retirement argument, since der Bodensee, created from the canonical factory in the real deploy script, passes the disjunctive gate on the general rule with nothing downstream of its `tvl()` path regressing once the `BODENSEE_POOL` exemption was removed. This is the closing measurement for the chain running PB-D50 through PB-D57 — the weight term, the provenance gate, the aggregation lockstep and now the gate's breadth, each proven by a test built to fail against the specific wrong shape rather than merely to pass. Unchanged: the formal surfaces, the PB-D52 (viii) Sepolia-fork evidence test and the deployment-record and section 11 disclosures all remain open, and live Sepolia still carries the original biased single-factory oracle because PB-D52 (viii) already disposed against a phase-4 re-broadcast | PB3.8i9j |
| PB-D59 | PB3.8i10: the PB-D52 (viii) evidence test passes 3 of 3 against live Sepolia state — `test/fork/PB52SepoliaOracleEvidence.t.sol` forks Sepolia at block 11410000, above the 11405532 that closed the rung-i2 seeding, stands up a corrected `TVLOracle` in process with only that instance's own governance state re-bound, and reads it beside the live oracle: the live one still returns `14933333333333333332400` for ixHelvetia, the corrected one returns exactly `14_000e18` at the same block against the same balances, and the 26-pool corrected sum falls far below the biased 1,271,468 total, with nothing re-capitalised and no seeding transaction replayed. The premise assertion runs FIRST deliberately, establishing that the fork carries the state PB-D50 was measured against, so a passing 14,000 is the estimator changing rather than the pool state differing. Two pre-flight discoveries removed a prerequisite that looked mandatory: PB3.7a's committed `SepoliaPhase4Addresses.sol` closed the phase-4 address gap that blocked the `setMiliariumRegistry` bind, and the concrete `MiliariumRegistry` also implements `IMiliariumSlotRegistry`, so the test resolves ixHelvetia through `poolAtSlot` on chain and no slot-keyed table rung was needed. This is the tree's FIRST Sepolia-fork test, which forces an operational posture: the fork gate is now two passes with the canonical env files copied to the working copy between them, never the reverse, and the env must be returned to mainnet afterwards or every mainnet fixture silently resolves its literals to codeless addresses, which is PB-D35 exactly. PB-D52 (viii) is thereby discharged, its decline of the ten-contract cascade now carrying its evidence, and live Sepolia keeps the biased oracle until Stage R by decision rather than omission | PB3.8i10 |
| PB-D60 | PB3.8i12: the formal re-gate is 55 of 55 with zero rework, because no hevm or Act surface asserts the venue ratio PB-D50 changed — the PB-D17 invocation verbatim discharges all 55 obligations, 7 in `AuthorizerProofs.sol`, 9 in `CCBEngineProofs.sol`, 30 in `EmissionProofs.sol` and 9 in `FeeRoutingHookProofs.sol`, with zero fail, zero warn and zero partial exploration, matching the PB-D17 original and the PB-D18 g3 re-attestation count for count. Pre-flight found the re-gate NARROWER than PB-D50 (x) anticipated, and that finding is the point: `CCBEngineProofs.sol` proves `EMASampler` against a mock deliberately not declared `is ITVLOracle`, so the proofs treat the oracle as an opaque `uint256` source and assert nothing about how the value is computed, and `ema_sampler.act` states the same boundary formally by recording `updateEMA` as RESIDUAL and taking the spot read as a free variable rather than a defined one. That boundary is why 55 of 55 required no source change: the weight term, the provenance gate and the disjunctive factory check all changed what `tvl()` returns and none changed that it returns a `uint256`, which is the entirety of what the formal surfaces depend on, and no harness constructs a `TVLOracle` so none was touched by the eleven-site constructor ripple that ran twice. Closes the PB-D50 through PB-D59 chain end to end with nothing remaining open from it; the live oracle stays pre-fix by the PB-D52 (viii) decision | PB3.8i12 |
| PB-D61 | PB3.8n: the fee-routing hook's `governanceModule` is a one-shot binding that burns its own admin, so `AureumGovernance` is structurally disqualified as its target on every chain — a Stage-R gate rather than a Sepolia artifact. `setGovernanceModule` assigns the module and zeroes `_governanceAdmin` in the same call under the C-D11 two-flag lock, and either flag alone rejects every subsequent attempt, so there is exactly one such call ever per hook deployment, with no rotation path anywhere in the system and no place for one among `DissolutionGovernanceHandoff`'s five authorities. `AureumGovernance` can reach neither gated entry, `setTrustedRouter` nor `routeGovernanceDeposit`, because `execute()` dispatches three proposal types to three fixed targets with no arbitrary-call path, no generic target field and no calldata parameter: pointing the hook's module at that contract would not delegate those two levers but DELETE them permanently, on a hook every Miliarium pool is bound to as its emissions recorder, and the failure is total and silent at seat time since the transaction succeeds and emits `GovernanceModuleSet` with the loss observable only when someone later needs a router seated. The constraint is chain-independent, which is what makes it a Stage-R gate: no document states the rule prospectively, and CLAUDE.md section 2's governance row speaks to the Vault's Authorizer, which genuinely does migrate at Stage K by an explicit call, so a Stage-R author reasoning by analogy from a row correct about a different slot would reach precisely the wrong target. Sepolia is seated correctly and needs no remediation, its `governanceModule` being the deployer EOA that is also the governance multisig on that chain, with the authority proven before the send by running the exact transaction as an `eth_call` rather than by deriving the private admin slot | PB3.8n |
| PB-D62 | PB3.8q: F-21 adjudicated High / FIX — a zero snapshot denominator makes the quorum test `0 * 10_000 < 0 * 2_000` false, so quorum passes vacuously and `CompositionChallenge` returns true at `0 >= 0` where Gauge and Fee return false at `0 > 0`; the strictness divergence is the symptom and the vacuous denominator is the defect, the Gauge and Fee branches being safe only through a cross-contract `VotingWeight` invariant rather than any check local to the tally. The window is unconditional rather than opportunistic, since `_positionPower` short-circuits below the 432,000-block EMA maturity so `poke` never pushes a checkpoint and `getPastTotalSupply` reads zero for every block in the first 60 days of any chain, and the same invariant zeroes every possible against-vote, so no defense exists inside it and the F-01 one-honest-vote mitigation cannot fire. Capture costs a 1,000 svZCHF deposit, no voting weight at all, and 122,400 blocks against a 432,000-block window, available across all 26 filled slots in parallel, with no administrative remedy since `script/DeployStageK.s.sol` L159 handed `MiliariumRegistry` governance to `AureumGovernance` and both `replaceSlot` and `setGovernanceContract` are `onlyGovernance` there. The fix is a zero-supply guard at the denominator, closing both branches at once and covering the mid-life poke-to-zero re-entry path; the `totalVotes == 0` belt-and-braces was declined as redundant. Blast radius sized at pre-flight per I13: the unit suite is unaffected on a non-zero `setUp` supply, the PB3.8p PoC inverts by design into the regression test, and the three tally-reaching fork suites are the named exit gate | PB3.8q |
| PB-D63 | PB3.6b: F-22 adjudicated Critical / FIX-in-source-for-Stage-R — `AureumGovernanceAuthorizer.canPerform` grants `GOVERNANCE_CONTRACT` every actionId unconditionally, but `AureumGovernance.execute()` dispatches over a fixed proposal-type enumeration whose `Proposal` struct carries no target field and no calldata field, so nine of `VaultAdmin`'s eleven `authenticate`-gated entries are permitted to governance and reachable by no caller at all. The two survivors, `pauseVault` and `enableRecoveryMode`, are both ENTRIES whose inverses sit among the nine, so every emergency lever is a door that opens only inward, and the two entered states differ in stickiness: a Vault pause self-clears at the buffer-period end roughly four and a half years out, whereas recovery mode never self-clears, and `enableRecoveryMode` skips authentication entirely once anything is paused, so after any pause ANY anonymous address may place ANY pool into permanent recovery mode. The emergency window bounds entry but not the state entered, so any exit authority must either outlive the entry authority or be backed by a second permanent path. Severity Critical because the trigger is CORRECT USE rather than misuse — a responder doing exactly what the mechanism exists for is what arms it — with custody intact throughout, since pools in recovery mode still permit proportional exit. Prior art at K-D9, PB-D12 (ii) and PB-D61 / RB-017 concerns contracts gated by their own `governanceContract` slot; no document enumerated the VAULT's own admin surface, and three live artifacts presupposing the opposite are named for correction. `setAuthorizer` is the keystone and is ranked first, but restoring it alone is circular and insufficient, so the constrained type must carry the exits as well. The fix is two-layered because a proposal can never be an emergency response at 122,400 blocks of latency; the exit grant dies with entry, which is safe ONLY because layer two can express the exits permanently by vote. Sepolia is NOT remediated by decision, the PB-D52 (viii) shape, and the CircuitBreaker architecture is DESCOPED to its own track so it cannot gate Stage R | PB3.6b |
| PB-D64 | PB3.6c: the F-22 build shape, settling the typed-domain questions PB-D63 left open and amending its (ix) wording from one proposal type to three — `VaultAuthorizerChange`, `VaultUnpause` and `VaultRecoveryDisable` as three named types, the one-type-plus-sub-enum alternative rejected because it adds a second dispatch level inside a single `_executeProposal` branch where the contract's shape is one type to one path with no nesting, and forces `_voteSucceeded` to branch on the sub-type the moment the majorities differ, which they do. The enum values APPEND, since they are positional and stored proposals plus every external decoder of `getProposal` key on the index. `Proposal` gains exactly one member, `address newAuthorizer`, read only by `VaultAuthorizerChange`, while `VaultRecoveryDisable` reuses the existing `targetPool` and `VaultUnpause` carries no payload at all, taking the struct from thirteen members to fourteen — additive in storage, and a tuple-width change for any external `getProposal` decoder, which is the one caller-visible consequence. Majorities differ BY DIRECTION, deliberately: the authorizer change joins the existing two-thirds branch because it is the lever that redefines every permission in the system, while the two de-escalations stay simple-majority because it would be perverse to make leaving an emergency state harder than the bar that never guarded entering one. The shared two-day timelock is explicitly considered and retained. `proposeVaultAuthorizerChange` carries propose-time guards only, the existing `ZeroAddress()` plus a codeless-address rejection, sufficient by the F-20 precedent because the value is frozen in the proposal at creation with no mutable eligibility surface before execute; the two de-escalation entries carry no propose-time guard, since `disableRecoveryMode`'s own checks fail loud and predictably at execute time. Layer one lands first and is independent, its fuzz-exclusion assume set named as a trap that would silently stop asserting rather than fail. The F-22 PoC does NOT invert, since `setProtocolFeeController` stays unreachable by design, so it is reframed from proof-of-defect to attestation-of-accepted-residual. Six-rung ladder a through f, with clause (x) recording the `AuthorizerNotContract(address newAuthorizer)` guard error the build added at b1 | PB3.6c |

---
## Sub-step roadmap (PB1—PB5)

> Execution spine. PB2 opens with the five PB-D5 adjudications — chat beats, each locking its own PB-D entry in NOTES (landed as one §8e.1 NOTES edit per lock), no build before its lock; build sub-steps from adjudication outcomes insert as lettered steps (PB2.1b, …). PB2.11/PB2.12 run LAST in A so their results bind the near-frozen tree (PB-D4). PB3 opens with the PB-D1 environment verify pass; the broadcast (PB3.5) is user-run per §8b. Every numbered sub-step is one §8e.1 per §8e unless marked chat-only, user-run, or user-side.

### PB1 — Stage entry: freeze + plan
- **PB1.1** — `docs/STAGE_P_BIS_NOTES.md` entry freeze (PB-D1—PB-D5). ✅ `f84fa36`.
- **PB1.1a** — PB-D3 base amendment b5f4b8b → f72b309 (user-adjudicated). ✅ `986a223`; CLAUDE.md §11 repointed `db84611`.
- **PB1.2** — `docs/STAGE_P_BIS_PLAN.md` — this file.
- Checkpoint: freeze + plan committed on `stage-p-bis`; §11 names the next sub-step.

### PB2 — Workstream A: open-issue close-out
- **PB2.1** — adjudicate OQ-20 / OQ-21 (controller-initiated `routeYieldFee` + `BLOCKS_PER_EPOCH` cadence throttle, E-D10): build vs descope; locks as its own PB-D entry, build inserts as PB2.1b+.
- **PB2.2** — adjudicate OQ-22 (2-hop TVLOracle valuation fallback through ZCHF / sUSDS; the pre-freeze "fee-routing" label was wording drift, corrected at PB-D7): build vs descope; own PB-D lock.
- **PB2.3** — adjudicate pool restoration: 04 ixViatica / 07 ixCambio (E-D17 literals bar) + the ixCasper waEthwstETH 16% leg (Aureum `CompositeRateProvider`) + the OQ-I10 recorder-binding remainder — restore vs descope-to-the-composition-challenge-path (exists since Stage O); own PB-D lock.
- **PB2.4** — adjudicate F-13 (S5 Low, unbounded internal-leg slippage): rate-derived `limitRaw` / `minBptAmountOut` bound vs accept-with-quantifying-sandwich-sim; own PB-D lock.
- **PB2.5** — adjudicate F-15 (S9 Low, `VaultClassRegistry` veto live-denominator): F-06-style snapshot propagation vs accept; own PB-D lock.
- **PB2.6** — the PB-D2 wiring build per the PB-D11 + PB-D12 amendments: the DISSOLUTION-TIME governance-handoff rotation script (standalone ops artifact, post-OQ-10 — the five re-settable rotations: `EmissionDistributor` / `BodenseeBootstrapChannel` / `TVLOracle` / `EfficiencyOracle` `setGovernanceContract` + `SwapAndDepositToBodensee.setDonateAuthorizer`; `VaultClassRegistry` is one-shot-sealed and reroutes to the PB3.4 production bind per PB-D11) + a P-D36-style fork-test witness on the orchestrator-deployed stack asserting the rotations land AND the stranded family reverts under `AureumGovernance` v1 (the PB-D12 freeze-witness) + the `pauseManager` inert-pointer attestation; includes the one-line `STAGES_OVERVIEW.md` roster-wording reconciliation with PB-D2 (`pauseManager` listed among rotations pre-freeze).
- **PB2.7** — DESCOPED per PB-D13 (user-adjudicated 2026-07-14): the one-file namespacing scope is stale by an order of magnitude (224 `vm.setEnv` sites across 16 fork files; 48 test contracts; six writer files run multiple sibling contracts over shared setEnv fixtures, so the race lives inside single-file H-D40 runs too), a real fix re-opens the P10-validated deploy plumbing (all 16 `Deploy*.s.sol` read fixed literal env keys inside `deploy`), and the "regression proves parallel-safe" exit gate is unevidenceable on the Ankr free-tier posture (D35 hang pattern). `--threads 1` is promoted from F-D11 workaround to permanent canonical fork-invocation posture; the D36 branch-(b) parameterization records as RB-006. Build roster: PB2.7a the PB-D13 lock (NOTES + this line + the L22 tracker row), PB2.7b the RB-006 row + hardening-class bullet, PB2.7c the `STAGES_OVERVIEW.md` bullet close, PB2.7d the Completion Log rows + the Decisions-table back-fill (PB-D6 through PB-D13), PB2.7e the CLAUDE.md §11 rewrite.
- **PB2.8** — direct `ReentrancyGuard.selector` test on `BodenseeBootstrapChannel.distribute()` via roll-plus-accrue mock reentry per PB-D14 (`vm.transientStore` absent from pinned forge-std v1.15.0; the Stage-G sibling guard already directly witnessed; H3.7e's bubble-up test stays as the stale-accrual pin). Build roster: PB2.8a this lock (NOTES + this line + the L23 tracker row + the Decisions row), PB2.8b the one-file test build, PB2.8c the RB-007 row (Closed at the b hash), PB2.8d the Completion Log rows + the CLAUDE.md §11 rewrite.
- **PB2.9** — P-D42 coverage-tooling re-eval RE-AFFIRMED per PB-D15 (user-adjudicated 2026-07-14): the confirmatory re-probe (`forge coverage --ir-minimum --no-match-path "test/fork/**"`, 2026-07-14) reproduces the P-D42 Yul stack-too-deep byte-identically on the byte-identical toolchain (forge `f83bad91` / solc 0.8.26; tree grown 316 → 319 coverage-compile files); the execution-evidence waiver re-affirms (908 Part-A unit + the PB2.x suites), coverage re-eval rides to the post-audit / newer-toolchain gate with the hevm / Act bundle. Build roster: PB2.9a this lock (NOTES + this line + the L24 tracker row + the Decisions row), PB2.9b the `STAGES_OVERVIEW.md` L386 P-D42-clause close, PB2.9c the Completion Log rows + the CLAUDE.md §11 rewrite.
- **PB2.10** — aumm-site spec edits (user-side, runs in parallel): §viii/§ix per OQ-20/21 + the I-reframe, §xxi sandbox row, F-2 Incendiary amendments, FINDINGS corrections, + the PB-D8 (iv) items — `03_ixCasper.md` Fluid-vs-"Flux Finance" mislabel (fWSTETH / fWETH are Fluid per the `07a` rows; the code always bound the Fluid addresses), `04_ixViatica.md` "Flux Finance BRZ vault" wording, `07a_tokens.md` slot-03 waEthwstETH row (M-D11 "RP unresolvable" → the PB-D8 Aureum-composite restoration). Gate: the spec matches the code before PB5.
- **PB2.11** — WK.18 thin-venue populated-roster fork sim BUILT per PB-D16 (user-adjudicated 2026-07-14): F-04's spot-tvl atomic surface is Fixed (the 60-day EMA maturity + freshness gate on `VotingWeight._positionPower`), so the sim is a populated-roster positive-control + economics quantifier — NOT a bug hunt — closing the R1-blocked WK.18 could-not-assess with fork-grade evidence; three faces: atomic pump moves `governanceWeight` ≈ 0 (gate holds), cross-venue mean dilutes 1/N, one sustained-pump scenario fee-negative. Build roster: PB2.11a this lock (NOTES + this line + the L25 tracker row + the Decisions row), PB2.11b the `test/fork/` sim build, PB2.11c the whitehat-ledger WK.18 closure + Completion Log + CLAUDE.md §11 rewrite.
- **PB2.12** — hevm + Act formal-methods bundle BUILT per PB-D17 (user-adjudicated 2026-07-15): hevm `release/0.58.0` (argotorg — the repos moved off ethereum/*; native `hevm-arm64-macos` asset + the resident z3 4.15.4) installs at the §8b beat (user-run) and runs locally against the four Aureum-owned surfaces (fee-routing hook, CCB/EMA engine, emission accrual, authorizer window); Act `v0.2.0` is source-only, so the `.act` specs are authored as audit-handoff artifacts with machine-checking attested firm-side (`STAGES_OVERVIEW.md` L411). Deliverable: top-level `formal/` (act/ + hevm/ + RESULTS.md, the property → status map). Build roster: PB2.12a this lock (NOTES + this line + the L26 tracker row + the Decisions row), PB2.12b the §8b hevm install (user-run), PB2.12c—f the per-surface spec + symbolic-run sub-steps (hook → CCB/EMA → emission accrual → authorizer window; property roster pinned at each pre-flight), PB2.12g formal/RESULTS.md + Completion Log + the CLAUDE.md §11 workstream-A close (LAST in A).
- **PB2.13** — the OQ-23 reopening BUILT per PB-D18 (user-adjudicated 2026-07-16; the new LAST in A, superseding the PB2.12 LAST-in-A phrasings in the Surfaces table, the roadmap preamble, and the PB-D4 row — all left unedited as historical state): `delta_global` re-keys from the Miliarium-only OQ-23 (iii.b) aggregate to the all-Active-gauge aggregate (`GaugeRegistry.gaugeCount()` / `gaugeAt()`, the P-D13 enumeration; raw ungated sum; F-D18 cold-start semantics carry over); `delta_basket` withdrawn same-day — the damping is inherent in F-5/F-6 one-denominator proportionality, so the spec needs no edit and the F-8/§vii items queued into PB2.10 at the g3b anchor drop; `delta_intra` decoupled-unchanged (OQ-23 iv.a); constants untouched (F-D7). Mechanism per PB-D18 (v): ctor 2→3-arg restoring the P-D22-removed gauge-registry slot with a live consumer + the F-D20-mirror one-shot `setGaugeRegistry` seal; 12 construction sites enumerated up front; EmissionDistributor / GaugeRegistry / EMASampler / ICCBMultiplier zero-change. Zero-TVL disposition per PB-D18 (vi): the F-5 multiplicative kill + the `_gatedTvlEMA` return-0 gate traced pre-lock; four residual windows (drain transient, stale score, dust-LP capture, equal-regime carve-out) evidenced at h. Build roster: a the PB-D18 lock; b this plan section (Surfaces row + Decisions row + this bullet); c the FINDINGS OQ-23 reopen ((iii.b) superseded, (iv.a) decoupled-unchanged, delta_basket recorded-and-withdrawn); d the `CCBMultiplier` rebuild (G16 `is IGaugeRegistry` sweep first; `gaugeCount`/`gaugeAt` additive on `src/ccb/IGaugeRegistry.sol`); e unit (enumeration mock restored, F-D20-mirror setter trio, mixed-roster delta_global math, cold-start/zero-aggregate pins); f fork + deploy-wiring (the 12 ctor sites, `DeployStageF` third arg, orchestrator seal + P10-family wiring assertion); g formal (`CCBEngineProofs.sol` ctor sites + mirrored setter proofs, suite 52 → ~55 with the §8b user-run re-attest; `ccb_multiplier.act` residual-footer rewrite; `formal/RESULTS.md` rows); h the zero-TVL disposition (unit pins; fork economics on the residual windows if the pins leave material doubt; exit Fixed / as-designed-with-evidence / Accepted-risk); i close (Completion Log family rows; §11 anchor amendments — the delta_basket sentence, the clamp/step clause, the PB2.10 item drop; workstream A closes → PB3.1).
- Checkpoint: every roster item Fixed / Built / Descoped-with-PB-D-entry; split-form regression green; whitehat ledger + RB register rows updated.

### PB3 — Workstream B: Sepolia deployment
- **PB3.1** — the PB-D1 environment verify pass (user-run, §8b network reads): faucet availability, Permit2 presence at the canonical address, explorer contract-verification flow. Red result blocks PB3 → fallback discussion, own PB-D entry.
- **PB3.2** — P8 testnet token stubs per PB-D20: `test-stubs/` two-template tier set (`StubERC4626` fixed 1:1 NAV, `StubERC20` ctor-set decimals; Tier-A anchors = named instances) + the base-level envOr override resolver in `deploy-miliarium-pool.s.sol` (STUB_-literal keys, passthrough default, non-zero-RP fail-fast) + the emitted Sepolia env-map (Tier D resolved to the env-map arm — no on-chain registry); roster = the Stage-P Builds list + the Tier-A underlyings (stub ZCHF/USDS); build ladder a–f per PB-D20 (vi).
- **PB3.3** — the canonical Balancer V3 Router redeployed against the Aureum Vault per PB-D22 (the P-D26 close-out): permit2 at Balancer's pin (PB3.3a, interfaces-only), `script/DeployRouter.s.sol` (three explicit env reads + the Aureum-branded version constant), the Sepolia hook module-aim at the multisig (one-shot; Sepolia instance only — the mainnet aim stays the Stage-Q A/B input) + the `setTrustedRouter` seat honoring WH-P6 S10 (b), and the mainnet-fork witness realizing the P-D26 (3) production-router leg (the F-17 fix's symmetric-accounting dependency goes live here); build ladder a–e per PB-D22 (vi).
- **PB3.4** — the production `DeployStageP.run()` un-deferred per PB-D23 (the P-D26 orchestration close-out): `run()` composes the sub-scripts' env-keyed `run()`s under the real governor (no `GOVERNANCE_MULTISIG` override; the dual-path CCB seal — `deploy()` forwards through `f`, `run()` calls the governor directly), the F/G/H `run()`s normalize off bare `startBroadcast()` to the env-governor convention, and the PB-D11 (iii) `VaultClassRegistry` bind moves post-K in both spines (target `address(governance)`, the one L562 fixture mirror); fork-rehearsed on a mainnet fork at the PB-D19 future genesis (`GENESIS_BLOCK = broadcast + 100_800`) with a G10 nesting-probe rung before the harness shape commits, re-confirming the four post-conditions; the two PB-D19 (v) pre-genesis pins land at the e-rung; build ladder a–f per PB-D23 (vii); the ladder is amended in flight — c splits into c1/c2/c3 per PB-D24 (iv), d into d1 (the nesting probe, fork-green first run) + d2a–d2d (the stub-pair getter / the standalone rehearsal fixture / `run()` + post-conditions / the Router leg) per PB-D25 (v).
- **PB3.5** — the live Sepolia broadcast + explorer verification per PB-D27 (user-run; per-command §8b approval; testnet only — §8c keeps mainnet banned): the base layer stays per-granular per PB-D23 (vii), so the go-live is a multi-broadcast sequence threading addresses through `.env.sepolia` (refreshed into `.env` before each command) rather than the rehearsal's one-process `vm.setEnv` (PB-D21 (v) amended for the live case, PB-D27 (iii), superseded by PB-D35 and PB-D36), driven by a committed operator runbook at `docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md`; a committed `DECIMALS_<literal>` table discharges the PB-D23 (v) carry — the Sepolia stub deploy cannot read `decimals()` off a mainnet literal that has no code there — and is read fail-fast with no default (PB-D27 (ii)); the four-deep base-layer address cycle resolves by EOA-scoped prediction, since broadcast CREATEs issue from the deployer rather than the script contract and every rehearsal formula is therefore structurally wrong live, verified by a Sepolia-fork dry run at the REAL deployer nonce plus `DeployDerBodensee` / `DeployFeeRoutingHook` self-asserts against the keys the vault step already consumed (PB-D27 (iv)); `DeployDerBodensee` gains the `_resolveStub` / `_resolveRateProvider` mirror on its two hardcoded mainnet rate-provider literals, with `envOr` passthrough deliberately retained so Stage R needs no identity map, plus its first-ever coverage as a script-driven fork witness (PB-D27 (v)/(vi)); ops gates run measure-then-grind — a `StagePRunRehearsal` gas measurement fixes the SepETH target before any faucet accumulation, discharging the PB-D19 gas carry, then the Etherscan key and the deployer-key posture (PB-D27 (vii)); and per PB-D27 (ix) Sepolia keeps the UPSTREAM `WeightedPoolFactory` for both der Bodensee and the 26 pools — the selector-identical `create()` means neither the P10 close gate nor the rehearsal ever exercised the Aureum factory's `QualityGateUnsatisfied` guard, and splitting the factories would fracture the `GaugeEligibility.approvedFactory` singleton immutable's overloaded `WEIGHTED_POOL_FACTORY` key pre-audit, so the real-factory deploy becomes a named Stage-R gate with an RB row for the silent-collision trap; build ladder a1–j per PB-D27 (x), with h never folded into another rung — and per PB-D29 h is the dry run's phase A only (the chain-invariant per-step counts plus the analytic projections), its phase B per-step pre-flight running inside i, since simulation never advances the deployer nonce; and per PB-D30 the fee-routing hook and der Bodensee swap to steps 5 and 6, reducing phase A to four counts (stubs, vault, weighted-pool factory, AuMM) that all simulate standalone on Sepolia, der Bodensee remaining the only base-layer step that needs a live predecessor.
- **PB3.6** — the F-22 fix per PB-D63 as amended by PB-D64: post-Stage-K the Vault's own admin surface is permitted to `AureumGovernance` unconditionally by `AureumGovernanceAuthorizer.canPerform` yet reachable by no caller, because `execute()` dispatches over a fixed proposal-type enumeration whose `Proposal` struct carries no target field and no calldata field — nine of `VaultAdmin`'s eleven `authenticate`-gated entries are stranded, and the two survivors, `pauseVault` and `enableRecoveryMode`, are both ENTRIES whose inverses sit among the nine, so every emergency lever is a door that opens only inward, an unpausable pause lifting only at the buffer-period end roughly four and a half years out while recovery mode never self-clears at all. Two layers, because a 122,400-block proposal can never be an emergency response: `AureumGovernanceAuthorizer` widens `_isEmergencyAction` from two actionIds to four so the emergency principal's in-window response is not one-way, and `AureumGovernance` gains three constrained proposal types carrying named targets with no calldata field and no generic dispatch — `VaultAuthorizerChange` at the two-thirds bar as the keystone that redefines every permission, `VaultUnpause` and `VaultRecoveryDisable` at simple majority as de-escalation. Six rungs: a layer one, b the layer-two contract change, c the reachability tests plus the PoC reframe from proof-of-defect to accepted-residual attestation, d the three stale-artifact corrections, e the F-22 ledger row, f the Decisions rows and the §11 update. Severity Critical, disposition FIX-in-source-for-Stage-R: Sepolia is NOT re-broadcast, exactly as PB-D52 (viii) left the live oracle, and the CircuitBreaker architecture is descoped to its own track so it cannot gate Stage R.
- **PB3.7** — deployed-addresses artifact committed (the frontend-consumption contract).
- **PB3.8** — pool seeding and oracle population per PB-D41 as amended by PB-D42 and PB-D43, pre-genesis: the governance `tokenToUnderlying` map — svZCHF self-mapping so it equals the `SVZCHF` immutable per PB-D42 (iii), other WITH_RATE wrappers mapping share to an underlying stub reachable only through a live `asset()` read per PB-D42 (iv), STANDARD and AuMM self-mapping — plus `addConstellationPool(derBodensee)` and a SINGLE `addHopUnderlying` seed, the sUSDS-derived underlying and nothing else, hop entries being underlyings rather than token addresses per PB-D43 (ii) with svZCHF structurally ineligible and raw ZCHF inert per PB-D43 (iii) and (iv), then initialization of all 26 Miliarium pools against the PB-D41 (vi) USD 1,000,000 banded allocation table with within-pool splits from `normalizedWeights`, then the EMA bootstrap that seeds `tvlEMA` to spot per F-D15, then the standing activity and keeper infrastructure on its own funded EOA — never the canonical deployer, whose uninterrupted nonce sequence backs every committed address projection per PB-D39 (vi); slot 02 seeded but unpriced by design per PB-D41 (v); build ladder a–k per PB-D41 (xi), with rung e re-specified by PB-D42 (vi) as derive-commit-then-assert rather than a derivation from the committed `STUB_` pairs alone and its artifact fixed by PB-D44 as the Solidity library `script/config/SepoliaTokenUnderlyings.sol`, rung f shaped by PB-D45 as a chain-asserted verify-all-then-write-all script idempotent across both append-only setters, rung g routed through the Aureum Router by PB-D46 because every existing initialize call uses a harness callback no broadcast can execute, the `src/emission/TVLOracle.sol` L12 and L19 H-D8 NatSpec correction at rung d, and a live-Sepolia dry run at rung h before any broadcast, re-scoped from a fork because both scripts are chain-guarded to 11155111 and the state they exercise exists only on Sepolia, which surfaced at PB-D47 the slot-02 rate-provider collision that leaves that pool unrecoverable and forces a third-sender redeploy.
- **PB3.9** — der Bodensee dust initialization and first F-0 distribute per PB-D41 (viii), hard-gated on `GENESIS_BLOCK`: AuMM total supply is zero until genesis and the pool is 40 percent AuMM, so the dust is sourced from the first claimed LP emissions on the seeded 26, then all three legs initialize, then `distribute()` releases the held `pendingAccrual` and the fee path deepens the stable side as F-11 designs; rows land in the deployment record per §8f; the mainnet mirror of this ordering is the Stage-R gate at PB-D41 (ix), registered as RB-011.
- **PB3.10** — F-14 ops + fee-custody rails (ixAetheron off-chain conversion, P-D12).
- Checkpoint: full stack live on Sepolia, explorer-verified, artifact committed; the 26 pools seeded and reporting non-zero TVL through a populated oracle, the EMA bootstrapped and the keeper running; der Bodensee initialized once genesis passes per PB3.9; the wiring witness re-run green where fork-replayable against the live deployment.

### PB4 — Workstream C: frontend
- **PB4.1** — hand the addresses artifact to `aumm-app` (OQ-18, separate repo); track the MVP milestone from this repo.
- **PB4.2** — completion gate: the frontend demonstrably reading the Sepolia deployment; evidence recorded in the Completion Log.
- Checkpoint: PB-D4's workstream-C gate satisfied from this repo's side.

### PB5 — Stage close
Two-commit PENDING_PBIS (PB-D3): the close commit writes `PENDING_PBIS` (the `STAGES_OVERVIEW.md` master row + this Completion Log; `CLAUDE.md` §11 stays hash-free) → the `*-backfill` commit substitutes the real hash → tag `stage-p-bis-complete` at the backfill commit → ff-merge `stage-p-bis` → `main`. `stage-p-bis-complete` is the frozen audit candidate handed to Stage Q.
- Checkpoint: zero open workstream-A items; Sepolia live; the frontend gate met; regression logged; the tag at the backfill commit on `main`.

---
## Completion Log
| Sub-step | Commit | Status |
| --- | --- | --- |
| PB1.1 | `f84fa36` | ✅ docs/STAGE_P_BIS_NOTES.md — entry freeze (PB-D1—PB-D5) |
| PB1.1a | `986a223` | ✅ PB-D3 base amendment b5f4b8b → f72b309 (user-adjudicated); CLAUDE.md §11 repointed at `db84611` |
| PB1.2 | `9446cbd` | ✅ docs/STAGE_P_BIS_PLAN.md — plan + PB1—PB5 roadmap + Completion Log |
| PB2.1 | `88c453d` | ✅ PB-D6 lock — OQ-20/OQ-21 BUILD adjudication (user-adjudicated) |
| PB2.1b | `7e250fc` | ✅ src — routeYieldFeeToHook entry point + per-pool BLOCKS_PER_EPOCH throttle |
| PB2.1c | `232b346` | ✅ unit tests — authenticate gate, throttle boundary, stamp-on-revert, bptMinted propagation (42/42) |
| PB2.1d | `28d4193` | ✅ fork witness — collect→route→throttle+auth end-to-end (8/8; closes OQ-20 D4.6 deferral) |
| PB2.1e | `516fa68` | FINDINGS OQ-20/21 closure + Completion Log — this commit |
| PB2.2 | `28c653b` | ✅ PB-D7 lock — OQ-22 BUILD adjudication (2-hop TVLOracle valuation fallback, user-adjudicated) |
| PB2.2b | `61cab54` | ✅ src — _directRatio extraction + _twoHopRatio + hop-underlying roster (ITVLOracle/consumers/constructor unchanged) |
| PB2.2c | `eacb600` | ✅ unit tests — hop gates, direct-wins precedence, mean-across-intermediates, h2-first cheap gate, no-transitive-hop (55/55) |
| PB2.2d | `78c4931` | ✅ fork witness — real USDC via USDS hop, Bodensee hop-2, EMASampler propagation, auth gate (12/12) |
| PB2.2e | `76c6be2` | FINDINGS OQ-22 Phase-2 addendum + L70 label reconciliation + Completion Log — this commit |
| PB2.3 | `1f07d4a` | ✅ PB-D8 lock — pool-restoration adjudication: ixCasper composite-RP BUILD; 04/07 DESCOPE to the challenge path; OQ-I10 closed (user-adjudicated) |
| PB2.3b | `602e5d7` | ✅ script — 03_ixCasper.s.sol config(address) param form + DeployIxCasper view/env read; waEthwstETH restored at index [1], QG 68% → 84% |
| PB2.3c | `fd3dfc2` | ✅ fork fixtures — StageM + StageP composite deploy + env-wire ahead of DeployIxCasper (StageM 7/7, StageP 13/13) |
| PB2.3d | `62e6846` | ✅ CompositeRateProvider.sol NatSpec — PB-D8 second-consumer note (comment-only) |
| PB2.3e | `50aa0e3` | STAGE_I_PLAN + STAGE_I_NOTES OQ-I10 closure + PB2.10 (iv) items + Completion Log — this commit |
| PB2.4 | `6fc8416` | ✅ PB-D9 lock — F-13 SPLIT adjudication (onAfterSwap ACCEPT + sim; routeYieldFee BUILD caller bounds; dormant entries threaded, user-adjudicated) |
| PB2.4b | `ebf5aaa` | ✅ src — caller-supplied minDepositTokenOut + minBptAmountOut threaded (IAureumFeeRoutingHook + AureumFeeRoutingHook + AureumProtocolFeeController pass-through; onAfterSwap 0/0) |
| PB2.4c1 | `69e77c9` | ✅ tests — arity ripple for the bound params across unit + fork call sites (zero bounds, no new coverage; 895 unit green) |
| PB2.4c2 | `1e91e2f` | ✅ unit — bound-threading coverage (order-pinned exact-calldata proofs, all three unlock payloads, controller pass-through, bound-revert stamp-not-advanced; 901 unit green) |
| PB2.4d1 | `6fc1625` | ✅ fork — bounded-route witness (real-Vault BptAmountOutBelowMin + SwapLimit on too-tight bounds, both legs green at realistic bounds; 12/12 fork) |
| PB2.4d2 | `137f1bd` | ✅ fork — onAfterSwap ACCEPT evidence (fee-rider sandwich sim; feeRider 0.375% of swap, skewCost 12× the prize; 14/14 fork) |
| PB2.4e | `5fa40db` | AUREUM_WHITEHAT_OUTPUT F-13 SPLIT row + Completion Log — this commit |
| PB2.5 | `34e4c27` | ✅ PB-D10 lock — F-15 ACCEPT adjudication (poke-invariant truth ceiling; both snapshot fixes rejected as regression-bearing, user-adjudicated) |
| PB2.5b | `f36155b` | ✅ unit — F-15 ACCEPT evidence (real-VotingWeight poke sim: baseline trip, suppression face, truth-ceiling kill, recruit-flip, zero-denominator panic + self-heal edges; 6/6, 907 unit green) |
| PB2.5c | `4e438ed` | AUREUM_WHITEHAT_OUTPUT F-15 Accepted-risk-standalone row + Completion Log — this commit |
| PB2.6-pre | `f444d32` | ✅ PB-D11 lock — VaultClassRegistry one-shot-sealed (not re-settable), reroutes to the PB3.4 production bind; PB2.6 narrows to the five-rotation script (user-adjudicated) |
| PB2.6-pre2 | `ef7ea12` | ✅ PB-D12 lock — emission-layer rotation strands the operational-wiring family under AureumGovernance v1; rotations pinned to dissolution-time, the freeze witnessed as v1 truth (user-adjudicated) |
| PB2.6b | `84a859d` | ✅ script/DissolutionGovernanceHandoff.s.sol — the five dissolution-time rotations (4× emission `setGovernanceContract` + `SwapAndDepositToBodensee.setDonateAuthorizer`); VaultClassRegistry not rotated |
| PB2.6c | `ad42757` | ✅ test/fork/DissolutionGovernanceHandoff.t.sol — PB-D12 rotation witness on the orchestrator-deployed stack (rotations land + multisig lockout + PB-D12(ii) freeze) |
| PB2.6c-fix2 | `b850e8c` | ✅ fork-witness fix — cache handles before `vm.prank`/`vm.expectRevert` (a chained `orchestrator.X()` getter was the "next call" the cheatcodes latched onto); 4/4 fork-green |
| PB2.6d | `7ee86ca` | ✅ pauseManager inert-pointer attestation (Miliarium pauseManager == the write-once GOVERNANCE_MULTISIG, non-exclusive via the authorizer per F-20); 5/5 fork-green |
| PB2.6e | `5cfbd2f` | ✅ docs/STAGES_OVERVIEW.md — multisig-dissolution remainder bullet CLOSED (VaultClassRegistry sealed + pauseManager inert both dropped from the rotation enumeration) |
| PB2.6f | `76e99cd` | STAGE_P_BIS_PLAN.md Completion Log — PB2.6 close — this commit |
| PB2.6g | `a0357ff` | ✅ docs/STAGE_P_BIS_NOTES.md — PB10 lesson (vm.prank AND vm.expectRevert latch a chained getter; P-D38 recurrence, promoted to the §11 index) |
| PB2.6h | `7e9d71f` | ✅ CLAUDE.md — PB10 index bullet + §11 resume anchor (PB2.6 closed; next PB2.7) |
| PB2.7a | `fc09269` | ✅ PB-D13 lock — PB2.7 DESCOPED (stale one-file scope vs the 224-site/16-file measured surface; `--threads 1` permanent posture; user-adjudicated) + plan L22/L75 re-scope |
| PB2.7b | `c6fd6fa` | ✅ ROBUSTNESS_BACKPORT_REGISTER.md — RB-006 row + Test-fixture isolation hardening class |
| PB2.7c | `04db2e4` | ✅ docs/STAGES_OVERVIEW.md — workstream-A D36/F-D11 bullet CLOSED at PB2.7 |
| PB2.7d | — | Decisions-table back-fill (PB-D6—PB-D13) + PB2.6g/h rows + PB2.5c/PB2.6f hash back-fill + Completion Log — this commit |
| PB2.8a | `02536d5` | ✅ PB-D14 lock — `vm.transientStore` absent at pinned forge-std v1.15.0; direct guard witness re-mechanized as roll-plus-accrue mock reentry, channel-only scope + plan L23/L84 re-scope |
| PB2.8b | `612ce0a` | ✅ test/unit/BodenseeBootstrapChannel.t.sol — direct ReentrancyGuard.selector witness on distribute() via roll-plus-accrue mock reentry; arm in MockVault.settle (the getPoolTokenInfo placement was a STATICCALL where the inner state write exceptionally halted — two fix rounds); 35/35 file-scoped, 908/908 Part A |
| PB2.8c | `d11f542` | ✅ ROBUSTNESS_BACKPORT_REGISTER.md — RB-007 row (Invariant-tests class, created Closed at 612ce0a) |
| PB2.8d | — | CLAUDE.md §11 rewrite (PB2.8 closed; ReentrancyGuard.selector carry-forward → Dropped-as-CLOSED; resume anchor → PB2.9) + Completion Log — this commit |
| PB2.9a | `c04b00e` | ✅ PB-D15 lock — P-D42 coverage re-eval: re-probe reproduces the Yul stack-too-deep on the byte-identical toolchain (forge f83bad91 / solc 0.8.26); execution-evidence waiver re-affirmed + plan L24/L86 re-word + Decisions row |
| PB2.9b | `38a68f9` | ✅ docs/STAGES_OVERVIEW.md — workstream-A WK.18/P-D42 bullet: P-D42 clause CLOSED at PB2.9 (WK.18 clause stays open → PB2.11) |
| PB2.9c | — | CLAUDE.md §11 rewrite (PB2.9 closed; resume anchor → PB2.11; no carry-forward drop) + Completion Log — this commit |
| PB2.11a | `505e538` | ✅ PB-D16 lock (NOTES + plan) — WK.18 sim BUILT disposition: F-04's atomic surface already Fixed via the 60-day EMA gate, so the sim is a populated-roster positive-control + economics quantifier, not a bug hunt; plan L25/L90 + L66 Decisions row |
| PB2.11b-i | `b2848ec` | ✅ test/fork/WK18ThinVenuePumpSim.t.sol — sim scaffold (StageN 21-pool populated-roster base + ported VotingWeight wiring; wiring self-test, 1/1 fork-green) |
| PB2.11b-ii | `aec5d67` | ✅ venue pricing layer — two hookless USDC/svZChf constellation venues (_directRatio(USDC) = their cross-venue mean; a one-sided venue pump moves the mean live; 2/2 fork-green) |
| PB2.11b-iii | `0d49203` | ✅ real matured-EMA scored pilot — gauge-seed + recorder position + updateEMA seed / +60d roll / refresh, no _mockPoolEma; 3/3 fork-green |
| PB2.11b-iv | `74ba708` | ✅ face 1 positive control — an atomic venue pump moves spot tvl but leaves governanceWeight byte-identical (the EMA-not-spot proof); 4/4 fork-green |
| PB2.11b-v | `8395d9c` | ✅ face 2 cross-venue dilution — a single-venue pump = 1/N of the both-venue delta (wei-exact, N=2); WH-P6 addConstellationPool double-append attestation; 5/5 fork-green |
| PB2.11b-vi | `b610ca2` | ✅ face 3 sustained-pump economics — +99.6% spot tvl but only +10.69% governanceWeight over 21 daily EMA samples, bounded fee-negative (weightGainBps×2 = 2138 vs spotInflationBps = 9959); on-ramp timeFactor confound caught + fixed; 6/6 fork-green |
| PB2.11c-a | `58ddc0e` | ✅ docs/white_hat/AUREUM_WHITEHAT_OUTPUT.md — WK.18 could-not-assess CLOSED (populated-roster fork-sim evidence; F-04 stays Fixed at c283371+3d80ac8, no findings-table change) |
| PB2.11c-b | `d76ee57` | ✅ docs/STAGES_OVERVIEW.md — WK.18 clause CLOSED at PB2.11 (the P-D42 clause stays byte-identical at PB2.9; the combined bullet now carries a distinct closure tag per clause) |
| PB2.11c-c | `b442248` | ✅ docs/STAGE_P_BIS_PLAN.md Completion Log — PB2.11 family rows (a through c-d planned; the family fanned out further at c-d/c-e/c-f, reconciled at c-g) |
| PB2.11c-d | `0a60321` | ✅ docs/STAGE_P_BIS_NOTES.md — PB11/PB12/PB13 engineering-lesson recipes (em-dash-in-string-literal / solc 8936; timeFactor/ON_RAMP pre-roll confound; insert-fence placement), the NOTES-backed precursor to the §11 index bullets (PB10 precedent) |
| PB2.11c-e | `1ced206` | ✅ CLAUDE.md §11 — two additive edits (WK.18 → Dropped-as-CLOSED; PB11/PB12/PB13 added to the Engineering-lesson index), anchor untouched |
| PB2.11c-f | `e2452a9` | ✅ CLAUDE.md §11 — Last-update resume-anchor advance (PB2.11 close sentence + roster-complete bump + Next sub-step → PB2.12, corrected plan line-refs); date → 2026-07-15 |
| PB2.11c-g | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — reconciles the c-c/c-d placeholder rows + adds c-e/c-f/c-g rows; PB2.11 CLOSED — this commit |
| PB2.12a | `56a46dd` | ✅ PB-D17 lock — hevm/Act BUILD disposition (PB2.12 hevm/Act bundle; user-adjudicated) |
| PB2.12b | `—` | ✅ §8b hevm 0.58.0 install (user-run; provenance recorded in `formal/RESULTS.md` §1) |
| PB2.12c1 | `47880cc` | ✅ test/formal/FeeRoutingHookProofs.sol — S5 hook harness, P-H1/P-H2 (pipeline validation) |
| PB2.12c3 | `c134fec` | ✅ test/formal/FeeRoutingHookProofs.sol — P-H3..P-H6, closing the S5 Tier-1 roster (9/9) |
| PB2.12c4 | `303445f` | ✅ formal/act/fee_routing_hook.act — S5 hook Act spec |
| PB2.12d1-d2 | `063a03f` | ✅ test/formal/CCBEngineProofs.sol — S7 CCB/EMA harness, P-E1 + rig validation |
| PB2.12d3 | `2e4f35e` | ✅ test/formal/CCBEngineProofs.sol — P-E2/P-E3/P-M1, closing S7 Tier-1 |
| PB2.12d4 | `629e16e` | ✅ formal/act/ema_sampler.act + ccb_multiplier.act — S7 Act spec pair |
| PB2.12d4b | `b51c48d` | ✅ formal/act/fee_routing_hook.act — operator reconciliation, closing the S5 spec's legacy-dialect residue |
| PB2.12e1 | `0aa7c15` | ✅ test/formal/EmissionProofs.sol — emission-accrual Tier-1 harness, AuMM cap/halving |
| PB2.12e2 | `33ae38d` | ✅ test/formal/EmissionProofs.sol — P-R*/P-ED* router + distributor cohorts (22 proofs) |
| PB2.12e3 | `3c41725` | ✅ test/formal/EmissionProofs.sol — P-BC* channel cohort, closing emission-accrual Tier-1 (30/30) |
| PB2.12e4a | `e87711c` | ✅ formal/act/aumm.act + aumm_minter_router.act — token-layer Act spec pair |
| PB2.12e4b | `7c3f65c` | ✅ formal/act/emission_distributor.act + bodensee_bootstrap_channel.act — engine-pair Act specs, closing PB2.12e |
| PB2.12f1 | `6eec86d` | ✅ test/formal/AuthorizerProofs.sol — authorizer-window Tier-1 harness, P-W1..P-W5 (7/7); suite at 52/52 |
| PB2.12f2 | `1e6e6d4` | ✅ formal/act/aureum_governance_authorizer.act + aureum_authorizer.act — authorizer-window Act pair, closing PB2.12f |
| PB2.12g1 | `a3b493e` | ✅ formal/RESULTS.md — property → status index over all four surfaces |
| PB2.12g2 | `408569e` | ✅ docs/STAGE_P_BIS_NOTES.md — PB14 lesson + PB-D17 as-built (vi) |
| PB2.12g3a | `8a4e15b` | ✅ CLAUDE.md — section-3 formal/ tree entries + PB14 index bullet |
| PB2.12g3b | `dd6d922` | ✅ CLAUDE.md — section-11 resume-anchor: PB2.12 CLOSED; OQ-23 reopened → PB2.13 threaded forward |
| PB2.12g4 | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — PB2.12 family rows (a through g4); PB2.12 CLOSED — this commit |
| PB2.13a | `f614f0e` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D18 lock (OQ-23 reopened; two same-day 2026-07-16 directives: delta_global → all-Active-gauge aggregate, delta_basket withdrawn; zero-TVL disposition attached; build roster a–i) |
| PB2.13b | `e24b746` | ✅ docs/STAGE_P_BIS_PLAN.md — PB2.13 plan section (Surfaces row + PB-D18 Decisions row + roadmap bullet), restoring plan-line citability for the family |
| PB2.13c | `005fbf5` | ✅ docs/FINDINGS.md — OQ-23 reopen: (iii.b) universe superseded to all-Active-gauges, (iv.a) decoupled-unchanged, delta_basket recorded-and-withdrawn (F-6 rationale); zero spec-side edits |
| PB2.13d1 | `c6a7c79` | ✅ src/ccb/IGaugeRegistry.sol — additive gaugeCount()/gaugeAt(uint256) enumeration members (P-D13 part (1); G16 inheritor sweep first) |
| PB2.13d2 | `3bac2ef` | ✅ src/ccb/CCBMultiplier.sol — the PB-D18 rebuild, full-file: ctor 2→3-arg (gauge-registry placeholder), F-D20-mirror one-shot setGaugeRegistry seal, delta_global re-keyed to the all-gauged aggregate |
| PB2.13e1 | `8529390` | ✅ test/unit/CCBMultiplier.t.sol — MockGaugeRegistry (settable dense gauge list) wired into the 3-arg ctor; 8 gauge tests; unit 916/916 |
| PB2.13e2 | `bb39704` | ✅ test/unit/CCBMultiplier.t.sol — five decoupling tests (gauge vs Miliarium universes genuinely independent; disjoint-roster 777e18-vs-1000e18 discriminator); file 44/44, Part-A 921/921 |
| PB2.13f1 | `72785ea` | ✅ CCBMultiplier 3-arg ctor ripple across the 9 remaining sites (1 script, 5 fork, 3 formal); GAUGE_REGISTRY_PLACEHOLDER env; empty MockGaugeRegistry keeps delta_global neutral; build green tree-wide, unit 916/916 |
| PB2.13f2 | `2b619ce` | ✅ script/DeployStageF.s.sol + DeployStageP.s.sol — post-G-stack orchestrator seal (sealAuthority + NotSealAuthority-guarded sealGaugeRegistry forwarder; DeployStageP post-condition (4) CCBGaugeRegistryNotSealed) |
| PB2.13f3 | `b4132a1` | ✅ test/fork/StageJIntegration.t.sol — retargets the two CCB aggregate assertions to the all-gauged universe; genuine decoupled witnesses at the integration level; file-scoped 5/5 fork-green |
| PB2.13g1 | `413aee7` | ✅ test/formal/CCBEngineProofs.sol — P-M2 gauge-registry one-shot mirror trio (mirrors P-M1 onto setGaugeRegistry); CCB proofs 6→9 |
| PB2.13g2 | `9c109b1` | ✅ formal/act/ccb_multiplier.act — 3-arg ctor rewrite (setGaugeRegistry transition mirroring setMiliariumRegistry; residual footer → the two-aggregate contract) |
| PB2.13g3 | `—` | ✅ §8b user-run hevm re-attest at the g2 tree (9c109b1); 55/55 prove_ PASS (forge clean && forge build --ast prerequisite recorded) |
| PB2.13g4 | `6ba6455` | ✅ formal/RESULTS.md — re-anchors the attested index to the g3 55/55 re-attest (superseding the PB2.12f1 52/52 baseline); 9 CCB proofs, P-M2 row, --ast solver-flag note |
| PB2.13h1 | `6a749c0` | ✅ test/unit/ZeroTvlDisposition.t.sol — F-8 clamp-ceiling resurrect-proof + windows (a)/(b): (59/61)^n drain half-life on the real EMASampler, poolScore persistence + permissionless self-clear both edges; 6/6 |
| PB2.13h2 | `59c7298` | ✅ test/unit/ZeroTvlDisposition.t.sol — ZeroTvlDustCaptureTest, window (c): 1-wei dust captures the full tranche wei-exactly while the score lives, zero after self-clear; 7/7 |
| PB2.13h3 | `6df0999` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D18 (viii) zero-TVL disposition outcome: all four (vi) windows quantified/closed (a–c as-designed-with-evidence, d note-only); no-fork-escalation rationale |
| PB2.13i1 | `691b8f5` | ✅ CLAUDE.md — section-11 resume-anchor: workstream A CLOSED at PB2.13; delta_basket withdrawn, clamp/step clause evaporated, PB2.10 F-8/§vii carry dropped; close-gate 928/928; Next → PB3.1 |
| PB2.13i2 | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — PB2.13 family rows (a through i2); workstream A CLOSED → PB3.1 — this commit |
| PB3.4-pre | `cded9c7` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D19 lock: GENESIS_BLOCK = broadcast + 100_800 (one epoch) decouples the emission clock from deploy; zero code change verified (env-only — DeployAuMM L33 no clamp, the P-D14 four-way assertion is cross-contract, no deploy-time schedule action); PB3.4 future-genesis rehearsal required; two optional pre-genesis pins |
| PB3.4-pre-b | `—` | docs/STAGE_P_BIS_PLAN.md — PB-D19 Decisions-table row + PB3.4 roster annotation + these Completion Log rows — this commit |
| PB3.1a | `51744cf` | ✅ CLAUDE.md — section-11 resume-anchor advance closing the PB3.1 Sepolia verify pass: RPC live (chain 11155111), Permit2 present, deployer 0xA851478dbee97375E784e9b98c0D7D599662bF85 funded 0.05 SepETH; Etherscan key + full gas budget carried non-blocking to PB3.5/PB3.4; PB-D19 backfilled into the anchor narrative; Next → PB3.2 |
| PB3.1b | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — PB3.1a row + this row; PB3.1 CLOSED → PB3.2 — this commit |
| PB3.2a1 | `414eddc` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D20 lock: base-level envOr resolver (STUB_-literal keys via vm.toString, passthrough default, non-zero-RP fail-fast), Tier D = env-map, real RPs over 18/18 stub 4626s (F-11 deviation recorded), two templates + named Tier-A instances, permissionless mint; build ladder a–f |
| PB3.2a2 | `—` | docs/STAGE_P_BIS_PLAN.md — PB-D20 Decisions row + PB3.2 roster annotation + these Completion Log rows — this commit |
| PB3.2b1 | `860bd2f` | ✅ test-stubs/StubERC20.sol — first template: ctor-set name/symbol/decimals over OZ ERC20 + permissionless uncapped mint(address,uint256); backs ixEDEL |
| PB3.2b2 | `5ac8799` | ✅ test-stubs/StubERC4626.sol — fixed-1:1-NAV 4626 stub (convertToAssets/previewRedeem identity, deposit/withdraw, seeding mint), deliberately not is-IERC4626 (mint-collision dodge), 18/18 inherited (F-11); backs svZCHF/sUSDS. Rung b complete |
| PB3.2c1 | `1ca2c9e` | ✅ test/unit/StubERC20.t.sol — 5 tests (metadata, decimals-respects-ctor at 6/8/18, permissionless mint, uncapped, accumulate); 5/5 |
| PB3.2c2 | `49fe65d` | ✅ test/unit/StubERC4626.t.sol — 11 tests: fuzzed 1:1 conversions, previewRedeem(1e18)==1e18, deposit/withdraw/allowance paths, unbacked seed-mint, both F-11 RP cases (accept 18/18, revert InvalidAssetDecimals(6)); 11/11. Rung c complete, 16/16 |
| PB3.2d1 | `b7bc0f7` | ✅ script/pools/deploy-miliarium-pool.s.sol — base-level STUB_ override resolver (_resolveStub vm.envOr passthrough + _resolveRateProvider WITH_RATE-zero fail-fast); QG loop reads pre-override cfg.* untouched; 26 config libs + 28 wrappers frozen; full-project build green |
| PB3.2d2 | `—` | ✅ no-STUB_-env byte-identity proof: the P10 fork suite (StagePIntegration.t.sol, --fork-url mainnet --threads 1) 13/13 green, passthrough byte-identical; verification-only, no commit |
| PB3.2e1 | `94a469c` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D21 lock: iterate-the-real-configs stub-deploy architecture (drift-free STUB_ derivation, two RP override paths, fork-derive-now / broadcast-at-PB3.5) |
| PB3.2e2a | `ad32d7b` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D21 (v) correction, address model user-adjudicated: plain CREATE, real map at PB3.5, rehearsal self-deploys via vm.setEnv |
| PB3.2e2b1 | `dd0f179` | ✅ test-stubs/DeployTestnetStubs.s.sol — iterate-and-deploy core: 26 config libs (21 zero-arg + 5 placeholder-arg), WITH_RATE/STANDARD classification, dedup by mainnet address |
| PB3.2e2b2 | `db7c23f` | ✅ test-stubs/DeployTestnetStubs.s.sol — named-key emission (5 N-D7 RPs + SV_ZCHF/SUSDS via config constants), coverage require gate, console2.log map |
| PB3.2e2c | `—` | ✅ mainnet-fork validation run (forge script --fork-url mainnet, no broadcast): script succeeded, coverage passed, 67 STUB_ pairs + 7 named keys with zero passthrough; verification-only, no commit |
| PB3.2e3 | `a867254` | ✅ test-stubs/sepolia-stubs.env — committed fork-sample map (67 STUB_ + 7 named), byte-verified via an independent determinism re-run; live addresses regenerated at PB3.5. Rung e complete |
| PB3.2f1 | `772292e` | ✅ CLAUDE.md — section-11 resume-anchor advance: PB3.2 COMPLETE, Next → PB3.3 |
| PB3.2f2 | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — the PB3.2 b1–f1 rows + this row; PB3.2 CLOSED → PB3.3 — this commit |
| PB3.3a | `0a35646` | ✅ lib/permit2 + remappings.txt + foundry.lock — permit2 installed at Balancer's toolbox pin (§8c chat-level approval); explicit permit2/ remapping per the pin-every-dependency convention; G10-class compile-probe green on the canonical Router.sol (9 files, no P-D27-class ICE) |
| PB3.3b1 | `d914d9b` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D22 lock: deploy-script shape (three explicit env reads), Sepolia module-aim at the multisig (mainnet stays the Q A/B input), Aureum-branded version string, fork-witness scope, build ladder a–e |
| PB3.3b2 | `—` | docs/STAGE_P_BIS_PLAN.md — PB-D21 Decisions back-fill + PB-D22 Decisions row + PB3.3 roster annotation + these Completion Log rows — this commit |
| PB3.3c | `e85466d` | ✅ script/DeployRouter.s.sol — the P9-granular canonical Router deploy script: three explicit `vm.envAddress` reads (AUREUM_VAULT / WETH_ADDRESS / PERMIT2_ADDRESS, no defaults) + the Aureum-branded `ROUTER_VERSION` constant + one `new Router(...)` against the byte-unmodified canonical submodule; H13-audited storage-only ctor chain; clean-cache compile green (60 files, zero warnings) |
| PB3.3d1 | `5e69280` | ✅ test/fork/RouterIntegration.t.sol — the PB-D22 (v) mainnet-fork witness, first half: the version pin + the F-09 fail-closed negative (a REAL unseated-Router add mints BPT but records nothing) + the governor one-shot module-aim-at-self + `setTrustedRouter` seat; 3/3 fork-green (`--fork-url mainnet --threads 1`) |
| PB3.3d2 | `15599c3` | ✅ test/fork/RouterIntegration.t.sol — the d2 symmetric F-17 pair: post-seat a REAL add credits `userLP` to the true LP for the exact BPT minted; a REAL `removeLiquidityProportional` (BPT allowance to the Router per Vault L1009) debits `userLP` in exact lockstep with live BPT; 5/5 fork-green. PB3.3d complete |
| PB3.3e1 | `a88f2da` | ✅ CLAUDE.md — section-11 resume-anchor advance: PB3.3 COMPLETE, Next → PB3.4 |
| PB3.3e2 | `—` | docs/STAGE_P_BIS_PLAN.md — the PB3.3c–e1 Completion Log rows + this row; PB3.3 CLOSED → PB3.4 — this commit |
| PB3.4a1 | `02d4454` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D23 lock, the production `run()` un-defer architecture: `run()` composes the sub-scripts' own `run()` entries under an env-keyed governor with no `GOVERNANCE_MULTISIG` override, the dual-path CCB seal, the F/G/H run-entry normalization, the post-K `VaultClassRegistry` bind, and the a–f build ladder |
| PB3.4a2 | `bad3c26` | ✅ docs/STAGE_P_BIS_PLAN.md — the PB-D23 Decisions row + the PB3.4 roster-bullet re-annotation |
| PB3.4b | `64bab0c` | ✅ script/DeployStageF.s.sol + DeployStageG.s.sol + DeployStageH.s.sol — the PB-D23 (ii) run-entry normalization: the tree's only bare `vm.startBroadcast()` + `_deploy(msg.sender)` shape moves to the env-governor convention (zero in-tree callers) |
| PB3.4c-pre | `72ad01a` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D24 lock, the composed-`run()` handle-capture mechanism: PB-D23 (i) composed the sub-`run()`s but left capture unspecified, so `c` splits into c1/c2/c3 (PB3.4c pre-flight blocker) |
| PB3.4c1 | `f56f95a` | ✅ script/DeployStageJ.s.sol + DeployStageK.s.sol + DeployStageL.s.sol — the PB-D24 (ii) handle-capture fix: extends the F/G/H public-storage pattern to J/K/L so the composed `DeployStageP.run()` can recover every handle |
| PB3.4c2 | `08e2828` | ✅ script/DeployStageP.s.sol + test/fork/StagePIntegration.t.sol — the PB-D11 (iii) / PB-D23 (iii) `VaultClassRegistry` bind reorder in the `deploy()` spine: the governance one-shot moves from pre-L to post-K targeting `address(governance)`, with the L562 fixture mirror |
| PB3.4c3 | `ecf4fcb` | ✅ script/DeployStageP.s.sol — the PB-D23 `run()` un-defer: replaces the `ProductionOrchestrationDeferredToPbis` footgun-guard with the composed production flow (`_setupEnvProduction` + `_assertBaseLayerGovernorProduction` + `_orchestrateProduction` + the shared `_assertPostConditions`) |
| PB3.4d1 | `4bfb6ee` | ✅ test/fork/BroadcastNestingProbe.t.sol — the PB-D23 (v) RUNG 1 G10-class nesting probe, pinning depth-2 `vm.startBroadcast` sender attribution through composed `run()` entries before the rehearsal-harness shape commits; fork-green on the first run |
| PB3.4d2-pre | `5da02b3` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D25 lock, the production-`run()` rehearsal-harness architecture: both open mechanism axes resolved (a real-EOA governor distinct from harness and orchestrator; a standalone fixture with no `StagePIntegrationFixture` inheritance so the P10 close gate stays byte-identical), the in-process stub replay, and the d2a–d2d ladder |
| PB3.4d2-pre-b | `e55d91b` | ✅ docs/STAGE_P_BIS_PLAN.md — the PB-D25 Decisions-table row + the PB3.4 roster-bullet build-ladder amendment |
| PB3.4d2a | `c099785` | ✅ test-stubs/DeployTestnetStubs.s.sol — the PB-D25 (ii) replay getters: additive `envPairCount()` + `envPairAt(uint256)` exposing the accumulated `(key, value)` emission pairs so the rehearsal replays the same drift-free map the e2 capture and the PB3.5 broadcast consume; the only change to an existing file in PB3.4d2 |
| PB3.4d2b1a | `df27dcd` | ✅ test/fork/StagePRunRehearsal.t.sol — the rehearsal-harness scaffold: SPDX/pragma, the ~40-import block, the contract shell, the PB-D25 (a) `GOVERNOR` and PB-D19 `GENESIS_OFFSET` constants, the state vars, and an empty `setUp`; compile-first, `forge build` green |
| PB3.4d2b1b | `88f0157` | ✅ test/fork/StagePRunRehearsal.t.sol — the nine helper bodies: `_replayStubs` (in-process `DeployTestnetStubs.run()` plus the d2a pair loop into `vm.setEnv`), the four der-Bodensee shape helpers, and the two D22-shaped unlock-callback init pairs |
| PB3.4d2b2a | `4dda4d8` | ✅ test/fork/StagePRunRehearsal.t.sol — `setUp` through der-Bodensee and the three pilots under the PB-D25/PB-D19 deltas (stub replay first, `GOVERNANCE_MULTISIG` = the EOA governor, `GENESIS_BLOCK` one epoch ahead), plus `_seedAmounts` deriving seeds from each token's own decimals because the stub roster does not preserve the mainnet decimal layout the P10 fixture's per-index literals assumed |
| PB3.4d2b2b | `9c129c3` | ✅ test/fork/StagePRunRehearsal.t.sol — the Majors, the Sector-3 tranche, the 26-pool roster env block, and `orchestrator.run()` itself; `baseAuthorizer` is captured pre-`run()` because Stage K migrates the Vault's authorizer mid-`setUp` |
| PB3.4d2c-pre | `dcc5142` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D26 lock, the stub-ordering defect the first rehearsal fork run surfaced as `TokensNotSorted()`: scoped as a production-path defect that would have failed the PB3.5 broadcast identically, the unconditional lockstep sort, the mainnet-invariance proof obligation, three rejected alternatives, and the PB-D20 (i) wording amendment |
| PB3.4d2c1 | `2536e9e` | ✅ script/pools/deploy-miliarium-pool.s.sol — the PB-D26 ascending token sort with lockstep weight permutation immediately before `create()`; `cfg` left pristine (a local `weights` copy feeds `create`), the order-independent QG loop untouched, resolver NatSpec corrected; `forge build` green |
| PB3.4d2c3 | `115819c` | ✅ test/fork/StagePRunRehearsal.t.sol — the four post-conditions re-asserted test-side plus the authorizer-migration pin; post-condition (1) additionally pins the FUTURE offset the in-run check cannot see. Carries the PB3.4d2c2 double gate: the P10 close gate re-ran 13/13 unchanged (the PB-D26 (v) mainnet-invariance proof, checked not asserted) and the rehearsal cleared all 26 pool deploys; 6/6 fork-green |
| PB3.4d2d1 | `32ccffc` | ✅ test/fork/StagePRunRehearsal.t.sol — the Router leg: `DeployRouter.run()` at the tail of `setUp`, deploy-only so the post-condition (3) at-rest baseline survives, plus `_seatRouter` driving the PB-D22 (iii) module-aim and `setTrustedRouter` under the real EOA governor; 8/8 fork-green, the seat test at 66,292 gas against 17k–30k for the pure-view tests |
| PB3.4d2d2 | `501ed98` | ✅ test/fork/StagePRunRehearsal.t.sol — the seated-add smoke assertion closing PB3.4d2: the permit2 two-step funding path and a REAL Router add crediting `userLP` to the true LP for exactly the BPT minted; 9/9 fork-green, the credit test at 789,234 gas against 17k–66k for the rest of the file, the witness that the recorder dispatch actually fired |
| PB3.4e | `159eddb` | ✅ test/unit/PreGenesisAccrual.t.sol — the two PB-D19 (v) pre-genesis pins on the one path P10 never ran: a pre-genesis `_accrueGlobal` touch takes the H-D15 empty-`totalScore` short-circuit and accrues nothing, so the inverted accrual window is never evaluated; and the 100,800-block pad is provably shorter than the 432,000-block EMA maturity gate, the relation making that short-circuit unconditional. Zero src change, 3/3 unit-green |
| PB3.4f1 | `a86d153` | ✅ CLAUDE.md — section-11 resume-anchor advance: PB3.4 COMPLETE, Next → PB3.5 |
| PB3.4f2 | `—` | docs/STAGE_P_BIS_PLAN.md — the PB3.4a1–f1 Completion Log rows + this row; PB3.4 CLOSED → PB3.5 — this commit |
| PB3.5a1 | `af1b8b2` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D27 lock, the PB3.5 Sepolia go-live adjudication. Ten clauses. (i) scope — broadcast + verify only, base layer stays per-granular per PB-D23 (vii), no mega-orchestrator. (ii) the decimals artifact — DeployTestnetStubs' mainnet-fork emission additionally commits a DECIMALS_<literal>=N table per STANDARD slot; Sepolia reads it fail-fast, discharging the PB-D23 (v) carry (IERC20Metadata.decimals() on a mainnet literal has no code on Sepolia); uniform-18 and dual-fork reads both rejected. (iii) the multi-broadcast runbook — amends PB-D21 (v)'s in-process vm.setEnv framing for the live case: addresses persist through .env between forge script processes: new docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md, ordered invocation list, stub map merged into .env before DeployDerBodensee (first consumer of SV_ZCHF/SUSDS and the STUB_ RP keys), .env confirmed gitignored/untracked. (iv) the base-layer address cycle — the rehearsal's computeCreateAddress/CREATE3-from-harness predictions are structurally wrong under real --broadcast (EOA is the CREATE sender); the live cycle is four-deep (vault's 3 EOA CREATEs -> hook/Bodensee predictions -> Bodensee's factory-CREATE3), so projections must cross the vault broadcast; mechanism is a Sepolia-fork dry run at the live deployer nonce (mainnet-fork cannot substitute — wrong nonce) plus script-side self-asserts in DeployDerBodensee and DeployFeeRoutingHook. (v) DeployDerBodensee stub-RP adaptation — mirrors _resolveStub/_resolveRateProvider from deploy-miliarium-pool.s.sol onto the two hardcoded mainnet RP literals (both already carry STUB_ keys in the committed map); envOr passthrough deliberately kept (not fail-fast) so Stage R needs no identity map; ascending sort and create() args already live-correct, byte-identical. (vi) mandates a pre-broadcast fork witness driving the script (not an inline create) — DeployDerBodensee.run() has zero coverage on any existing path, the same class of hole PB-D26 caught for pool ordering. (vii) ops gates ordered measure-then-grind: gas measurement off StagePRunRehearsal fixes the SepETH target before faucet grinding (discharging the PB-D19 carry), Etherscan key, deployer-key handling posture (key itself never recorded), GOVERNANCE_MULTISIG = the funded EOA per PB-D23 (iv). (viii) flags four D-D6 "fork-only" NatSpec lines as stale on broadcast day, non-blocking. (ix) is the flagged-and-verified structural finding: no script deploys the real src/factory/AureumWeightedPoolFactory — DeployAureumWeightedPoolFactory.s.sol deploys the upstream WeightedPoolFactory (E-D16 naming trap), and because both create() signatures are identical the selector collides silently — every P10 and PB3.4-rehearsal Miliarium pool created with ZERO factory-level QualityGateUnsatisfied coverage, admitted-set outcomes unaffected only because MIN_ERC4626_WEIGHT is independently duplicated script-side. Disposition: Sepolia keeps the upstream factory for both Bodensee and the 26 pools (the two already-validated spines' exact configuration) rather than splitting the GaugeEligibility.approvedFactory singleton-immutable's overloaded WEIGHTED_POOL_FACTORY env key pre-audit; real-factory deploy plus the key split plus the DeployStageG read change plus the der-Bodensee-provenance question are recorded as a named Stage-R gate, with a new RB row for the silent-selector-collision trap and the DeployStageG.s.sol L56 NatSpec correction folded into (viii)'s rung. (x) build ladder a1 (this lock) through j (Completion Log + CLAUDE.md Sec.11), twelve rungs, h (the dry-run gate) held separate per the unrecoverable-immutables risk in (iv). Pure append — 22 insertions, 0 deletions, 339 lines total, every PB-D1-PB-D26 line byte-identical. Next: PB3.5a2 (STAGE_P_BIS_PLAN.md — the PB-D27 Decisions-table row + PB3.5 roster annotation). |
| PB3.5a2 | `758a50e` | ✅ docs/STAGE_P_BIS_PLAN.md — the PB-D27 Decisions-table row + the PB3.5 roadmap-bullet annotation, plus a table-indexing gap fix. Adds three rows, not one: while locating the PB-D27 insertion point, the Decisions table was found to jump PB-D23 straight to PB-D25 — PB-D24 (the composed-run() handle-capture amendment, landed at PB3.4c) and PB-D26 (the stub-ordering fix, landed at PB3.4d2c) were both adjudicated and built but never indexed. All three land together, restoring PB-D1 through PB-D27 as a consecutive, gap-free sequence. The PB3.5 roadmap bullet (the one-line stub since PB1.2) is replaced with the full PB-D27 annotation: the DECIMALS_ table discharging the PB-D23 (v) carry, the .env-threading runbook amending PB-D21 (v) for the live case, the EOA-scoped base-layer prediction verified by a Sepolia-fork dry run at the real deployer nonce, the DeployDerBodensee stub-RP mirror plus its first script-driven fork witness, and the upstream-factory Sepolia disposition with the real Aureum factory deferred to a named Stage-R gate. Net 4 insertions, 1 deletion, 283 lines total; PB3.4/PB3.6/PB3.7 bullets byte-identical. Next: PB3.5b (the PB-D27 (ii) decimals artifact — the DeployTestnetStubs mainnet-fork emission extension, the committed DECIMALS_ table, the Sepolia STANDARD-path consumer read with no default, and the fork re-run attestation). |
| PB3.5b1 | `2bc9f4c` | ✅ test-stubs/DeployTestnetStubs.s.sol — purely additive STANDARD-slot decimals table, discharging the PB-D27 (ii) Sepolia carry (a mainnet literal's decimals() call has no code to hit on Sepolia). Five additive payloads: the decKeys/decVals array pair declared alongside envKeys/envVals; one _recordDecimals call inserted into _ensureStandard immediately after the existing decimals() read, capturing DECIMALS_<address>=<dec> before the stub is constructed; the _recordDecimals helper itself, appended after _record; _emitDecimalsMap, appended after _emitMap, console-logging the table as a second block kept separate from the address map so the two artifacts' distinct lifecycles (address map regenerates per broadcast; decimals table is committed once, mainnet truth) stay visually distinct in the emission log; and the _emitDecimalsMap() call as the new last statement of run(), after the existing _emitMap() call. Zero existing line modified, reordered, or deleted — git diff --numstat confirms 28 insertions, 0 deletions. envPairCount/envPairAt stay signature- and body-identical (the 2->3 grep-token count is one new doc-comment mention inside _recordDecimals, not a code change). forge build green. PB3.5b1 complete. Next: PB3.5b2 (the committed DECIMALS_ artifact from a mainnet-fork emission of this file). |
| PB3.5b2 | `1f7c6d5` | ✅ script/config/mainnet-token-decimals.env — the committed STANDARD-slot decimals table, discharging the PB-D27 (ii) Sepolia carry. 45 unique mainnet token literals mapped to their real ERC-20 decimals, captured verbatim from a mainnet-fork, no-broadcast run of test-stubs/DeployTestnetStubs.s.sol's new _emitDecimalsMap output (PB3.5b1). Byte-verified two ways: every key is a subset of the 67 STUB_ keys in the already-committed test-stubs/sepolia-stubs.env (no orphans, no WITH_RATE underlying leaking in — those are 18-dec by construction, never a mainnet read), and the saved file diffs empty against the fork run's own stdout. Distribution is 39x18/4x6/2x8, consistent with known mainnet truth (USDC/USDT/XAUt=6, WBTC/cbBTC=8) — the fork read is real, not decode garbage. Placed under script/config/ rather than test-stubs/ on two grounds: it is mainnet truth, not testnet-specific, so it belongs beside the deploy-time reference data Stage R will also read (GaugeGenesisManifest.sol already lives there); and appending it into sepolia-stubs.env would make that file rewrite its own producer once PB3.5i regenerates it wholesale from the live broadcast, reintroducing exactly the drift PB-D27 (ii) calls structurally impossible. New file only — sepolia-stubs.env confirmed byte-identical via git status. PB3.5b2 complete. Next: PB3.5b3 (the Sepolia-side STANDARD-path consumer read, fail-fast, no default). |
| PB3.5b3 | `3954568` | ✅ test-stubs/DeployTestnetStubs.s.sol — the Sepolia STANDARD-path consumer read, closing the PB-D27 (ii) decimals rung's design surface. New _standardDecimals(address) helper splits the source by block.chainid == MAINNET_CHAIN_ID (1) — the same mainnet-fork signal already established at Sanity.t.sol L59-L62 — deriving off the live literal on chain 1 and reading the committed script/config/mainnet-token-decimals.env table (merged into .env per PB-D27 (iii)) everywhere else, via vm.envUint with no Or and no default: a missing key reverts the run before any stub is constructed. A require bounds the table value to uint8 range before the cast, closing the PB-D26-class silent-truncation risk a malformed table entry would otherwise open. _ensureStandard's one decimals assignment now calls the helper; every other line in the function, and every other function in the file, stays byte-identical — git diff --numstat confirms exactly 2 deletions (the NatSpec line and the assignment) against 21 insertions. StagePRunRehearsal needs no change: it runs on a real chain-1 fork, so it stays on the derive path. forge build green. PB3.5b3 complete. Next: PB3.5b4 (the fork re-run attestation closing rung b). |
| PB3.5c1a | `f0ad475` | ✅ script/DeployDerBodensee.s.sol — the PB-D27 (v) stub-RP adaptation, mirroring _resolveStub / _resolveRateProvider from deploy-miliarium-pool.s.sol L50-63 onto the two hardcoded mainnet rate-provider literals (sUSDS 0x1195BE91..., svZCHF 0xf32dc0eE...). Both literals already carry STUB_ keys in the committed test-stubs/sepolia-stubs.env, so the override is drift-free — with no matching STUB_ key set the resolved address IS the mainnet literal, keeping Stage R byte-identical. _tokenConfig goes private pure to private view (the only signature change) since it now performs env reads; _normalizedWeight stays pure, unchanged. Deliberately kept envOr passthrough rather than fail-fast envAddress, per PB-D27 (v): tightening it would oblige Stage R to publish an identity map for keys it has no reason to set. git diff --numstat confirms exactly 3 deletions (the signature line and the two RP literal lines) against 32 insertions — run()'s six env reads, the runtime sort, the three _tokenConfig/_normalizedWeight calls, the PoolRoleAccounts block and the broadcast sequence all stay byte-identical. No script currently drives DeployDerBodensee.run() (grep-confirmed zero callers), so this edit has zero existing-test blast radius; the PB-D27 (vi) fork witness that will exercise it is a separate, later rung. forge build green. PB3.5c1a complete. Next: PB3.5c1b (the DER_BODENSEE_POOL self-assert per PB-D27 (iv)(3)). |
| PB3.5c1b | `6a55462` | ✅ script/DeployDerBodensee.s.sol — the PB-D27 (iv)(3) self-assert, closing rung c1. Reads DER_BODENSEE_POOL via fail-fast vm.envAddress (no default) and reverts BodenseeAddressMismatch if the just-created pool diverges from the prediction DeployAureumVault and DeployFeeRoutingHook have already consumed to seal their own immutables. Purely additive — git diff --numstat confirms 13 insertions, 0 deletions — landing outside the broadcast block, after vm.stopBroadcast() and before the existing success log, so a divergence aborts before the misleading success line prints. Motivation is the (iv) base-layer address cycle: under real --broadcast the CREATE3 salt resolves against the live EOA nonce, not the harness formulas the PB3.4 rehearsal used, so a silent mismatch here would register a pool at an address nothing else points to, surfacing only much later as an unrelated-looking bind error — exactly the failure class (iv)(3) exists to convert into an immediate abort. Zero existing line touched: the c1a resolvers, the create() argument list, and the broadcast sequence all stay byte-identical; forge build green. PB3.5c1 CLOSED. Next: PB3.5c2 (the DeployFeeRoutingHook self-assert on FEE_ROUTING_HOOK, the paired half of PB-D27 (iv)(3)). |
| PB3.5c2 | `597455c` | ✅ script/DeployFeeRoutingHook.s.sol — the PB-D27 (iv)(3) self-assert on FEE_ROUTING_HOOK, the paired half of c1's DeployDerBodensee assert, plus a stale-NatSpec correction the audit surfaced while grounding the edit. run() now reads FEE_ROUTING_HOOK via fail-fast vm.envAddress before startBroadcast (so a missing key aborts before any gas spends) and reverts HookAddressMismatch if the deployed hook diverges from it, after stopBroadcast and before the success log. deploy()/_deploy() — the no-broadcast entry the StagePIntegration and StagePRunRehearsal fork fixtures actually call — stay untouched; grep-confirmed nothing in script/ constructs or calls run(), so this rung has zero existing-test blast radius, same as c1a. The contract-level NatSpec at L12-18 falsely attributed this exact interlock to the DeployStageP orchestrator, naming computeCreateAddress(thisScript, 1) as the prediction mechanism and claiming the orchestrator asserts equality — neither holds: DeployStageP treats the base layer as a P-D31 fixture/env input and never constructs or predicts this script, and PB-D27 (iv) already names that exact formula a harness artifact, structurally wrong under real --broadcast. Rewritten to state what's actually true: the prediction is supplied by the caller (fixtures via the harness formula, live broadcast via the runbook's EOA-scoped projection), and this script's own contribution is the assert just added. The three remaining orchestrator mentions describing deploy()'s caller (L29/L31/L78 by new numbering) are left untouched, out of scope per this rung — deferred to PB3.5c2b. forge build green. PB3.5c2 complete. Next: PB3.5c2b (the three deploy()-caller orchestrator mentions, correcting fixture-only callers), or PB3.5d (the pre-broadcast fork witness) if c2b is deferred. |
| PB3.5d1 | `9efcb6a` | ✅ test/fork/DerBodenseeScriptWitness.t.sol — the PB-D27 (vi) pre-broadcast fork witness: scaffold, setUp, and the deployed-and-registered smoke test. Standalone by the PB-D25 rationale (not a StagePIntegrationFixture subclass), so the P10 close gate stays byte-identical and never becomes a regression surface. setUp builds a three-token stub roster (18-dec StubERC20 AuMM; two StubERC4626 over 18-dec StubERC20 underlyings for sUSDS/svZCHF, 18/18 because the ERC4626RateProvider ctor enforces both per F-11), stages ten env keys including the two STUB_ rate-provider overrides built through the same string.concat + vm.toString spelling the script's own resolver uses, deploys the UPSTREAM WeightedPoolFactory via DeployAureumWeightedPoolFactory.run() bound to the LIVE mainnet Balancer V3 Vault per PB-D27 (ix), predicts the pool, and drives DeployDerBodensee.run() — the first coverage that script has ever had on any path, closing the P-D34 hole where both the P10 fixture and the PB3.4 rehearsal inline wpf.create() instead. 1/1 fork-green. Two unknowns this rung existed to resolve, both now execution-grounded. (1) The broadcast-sender model: run() opens a bare vm.startBroadcast(), and the pool landed exactly at CREATE3.getDeployed(keccak256(abi.encode(DEFAULT_SENDER, chainid, BODENSEE_SALT)), factory) = 0x6b4EC3aAf155cEA0EA0D51f32B23d0005B782d22, so the create() caller the factory hashed into _computeFinalSalt was forge-std's DEFAULT_SENDER, not the script contract and not the test contract; the Vm.sol resolution order is confirmed by execution, and c1b's BodenseeAddressMismatch self-assert ran on a matching prediction rather than merely compiling. This validates the SHAPE of the PB-D27 (iv) prediction — broadcast-sender-scoped salt, factory as CREATE3 creator — which is what the live Sepolia broadcast uses with the deployer EOA substituted; it does NOT stand in for rung h, the Sepolia-fork dry run at the real deployer nonce, which remains the gate for live addresses. (2) A fresh upstream WeightedPoolFactory bound to the live forked mainnet Vault registers a pool successfully (isPoolRegistered true) — never exercised before in this repo, since P10 and the rehearsal always bind to a fresh Aureum Vault; confirms the unauthenticated-registerPool reading (VaultExtension L158: onlyVaultDelegateCall nonReentrant whenVaultNotPaused, no authenticate) and lets the witness avoid re-opening the four-deep PB-D27 (iv) immutable cycle that a fresh Aureum Vault would require. Known deferred nit: solc warns the smoke test's mutability can be restricted to view, and StagePRunRehearsal.t.sol L478 uses public view for this same setUp-does-the-work pattern; folded into d2 rather than spent as its own round trip. PB3.5d1 complete. Next: PB3.5d2 (ordering, lockstep-weight, resolved-RP-identity and token-type assertions, plus the view fix). |
| PB3.5d2 | `998f2d0` | ✅ test/fork/DerBodenseeScriptWitness.t.sol — the three substantive assertion tests plus the view-mutability fix, closing PB-D27 (vi) rung d. 4/4 fork-green; forge build now reports Compiler run successful with no warning attributable to this file. git diff --numstat confirms 83 insertions against 1 deletion, that single deletion being the d1 smoke test's declaration line taking the view keyword; an offset-corrected diff of the setUp region against 9efcb6a (old L45-112 vs new L47-114, the two-line shift the new imports introduce) reports byte-identical, so the entire stub roster, env staging and prediction block are untouched. Two imports added and no more: TokenInfo plus TokenType from VaultTypes.sol and IWeightedPool; IRateProvider is deliberately NOT imported, since the rate-provider struct member is only ever read through an address cast and naming the type would land an unused import. test_Witness_TokensRegisteredInAscendingOrder pins strict ascending order plus a membership count of three, which together fix the registered set exactly because strict ordering forbids duplicates — the first read-back of what the script's runtime sort actually produces against three freshly-CREATEd stub addresses whose relative order is arbitrary per run. test_Witness_WeightsPermutedInLockstepWithTokens is scoped in its own NatSpec as a regression pin rather than a bug hunt: DeployDerBodensee.s.sol builds token configs at L82-L84 and weights at L87-L89 from the same sorted locals and dispatches on token identity rather than index, so lockstep holds structurally today, and the test locks that property against a future refactor reintroducing the raw-config-order shape PB-D26 caught, where a sorted token array paired with an unpermuted weights array produced a mispriced pool that still registered cleanly. test_Witness_LegConfigSurvivedTheSort is the rung's real yield: c1a's _resolveRateProvider override was executed at d1 but never asserted, and this test proves both WITH_RATE legs resolved to the stub ERC4626RateProvider instances and are explicitly NOT the hardcoded mainnet literals — the precise envOr passthrough behaviour that, unmitigated, would register a Sepolia pool against addresses carrying no code — while the AuMM leg stays STANDARD with a zero rate provider and paysYieldFees false. Enum comparisons use assertTrue on an equality expression rather than a numeric cast, so no integer typecast lint is introduced; all assertion strings are ASCII per PB11. Determinism corroborated across runs: the factory at 0x34A1D3fff3958843C43aD80F30b94c510645C316 and the pool at 0x6b4EC3aAf155cEA0EA0D51f32B23d0005B782d22 are byte-identical in the d1 and d2 fork runs, so the DEFAULT_SENDER-scoped CREATE3 prediction is reproducible rather than a single lucky draw. PB-D27 (vi) is now fully discharged and rung d is CLOSED. Next: PB3.5e (the operator runbook at docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md, carrying the (iv) prediction model and per-step EOA transaction counts, the capture-to-.env key map, the (viii) NatSpec one-liners including the c2b deferral, and the (ix) DeployStageG L56 correction). |
| PB3.5e0a | `e796e20` | ✅ docs/STAGE_P_BIS_NOTES.md — the PB-D28 lock, amending PB-D27 (iv)(1)/(x). The as-written ladder was internally inconsistent: (iv)(1) requires the runbook's per-step EOA transaction counts derived from a dry run rather than read off the source, but (x) places the runbook at rung e and the dry run at rung h, four rungs later, and forbids folding h into another rung. Resolution: the two quantities the runbook carries separate cleanly. A step's transaction count is structural, following from that script's own CREATE and call sequence, invariant under the deployer's nonce; a predicted address is nonce-dependent, which is exactly why (iv)(2) pins the dry run to the real deployer's live nonce immediately before broadcast. Only the address genuinely needs h. Rung e therefore authors the runbook complete in every other respect, carrying a count table that is structurally complete — one row per step — with every cell reading a literal PENDING-h marker rather than a number, alongside the derivation method h will execute; h's scope grows by exactly one obligation, populating that table before any broadcast command issues, and remains the single unrecoverable-immutables gate. Two alternatives rejected and recorded: an early counting-only simulation, which the c1b and c2 self-asserts would defeat by reverting under any placeholder prediction that is not already the answer; and source-derived counts flagged unconfirmed, which (iv)(1) rejects by name because a written number is a number an operator can act on, and the failure mode of an undercount is a mis-projected address sealing a vault immutable — the exact unrecoverable class (iv) exists to convert into an abort. User-adjudicated 2026-07-26. Append-only: git diff --numstat confirms the insertion lands after PB-D27's existing (x) paragraph, and an offset-free diff of NOTES L318-339 against the prior commit reports byte-identical, so the PB-D27 lock itself is untouched by a single character. Cursor backtick-transcription verified clean (48 backticks survived, zero stripped) — the same hazard class as the D32/fbdef2f incident, on a payload dense enough to trigger it. PB3.5e0a complete. Next: PB3.5e0b (the plan-side PB-D28 Decisions-table row, mechanical per the existing PB-D11/PB-D24/PB-D26 precedent at STAGE_P_BIS_PLAN.md L78), then e1 (the runbook document itself). |
| PB3.5e0b | `c959908` | ✅ docs/STAGE_P_BIS_PLAN.md — the PB-D28 Decisions-table row, mechanical transcription of the e0a lock. Inserted immediately after the PB-D27 row per the existing PB-D11/PB-D24/PB-D26 amendment-row precedent; PB-D27 row and the rest of the table stay byte-identical (git diff confirms exactly one line added). Includes a same-beat house-style fix: the row's summary as first dictated used an ASCII double-hyphen where every other row in this table uses a real em-dash per section 10; caught on read-back against the surrounding rows and corrected to U+2014 before commit, so the table stays visually consistent. PB3.5e0b complete; PB-D28 is now cited from both NOTES and PLAN. Next: PB3.5e1 (the operator runbook itself at docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md, per PB-D27 (iii)/(iv)/(viii)/(ix) and PB-D28 (ii)/(iv) — the ordered invocation list, the capture-to-.env key map, the prediction model in prose, the count table with PENDING-h markers, the D-D6 NatSpec one-liners, and the DeployStageG L56 correction). |
| PB3.5e1a | `f6ad272` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — the operator runbook frame: front matter, safety posture, prerequisites, .env hygiene, phase overview, and the PB-D28 count table with every cell reading PENDING-h. New file, 65 lines, 6 sections. Section 0 pins the §8b per-command approval discipline, the mainnet-RPC-is-not-a-typo rule (discard and restart rather than patch in place), private-key handling, and the deployer-equals-GOVERNANCE_MULTISIG constraint per PB-D23 (iv), enforced operationally per PB-D27 (vii)(4). Section 1 states the PB-D27 (iii) cross-process .env threading model. Section 2 is a five-row prerequisites table reconciling PB3.1's carries (Etherscan key absent, gas budget deferred to rung f) against what is already true. Section 4's six-phase overview was drafted only after enumerating the actual env surface across script/ and test-stubs/ — two structural findings shaped it: DeployStageP threads nine intermediate addresses (MILIARIUM_REGISTRY, TVL_ORACLE, EFFICIENCY_ORACLE, EMA_SAMPLER, CCB_MULTIPLIER, SWAP_AND_DEPOSIT, VAULT_CLASS_REGISTRY, GAUGE_REGISTRY, EMISSION_DISTRIBUTOR, BODENSEE_CHANNEL) internally via its own vm.setEnv calls, so the operator never captures those; and DeployStageP.s.sol L255 records that the orchestrator makes no setTrustedRouter call, so the F-09 Router seat is an explicit operator step (phase 5), not something any script performs — an unseated Router mints BPT but records nothing. Section 5's ten-row count table has every cell reading the literal PENDING-h per PB-D28 (ii); no digit appears in any count cell. Section 3 is a deliberate one-line placeholder anchor for the e1b prediction-model insert. One same-beat fix folded in: the first save stripped all 19 blank-line separators between headings, paragraphs, list blocks and tables (46 lines instead of the intended ~65); caught on read-back against the file's own blank-line count (0) versus the project's established docs/ house style (STAGE_P_BIS_NOTES.md carries 174), and corrected via a full-content resave preserving every word, backtick and em-dash — verified by a blank-line-stripped diff against the pre-fix content reporting no difference. Two structural findings surfaced while grounding this sub-step, both worth flagging for e1c/e1d: env-key aliasing is real across the script surface (VAULT/AUREUM_VAULT, BODENSEE_POOL/DER_BODENSEE_POOL, SVZCHF/SV_ZCHF), and DeployStageP bridges only two of those pairs in-process, so the rest is an operator .env-authoring obligation the F-D11 env-key-namespacing carry-forward already names; and all 26 pool scripts share one console2.log line (Miliarium pool deployed at:) with no per-pool disambiguation, so stdout-only capture cannot tell pools apart and the capture map must be either one-invocation-at-a-time or keyed off broadcast/*.json by script name. PB3.5e1a complete. Next: PB3.5e1b (the PB-D27 (iv) EOA-scoped prediction model in prose, filling section 3). |
| PB3.5e1b | `89a6670` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 3 filled with the PB-D27 (iv) EOA-scoped prediction model, written for the broadcast operator. Six paragraphs. Why every rehearsal formula is wrong live: --broadcast issues each new X(...) as a transaction from the deployer EOA, so vm.computeCreateAddress(address(hookScript), 1) and the harness-creator CREATE3 at StagePRunRehearsal.t.sol L130-L138 are test-process artifacts, not live formulas. The cycle is four deep: DeployAureumVault.s.sol L127-L128 reads DER_BODENSEE_POOL and FEE_ROUTING_HOOK before deploying anything, sealing both as fee-controller immutables at L184-L188, while the hook needs the Vault and der Bodensee is created by a factory that also needs the Vault — the first step's inputs are two of the last steps' outputs. Inside the vault step the projection is already live-correct: run() passes msg.sender into _deploy at L95-L97, which projects authorizer/controller/factory at N/N+1/N+2 (L164) and derives the Vault as a CREATE3 off the predicted factory (L171) — only the step's INPUTS need external derivation, not its internal arithmetic. Der Bodensee's salt is scoped to the broadcast sender per BasePoolFactory._computeFinalSalt (lib/balancer-v3-monorepo/pkg/pool-utils/contracts/BasePoolFactory.sol L126-L128), and PB3.5d's DerBodenseeScriptWitness proved this by execution rather than by reading — the pool landed exactly at the DEFAULT_SENDER-derived address, pinning the mechanism the live case substitutes the deployer EOA into. What the operator must project and when: the factory as an EOA CREATE, der Bodensee via CREATE3.getDeployed keyed on the deployer and BODENSEE_SALT, and the hook as a later EOA CREATE — each advancing the nonce by transaction totals section 5 does not yet contain, so no projection is computable and no broadcast may begin until rung h populates it. Three self-asserts and their limits: DeployAureumVault's own pre-existing FactoryAddressMismatch (L203-L205, self-contained), DeployDerBodensee's BodenseeAddressMismatch and DeployFeeRoutingHook's HookAddressMismatch (PB3.5c1b/c2) — the latter two fire against immutables the fee controller has ALREADY sealed, so they abort the sequence but cannot unwind the vault; recovery is a full restart on fresh addresses, tolerable on testnet gas and the reason the rung h dry run is mandatory rather than advisory. Sections 0-2 and 4-5 confirmed byte-identical via content-anchored diffs (avoiding the fixed-line-number offset trap from d2's setUp check); blank-line count went 19 to 24, exactly the five new separators six paragraphs require, so the e1a blank-line-stripping defect did not recur. No transaction count and no count-shaped digit anywhere in the new prose; the chain id 11155111 and the L-number citations are the only numerals. PB3.5e1b complete. Next: PB3.5e1c (the per-step command lines and the stdout/broadcast-json-to-.env capture map, including the env-key-aliasing and pool-disambiguation hazards flagged at e1a). |
| PB3.5e1c | `0797e21` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 6 appended: the phase 1-2 invocation form, key posture, salt pins, and the seven-step command-and-capture map for stubs through the Router. User-adjudicated: BODENSEE_SALT pinned to bytes32(uint256(2)) matching the PB3.4 rehearsal rather than .env.example L96's stale zero value, so rung h's dry run exercises the same der-Bodensee derivation the live broadcast will; deployer key handling via cast wallet import into forge's keystore (--account, never --interactive per-invocation and never  from .env), chosen for the roughly three-dozen-invocation volume this sequence runs to. Two gaps surfaced and recorded rather than silently patched: three keys (EMERGENCY_MULTISIG, WETH_ADDRESS, GENESIS_BLOCK) are consumed by this sequence but absent from .env.example, and WETH_ADDRESS specifically has no Sepolia value anywhere in this repo — the fork fixtures inject the mainnet literal, which carries no code on Sepolia — so it is named as an operator-pinned prerequisite rather than invented here. The alias-bridging distinction from e1a is made concrete: DeployStageP bridges VAULT_EXPLORER/SVZCHF from VAULT/SV_ZCHF internally (L152-L153), so only AUREUM_VAULT/BODENSEE_POOL remain operator obligations alongside their already-covered pair keys. Step 2 (the vault) is the load-bearing simplification: it needs NO broadcast/*.json capture at all, because every address it produces — authorizer at N, fee controller at N+1, factory at N+2, Vault via CREATE3.getDeployed(SALT, factory) — is a projection the operator already computed in section 3; the broadcast confirms rather than reveals, and the pre-existing FactoryAddressMismatch self-assert (L203-L205) is the guard if it doesn't. Steps 3-7 each name their console2.log capture line verbatim, sourced from the actual script files rather than assumed. Verified zero fabricated Sepolia WETH address in the saved file (the mainnet WETH literal does not appear), confirming the one value I deliberately withheld was not invented by the drafting pass. Sections 0-5 confirmed byte-identical to HEAD via an offset-free diff (this insert is append-only against a known 75-line prior file). PB3.5e1c complete. Next: PB3.5e1d (phase 3 through 6 — the 26 pool-script invocations with the shared-stdout-line disambiguation strategy flagged at e1a, the DeployStageP.run() single invocation, the setTrustedRouter Router-seat command, and explorer verification), closing rung e1. |
| PB3.5e1d | `84b5b25` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 7 appended (phases 3-6: pools, orchestration, Router seat, verification) plus a section 4 correction, closing rung e1. User caught a real misframing mid-draft: sections 4 and the initial e1d draft both described the 26 pools as three pilots, five majors and eighteen Miliarium slots, but MiliariumRegistry.slotOf(pool) exposes one flat slot space with no second pool class anywhere in the registry, and STAGES_OVERVIEW.md L15 already says 28 Miliarium pools gauged — the PILOT_/MAJOR_/MILIARIUM_ .env-key prefixes record only which stage a pool first landed in, not a taxonomy. Both the section 4 phase overview and the new section 7 are corrected accordingly; the pool table is rebuilt in ascending slot order (01, 02, 03, 05, 06, 08, 09 ... 28) rather than stage-grouped order, which makes the flat numbering visible and puts the 04/07 gaps where they belong. The 26-row script-to-.env-key mapping does not exist anywhere in the pool scripts themselves — confirmed by grep returning zero hits across all 26 files — so it was extracted by pairing test/fork/StagePRunRehearsal.t.sol L216-L256 (script-to-array-index) with L268-L318 (array-index-to-env-key), the only place in the repo binding script to key, validated by that fixture's 9/9 fork-green run; the saved table cross-checked byte-for-byte against that same source and matches exactly. Phase 3 also names the pool-disambiguation hazard flagged at e1a: all 26 scripts share one console2.log line with no per-pool identifier, so capture must be one-invocation-at-a-time or keyed off broadcast/<ScriptName>.s.sol/11155111/run-latest.json. Phase 4 documents DeployStageP as one composed invocation that threads nine intermediate addresses internally, so none of them are an operator capture obligation, while EMERGENCY_MULTISIG — one of the three keys missing from .env.example, named at e1c — is a phase 4 read. Phase 5 makes explicit that no script performs the F-09 trusted-router seat, quoting the two-call setGovernanceModule/setTrustedRouter sequence DeployRouter.s.sol L25 already documents, and states the failure mode plainly: an unseated Router mints BPT without recording the position. Phase 6 covers Etherscan verification timing and notes the sepolia-stubs.env regeneration is rung i, not this one. One false alarm in my own verify tooling, resolved by direct inspection rather than trusted at face value: an ascending-order check flagged slot 10 as out of position, but the grep pattern also matched section 5's unrelated 10-row count-table entry, which shares the same two-digit-in-pipes shape; reading the actual table confirmed correct ascending order throughout. git diff --numstat 63/1, matching one section-4 line replaced plus the section-7 append; only this file modified. PB3.5e1c and e1d together close rung e1 in full: prediction model, command-and-capture map, pool roster, orchestration, Router seat, and verification are all documented. PB-D27 (iii) is fully discharged. Next: PB3.5e2 (the PB-D27 (viii) D-D6 NatSpec one-liners across DeployFeeRoutingHook.s.sol L26, DeployAureumWeightedPoolFactory.s.sol L14, DeployDerBodensee.s.sol L19 and DeployStageF.s.sol L43), e3 (the DeployFeeRoutingHook orchestrator-attribution corrections deferred from PB3.5c2 as c2b), or e4 (the PB-D27 (ix) DeployStageG.s.sol L56 correction) — per PB-D28 (v) these three close out rung e before f. |
| PB3.5e2 | `7f57634` | ✅ four scripts — the PB-D27 (viii) D-D6 fork-only-scope NatSpec correction, noting the PB3.5 Sepolia broadcast now that these scripts are exercised live rather than only on a mainnet fork. D-D6 (STAGE_D_PLAN.md L88) reserves MAINNET for Stage R and says nothing about testnets, so the correction is additive framing, not a retraction. Grounding surfaced a real divergence from PB-D27 (viii)'s own description: it characterized all four files as carrying flat no production broadcast path wording, but DeployStageF.s.sol and DeployFeeRoutingHook.s.sol already said no production broadcast within Stage P — a narrower phrasing PB-D27 (viii) did not anticipate — while DeployAureumWeightedPoolFactory.s.sol and DeployDerBodensee.s.sol did carry the flat wording as described. All four still needed the fix: a reader in Stage Q would not parse within Stage P as and this changed at P-bis. Edit shape differed accordingly — files 1 and 2 got a flat one-clause replacement; files 3 and 4 preserved their trailing run()/deploy() sentence verbatim while only the opening clause changed, per file, since that sentence remains accurate and unrelated to the broadcast-scope point. Two grep lines in my own verify block false-alarmed at 0 (expected 1) on files 3 and 4: Cursor reflowed each full paragraph as one unit rather than preserving the trailing sentence's original line breaks, so a substring grep anchored to the old line boundaries no longer matched even though every word of that sentence survived unchanged — confirmed false by reading both files' actual diffs directly rather than trusting the grep count, closing the gap on file 3 with a second isolated git diff request after its first paste truncated with no hunk content. Four files, one comment clause each, zero non-comment lines touched; git status confirms only these four files modified. PB3.5e2 complete. Next: PB3.5e3 (the DeployFeeRoutingHook.s.sol L29/L31/L78 orchestrator-attribution corrections deferred from PB3.5c2 as c2b) or e4 (the PB-D27 (ix) DeployStageG.s.sol L56 correction). |
| PB3.5e3 | `834dc57` | ✅ script/DeployFeeRoutingHook.s.sol — the orchestrator-attribution correction deferred from PB3.5c2 as c2b. Three mentions claimed DeployStageP (the P9.5 orchestrator) calls or constructs this script's deploy() entry; grep-confirmed DeployStageP.s.sol contains zero references to DeployFeeRoutingHook anywhere. The actual callers, confirmed at the call sites, are the two fork fixtures: StagePIntegration.t.sol L125/185 and StagePRunRehearsal.t.sol L129/193, both constructing DeployFeeRoutingHook directly and calling deploy() with resolved args — exactly the P-D34 fixture-bypass pattern c2's own commit already named for the run()-side prediction. The corrected text keeps the true half of the original claim — that deploy()'s explicit typed arguments sidestep the DER_BODENSEE_POOL/BODENSEE_POOL and SV_ZCHF/SVZCHF env-key divergence P-D25 documents — but stops attributing that to the orchestrator calling deploy(), since deploy() bypasses env entirely regardless of caller; the SV_ZCHF/SVZCHF normalization DeployStageP does perform is real but unrelated to this entry point, re-cited accurately to DeployStageP.s.sol L152-L153 (re-verified this sub-step, confirming the citation survived untouched since e1c). Second correction on the deploy() function's own NatSpec, same substitution. Both diff hunks matched the drafted replacement text exactly with no reflow, since the replacement was already given explicit line breaks rather than left to Cursor's own wrapping — avoiding the e2 false-alarm class entirely. git status confirms only this file touched. PB-D27 (viii)'s three-mention citation (L29/L31/L78 by the pre-e2 numbering) is now fully resolved. Next: PB3.5e4 (the PB-D27 (ix) DeployStageG.s.sol L56 correction — WEIGHTED_POOL_FACTORY named as the AureumWeightedPoolFactory, false on every existing path since Sepolia and both validated spines bind the upstream Balancer factory), closing rung e. |
| PB3.5e4 | `c56985a` | ✅ script/DeployStageG.s.sol — the PB-D27 (ix) WEIGHTED_POOL_FACTORY correction, closing rung e. L56 (now shifted) falsely named the env var as holding AureumWeightedPoolFactory; confirmed false on every existing path — both StagePIntegration.t.sol and StagePRunRehearsal.t.sol set WEIGHTED_POOL_FACTORY to awpf, an Aureum-typed handle assigned from DeployAureumWeightedPoolFactory().run(), which constructs the UPSTREAM Balancer WeightedPoolFactory bound to the Aureum Vault, not src/factory/AureumWeightedPoolFactory.sol. The real Aureum factory is constructed only in StageGIntegration.t.sol, PilotPools.t.sol, CCBEngine.t.sol and the unit suite — never on a production path. Corroborated independently: GaugeEligibility.sol consumes approvedFactory through IBasePoolFactory(approvedFactory).isPoolFromFactory(pool) (L373), the upstream interface, and GaugeEligibility's own constructor param doc at L148 already says Balancer pool factory rather than claiming the Aureum type — this script's doc line was the one outlier. The doc line is corrected to describe the variable by its actual role (the GaugeEligibility approvedFactory admission) rather than by a wrong type name, and a nine-line E-D16 dev note is added recording the trap: the factory-level QualityGateUnsatisfied guard never runs on any validated path, the script-side MIN_ERC4626_WEIGHT check duplicates the same threshold so the admitted set is unaffected, Sepolia keeps this configuration, and Stage R deploying the real factory requires splitting the WEIGHTED_POOL_FACTORY / AUREUM_WEIGHTED_POOL_FACTORY overload — all already adjudicated at PB-D27 (ix), transcribed here rather than re-decided. Both diff hunks matched the drafted payload exactly, no reflow, since every replacement line was given explicit content up front. The vm.envAddress("WEIGHTED_POOL_FACTORY") read is untouched (now at L127 after the insert); all 10 env-var doc entries intact; only this file modified. PB-D27 (viii) and (ix) are both now fully discharged in code. Rung e (e0a/e0b the PB-D28 lock, e1a-e1d the runbook, e2 the D-D6 NatSpec correction across four files, e3 the orchestrator-attribution fix, e4 this correction) is CLOSED in full. Next: PB3.5f (the PB-D27 (vii)(1) gas measurement on the StagePRunRehearsal spine, user-run under Sec8b, fixing the SepETH target and recorded into the runbook — discharging the PB-D19 gas carry). |
| PB3.5f1 | `584b632` | ✅ test/fork/StagePRunRehearsal.t.sol — the PB-D27 (vii)(1) gas instrumentation on the production spine. Two gasleft() reads bracket the whole setUp deployment path (L116 before _replayStubs, L339 after the Router seat) with one console2.log emitting the delta; no other change, no assertion touched. Measured 367,079,280 gas, IDENTICAL across all nine tests — setUp re-runs per test, so nine matching values is the determinism check passing, confirming no nondeterminism sits in the deploy path. 9/9 fork-green, unchanged from PB3.4d2: the instrumentation did not perturb the validated harness. The bracket deliberately opens BEFORE _replayStubs so the 67 stub-token deploys are inside the measurement — Sepolia deploys those too, so this is the right number for the funding decision rather than an understatement. Funding consequence: at 1-3 gwei the spine costs 0.37-1.10 SepETH against a deployer holding 0.05, so the PB3.1 gas carry stands and the PB-D19 target of roughly 1-2 SepETH is now evidenced rather than estimated; 367M is a floor, since forge script --broadcast sends each CREATE as its own transaction and adds per-tx intrinsic gas plus init-code calldata at 16 gas per byte, order 2-3 percent, inside the headroom. Build-warning audit run alongside and recorded here rather than deferred: 11 warnings, all pre-existing, ZERO in src/ (all in script/ or test/), and none citing this file. The three solc-2519 shadow warnings at DeployStageK.s.sol L143-L145 are benign on inspection — _wire reads only its parameters, contains no assignments at all, and both callers (run() L87, deploy() L98) pass the same storage handles _deploy just populated, so shadow and shadowed are identical at every call site. The forge-lint divide-before-multiply at test/unit/EmissionDistributor.t.sol L642 is LOAD-BEARING and must not be fixed: (capT * 1e18 / (1e18 - capT)) * sOther / 1e18 expands production EmissionDistributor.sol L494 capT.divDown(1e18 - capT).mulDown(totalScore - oldEffective) operation-for-operation including the intermediate floor, and L650 asserts exact equality, so reordering to multiply-first would break the test — a disable-next-line with rationale is the correct future cleanup, not a rewrite. Remaining warnings (four erc20-unchecked-transfer, one unsafe-typecast on a small-counter synthetic address, two mock pure-mutability) are test scaffolding, each transfer followed by an exact balance assertion so a false return fails loudly. Next: PB3.5f2 — record the measured figure and the SepETH target into docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md, discharging the PB-D19 gas carry. |
| PB3.5f2 | `ffb20d3` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — the PB-D27 (vii)(1) gas measurement recorded into the runbook, discharging the PB-D19 gas carry and closing rung f in full (f1 measured at 584b632, f2 records it here). Two edits, one fact landed in two places. Edit 1 replaces the prerequisites-table row 2 forward-reference (0.05 at PB3.1; the target is fixed by the rung f gas measurement / Gate: rung f) with the resolved figure (target ~1-2 SepETH per the rung f gas measurement, section 6 / Gate: before phase 1) — the row no longer points forward to an undone step, it states the number. Edit 2 inserts a new Gas budget paragraph after Step 7 (the Router command) and before the section 7 header, matching the five existing bolded-lead-in paragraphs already in section 6: the measured 367,079,280 gas figure, the determinism note (setUp reruns per test, so nine identical values across all nine fork tests is the harness confirming nothing nondeterministic sits in the deploy path), the 0.37-1.10 SepETH range at 1-3 gwei Sepolia basefee, the floor-not-estimate caveat (forge script --broadcast sends each CREATE as its own transaction, adding per-tx intrinsic gas plus init-code calldata at 16 gas per byte on top, order 2-3 percent), and the funding instruction — roughly 1-2 SepETH via the pk910 PoW faucet, the 0.05 SepETH from PB3.1 is 7 to 22 times short. Both diff hunks matched the drafted payload exactly — backticks and em dashes transcribed literally, the number transcribed with no rounding, no reflow. Only this file modified; git status confirms zero other files touched. PB-D27 (x) build ladder: a1-a2-b-c1-c2-d-e now closed (prior rungs), f (this pair) now closed. Next: PB3.5g (the PB-D27 (ix) RB register row — the Aureum WeightedPoolFactory exposes no positive identity marker, so any future mis-set WEIGHTED_POOL_FACTORY key silently disables its quality-gate guard rather than reverting; the row records that trap in docs/ROBUSTNESS_BACKPORT_REGISTER.md). |
| PB3.5g | `8db2e48` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — the PB-D27 (ix) RB register row (RB-008) plus the new Contract-identity verification hardening class it belongs to, closing rung g. Two edits, additive only — RB-001 through RB-007 and every other section untouched. |
| PB3.5h0a | `731af0b` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D29 lock, the PB-D27 (iv)(2) amendment making the pre-broadcast dry run two-phase. |
| PB3.5h0b | `69c8e14` | ✅ docs/STAGE_P_BIS_PLAN.md — the plan-side PB-D29 Decisions row plus the PB3.5 roster-row correction, mirroring the NOTES lock from PB3.5h0a. |
| PB3.5h0c | `dbe4aba` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D30 lock, the PB-D29 (v) amendment swapping the hook and der Bodensee in the base-layer order. |
| PB3.5h0d | `0d3b42f` | ✅ docs/STAGE_P_BIS_PLAN.md — the plan-side PB-D30 Decisions row plus a second roster-row correction, closing the h0 pre-flight series. |
| PB3.5h1a | `cfba6f7` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — the PB-D30 hook-before-Bodensee reorder, landed at all four places the old order was asserted. |
| PB3.5h1b | `bb1e7be` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 8, the rung h phase A / phase B operator procedure locked at PB-D29 and PB-D30. |
| PB3.5h2a | `a022cc5` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D31 lock, the DeployTestnetStubs missing-broadcast defect the rung h phase A dry run surfaced. |
| PB3.5h2a2 | `cc7e5cc` | ✅ docs/STAGE_P_BIS_PLAN.md — the plan-side PB-D31 Decisions row. |
| PB3.5h2b | `d83c9f9` | ✅ test-stubs/DeployTestnetStubs.s.sol — the PB-D31 (iv) broadcast wrapper, the fix for the missing-broadcast defect found at h2a. |
| PB3.5h2c | `fa57241` | ✅ StagePRunRehearsal re-run — the PB-D31 (v) no-regression gate, 9/9 fork-green. |
| PB3.5h3a | `a22553f` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 5 populated with the measured phase-A counts. |
| PB3.5h3b | `d9a2593` | ✅ docs/STAGE_P_BIS_PHASE_A_RECORD.md — the dated phase-A measurement record. |
| PB3.5h3c | `6b12916` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 8's Residual corrected, closing rung h. |
| PB3.5h4 | `e1e8e4b` | ✅ CLAUDE.md — interim section 11 advance covering PB3.5 rungs a1 through h. |
| PB3.5i | `3b7a4d7` | ✅ docs/STAGE_P_BIS_SESSION_HANDOFF.md — session handoff for the halted rung i broadcast. |
| PB3.5i0c | `244ed2f` | ✅ docs/STAGE_P_BIS_SESSION_HANDOFF.md — reconciled with PB-D32; section 4's recovery claim withdrawn and corrected. |
| PB3.5i0a | `ef237ab` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D32, the EIP-170 Vault-oversize lock. |
| PB3.5i0b | `581072f` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D32 Decisions-table row. |
| PB3.5i1a | `e29fee3` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D33, the optimizerSteps cause and the foundry config fix. |
| PB3.5i1b | `33bca0b` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D33 Decisions-table row. |
| PB3.5i3 | `4268e50` | ✅ EIP-170 fix via a scoped vault compiler profile — Vault 28,304 to 24,393. |
| PB3.5i4a | `edb4332` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D34, the scoped vault-profile mechanism and its measured blast radius. |
| PB3.5i4b | `e3ba38b` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D34 Decisions-table row. |
| PB3.5i5a | `75f7dc9` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D35, the .env Sepolia/mainnet-fork contamination fix. |
| PB3.5i5b | `6a8fbad` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D35 Decisions-table row. |
| PB3.5i5c | `bb489af` | ✅ docs/STAGE_P_BIS_SESSION_HANDOFF.md — reconciled with PB-D32 through PB-D35; the EIP-170 blocker closed. |
| PB3.5i5d1 | `2d8806a` | ✅ .gitignore — .env.sepolia added to the Environment block. |
| PB3.5i5e1 | `5f5ee79` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D36 lock, the three-file copy-and-refresh environment model. |
| PB3.5i5e2 | `33824bd` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D36 Decisions-table row. |
| PB3.5i5e3 | `8e1a060` | ✅ .gitignore — .env.mainnet added to the Environment block. |
| PB3.5i5e4d | `10f38de` | ✅ .gitignore — out-vault/ ignored as a build product. |
| PB3.5i5e5a | `3521725` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 1 rewritten for the PB-D36 three-file environment model. |
| PB3.5i5e5b | `7884f3e` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 2 prerequisites retargeted to the PB-D36 three-file model. |
| PB3.5i5e6a | `18867a9` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 6 gains the Capture and refresh paragraph. |
| PB3.5i5e6b | `a0b606e` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 6's eight write-destination references retargeted to .env.sepolia. |
| PB3.5i5e7 | `1e7f496` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 6's Capture-and-refresh scope widened to the whole runbook; section 7's two remaining .env key references retargeted to .env.sepolia. |
| PB3.5i5e8 | `7310a37` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 8 phase A writes its projections into .env.sepolia and refreshes .env before each simulation reads them. |
| PB3.5i5e9 | `ba983f3` | ✅ docs/STAGE_P_BIS_PLAN.md — L121 roster paragraph corrected for the PB-D36 three-file environment model. |
| PB3.5i6a | `e14bae0` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D37 lock: the deployed- bytecode-size gate lands as a named-artifact Solidity test. |
| PB3.5i6b | `24c7a36` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D37 Decisions row added, mirroring the STAGE_P_BIS_NOTES.md lock landed at e14bae0. |
| PB3.5i6c1 | `33cb649` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D37 clause (xi): artifact references use full source path, not bare identifier. |
| PB3.5i6c2 | `7ff3909` | ✅ foundry.toml — ./src added to fs_permissions, read access, for the PB-D37 vm.readDir completeness check. |
| PB3.5i6c3 | `43f031a` | ✅ test/unit/BytecodeSize.t.sol — the PB-D37 deployed- bytecode-size gate, 4/4 green. |
| PB3.5i6d | `68a431b` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-009 and RB-010 added, closing out the PB-D37 register obligations. |
| PB3.5i7a | `54fea41` | ✅ docs/STAGE_P_BIS_NOTES.md — PB15 lesson landed, the recipe PB-D33 (x) promised and never delivered. |
| PB3.5i7b | `9a20b31` | ✅ CLAUDE.md — section 2 Compiler row corrected for the two-profile reality and the withdrawn parity claim. |
| PB3.5i7c | `ba5336a` | ✅ CLAUDE.md — PB15 added to the section 11 engineering-lesson index. |
| PB3.5i7d | `8f862f8` | ✅ CLAUDE.md — section 11 resume anchor advanced through rung i7. |
| PB3.5i7b2 | `10065fb` | ✅ foundry.toml — header comment corrected, the last resident copy of the withdrawn Balancer toolchain-parity claim. |
| PB3.5i8a1 | `00f4854` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D38 header and clauses (i)-(iv), the resume-arithmetic rule and the grep-fallback withdrawal. |
| PB3.5i8a2a | `b0c7dd5` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D38 clause (v), the canonical projection set replacing the void table. |
| PB3.5i8a2b | `8abfad4` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D38 clauses (vi) and (vii), completing the lock. |
| PB3.5i8b | `a3177ad` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D38 Decisions row added, mirroring the STAGE_P_BIS_NOTES.md lock landed at 8abfad4. |
| PB3.5i8c1 | `980a6ce` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — the no-jq count fallback is WITHDRAWN per PB-D38 (iv), a confirmed double-counter. |
| PB3.5i8c2 | `a0c5b01` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — greenfield-versus- resume paragraph added ahead of Phase A, per PB-D38 (ii). |
| PB3.5i8c3 | `ec65ca1` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — resume caveat added to section 5, closing i8c. |
| PB3.5i8d1 | `bc3ce85` | ✅ docs/STAGE_P_BIS_SESSION_HANDOFF.md — canonical PB3.5i8 projection set added beneath the void table in section 4. |
| PB3.5i8d2a | `2d97a62` | ✅ docs/STAGE_P_BIS_SESSION_HANDOFF.md — section 3's hard constraint updated for the completed phase A re-run. |
| PB3.5i8d2b | `ea86d13` | ✅ docs/STAGE_P_BIS_SESSION_HANDOFF.md — section 6 rewritten for the ladder as executed through i8, replacing the two PB-D36-withdrawn mechanisms and the stale 0.05 SepETH funding gate. |
| PB3.5i8d3a | `aa05ade` | ✅ docs/STAGE_P_BIS_PHASE_A_RECORD.md — section 7 added, recording the 2026-07-29 phase A re-run with the PB-D38 canonical projection set. |
| PB3.5i8d3b | `2b173c0` | ✅ docs/STAGE_P_BIS_PHASE_A_RECORD.md — preamble and Decisions bullet reconciled with the two-run history, closing i8d and rung i8. |
| PB3.5i9a | `3dec467` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — resume-branch banner added ahead of section 6's Step 1, so an operator cannot run the re-broadcast PB-D38 (ii) forbids. |
| PB3.5i9e | `4d0157d` | ✅ script/CreateAureumVault.s.sol — create-only recovery script for the PB3.5i9d rejected fourth transaction, calling factory.create() without re-running DeployAureumVault.run(). |
| PB3.5i9f1 | `0f9c419` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — committed Sepolia deployment record, base-layer nonce ledger 87 to 97 plus the stub-roster pointer, closing the gitignored-artifact gap. |
| PB3.5i9f2 | `a20d346` | ✅ CLAUDE.md — section 8f added, making live-chain broadcast recording a standing rule per chain (Sepolia now, mainnet at Stage R). |
| PB3.5i9f3 | `780b79f` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — gas-limit and provider-ceiling narrative, the nonce-93 recovery account, and the on-chain verification evidence added per CLAUDE.md 8f. |
| PB3.5i9f4 | `aba1443` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — Router at nonce 98 recorded, WETH provenance established by bytecode measurement, drpc partial-method degradation and the cast-code-pipe verification trap logged per 8f. |
| PB3.5i9h | `237ac89` | ✅ .gitignore — Environment block closed to a class rule covering .env and every .env.* overlay, with the committed .env.example exempted, so hand-made backups and future chain overlays cannot be swept into a commit. |
| PB3.5i9i1 | `71298fd` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D39 lock added, adjudicating the ixAetheron/der-Bodensee salt collision. |
| PB3.5i9i2 | `fd040ca` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D39 Decisions row added, mirroring the STAGE_P_BIS_NOTES.md lock landed at 71298fd. |
| PB3.5i9j1 | `fa1d8c5` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D40 lock added, retiring the three-class pool-key taxonomy. |
| PB3.5i9j2 | `3110af0` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D40 Decisions row added, mirroring the STAGE_P_BIS_NOTES.md lock landed at fa1d8c5. |
| PB3.5i9j3 | `54a25ea` | ✅ script/DeployStageP.s.sol, script/DeployStageM.s.sol, script/DeployStageJ.s.sol, script/DeployStageI.s.sol, test/fork/StagePRunRehearsal.t.sol, test/fork/StagePIntegration.t.sol, test/fork/StageMIntegration.t.sol, test/fork/DeployStageJ.t.sol, test/fork/DeployStageI.t.sol — eight pool env keys renamed to MILIARIUM_POOL_nn per PB-D40. |
| PB3.5i9j4 | `88bc49e` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — eight fossil pool keys retired from the slot table and the prefix-explanation paragraph rewritten, per PB-D40. |
| PB3.5i9j6 | `be0c2b9` | ✅ test/fork/DeployStageI.t.sol — EFFICIENCY_ORACLE placeholder added to setUp, mirroring DeployStageH.t.sol; closes a pre-existing gap the PB-D40 (iv) gate surfaced. |
| PB3.5i9j7 | `740201a` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D40 lock closed with the (iv) gate result, the pre-existing EFFICIENCY_ORACLE defect it surfaced, and the measured blast radius, per i9j6. |
| PB3.5i9k1 | `8e4d753` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — section 7 added, the 25-row phase-3 pool record per PB-D39 (vii); paragraph spacing corrected at i9k2 before commit. |
| PB3.5i9k3 | `1abda01` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — section 8 added, slot 02 ixAetheron's second-sender deployment recorded per PB-D39 (v)(3); phase 3 (all 26 Miliarium Aureum pools) complete. |
| PB3.8a | `68c2363` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D41 lock added, the pool-seeding sub-step: the empty-oracle gap, the USD 1,000,000 allocation table, the PB3.8/PB3.9 genesis split and the Stage-R der Bodensee gate. |
| PB3.8b | `791121e` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D41 Decisions row plus the PB3.8 and PB3.9 sub-step rows; PB3 checkpoint amended for seeding, the EMA, the keeper and the post-genesis Bodensee init. |
| PB3.8c | `af43d09` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-011 registered, the der Bodensee genesis-ordering trap (three-token pool needs AuMM before genesis mints any); new Deployment-sequencing hardening class, per PB-D41 (ix). |
| PB3.8d | `d0bca80` | ✅ src/emission/TVLOracle.sol — H-D8 NatSpec corrected at L12 and L19; BODENSEE_POOL documented as stored-for-identity, not enumerated by _directRatio, per PB-D41 (iii). Comment-only, zero behavior change. |
| PB3.8e-pre | `6a30033` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D42 lock added, amending PB-D41 (iii): svZCHF self-map exception (blanket WITH_RATE rule priced every pool at zero), WITH_RATE underlyings absent from the committed stub map, derive-commit-then-assert model for the rung-e derivation. |
| PB3.8e-pre2 | `fbf839e` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D42 Decisions row added; PB3.8 sub-step row amended for the svZCHF self-map exception, the live asset() derivation, and the L12/L19 NatSpec correction range. |
| PB3.8e1 | `d763273` | ✅ test-stubs/DeriveTokenUnderlyings.s.sol — read-only derivation of the Sepolia tokenToUnderlying map; svZCHF self-map precedence per PB-D42 (iii), WITH_RATE underlyings resolved via live asset() per PB-D42 (iv). |
| PB3.8e1b | `05292ca` | ✅ docs/STAGE_P_BIS_NOTES.md — PB16 lesson entry added, files outside foundry's compile roots are invisible to forge build; caught at PB3.8e1 when a green build proved nothing about the one new file it should have gated. |
| PB3.8e1c | `6a420bd` | ✅ CLAUDE.md — PB16 added to the section 11 engineering-lesson index; a cold session now sees the compile-root rule without reading the stage notes. |
| PB3.8e2b | `2617dd0` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D43 lock added, correcting PB-D42 (v): hop-roster entries are underlyings not tokens; svZCHF and raw ZCHF are ineligible/inert as hops, sUSDS-share-vs-underlying is the live trap; permissionless-pool scoping and RB-012/RB-013 carry-forwards recorded. |
| PB3.8e2c | `5101d9a` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D43 Decisions row added; PB3.8 sub-step row corrected from plural asset()-output hop seeds to the single sUSDS-derived underlying. |
| PB3.8e2d | `36bb4ab` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-012 row added (new Indirection-level typing hardening class), recording PB-D43 (vi)'s hop-roster level rule as a Stage-R gate: addHopUnderlying must accept a value tokens resolve TO, never a token address. |
| PB3.8e2e | `3e14060` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-013 row added (new Cross-authority coupling hardening class), recording PB-D43 (vii)'s class-admission-versus-oracle-mapping seam: permissionless VaultClassRegistry finalize can outpace onlyGovernance TVLOracle mapping, degrading to a misleading TVLFloorNotMet revert and a fee-sink retry loop. |
| PB3.8e2f | `8fdc866` | ✅ docs/STAGE_P_BIS_NOTES.md + docs/STAGE_P_BIS_PLAN.md — PB-D43 line-citation fix, L288 to L287 for the _twoHopRatio skip guard (three occurrences: NOTES L694, NOTES L699, PLAN L94), no prose change. |
| PB3.8e3a | `2519fde` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D44 lock added, fixing the committed Sepolia token-underlying map's form: a Solidity library under script/config/ mirroring GaugeGenesisManifest's shape but without named constants (t/u locals, diff-verifiable against the derivation log), chain-scoped by name, NatSpec and a rung-f assertion per RB-010, regeneration-safe only because DeriveTokenUnderlyings is read-only unlike DeployTestnetStubs. |
| PB3.8e3b | `169271f` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D44 Decisions row added; PB3.8 sub-step row rung-e clause now names the artifact, the Solidity library script/config/SepoliaTokenUnderlyings.sol. |
| PB3.8e3c | `d9324ad` | ✅ script/config/SepoliaTokenUnderlyings.sol — committed Sepolia token-to-underlying map, 60 pairs transcribed from test-stubs/pb38e2-derived.log per PB-D44 (no named constants, t/u locals for diff-verifiability); one transcription error at index 20 caught by the source-diff gate and fixed before commit. |
| PB3.8f-pre | `9905e23` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D45 lock added, closing the rung-f wiring-script decisions PB-D44 (vi) deferred: block.chainid assertion over the AUMM_ENV_CHAIN marker, verify-all-then-write-all phasing, per-class asset() versus structural assert rule, and read-before-call idempotency on both append-only setters against the partial-run revert PB3.5 already made a live risk. |
| PB3.8f-pre2 | `f1c3454` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D45 Decisions row added; PB3.8 sub-step row rung-f clause added, naming the wiring script as chain-asserted, verify-all-then-write-all, and idempotent across both append-only setters. |
| PB3.8f | `51fd901` | ✅ script/WireTVLOracleSepolia.s.sol — Sepolia TVLOracle wiring script per PB-D45: block.chainid guard, governance cross-check, hop-seed cross-check against the committed map, two-phase verify-all-then-write-all with per-class asset()-versus-structural assertion, idempotent map writes and both append-only roster calls, der Bodensee read from the oracle's own immutable rather than an env key. |
| PB3.8g-pre | `058bfb1` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D46 lock added, fixing rung g's initialization path before any seeding code lands: every existing initialize call uses an unbroadcastable test-harness callback, so seeding routes through the Aureum Router; WITH_RATE legs fund by real deposit rather than an unbacked share mint; amounts derive from allocation times registered normalizedWeights at one dollar per token; one idempotent deployer-broadcast script per PB-D45's resumability precedent. |
| PB3.8g-pre2 | `172f56c` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D46 Decisions row added; PB3.8 sub-step row rung-g clause added, naming the seeding script as Router-routed given the harness-callback broadcast blocker every existing initialize call shares. |
| PB3.8g1 | `fc59fe1` | ✅ script/config/MiliariumSeedAllocations.sol — slot-keyed PB-D41 (vi) seed-allocation table, 26 rows transcribed from docs/STAGE_P_BIS_NOTES.md L618-L643 in the lock's own band order; band totals sum to exactly 1,000,000, asserted by the consuming script per PB-D46 (v). |
| PB3.8g2 | `23785c5` | ✅ script/SeedMiliariumPoolsSepolia.s.sol — rung-g Miliarium pool seeding script per PB-D46: Router-routed initialization since the existing unlock-callback pattern cannot broadcast, live asset() probe for ERC-4626 legs rather than the committed token map, real deposit funding for share legs rather than an unbacked mint, amounts from allocation times live-read weights and decimals, allocation-sum and chain guards both ahead of the broadcast, idempotent at every step. |
| PB3.8h1 | `baac453` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D47 lock added, recording what the rung-h dry run found: slot 02 registered a token as its own rate provider because the stub map keys tokens and providers in one namespace and a self-providing token shadows itself; rate providers are sealed at registration so the pool is unrecoverable and needs a third-sender redeploy, while the provider it needed was deployed at nonce 14 all along and recovered by arithmetic at zero new cost. |
| PB3.8h2 | `33359f5` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D47 Decisions row added; PB3.8 sub-step row rung-h clause corrected from a mainnet-fork rehearsal to the live-Sepolia dry run that actually ran, naming the slot-02 rate-provider collision it surfaced. |
| PB3.8h3 | `70ae166` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-014 row added (new Deployment-record completeness hardening class), recording the PB-D47 (viii) deploy-script under-recording gap: DeployTestnetStubs creates three contracts per WITH_RATE token and records only one, and whether the other two are recoverable depends on topology rather than effort. |
| PB3.8h4 | `1ed7c9a` | ✅ test-stubs/DeployTestnetStubs.s.sol — rate-provider keys moved to their own STUB_RP_ namespace (stubRpOf), disjoint from the token stubOf map, retiring the PB-D47 self-referential-provider collision; the script stays forward-only per PB-D42 and is never re-run. |
| PB3.8h5 | `7987520` | ✅ script/pools/deploy-miliarium-pool.s.sol + script/DeployDerBodensee.s.sol — _resolveRateProvider reads the STUB_RP_ namespace first with a STUB_ fallback, then probes getRate() on every non-zero resolved provider before returning; the fallback keeps the eight already-live-keyed providers resolvable since DeployTestnetStubs is forward-only per PB-D42, and the probe is what makes that fallback safe against the PB-D47 slot-02 self-referential-provider shadow. |
| PB3.8h5b | `ed5e9e1` | ✅ test/fork/StagePRunRehearsal.t.sol — the rehearsal harness reads der Bodensee's two stub rate providers from the STUB_RP_ namespace that h4 moved them to; the lookup is a direct vm.envAddress bypassing _resolveRateProvider, so neither h5's namespace read nor its legacy STUB_ fallback covered it, and the failure surfaced only because the rehearsal had not been re-run since h4. Four surrounding comments corrected for the split namespace and the renamed derivation. 9/9 fork-green. |
| PB3.8h6a | `6c963f0` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — section 9 added, documenting the slot-02 weETH rate-provider collision and its recovery: the single-namespace stub map returned the weETH stub itself as slot 02's provider, VaultExtension seals TokenInfo at registration with no setter, so the row is superseded rather than corrected; the recovered provider at nonce 14 is identified by wrappedToken() matching the registered stub, costing no new contract or gas. Section 8's row labeled SUPERSEDED, see section 9. Cross-referenced to RB-014 and the STUB_RP_ fix landed at h4/h5. |
| PB3.8h7d | `4e09bda` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — section 10 added, recording the live third-sender redeploy of slot 02 ixAetheron: nonce 0, block 11399372, 4,933,856 gas, identical to the superseded broadcast to the unit and differing only in the constructor address. getPoolTokenInfo against the live pool confirms the weETH leg now carries the recovered ERC4626RateProvider rather than the weETH stub itself, closing PB-D47 in sealed on-chain state. Section 8's superseded row stands unchanged per section 8f. |
| PB3.8h9a | `2590d85` | ✅ script/DeployStageP.s.sol — the production orchestration path seeded GAUGE_REGISTRY_PLACEHOLDER from address(this), which forge script rejects because a script contract is ephemeral under --broadcast and is never deployed; DeployStageP.run() therefore reverted immediately after Stage J, with F, G, H, I, K and L unreachable behind it. Replaced with the in-scope governor: the CCBMultiplier constructor only zero-checks its three registry slots and never calls them, and this slot is sealed to the real GaugeRegistry once the G stack lands, so any non-zero address suffices. The fork deploy() path at L102 keeps address(this) deliberately, where the script contract genuinely exists. Third instance of the test-versus-script class after PB-D31 and PB-D46, and invisible to StagePRunRehearsal 9/9 because a test contract has a real address. Live-Sepolia dry run now completes: 114 transactions, 16 CREATEs covering the full F-through-L set, 36,934,592 gas. |
| PB3.8h9b | `fb64093` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D48 lock added, recording the first live-Sepolia dry run of phase 4 (DeployStageP.run()): the address(this) production-path defect fixed at h9a (2590d85), the third instance of the test-versus-script class after PB-D31 and PB-D46 and invisible to StagePRunRehearsal for the same structural reason each time; the measured shape of a clean run (114 transactions, 16 CREATEs, 36,934,592 gas), answering runbook row 9; the finding that phase 4, 5 and 6 have never been broadcast at all, which is what blocks rung f (the oracle does not yet exist) and rung i; the ordering accident that made the slot-02 redeploy free rather than a governance replaceSlot; and the 114-transaction non-idempotency exposure the orchestration carries, with the resume/--slow posture left open for the next beat. |
| PB3.8h9c | `16f822a` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D48 Decisions row added, mirroring the phase-4 dry-run lock: the address(this) production-path defect at DeployStageP.s.sol L178, forge script's rejection of it under --broadcast, the third instance of the test-versus-script class after PB-D31 and PB-D46, the governor fix, the measured clean-run shape (114 transactions, 16 CREATEs, 36,934,592 gas), and the finding that phases 4 through 6 have never been broadcast, which is what blocks rungs f and i. |
| PB3.8h9d | `4422dd2` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 5 row 9 filled from PB-D48 (114), the phase-4 transaction count answering the PENDING-h cell the runbook has carried since PB-D28. Row 9 is flagged as a dry-run count rather than a broadcast count, since phases 4 through 6 remain unbroadcast and the section's own framing otherwise implies rung i has already occurred. |
| PB3.8h9d2 | `8e7ec53` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 8's phase-A closing step still claimed rows 5 through 10 all wait for the broadcast, contradicting section 5 the moment h9d filled row 9. Corrected to rows 5 through 8 and row 10, with a clause recording that row 9 fills from the PB-D48 rung-h9 dry run rather than from phase A or the broadcast. Surfaced by h9d's own verification, which found the duplicate claim two sections away. |
| PB3.8h9e | `4a49e18` | ✅ CLAUDE.md — section-11 resume-anchor advance to 2026-08-01. Two substring replacements inside the single anchor paragraph (line 446), per the a86d153 precedent: the date, and the whole stale gas-blocked tail. The claim that rungs i8 and i9 are hard-blocked on gas is withdrawn as false — PB3.5 completed, the deployer holds 5.85 SepETH, and the base layer, 87 stubs, twenty-five pools and slot 02 are all live and recorded. Replaced with the PB3.8 state through h9d2: PB-D47 closed across h1 to h8 (the STUB_RP_ namespace split, the recovered weETH provider, the third-sender slot-02 redeploy, the clean 26-of-26 seeding dry run) and PB-D48 at h9 (the address(this) production-path defect, fixed at 2590d85, with phase 4 now completing at 114 transactions and 36,934,592 gas). The blocking finding is recorded plainly: phases 4, 5 and 6 have never been broadcast, which is why rung f cannot run and rung i sits downstream. Carries updated — the absent Etherscan key, the sealed GENESIS_BLOCK at 11477620, the 114-transaction non-idempotency exposure with its unverified resume posture, two unlocked operator-environment hazards, and two stale citations. git diff --numstat reads 1 insertion / 1 deletion, the structural proof the edit stayed inside the paragraph. |
| PB3.8h9e2 | `8c40b1d` | ✅ docs/STAGE_P_BIS_SESSION_HANDOFF.md — the 2026-07-27 status preamble replaced by a dated supersession banner. Lines 3 to 7 carried three claims that section 11 contradicted as of the h9e advance: that the Sepolia sequence was mid-sequence and halted with the base layer dead from nonce 87, that section 11 itself was current only through rung h, and that the NOTES locks therefore outrank it. All three are false — PB3.5 completed at rungs i8 and i9, the base layer and 87 stubs and twenty-five pools and slot 02 are live and recorded, and section 11 was advanced past every lock this file cites. The banner withdraws them by name, redirects to section 11 and the PB-D48 lock at NOTES L771, and states plainly that the phase-4 broadcast is what remains outstanding. The body freezes as history rather than being rewritten or deleted: PB-D36 (vii) removed the void projection keys from .env.sepolia on the express ground that their values survive in this file's section 4, so that archival citation is load-bearing and sections 1 through 6 stay exactly as written. The hazard closed is one of naming — the file advertises itself as the resume anchor, so a cold session opens it by name and would have read a no-further-broadcast-may-occur instruction immediately before sequencing one. git diff --stat reads 5 insertions / 3 deletions, the two interior blank lines matched as context. |
| PB3.8h9f1 | `9d993af` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D49 lock added, closing the UNVERIFIED that PB-D48 (viii) left on the phase-4 resume posture. forge script --help at the pinned 1.6.0-v1.7.0 build f83bad91 states that --resume does not simulate the script again and expects nonces to have remained the same, which retires the vm.setEnv threading hazard outright: the thirteen production-path setEnv writes do thread in-process handles, but no re-execution means they are never re-derived, and the frozen artifact — 114 transactions, 98 CALL and 16 CREATE, every one carrying an explicit nonce, first CREATE MiliariumRegistry at 0x7d — replays correctly because a CREATE address is a pure function of sender and nonce. --slow is locked as the other half of a pair rather than as seasoning, being what makes the stall boundary clean enough for resume to be specified against, at a cost of roughly 23 minutes across 114 confirmed-sequential transactions. Nonce reconciliation before any resume is MANDATORY and stated as a prohibition rather than a recommendation: resume is forbidden until the live deployer nonce equals the next pending transaction's recorded nonce, because section 8f's no-receipts artifact — the same one that made nonces 90 to 92 recoverable only by a manual chain read, and the reason CreateAureumVault.s.sol exists — is precisely what produces a mismatch. The nine-way decomposition alternative is considered and rejected on PB-D13's ground now that re-simulation is off the table. Zero code change; the P10-validated composed path is untouched. Ladder: h9f2 the plan Decisions row, h9f3 the runbook procedure, h9g the broadcast. git diff --stat reads 18 insertions and 0 deletions, the structural proof the append touched nothing above line 787. |
| PB3.8h9f2 | `b65fcde` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D49 Decisions row added, mirroring the NOTES lock at docs/STAGE_P_BIS_NOTES.md L789-L805. Records the forge script --resume determination: the pinned 1.6.0-v1.7.0 build's own --help text states resume does not re-simulate and requires nonces to have remained the same, which retires the vm.setEnv threading hazard PB-D48 left UNVERIFIED since the frozen 114-transaction dry-run artifact already carries every CREATE and CALL at its correct recorded nonce. --slow locked as the other half of the posture rather than an optional flag; nonce reconciliation before any resume locked MANDATORY against section 8f's no-receipts partial-broadcast shape; the nine-way decomposition alternative rejected on PB-D13's ground now that resume is confirmed replay-based. Zero code change. git diff --stat reads 1 insertion / 0 deletions. |
| PB3.8h9f3a | `4694cce` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 9 added, the phase-4 stall-recovery procedure per the PB-D49 lock. Locks the pre-flight as a hard gate rather than a suggestion: --resume is FORBIDDEN until the deployer's live confirmed nonce equals the broadcast artifact's first-unsent recorded nonce, six ordered R1 through R6 steps walking active-env, confirmed-nonce, pending-nonce, artifact-boundary, comparison and reconciliation in that sequence so a mempool-pending transaction cannot be mistaken for a landed one. Field semantics — null hash means unsent, a non-null hash with no matching receipt is the section-8f gap — are stated as confirmed at PB3.8h9f against both a dry-run and a live broadcast artifact rather than assumed. The resume invocation itself, forge script script/DeployStageP.s.sol:DeployStageP --broadcast --resume --slow --account aumm-sepolia, is checked against the foundry scripting guide's own example (--resume accompanies --broadcast, confirmed 2026-08-02 via WebFetch against getfoundry.sh rather than assumed from local --help text alone) rather than guessed, closing the one open question --help left unanswered. A What-not-to-do list closes with the operational reading of PB-D49 (vii): no fresh re-invocation after a partial run, no hand-editing the gitignored broadcast artifact, and nonce exclusivity on the deployer extended from phase A's four-transaction window to a 114-transaction one. git diff --stat reads 57 insertions and 0 deletions, an append with nothing above line 276 disturbed. |
| PB3.8h9f3b | `46b4076` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — section 9 wired into the two places an operator would otherwise reach it too late. Section 0 gains the nonce-exclusivity bullet as a standing safety-posture rule rather than a fact buried in section 9's prose, extending the deployer-sends-nothing discipline PB-D49 (vii) states for phase 4 to the same shelf as the existing DEPLOYER_PRIVATE_KEY and mainnet-RPC bullets. The phase 4 Command bullet gains --slow as mandatory, not optional, with the one-line reason a stall's clean confirmed-versus-unsent boundary is what --resume needs to recover from. A closing bullet names phase 4 as the only phase with its own recovery procedure and points at section 9 rather than restating it, with the one sentence that matters most repeated once more: no re-invocation from the top after a partial run. git diff --stat reads 3 insertions and 1 deletion, the Command-line replacement plus the two new bullets. |
| PB3.8h9f4 | `2e75cb5` | ✅ docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md — nine commands corrected from a broken --rpc-url $SEPOLIA_RPC_URL form to the working foundry alias. The variable form fails in a plain shell: zsh expands the command line before foundry ever starts, zsh has never read .env, and the empty expansion makes cast and forge script exit on a missing-value error. Surfaced live during the h9g pre-flight when the operator ran the drafted command and hit exactly that error on checks 6 and 7. Fixed to --rpc-url sepolia everywhere except the resume invocation, which foundry issue 4346 reports as not resolving aliases at all — that one line instead lifts SEPOLIA_RPC_URL out of .env directly by command substitution, needing neither an export nor a set -a and leaving the section-1 posture (foundry alone auto-loads .env) intact rather than contradicted. Section 1's own sourcing claim gains one clause naming the mechanism explicitly, since the claim was true of foundry's env reads and silently false of shell-side variable expansion on nine command lines including three in the newly-landed section 9 recovery procedure. git diff --stat reads 9 insertions and 9 deletions, nine whole-line replacements and nothing structural changed. |
| PB3.8h9h1 | `23140d4` | ✅ docs/STAGE_P_BIS_PHASE_4_LEDGER.md — the complete 114-transaction ledger for the phase-4 broadcast, committed as its own evidence file. Section 8f requires every broadcast transaction to get a committed row, and broadcast/ is gitignored, so forge's run-latest.json is not a durable record; this file is. Generated directly from that artifact by script rather than transcribed, on the PB-D44 derive-commit-then-assert pattern, so verification is a regeneration diff rather than a read-back and 114 rows of hashes carry no transcription risk. Holds all 114 rows with nonce, type, contract, function, target or deployed address, block, gas used and transaction hash. Shape: 114 transactions, 114 receipts, every one successful, nonces 125 to 238 contiguous, blocks 11403323 to 11403470, 27,666,605 gas total. Split out from the main deployment record on the STAGE_P_BIS_PHASE_A_RECORD precedent so the record keeps its 16 deployed addresses and narrative readable while losing no per-transaction detail. Deployed addresses, aggregates and narrative land next as record section 11. |
| PB3.8h9h2 | `34b2fa7` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — phase 4 recorded per section 8f. New section 11 carries the sixteen contracts DeployStageP.run() deployed at nonces 125 to 238, with address, block, gas and hash each, plus the aggregate shape: 114 transactions, 114 receipts, every one successful, blocks 11403323 to 11403470, 27,666,605 gas for 0.029383870046196392 ETH. Narrative records what no artifact holds — why the estimate ran a third high (forge's buffer plus a halved gas price), why the single most consequential transaction at nonce 237 appears with no contract name (the Vault builds into out-vault under the PB-D34 profile, so forge could not resolve its ABI), that post-conditions passed in simulation and were therefore re-read on chain afterwards with all four confirmed, the pre-flight state including the nonce-125 reading, and that the PB-D49 resume path was provisioned and never needed. Section 10 gains the nonce-124 line: the deployer funded the third sender 0.05 SepETH at block 11398896, a plain value transfer that left no broadcast artifact and had gone unrecorded until phase 4's nonce arithmetic exposed the gap between the last pool at 123 and the first orchestration transaction at 125. Section 6 points at the 114-row ledger. Verification was a source diff rather than a read-back: all 32 hex strings in the new table were confirmed present in the generated ledger, zero mismatches. git diff --stat reads 37 insertions and 0 deletions. |
| PB3.8h9i | `fd409d7` | ✅ CLAUDE.md — section-11 resume-anchor advance to 2026-08-02. Four substring replacements inside the single anchor paragraph at line 446, per the a86d153 and 4a49e18 precedents. The bolded blocking finding that phase 4 has never been broadcast is withdrawn as false and replaced with the h9g result: 114 transactions at nonces 125 to 238 contiguous, 114 receipts all successful, 16 CREATEs, blocks 11403323 to 11403470, 27,666,605 gas for 0.0294 ETH, with the four post-conditions re-read on chain rather than inherited from the simulation that asserted them — the Vault authorizer now AureumGovernanceAuthorizer, GENESIS_BLOCK 11477620 agreeing across three contracts, the CCB seal bound, slot 01 gauged, deployer nonce 239 equal to 125 plus 114. Records both evidence files and why the ledger exists at all, broadcast being gitignored. The Next clause moves from the broadcast to PB3.8 rung f, unblocked for the first time because TVL_ORACLE now exists at nonce 126, and carries the PB-D49 closure of the resume-posture question PB-D48 (viii) left open plus the h9f4 rpc-url shell-expansion defect that the pre-flight surfaced. Etherscan carry widened to include the sixteen phase-4 contracts. One archaeology finding recorded: deployer nonce 124 was an unlogged 0.05 SepETH transfer funding the PB-D47 third sender, exposed by phase 4's own nonce arithmetic. git diff --numstat reads 1 insertion and 1 deletion, the structural proof the edit stayed inside the paragraph. |
| PB3.8h9j | `3a1fa6a` | ✅ PB17 engineering lesson recorded — a tool that reads .env for its own configuration does not make $VAR work on the command line invoking it, since the shell expands the line before the tool ever launches and has never read that file. Caught at the h9g pre-flight when the runbook's own documented --rpc-url $SEPOLIA_RPC_URL form died on a missing-value error, and the lesson is less the mechanism than its history: the same command form had been silently routed around by hand across all of PB3.5's base-layer and pool broadcasts without the document ever being corrected, leaving section 9's stall-recovery procedure carrying the broken form into the one scenario — a stalled 114-transaction broadcast — where an operator can least afford a command that does not run. docs/STAGE_P_BIS_NOTES.md gains the PB17 section; CLAUDE.md's engineering-lesson index gains the one-line bullet immediately after PB16, same shape: a passing outcome mistaken for a passing check. git diff --numstat reads 4/0 and 1/0, pure appends in both files. |
| PB3.8i1a | `803150f` | ✅ docs/STAGE_P_BIS_TVL_WIRING_LEDGER.md — the 62-transaction ledger for the rung-f TVL oracle wiring broadcast, committed as its own evidence file on the phase-4 ledger precedent. Section 8f requires every broadcast transaction to get a committed row and broadcast/ is gitignored, so forge's run-latest.json is not durable; this file is. Generated from that artifact by script rather than transcribed, per PB-D44 derive-commit-then-assert, so verification is a regeneration diff. Shape: 62 transactions, 62 receipts, every one successful, nonces 239 to 300 contiguous, blocks 11404897 to 11404966, 3,264,803 gas against a 4,509,460 estimate. Carries an arguments column the phase-4 ledger did not need, recording all 60 token-to-underlying pairs as actually sent, so the committed map in script/config/SepoliaTokenUnderlyings.sol and the chain can be diffed by a later reader with no RPC access. Two entries are worth finding in it: nonce 239 is the index-0 hop seed the script asserts against HopSeedMismatch, and nonce 240 is svZCHF mapping to itself, the PB-D42 (iii) numeraire exception whose failure mode is a silent zero tvl. Nonces 299 and 300 follow all sixty writes, making the PB-D45 ordering requirement visible in the record rather than only enforced in code. |
| PB3.8i1b | `22776b6` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — section 12 records the rung-f TVL oracle wiring per section 8f. 62 transactions at nonces 239 to 300 contiguous, blocks 11404897 to 11404966, 3,264,803 gas for 0.00341643207547946 ETH, all 62 receipts successful: sixty setTokenUnderlying writes, then addConstellationPool seating der Bodensee, then addHopUnderlying seeding the PB-D43 (vi) intermediate. No address table, because the run deploys nothing; the oracle itself is section 11's nonce-126 row. Narrative carries what the artifact cannot. The PB-D45 ordering requirement is shown to have mattered by the gas rather than asserted — the roster call cost 252,390 against roughly 48,676 for a map write, because it appends under every token already resolving non-zero, so running it before the map would have cost a fraction and left a venue invisible to _directRatio. Verification was re-read on chain rather than inherited: roster true, hop true, svZCHF self-mapping, deployer nonce 301 equal to 239 plus 62 — the svZCHF entry being the one whose failure mode is a silent zero tvl with no revert and no event. Records why this script needs no section-9 equivalent: PB-D45 (iv) idempotency means a stall recovers by re-running rather than by --resume, the exact inverse of phase 4, and --slow was carried for RPC politeness rather than correctness. Section 6 gains the ledger pointer. Verification was a source diff: every address in the new section confirmed present in the generated ledger. git diff --stat reads 15 insertions and 0 deletions. |
| PB3.8i2-PBD50 | `f5be388` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D50 lock added, disposing the TVLOracle weight-normalisation finding as FIX with an H-D9 amendment rather than a silent patch. The seeded 26-pool constellation showed tvl() summing to 1,271,468 svZCHF against ~987,000 expected with no trade yet executed, and ixHelvetia's closed form — 2800 times 4/3 plus 11200 equals 14933.33 recurring, matching the on-chain reading to the repeating digit — traces the gap to _venueRatio's bare balance ratio, which H-D9 specifies verbatim (no pool-type-specific curve math, weighted ratios, consumed) and justifies as exact at AMM parity. That justification is false for any venue where quote and base weights differ, which is every venue in the constellation; the bias is a fixed multiplicative constant per venue rather than off-parity noise, so it survives trading rather than washing out under it, and slots 11 and 17 at identical 70,000 dollar allocations reading 34 percent apart is H-D9's own named escape clause — materially wrong F-7 rankings — firing pre-mainnet. Fix is narrow: _venueRatio gains the wBase over wQuote term, no Jacobian, no invariant math; everything else in the pipeline (token map, cross-venue averaging, two-hop fallback, EMA cadence) is untouched. Names its own new trust surface — normalized weights are pool-self-reported rather than Vault-authoritative, the same F-12 provenance caveat GaugeEligibility already carries — as a typed-domain question to settle before code per section 12. Cost stated explicitly: reopens audited TVLOracle.sol pre-audit, amends a Stage-H LOCKED decision, needs a re-gate across unit, fork and formal surfaces. PB-D51 reserved for the independent seeding-side finding — PB-D46 funds pools by token count rather than by value, latent on Sepolia's fiat stubs, fatal on mainnet — explicitly not merged with this lock since neither defect explains the other's Sepolia face. git diff --numstat reads 24 insertions and 0 deletions, a pure append. |
| PB3.8i2a | `f69e2dd` | ✅ docs/STAGE_P_BIS_POOL_SEEDING_LEDGER.md — the complete 389-transaction ledger for the Miliarium pool seeding broadcast, committed as its own evidence file on the phase-4 and rung-f precedent. Section 8f requires every broadcast transaction to get a committed row and broadcast/ is gitignored, so forge's run-latest.json is not durable; this file is. Generated from that artifact by script rather than transcribed, per PB-D44 derive-commit-then-assert, so verification is a regeneration diff and 389 rows of hashes carry no transcription risk. Shape: 389 transactions, 389 receipts, every one successful, nonces 301 to 689 contiguous, blocks 11405061 to 11405532, 33,643,685 gas against a 47,941,303 estimate; breakdown 177 approve, 127 mint, 59 deposit, 26 initialize. The arguments column carries the seeding amounts as actually sent, and earns its place here more than in either predecessor: MiliariumSeedAllocations is authored rather than derived, so unlike the token map it has no diff to verify it against, and this ledger is the only external evidence of what that table produced. Nonce 301 is checkable by hand — 18,200e18 minted is 70,000 times the 0.26 weight at one dollar per token, the PB-D46 derivation — followed at 302 by the approval to the share stub ahead of the deposit, the WITH_RATE funding path visible in sequence. |
| PB3.8i2-PBD51 | `d23cda6` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D51 lock added, recording the seeding-side value-weight finding as a Stage-R gate with no Sepolia remediation. Balancer normalized weights are shares of pool VALUE, so funding to declared weights requires balance_i = weight_i times V divided by P_i; PB-D46 (iv) funds by allocation times weight, which is that formula with every price silently set to one. The lock is careful about what it does not overturn: PB-D46 (iv) is correct and correctly scoped, deriving the internal consistency itself and closing with the only coherent choice for stubs carrying no feed, which is true because the stubs are fabricated 1:1 placeholders where a flat one-dollar fiat is not an approximation of a real price but the only price that exists. The gap is a missing boundary rather than a mistaken derivation — nothing states the convention is testnet-only, and the load-bearing sentence names no price step because there is none reads as general while being a testnet accommodation, in a lock a Stage-R author would consult as the authority on capitalisation. Names the irony in its own location: PB-D46 (iv) cites PB-D26, a mispriced pool that registers cleanly, as its reason for reading weights from the Vault, while the convention it defends produces exactly that failure on mainnet where an 80/20 WETH-and-stable pool funded by token count puts nearly all its value in one leg. Disqualifies one price source outright — TVLOracle must not supply P_i while PB-D50 is open, since funding legs through the same biased estimator yields pools that look correct to the oracle and are wrong in fact, a self-concealing outcome. Independence from PB-D50 stated explicitly so no single disposition can read as closing both. Ladder: plan Decisions row, RB-015 register row, section 11 pre-mainnet carry, Stage-R price-source adjudication. git diff --numstat reads 20 insertions and 0 deletions, a pure append. |
| PB3.8i2b | `9f5bb7a` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — section 13 records the Miliarium pool seeding per section 8f. All 26 pools capitalised: 389 transactions at nonces 301 to 689 contiguous, blocks 11405061 to 11405532, 33,643,685 gas for 0.035700732881202711 ETH, all 389 receipts successful — 177 approve, 127 mint, 59 deposit, 26 initialize. No address table, the run deploys nothing. Records the first externally-observable protocol state: every pool funded, TVLOracle.tvl non-zero across all 26 confirming no PB-D42 (ii) silent map failure, deployer nonce 690 equal to 301 plus 389. Explicitly declines to claim oracle TVL matches accounting allocation — the roster sums to 1,271,468 svZCHF against roughly 987,000 expected, and that gap is attributed to PB-D50's weight-normalisation finding rather than to this broadcast, with reconciliation deferred until that lock closes. Slot 02's 3,211.91 reading against PB-D41 (v)'s approximately-zero expectation is recorded as a lock-versus-observation discrepancy with its ixEDEL-leg mechanism named, not as a seeding defect. The funding convention itself is recorded as correct and complete under PB-D51's bound — the only price that exists for a fabricated stub is one dollar, so quantity-proportional funding is value-proportional funding here, with no Sepolia remediation implied. Gas landed a third under estimate for the third consecutive broadcast, and the dry run independently confirmed phase 4's hook-binding changed initialization cost by nothing. git diff --stat reads 21 insertions and 0 deletions. |
| PB3.8i2c | `7ffe03b` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D50 and PB-D51 Decisions rows added, mirroring the locks at docs/STAGE_P_BIS_NOTES.md L811 and L835. PB-D50 records that the live seeded constellation disproves H-D9's exactness lemma: with 26 pools capitalised and no swap executed, tvl() sums to 1,271,468 svZCHF against roughly 987,000 accounting, ixHelvetia closes the arithmetic without remainder through nine venues at ratios 4 and 1 giving an unweighted mean of 4/3, and the code is faithful to its lock so the defect is the lock's accuracy claim rather than drift — a bare balance ratio is parity-exact if and only if quote and base weights are equal, which no venue satisfies, making the bias a fixed multiplicative constant that survives trading rather than off-parity noise that arbitrage bounds. Disposition FIX with H-D9 amended, the minimal weight term and explicitly not StableMath, with the pool-self-reported-weights trust surface flagged as a typed-domain blocker to settle before code and the PB-D13 cost named. PB-D51 records the independent seeding-side finding as a Stage-R gate: Balancer weights are value weights, PB-D46 (iv) is correct and correctly scoped for stubs carrying no feed, and the gap is a missing chain boundary rather than a mistaken derivation, with TVLOracle disqualified as the Stage-R price source while PB-D50 is open. Independence stated in both rows so no single disposition reads as closing both. git diff --stat reads 2 insertions and 0 deletions. |
| PB3.8i2d | `b669bf1` | ✅ CLAUDE.md — section-11 resume anchor advanced past the rung-f wiring and the pool seeding, and the two design findings recorded. Two substring replacements inside the single anchor paragraph at line 446. The phase-4 record is extended rather than displaced: rung f ran WireTVLOracleSepolia as 62 transactions at nonces 239 to 300 for 3,264,803 gas, sixty map writes then the roster and hop calls in the PB-D45 order, with the roster call's 252,390 gas against roughly 48,676 for a map write standing as the evidence that ordering mattered; rung i2 then ran SeedMiliariumPoolsSepolia as 389 transactions at nonces 301 to 689 for 33,643,685 gas, capitalising all 26 Miliarium pools with every receipt successful, leaving the deployer at nonce 690. Both ledgers and deployment-record sections 12 and 13 are cited. The findings are stated with reproducible evidence rather than by reference: PB-D50 disposes as FIX with an H-D9 amendment because a bare balance ratio is parity-exact only at equal weights and no venue qualifies, with the 1,271,468 against 987,000 sum, ixHelvetia's nine-venue mean of 4/3, and the 33.75 percent spread between identically funded slots 11 and 17 that fires H-D9's own escape clause; PB-D51 is a Stage-R gate on value-weight funding with TVLOracle disqualified as the price source while PB-D50 is open. Independence of the two is stated. The Next clause moves from rung f to the H-D9 amendment, the RB-015 row, the clause (ix) provenance adjudication, and the _venueRatio fix with its re-gate, noting phases 5 and 6 outstanding and PB3.6 and PB3.7 not started. git diff --numstat reads 1 insertion and 1 deletion, the structural proof the edit stayed inside the paragraph. |
| PB3.8i3 | `6dab12e` | ✅ docs/STAGE_H_NOTES.md — H-D9 v2 amendment block inserted, withdrawing only the exact-at-AMM-parity accuracy lemma per PB-D50. |
| PB3.8i4 | `a07ea8c` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D52 lock appended, resolving PB-D50 clause (ix) and pricing the redeploy cascade. |
| PB3.8i5 | `3ba648e` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D52 Decisions row added at L103, mirroring the lock at docs/STAGE_P_BIS_NOTES.md L855. |
| PB3.8i6a | `8081ddb` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-015 row added for PB-D51, plus the new Degenerate-parameter scoping hardening class it declares. |
| PB3.8i6b | `4b264da` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-016 row added for the GaugeEligibility provenance-ordering defect, filed under Contract-identity verification per PB-D52 (iv). |
| PB3.8i7 | `8ee0535` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D53 lock appended, settling the four typed-domain questions the _venueRatio build must answer before code lands. |
| PB3.8i7b | `3d669ca` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D53 Decisions row added at L104, mirroring the lock at docs/STAGE_P_BIS_NOTES.md L879. |
| PB3.8i8 | `870fc2f` | ✅ src/emission/TVLOracle.sol — _venueRatio corrected with the weight term, factory-provenance gate, and two skip guards, per PB-D53. |
| PB3.8i9-pre | `eb03b87` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D54 lock appended, recording the pre-flight finding that the unit suite prices venues that do not exist and re-scoping the constructor-ripple ladder. |
| PB3.8i9a | `95a5158` | ✅ test/fork/mocks/StageHMocks.sol — MockWeightedPool and MockBasePoolFactory doubles added per PB-D54 (vi). |
| PB3.8i9b1 | `4f830df` | ✅ script/DeployStageF.s.sol — approvedFactory threaded through the TVLOracle construction, reading WEIGHTED_POOL_FACTORY per PB-D53 (vii). |
| PB3.8i9b2 | `2c2d016` | ✅ test/unit/TVLOracle.t.sol — approvedFactory threaded through all seven constructions, sixth zero-address guard test added, immutable witnessed. |
| PB3.8i9b3-pre | `b6806c6` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D55 lock appended, recording the two-factory finding and the BODENSEE_POOL provenance exemption. |
| PB3.8i9b3a | `0eec0d4` | ✅ src/emission/TVLOracle.sol — BODENSEE_POOL exempted from the provenance gate per PB-D55 (v). |
| PB3.8i9b3b | `205ca80` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D54 and PB-D55 Decisions rows added, mirroring the locks at docs/STAGE_P_BIS_NOTES.md L897 and L913. |
| PB3.8i9b3 | `529b252` | ✅ approvedFactory threaded through the last three TVLOracle construction sites, completing the eleven-site PB-D53 (vii) ripple. |
| PB3.8i9c1 | `2a0c1cb` | ✅ test/unit/TVLOracle.t.sol — _setComposition rebuilt to give every priced venue real code, equal weights and factory provenance via vm.etch, centralizing the fix at one helper per PB-D54 (iv). |
| PB3.8i9c2 | `cba6840` | ✅ test/whitehat/F19_uninitializedVenueLiveness.t.sol — _setVenue rebuilt to give VENUE_A and VENUE_B real code, equal weights and factory provenance, applying the TVLOracle.t.sol _setComposition precedent to this file's own helper shape. |
| PB3.8i9d1 | `91675ab` | ✅ test/unit/TVLOracle.t.sol — test_tvl_venueMultiTokenSummingPerUnderlying corrected from 100e18 to 200e18, the one pre-existing assertion the PB-D50 weight term moves. |
| PB3.8i9d2 | `46aa8ec` | ✅ test/unit/TVLOracle.t.sol — _setCompositionWeighted helper and the two unequal-weight PB-D54 (vi) tests added, proving the weight term applies and aggregates in lockstep. |
| PB3.8i9d3 | `db5f1d6` | ✅ test/unit/TVLOracle.t.sol — the three PB-D54 (vi) skip-guard tests added, completing the five-test roster. |
| PB3.8i9f | `d95c550` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D56 lock appended, recording the green unit re-gate and correcting PB-D54 (iv) by measurement. |
| PB3.8i9f2 | `5a3c26b` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D56 Decisions row added, mirroring the lock at docs/STAGE_P_BIS_NOTES.md L929. |
| PB3.8i9g | `cd72b0f` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D57 lock appended, recording the mainnet-fork provenance gap and the two-factory disjunctive gate that retires the PB-D55 exemption. |
| PB3.8i9h | `36dadc3` | ✅ src/emission/TVLOracle.sol — approvedFactoryAureum immutable added, provenance gate made disjunctive across both weighted-pool factories, BODENSEE_POOL exemption retired per PB-D57. |
| PB3.8i9i1 | `3dd731c` | ✅ script/DeployStageF.s.sol — approvedFactoryAureum threaded through the TVLOracle construction, reading AUREUM_WEIGHTED_POOL_FACTORY per PB-D57 (vii). |
| PB3.8i9i2 | `7ce0046` | ✅ test/unit/TVLOracle.t.sol — approvedFactoryAureum threaded through all eight constructions via a second, distinct MockBasePoolFactory, seventh zero-address guard test added, immutable witnessed. |
| PB3.8i9i3 | `500892f` | ✅ approvedFactoryAureum threaded through the last three TVLOracle constructions, putting wpf and awpf into their true distinct slots. |
| PB3.8i9i4 | `6040636` | ✅ test/unit/TVLOracle.t.sol — test_tvl_venueFromSecondFactoryOnly_isAccepted added, exercising the disjunction branch no other test reaches. |
| PB3.8i9j | `0813016` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D58 lock appended, recording the PB-D57 re-gate as green at every level and closing the sub-ladder. |
| PB3.8i9k | `bdfc628` | ✅ CLAUDE.md — section-11 resume anchor advanced past the entire PB-D50 through PB-D58 chain, which it previously did not mention at all. |
| PB3.7a | `edfd6a7` | ✅ script/config/SepoliaPhase4Addresses.sol — committed library holding the sixteen phase-4 Sepolia addresses, first PB3.7 rung and the RB-014 discharge for this set. |
| PB3.8i10 | `ceabcfd` | ✅ test/fork/PB52SepoliaOracleEvidence.t.sol — the PB-D52 (viii) Sepolia-fork evidence test. |
| PB3.8i10b | `d1190cf` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D59 lock appended, recording the PB-D52 (viii) Sepolia-fork evidence test as 3 of 3 green against live seeded state. |
| PB3.8i11 | `d17449a` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — section 13 discloses that the live TVLOracle remains pre-fix by decision, correcting two statements that had gone stale. |
| PB3.8i11b | `97e8cb0` | ✅ CLAUDE.md — section-11 resume anchor advanced past the entire PB-D50 through PB-D58 chain, which it previously did not mention at all. |
| PB3.8i12 | `7dd6a82` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D60 lock appended, recording the formal re-gate as 55 of 55 with zero rework required, closing the last unmeasured surface of the PB-D50 through PB-D59 chain. |
| PB3.8j | `—` | docs/STAGE_P_BIS_PLAN.md — the PB3.5, PB3.7 and PB3.8 Completion Log rows, 193 of them generated from git log rather than transcribed per the PB-D44 derive-rather-than-transcribe precedent, plus this row; the log is current through PB3.8i12 — this commit |
| PB3.8k | `cc65880` | ✅ CLAUDE.md — PB18 lesson appended, the em-dash sentinel must be counted mechanically rather than predicted by eye, closing the gap this session's own four-miss pattern exposed. |
| PB3.8l | `9e25144` | ✅ docs/STAGE_P_BIS_NOTES.md — PB18 lesson entry appended, the em-dash miscount traced to mixing grep -c (matching lines) with grep -o piped to wc -l (occurrences) as if they shared units. |
| PB3.8m | `c000358` | ✅ docs/STAGE_P_BIS_PLAN.md — Completion Log rows for PB3.8k and PB3.8l; the log reached PB3.8l and stopped there, the row for this commit itself never landing, which is the gap the PB3.8x family closes. |
| PB3.8n | `21317b7` | ✅ docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md — section 14 appended, recording the phase 5 F-09 Router seat at nonces 690 and 691 and the setGovernanceModule authority finding it surfaced. |
| PB3.8o | `da84386` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D61 lock appended, the hook governanceModule one-shot binding that structurally disqualifies AureumGovernance as its target on every chain, carried to Stage R as RB-017. |
| PB3.8p | `0b97d3c` | ✅ test/whitehat/F01_quorumSnapshotTiming.t.sol — F-21 PoC added; the CompositionChallenge branch of _voteSucceeded succeeds with zero votes at a zero-supply snapshot where the Gauge and Fee branch defeats, confirmed by a same-fixture control pair and driven through queue and execute to slot capture. |
| PB3.8q | `50cf99d` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D62 lock appended, F-21 adjudicated severity High and disposition FIX across eight clauses: the vacuous denominator, the unconditional 60-day EMA-maturity window, the structural absence of any defense inside it, the absent administrative remedy, and the pre-flight blast radius. |
| PB3.8r | `5085947` | ✅ docs/STAGE_P_BIS_PLAN.md — PB-D62 Decisions-table row added. The audit for this rung also found the table carries no rows for PB-D57 through PB-D61, a five-lock gap this row now sits above. |
| PB3.8s | `1fc0c66` | ✅ src/governance/AureumGovernance.sol — the F-21 fix; _voteSucceeded caches the snapshot supply into snapshotSupply on a single getPastTotalSupply read and returns false when it is zero, rejecting the vacuous quorum ahead of either majority branch. Returns rather than reverts, so state() stays total over non-existent proposals. |
| PB3.8t | `ccccc19` | ✅ test/whitehat/F01_quorumSnapshotTiming.t.sol — the PoC inverted into regression tests; the composition case asserts Defeated with queue reverting ProposalNotSucceeded and the slot unmoved, and the stale F-06 StillOpen carry test is replaced by a real-1e18-turnout zero-snapshot Defeated case. Three tests changed rather than the two first sized. Part-A 961/961 green. |
| PB3.8u | `849566f` | ✅ docs/white_hat/AUREUM_WHITEHAT_OUTPUT.md — F-21 findings row added at Status Fixed, surface S8/S9, severity High, confidence Confirmed; the 8404-character count served as the backtick-strip sentinel. |
| PB3.8v | `0261619` | ✅ CLAUDE.md — section 11 updated for F-21 and dated 2026-08-05; roster corrected from F-01 through F-20 at 18 Fixed to F-01 through F-21 at 19 Fixed, with the formal/ bundle recorded as needing no re-gate. |
| PB3.8w | `1118b31` | ✅ CLAUDE.md — section 11 Next sentence corrected; the formal/ re-gate and the phase 5 Router seat were stale pointers to completed work per PB-D60 and PB3.8n. The replacement carried forward a third, unverified claim about this Completion Log, corrected at PB3.8y. |
| PB3.8x | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — the PB3.8m through PB3.8w rows plus this row, derived from git log rather than transcribed per the PB-D44 precedent; the log is current through PB3.8x — this commit |
| PB3.8y | `f43914c` | ✅ CLAUDE.md — section 11 corrected again; the PB3.8w replacement sentence had itself carried an unverified claim that this Completion Log was unlogged since PB3.4f2, false because PB3.8j had already backfilled 193 rows |
| PB3.6a | `494ed68` | ✅ docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-017 row plus a new Irreversible-authority binding hardening class, backfilling the PB-D61 hook governanceModule disqualification as a Stage-R gate |
| PB3.6b-F22a | `c060c6a` | ✅ test/fork/F22_vaultAdminSurfaceUnreachable.t.sol — the F-22 PoC, 3/3 fork-green; three faces separating permission from expressibility, the total canPerform grant, the Vault accepting setProtocolFeeController under a governance prank, and the multisig taking SenderNotAllowed |
| PB3.6b | `532ec4b` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D63 lock appended, F-22 adjudicated severity Critical and disposition FIX-in-source-for-Stage-R across eleven clauses |
| PB3.6c | `657739b` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D64 lock appended, the F-22 build shape settled and PB-D63 (ix) amended from one proposal type to three |
| PB3.6d-a1/a2 | `5bef424` | ✅ src/governance/AureumGovernanceAuthorizer.sol and test/unit/AureumGovernanceAuthorizer.t.sol — layer one; the emergency action set widened from two actionIds to four, pairing each entry with its inverse, unit suite 12 to 14 with the fuzz-exclusion assume set repaired to all four |
| PB3.6d-b1 | `4e15d3c` | ✅ src/governance/AureumGovernance.sol — layer-two declarations only: three appended ProposalType values, the IAuthorizer import, the AuthorizerNotContract error, header NatSpec from three proposal types to six. Part-A 963/963 |
| PB3.6d-doc1 | `db9ee28` | ✅ CLAUDE.md — section 11 advanced to the F-22 mid-ladder state, with three claims the finding invalidated corrected |
| PB3.6d-b | `a8cfa8b` | ✅ src/governance/AureumGovernance.sol — layer-two expressibility: the fourteenth Proposal member newAuthorizer, the seventh _createProposal parameter, the VaultAuthorizerChange two-thirds join, the trailing bare else converted to an explicit six-branch chain with no catch-all, and the three propose entries. Part-A 963/963 unchanged |
| PB3.6d-c | `957ec73` | ✅ test/unit/AureumGovernance.t.sol, test/fork/StagePIntegration.t.sol and test/fork/F22_vaultAdminSurfaceUnreachable.t.sol — twelve reachability tests, the mainnet-fork keystone witness driving setAuthorizer end to end, and the PoC reframed from proof-of-defect to accepted-residual attestation. Part-A 975/975; fork 14/14 and 3/3 |
| PB3.6d-d | `0c6024c` | ✅ docs/STAGE_A_PLAN.md, src/vault/AureumVaultFactory.sol and test/fork/DissolutionGovernanceHandoff.t.sol — the three stale-artifact corrections PB-D63 (vii) names; comments and prose only, no behavioral change |
| PB3.6d-e | `8d394a0` | ✅ docs/white_hat/AUREUM_WHITEHAT_OUTPUT.md — F-22 findings row at Status Fixed, surface S8, severity Critical, confidence Confirmed, with the accepted residual named explicitly so Fixed does not imply all eleven stranded entries were restored |
| PB3.6d-f1 | `e3521e1` | ✅ docs/STAGE_P_BIS_NOTES.md — PB-D64 clause (x) appended, recording the AuthorizerNotContract signature that had lived only in the b1 commit |
| PB3.6d-f2 | `263fd5d` | ✅ docs/STAGE_P_BIS_PLAN.md — the PB-D63 and PB-D64 Decisions rows appended after PB-D62; the PB-D57 through PB-D61 five-lock gap deliberately left open as separate pre-existing work |
| PB3.6d-f3 | `94e125d` | ✅ docs/STAGE_P_BIS_PLAN.md — the F-14 Surfaces row renumbered from PB3.6 to PB3.10 and relocated below the PB3.7 row so the table stays ascending by sub-step |
| PB3.6d-f4 | `3f28b0e` | ✅ docs/STAGE_P_BIS_PLAN.md — the stale PB3.6 roadmap bullet rewritten to describe the F-22 fix, and a PB3.10 bullet added after PB3.9 carrying the F-14 content verbatim |
| PB3.6d-f5 | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — the PB3.8y row plus the PB3.6a through PB3.6d-f4 rows plus this row, derived from git log rather than transcribed per the PB-D44 precedent; the log is current through PB3.6d-f5 — this commit |
| PB3.6d-f6 | `3aeaa2e` | ✅ CLAUDE.md — the section 11 F-22 block rewritten from mid-ladder-open to fixed; all six PB-D64 (ix) rungs recorded with their commit hashes, and the accepted residual named explicitly so Fixed does not read as full closure of every stranded entry |
| PB3.6d-f7 | `6530719` | ✅ CLAUDE.md — the stale Next block rewritten past the closed ladder; the Decisions-gap claim corrected from an overcounted span to the accurate five locks PB-D57 through PB-D61, and the pool-pause reachability question raised at rung d carried forward rather than dropped. Landed via one approved Python exception, StrReplace being unable to match inside the roughly 49,000-character L446 — the second such failure after PB3.6d-doc1 |
| PB3.6d-f8 | `d0e6ded` | ✅ CLAUDE.md — the whitehat roster moved F-22 into the Fixed count, 19 to 20, clearing the last PB3.6d-b2 reference anywhere in the file; the ledger now carries zero open findings across all twenty-two raised to date. Closes the F-22 ladder |
| PB3.6d-f9 | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — the PB3.6d-f6 through PB3.6d-f8 rows plus this row, closing the three-commit tail that f5 could not cover because those commits had not yet landed; the log is current through PB3.6d-f9 and the F-22 family is fully logged — this commit |
| PB3.6d-g1 | `f543d7e` | ✅ CLAUDE.md — section 11's resume paragraph reflowed from one 49,992-character line into 45 lines by replacing 44 spaces with newlines, a pure whitespace change proven byte-identical by rejoining the lines with single spaces and matching the pre-edit content hash. Closes the recurring defect that forced approved Python exceptions at PB3.6d-doc1 and PB3.6d-f7 |
| PB3.6d-g2 | `83afd28` | ✅ CLAUDE.md — the stale section 11 sentence calling PB3.6 an unstarted F-14 ops step corrected; PB3.6 is the completed F-22 fix, F-14 is renumbered PB3.10, and PB3.7 has its first rung. Landed by ordinary StrReplace on a 101-character line, the g1 reflow proving its value on first use |
| PB3.6d-g3 | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — the PB3.6d-g1 and PB3.6d-g2 rows plus this row; the log is current through PB3.6d-g3 — this commit |
| PB3.6d-h1 | `42c3194` | ✅ docs/STAGE_P_BIS_PLAN.md — the PB-D57 through PB-D61 Decisions rows backfilled, each drafted from its own NOTES lock rather than any anchor's paraphrase; the table now runs PB-D1 through PB-D64 in unbroken sequence, verified by an index walk rather than by eye |
| PB3.6d-h2 | `—` | docs/STAGE_P_BIS_PLAN.md Completion Log — the PB3.6d-h1 row plus this row; the log is current through PB3.6d-h2 — this commit |
