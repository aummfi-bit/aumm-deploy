# STAGE_P_PRIME_QUEUE.md — Workstream A Adjudication Queue

**Stage:** P-prime — Seam-1 Audit Close-Out. Created at PP3.1a, 2026-08-21. **53 rows** (51 root causes + 2 composites) in `STAGE_P_PRIME_PLAN.md` §"Collapsed defect list" order, one family section per family. Landing progressively: PP3.1a Family A, then B, C, G, D, E, and F+H.
**Source of truth, and what this file deliberately does NOT carry.** `STAGE_P_PRIME_PLAN.md` §"Collapsed defect list" (L60—L168) is the authority for `RC`, `audit files`, `post-review severity`, `fix intent` and `ordering constraints`; those five columns are transcribed from it and it wins on any disagreement. The root-cause PROSE is not restated here — a reader who needs why a row exists reads the plan's table, so there is one description of each defect in the repository rather than two that drift.
**`redeploy unit` — the closed seven-token vocabulary (PP-D40 (ii), (iii)).** `hook`, `ccb`, `emission-stack`, `tvl-oracle`, `governance`, `token-schedule`, `source-only`. Exactly one per row, naming the ENTRY contract the fix edits and never PP-D12's transitive closure, which PP4.0 computes from the immutables. `token-schedule` MEANS a chain restart: it takes `AuMM` with it and abandons `GENESIS_BLOCK`.
**`done-criteria` — an artifact, never prose (PP-D40 (vi)).** The form is `path::case`, so PP4.52 reads a green run rather than a judgment. Names at creation are the INTENDED artifact and are binding on PP3.2, which either creates it or records why the row cannot reproduce.
**Unfilled cells read `—` rather than blank,** so a cell awaiting its rung is distinguishable from one silently dropped, and `grep` counts the remaining work.
**Fill order.** PP3.1 creates the row; PP3.2 fills `reproduction PoC`; PP3.3 fills `verdict`; PP4 fills `F-NN` with the ledger row when the fix lands. Severity is the report's post-review figure and PP3.3 may move it, but only with a reproduction or a refutation recorded against the row.

## Family A — proposal creation welded to a der Bodensee donation (5 files, 4 root causes)

| RC | audit files | post-review severity | reproduction PoC | verdict | fix intent | ordering constraints (PP-D) | redeploy unit | done-criteria | F-NN |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A.1 | `pool-pause-bricks-all-propose-entrypoints` (H), `propose-path-is-welded-to-two-third-party-rate-providers-with-no-fallback` (M) | High | — | — | escrow + `flushDeposits()`; typed `PoolPause`/`PoolUnpause` proposal pair (C.5); rate providers named as a trust assumption | PP-D10, PP-D17, PP-D24 | governance | `test/whitehat/P1_A1.t.sol::test_proposeVaultUnpause_survivesBodenseePause` | — |
| A.2 | `stray-token-transfer-bricks-bodensee-donation-helper` (H) | High | — | — | delta assertion or donate `balanceOf(this)`; permissionless `flush(token)` hard-wired to der Bodensee | PP-D11 (same rung as A.3) | emission-stack | `test/whitehat/P1_A2.t.sol::test_strayTransferDoesNotBrickDonation` | — |
| A.3 | `donate-exact-reserve-delta-assertion-bricks-all-governance-if-a-rate-provider-reports-below-par` (M) | Medium | — | — | `if (postReserve <= preReserve) revert ReserveDidNotRise`; settle the Vault's returned `amountsIn` after the add | PP-D11, PP-D13 (before E.3) | emission-stack | `test/whitehat/P1_A3.t.sol::test_donate_strictRise` | — |
| A.4 | `donateauthorizer-can-unilaterally-and-permanently-brick-all-governance` (H) | High | — | — | governance entry made immutable (constructor) or non-removable; two-step authorizer rotation | PP-D10, PP-D22 (handoff rotation 5 loses its rationale) | emission-stack | `test/whitehat/P1_A4.t.sol::test_governanceEntryNotRemovable` | — |
