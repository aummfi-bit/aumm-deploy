# STAGE_P_PRIME_QUEUE.md — Workstream A Adjudication Queue

**Stage:** P-prime — Seam-1 Audit Close-Out. Created at PP3.1a, 2026-08-21. **53 rows** (51 root causes + 2 composites) in `STAGE_P_PRIME_PLAN.md` §"Collapsed defect list" order, one family section per family. Landing progressively: PP3.1a Family A, then B, C, G, D, E, and F+H.
**Source of truth, and what this file deliberately does NOT carry.** `STAGE_P_PRIME_PLAN.md` §"Collapsed defect list" (L60—L168) is the authority for `RC`, `audit files`, `post-review severity`, `fix intent` and `ordering constraints`; those five columns are transcribed from it and it wins on any disagreement. The root-cause PROSE is not restated here — a reader who needs why a row exists reads the plan's table, so there is one description of each defect in the repository rather than two that drift.
**`redeploy unit` — the closed seven-token vocabulary (PP-D40 (ii), (iii)).** `hook`, `ccb`, `emission-stack`, `tvl-oracle`, `governance`, `token-schedule`, `source-only`. Exactly one per row, naming the ENTRY contract the fix edits and never PP-D12's transitive closure, which PP4.0 computes from the immutables. `token-schedule` MEANS a chain restart: it takes `AuMM` with it and abandons `GENESIS_BLOCK`.
**`done-criteria` — an artifact, never prose (PP-D40 (vi)).** The form is `path::case`, so PP4.52 reads a green run rather than a judgment. Names at creation are the INTENDED artifact and are binding on PP3.2, which either creates it or records why the row cannot reproduce.
**Unfilled cells read `—` rather than blank,** so a cell awaiting its rung is distinguishable from one silently dropped, and `grep` counts the remaining work.
**Fill order.** PP3.1 creates the row; PP3.2 fills `reproduction PoC`; PP3.3 fills `verdict`; PP4 fills `F-NN` with the ledger row when the fix lands. Severity is the report's post-review figure and PP3.3 may move it, but only with a reproduction or a refutation recorded against the row.

## Family A — proposal creation welded to a der Bodensee donation (5 files, 4 root causes)

| RC | audit files | post-review severity | reproduction PoC | verdict | fix intent | ordering constraints (PP-D) | redeploy unit | done-criteria | F-NN |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A.1 | `pool-pause-bricks-all-propose-entrypoints` (H), `propose-path-is-welded-to-two-third-party-rate-providers-with-no-fallback` (M) | High | — | — | escrow + `flushDeposits()`; typed `PoolPause`/`PoolUnpause` proposal pair (C.5); rate providers named as a trust assumption | PP-D10, PP-D17, PP-D24 | governance | `test/whitehat/P1_A1.t.sol::test_proposeVaultUnpause_survivesBodenseePause` | — |
| A.2 | `stray-token-transfer-bricks-bodensee-donation-helper` (H) | High | — | — | delta assertion or donate `balanceOf(this)`; permissionless `flush(token)` hard-wired to der Bodensee | PP-D11 (same rung as A.3) | emission-stack | `test/whitehat/P1_A2.t.sol::test_strayTransferDoesNotBrickDonation` | — |
| A.3 | `donate-exact-reserve-delta-assertion-bricks-all-governance-if-a-rate-provider-reports-below-par` (M) | Medium | — | — | `if (postReserve <= preReserve) revert ReserveDidNotRise`; settle the Vault's returned `amountsIn` after the add | PP-D11, PP-D13 (before E.3) | emission-stack | `test/whitehat/P1_A3.t.sol::test_donate_strictRise` | — |
| A.4 | `donateauthorizer-can-unilaterally-and-permanently-brick-all-governance` (H) | High | — | — | governance entry made immutable (constructor) or non-removable; two-step authorizer rotation | PP-D10, PP-D22 (handoff rotation 5 loses its rationale) | emission-stack | `test/whitehat/P1_A4.t.sol::test_governanceEntryNotRemovable` | — |

## Family B — voting weight not backed by present capital (11 files, 7 root causes + 1 composite)

| RC | audit files | post-review severity | reproduction PoC | verdict | fix intent | ordering constraints (PP-D) | redeploy unit | done-criteria | F-NN |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| B.1 | `quorum-denominator-counts-only-ever-poked-holders` (H) | High | — | — | protocol-maintained aggregate denominator (recorder-side, per-pool) — a design change; interim absolute floor; zero-weight `castVote` reverts; longer `VOTING_DELAY` and a separate bar for `VaultAuthorizerChange` | PP-D14 (INV-1), PP-D15 (F-21 coupling), PP-D12 | governance | `test/whitehat/P1_B1.t.sol::test_quorumDenominatorCountsUnpokedHolders` | — |
| B.2 | `stale-voting-weight-persists-after-withdrawal` (H) | High | — | — | recorder-gated `VotingWeight.onPositionClosed(pool, holder)` called from `recordWithdrawal`, `_syncDown` and the revocation branches; `castVote` re-gated on live weight as defence in depth | PP-D12, PP-D16 (INV-8) | governance | `test/whitehat/P1_B2.t.sol::test_withdrawalClearsVotingWeight` | — |
| B.3 | `pooltotallp-is-not-the-pools-bpt-supply-governance-share-denominator-drifts-both-ways` (H), `pooltotallp-inflatable-with-flash-capital-forced-poke-collapses-the-electorate` (H) | High | — | — | `share = min(userLP, balanceOf) / IERC20(pool).totalSupply()`; invariant test `poolTotalLP <= totalSupply`; third-party `poke` may not reduce a checkpoint except on the holder's own position change | PP-D12, PP-D17 (seed accounting), PP-D18 | governance | `test/whitehat/P1_B3.t.sol::test_shareDividesByBptTotalSupply` | — |
| B.4 (composite) | `composite-dust-lp-position-buys-permanent-vault-capture` (C) | Critical (composite) | — | — | — | closes via B.1 + B.3 + C.4 | n/a (composite) | B.1, B.3 and C.4 all at verdict Fixed with ledger hashes | — |
| B.5 | `syncposition-is-an-attacker-timed-180-day-governance-weight-kill-switch` (M), `out-of-band-bpt-exit-preserves-qualification-clock` (L) | Medium | — | — | read-time rule `if (held < lp) return 0` in `_positionPower` (closes both directions); `_syncDown` resets the clock only on full drain or uses the weighted-average form | PP-D12 | governance | `test/whitehat/P1_B5.t.sol::test_positionPowerZeroWhenHeldBelowLp` | — |
| B.6 | `trusted-router-seat-is-an-arbitrary-disenfranchisement-primitive` (H) | High | — | — | attribute positions to `AddLiquidityParams.to` / `RemoveLiquidityParams.from`, never `getSender()`; `recordWithdrawal` refuses to reset below what live BPT justifies | PP-D12 (hook unit) | hook | `test/whitehat/P1_B6.t.sol::test_positionAttributedToRecipientNotSender` | — |
| B.7 | `vetoproposal-stale-numerator-live-denominator-ratchet` (M), `vetoproposal-zero-denominator-panic` (M) | Medium | — | — | bank the vetoer's *fraction* at their own call; reject zero-weight vetoes; F-21-style zero guard; refuse `finalizeProposal` when the window elapsed at zero supply | PP-D14 | governance | `test/whitehat/P1_B7.t.sol::test_vetoBanksFractionAtCallTime` | — |
| B.8 | `era-exponent-flip-applied-lazily-per-holder` (L) | Low | — | — | apply the exponent at read time, or a permissionless `rebase()` at the boundary; at minimum document | PP-D12 | governance | `test/whitehat/P1_B8.t.sol::test_eraExponentAppliedAtReadTime` | — |

## Family C — authority outliving the constitution, and authority permitted to a principal that cannot use it (25 files, 8 root causes)

**Two notes on this family's numbering and its two blank redeploy cells.** `C.3` carries NO row here and that is not an omission: the plan folds it into C.2 as an instance of the fixed-dispatch class rather than a separate root, so the eight rows below are C.1, C.2 and C.4 through C.9, and the numbering gap is preserved so a `C.3` reference found elsewhere resolves. `C.1` and `C.2` read `—` in `redeploy unit` because PP-D23 and PP-D22 are OPEN and are what decide it — making the constitutional claim true redeploys contracts, making it accurate edits `10_constitution.md` and redeploys nothing. Per PP-D40 (v) the token is not invented ahead of the decision; it lands when Workstream C closes.

| RC | audit files | post-review severity | reproduction PoC | verdict | fix intent | ordering constraints (PP-D) | redeploy unit | done-criteria | F-NN |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C.1 | `constitutional-immutability-claim-vs-standing-authorities` (H) | High | — | — | decide the direction (PP-D23 OPEN): make the claim true (constructor-stamped expiries in the `:89` shape on each retained slot; `pauseManager = address(0)` at Stage R; fire the one-shots; renounce the factory owner) or make the claim accurate (rewrite §xxix to enumerate the ten) | PP-D22, PP-D23 | — | `docs/STAGE_P_PRIME_NOTES.md` PP-D23 clause + the `aumm-site` §xxix commit | — |
| C.2 | `dissolution-handoff-is-ungated-irreversible-and-mandated-by-the-constitution` (H), `stage-k-registries-permanently-welded-to-aureumgovernance-v1` (M), `revokevaultclass-backstop-reachable-by-no-caller` (M), `tvloracle-valuation-wiring-frozen-while-composition-challenge-stays-live` (M), `composition-challenge-seats-pool-with-no-recorder-binding` (M), `empty-miliarium-slots-unfillable-after-governance-handoff` (L) | High | — | — | either give governance typed branches for the ongoing operations (`distribute`, recorder bind — preferably a permissionless self-heal from the pool's Vault-registered hook, `TokenUnderlyingChange`, `RegistryGovernanceMigration`, `VaultClassRevoke`, a fill path for empty slots) or leave those slots where they are and correct §xxix; five setters gain `code.length` + two-step; `BodenseeBootstrapChannel.setGovernanceContract` reverts while `pendingAccrual != 0`; `_rotate()` reads back every slot | PP-D22 (the dissolution decision, Workstream C), PP-D19 (revokeVaultClass ⇒ G.5 first), PP-D12 | — | `docs/STAGE_P_PRIME_NOTES.md` PP-D22 clause + `test/fork/DissolutionGovernanceHandoff.t.sol` re-gated | — |
| C.4 | `emergency-window-recreatable-via-vaultauthorizerchange` (H), `vault-authorizer-change-no-execute-time-sanity-check` (M), `stage-b-authorizer-remains-a-ready-made-permanent-multisig-capture-target` (L) | High | — | — | after `setAuthorizer`, require `IAuthorizer(VAULT.getAuthorizer()).canPerform(setAuthorizerActionId, address(this), VAULT)`; re-check `code.length` at execute; a higher bar and longer timelock for this type alone; address-book `superseded` flag on the Stage-B authorizer | PP-D25 OPEN (ossification), PP-D10 | governance | `test/whitehat/P1_C4.t.sol::test_setAuthorizerRejectsSelfLockout` | — |
| C.5 | `composite-recovery-mode-is-a-permanent-self-service-protocol-fee-exemption` (H), `recovery-mode-bits-outlive-the-emergency-window-and-zero-protocol-revenue` (M), `pool-role-accounts-are-caller-chosen-and-never-inspected-at-admission` (L) | High | — | — | recovery mode disqualifying in `GaugeEligibility` and `recordScore`; typed `PoolPause`/`PoolUnpause` proposal pair; batched `VaultRecoveryDisable`; exit grants outlive entry grants by an epoch; role-account conjunct at both admission gates; Stage R registers `pauseManager = address(0)` | PP-D24, PP-D12 | emission-stack | `test/whitehat/P1_C5.t.sol::test_recoveryModePoolDisqualified` | — |
| C.6 | `admission-authority-grants-permanent-emission-entitlement` (M), `admission-authority-vetoes-passed-composition-proposal` (M), `admission-authority-rotation-is-a-one-transaction-brick` (L) | Medium | — | — | snapshot the rail conjunct into the proposal at propose; standing re-check in `recordScore` for the *emission* leg; revocations delayed past a full proposal lifecycle; two-step rotation | PP-D26 (the antagonism) | emission-stack | `test/whitehat/P1_C6.t.sol::test_admissionSnapshotAtPropose` | — |
| C.7 | `unburned-incendiary-admin-is-a-dangling-one-shot-authority` (L), `ccbmultiplier-registrysetter-unburned-one-shot-repoints-the-miliarium-oracle` (M) | Medium | — | — | prefer constructor immutables where the value is known at construction (both are, once D.8 redeploys `CCBMultiplier`); `renounceIncendiaryAdmin()`; getters for burn state; `DeployStageP` post-conditions | PP-D27 (do not spend a Sepolia tx on a contract being redeployed) | ccb | `test/whitehat/P1_C7.t.sol::test_registrySetterIsImmutable` | — |
| C.8 | `aumtcontract-binding-is-irreversible-and-unvalidated` (M), `tvloracle-setmiliariumregistry-one-shot-seal-with-no-code-length-check` (L), `approved-factory-immutable-with-no-recovery-path` (L), `authorizer-constructor-accepts-configurations-that-silently-void-its-own-guarantee` (Info), `moduleadmin-gate-untested-and-burn-is-conditional-on-both-setters` (Info), `vault-factory-standing-owner-is-inert-create-permanently-unreachable` (Info) | Medium | — | — | `code.length` or an interface probe on every seating; `pool` validated against the Vault; `try/catch` on the hook's recorder dispatch so a recorder fault degrades accounting instead of locking LPs; post-condition `require`s after every wiring call; the register's pre-send `eth_call` rule as a runbook gate; `renounceOwnership()` on the factory; the two authorizer `require`s | PP-D12, PP-D28 (RB-017 promoted to a runbook gate) | emission-stack | `test/whitehat/P1_C8.t.sol::test_recorderFaultDegradesNotLocks` | — |
| C.9 | `recoverstrandedfees-route-is-caller-chosen-with-a-zero-slippage-floor` (L) | Low | — | — | non-zero caller-supplied minimum donated as a post-condition; hops restricted to registry-known venues; landed amount in the event; a rotation path or a governance proposal type for router trust | PP-D12 (hook unit) | hook | `test/whitehat/P1_C9.t.sol::test_recoverStrandedFeesEnforcesMinimum` | — |

## Family G — proposal lifecycle and the composition branch (6 files, 5 root causes + 1 composite)

| RC | audit files | post-review severity | reproduction PoC | verdict | fix intent | ordering constraints (PP-D) | redeploy unit | done-criteria | F-NN |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G.1 | `sleeper-de-escalation-proposals-defeat-emergency-levers` (M) | Medium | — | — | `Expired` when `eta == 0 && block.number >= endBlock + QUEUE_DEADLINE_BLOCKS`; proposer cancel; de-escalation types bound to the emergency state at propose and rejected if re-entered afterwards | PP-D29 (the pre-queued exit is today the ONLY exit from a Vault pause — G36 §2.2 — so G.1 lands after or with A.1) | governance | `test/whitehat/P1_G1.t.sol::test_queuedProposalExpires` | — |
| G.2 | `composition-challenge-victim-resolved-at-execute-time` (M) | Medium | — | — | store the incumbent in `targetPool` and require `poolAtSlot(p.slot) == p.targetPool` at execute; serialise challenges per slot | PP-D30 (with G.3 in one rung — the same six lines) | governance | `test/whitehat/P1_G2.t.sol::test_challengeIncumbentFrozenAtPropose` | — |
| G.3 | `composition-challenge-bricked-by-permissionless-gauge-activation` (H) | High | — | — | gate propose on `gaugeStatus == None && slotOf == 0` and re-check at execute with a distinct error; let the branch TOLERATE an already-`Active` candidate — **only together with the `GaugeChallenge` execute-time `slotOf(p.targetPool) == 0` re-check** | **PP-D9** (the refuted-hypothesis coupling) | governance | `test/whitehat/P1_G3.t.sol::test_challengeSurvivesGaugeActivation` | — |
| G.4 (composite) | `composite-composition-challenge-is-a-monotone-electorate-destruction-ratchet` (M) | Medium (composite) | — | — | — | closes via C.2 (bind) + G.2 (freeze incumbent) + B.1 (denominator) + a global challenge rate limit | n/a (composite) | C.2, G.2 and B.1 at verdict Fixed with ledger hashes, plus the global challenge rate limit landed | — |
| G.5a | `stale-and-duplicate-proposals-are-permanent-readmission-tickets` (M) | Medium | — | — | re-check admission at finalize; finalization deadline; one live proposal per `admissionValue`; clear `admissionTypes` on revoke | **PP-D19** (must land BEFORE C.2 makes `revokeVaultClass` reachable) | governance | `test/whitehat/P1_G5a.t.sol::test_finalizeRechecksAdmission` | — |
| G.5b | `finalizeproposal-nonexistent-id-admits-zero-address` (Info) | Info | — | — | `if (proposalId >= nextProposalId) revert UnknownProposal` on both entries | PP-D15 (the analogous governance-side guard rides on F-21) | governance | `test/whitehat/P1_G5b.t.sol::test_finalizeRejectsUnknownId` | — |
