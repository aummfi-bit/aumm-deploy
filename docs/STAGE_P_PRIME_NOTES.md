# STAGE_P_PRIME_NOTES.md — Stage P-prime Design Freeze + Findings Log

**Stage:** P-prime — Seam-1 Audit Close-Out.
**Status:** Entry freeze locked 2026-08-21 (PP-D1—PP-D9, plus the four entry-adjudicated rows PP-D20 / PP-D21 / PP-D38 / PP-D39). **PP2 (Workstream 0) executed 2026-08-21** — both keys rotated, PP2.3 skipped on chain, `4b589bc` pruned, backup deleted; residual CLOSED. Branch `stage-p-prime` (cut from `stage-p-bis` @ `9ec513d` = the audited commit) per PP-D2. PP-D10—PP-D19 and PP-D22—PP-D37 are indexed in `STAGE_P_PRIME_PLAN.md` and lock here at their sub-steps; this file gains each entry when it locks. Execution truth until superseded by explicit amend. No code lands until this freeze locks (§12 ambiguity-gate).
**Reproduction pins:** auditician `22aa9851caf68f13c9439bd145ef7594f217df5c`; target aumm-deploy `9ec513d99a68fb454a8a54271b34b884f40f2088` (`stage-p-bis`); snapshot 2026-08-19; run completed 2026-08-21; the clone at `~/audits/aumm-q-governance-2026-08/` (82 confirmed / 11 invalid / 0 plausible; 2 Critical, 17 High, 32 Medium, 14 Low, 17 Info after the independent review — `PROGRESS.md`'s pre-review 78 / 21 High / 13 Info is superseded).
**Canonical spec:** `STAGE_P_PRIME_PLAN.md` (the collapsed defect list, the twenty fix-sequencing constraints, the Decisions index); `report/audit-report.tex` (Recommendations 1—11 in priority order; "Defects that only bite once you fix something else"); constitution §xxix (`10_constitution.md` L203).
**Related:** `STAGE_P_BIS_NOTES.md` PB-D2 / PB-D11 / PB-D12 (the dissolution class this stage re-adjudicates under PP-D22), PB-D10 (F-15, REOPENED by G39 §5), PB-D61 / RB-017 (the admissibility rule the report quotes against the Stage-K handoff), PB-D63 / PB-D64 (F-22, whose accepted residual G36 correction 4 reclassifies as a latch), PB-D68 (F-23, whose strict-rise fix G10 shows was never backported to the helper), PB-D69 (the admission flag family C.6 turns on), PB-D70 (the redeploy cascade PP-D12 inherits); `CLAUDE.md` §11 (the operator-secret residual PP-D8 closed at PP2, 2026-08-21); `docs/white_hat/AUREUM_WHITEHAT_OUTPUT.md` (F-01 / F-05 / F-16 shown incomplete, F-15 reopened).

**Framing.** Stage P-prime exists to consume the seam-1 audit output before Stage Q engages a firm: 82 confirmed files reduce to 51 distinct root causes in eight families plus two composites that own no root, every root cause exits Fixed, Accepted-risk-with-evidence, or False-positive-with-refutation, and closure is proven by a second seam-1 run against the patched commit rather than asserted from the ledger. The stage is inserted after P-bis because the audit ran against P-bis's tip while P-bis is still open (PB3.9, PB5); the name is the user's and is not re-litigated. Four sequenced workstreams (PP-D3): **(0)** operator security, **(A)** adjudication, **(B)** the patch cycle, **(C)** non-code findings, then the re-run gate. `stage-p-prime-complete` replaces `stage-p-bis-complete` as the audit candidate Stage Q receives.

---

## PP-D1 — Stage scope: consume seam 1 only; every root cause exits with a recorded disposition

The input is bounded to what the seam-1 run produced: the 82 files under `audit-state/issues/confirmed/`, the 11 under `issues/invalid/` (read for their refutations, never re-filed), the 18 globals, and the report. The unit of work is the root cause, not the file — the plan's collapse is by fix: two files share a root only when one code change closes both, and files the validators kept apart for disjoint or antagonistic fixes stay apart (the `admissionAuthority` pair, the two `vetoProposal` faces, the three F-16 stages, the two G10 contracts). Exit rule per root cause: **Fixed** (ledger row at `Fixed` with the commit hash and an inverted PoC), **Accepted-risk-with-evidence** (a quantifying artifact in the PB-D9 sandwich-sim shape, never a prose argument alone — the audit reopened F-15 precisely because PB-D10's acceptance was an argument about one direction), or **False-positive-with-refutation** (the PP3.2 PoC fails to reproduce and the refutation is written into the queue and here). Nothing is silently carried past the tag; every disposition is re-checked by the PP6 re-run. Out of scope by this entry: seams 2—5 (PP-D20), the CircuitBreaker track (PB-D63 descoped it; this stage builds only the typed `PoolPause` / `PoolUnpause` pair under PP-D24), any Vault or submodule edit (§8c — the two upstream changes the report suggests are declined in favour of the Aureum-layer alternatives it also names), and any reopening of PB-D63 (xi)'s scoping of F-22 beyond restating its residual as conditional on no authorizer change ever succeeding.

## PP-D2 — Branch `stage-p-prime` cut from `stage-p-bis` @ `9ec513d`, the audited commit

The branch base is the audited commit and not `stage-p-bis`'s moving tip, so that `git diff 9ec513d..stage-p-prime -- src/` is exactly the remediation the PP6 re-run measures, with nothing from PB3.9 or PB5 mixed in. P-bis stays open and rebases nothing; whether `stage-p-prime` rebases onto P-bis's close or the reverse is decided with PP-D21 (the recommendation in the plan's PP7 is P-prime → P-bis → `main`, because PB3.9 runs on generation 4 if PP-D21 (b) holds). Close mechanics mirror PB-D3: two-commit PENDING close (`PENDING_PPRIME` in the `STAGES_OVERVIEW.md` master row + the Completion Log; `CLAUDE.md` §11 hash-free) → the `*-backfill` commit → tag `stage-p-prime-complete` at the backfill commit. §8c's push-to-main ban applies as written; the 2026-08-18 carve-out covered one verified single-file fast-forward and extends to nothing here.

## PP-D3 — Stage shape: four sequenced workstreams 0 → A → B → C → gate

**(0)** Operator security runs FIRST and needs no adjudication: the deployer private key and the Etherscan API key are exposed in an orphaned but reflog-recoverable commit (`CLAUDE.md` §11) and have not been rotated; the item is independent of every finding and waits behind nothing (PP-D8). **(A)** Adjudication before any build (PP-D4), so no rung re-derives judgment. **(B)** The patch cycle in the dependency order the plan's "Fix sequencing" fixes — never family order, because the twenty constraints cross families. **(C)** Non-code findings run alongside B, with one exception: the dissolution decision (PP-D22) gates B's registry-branch rung, so PP5.1 is scheduled before PP4 rung 17 rather than after B closes. **Gate:** the seam-1 re-run (PP-D7) runs once, on the final commit, after the formal bundle re-attests last in B (PP-D37). The one A→B→C ordering P-bis used is not enough here because C's decisions change B's shape; the four-way order and its single cross-edge are stated so a cold session does not open rung 17 early.

## PP-D4 — Adjudication-first, per root cause: the queue row locks before the rung opens

`docs/STAGE_P_PRIME_QUEUE.md` (PP3.1) holds one row per root cause and per composite — 53 rows — with `RC | audit files | post-review severity | reproduction PoC | verdict | fix intent | ordering constraints (PP-D) | redeploy unit | done-criteria | F-NN`. A Workstream-B §8e.1 may open only when its row carries a verdict, a fix intent and done-criteria, and the prompt's `Must match` is drafted from that row. This is the §12 ambiguity-gate applied to external findings: the audit's own record shows what happens when a fix is reasoned about in isolation (G16 retracted a recommendation that would have introduced an unbounded-gas catch-up; the `admissionAuthority` pair's two natural fixes make each other worse), so judgment is materialized once, in the queue, and the rungs transcribe it. Verdicts lock in family batches (PP3.3), each batch one NOTES edit, so a later rung cites a PP-D clause rather than a chat turn.

## PP-D5 — Whitehat ledger numbering: one F-NN row per distinct root cause, F-24 through F-74

The ledger is one row per finding and append-only; the audit's 82 files are not 82 findings. One row per root cause (51 rows, F-24—F-74) keeps the ledger's granularity at the fix, which is what a `Fixed` hash attests to; each row's Explanation names every audit file it closes and carries every validator correction the plan marks **[corrected]**, so the ledger is readable without the clone. The two composites (`composite-dust-lp-position-buys-permanent-vault-capture`, `composite-composition-challenge-is-a-monotone-electorate-destruction-ratchet`) are recorded inside their component rows (B.1 / B.3 / C.4 and C.2 / G.2 / B.1 respectively), not as rows, because a composite has no fix of its own and a row with no `Fixed` hash would sit Open forever. Stage/Step is `P1.<RC>` (the reproduction PoC's step); Surface per the S0—S12 catalog; Status enters at `Confirmed` on the PoC, never at `Open`, because PP3.2 runs before PP3.4. The coverage roll-up gains a "Seam-1 external audit (auditician 22aa9851 @ 9ec513d)" row.

## PP-D6 — Severity input is the report's post-review figure; Workstream A moves a row only on reproduction or refutation

The working severities are 2 / 17 / 32 / 14 / 17 — the independent review's figures, not `PROGRESS.md`'s pre-review 2 / 21 / 28 / 14 / 13. Where a validator note records a raise that the review later returned (the four High→Medium returns) the plan carries both and binds the later one. A root cause's severity is the maximum over its files. Workstream A may move a row in either direction only with an artifact: a PP3.2 reproduction that shows the impact differs from the filing, or a refutation. The two boundaries the report itself flags as arguable are re-decided there and nowhere else: `composition-challenge-victim-resolved-at-execute-time` (Medium, raisable to High by a reviewer who accepts G21's attendance-bar result) and `tvl-floor-is-checked-only-at-admission-…` (Low only on the missing auto-revocation; an Info + Low split is defensible). The six author-validated findings the report names in §secmethod get their PP3.2 reproduction first within their family, because they had refutation attempts but no independent re-derivation.

## PP-D7 — The exit gate is a second seam-1 run; the delta must be zero-or-explicitly-accepted

A fresh auditician clone at the PP-D39 commit, `audit-target/` at the rung-19 commit (the frozen `src/`), `audit-context/` byte-identical to the first run's plus `CORRECTIONS.md` (PP1.3), `RUN-METADATA.md` written with BOTH hashes before `/audit` launches, the same model and effort as the first run, `issues/plausible/` at zero before the delta is read. Acceptance, stated before the run: (i) every queue row at verdict Fixed has no surviving confirmed file, or one whose validator text states the fix holds and re-files a residual — which becomes a NEW row; (ii) every Accepted-risk row is re-confirmed at its accepted severity or lower; (iii) zero NEW Critical or High — any reopens PP4; (iv) NEW Medium / Low / Info are triaged into the queue and fixed or accepted before PP7; (v) the regenerated capability matrix, diffed against G37, shows every row the plan retires as absent or INERT. Both hashes of both runs and the delta table land in this file's "Exit gate" section. What the gate proves is bounded and stated: the same class of tool has the same blind spots (the report's §secmethod names six author-validated findings and a self-validation that missed two duplicates), so PP6 establishes that the harness no longer finds what it found, not that nothing remains; Stage Q's firm is the independent instrument and receives the first report, the queue and the delta.

## PP-D8 — Workstream 0: rotate both keys, purge the orphan, delete the backup, prove the negative

The 2026-08-18 incident left one residual open: `4b589bc` on `main` swept 163 files including every `.env*` variant carrying `DEPLOYER_PRIVATE_KEY` (`.env.example` L43) and `ETHERSCAN_API_KEY` (L32); it was never pushed and is reachable from no remote ref, but it stays reflog-recoverable until garbage collection, and neither key has been rotated (`CLAUDE.md` §11). Scope, all user-run per §8b / §8c: (i) a new deployer EOA, funded from the old one (5.316662104309836821 SepETH at the last reading), written to `.env`, `.env.sepolia`, `.env.mainnet` after `git check-ignore -v` on all three; (ii) a regenerated Etherscan key with the old one revoked; (iii) on Sepolia generation 3, the rotatable seats the old EOA holds — the four emission `setGovernanceContract`s, `setDonateAuthorizer`, `setAdmissionAuthority` — move to the new EOA ONLY IF PP-D21 keeps generation 3 alive past PP6, each preceded by the RB-017 `eth_call` from the intended sender; the immutable seats (`pauseManager` on all 27 pools, the hook's `governanceModule`, `_incendiaryAdmin`, `CCBMultiplier.registrySetter`) stay on the old EOA, accepted on a testnet and recorded in deployment-record section 27 with the reason; (iv) `git reflog expire --expire=now --all && git gc --prune=now --aggressive` on `main`, then `git log --all -p -S` for each secret printing nothing on every branch, then deletion of `~/aumm-env-backup-20260818-145359`, the four commands and their empty outputs recorded here. The exposed key is a testnet key holding SepETH and testnet seats; the reason to rotate anyway is that the same operator posture carries to Stage R, and a residual that `CLAUDE.md` has called open since 2026-08-18 does not get to age into the mainnet runbook.

**PP-D8 amendment (2026-08-21, same day as the lock).** Clause (iii)'s condition — "ONLY IF PP-D21 keeps generation 3 alive past PP6" — evaluates **FALSE**, because PP-D21 (b) retires generation 3 at a generation-4 refresh after the exit gate. **PP2.3 therefore moves zero seats and sends no transaction.** The rotatable seats stay on the old EOA: the four emission `setGovernanceContract`s, `setDonateAuthorizer` and `setAdmissionAuthority`. The four seats that cannot move at all — `pauseManager` on 27 pools, the hook's `governanceModule`, `_incendiaryAdmin` and `CCBMultiplier.registrySetter` — are not a gap in this workstream but root causes C.1 and C.7 of the collapsed defect list, and generation 4 is where they are fixed rather than rotated. Spending real authority-write transactions on a constellation already scheduled for replacement buys nothing and adds risk, since each rotation is a live authority write that the RB-017 rule requires a pre-send `eth_call` to justify; the honest record is the skip and its reason, so deployment-record section 27 states that zero seats moved and why, rather than describing a rotation that did not happen. **This narrows PP2.3 to documentation and narrows nothing else:** PP2.1 and PP2.2 are unaffected, because the exposure is a KEY that leaked, not a seat that is misplaced, and a leaked key stays leaked whichever constellation it points at. One correction rides with this amendment: PP-D8 as written names `ETSCAN_API_KEY`, which is not a variable that exists — the key is `ETHERSCAN_API_KEY` (`.env.example:32`), and the name is corrected in place here and in the plan's PP-D8 row so a later session greps a string that resolves.

## PP-D9 — The G.3 fix carries a mandatory companion: an execute-time `slotOf(p.targetPool) == 0` re-check on the `GaugeChallenge` branch

The refuted hypothesis "a Miliarium pool can be gauge-challenged out" (`PROGRESS.md` "refuted hypotheses") is refuted only by an undocumented cross-contract coupling: `proposeGaugeChallenge` checks `slotOf(targetPool_) == 0` at propose time only (`src/governance/AureumGovernance.sol` L229), and that premise cannot be falsified today because the only reachable `replaceSlot` caller, the composition branch (L432), immediately calls `registerGaugeFromComposition` (L433), which rejects an `Active` pool (`src/gauge/GaugeRegistry.sol` L170). A pool that was `Active` at a gauge challenge's propose time can therefore never become a Miliarium member before that challenge executes. The natural fix to `composition-challenge-bricked-by-permissionless-gauge-activation` — let the composition branch TOLERATE an already-`Active` candidate, so a 100-svZCHF `activateGauge` cannot poison it — makes an `Active` pool seatable, falsifies the premise, and lets a banked simple-majority `GaugeChallenge` terminally revoke a Miliarium pool, bypassing the 2/3 bar the constitution requires for any structural change to the 28. Lock: the tolerate change and the `GaugeChallenge` re-check at L426-427 land in ONE rung (PP4 rung 9, with G.2's incumbent freeze over the same six lines per PP-D30), with a comment at both sites naming the coupling, and a regression that banks a gauge challenge against an `Active` unslotted pool, seats that pool by composition, and asserts the banked challenge reverts at execute. This is the first of the twenty constraints and the template for the rest: a fix is not done until the thing that was relying on the defect is named.

## PP-D20 — Seams 2—5: two run inside P-prime after PP6 against the patched commit; the rest go to Stage Q (user-adjudicated 2026-08-21)

**(i) Stated gap first.** No audit artifact records a seam roster. `audit-context/AUDIT-INSTRUCTIONS.md` names seam 1 and, in passing, "seam 5's scope" for `script/`; the report's §"Suggested next seams" names the emission mathematics and CCB scoring formulas, the fee-routing hook's swap logic, and the constellation's economic design as areas examined only where authority passed through them. The roster below is therefore this stage's proposal, not a recovered fact: seam 2 = emission mathematics + CCB engine (`src/emission/`, `src/ccb/`), seam 3 = the fee-routing hook and fee controller value paths (`src/fee_router/`, `src/vault/AureumProtocolFeeController.sol`), seam 4 = oracle, rate providers and pool composition (`src/emission/TVLOracle.sol`, `src/rate_provider/`, `src/factory/`), seam 5 = `script/` deploy wiring and the runbooks. **(ii) Placement.** Seams 2 and 3 run inside P-prime, serially, AFTER PP6 and against the patched commit, each with its own re-run gate of the PP-D7 shape — because they audit the code this stage changes most (the electorate redesign, the distributor, the hook unit), and a seam run against the audited commit would find defects already fixed and miss defects the fixes introduce. Seams 4 and 5 go to Stage Q as the firm's first deliverable, with this stage's queue and delta as their input. **(iii) Rejected:** running any seam in parallel with Workstream B on the audited commit — seam 1 produced twenty ordering constraints against a frozen tree; a seam's findings against a moving tree would be adjudicated twice and the second adjudication would be against code that no longer exists. **(iv) Consequence for the plan:** PP6 becomes PP6 (seam 1), PP6-bis (seam 2), PP6-ter (seam 3), each recorded with both hashes; the stage closes after the third; the plan's roadmap gains those rows at PP3.1 when the queue is created.

## PP-D21 — Sepolia generation 4: yes, after PP6 and after PP-D22 is decided (user-adjudicated 2026-08-21)

Generation 3 cannot be patched in place — the Vault seals `_hooksContracts` at registration, `FEE_ROUTING_HOOK` is immutable on the controller, `setEmissionRecorder` / `setAuMTContractForPool` / `setMiliariumRegistry` / `setVotingWeight` / `setGovernanceContract` on `VaultClassRegistry` are one-shot, and `AureumGovernance` holds its six dependencies `immutable` (`src/governance/AureumGovernance.sol` L39-86), so the transitive redeploy unit (PP-D12) is the whole Stage F—L stack plus the 27 pools; only the Vault, the factory, the 87 stubs and `AuMM` survive, and `AuMM` survives only if `GENESIS_BLOCK` is kept — which it is, since no P-prime rung changes the token (the F.3 constant lives in `AureumTime` and moves era boundaries without touching the sealed genesis). **Lock:** a generation-4 refresh runs AFTER PP6 (so it deploys the code the re-run measured) and AFTER PP-D22 is decided (so the dissolution script's read-backs, the `PoolPause` pair and the authorizer probe are wired the way Stage R will wire them), re-running the PB3.13 / PB3.14 procedure with the stubs reused. The reason is the one P10 established: the wiring classes this stage changes — handoff read-backs, typed de-escalation, execute-time probes, recorder self-heal — are exactly what a fork harness exercises against a test-contract governor and a live broadcast exercises against an EOA, and PB-D31, PB-D46 and PB-D48 each record a defect that survived the fork harness and died at the live dry run. PB3.9 (der Bodensee's first distribute) runs on generation 4, which is why the merge order in PP7 is P-prime → P-bis → `main`. **Rejected:** (a) no testnet redeploy — leaves Stage R as the first deployment of every changed wiring path; (c) generation 4 only for the frontend — the frontend is not the risk.

## PP-D38 — The audit harness configuration is versioned in this repo now, at `docs/audit/seam-1/`, and promoted to `aumm-skill` later (user-adjudicated 2026-08-21)

The reusable configuration is small and load-bearing: `audit-context/AUDIT-INSTRUCTIONS.md`, `THREAT-MODEL-SEED.md` and `RUN-METADATA.md` from the clone, plus the `CORRECTIONS.md` this stage writes (the carried corrections and refuted hypotheses a re-run must not re-derive). The PP6 re-run needs all four reproducible from a commit hash, and the hash it needs is this repo's. `aumm-skill` is the wrong home today on two grounds: its `references/` tree is generated from aumm-site and protected by a CI check that rejects hand edits, and its `SKILL.md` is the spec-grounding layer for answering questions about the protocol, not a tooling surface — a per-seam threat-model seed is a different kind of artifact from a canon reference. **Lock:** PP1.3 creates `docs/audit/seam-1/` with byte copies (USER VERIFY diffs each against the clone, never against a retyped expected-file, per the §8e.1 transcription rule); seams 2 and 3 add `docs/audit/seam-2/` and `seam-3/` at their runs; promotion to `aumm-skill` — under a new top-level `audit/` that the protect-references check does not cover, or to a dedicated repo — is decided when a second seam has proven the template reusable, and is a copy.

## PP-D39 — The exit gate runs on the same auditician commit, `22aa9851`; a second pass at upstream HEAD only if the first is clean (user-adjudicated 2026-08-21)

A delta attributes to exactly one change only when exactly one thing changed. Re-running on a newer harness would fold harness improvements and harness regressions into the target's delta, and neither could be separated afterwards. **Lock:** PP6.1 clones auditician at `22aa9851caf68f13c9439bd145ef7594f217df5c`; if the PP6.3 delta is zero-or-explicitly-accepted, a second run at upstream HEAD is permitted and is recorded as its own row with its own hash pair, its findings triaged under the same PP-D7 rule — it is a bonus pass, not the gate. The same rule applies to seams 2 and 3 under PP-D20: each pins the harness commit it runs on before it launches and records it in that seam's `RUN-METADATA.md`.

## PP-D40 — The queue's `redeploy unit` column is a closed seven-token vocabulary naming the ENTRY contract rather than the transitive closure, and `done-criteria` names a test artifact (user-adjudicated 2026-08-21)

**(i) Why this locks before PP3.1a rather than being settled per row.** The queue's ten columns split three ways: five are transcribed from the §"Collapsed defect list" tables, three (`reproduction PoC`, `verdict`, `F-NN`) are deliberately empty at creation, and two are authored from scratch. `redeploy unit` is the column PP4.0 aggregates into the generation-4 manifest, so an unenumerated vocabulary yields rows that are individually plausible and collectively inconsistent — the §12 typed-domain gate in its exact shape, and cheaper to fix in a lock than across 53 saved rows.

**(ii) SEVEN tokens, not the six proposed at adjudication — a correction with its cause.** The six-token set was drawn from the §"Fix sequencing" rung table, which names its units only in passing (order 10 "D.6 also redeploys `TVLOracle`", 13 "distributor/oracle/eligibility unit", 14 "`CCBMultiplier` redeploy", 15 "hook unit", 16 "governance/eligibility/script"). That table never mentions Family F, and Family F is the gap: F.2 edits `src/token/AuMMMinterRouter.sol` and F.3 edits `src/token/AuMM.sol:23` together with `src/lib/AureumTime.sol:28`, all of which PP-D12 places in the SURVIVING set. Both collapse to the same unit for different reasons — `AuMM.setMinter` is one-shot and spent, so a new router orphans the token, and `AureumTime` is a pure library whose functions are all `internal` and therefore inlined into ELEVEN consumers including `AuMM` and `AureumProtocolFeeController`. Either edit takes `AuMM` with it, abandons `GENESIS_BLOCK` and restarts the emission clock: the PB-D70 cascade. That is the most expensive statement the column can make and it does not fold into a token that means something cheaper.

**(iii) The map, by the file a fix edits.** `hook` — `fee_router/AureumFeeRoutingHook.sol`. `ccb` — `ccb/CCBMultiplier.sol`, `CCBScore.sol`, `CCBShare.sol`, `EMASampler.sol`. `emission-stack` — `emission/EmissionDistributor.sol`, `emission/EfficiencyOracle.sol`, `emission/BodenseeBootstrapChannel.sol`, `gauge/GaugeRegistry.sol`, `gauge/GaugeEligibility.sol`, `gauge/SwapAndDepositToBodensee.sol`, `incendiary/IncendiaryRegistry.sol`. `tvl-oracle` — `emission/TVLOracle.sol` alone, broken out because rung 10 redeploys it on D.6 independently of the distributor. `governance` — `governance/AureumGovernance.sol`, `VotingWeight.sol`, `AureumGovernanceAuthorizer.sol`, `gauge/VaultClassRegistry.sol`, `registry/MiliariumRegistry.sol`. `token-schedule` — `token/AuMM.sol`, `token/AuMMMinterRouter.sol`, `lib/AureumTime.sol`; per (ii) this token MEANS a chain restart and a row carrying it says so. `source-only` — no deployed bytecode changes: `docs/`, `test/`, `formal/`, `script/`, and the `aumm-site` spec repo.

**(iv) The token names the ENTRY contract, never the closure.** PP-D12 states the transitive closure of a `ccb` change as the whole Stage F–L stack plus the 27 pools, with only the Vault, the factory, the stubs and `AuMM` surviving. Expanding that per row would make every non-`source-only` row read the same thing and carry no information, so the column records where the source edit lands and PP4.0 — which already owns "the dependency map derived from every `immutable` and every one-shot setter" — computes what that forces. The queue is the input to that map, not a second copy of it.

**(v) Exactly one token per row.** A fix spanning buckets takes the token of its primary source change and names the others in `ordering constraints` per PP-D12; where two are genuinely co-equal the row takes the one earliest in the cascade `ccb → emission-stack → hook`, because PP-D12 makes the earlier force the later. A root cause that fits no token is a blocker to raise, not a token to invent.

**(vi) `done-criteria` names an artifact, never prose alone.** The form is `path::case`, e.g. `test/whitehat/P1_A3.t.sol::test_donate_strictRise`, so PP4.52 reads a green run rather than a judgment. Rows that cannot reproduce — the Family H Infos and the Workstream-C rows — name the artifact that attests instead: a NOTES clause, an `aumm-site` commit, or a ledger row. `reproduction PoC` is filled at PP3.2, `verdict` at PP3.3, `F-NN` when the ledger row lands in PP4; all three stay empty at creation per the PP3.1 spec.

**(vii) AMENDMENT, 2026-08-21 at PP3.1f-pre — clause (iii)'s map omitted `src/vault/` and `src/factory/`, and Family E raised it.** E.2, E.3 and E.4 all edit `src/vault/AureumProtocolFeeController.sol`, which no token covered, so clause (v)'s "a root cause that fits no token is a blocker to raise" fired as designed. The resolution is a measured weld rather than a preference: `FEE_ROUTING_HOOK` is `immutable` at `AureumProtocolFeeController.sol:222` and set in the constructor at `:252`, so the controller cannot be redeployed independently of the hook and PP-D12 constraint 11 already prices the pair as one redeploy; rung 15 calls it "the hook unit". Clause (iii) is therefore READ AS: `hook` covers `fee_router/AureumFeeRoutingHook.sol` AND `vault/AureumProtocolFeeController.sol`; `governance` additionally covers `vault/AureumAuthorizer.sol`, the Stage-B authorizer C.4 and C.8 touch. `vault/AureumVaultFactory.sol` and `factory/AureumWeightedPoolFactory.sol` stay OUT of every token: they are in PP-D12's surviving set, the only remedy any row proposes for them is `renounceOwnership()`, which is an operator transaction and not a source edit, so they take `source-only`; a root cause that genuinely edits their SOURCE breaks the surviving set and is a blocker under clause (v) rather than a token assignment. `src/rate_provider/` is named by no root cause at all. No eighth token is created and the vocabulary stays closed at seven.

## PP-D41 — Capability, not conduct: no row exits Accepted-risk on operator trust (user-adjudicated 2026-08-21)

A queue row does not exit Accepted-risk on the ground that the key holder is trusted, is the project's own operator, or has no motive. The disposition bar is capability: the capability is absent, or bounded by code that still holds if the holder is hostile. The banned rationale is the test rather than a value-scale filter, so a non-value capability such as E.9's protocol-wide liquidity kill-switch is caught exactly as a value-transfer one is. Economic findings whose remedy is a genuine cost-benefit argument are unaffected and may still exit Accepted-risk with quantifying evidence (F-13 / PB-D9). Inertness is itself a capability argument — a key that can do nothing harmful is bounded by construction — so rows resting on it stay eligible (C.8). Where a fix bounds blast radius rather than removing the capability, the bound is defence in depth and the removal is the primary remedy. This constrains the standing-key rows (C.1, C.4, C.5, C.7 and their kind): enumerating unbounded authorities in §xxix is not an Accepted-risk. It does NOT close PP-D22 (PP5.1) or PP-D23 (PP5.4); those stay per-slot and per G41 row, with this lock as the input that option (b) for an unbounded key cannot satisfy — a constructor-stamped expiry that holds against a hostile holder can, and correcting §xxix after the key is gone or bounded is housekeeping rather than the remedy. **F.1 obligation:** F.1's queue `done-criteria` still names the clamp (`test_boostClampedToLpTrancheFraction`); PP3.3 inverts it to a seating-control case (`code.length` / interface probe / Vault `pool` validation / two-step delayed rebind), with the clamp case retained as a second test and defence in depth. This lock does not edit the queue.

## PP-D42 — done-criteria `path::case`: the CASE NAME (the PP-D40 (vi) invert) is binding at creation, the PATH follows D35 once the PoC lands (user-adjudicated 2026-08-21)

PP-D40 (vi) fixes done-criteria as `path::case` and the queue preamble makes creation names binding on PP3.2. Both are READ AS: the binding half is the CASE NAME — the reproduction-vs-fix invert (A.2's `Bricks` → `DoesNotBrick`, E.6's `IsBurned` → `DoesNotForfeit`), which encodes what the fix must prove and is fixed at adjudication. The PATH is mechanical: the directory the landed reproduction PoC lives in, which D35 fixes by whether the finding is fork-only (`test/fork/<fixture>.t.sol`) or unit (`test/whitehat/P1_<RC>.t.sol`) and which is knowable only once PP3.2 has built the PoC. Where PP3.1 guessed `test/whitehat/` for a finding PP3.2 then reproduces fork-only, PP3.2 corrects the done-criteria path to the fork fixture in the same rung and keeps the case name byte-identical. This is not the ad-hoc cell edit the preamble bars: the preamble bars re-authoring the intended artifact from memory, whereas a path reconciled to a green fork run under D35 discharges (vi) rather than overriding it. It is distinct from the PP-D41 F.1 obligation, which changes the REMEDY a case proves (clamp → seating-control) and is PP3.3's to adjudicate; PP-D42 changes only the directory, is settled by D35 plus a green run, and carries no verdict content. At this lock's creation, across the nine PoCs then filled, exactly one row was directory-split: the five plain rows (A.1, A.3, A.4, C.4, E.1) are fork-to-fork same-case, A.2 / E.6 / F.1 are same-directory inverts, and only E.4 named `test/whitehat/` for a finding the real Vault and `authenticate` chain force fork-only. **E.4 application:** `test/whitehat/P1_E4.t.sol::test_routeYieldFeeDebitsLedger` → `test/fork/AureumFeeRoutingHook.t.sol::test_routeYieldFeeDebitsLedger`, corrected in the companion queue rung; the case name `test_routeYieldFeeDebitsLedger` — the fix-side invert of the landed `test_P1_E4_routeYieldFeeToHookSpendsWithoutDebitingLedger` — is unchanged. Any later fork-only row whose PP3.1 path assumed `test/whitehat/` is corrected the same way when its PoC lands. **Census as of PP3.2ai:** sixteen PoCs filled, twelve carrying fork done-criteria and four unit; six rows have taken the path correction (E.4, E.2, E.3, C.6, G.3, G.2) across seven applications, C.6 twice because its veto leg landed in a different fixture from its grant and rotation legs; no filled row remains directory-split. The queue is the authority on the running figure, and this paragraph is not re-amended per application.

## PP-D31 — The keeper model is (c) BOTH: three surfaces fold on-chain, two stay a funded keeper named as a trust assumption (user-adjudicated 2026-08-22)

Verified on disk before adjudication, not taken from the plan summary. FIVE production-zero surfaces, not six: `updateEMA` (`src/ccb/EMASampler.sol`), `updateMultiplier` (`src/ccb/CCBMultiplier.sol`), `recordScore` (`src/emission/EmissionDistributor.sol`), `advanceTournament` (`src/gauge/GaugeRegistry.sol`) and `poke` (`src/governance/VotingWeight.sol`) each have ZERO call sites across `src/` and `script/`, while carrying 63, 47, 82, 2 and 73 call sites in `test/` respectively. They are not dead code; they are functions only a fixture has ever driven, which is why every green suite in this repo is a hand-operated keeper. **`recordEmissions` is NOT in the keeper set** and the plan's count of six is corrected here: `EmissionDistributor._settlePool` calls it at `:396` on every settlement and it is gated `onlyEmissionsRecorder`, so it is already protocol-driven. **(a) is not a new pattern.** `EmissionDistributor.claim` at `:608` already opens `_accrueGlobal()` then `_settlePool(pool)`, so folding accumulator advancement into an economically motivated user call is a mechanism this codebase already runs. **FOLD SET (a)** — `recordScore` into the first economically motivated settle/claim path, extending that existing lazy settle; `poke` into the recorder path per PP-D18 / B.2, never a free third-party staleness write; `updateEMA` into the first `recordScore`/`claim` of the day for that pool. **KEEPER SET (b)** — `advanceTournament` and `updateMultiplier`, which have no profit motive at all: nobody is paid to advance the tournament, and CCB reads the EMA but never writes it. Both are named as a trust assumption in the constitution and the runbook. **CONSEQUENCE FOR D.5, which is why it lands first or in the same rung.** Under (c) a keeper outage is NOT a global freeze: active pools keep advancing EMA, score and weight through the folded paths, and only idle pools plus the tournament and the multiplier go stale. D.5's freeze-not-zero therefore specifies an IDLE-AND-KEEPER-ONLY freeze with the active paths self-healing, and that target must exist before D.1's `MIN_SAMPLES` floor or D.3's tighter freshness window increase sampler dependence — with no caller, either gate landing first would make every pool immature or stale by default.

## PP2 — Workstream 0 executed 2026-08-21

The 2026-08-18 residual is CLOSED. Measured, not derived. No private key and no API key is recorded here.

**PP2.1 — new deployer EOA, funded, then written.** `git check-ignore -v` on `.env`, `.env.sepolia`, `.env.mainnet` returned `.gitignore:17:.env` and `.gitignore:18:.env.*` for the two overlays. A first `cast wallet new` keypair was discarded after its private key appeared in chat and was never funded. The funded address is `0xbc52e83BfAa56997575EaBa8d33Fc2D47c6eFF4D`, persisted off-chat, derive-checked at length 66. Dust on the old EOA is **0.25 SepETH**, chosen so `GOVERNANCE_MULTISIG` / `pauseManager` on generation 3 can still pull the lever; PP2.3 moves nothing, so "leave gas for PP2.3" is withdrawn. Order was generate → persist → send from the old key → confirm balances → overwrite the three env files. Overwrite-before-fund is the same class of hazard as destroying the signer before the send is known-good; the backup had to outlive the transfer and did. `DEPLOYER_PRIVATE_KEY` in the env files is an empty slot by design (PB-D27 (vii)(3) / PB-D36 (ix)); the live signer is forge keystore account `aumm-sepolia`. `cast send --private-key "$DEPLOYER_PRIVATE_KEY"` failed to decode; the send used `--account aumm-sepolia`. `cast to-wei 0.25ether` (no space) died with `digit 14 is out of range for base 10` and would have set `SEND` equal to the full balance; the send used the wei literal `250000000000000000`. Pre-send: `BAL=5316662104309836821`, `DUST=250000000000000000`, `SEND=5066662104309836821`. Receipt: `status 1`, `from` `0xA851478dbee97375E784e9b98c0D7D599662bF85`, `to` `0xbc52e83BfAa56997575EaBa8d33Fc2D47c6eFF4D`, `gasUsed` 21000, `effectiveGasPrice` 1021010936, block **11536209**, `transactionHash` `0xc8d41523ea42182d9bc50c512dc725601f304f3297684ffee33e045d79954d89`. Post-send balances: old `249978558770344000` (0.25 ether minus `21000 * 1021010936` gas), new `5066662104309836821`. Keystore split: `aumm-sepolia` still `0xA851478dbee97375E784e9b98c0D7D599662bF85`; new account `aumm-sepolia-gen4` is `0xbc52e83BfAa56997575EaBa8d33Fc2D47c6eFF4D`. All three env files then derived to the new address at `keylen=66`.

**PP2.2 — Etherscan key regenerated, old revoked.** New `ETHERSCAN_API_KEY` written to the same three files at `keylen=34` each; old key deleted in the Etherscan dashboard. Create-then-write-then-revoke, not revoke-first.

**PP2.3 — skipped on chain.** PP-D8 (iii) evaluated FALSE under PP-D21 (b). Zero authority-write transactions. Seats stay on `0xA851478dbee97375E784e9b98c0D7D599662bF85`. Recorded in deployment-record section 27.

**PP2.4 — purge.** Ran from `/Users/janus/code/aumm-deploy` on `stage-p-prime` (the main worktree; `expire --all` is repo-global and does not require checking out `main`). Backup length table: every file `pk=0`, many `ek=34` — the deployer private key was never in those env files (keystore posture); the leaked value searchable from the backup is the Etherscan key. `git log --all -S <backup ETHERSCAN_API_KEY>` printed nothing **before** the prune, because `4b589bc` is an orphan and `--all` does not walk the reflog (PB20: that zero does not prove the purge). Positive control, before prune: `git cat-file -t 4b589bc` printed `commit`; `git grep -l -F <backup ETHERSCAN_API_KEY> 4b589bc` listed `.env.mainnet`, `.env.mainnet.bak-pre-PB3.13f2`, `.env.sepolia`, `.env.sepolia.a3-pre`, `.env.sepolia.a5-pre`, `.env.sepolia.a7-pre`, `.env.sepolia.a8-pre`, `.env.sepolia.a9-pre`, `.env.sepolia.bak-pre-PB3.13f2`, `.env.sepolia.bak-pre-a3`, `.env.sepolia.bak-pre-a5`, `.env.sepolia.bak-pre-gen3`, `.env.sepolia.bak-pre-phase3`, `.env.sepolia.bak-pre-phase4`, `.env.sepolia.gen1-bak`, `.env.sepolia.phase3-pre`, `.env.sepolia.step4-pre`, `.env.sepolia.step6-pre`, `.env.sepolia.step7-pre`. Then `git reflog expire --expire=now --all && git gc --prune=now --aggressive` (8398 objects). After prune: `git cat-file -t 4b589bc` → `fatal: Not a valid object name 4b589bc`; the same `git grep` → `fatal: ambiguous argument '4b589bc'`; `git log --all --oneline -S` still printed nothing. `rm -rf ~/aumm-env-backup-20260818-145359` then `test ! -e` printed `backup-gone`. No deployer-key `-S` was run: there was no non-empty `DEPLOYER_PRIVATE_KEY` in the backup to search for, and exporting `aumm-sepolia` to feed `-S` would have put the still-live gen-3 pause key on a command line. The load-bearing proofs are the object deletion and the Etherscan-key `git grep` against `4b589bc` going from a hit list to an invalid-object error.

**PP2.5 — the env slots emptied; keystore-only posture restored.** User-directed 2026-08-21, after the PP2 record landed at `6261677`. PP2.1 wrote the rotated key into `DEPLOYER_PRIVATE_KEY` in all three env files at `keylen=66`, which departs from PB-D36 (ix) — "the env entry stays a documented empty slot rather than becoming a second place a secret can sit" — so PP2.1's closing sentence is SUPERSEDED by this paragraph. The old key's posture had been keystore-only, which PP2.4's own `pk=0` backup table proves, so the rotation had moved a secret from one encrypted store into three plaintext files rather than between two equivalent homes, and `.env.mainnet` was one of them, which is how the posture would have reached Stage R. Ordering was gated rather than assumed: `cast wallet address --account aumm-sepolia-gen4` derived `0xbc52e83BfAa56997575EaBa8d33Fc2D47c6eFF4D` BEFORE any file was touched and the edit was written to run only on that match, because emptying a slot holding the sole copy of a funded key is the hazard class PP2.1 rejected as overwrite-before-fund. Purity was proven per file by `shasum -a 256` over the file with the `DEPLOYER_PRIVATE_KEY` line excluded, before and after: `.env` and `.env.sepolia` both `c9026308`, `.env.mainnet` `b344df6c`, identical across the edit, so exactly one line per file changed and no `.env.*` backup variant was created; and because `ETHERSCAN_API_KEY` sits inside the hashed region, that same identity proves PP2.2's key untouched. After: `pk_len=0` on all three, `git status -sb` clean. The edit could not break a deploy path, measured rather than assumed: `git grep DEPLOYER_PRIVATE_KEY -- script/ test/ tools/ foundry.toml` returns nothing, so the slot has ZERO consumers and was already the documented-empty slot PB-D36 (ix) describes. PB-D36 (ix) therefore needs no amendment — its "presently empty in both" is true again — and the excursion is recorded here rather than by editing a locked P-bis decision. `.env` and `.env.sepolia` shared a hash at the time of the edit, so the active working copy is the Sepolia set and `AUMM_ENV_CHAIN` reads `sepolia`; PB-D35 requires `cp .env.mainnet .env` before any mainnet fork run in PP4. One carry, NOT fixed here: `docs/STAGE_P_BIS_SEPOLIA_RUNBOOK.md` is generation-3 shaped, naming `--account aumm-sepolia` at three sites and `--sender 0xA851478dbee97375E784e9b98c0D7D599662bF85` at three more, plus the L96 posture paragraph naming `cast wallet import aumm-sepolia`; it is correct as a record of what generation 3 ran and wrong as an instruction for generation 4, and it is a PP-D21 refresh item.

## PP10 — a fork fixture's `setUp` mocks decide which defects it can express; read them before attaching a reproduction to it

**Caught 2026-08-24 at PP3.2by (`test/fork/StageIIntegration.t.sol`).** B.3's flash-inflation reproduction was attached to `StageIIntegrationFixture` because that fixture visibly had everything the PoC needed: `Vault.unlock` deposit and withdrawal callbacks, a self-seat as the hook's trusted router at L104, and a `getSender()` implementation at L120. Both new cases failed on their first run, one reporting a BPT balance of `1e30` for an address that had never received BPT.

**Root cause — an inherited blanket mock.** `test/fork/StageGIntegration.t.sol` L226, reached through the fixture chain, calls `vm.mockCall` on every pilot pool for `balanceOf(address)` returning `1e30`, matched on the signature alone and therefore answering for every caller and every argument. Two mechanisms the PoC turned on are consequently no-ops in that fixture and in every fixture beneath it: the F-17 read-cap at `src/governance/VotingWeight.sol` L180—L181 never binds, because the mocked balance always dominates the recorded stake, and `EmissionDistributor._syncDown` always takes its `recorded <= referenceBalance` early return, so no `syncPosition` heal is expressible at all. The second case was not failing; it was unrunnable.

**This is not a fixture defect.** StageG documents the mock at L218—L224 and gives its reason: those fixtures stage an artificial credited-but-holdless position, which the F-17 receipt cap correctly scores as a phantom, so the receipt view is mocked to a dominating value to keep `_syncDown` and `_positionPower` out of the way, and the comment states outright that the receipt invariant is proven in the unit suites and in the real-BPT faithful test. Every word of it held under test: the untrusted exit's burn worked, `totalSupply` fell by exactly the burned amount, and three assertions passed before the mocked view failed the fourth.

**How it presents, and why that is the trap.** A fixture that cannot express a defect fails as a WRONG ASSERTION rather than as a missing capability. A balance of `1e30` against an expected zero reads as a defective reproduction, not as an unsuitable host, and the assertions that pass beforehand make the run look like a nearly-correct test rather than a misplaced one. Helper availability is not fixture suitability — and the helpers are the visible property, while the mocks are not.

**Fix — relocate, do not patch.** Patching in place would have meant mocking the very balance the first case asserts, which is circular, and the second case would have stayed unrunnable regardless. `test/fork/RouterIntegration.t.sol`, on the StageP chain, carries no blanket `balanceOf` mock and seats the canonical Router on the hook allowlist, which is also closer to the plan's own description of the attack than a fixture acting as its own router.

**Generalization.** Before attaching a reproduction to an existing fork fixture, grep its whole inheritance chain for `vm.mockCall` and `vm.etch`, and ask of each hit which mechanism under test it no-ops. A mock installed for one fixture's convenience is inherited silently by every fixture beneath it, and the mechanism it disables is often exactly the one a later reproduction exists to exercise.

**Cross-references:**

- **PB19** (`STAGE_P_BIS_NOTES.md` L1193) — the parent class, whose own rule already names "what layer a mock intercepts" among the things to check before borrowing from working code. This entry exists because PB19's trigger is copying a property from a sibling, while this trigger is choosing a host fixture, and a reader with the second trigger does not think to consult the first.
- **F15** (`STAGE_F_NOTES.md` L600) — the same reachability failure one rung earlier: a recipe that existed and carried the governing rule, but could not be found from the file a cold session actually reads.
- **`test/fork/StageGIntegration.t.sol` L218—L226** — the mock and its stated rationale.
- **PP3.2by** — the sub-step where the misattachment surfaced; reverted rather than patched, and re-authored against the correct fixture.

## PP11 — a zero-hit sentinel is unsatisfiable against any term the drafter's own `Must match` requires to appear in prose

**Caught repeatedly across 2026-08-24, never once costing a fix cycle and never once earning its place.** The §8e.1 USER VERIFY block routinely pairs a zero-expectation with a positive control per PB20. The recurring miss is narrower: the drafter writes a `Must match` bullet REQUIRING some term to appear in a comment or NatSpec, then writes a USER VERIFY line expecting `grep -c` on that same term to read zero. The save is correct, the grep is correct, and the prediction is impossible. It fired on `getSender` at PP3.2by-retry, on `EmissionDistributor` at PP3.2co, on `VotingWeight` at PP3.2ct, and on `AuMTAlreadyBound` at PP3.2dc — four times in one session, each time resolved by reading the file and confirming the hit was the very sentence the prompt had demanded.

**Why PB20 does not already cover it.** PB20 says a zero-hit sentinel proves nothing until the pattern's REACH on the target is measured, and prescribes pairing every zero-expectation with a known-positive control. Both halves were being followed. The reach failure here is not in the target file's pre-existing content but in the payload the drafter is about to commission — the prompt and the sentinel are written in the same breath, and the sentinel is checked against the file as it exists rather than as it is about to exist. Adding a positive control does not help: the zero still reads as one or two and still looks like a defect.

**The fix, demonstrated mid-session.** Sentinel the CODE form, not the bare term. `grep -c 'vm.mockCall(\|vm.etch('` with the opening parenthesis reads zero on a file whose comment discusses `vm.mockCall` in prose, where the bare pattern reads one; the same trick distinguishes `function getSender` from a NatSpec mention of `getSender()`. Where no code form exists — a reserved done-criteria case name, say — the correct move is to keep the zero-hit sentinel and NOT require the term anywhere in the payload, which is what the reserved-name sentinels do correctly throughout the stage. Either discipline works; writing both a requirement and a zero-expectation for the same string does not.

**Generalization.** Before writing a zero-expectation into a USER VERIFY block, grep the DRAFTED PROMPT for the same pattern. If the prompt requires the term, either narrow the sentinel to a form only code can produce, or drop the requirement. This is the drafting-time twin of PB18: PB18 says measure the payload rather than predicting its counts; PP11 says the payload includes the prose you are about to commission.

**Cross-references:**

- **PB20** (`STAGE_P_BIS_NOTES.md`) — the parent reach rule; its remedy, a paired positive control, does not resolve this variant.
- **PB18** (`STAGE_P_BIS_NOTES.md`) — the measure-do-not-predict sibling, whose subject is counts rather than reach.
- **PP3.2by-retry, PP3.2co, PP3.2ct, PP3.2dc** — the four occurrences, all harmless, all resolved by reading the saved file.

## PP12 — a `-r` grep handed explicit file arguments does not recurse, and its short result does not look truncated

**Caught at PP3.2do, 2026-08-25, one rung before the wrong number would have entered the queue as fact.** The H.1 pre-flight needed the size of the spec-side fee divergence, and the sweep was a `grep -rn` for the two fee literals over `*.md`, run from the `aumm-site` clone root and piped through `head -12`. It returned 8 matches across 5 files, and that figure was about to become the queue cell's edit-surface estimate. The true figure is 17 occurrences across 11 files. The `-r` was inert: the shell had already expanded `*.md` into an explicit list of root-level files, and grep does not descend into directories it was never handed. The nine hits inside `dist/aumm-skill/`, a GENERATED mirror of the same spec documents, were invisible — and that mirror is what an AI consumer loads, so a fix sized from the 8-figure would have corrected the sources and left the artifact people actually read still saying 0.03%.

**Why the result did not look wrong.** The usual tell for a truncated sweep is output ending exactly at the limit. Here `head -12` returned 8 lines, which reads as a complete result with room to spare; the narrowing happened upstream of `head`, in the file set, where nothing announces itself. A sweep over too small a universe returns a well-formed, plausible, internally consistent answer.

**Why neither PB20 nor PB18 already covers it.** PB20 governs the PATTERN's reach on a target — a zero that could never have matched. Here the pattern was correct and the count non-zero; what failed was the TARGET SET's extent. PB18 says a count is a prediction until a tool produces it, but a tool did produce this one and ran correctly, over a smaller universe than intended. Neither parent catches a measurement whose denominator is wrong.

**The second trap in the same command.** The corrected form must quote its pattern: `--include=*.md` unquoted is glob-expanded by zsh before grep ever sees it and dies on `no matches found`, which is at least a loud failure. `grep -r --include='*.md' .` is the form that works, taking a DIRECTORY argument and never a glob.

**Generalization.** A measurement's universe is part of the measurement, so report the denominator beside the numerator. Pair every tree sweep with its file count, `grep -rl` piped to `wc -l` alongside the occurrence count, because an unexpectedly small file count is the only symptom a silently narrowed universe produces. Never hand `-r` a shell glob.

**Cross-references:**

- **PB20** (`STAGE_P_BIS_NOTES.md`) — the parent reach rule, whose subject is a pattern's reach on a target rather than the target set's own extent.
- **PB18** (`STAGE_P_BIS_NOTES.md`) — measure rather than predict; here the tool ran and was still wrong, because the universe was.
- **PP3.2do** — the rung where it surfaced and was corrected before the figure was recorded; that queue cell and the PP3.2dq §11 obligation both carry 17 across 11 files.

## PP13 — measuring the payload does not discharge measuring the file it lands in

**Caught across PP4.1, 2026-08-26 to 2026-08-27, in roughly a dozen verify blocks, none of which failed a save.** PB18 was followed throughout: every payload went through `wc -l` and `grep -o` before handoff, and every payload count was right. What was wrong, repeatedly, was the number DERIVED from that measurement about the file the payload lands in. PP4.1c12 is the clean instance — BLOCK A was measured as content, then the file total was predicted as `150 + 15` for a block that is 23 lines, giving 167 against a true 175. The save was correct; the expectation was not.

**The same shape in four other costumes.** An anchor cited at its PRE-INSERT position when inserting necessarily shifts it (PP4.1b put `---` at L274; it had moved to L275). An occurrence count asserted from the drafter's reading of the NEW text alone when the term already lived elsewhere in the file (`FORTY-ONE` predicted 1, measured 2, the earlier amendment already carrying it). A count asserted for a form the code does not actually contain (`address vault_` predicted 2, measured 1, because the guard reads `vault_ == address(0)` and holds no such substring). And a roster recalled rather than counted (`revert ZeroAddress();` predicted 9, measured 11, three other guarded setters forgotten).

**Why PB18 does not already cover it, and why that let the habit survive.** PB18 governs the PAYLOAD — the text about to be inserted, measured before handoff. Every miss above was a claim about the FILE: its totals after the insert, its existing occurrences, its anchors' positions once shifted. Two different universes, and measuring the first feels like diligence for the second. The command WAS run, the number it produced WAS correct, and it answered a different question than the verify block asked.

**The rule.** A `Must match` or USER VERIFY number about the SAVED FILE is a prediction until measured against that file. For a pure insertion the cheapest instrument is `git diff --stat`, whose insertion count is independent of the drafter's arithmetic; for an existing-occurrence claim it is one `grep -c` on disk BEFORE the expectation is written; for an anchor's position it is the observation that everything below an insert has moved. Where a number genuinely cannot be measured in advance, label it DERIVED in the chat text so a mismatch reads as a bad prediction rather than a bad save.

**Cross-references:**

- **PB18** (`STAGE_P_BIS_NOTES.md`) — the payload half of the same discipline; this is its file-state complement, not a restatement of it.
- **PP4.1c12** — the clean instance: payload measured, file total derived, 167 against 175.
- **PP4.1b / PP4.1c4b / PP4.1c11-pre** — the shifted anchor, the substring form the code does not contain, and the term already living elsewhere.

## PP14 — a fixed `tail -N` selects by position, and truncated output looks exactly like a report

**Caught twice in one session, 2026-08-30, at the PP4.7 rung gate and again at the PP4.8e3 build gate.** The rung-7 fork roster ran 25 file-scoped invocations through a loop capturing `tail -1` per file. Forge prints its `Tip: run forge test --rerun` line AFTER the summary when a run has failures, so for the one file that failed, the captured line was the tip while the `[FAIL]` rows and the tally scrolled past unread — the failure text was destroyed and no cause could ever be assigned, which is why `WK18ThinVenuePumpSim`'s transient is recorded as UNEXPLAINED rather than diagnosed. Hours later `forge build 2>&1 | tail -20` captured only trailing lint warnings from an unrelated fixture and cut off the verdict line, leaving the build's success unreadable at the exact rung whose success criterion WAS compilation.

**Why it survives a careful reader.** Truncated output does not announce itself. A tip line reads as a summary; a tail of warnings reads as a clean run that has warnings; an empty filter result reads as nothing to report. In all three the command ran, the tool answered correctly, and the harness threw the answer away — so the failure is invisible precisely where a reader is looking for reassurance, and both instances here were caught by accident rather than by the check.

**The rule.** Select on the OUTCOME, never on position. Filter for the line forms that carry the verdict — `^Ran .* test suite`, `^\[FAIL`, `Compiler run successful`, `Compilation failed` — and where the tool offers an exit status, read THAT instead of any text, because `EXIT=$?` cannot miss a line form the drafter failed to anticipate. A fixed `tail` is admissible only where a tool's last line is guaranteed to be its verdict, which forge guarantees in neither of its two most-used commands.

**Cross-references:**

- **PB20** (`STAGE_P_BIS_NOTES.md`) — the pattern-reach sibling: a zero-hit sentinel proves nothing until the pattern's reach on the target file is measured.
- **PP12** — the file-set sibling: a `-r` grep handed explicit file arguments does not recurse, and its short result does not look truncated.
- **PP4.7i** — the WK18 transient, recorded unexplained because this defect destroyed its evidence; **PP4.8e3b** — the build verdict lost to `tail -20` and recovered by `EXIT=$?`.

## PP3.3 — Family A verdicts (A.2 and A.3 locked; A.1 and A.4 deferred)

**Locked.** Both rows exit **Fix**, and neither severity moves.

- **A.2 — Fix (High, unchanged).** The capability is available to any address at a cost of two wei, permanently, with no rescue path in the contract. PP-D41's bar is a capability that is absent or bounded by code against a hostile holder, and this is neither; it is not an economic cost-benefit case either, the attacker's cost being approximately zero. Re-verified at HEAD before locking: the absolute residual `if (residual != 0) revert HelperBalanceNonZero(residual)` appears TWICE and identically, at `src/gauge/SwapAndDepositToBodensee.sol:351-354` and `:381-384`, once per rail, which is what makes the full brick cost two wei rather than one.
- **A.3 — Fix (Medium, unchanged).** The trigger needs no attacker at all: a third-party rate provider reporting below par bricks every governance entry, because `_createProposal` donates before it records. Nothing bounds it in code. This one is not a design question — the correct form ALREADY EXISTS in this repository at `src/fee_router/AureumFeeRoutingHook.sol:641-643`, `if (postReserve <= preReserve) revert ReserveDidNotRise(...)`, carrying a PB-D68 (xvii) comment stating why exactness fails on a DONATION: that branch round-trips raw through scaled18 rather than deep-copying `maxAmountsIn`, so the consumed amount is not caller-predictable. The sibling at `:424-427` still asserts exact equality. The fix is a backport of a proven form.

**Severity: neither moved, deliberately.** The queue preamble lets PP3.3 move a severity, but only against a recorded reproduction, and both rows reproduce exactly as filed. A.3's consequence matches A.2's while its trigger requires an oracle state where A.2's requires two wei from anyone — which is precisely what the report's High/Medium split already encodes. Moving either would re-litigate a post-review figure without new evidence.

**Deferred with cause.** Neither row is unripe; each waits on a decision that IS its disposition, and PP-D4 makes a verdict a lock rather than a placeholder, so "Fix pending PP-D22" is not a PP3.3 product.

- **A.1** — waits on **PP-D17** (seed accounting, decided ONCE for governance and emission). Its other constraints, PP-D10 and PP-D24, are locked.
- **A.4** — waits on **PP-D22** (the dissolution handoff). The interaction is direct rather than incidental: rotation 5, `setDonateAuthorizer`, loses its rationale entirely once A.4's fix makes governance's donate entry immutable, so the shape of A.4 and the decision on whether to run the handoff are one question.

**Cross-family: this batch does not own its own fix rung.** PP-D11 binds A.2, A.3 AND **E.1** into ONE PP4 rung (rung 2) in one shape — strict rise or delta, never an absolute `balanceOf(this)` — on the G10 ground that the project found the right form once at PB-D68 (xvii) and did not propagate it. E.1 is unblocked and locks in the E batch. Verdict batching is per family; this fix rung is not, and PP4 must not lose the grouping because the A batch closed first.

**A.2's sweep is load-bearing, and the fix-intent cell understates it.** That cell offers "delta assertion or donate `balanceOf(this)`" as alternatives. The second half alone is NOT a complete remedy and the queue cell is deliberately left unedited here rather than rewritten mid-adjudication. Verified at HEAD: a rescuer cannot clear a stray residual by donating it, because the Vault rejects a sub-minimum amount at `_ensureValidTradeAmount` (`lib/.../Vault.sol:1509`, reverting `TradeAmountTooSmall()` at `:1524`, reached from the add-liquidity path at `:689`) before the helper's residual check is ever evaluated. A sweep or permissionless flush is therefore part of the remedy, not an optional companion to it. `IncendiaryRegistry.buyBoost` is a fourth victim of the same assertion.

## PP3.3 — Family B verdicts (six locked; B.3 and B.4 deferred)

**Locked.** Five exit **Fix** and one exits **Accepted-risk**; no severity moves. Each was re-verified at HEAD before locking rather than inherited from its reproduction rung.

- **B.1 — Fix (High, unchanged).** The quorum denominator is `_totalQualifiedWeight` (`src/governance/VotingWeight.sol:114-115`), the sum of POKED checkpoints, so it counts only holders who opted in. At launch the trace is provably empty and the turnout-relative two-thirds bar degenerates unopposed.
- **B.2 — Fix (High, unchanged).** `recordWithdrawal` (`src/emission/EmissionDistributor.sol:579-598`) contains ZERO references to `VotingWeight` — it writes the clock and `userLP` and nothing else — so a closed position keeps voting until some third party pokes it.
- **B.5 — Fix (Medium, unchanged).** `_syncDown` zeroes `effectiveQualBlock[pool][user]` unconditionally at `:525` and `syncPosition` is permissionless and names its victim at `:532-533`, making the §viii reset a stranger's option to exercise one block before a snapshot.
- **B.6 — Fix (High, unchanged).** Both liquidity callbacks trust `IRouterSender(router).getSender()` and `recordWithdrawal` zeroes the clock on any `amount > 0` (`src/fee_router/AureumFeeRoutingHook.sol:590-591`), so a trusted router zeroes any holder's matured clock for a dust burn.
- **B.7 — Fix (Medium, unchanged).** See the F-15 carry below.
- **B.8 — Accepted-risk (Low, unchanged).** The first Accepted-risk of the stage, and it clears PP-D41 on capability rather than on trust: the corrective is PERMISSIONLESS, any third party re-poking any holder restores the era-1 exponent, so the drift is bounded by code and not by conduct. The gain is also quantified and modest, `ema^(1/12)`, which is 3.16x at 1e6 CHF and 4.64x only at 1e8. **That quantification is MEASURED, not derived** — `test/whitehat/P1_B8.t.sol:94` asserts the ratio against a `1_000_000^(1/12)` constant and `:124` asserts the self-heal. Keep the contrast in view when D.7's Info half and C.8's factory-owner reach their batches: both rest on source argument alone, where this row has a test artifact. The row's own fix intent sets its floor at "at minimum document".

**Deferred with cause.**

- **B.3** — waits on **PP-D17** (seed accounting). Its done-criteria additionally names the `IERC20(pool).totalSupply()` branch PP-D17 does not recommend, so the name cannot settle before the decision does.
- **B.4 (composite, Critical)** — waits transitively. It closes via B.1 + B.3 + C.4, and B.3 (PP-D17) and C.4 (PP-D25) are both deferred. B.1 locking Fix does not release it.

**The composite rule, recorded once here and applying to G.4 as well.** A composite row defers until EVERY constituent is locked. Its disposition is not independently determinable — if a constituent's own decision resolves away from Fixed, the composite's closure path breaks — and PP-D4 makes a verdict a lock rather than a placeholder. This also corrects the PP3.3 pre-flight readiness map, which was built by grepping the ordering-constraints column for open PP-D ids and therefore missed both composites, whose dependency is expressed in prose ("closes via ...") rather than as a PP-D reference. **The corrected census is 40 unblocked and 13 deferred, not 42 and 11.**

**B.7 reopens F-15's Accepted-risk disposition (PB-D10), and the reopening is narrow.** `vetoProposal` banks absolute `vetoSupport` and divides by a LIVE `votingWeight.totalSupply()` at `src/gauge/VaultClassRegistry.sol:284-286` with no zero guard. PB-D10's evidence suite `test/whitehat/F15_vetoDenominatorInflation.t.sol` carries six tests, NONE of which burn, withdraw or revoke, and its own header states that it bounds defender INFLATION. Those six assertions remain true; what fails is PB-D10 reading them as bounding the whole risk, because B.7's ratchet moves the denominator DOWN. Three carries, none of them this sub-step's work: **(1)** F-15's whitehat ledger row needs re-statusing, which is PP3.4's; **(2)** PB-D10 in `docs/STAGE_P_BIS_NOTES.md` needs a reopening amendment, which is a cross-stage beat; **(3)** the zero-denominator face is NOT an edge but the launch condition — `totalSupply()` returns the poked sum, structurally zero for the first 60 days of every chain, which is F-21's condition at a second call site, and `AureumGovernance.sol:367` carries the PB-D62 guard `if (snapshotSupply == 0) return false` while `VaultClassRegistry` has none, so during that window every veto panics and every class finalizes unopposed.

**B.1's done-criteria rewrite is deferred to PP4 rung 12.** `test_quorumDenominatorCountsUnpokedHolders` names a recorder-side denominator that PP-D14 says breaks INV-1 by construction, but PP-D14 also requires the redesign to STATE the invariant that replaces INV-1 and assigns that work to PP4 rung 12 in its own owner column. Inventing a replacement case name now would bake a fix shape ahead of the invariant justifying it, which is the shape PP-D41 flags on F.1's clamp. The verdict is Fix; the name waits.

**A recurring meta-defect, named but not filed.** Three rows adjudicated so far are the same failure: a correct form found once and never propagated. A.3 (the hook's strict rise at `AureumFeeRoutingHook.sol:641-643`, its sibling helper still asserting exact equality), B.7 (F-21's zero guard on the governance path, absent from `VaultClassRegistry`), and H.5 (`version()` reporting an Aureum name on an upstream body). PP-D11 already names the class via G10. No RB row is opened here — PP3.5 owns the register — but the pattern is recorded now so that beat does not have to rediscover it.

## PP3.3 — Family C verdicts (five locked; C.1, C.2 and C.4 deferred)

**Locked.** All five exit **Fix** and no severity moves. Each rests on an anchor re-verified at HEAD rather than inherited from its reproduction rung.

- **C.5 — Fix (High, unchanged).** `VaultAdmin.enableRecoveryMode` skips `_authenticateCaller()` whenever the pool or the Vault is paused — Balancer's own comment says so — the recovery bit never expires, and `unpausePool` does not clear it. A pool's `pauseManager` therefore opts that pool out of the 50/50 protocol-fee split for roughly four years at about 120k gas, against a repair path costing 17 days and 1,000 svZCHF.
- **C.6 — Fix (Medium, unchanged).** The IDENTICAL rail conjunct is evaluated at two different times: `src/gauge/GaugeEligibility.sol:434-435` at activation, where `true` buys a permanent emission entitlement that a later `false` does not remove, and `:454-455` at execute, where `false` annuls a passed two-thirds vote and burns the proposer's bond. The row's own corrections bound it and are kept: the flag confers ZERO governance weight and the key cannot mint to itself.
- **C.7 — Fix (Medium, unchanged).** `registrySetter` is deployer-pinned at `src/ccb/CCBMultiplier.sol:143` and never sealed: `setMiliariumRegistry` appears twice in `script/` and BOTH are the `TVLOracle` leg, so no script fires the `CCBMultiplier` one-shot. The hook's `_incendiaryAdmin` is likewise unfired and, unlike the CCB slot, has no getter and no clamp.
- **C.8 — Fix (Medium, unchanged).** Exactly TWO occurrences of `code.length` exist in all of `src/`: a NatSpec at `src/gauge/SwapAndDepositToBodensee.sol:294` documenting its own absence, and one real guard at `src/governance/AureumGovernance.sol:279`. That single guard sits on the only REVERSIBLE path in the set, while all fifteen one-shot and irreversible seatings guard `!= 0` alone, none probing an interface and none carrying a deploy-time read-back.
- **C.9 — Fix (Low, unchanged).** `if (depositAmount == 0) return 0;` at `src/fee_router/AureumFeeRoutingHook.sol:611` returns before the `ReserveDidNotRise` proof-of-delivery at `:641-643`, while `StrandedFeesRecovered` still reports the full `amountIn`. The row's "zero test coverage" clause was RETRACTED by the report itself on a truncated grep, and that retraction stands.

**PP-D41 on the two standing-key rows it names here.** Neither C.5 nor C.7 may rest on trust, and neither does. C.5's capability is unbounded by code: any pool's `pauseManager`, no expiry inside the Vault's four-year window, no governance path to reverse it. C.7's CCB face IS bounded — the `[0.75, 1.25]` clamp is an inductive invariant, so a repoint buys availability rather than magnitude — but PP-D41 is explicit that a blast-radius bound is defence in depth while removal is the primary remedy, and the hook's `_incendiaryAdmin` carries no bound at all. Both exit Fix.

**Deferred with cause.**

- **C.1** — waits on **PP-D22 AND PP-D23**, the only doubly-gated row in the queue. Its done-criteria is a PP-D23 clause plus an `aumm-site` §xxix commit.
- **C.2** — waits on **PP-D22**; its done-criteria IS a PP-D22 clause, so the decision and the artifact are the same object.
- **C.4** — waits on **PP-D25** (the authorizer probe versus deliberate ossification).

**Row granularity versus face granularity, settled here for the rest of the stage.** The plan expects three Accepted-risk dispositions — B.8, D.7's Info half and C.8's `vault-factory-owner` — but two of those three are sub-row FACES and the queue carries one verdict cell per row. C.8 is the first to hit it: its root cause is the inconsistent-hardening class across fifteen seatings and plainly needs fixing, while `vault-factory-standing-owner-is-inert` is one of its six audit files, Info, and genuinely inert. PP-D41 keeps inertness eligible, but `renounceOwnership()` is already in C.8's fix intent at near-zero cost, so the face is remediated WITH the row rather than split out of it. **The consequence, stated so PP3.4 does not re-derive it: at queue-row granularity the plan's three expected Accepted-risks collapse to ONE, B.8, already locked at PP3.3b; D.7's Info half and C.8's factory-owner are intra-row notes.** PP3.4 may still split an `F-NN` ledger row per face where a face warrants it, the F-13 precedent.

**C.5's unestablished residual is CARRIED, not chased, and its owner is named.** `CLAUDE.md` §11 records this as an open thread for PP3.3, so it is discharged here by assignment rather than by silence. The measurement stands: control 24,963,783,544,357 against recovery 2,483,268,481, about 10,053 to 1, with the swap still succeeding. The SOURCE of that 2,483,268,481 residual is NOT established — the hook held zero svZCHF and zero sUSDS immediately before that swap, so it is not swept dust — and per PB22 no cause is asserted that was not verified. It does not bear on the verdict, which is Fix wherever the residual originates. The settle path is to instrument the hook's balance across the swap inside the existing PoC, and ownership passes to **PP4's C.5 rung**, which re-measures that path anyway. Turning a verdict batch into a fork investigation is the wrong trade.

## PP3.3 — Family G verdicts (five locked; G.4 deferred)

**Locked.** All five exit **Fix** and no severity moves. Each rests on an anchor re-verified at HEAD.

- **G.1 — Fix (Medium, unchanged).** `state()` returns `Succeeded` on `eta == 0` at `src/governance/AureumGovernance.sol:340` with NO time bound; expiry exists only downstream of `queue`, so a proposal never queued sits outside every window forever. `queue` at `:388` and `execute` at `:401` are both permissionless, and `TimelockNotMet` reverts at `:407` BEFORE `p.executed = true` at `:408`, so a premature attempt never consumes the ticket.
- **G.2 — Fix (Medium, unchanged).** A composition challenge is a mandate against a SLOT INDEX, not against an occupant: propose passes `targetPool_` as `address(0)` at `:244`, discarding the incumbent, and execute re-resolves `SLOT_REGISTRY.poolAtSlot(p.slot)` at `:430` and revokes whoever is standing there. Two honest challenges on one slot therefore let the second revoke the winner of the first.
- **G.3 — Fix (High, unchanged).** `activateGauge` is permissionless, `proposeCompositionChallenge` reads the candidate's gauge status ZERO times, and `registerGaugeFromComposition` reverts `AlreadyGauged` on an `Active` pool while no writer anywhere returns a status to `None`. A credible candidate is therefore pre-poisonable permanently, years before anyone proposes it.
- **G.5a — Fix (Medium, unchanged).** `VaultClassRegistry.finalizeProposal` (`:295-303`) tests only `finalized` and the veto window before writing `admittedClasses[...] = true`: no deadline and no re-check of admission. The row's correction is kept — bonds are never refundable, so a stockpile costs N x 1,000 svZCHF sunk, while the live half needs no stockpile at all.
- **G.5b — Fix (Info, unchanged).** Neither `finalizeProposal` nor `vetoProposal` bounds `proposalId` against `nextProposalId`, and a zero struct's `createdBlock == 0` makes the window test pass at any block above `VETO_WINDOW_BLOCKS`, admitting `address(0)` as a vault class. G14's heuristic explains why this is the single instance: the zero record AUTHORISES an action here and merely permits an opportunity at the other nine sites.

**Two severities are held ACTIVELY, and a later reader must not correct them upward.** Both carry provenance that reads like an error if the provenance is lost. **G.1's Medium** is an anti-double-count: it was raised Medium to High at validation on the `VaultAuthorizerChange` consequence and RETURNED to Medium at review, because that High leg is already counted in C.4, so restoring it would count one consequence twice across two rows. **G.2's Medium** is a considered hold carrying an explicit open invitation — the report says a reviewer who accepts G21's attendance-bar result may raise it to High — and the queue preamble permits a move only against new evidence, which this batch did not produce. Both stay where the report's post-review figure put them.

**Deferred with cause.**

- **G.4 (composite, Medium)** — closes via C.2 + G.2 + B.1 + a global challenge rate limit, and C.2 is PP-D22-gated, so the composite rule recorded at PP3.3b applies. One further point belongs here rather than with the fix: **the global challenge rate limit is a fix element with NO queue row of its own**, so it appears in no verdict batch and the census cannot track it. That is the same shape as PP-D9's companion re-check, and PP4 must carry both outside the row census or lose them.

**Cross-family fix bindings, verdict-independent.** Three of the five rows locked here have fix sequencing tied to rows that are still deferred; the verdicts do not wait on that, the sequencing does. **PP-D30** binds G.2 and G.3 into ONE rung over the same six lines at `AureumGovernance.sol:426-433`. **PP-D9** adds a mandatory companion to that rung, the `GaugeChallenge` execute-time `slotOf == 0` re-check, which carries no queue row. **PP-D19** orders G.5a before C.2 makes `revokeVaultClass` reachable, and C.2 is deferred. **PP-D29** orders G.1 after or with A.1, and A.1 is deferred.

## PP3.3 — Family D verdicts (seven locked; D.7 deferred)

**Locked.** All seven exit **Fix** and no severity moves. Each rests on an anchor re-verified at HEAD.

- **D.1 — Fix (High, unchanged).** Maturity is pure elapsed-block arithmetic with no sample-count floor across all three consumers, and the cold-start seed is a raw spot assignment, so a pumped seed left alone for the window and refreshed once reaches every consumer at almost the attacker's chosen value. F-05's analysis named this residual; its FIX is what was incomplete.
- **D.2 — Fix (High, unchanged).** See the acceptance refutation below.
- **D.3 — Fix (Medium, unchanged).** Decay is applied once per CALL while the freshness gate is measured in TIME and tolerates fourteen times the daily cadence, so a drained pool's EMA is held high for weeks by sampling exactly as rarely as the gate permits.
- **D.4 — Fix (Medium, unchanged).** `CCBMultiplier.updateMultiplier` reads three raw `tvlEMA` values with none of the gates the other three consumers apply, over a gauge set anyone can grow for 100 svZCHF, and a PER-POOL cadence guards a GLOBAL baseline. Bounded to plus or minus 25% by the clamp, which is what holds it at Medium.
- **D.5 — Fix (Medium, unchanged).** A fourteen-day outage of the unfunded `updateEMA` keeper drives `_positionPower` to zero for everyone, after which permissionless `poke(anyHolder)` is a one-way ratchet writing zeros into the quorum denominator until F-21 defeats every proposal; the kill is permanent per proposal even though live state recovers.
- **D.6 — Fix (High, unchanged).** `TVLOracle._venueRatio` calls `getPoolTokens`, `getPoolData` and `getNormalizedWeights` BARE at `:328-330`, so one reverting third-party `getRate()` anywhere in the roster reverts `tvl()` protocol-wide, `updateEMA` can never again advance its stamp, and fourteen days later the electorate is permanently drainable to zero.
- **D.8 — Fix (Medium, unchanged).** The loop accumulates `miliariumAgg` over the LIVE `miliariumRegistry.miliariumPoolsCount()` at `:226-227` and `:231` divides by the literal `MILIARIUM_POOL_COUNT = 28` declared at `:62`. The divisor is not the length walked. The baseline runs 7.143% low against a 0.1% dead zone, seventy-one times it, so a pool at the true constellation average is stepped DOWN every epoch where the anti-cyclical rule should step it up. `poolCount` is already in scope at `:227` for the fix.

**D.2 refutes an accepted-risk rationale written into the source itself, and the refutation is the same shape as B.7's reopening of F-15.** `src/ccb/EMASampler.sol:15-18` states the acceptance in its own NatSpec: the single-block-spike attack surface is accepted and defended in depth by F-10 efficiency-tournament tier caps, a six-week smoothing window and the Year-1 buffer, explicitly not at the EMASampler layer. That reasoning assumes a spike sitting INSIDE an otherwise honest series. But `:130-134` awards one exclusive sampling slot per pool per day to whoever calls first, and `:151` stamps `lastEMAUpdateBlock` to that caller's own block, so a bot calling first each day authors one hundred percent of the sequence. Smoothing cannot dilute an attacker who owns every sample. **Carried for PP4 and deliberately not acted on here: that acceptance rests on three legs, and a second leg is E.7a's subject, which reproduced every efficiency numerator at zero with ranking collapsed to address order. The acceptance must not be restored on the strength of its remaining legs without re-checking them, and E.7a's own verdict is still pending.**

**D.5's Medium is load-bearing and must be held actively, exactly as G.1's and G.2's are.** The plan states the reason: recoverable by anyone for gas, which is the distinction from D.6. Verified — `VotingWeight.poke(address holder)` at `:136` is `external` with no access modifier, so any party can undo the ratchet, while D.6's oracle revert can be cleared by NO caller at all. That asymmetry is the entire difference between this Medium and that High, and a later reader who levels them has erased the reason both figures are where they are.

**Two structural results this batch produces.**

- **The `ccb` redeploy unit is now fully adjudicated.** It carries exactly five rows — C.7, locked at PP3.3c, plus D.1, D.3, D.4 and D.8 locked here — and all five exit Fix. It is the first redeploy unit in the queue with no unlocked member, which matters because PP-D12 prices redeploys per unit rather than per row. D.2 is `hook`, D.5 is `governance` and D.6 is `tvl-oracle`, so those three do not join it.
- **PP-D31's fold set interlocks three of these rows.** Its FOLD SET (a) moves `updateEMA` into the first `recordScore` or `claim` of the day and `poke` into the recorder path per B.2. That is simultaneously the structural answer to D.5's unfunded-keeper root and a material change to D.2's monopolisation surface, because sampling that rides an economically motivated path stops being a dedicated bot's exclusive daily slot. PP-D31's own ordering then puts D.1 and D.3 after or with D.5.

**Deferred with cause.**

- **D.7** — waits on **PP-D23**. Its Info half is one of the two intra-row Accepted-risk notes settled at PP3.3c, so the row-granularity rule already applies to it when PP-D23 releases the row.

## PP3.3 — Family E verdicts (ten locked; E.9 deferred)

**Locked.** All ten exit **Fix** and no severity moves. Each rests on an anchor re-verified at HEAD.

- **E.1 — Fix (High, unchanged).** `distribute()` mints exactly `pendingAccrual`, settles it, then requires `balanceOf(this) == 0` with NO entry snapshot, so one wei by transfer or any LP's `claim(pool, channel)` reverts every future call. The revert is the identical `HelperBalanceNonZero` form A.2 carries, which is why PP-D11 binds the three sites into one rung and one shape.
- **E.2 — Fix (Medium, unchanged).** The fee controller's entire `authenticate` surface resolves to `AureumGovernance` after the Stage-K handoff and is reachable by no caller, while the permissionless `collectAggregateFees` keeps pushing swap-leg protocol fees into that dead ledger.
- **E.3 — Fix (Medium, unchanged).** `_globalProtocolYieldFeePercentage` is never initialised, its only write site being the `authenticate`-gated setter, so every pool is stamped 0% at registration and the constitution's 10% ERC-4626 skim is 0% in code.
- **E.4 — Fix (Low, CONTINGENT — see below).** `routeYieldFeeToHook` approves and lets the hook pull `amount` from the controller's commingled balance without debiting `_protocolFeeAmounts[pool][token]` and without bounding `amount` by the pool's credit.
- **E.5 — Fix (Medium, unchanged).** `recordScore`'s `isGaugeApproved` gate blocks the CORRECTION as well as the abuse: a revoked pool's score can be removed from `totalScore` by no caller, `claim` has no gauge gate, and every surviving pool is diluted permanently. The constitution's "loses: AuMM emissions" is false in code.
- **E.6 — Fix (Medium, unchanged).** `_accrueGlobal`'s `totalScore == 0` branch advances `lastAccrualBlock` without integrating, so a window in which every score self-cleared forfeits that window's entire LP tranche, and the permissionless `claim` lets any caller book the forfeiture at a moment of their choosing.
- **E.7a — Fix (Medium, unchanged).** `setFeeRecorder` appears in `script/` ZERO times, so every `numeratorSma` is zero, `computeEpochSnapshot`'s sort falls through to its address tie-break, and the single highest-addressed pool is clamped to about 0.1% against an honest 3.6% share, permanently after the handoff.
- **E.7b — Fix (Medium, unchanged).** `advanceTournament` enumerates the whole active set in one transaction with a per-pool external call, a cold `SSTORE` and an O(n-squared) sort, and the revert is self-perpetuating because whatever the last affordable snapshot wrote freezes forever, caps included. Measured at PP3.2: 1,152,749 gas at 30 pools and 3,447,068 at 90, a marginal 38,238 per pool, extrapolating to 34,031,820 at the row's 890 pools against a 30M block — and a LOWER bound, since ascending seeding hands insertion sort its best case.
- **E.7c — Fix (Medium, unchanged).** The clamp solves for a score against `totalScore` as it stands in the caller's own block and freezes it, and `recordScore` is permissionless, so the caller picks the denominator and the realised share drifts above the cap.
- **E.8 — Fix (Medium, unchanged).** `poolBoostCursor` advances only inside the non-zero-registry guard, so a rebind after the H-D29 valve pays the whole unbound window's backlog on top of a full tranche. **The verdict is Fix on the REPRODUCED distributor-side face; the registry-side half — `buyBoost` holding no distributor reference, so buyers keep paying for a stream nothing will deliver — is NOT reproduced**, per the §11 carry-forward, so PP4 implements that half against an unproven reproduction. E.8 also closes with F.1's clamp, which bounds the backlog.

**E.4's Low is CONTINGENT and expires on its own fix, which is a different thing from the severities held actively elsewhere.** D.5's Medium, G.1's Medium and G.2's Medium are each stable figures held for a standing reason. E.4's Low is not: the row is Low ONLY because E.2 makes the caller unreachable, and the plan names it a fix-blocker in those words — restoring reachability makes the missing debit live in the same transaction. So "Low" here must never be read as "safe to defer"; it is a severity that becomes wrong the moment the row above it is repaired, and PP-D13 exists to prevent exactly that window.

**PP-D13's chain crosses two families AND two redeploy units, and neither fact is visible from any single row.** The order is `E.4 debit` then `E.2 reachable withdrawals` then `A.3 strict rise` then `E.3 non-zero yield fee`, with no step before its predecessor. Two consequences for PP4. First, **A.3 was already locked at PP3.3a and sits in TWO orderings at once** — PP-D11, one rung and one shape with A.2 and E.1, and PP-D13 as step three here — and both must be satisfied. Second, **the chain alternates redeploy units**: E.4, E.2 and E.3 are `hook` while A.3 is `emission-stack`, so this cannot be discharged by a single hook redeploy; an emission-stack change has to land BETWEEN E.2 and E.3.

**E.3's softening depends on a capability C.4 may remove.** The report corrects "can never be set" as one word too strong, because an authorizer change reaches the setter — but that path is `ProposalType.VaultAuthorizerChange`, which is C.4's subject and is DEFERRED on PP-D25, the authorizer probe versus deliberate ossification. If PP-D25 resolves toward ossification, the correction lapses and "can never be set" becomes literally true again. The durable half of the filing survives either way and is what PP-D13's ordering encodes: switching the skim on without a reachable exit makes the stranding real.

**E.6's naive fix is UNSAFE and PP-D33 gates it.** Returning without writing the cursor pins it, and `_phaseAwareBody:332` calls `IIncendiaryRegistry.integratedSkim`, which loops once per 14-day epoch spanned with no cursor and no `try/catch`, so a pinned cursor makes the eventual catch-up unboundedly expensive for whichever user triggers it. The cursor change therefore lands only after `integratedSkim` is O(1) via a prefix sum.

**Structural: the `hook` redeploy unit is now fully adjudicated.** E.2, E.3 and E.4 join B.6, C.9 and D.2, so all six `hook` rows exit Fix — the second unit after `ccb` with no member left unlocked. `emission-stack` moves to 12 of 16 locked, its remaining four being A.4, D.7 and E.9 deferred plus F.1 pending the next batch.

**Deferred with cause.**

- **E.9** — waits on **PP-D22**. Its capability, repointing the emissions recorder, is a protocol-wide claim and liquidity kill switch, and PP-D41 already forecloses any disposition of it that rests on the holder being trusted.

## PP3.3 — Family F verdicts (F.1 locked; F.2 and F.3 deferred)

**Locked.**

- **F.1 — Fix (Critical, unchanged).** The queue's only Critical root cause. `EmissionDistributor.setIncendiaryRegistry` at `:227-231` is a three-line body — read the old address, write the new one, emit — with no zero guard, no `code.length`, no one-shot and no timelock, the zero address being permitted BY DESIGN as the H-D29 deprecation valve. Its sibling `setAuMTContractForPool` at `:215-219` validates `newAuMTContract` against zero and against a prior binding while leaving `pool` ENTIRELY unvalidated, so any address can be seated as a pool. The boost leg then adds whatever the bound contract returns straight into `poolAccRewardPerLP` at `:406-411`, outside every gate the allocation leg carries. PP-D41 bars any disposition resting on the holder being trusted, and PP-D35 orders this fix before any dissolution decision is exercised.

**PP3.3's one mandated done-criteria rewrite is discharged here.** PP-D41 carries a standing obligation in its own text: F.1's done-criteria named only the clamp, `test_boostClampedToLpTrancheFraction`, and PP3.3 must invert it to a seating-control case with the clamp retained as a second test. The reason is PP-D41's general rule rather than a preference about this row — where a fix bounds blast radius instead of removing the capability, the bound is defence in depth and the removal is the primary remedy, so a done-criteria naming only the bound would let PP4 close a Critical row without ever building the control that removes it. The cell now reads `test_setIncendiaryRegistryRequiresCodeAndTwoStep` first and `::test_boostClampedToLpTrancheFraction` second. **This is the ONLY done-criteria cell PP3.3 edits.** B.1's rewrite was deferred to PP4 rung 12 at PP3.3b, because PP-D14 requires the replacement invariant to be stated there before a name can be chosen honestly.

**F.1's seating control is largely C.8's class fix applied to these two setters, and PP4 must not build them twice.** C.8's fix intent already reads `code.length` or an interface probe on EVERY seating, plus `pool` validated against the Vault — which is precisely what F.1's primary remedy needs at `setIncendiaryRegistry` and `setAuMTContractForPool`. C.8 is the same `emission-stack` redeploy unit and was locked Fix at PP3.3c. The two rows are one body of work at the seating layer, with F.1's clamp as the additional defence-in-depth piece that C.8 does not carry.

**Deferred with cause, and the census is unchanged at 40 unblocked and 13 deferred.**

- **F.2** — waits on **PP-D36**. Its verdict would be Fix under either PP-D36 outcome, since the router exposes `mintFor` as its only function with no pause, rotation, rate limit or per-consumer budget regardless of what the schedule does; only the ceiling's derivation depends on the corrected schedule, and the row's own constraint says F.3 first. Locking it now would buy one census row, would not unblock PP4 because the ordering still holds, and would reverse the census correction ratified at PP3.3b for no gain.
- **F.3** — waits on **PP-D36**, and here the decision is the row's whole content. Under the current PP-D36 text both options are code changes, `BLOCKS_PER_ERA = 10_500_000` or a raised `MAX_SUPPLY`, so Fix is the likely verdict either way; an Accepted-risk outcome would require amending PP-D36 to admit documenting the overshoot rather than closing it.

**One analogy is recorded as imperfect so a later reader does not lean on it.** The C.1 and D.7 deferrals rest on PP-D23 deciding whether a fix is a CODE change or a SPEC change, which is why those rows carry an empty `redeploy unit` cell pending the decision. PP-D36 is not that shape: both of its options are code changes and F.2 and F.3 already carry a committed `token-schedule` unit. Their deferral is sound on ordering and on census stability, not on the code-versus-spec ground that governs C.1 and D.7.

## PP3.3 — Family H verdicts (four locked; H.1 deferred) and PP3.3 CLOSE

**Locked.** All four exit **Fix**, all are Info, and all carry the `source-only` redeploy unit. Every face was verified at HEAD during PP3.2 and the H.3 faces were re-verified again at this rung, because H.3's own done-criteria targets `CLAUDE.md` §11, which this stage has edited repeatedly since.

- **H.2 — Fix (Info, unchanged).** Four interface headers state the wrong principal or mechanism for live gates. Two of them, `IBodenseeBootstrapChannel.sol:12` and `EmissionDistributor.sol:20`, BOTH claim to be the sole `IAuMM.mint` entry point, for `distribute()` and `claim()` respectively, so the two claims are mutually exclusive and F.2's PoC has already measured two allowlisted consumers minting different amounts through `mintFor`. One row imprecision is corrected in place: `:233` reads "Gated to the Aureum Incendiary contract", not "governance contract" as the row implies; the substance, a contract principal documented where the live gate is an EOA-held slot, holds for both.
- **H.3 — Fix (Info, unchanged).** `CLAUDE.md:26` still calls AuMT a per-pool LP receipt while `README.md:64` states positively that it IS the Vault's Balancer Pool Token with no separate `AuMT.sol`, and the audit brief designates `CLAUDE.md` as ground truth. `11477620` still occurs four times against `11597476`'s three, and §3's tree still omits `deployments/`, `test-stubs/` and `tools/`, all three present on disk. Re-measured at this rung: this stage's own §11 edits neither fixed nor worsened any of the three.
- **H.4 — Fix (Info, unchanged).** `prove_emergency_scopeLimited` is false by inspection with two named counterexamples, `EMERGENCY_ACTION_UNPAUSE_VAULT` and `EMERGENCY_ACTION_DISABLE_RECOVERY_MODE`, each satisfying both its `vm.assume`s since PB3.6a widened `_isEmergencyAction` to four ids.
- **H.5 — Fix (Info, unchanged).** `new AureumWeightedPoolFactory` appears in four test files and in NO `src/` or `script/` path, so the 52% factory-level gate has no on-chain instance and both production spines cast an upstream body to the Aureum type. The row's `[corrected]` clause holds in full: the substitution IS documented in-tree at `DeployStageG.s.sol:63-70` per PB-D27 (ix).

**H.5's own fix intent is QUALIFIED by a PP3.2 result and must not be implemented as written.** That intent ends "never cast an address to a contract it is not — assert `version()` first". Asserting `version()` would NOT catch this substitution: `DeployAureumWeightedPoolFactory.s.sol:39` constructs the upstream `WeightedPoolFactory` with a `FACTORY_VERSION` string naming `AureumWeightedPoolFactory`, and `WeightedPoolFactory` passes that string straight to `Version(factoryVersion)` at its constructor, so the deployed upstream body REPORTS the Aureum name. A provenance check must probe behaviour or creation-code identity, not the self-reported version string.

**H.4's remedy has two parts and a gate, and the second part is the one that matters.** PP-D37 bars re-running the formal bundle until after every `src/` change in Workstream B, so the re-run and re-anchor are PP4's last step, not this beat's. The deeper defect is structural rather than the assertion itself: the four `test/formal/` files carry 55 `prove_` functions and ZERO `test` functions, so forge discovers none of them and no Part-A count can ever see this failure — only `hevm test --match prove_` can, and it last ran at `9c109b1` on 2026-07-17, three weeks before the widening landed at `5bef424`. Fixing only the assertion would leave that blind spot intact, which is what the fix intent's `.t.sol`-mirror-or-CI clause exists to close. A SECOND staleness must be settled at the same re-anchor: `formal/RESULTS.md` says the suite stands at 52/52 in its headline Status block while its own Result block reports 55/55 re-attested at that same commit.

**Deferred with cause.**

- **H.1** — waits on **PP-D23**, per-divergence. Its fee face is the largest single edit surface any deferred row carries: `0.03%` occurs 17 times across 11 files in `aumm-site`, 8 in the five canonical docs and 9 more in a GENERATED `dist/aumm-skill/` mirror, while `0.02%` occurs ZERO times anywhere, so PP5.4's work is a source pass plus a regeneration rather than one wording fix.

**PP3.3 IS COMPLETE. Forty of forty unblocked rows carry a verdict: 39 Fix and 1 Accepted-risk, with ZERO False-positive across the entire stage** — consistent with PP3.2's zero-refuted reproduction column, since nothing that reproduced turned out not to be a defect. The single Accepted-risk is B.8, and it clears PP-D41 on capability rather than conduct with a MEASURED quantification behind it. The thirteen deferred rows group by the decision that gates them: **PP-D17** holds A.1 and B.3; **PP-D22** holds A.4, C.2 and E.9; **PP-D23** holds D.7 and H.1; **C.1 is held by both PP-D22 and PP-D23**, the only doubly-gated row; **PP-D25** holds C.4; **PP-D36** holds F.2 and F.3; and the composite rule holds **B.4** and **G.4** until every constituent locks. Five open decisions therefore stand between here and a complete verdict column.

**What PP3.4 inherits, so it does not re-derive any of it.** The row-versus-face granularity rule settled at PP3.3c: at queue-row granularity the plan's three expected Accepted-risks collapse to one, and PP3.4 may still split an `F-NN` ledger row per face where a face warrants it, the F-13 precedent. Two ledger obligations from PP3.3b: **F-15's row needs re-statusing** because B.7 reopened PB-D10's disposition, and a **PB-D10 reopening amendment** in `docs/STAGE_P_BIS_NOTES.md` is a separate cross-stage beat. One register obligation for PP3.5: the recurring **found-once-never-propagated** class, now visible in four rows rather than three — A.3's strict-rise form, B.7's F-21 zero guard, D.2's monopolised-sampling refutation of an in-source acceptance, and H.5's version string — which PP-D11 already names via G10 but which has no RB row.

## PP4.0 — Fix conformance and protected invariants (PP-D12, part 1)

**Every fix in this workstream conforms to the canon, and a fix that makes a finding disappear by weakening a protocol guarantee is not a fix.** The canon is `aummfi-bit/aumm-site`, read from the local clone per `CLAUDE.md` §4; where code and canon disagree the canon is the authority, unless PP-D23 has adjudicated that particular divergence the other way, which is the H family's business and no other rung's. FOUR anti-shapes are named here because each is the natural relief a fix author reaches for when the correct fix is expensive, and each trades a finding for a lesser protocol: adding a setter to an `immutable` slot so that a redeploy can be avoided; converting a structurally fixed destination or authority into a governance-mutable one; lowering a threshold, floor or window so that a boundary case stops failing; and widening an authority so that a caller which legitimately cannot reach something now can. A remediation stage is exactly where this pressure peaks — 51 root causes, several of them expensive — and the audit's whole value evaporates if what emerges is a weaker protocol that merely passes a re-run. Where the correct fix genuinely costs a redeploy, THE REDEPLOY IS THE FIX; the closure table in part 2 prices every one of them so the cost is known in advance rather than discovered mid-rung.

**PROTECTED INVARIANT — the protocol fee destination is der Bodensee, structurally, and no rung in this workstream touches it.** Verified at HEAD rather than assumed. `DER_BODENSEE` is `immutable` at `AureumFeeRoutingHook.sol:65` and written once at `:259`; every other occurrence in that file is a read or a guard. `DER_BODENSEE_POOL` is `immutable` at `AureumProtocolFeeController.sol:207`, written once at `:251`. NO setter exists for either anywhere in `src/` — the only `set*` functions on that path are the two pool-creator fee percentages, which the protocol pins at zero forever. The hook and the controller are additionally MUTUALLY PINNED, `FEE_CONTROLLER` `immutable` on the hook and `FEE_ROUTING_HOOK` `immutable` on the controller, so neither can be substituted for the other: that pair is an irreducible cycle with no setter to break it, resolved at deploy time by address prediction, which is what `script/DeployFeeRoutingHook.s.sol` reads as `predictedHook` and what PB-D27's phase-A projections computed. The one Vault-layer redirect, `Vault.setProtocolFeeController`, is unreachable by design per PB-D63 (xi) and `AureumVaultFactory.sol` already documents exactly that in its own NatSpec, so F-22's accepted residual is load-bearing here rather than merely unclosed. The dependency graph states the same thing quantitatively: redeploying der Bodensee drags 18 of the 24 contracts in `src/`. THE CYCLE IS THE GUARANTEE, NOT A DEPLOYMENT WART, and it must not be read as an inefficiency the map invites relief from — six queue rows carry `hook` as their redeploy unit, and a fix author pricing that redeploy will reach for a setter on one of these slots, which would convert the fee destination from structural to governance-mutable in a single line. No rung does that. PB-D68 (xi) already paid this price knowingly, refusing to swap a corrected hook under live pools and refreshing an entire Sepolia generation instead.

## PP4.0 — Redeploy closure (PP-D12, part 2)

**The closure below was DERIVED mechanically from `src/` at HEAD, not transcribed, and the method matters because a hand-read map would have been wrong.** Every `immutable` declaration in `src/` was parsed and its target resolved by type, by variable name, or by an explicit alias; entries that resolved to nothing were PRINTED rather than dropped, which is the only reason three real edges were caught — `AureumVaultFactory.INITIAL_FEE_CONTROLLER` is typed `IProtocolFeeController`, which strips to a name no contract carries, `AureumGovernance.SLOT_REGISTRY` is typed `IMiliariumSlotRegistry` for the same reason, and der Bodensee appears under two names a first alias set missed. The final graph is 24 contracts and 46 edges. An edge means the holder pins the target's ADDRESS at construction, so redeploying the target forces redeploying the holder, transitively. Read the table as a price list: the `drags` column is what a rung in that unit must rebuild, and `governance` costing one contract while `token-schedule` costs thirteen is why fix ORDER is not the same question as fix count.

| redeploy unit | root | queue rows | drags | the closure |
| --- | --- | --- | --- | --- |
| `governance` | `AureumGovernance` | 14 | 1 | `AureumGovernanceAuthorizer` |
| `emission-stack` | `EmissionDistributor` | 16 | 4 | `AuMMMinterRouter`, `AureumGovernance`, `AureumGovernanceAuthorizer`, `VotingWeight` |
| `ccb` | `CCBMultiplier` | 5 | 5 | the `emission-stack` set plus `EmissionDistributor` |
| `tvl-oracle` | `TVLOracle` | 1 | 11 | the `ccb` set plus `CCBMultiplier`, `EMASampler`, `EfficiencyOracle`, `GaugeEligibility`, `GaugeRegistry`, `IncendiaryRegistry` |
| `hook` | `AureumFeeRoutingHook` | 6 | 11 | `AuMMMinterRouter`, `AureumFeeRoutingHook` ITSELF, `AureumGovernance`, `AureumGovernanceAuthorizer`, `AureumProtocolFeeController`, `AureumVaultFactory`, `EmissionDistributor`, `GaugeEligibility`, `GaugeRegistry`, `IncendiaryRegistry`, `VotingWeight` |
| `token-schedule` | `AuMM` | 2 | 13 | the `hook` set minus its self-edge, plus `BodenseeBootstrapChannel`, `EfficiencyOracle` and `AuMMMinterRouter` |
| `source-only` | none | 4 | 0 | no contract is deployed; the change is to scripts, tests or docs |
| PROTECTED | der Bodensee pool | n/a | 18 | every contract in `src/` except `AuMM`, `AureumAuthorizer`, `AureumWeightedPoolFactory`, `CompositeRateProvider`, `ERC4626RateProvider` and `MiliariumRegistry` |

## PP4.0 — One-shot setter taxonomy and burn state (PP-D12, part 3)

**The fifteen one-shot setters G12 enumerates are NOT one class, and the difference decides whether a fix may touch them.** Classified at HEAD by asking, of each setter, whether its TARGET holds the HOLDER as an `immutable`. SIX are CYCLE-BREAKERS, where the answer is yes and the pair could not be deployed at all unless one side were seated after construction: `AuMM.setMinter` against `AuMMMinterRouter.AUMM`; `EmissionDistributor.setMintRouter` and `BodenseeBootstrapChannel.setMintRouter` against that same router's `EMISSION_DISTRIBUTOR` and `BOOTSTRAP_CHANNEL`, the router being a three-spoke hub whose three setters exist solely to close it; `GaugeEligibility.setGaugeRegistry` against `GaugeRegistry.gaugeEligibility`; and both `SwapAndDepositToBodensee` setters against `VaultClassRegistry.helper` and `GaugeRegistry.swapAndDeposit`. SEVEN are ORDERING-BREAKERS, where the target holds no reciprocal immutable and the setter exists only because the target is constructed later: the hook's `setIncendiaryModule` and `setEmissionRecorder`, both `CCBMultiplier` setters, `TVLOracle.setMiliariumRegistry` — `MiliariumRegistry` carries ZERO immutables, which is what settles that one and its `CCBMultiplier` sibling — and both `VaultClassRegistry` setters. TWO are NEITHER: `setGovernanceModule` seats a Safe rather than a contract, and `setAuMTContractForPool` is a per-pool binding outside the deploy graph entirely. The consequence for PP4 is asymmetric and must not be flattened: a cycle-breaker CANNOT be removed, since removing it makes its pair undeployable, while an ordering-breaker could in principle be replaced by reordering the deploy, so a fix proposing that is arguable rather than barred. Note also that the two `SwapAndDepositToBodensee` setters carry the codebase's ONLY conditional burn, each releasing `moduleAdmin` only once the other has run, so half-sealed is a reachable state and nothing on chain distinguishes it from sealed — which is C.8's Info instance, not a separate finding.

**The closure table counts `immutable` edges ONLY, and a SPENT one-shot setter is an immutable edge on the deployment that spent it.** This is the one place the table must be read twice. Burn state is per-DEPLOYMENT, not per-source: at Stage R nothing is burned yet and the whole set deploys in dependency order, so the immutable closure is the entire price. On a live generation it is not. `AuMM.setMinter` is already spent on Sepolia generation 3, so redeploying `AuMMMinterRouter` there cannot re-point `AuMM.minter` at the new router and therefore forces `AuMM` itself, and `AuMM` drags thirteen. A unit the table prices at four becomes the most expensive in the protocol, and the same inversion applies to every cycle-breaker whose burn has fired. This is not a defect in the map; it is why PP-D21 retires generation 3 rather than patching it in place, and why the generation-4 refresh is scheduled after the PP6 exit gate rather than interleaved with PP4. A rung that touches a live generation prices its change against the burn state recorded in `docs/STAGE_P_BIS_SEPOLIA_DEPLOYMENT_RECORD.md`; a rung that only changes source for Stage R prices it against the table alone.

## PP4.0 — The per-rung test gate (PP-D12, part 4)

**The per-rung test gate, written once here so that no fix rung restates it.** THREE invocations, split by PATH per D35, because a fork test swept into the unit run fails there and a unit run that reaches the fork tree hangs on the rate limit. PART-A UNIT is `forge test` with `--no-match-path` over the fork tree; its baseline as of PP4.0 is 1060 passed, 0 failed and 1 SKIPPED across 90 suites, and RB-024 requires QUOTING BOTH NUMBERS rather than rounding to green. That skip is `test/unit/DeploymentArtifactAgreement.t.sol:44`, deferred by user adjudication to the generation-4 artifact rebuild, so it is expected to stay skipped through all of PP4 and a rung reporting a clean sweep with no skip has measured something other than this suite. FORK is FILE-SCOPED, one of the 33 files under `test/fork/` per invocation, with `--match-path` naming that file plus `--fork-url mainnet --threads 1 -vv`; never unfiltered, which hangs per D35, and never whole-tree across the fork directory, which fails on D36's sequential face even at one thread. SIZES is `test/unit/BytecodeSize.t.sol` at 4 tests, run by any rung whose contract grows, and PB-D37 forces its named-artifact form because neither compiler profile passes an unscoped size check.

**Before any fork invocation the operator environment is checked BY HAND, and as of PP4.0d RB-023 is REOPENED, so that check must not be assumed to have happened.** What exists: `CLAUDE.md` §11 carries the literal sentinel and states that it runs in the SAME shell that will invoke forge, and `test/fork/ForkEnvGuard.sol` enforces the same thing structurally at `setUp` — but in only SEVEN of the 33 fork files, so 26 of them fail at their first dereference rather than at their guard. What does NOT exist is the runbook half: its marker check still greps the file on disk rather than the resolved value, which passes green on a dirty shell. Treat the sentinel as a required manual step of every fork invocation until RB-023 closes for real. The two-pass `.env` discipline per PB-D59 touches exactly ONE file, `test/fork/PB52SepoliaOracleEvidence.t.sol`, the tree's only Sepolia-fork test: copy the canonical sepolia file onto the working `.env` for it and RETURN the working copy to mainnet afterwards, always canonical to working copy and never the reverse, or every mainnet fixture silently resolves its literals to codeless Sepolia addresses, which is PB-D35 recurring. FIVE obligations per rung, beyond the three invocations: the PP3.2 PoC inverts to a regression in the SAME rung; the ledger row moves to `Fixed` with the commit hash, the queue's `F-NN` cell already naming which row since PP3.5f; the rung's NOTES entry records what the fix DECLINED to do and why; where the rung's redeploy unit drags more than itself, the closure in part 2 is cited in the prompt's `Must match` so the blast radius is stated before the edit rather than discovered after it; and per PP-D43 the rung NAMES the canonical clause its fix conforms to, by file and section, in both the `Must match` and the NOTES entry, STOPPING for adjudication if it cannot name one.

## PP-D43 — Canon conformance is binding (the protected set and the escape path)

**PP-D43 (user-adjudicated 2026-08-26): canon conformance is BINDING, and the protected set is §xxix of `10_constitution.md` adopted WHOLESALE rather than a rival list of our own devising.** That section declares itself the single canonical source for all immutable protocol parameters and states its standard without qualification: immutable from block 0, cannot be changed by any means. It covers the supply cap of 21,000,000, the emission halving schedule and the block emission rates, the canonical time constants including `BLOCKS_PER_ERA` at 10,512,000 whose alias is exactly four times `BLOCKS_PER_YEAR`, the derived time boundaries, and the anti-gaming safeguards it classes beside them. FIVE further invariants are protected outside §xxix and are named here so that no rung has to hunt for them: the fee destination, per PP4.0 part 1; no treasury, at `04_tokenomics.md:163`; no wei of AuMM, svZCHF or sUSDS leaving der Bodensee, at `04_tokenomics.md:213`; AuMM barred from emission-eligible pools, at `10_constitution.md:168`; and the Vault's byte-identity with audited Balancer V3, per `CLAUDE.md` §1. NO rung in this workstream changes any of them. A fix that requires one of them to move is not a fix, it is a constitutional amendment, and it STOPS.

**FIFTH per-rung obligation, extending the part-4 gate, and the single escape path.** Every rung NAMES the canonical clause its fix conforms to, by file and section, whether that is `10_constitution.md` §xxix, a clause of `04_tokenomics.md`, or a formula id from `11_formulas.md`, and it names it twice: in the prompt's `Must match` and again in the rung's NOTES entry. A rung that CANNOT name one STOPS and goes to adjudication rather than proceeding on the fix author's judgement, because the inability to cite is the signal itself and not an inconvenience to be worked around. Canon changes by exactly ONE route, a PP5.4 spec edit, user-adjudicated, landing upstream in `aummfi-bit/aumm-site` before any code depends on it: never inside a fix rung, never as a code accommodation, and never by a rung concluding that the canonical value is the thing that is wrong. Where code and canon disagree the CODE yields, unless PP-D23 has adjudicated that particular divergence the other way, which is the H family's business and no other rung's.

## PP-D44 — F.1 (PP4.1, rung 1): the boost clamp, both setter controls, and the F.1/C.8 seam

**PP-D44 (user-adjudicated 2026-08-26): F.1 is the queue's only Critical and rung 1 of the fix-sequencing order per PP-D35, and it lands as three changes to `src/emission/EmissionDistributor.sol` — a consumer-side clamp on the boost leg, and a hardening of BOTH seating setters.** The defect is two-sided. `_settlePool`'s boost leg at `:405-413` reads `boostIntegral` from whatever contract occupies the `incendiaryRegistry` slot and adds the result straight into `poolAccRewardPerLP` at `:410`, outside every gate the allocation leg above it carries; and `setIncendiaryRegistry` at `:227-231` is a three-line body that seats any address into that slot with no `code.length`, no one-shot and no timelock. **Canon citation per PP-D43:** `10_constitution.md` §xxviii at L97, which places Incendiary claims as a skim from the LP emission tranche before the CCB splits the remainder and binds them to the shared 15%-per-epoch cap, and §xxix at L135, the `BOOST_CAP_BPS` row reading 1,500 bps = 15% of an epoch's emission integral. The clamp INVENTS no bound: it makes the distributor enforce an immutable it presently delegates to the very contract the clamp exists to bound.

**Scope: F.1 owns the seating layer on BOTH setters, and that is F.1's own language rather than a courtesy pull-forward from C.8.** The queue's F.1 fix-intent cell already reads `pool` validated against the Vault alongside the clamp, the `code.length` and the two-step, so `setAuMTContractForPool` sits inside F.1's stated remedy rather than being borrowed into it; PP3.3g records the two rows as ONE body of work at the seating layer, and F-67 warns in terms that PP4 must not build them twice. C.8 RETAINS every non-seating face it carries — the `try/catch` on the hook's recorder dispatch so a recorder fault degrades accounting instead of locking LPs, the remaining seatings, the post-condition read-backs after every wiring call, PP-D28's pre-send `eth_call` runbook gate, `renounceOwnership()` on the factory, and the two authorizer `require`s — and its rung is unchanged in every respect but this one. A later reader must not conclude from this lock that C.8 shrank to nothing.

**The clamp.** `boostDue` is bounded by `cap = (BOOST_CAP_BPS * _lpTrancheIntegral(capFrom, block.number)) / 10_000` and clamped DOWN to it, where `boostFrom = poolBoostCursor[pool] + 1` reaches `boostIntegral` UNFLOORED and `capFrom` is that same value floored at `GENESIS_BLOCK + 1`, computed only when a boost is due. The floor is required rather than defensive, and PP4.1c3 CORRECTED the mechanism first named here: `_lpTrancheIntegral` is documented for `from >= GENESIS_BLOCK + 1`, and an unbound pool whose cursor still reads zero would otherwise walk from block 1 into `_bootstrapApSum`'s `from - anchorStart` subtraction, which underflows for any `from < GENESIS_BLOCK`. `AureumTime.eraIndex` is NOT that site — it guards pre-genesis and returns era 0 — so a reader checking it alone would conclude the floor was unnecessary; the conclusion held while the mechanism recorded beside it did not, which is PB22's shape. AMENDED at PP4.1c6, and this is the second falsified mechanism in one rung. The paragraph originally floored a SHARED `from` feeding BOTH calls, and claimed that raising the bound could not change honest delivery. That was verified for the RETURN VALUE — `boostIntegral` resolves both 1 and `GENESIS_BLOCK + 1` to epoch 0 through the same pre-genesis guard in `AureumTime.epochIndex`, and epoch 0 can carry no bucket because `buyBoost` reverts until year 1 ends, around epoch 26 — and then asserted as BEHAVIOUR, which is false. The CALLDATA changed, and anything keyed on `from` changed with it: four `BoostDelivery` tests reverted because their `vm.mockCall` is keyed on `(pool, 1, N)` while the call began arriving as `(pool, GENESIS_BLOCK + 1, N)`, hitting a codeless address that returns no data. The floor now applies to `capFrom` alone, `boostIntegral` never needed it, and the bound is unchanged. PB22 twice over in the same rung: right conclusion, wrong mechanism, over-broad claim. Pre-genesis emission is zero, so the floor changes no honest value. The literal is DUPLICATED on the distributor as its own `internal constant BOOST_CAP_BPS = 1_500` and is deliberately NOT read from `IncendiaryRegistry.sol:50`, because reading the cap from the contract being bounded reintroduces exactly the trust the clamp removes; `:459` carries the same plain-integer BPS arithmetic per L-D4 / L-D20, so the two sides agree by construction rather than by call. Excess is DISCARDED and never banked — `poolBoostCursor[pool]` still advances unconditionally to `block.number` at `:413` — and that is also what bounds E.8's backlog face, since a rebind after an unbound window is then measured against the tranche integral over that same window rather than billed as one lump.

**The two-step, which is a NEW shape and not a mirror.** `setIncendiaryRegistry` splits into `proposeIncendiaryRegistry(newRegistry)` and `acceptIncendiaryRegistry()`, both `onlyGovernance`: propose stores the pending address together with `block.number`, accept requires a strictly later block and commits, emitting the existing `IncendiaryRegistrySet(old, new)` so the event surface is unchanged. `code.length > 0` is required on a NON-ZERO proposal only, which preserves the H-D29 zero deprecation valve; the zero write routes through the same two steps with NO fast path — a single-step zero is itself a lever that strands boost delivery protocol-wide, and PP-D41 bars a disposition resting on the holder declining to pull it. There is NO propose/accept rebind precedent anywhere in `src/`, so this shape is locked here rather than mirrored from something: `VaultClassRegistry`'s propose/veto/finalize is a different animal — a permissionless admission carrying a veto window rather than a two-party rebind — and is NOT the model. The gap is ONE BLOCK and no more, because a longer delay would introduce a constant §xxix has no row for while one block already buys the entire property: the seat-and-drain sequence cannot be atomic, so the PoC's four calls become two transactions in two blocks with the pending registry publicly readable in between.

**Pool validation, and the one structural cost this rung pays.** `setAuMTContractForPool` at `:215-219` validates `newAuMTContract` against zero and against a prior binding while leaving `pool` ENTIRELY unvalidated, which is precisely what lets the F.1 PoC seat the attacker as recorder for a fabricated pool. It gains `IVault(vault).isPoolRegistered(pool)` reverting `PoolNotRegistered(pool)`, plus `code.length > 0` on the recorder per C.8's every-seating rule. The shape mirrors `GaugeEligibility.sol:39` literally — an `address public immutable vault` read as `IVault(vault)` at the call site — and `isPoolRegistered` is real Vault API rather than an invented membership probe: it is declared at `IVaultExtension.sol:127` and reaches `IVault` through the inheritance list at `IVault.sol:13`, VERIFIED at HEAD rather than assumed, per G10. The cost is MEASURED and not estimated. The constructor goes from eight arguments to nine with a `ZeroAddress` guard on the new one, and the ripple is NINETEEN call sites across ELEVEN files — `test/unit/EmissionDistributor.t.sol` alone constructs it nine times, and `test/formal/EmissionProofs.sol` must be updated to COMPILE even though forge runs none of its `prove_` functions. Every site that also calls `setAuMTContractForPool` needs a Vault answering `isPoolRegistered` for its pool, which is a harness addition rather than merely an extra argument, and that is the real weight of this rung. It is a MAP AMENDMENT and not a new unit: it adds an EmissionDistributor-to-Vault edge to the PP4.0 part-2 closure, the Vault sits in PP-D12's SURVIVING set, so the `emission-stack` price of four is unchanged and no cascade widens. Recorded here so the closure is not read as stale at PP4.52.

**The done-criteria gap this rung closes, and why it closes by ADDITION rather than by rewrite.** F.1's binding done-criteria names exactly two cases, `test/whitehat/P1_F1.t.sol::test_setIncendiaryRegistryRequiresCodeAndTwoStep` and `::test_boostClampedToLpTrancheFraction`, the first primary and the second retained as defence in depth — that ordering being PP3.3g's inversion discharging PP-D41's F.1 obligation. NEITHER names the pool-validation face, which this rung builds, and PP-D40 (vi) fixes done-criteria as `path::case` precisely so PP4.52 reads a green run rather than a judgment: a face built with no artifact pointing at it is a face PP4.52 cannot see. F.1's done-criteria therefore gains a THIRD case, `::test_setAuMTContractForPoolRequiresRegisteredPool`, APPENDED to the two existing names rather than replacing either, which makes this the second done-criteria edit of the stage after PP3.3g's inversion and the only one that rewrites nothing. The alternative was considered and REJECTED: leaving the face unnamed and letting C.8's own PoC invert finish the claim would make `Fixed` for C.8's `pool` face rest on a change that landed in a different rung with no artifact naming it, which is the stale-attestation shape PP-D37 keeps the formal bundle out of and PP3.2 found live in H.4.

**What this rung DECLINES, each with its reason, per the part-4 gate's third obligation.** The C.8 REBIND face, so `setAuMTContractForPool` stays one-shot per pool: rebinding is a topology decision rather than a validation one, I-D9 having removed it deliberately with its NatSpec at `:211` recording that an asymmetric rebind between the hook's `auMTByPool` and the distributor's `auMTContractByPool` would break per-pool topology — validating `pool` and making the binding reversible are separable, and only the first is F.1's. A LONGER TIMELOCK on the rebind, for the non-canonical-constant reason above. A FAST ZERO-VALVE on the deprecation path, for the lever reason above. READING `BOOST_CAP_BPS` from the registry instead of duplicating the literal, which would reintroduce trust in the contract being bounded. ROTATING `EmissionDistributor.governance` to `AureumGovernance`, which F-67 rejects in terms because it strands three setters while the consumer clamp holds under EITHER dissolution outcome — and that is exactly why PP-D35 orders this rung BEFORE PP5.1 decides the dissolution question rather than after, so the choice is made on its merits and not as a Critical's remedy. And every remaining C.8 face enumerated above, which is C.8's rung and not this one.

**AMENDMENT at the PP4.1c pre-flight, 2026-08-26 — the cost paragraph above priced the CONSTRUCTOR ripple only, and the setter split carries a second one.** The nineteen call sites across eleven files are the `new EmissionDistributor(` sites and stay correct as far as they reach, but `setIncendiaryRegistry` becoming two-step moves every CALLER of it as well: TWENTY-SIX call sites across SIX files, measured at HEAD — fourteen in `test/unit/EmissionDistributor.t.sol`, five in `test/formal/EmissionProofs.sol`, four in `test/whitehat/P1_E8.t.sol`, and one each in `script/DeployStageL.s.sol`, `test/fork/StageLIntegration.t.sol` and `test/whitehat/P1_F1.t.sol`, the `DeployStageL` site being the PRODUCTION path rather than a fixture. Neither set overlaps the other, since a constructor call site is not a setter call site, so PP4.1c prices at FORTY-FIVE call sites across FOURTEEN distinct files rather than the nineteen recorded above. The two ripples land in one rung by adjudication at this pre-flight rather than by oversight; the alternatives, dropping the two-step or deferring it behind the constructor ripple, were both put and both declined. TWO formal proofs additionally go SEMANTICALLY false rather than merely failing to compile, and both are rewritten in the SAME rung so that neither sits false through PP-D37's no-rerun window: `prove_distributor_setIncendiaryRegistry_gateAndClear` at `test/formal/EmissionProofs.sol:423` and `prove_distributor_governanceRotation` at `:400` each call the setter once and assert the slot committed, which a two-step makes untrue. Leaving them would reproduce H.4 exactly — a proof false by inspection surviving because forge runs none of the bundle's `prove_` functions and no Part-A count can ever see it — which is among the shapes this stage exists to close.

**SECOND AMENDMENT at PP4.1c4, 2026-08-26 — the first amendment was itself measured too narrowly, by a factor of about 2.3, and the reason is instructive.** Both figures above came from grepping `new EmissionDistributor(` and the callers of `setIncendiaryRegistry`, and NEITHER pattern can see what actually breaks. `EmissionDistributorHarness` at `test/unit/harness/EmissionDistributorHarness.sol` SUBCLASSES the distributor and forwards eight arguments through a base-constructor invocation, which solc reports as error 2973 rather than 6160; that line is invisible to a `new` grep, and repairing it changes the harness's OWN signature, dragging its SIXTEEN construction sites with it. The constructor ripple is therefore THIRTY-FIVE call sites across TWENTY-SIX files plus the harness declaration, not nineteen across eleven. Separately and unpriced anywhere above, gating `setAuMTContractForPool` on `isPoolRegistered` breaks every caller that passes a placeholder vault: FORTY-ONE call sites across TWENTY-TWO files, and NO unit-level Vault mock exists in `test/` to answer that call, exactly one fork file mentioning the function at all, so one must be written before any of them can pass. PP4.1c prices at roughly ONE HUNDRED AND FOUR edit points across about thirty files. The user re-adjudicated at PP4.1c4 against the corrected figure and elected to PROCEED AS LOCKED, across six sub-rungs c5 through c10, two of which are deliberate red windows each closed by the rung immediately following. The lesson is PB18's one level up: a count is a prediction until the thing counted is the thing that breaks, and `new X(` counts CONSTRUCTIONS while what breaks is CONSTRUCTOR ARITY, which inheritance propagates and no call-site grep can follow.

**THIRD AMENDMENT at PP4.1c11, 2026-08-27 — the two guards on `setAuMTContractForPool` are SPLIT, and only the pool check lands inside F.1.** The pool-validation paragraph above names both `IVault(vault).isPoolRegistered(pool)` and `code.length > 0` on the recorder. They have different owners and wildly different costs, measured at HEAD rather than assumed. The POOL check is F.1's own remedy and the face its PoC exploits, and it ripples to NOTHING: `MockRegisteredVault` answers true for every pool by default, which is precisely why it was built that way at PP4.1c5a, so all FORTY-ONE `setAuMTContractForPool` call sites keep passing untouched. The RECORDER check is C.8's class fix, its row reading `code.length` or an interface probe on EVERY seating, and it would break roughly TWENTY-FIVE to THIRTY of those forty-one: `AUMT_REC` is `address(0xA0DC)`, `AUMT_REVOKED` is `address(0xAB01)`, and every short local is a `makeAddr`, none of which carries code, so each site would need a deployed stub or a `vm.etch`. It therefore rides with C.8, where that fixture work is paid ONCE across all fifteen seatings instead of twice. **The second reason is sharper than the cost.** F.1's inverted regression binds `ATTACKER`, a plain address, as the recorder in step 1, and depends on that call SUCCEEDING so the sequence reaches the clamp it exists to pin. A recorder `code.length` guard would make step 1 revert, the exploit would stop at seating, and the clamp regression would silently lose its subject while still reading green — a test that passes for a reason it does not name is the shape PP4.1c8 split into two phases to avoid.

**CLOSING RECORD at PP4.1c25, 2026-08-27 — F.1 is BUILT and F-67 reads Fixed at `daa293e`.** All three remedies are green: the clamp (`7d79cd2`, floor narrowed at `380940f`), the `isPoolRegistered` gate (`f4d2442`), and the two-step (`f9b43c5`). **Canon per PP-D43:** `10_constitution.md` §xxviii L97 and §xxix L135 — the two-step introduces NO constant, the gap being the bare strict block inequality, precisely because §xxix carries no delay row to cite. Gates: Part-A 1063 passed, 0 failed, 1 skipped of 1064 across 90 suites, the skip being RB-024's deferral; `StageLIntegration` 2/2 and `DeployStageL` 3/3 file-scoped on a mainnet fork with the RB-023 sentinel reading 0 in the same shell. The ripple measured TWENTY-SEVEN real call sites across seven files, not the twenty-six the first amendment predicted — `test/whitehat/P1_F1.t.sol` carried two, not one, and `test/formal/EmissionProofs.sol` carried five where a `.setIncendiaryRegistry(` grep saw three, the two invisible ones being `abi.encodeCall` function-POINTER references, which take a comma rather than a paren. That is the first amendment's own lesson recurring inside the rung that was written to apply it.

**What this rung DECLINED, beyond the lock's four.** A local binding helper in `test/whitehat/P1_E8.t.sol`: four sites in three shapes, two needing bespoke absolute block targets, so each stays explicit. Modelling the two-step as Act transitions: both halves read the environment and propose queries `code.length`, so they route to a residual per `formal/act/ema_sampler.act` L41—L60's convention for `updateEMA`, and Act is source-only upstream and NOT installed, so any invented construct would be unfalsifiable here. Renaming F.1's done-criteria case despite its name carrying the retired symbol: PP-D40 (vi) binds case names at creation. And re-running the formal bundle, barred by PP-D37 — which is why `formal/RESULTS.md` gains `PENDING-REATTESTATION` to the PB-D17(iv) vocabulary and marks P-ED3 and P-ED4 with it rather than carrying `PROVED` forward from `9c109b1`, that being H.4's exact shape.

**THREE consequences the lock did not anticipate, each a fixture or artifact cost rather than a design change.** FIRST, the registry `code.length` gate collides with the unit suite exactly as the third amendment predicted for the RECORDER check — every registry bound in `test/unit/EmissionDistributor.t.sol` is a bare `address(0xBEEF)` with no code — but this gate is F.1's OWN remedy and cannot ride to C.8, so `_bindIncendiaryRegistry` etches a byte at non-zero targets and the whole file routes through it. SECOND, `forge script` executes an entry at a single `block.number`, so propose and accept CANNOT share one invocation: `script/DeployStageL.s.sol` splits into `run()` which proposes and a separate `runAcceptRegistry()` / `acceptRegistry(address)` pair, making the Stage-L bind a two-invocation operator procedure. This is the fourth instance of the simulation-versus-broadcast class after PB-D31, PB-D46 and PB-D48. THIRD, the constructor ripple at c5—c10 MISSED `formal/act/emission_distributor.act`, whose constructor still described the eight-parameter form after `0a95429` made it nine; a ripple measured over Solidity construction sites cannot see a spec that describes the same constructor in another language.

**And one measurement failure of my own, which is PP12 rather than PB18.** Every sweep written during this rung scoped to `src/ script/ test/`, so `formal/` sat OUTSIDE the searched universe and the c20i sweep reported the symbol gone when two live references remained — the `.act` transition and its `RESULTS.md` coverage row. It was caught only because the c23a sweep happened to widen the directory list. The pattern was right and the tool ran; the file set was wrong, which is exactly PP12's shape and not PB18's. A sweep that certifies a symbol's removal names EVERY tracked directory, or reports which ones it searched.

## PP-D45 — PP4.2 (rung 2): the residual delta, A.3's strict rise, and the channel carve-out

**PP-D45 (user-adjudicated 2026-08-27): PP4.2 is PP-D11's one-shape rung — A.2, A.3 and E.1 — and it lands as FOUR edits across TWO contracts, not three, because A.2 carries two identical sites.** The defects are two shapes, not one, and PP-D11's phrase "one shape" governs only the first. THE RESIDUAL SHAPE, at `src/gauge/SwapAndDepositToBodensee.sol` in both `swapAndDeposit` and `donate`, and at `src/emission/BodenseeBootstrapChannel.sol` in `distribute()`: an ABSOLUTE `balanceOf(address(this)) != 0` check reverting `HelperBalanceNonZero`, which any pre-existing wei bricks permanently. Each becomes an ENTRY-SNAPSHOT DELTA — read the balance before the `_vault.unlock` and revert only if the post balance EXCEEDS it — so a residual that was already there is tolerated while a residual the call itself creates still reverts. **E.1's snapshot is taken BEFORE `mintRouter.mintFor`**, not after, because the mint transiently adds `amount` to the very balance under test; a post-mint snapshot would measure nothing. THE RISE SHAPE, at `SwapAndDepositToBodensee._swapAndDepositCallback` only: exact reserve equality `postReserve != preReserve + amount` becomes `postReserve <= preReserve` reverting a NEW `ReserveDidNotRise(uint256 preReserve, uint256 postReserve)`, byte-mirroring `src/fee_router/AureumFeeRoutingHook.sol:641-643`, which PB-D68 (xvii) already got right and never backported — the found-once-never-propagated class PP-D11 names via G10. **Canon per PP-D43:** `04_tokenomics.md:213`, no wei of AuMM, svZCHF or sUSDS leaving der Bodensee — every one of these four edits keeps the conservation assertion that guarantees it and only widens the tolerance to what the Vault actually does, so no rung here weakens the guarantee it protects.

**THE CARVE-OUT, and it is the single thing this rung is most likely to get wrong.** `src/emission/BodenseeBootstrapChannel.sol:308` carries the IDENTICAL exact-delta assertion A.3 condemns, `postReserve != preReserve + amount` reverting `ReserveDeltaMismatch`, and it is CORRECT THERE AND IS NOT TOUCHED. The channel donates AuMM: a STANDARD token at unit rate with no rate provider, so the Vault's DONATION branch performs no scaled18 round-trip on it and `PoolDataLib` debits it no yield fee. A.3 is scoped by the plan to 18-decimal WITH_RATE rails for exactly that reason, which is why PB-D68 (xvii) was a hook-only correction in the first place. **This is PB19 running in reverse.** PB19 records the cost of copying the AuMM-valid exact form ONTO rails; the error available here is copying the rails fix BACK onto AuMM, and PP-D11's "one shape" phrasing invites it. The residual fix touches the channel; the rise fix does not. A rung that changes `:308` has broken a correct assertion and weakened a conservation guarantee to close a defect that is not there.

**TWO done-criteria cells are CORRECTED, and this is the adjudicated half of the lock.** A.2's cell was created correctly as the invert, `test/fork/SwapAndDepositToBodensee.t.sol::test_P1_A2_strayWeiDoesNotBrickEitherRail`. A.3's and E.1's were created naming their own reproductions verbatim — `::test_P1_A3_belowParRateBricksDonate` and `test/fork/StageHIntegration.t.sol::test_P1_E1_strayWeiBricksDistribute` — so both name a case asserting the defect is LIVE and neither can serve as a fix gate: PP4.52 would read a green run of a test whose name claims the brick still bites. PP-D42 fixes the cell as the reproduction-versus-fix INVERT and cites A.2's own `Bricks` to `DoesNotBrick` as the exemplar, so these two are MALFORMED AT CREATION rather than binding as written. They become `::test_P1_A3_belowParRateDoesNotBrickDonate` and `::test_P1_E1_strayWeiDoesNotBrickDistribute`, PATHS UNCHANGED since both reproductions already landed on their D35-correct fork fixtures. This is the SECOND done-criteria edit of the stage after F.1's inversion at PP3.3g, and it follows that precedent deliberately: the correction is adjudicated in its own beat BEFORE the code rung, so no fix shape is baked in while its gate is still wrong.

**What this rung DECLINES.** The permissionless `flush(token)` hard-wired to der Bodensee that A.2's fix-intent offers as an alternative: the delta assertion alone closes the brick, and a new permissionless external entry on the donation helper is a surface expansion this rung has no reproduction for. Dropping `:276-277` entirely, which E.1's fix-intent offers: `BptMintedOnDonation` and the reserve check do prove the tranche landed, but the residual check additionally proves the helper consumed what it pulled, and a delta keeps that proof while removing the brick. And retiring `ReserveDeltaMismatch` from `SwapAndDepositToBodensee`: it goes unused there once A.3 lands, but the channel still declares and uses its own, and deleting an error from one contract while its twin survives in the other is churn this rung does not need.

**FIRST AMENDMENT at PP4.2c, 2026-08-27, before any code landed — the residual delta needs DIFFERENT arithmetic in the two contracts, and the form locked above is VACUOUS for A.2.** The lock says to read the balance before the `_vault.unlock` and revert if the post balance exceeds it. That is correct for E.1 and wrong for A.2, because the two contracts obtain `amount` by opposite routes, VERIFIED at HEAD rather than assumed. `SwapAndDepositToBodensee` has NO `transferFrom` anywhere: callers PUSH the tokens first and then invoke, `GaugeRegistry.sol:145-146` doing `safeTransfer(swapAndDeposit, ANTI_SPAM_FEE)` immediately before `swapAndDeposit(...)`, and `AureumGovernance.sol:213-214` doing `safeTransferFrom(msg.sender, address(BODENSEE_CHANNEL), amount)` immediately before `donate(...)`. So the entry balance is already `residual + amount`, the post balance is `residual`, and `residual > residual + amount` is NEVER true: the assertion would compile, would survive the reproduction's inversion, and would assert NOTHING. `BodenseeBootstrapChannel` is the opposite: it MINTS to itself via `mintRouter.mintFor(address(this), amount)` inside `distribute()`, so a snapshot taken before that mint is genuinely the pre-existing residual and the locked comparison holds exactly as written.

**The corrected A.2 form, and the error's argument.** Snapshot `entryBalance` at the top of each entry, after the `payToken` validation — `payToken` is a validated parameter and constant within the frame, so the snapshot and the tail check read the same token. At the tail compute `expectedResidual = entryBalance - amount` and revert `HelperBalanceNonZero` when the post balance EXCEEDS that. The subtraction cannot underflow on any reachable path: the callback's `payToken.safeTransfer(address(_vault), amount)` requires the helper to hold at least `amount`, nothing adds to the balance between the snapshot and that transfer, and a callback revert makes the tail unreachable — and under solc 0.8 an impossible underflow would revert loudly rather than wrap. **The error's ARGUMENT changes from the total residual to the EXCESS**, `residual - expectedResidual`, which is the only figure that is non-zero exactly when the error fires; reporting the total would leave an operator unable to tell a tolerated pre-existing residual from an excess the call created, and reporting the excess makes the existing name `HelperBalanceNonZero` read correctly rather than approximately. E.1 keeps the plain `residual` argument, because there the tolerated baseline is whatever predates the mint and the residual IS the excess whenever the two differ.

**Why this is recorded as an amendment rather than silently built.** The lock was written from the defect's shape and not from the callers' behaviour, and one read of two call sites falsified it. PP-D44 took three amendments for the same reason, each after the compiler or the suite contradicted something already recorded as settled; this one lands BEFORE the code rung instead of after it, which is the only difference and the one worth keeping. A fix author reading only the residual-shape paragraph above would have written the vacuous form in both contracts, and no test in the tree would have caught it — the reproduction inverts to a SUCCESS assertion, and a vacuous guard passes a success assertion perfectly.

**SECOND AMENDMENT at PP4.2g, 2026-08-27 — A.3's fix has TWO clauses and the lock carried only one; the fork gate caught it.** `test_P1_A3_belowParRateDoesNotBrickDonate` failed `BalanceNotSettled()` at the first PP4.2 fork run, four of five passing. The queue's A.3 fix-intent reads "`if (postReserve <= preReserve) revert ReserveDidNotRise`; SETTLE THE VAULT'S RETURNED `amountsIn` AFTER THE ADD", and PP-D45 above transcribed only the first clause. The helper still transferred and settled the OFFERED `amount` BEFORE the add, so once a below-par rate makes the DONATION consume less than offered, the helper holds unsettled credit and the unlock cannot close. This is PB-D68 **(xix)**, the hook's SECOND correction, sitting five lines below the `:641-643` the lock cites as its template and present in the pre-flight output that was read before drafting. The strict rise alone does not fix A.3; it converts a `ReserveDeltaMismatch` brick into a `BalanceNotSettled` brick.

**Closing it CONFLICTS with A.2's guard, and the conflict is structural rather than incidental.** With settle-after, the helper transfers only `amountsIn[idx]`, so the unconsumed rounding remainder STAYS on the helper as dust — the hook's own NatSpec says so in terms and relies on its next route sweeping it, a sweep this helper does not have. The residual then reads `stray + dust` while A.2's baseline `entryBalance - amount` reads `stray`, so the guard built at PP4.2d fires on the dust the A.3 fix necessarily creates. Every alternative shape was traced and leaves the same dust: donating `balanceOf(this)` rounds the larger amount and strands the same remainder, and `sendTo`-ing the excess credit back returns it to the helper by definition. **RESOLVED (user-adjudicated): thread the ACTUAL consumed amount back.** A FIFTH transient slot carries `amountsIn[idx]` from the callback to the entry, and the baseline becomes `entryBalance - consumed` rather than `entryBalance - amount`. The conservation proof survives exactly, with no tolerance figure and therefore no PP-D43 exposure; the two REJECTED alternatives were dropping the residual assertion outright, which is the option the plan offers for the sibling E.1 and which retires a conservation assertion rather than repairing it, and bounding the dust with a slack constant, which invents a number no canonical clause supports.

**The comparison stays `>` on the difference rather than `!=`, and that is deliberate.** With the baseline now `entryBalance - consumed`, the below-baseline direction is structurally unreachable: the settle transfer is the callback's only outflow of `payToken` and it moves exactly `consumed`, so a residual BELOW the baseline cannot arise. `>` is therefore exact in effect while keeping `HelperBalanceNonZero`'s argument meaningful as the excess that should not be there, where `!=` would force the argument to a bare total an operator cannot interpret without the expected value beside it. **One test consequence, recorded so the rung does not read as a regression:** `test_P1_A3_belowParRateDoesNotBrickDonate` asserts a zero helper residue, which was correct under settle-before and is wrong under settle-after — the below-par case leaves exactly the rounding dust it exists to demonstrate, so that assertion moves from zero to the dust the strict rise now tolerates.

**THIRD AMENDMENT at PP4.2h, 2026-08-27, before any code landed — the threading mechanism changes from a fifth transient slot to `unlock`'s own return channel, which this codebase already uses.** `AureumFeeRoutingHook.sol` has THREE call sites (`:684`, `:727`, `:773`) that capture `bytes memory result = _vault.unlock(...)` and decode it, and `AureumProtocolFeeController.sol:269` carries an explicit comment marking a site where the return is deliberately unused — VERIFIED at HEAD, this codebase already has a live convention rather than an invented one. The exact shape, from `_routeYieldFeeUnlocked` at `AureumFeeRoutingHook.sol:704`: the callback declares `external onlyVault returns (uint256 bptMinted)` and returns the value directly, solc handling the ABI encoding; the caller does `bptMinted = abi.decode(result, (uint256))`. `_swapAndDepositCallback` adopts the identical shape: `returns (uint256 consumed)`, assigning `consumed = amountsIn[idx]` in place of the current bare `.safeTransfer`/`.settle` pair's silent discard, and both `swapAndDeposit` and `donate` capture `bytes memory result = _vault.unlock(...)` then `uint256 consumed = abi.decode(result, (uint256))` immediately after.

**Why this is strictly better than the slot, stated so a later reader does not have to re-derive it.** The four EXISTING transient slots in this file are all written by the ENTRY before `unlock` and cleared by the entry after — a single direction. A fifth slot for this purpose would run the OPPOSITE direction, written by the callback during the call and read by the entry after, which is a second write/read protocol in the same file for one single value with no reuse. The return channel needs no new slot declaration, no reversed-direction write-then-clear pairing to get subtly wrong, and cannot go stale, because it is scoped to the single `unlock` call rather than to a slot that persists across the whole transaction. `D22` in `CLAUDE.md`'s engineering-lesson index already names the risk on the DECODE side — inner `returns (uint256)`, not encoded `bytes` — which this rung now has three real, already-correct examples of in the sibling contract to check against rather than getting right from first principles.

**What does NOT change.** The RESOLUTION locked at the second amendment is untouched: `expectedResidual = entryBalance - consumed`, the comparison stays `>` on the difference for the reason already stated there, and the below-baseline direction remains structurally unreachable. Only the PLUMBING that gets `consumed` from the callback to the entry moves.

## PP-D46 — the DeployStageP accept entry, and the PP4.1 fork-gate gap that surfaced it

**PP-D46 (user-adjudicated 2026-08-27): PP4.1 was closed with an INCOMPLETE fork gate, and two fixtures were left red by its own changes.** The gap was found while running PP4.2's wider gate, not PP4.1's. `test/fork/DissolutionGovernanceHandoff.t.sol` fails 2 of 9 with `PoolNotRegistered`, the error F.1 ADDED at `f4d2442`, because both `test_P1_C2_*` cases bind a recorder for a `makeAddr` pool that the real Vault has never registered — precisely what the new gate exists to reject. `test/fork/StagePIntegration.t.sol` fails 1 of 23 at `test_wires_stageHandoffsComplete`, whose fourth assertion reads the L-D25 boost leg and finds zero, because `DeployStageL.run()` only PROPOSES as of `325c1f9` and `DeployStageP` composes it. PP4.1's gate ran Part-A, `StageLIntegration` and `DeployStageL`; neither failing fixture was in it, though both exercise contracts F.1 changed. **The lesson is the gate's scope rule and it generalises past this rung: a fix rung's fork gate covers every fixture that EXERCISES the changed contract, not merely the fixtures the rung EDITED.** The four files PP4.1 touched under `test/` were the four it thought about; the two it broke were the two it never listed. F-67's `Fixed at PP4.1` stands — the remedy is correct and its done-criteria are green — but the closure was attested on a gate that had not been run to completion, and PP4.2 pays the difference.

**The production gap is real and separate from the stale tests.** `DeployStageP.run()` no longer completes the Stage-L wiring: it arms the incendiary registry and stops, so a spine that returns successfully leaves the protocol with `incendiaryRegistry` at zero and the L-D25 boost leg dead. `_assertPostConditions()` does not check that binding, so nothing fails loudly. **RESOLVED: `DeployStageP` gains a second entry** forwarding to `DeployStageL.runAcceptRegistry()`, invoked as a second `forge script` run at a later block, mirroring the split `DeployStageL` already carries. Two rejected alternatives, each for a stated reason. A RUNBOOK-ONLY step was rejected because the orchestrator would still produce an incompletely-wired protocol with only prose between that and mainnet, the shape RB-025 and PP-D28 exist to harden against. And adding the incendiary binding to `_assertPostConditions()` was rejected AS STATED, because that function runs at the END of `run()` — asserting the binding there would make the production spine fail BY DESIGN, since the registry is legitimately unbound at that instant. The assertion moves to the ACCEPT entry instead, which is the only point at which it is true, so option 3's intent survives at the one place it can hold.

**The two stale fixtures, and what their fixes must NOT do.** The `StagePIntegration` fixture drives the accept explicitly rather than having it buried inside `DeployStageL.deploy()`: PP4.1c20a considered and rejected a `vm.roll` inside that testable entry, on the ground that it would make a fork test assert a binding which in production requires a separate operator action, and that reasoning is unchanged. The two `test_P1_C2_*` cases move from `makeAddr` pools to pools the Vault actually knows, preserving the C.2 mechanism they exist to demonstrate — that after dissolution the bind path works only under prank and no proposal type can emit it — WITHOUT weakening it into a test of F.1's gate, which is a different finding with its own row. C.2's verdict is DEFERRED under PP-D22, so its reproductions stay reproductions and neither is inverted here.

**CLOSING RECORD at PP4.2z, 2026-08-27 — PP4.2 is BUILT and F-25, F-26 and F-56 all read `Fixed at PP4.2`.** Commits: A.2 and A.3 at `1216b10`, `9979185`, `427f75f` with tests at `025f3f5`, `5fd3f15`, `a30011a` and `8b3c7f4`; E.1 at `d97432f` with tests at `627cf88` and `57dc871`; the PP-D46 fallout at `b0ef486`, `5eef63d`, `a50f9b5`, `e4db5d3`, `3f42c53` and `59a4b00`; the ledger at `517cfd9` and `61feb37`. **Canon per PP-D43:** `04_tokenomics.md:213`, no wei of AuMM, svZCHF or sUSDS leaving der Bodensee — every edit KEPT its conservation assertion and only widened the tolerance to what the Vault actually does, and the A.3 done-criteria now pins that widening as an identity rather than an inequality: reserve rise plus stranded dust equals the offered amount, so nothing is lost. Gates: Part-A **1064 passed, 0 failed, 1 skipped of 1065** across 90 suites, the skip being RB-024's deferral and the total being 1065 rather than 1064 because PP4.1c21 added a case to `P1_F1.t.sol` after the last full run; and eight fork files file-scoped with the RB-023 sentinel reading 0 in the same shell — `SwapAndDepositToBodensee` 5/5, `StageHIntegration` 13/13, `StageGIntegration` 40/40, `DeployStageG` 1/1, `DeployStageK` 6/6, `StagePIntegration` 23/23, `DissolutionGovernanceHandoff` 9/9, and `StagePRunRehearsal` **10/10, up from 9** with the accept coverage this rung added.

**FOUR results not recoverable from the diffs.** FIRST, the locked delta was VACUOUS and only a source read caught it: `SwapAndDepositToBodensee` has NO `transferFrom`, callers PUSH before invoking, so the entry balance already contains `amount` and the locked `post > entry` comparison could never fire. It would have compiled, survived the reproduction's inversion, and asserted nothing — a vacuous guard passes a success assertion perfectly, so no test in the tree would have caught it. SECOND, A.3 has TWO clauses and the strict rise alone converts one brick into another: settling the OFFERED amount before the add leaves the unlock over-credited once a below-par rate makes the DONATION consume less, and the fork gate surfaced it as `BalanceNotSettled`. THIRD, closing that conflicts with A.2's guard STRUCTURALLY — settle-after strands the rounding remainder as dust, which the `entryBalance - amount` baseline rejects — and every alternative shape traced leaves the same dust, so the resolution had to be threading the ACTUAL consumed amount back, which `unlock`'s own return channel already carries in three sibling call sites. FOURTH, the CARVE-OUT held and its evidence is a passing test: `BodenseeBootstrapChannel.sol:308` keeps the exact-delta assertion A.3 condemns, correctly, because the channel donates AuMM at unit rate with no rate provider, and `test_Distribute_RevertWhen_ReserveDeltaMismatch` still passes untouched.

**What this rung DECLINED, beyond the lock's four, and one process cost worth naming.** Retiring `ReserveDeltaMismatch` from `SwapAndDepositToBodensee`, which is now unused there but whose twin is live in the channel; a permissionless `flush(token)`; and dropping the residual assertion outright, the option the plan offers for E.1, which would have retired a conservation assertion rather than repairing it. **The process cost: THREE stale unit tests and TWO stale fork fixtures were found by gates rather than by sweeps.** PP4.2f found `testReserveDeltaMismatchUnderMockedReserves` by sweeping `test/fork/` for the changed error and never ran the same sweep over `test/unit/`, where two more sat; and PP-D46 records the two fork fixtures PP4.1 never listed. Both are the same omission at different scopes — the sweep that finds a stale expectation must cover every directory that references the changed symbol, and the gate that proves a fix must cover every fixture that exercises the changed contract. Neither is a new rule; both were available and neither was applied until a red run forced it.

## PP-D47 — E.6 (PP4.5, rung 5): the pending window, the era-bounded catch-up, and the forfeiture's end

**PP-D47 (user-adjudicated 2026-08-27): E.6 lands as shape (b) of its fix intent — the bounded-chunk settle — as TWO coupled edits inside `EmissionDistributor._accrueGlobal` and NOTHING else; the registry, its interface's behaviour, and every mock stay untouched.** EDIT ONE, the guard: the empty-interval short-circuit tightens from `block.number == last` to `block.number <= last`, making the cursor monotone by construction — a pre-genesis touch (PB-D19 seeds `lastAccrualBlock` at a FUTURE `GENESIS_BLOCK`) now no-ops without dragging the anchor below genesis, so the below-genesis snap that `test/unit/PreGenesisAccrual.t.sol:99-108` pins today, and the inverted-window hazard PB-D19 (v) guards, both become structurally impossible rather than guarded. EDIT TWO, the branch and the bound: the `totalScore == 0` branch stops writing `lastAccrualBlock` — it returns with the cursor where it was, so a window in which every score self-cleared stays PENDING instead of forfeited — and the integrate path clamps its upper end to `to = min(block.number, last + MAX_ACCRUAL_SPAN_BLOCKS)` with `MAX_ACCRUAL_SPAN_BLOCKS = AureumTime.BLOCKS_PER_ERA`, writing the cursor to that clamped `to`. One call's work is thereby bounded by one era — at most two iterations of `_lpTrancheIntegral`'s era loop and roughly 104 epoch buckets in the continuous-phase `integratedSkim` walk — regardless of how long the gap grew, and a backlog drains across successive touches, same-block touches included, because the tightened guard compares against the ADVANCED cursor. When no gap exists the clamp never binds and the function is behaviour-identical to today. **Canon per PP-D43:** `11_formulas.md` F-6, Effect — "The entire post-Incendiary emission is distributed in proportion to scores. No emissions left unallocated." The live branch leaves a whole window unallocated; the fix defers the window to the next accrual with live scores, which is the only F-6-conformant disposition, since shares are undefined while the score sum is zero.

**What changes semantically, stated so the NatSpec amendment is a record rather than a surprise.** H-D26's "fresh against `block.number`" weakens to "fresh against the cursor, which equals `block.number` except mid-drain": during a drain, `_settlePool` and `claim` read a monotone, converging accumulator — value is DEFERRED, never lost — and the drain needs no new entry point, every existing mutating entry advancing one era per call. The crediting rule is explicit: a pending window is credited pro-rata to the scores live at drain time. And the fix intent's two-sided conservation invariant pins the whole thing as an IDENTITY, not an inequality: over any zero-score gap plus its drain, the accumulator's growth times the score equals `_lpTrancheIntegral` over the drained range to the wei — forfeiture would fail it low, double-counting would fail it high.

**Why (b) and not (a), recorded so the Fenwick's absence reads as declined rather than missed.** A plain write-time prefix sum is UNCONSTRUCTIBLE against `_placeBoost:483-495`, which writes only FUTURE epochs and tops the same epoch up across purchases until it begins; an honest (a) is therefore a Fenwick tree over per-block epoch rates — O(log) both sides, about forty lines of new data structure on the emission surface across two contracts — or a lazy freeze-and-roll whose unfrozen tail goes unbounded exactly in the no-activity case E.6 is about. (b) is one guard and one clamp in one contract; it preserves L-D23's divide-first exact-tiling arithmetic byte-identically — the surface that already ONCE chose the direct walk over a cache, when L-D23 superseded L-D9 — and it closes the tail (a) also closes: a decades-long single-call catch-up whose walk exceeds the block gas limit would resurrect the forfeiture as a gas-brick, and under the era bound no such call exists. What (a) would have bought and (b) declines: bounding every OTHER `integratedSkim` consumer at the source, in particular the F.1 clamp's one-time cold-cursor `_lpTrancheIntegral(GENESIS_BLOCK + 1, block.number)` walk on a pool's first boost delivery — pre-existing, one-time per pool, roughly 26 epochs per year of protocol age, and not this rung's defect. (c), the `try/catch`, is declined on H-D29's own recorded ground: silent skim-suppression past the 21M cap is worse than a loud revert.

**One stale document is corrected in passing, because it names a mechanism that does not exist.** `IIncendiaryRegistry.sol:12` still promises "bucket-cached cumulative sums internally so `integratedSkim` itself stays O(1)" — an L-D9 survivor that L-D23's rewrite orphaned, RB-029's comment-names-a-different-mechanism class verbatim. It is rewritten to state the direct epoch walk and the distributor-side era bound that now contains it. Doc-only; the registry's runtime bytecode does not move.

**Blast radius, measured at HEAD rather than predicted.** `test/unit/EmissionDistributor.t.sol:1138` `test_Accrual_EmptyTotalScoreGuardAdvancesBlockOnly` pins EXACTLY the removed write and inverts to the pending semantics; the two `PreGenesisAccrual` snap pins at `:99-108` invert to anchor-holds-at-genesis while the `:89-94` premise pin stands; `test/whitehat/P1_E6.t.sol`'s burned case inverts to the done-criteria name `test_zeroTotalScoreWindowDoesNotForfeit` and gains the era-drain case and the two-sided conservation case; the `integratedSkim` mockCall expectations at `test/unit/EmissionDistributor.t.sol:1499-1608` are untouched so long as no test's accrual span crosses one era, which is asserted at drafting, not assumed. The formal sweep covers `test/formal/` and `formal/act/` for any distributor-accrual property the cursor semantics falsify — the `lastAccrualBlock` hits at `EmissionProofs.sol:556-582` are the CHANNEL's cursor, a different contract, but the sweep runs regardless, and any rewrite lands as PENDING-REATTESTATION per PP-D37. The fork gate per PP-D46 covers every fixture exercising `EmissionDistributor`: the symbol grep's thirteen — DeployStageH, DeployStageI, DeployStageL, StageHIntegration, StageGIntegration, StageIIntegration, StageKIntegration, StageLIntegration, StageMIntegration, StageNIntegration, StagePIntegration, RouterIntegration, DissolutionGovernanceHandoff — plus `StagePRunRehearsal`, which exercises the deploy spine that constructs it; fourteen file-scoped runs behind the RB-023 sentinel.

**What this rung DECLINES, beyond the Fenwick.** Any registry edit and any new external entry — no permissionless drain function, because every existing mutating entry already drains a chunk and a keeper wanting speed can batch `recordScore` calls; any change to `poolBoostCursor` or the boost-delivery leg, whose defects are E.8's and live in their own rows; and any touch on `BodenseeBootstrapChannel.lastAccrualBlock`, a same-named cursor with its own semantics in a contract this rung does not open.

**FIRST AMENDMENT at PP4.5c, 2026-08-27, before any code landed — the pending window as locked is credited to ONE pool, structurally rather than as a race, and the launch case makes it certain.** The lock says a pending window is credited pro-rata to the scores live at drain time and treats that as self-evidently fair. It is not, and the reason is call ORDER, verified at HEAD rather than assumed: `_accrueGlobal()` runs at step 2 of `recordScore` (`:532`) while the score write lands at step 10 (`:558`), so the first `recordScore` after a zero-score gap still observes `totalScore == 0`, returns, and writes pool A's score; the SECOND call then integrates the entire backlog against a denominator consisting of pool A alone. An honest keeper batching all 26 calls produces exactly that outcome — this is the default path, not an attack, and no ordering of honest calls avoids it. **Against F-1 it is canon-hostile.** During bootstrap `_alphaF3` is 0, so the H-D33 Miliarium reshape gives every scored pool `f5Total / 28`, the canonical equal split of `11_formulas.md` F-1; crediting one pool 100% of a window the spec divides 28 ways is precisely the divergence PP-D43 makes binding. **And the launch window is guaranteed, not hypothetical:** `EMA_MATURITY_BLOCKS` is 432,000 and `_gatedTvlEMA:515-521` returns 0 until it elapses, so `totalScore == 0` holds for the whole post-genesis maturity window — on generation-3 numbers, seeding at 11405532 against genesis 11597476 leaves about 240,000 blocks, some 33 days of bootstrap-rate tranche, landing entirely on whichever pool is scored first. The locked shape therefore replaces a forfeiture every LP shares with a windfall one pool takes, which is E.6's own second face — a caller booking the outcome at a moment of their choosing — reappearing with the sign flipped.

**RESOLVED (user-adjudicated): cold-start FORFEITS and lapses are HELD, and the span bound drops from one era to one epoch.** The zero-score branch advances `lastAccrualBlock = block.number` while `accRewardPerScoreUnit == 0` and returns WITHOUT advancing it otherwise — two lines, no new state. The sentinel is exact for its purpose: the accumulator is zero if and only if no accrual has ever completed against live scores, which is the launch window and nothing else, and it is monotone thereafter. A window in which no pool was ever eligible is forfeited because no pool earned it — the 60-day maturity gate is F-04's own anti-spot-pump FIX, so the emission it withholds is unearned by construction rather than lost — while a genuine lapse, where scores WERE live and staled, stays pending exactly as E.6 requires. `MAX_ACCRUAL_SPAN_BLOCKS` becomes `AureumTime.BLOCKS_PER_EPOCH`, 100,800, rather than `BLOCKS_PER_ERA`: the bound now serves two purposes rather than one, capping both the per-call gas at roughly two `integratedSkim` buckets and the per-call MISALLOCATION at fourteen days, so E.6's own reported scenario — one 14-day keeper lapse staling all 26 at once — drains in exactly one call while any longer lapse spreads across successive calls as the roster refills.

**One caveat recorded rather than hidden, and one alternative declined.** The `accRewardPerScoreUnit == 0` sentinel also reads zero in the pathological case where every accrual so far rounded to zero under `divDown` — a contribution smaller than the score sum — after which a subsequent lapse would be treated as cold-start and forfeited. That edge fails in the CONSERVATIVE direction, matching today's shipped behaviour rather than over-crediting, and an explicit `bool hasAccrued` is the exact alternative, declined because a storage slot for a case that requires the accumulator to have never moved buys precision no reachable state needs. DECLINED separately: preserving the launch window and adding a mechanism to spread its credit across the roster, which is the most canon-faithful answer and the one this rung cannot afford — it needs either new per-pool state or a deferred-release design, and PP-D47 is scoped to two edits in one function. That option is not refuted, only deferred; if a later reader wants the launch tranche distributed rather than withheld, it is a design rung of its own and not an amendment to this one.

**What this changes in the blast radius stated above.** The `PreGenesisAccrual` inversion is UNAFFECTED — the tightened `<=` guard still makes the below-genesis snap impossible, and the cold-start branch cannot fire before genesis because that guard returns first. `test_Accrual_EmptyTotalScoreGuardAdvancesBlockOnly` at `test/unit/EmissionDistributor.t.sol:1138` now inverts to TWO cases rather than one, since the branch it pins has two arms: cold-start still advancing the cursor, and a post-accrual lapse holding it. The done-criteria case `test_zeroTotalScoreWindowDoesNotForfeit` must therefore establish a completed accrual BEFORE the gap it measures, which `test/whitehat/P1_E6.t.sol`'s reproduction already does at its control assertion — the existing PoC drives a healthy accrual first and asserts `accAfterHealthyAccrual > 0`, so the fixture needs no new setup to reach the held-pending arm.

**SECOND AMENDMENT at PP4.5l, 2026-08-28, found by the Part-A gate rather than by any sweep — the epoch clamp splits the protocol's most common window by exactly one block, and the first amendment's "drains in exactly one call" is CORRECTED to one or two.** `MAX_ACCRUAL_SPAN_BLOCKS` is `AureumTime.BLOCKS_PER_EPOCH`, and so is `EMA_STALENESS_BLOCKS` at `EmissionDistributor.sol:60`; the staleness gate is a STRICT `>`, so a window built to just exceed one staleness period measures `BLOCKS_PER_EPOCH + 1` blocks, one more than the clamp releases per call. `test/unit/ZeroTvlDisposition.t.sol::test_ZeroTvl_windowC_dustCapturesUntilSelfClear` is exactly that shape and failed by `1e18`, one block at unit rate, against a 100,801-block leg. The collision is structural rather than a coincidence of that fixture: every window in this protocol expressed as one staleness period plus the strictness offset now needs a second call.

**The constant is KEPT at one epoch and the TEST is corrected, which is the opposite of the reflex.** Three alternatives were weighed and rejected. Two epochs would drain every such window in one call but DOUBLES the adjudicated misallocation bound from fourteen days to twenty-eight, undoing the first amendment's whole rationale for the figure. `BLOCKS_PER_EPOCH + BLOCKS_PER_DAY` would clear the boundary at fifteen days but is an unprincipled constant chosen to dodge one test. And leaving the fixture red was never an option, since it is wei-exact EVIDENCE for the closed PB-D18 (vi) disposition rather than an ordinary regression test. What the fixture asserts is an economic magnitude — one wei of dust captures the FULL tranche while a stale score lives — and that claim is untouched by how many calls deliver it, so the correction is a second `claim` in leg 1 and nothing else.

**Legs 2 and 3 need NO change, and leg 3 passes BECAUSE of the fix rather than despite it.** Traced against the real constants before any edit: with leg 1 fully drained the cursor sits at `t1`, so leg 2's span is exactly `BLOCKS_PER_EPOCH`, which is NOT greater than the clamp and therefore integrates whole in one call; and leg 3, which asserts that the epoch after the self-clear pays zero, now holds because `totalScore == 0` with a non-zero accumulator HOLDS the cursor, so the cumulative capture stays pinned at the leg-2 figure the assertion already names. A fixture written to pin the old forfeiture semantics would have gone red at leg 3; that it goes green is a positive control on the lapse arm that no test in `P1_E6.t.sol` supplies.

**The sweep axis this exposes is new and belongs in the register, not just in this rung.** PP4.2's recorded lesson is that a sweep must cover every DIRECTORY referencing a changed symbol, and that lesson was followed here — `test/`, `src/`, `script/`, `formal/` were all swept for `lastAccrualBlock`, `_accrueGlobal` and the changed comparison. `ZeroTvlDisposition.t.sol` names NONE of them. It collided with the fix because of a QUANTITY, the number of blocks it spans in a single accrual, and no grep over identifiers can find that. A clamp introduces a class of blast radius that symbol sweeps are structurally blind to: every existing caller whose span exceeds the new bound. The instrument that found it was the full Part-A gate, which is the argument for running the whole suite before declaring a clamp landed rather than only the files the rung touched.

**CLOSING RECORD at PP4.5q, 2026-08-28 — PP4.5 is BUILT and F-61 reads `Fixed at PP4.5`.** Commits: the lock and its plan row at `893844f` and `09459ef`, the first amendment and its row correction at `3a25736` and `b681856`; the code at `8b31a7f` (the `MAX_ACCRUAL_SPAN_BLOCKS` bound and the cursor NatSpec) and `332786b` (the non-strict guard, the cold-start fork and the clamp); the tests at `28ec156` (the PB-D19 (v) pin inverted to hold-at-genesis), `0c77363` (the done-criteria case), `a386c78` (the harness swap), `ee3ff63` (cold-start forfeiture and one-epoch drain) and `2bda4ce` (the two-sided conservation identity); the second amendment and its fallout at `9d223d0` and `30b2c4c`; the superseded prose at `a5fd340`; RB-030 at `4d2bb38`; the ledger at `fefd47b`. **Canon per PP-D43 is TWO clauses, not one, and the second is why this rung has a fork at all:** `11_formulas.md` F-6, "No emissions left unallocated", which the live branch violated by forfeiting a whole window; and F-1's equal 1/28 bootstrap split, which the NAIVE fix would have violated by handing that window to one pool. Gates: Part-A **1067 passed, 0 failed, 1 skipped of 1068** across 90 suites, the skip being RB-024's deferral and the total being 1068 rather than 1065 because `P1_E6.t.sol` went from one case to four; and **13 of 13 fork files green**, file-scoped with the RB-023 sentinel reading 0 in the same shell — the roster being every fixture that EXERCISES `EmissionDistributor` per PP-D46 rather than the two this rung edited, which is `DeployStageH` 1/1, `DeployStageI` 1/1, `DissolutionGovernanceHandoff` 9/9, `RouterIntegration` 7/7, `StageGIntegration` 40/40, `StageHIntegration` 13/13, `StageIIntegration` 4/4, `StageKIntegration` 9/9, `StageLIntegration` 2/2, `StageMIntegration` 7/7, `StageNIntegration` 9/9, `StagePIntegration` 23/23 and `StagePRunRehearsal` 10/10. Six of those thirteen had no recorded figure anywhere before this rung and are now pinned.

**FIVE results not recoverable from the diffs.** FIRST, the cold-start fork is NOT defensive coding: without it the entire pending backlog is credited to ONE pool, structurally rather than as a race, because `_accrueGlobal` runs at step 2 of every mutating entry while the score write lands at step 10 — so the call that restores a score still observes `totalScore == 0` and holds, and the drain lands on the call after it, against a denominator of exactly one pool. An honest keeper batching all 26 `recordScore` calls produces that outcome; no ordering of honest calls avoids it. SECOND, the clamp collided with a canonical constant rather than landing in open space, and the full Part-A gate found it where no sweep could — filed as RB-030. THIRD, NO machine-checked formal property is falsified, and that was VERIFIED rather than assumed: `EmissionProofs.sol`'s four `lastAccrualBlock` assertions are all `channel.lastAccrualBlock()`, P-BC3's "accrue structural guards" is the CHANNEL's `accrue()` per `formal/RESULTS.md:203`, and P-ED1 through P-ED4 are distributor GOVERNANCE locks; the distributor's `_accrueGlobal` has no proof asserting it, so PP-D37's bar on re-running the bundle was never engaged. FOURTH, `ZeroTvlDisposition` leg 3 passes BECAUSE of the fix rather than despite it — it asserts the epoch after a self-clear pays zero, which now holds because the lapse arm HOLDS the cursor, making it a positive control on that arm which no case in `P1_E6.t.sol` supplies. FIFTH, the O(1) promise corrected at `IIncendiaryRegistry.sol:12` described a bucket-cached cumulative sum that NEVER SHIPPED: L-D23's direct epoch walk superseded the L-D9 crystallize-cache model, and the interface comment outlived the design it documented.

**What this rung DECLINED.** The Fenwick tree that an honest shape (a) requires, because `_placeBoost` writes only FUTURE epochs and tops the same epoch up until it begins, so a write-time prefix sum is unconstructible and the real cost is about forty lines of new data structure across two contracts; any registry edit at all; a permissionless drain entry, since every existing mutating entry already drains a chunk; raising the clamp to two epochs when `ZeroTvlDisposition` went red, which would have doubled the adjudicated misallocation bound to twenty-eight days to avoid editing one test; and preserving the launch window behind a mechanism that spreads its credit across the roster, which is the most canon-faithful answer available and is DEFERRED rather than refuted — it needs new per-pool state or a deferred-release design, and belongs to a design rung of its own.

**One process cost, and it is a single habit wearing four costumes.** Four expectations in this rung were wrong and the payloads were right every time: an em-dash tally that added a payload OCCURRENCE count to a file LINE count; a zero-hit sentinel whose pattern was never measured for REACH, so a legitimate second hit read as a failure; a `grep -B1` where solc prints the file:line AFTER the warning, which filtered out the very thing it was written to find; and a sentinel whose baseline was measured while its payload contribution was assumed. Every one is the same rule failing — a number asserted about a file without running the IDENTICAL command against both the payload and the baseline — and PP13 already names half of it. The cure applied from PP4.5m onward was to print BASE, OUT and IN for every sentinel in one table before drafting the verify block, after which no expectation missed. The compounding cost was worse than the errors: two full `forge build --force` rebuilds were spent chasing a warning that belonged to `test/unit/EmissionDistributor.t.sol:420` and `:431` all along, and the evidence identifying it was already in the session's own earlier census. Read the transcript before spending a rebuild to re-derive what it already holds.

## PP-D48 — E.4, E.2 and E.3 (PP4.6, rung 6): the ledger debit, the two drains, the yield pin, and the gate that stays

**PP-D48 (user-adjudicated 2026-08-28): rung 6 is PP-D13's chain E.4 then E.2 then E.3 — A.3's precondition was discharged at PP4.2 and is not a sub-step here — and it lands as FOUR settled changes plus ONE clause left deliberately OPEN.** (i) **E.4:** `routeYieldFeeToHook` DROPS its `amount` parameter, reads `_protocolFeeAmounts[pool][token]`, zeroes it, and approves exactly that, byte-mirroring `_withdrawProtocolFees:791-799` eleven lines below it; `minBptAmountOut` is passed as 0 because PB-D68 (xiv) already made it dead as a bound, a donation reverting on any non-zero BPT. Ripple MEASURED: 21 call sites in `test/unit/AureumProtocolFeeController.t.sol`, 6 in `test/fork/AureumFeeRoutingHook.t.sol`, and ZERO in `src/` or `script/` — the absence of production callers being E.2's own point. (ii) **E.2's drains:** `withdrawProtocolFees` and `withdrawProtocolFeesForToken` drop `authenticate` while KEEPING the recipient pin to `FEE_ROUTING_HOOK`; both are a pure `safeTransfer` to a hard-pinned address with no swap and no slippage, so permissionless costs nothing and self-heals. (iii) **E.2's ledger clause, which is load-bearing rather than tidy-up:** `collectAggregateFees` must forward the swap leg the way the hook path already does. The asymmetry is verified at HEAD — the keeper path `_receiveAggregateFees:315-316` credits BOTH `ProtocolFeeType.SWAP` and `ProtocolFeeType.YIELD` into `_protocolFeeAmounts`, while the hook path at `:423-425` credits YIELD only and forwards swap through `_receiveAggregateFeesSwapForward`. Without (iii), E.4's route-the-full-credit mixes STANDARD swap dust into a function whose name and whose canon clause are both about yield. (iv) **E.3:** the constructor pins `_globalProtocolYieldFeePercentage` to 1e17; `registerPool` forces `aggregateYieldFeePercentage = 0` when `pool == DER_BODENSEE_POOL`, mirroring D-D15's swap-side ignore-the-exempt-flag pin; and BOTH yield setters hard-revert exactly as the swap pair does at `:641` and `:661`. **Canon per PP-D43:** `10_constitution.md` §xxix L150—151, whose three obligations are the 10% rate, 100% of the skim to der Bodensee, and der Bodensee excluded from collection.

**Why the hard-revert is REQUIRED rather than an optional extra, and why the Bodensee gap is registration rather than collection.** §xxix declares its parameters immutable from block 0, so a pinned 10% with live setters is not the canonical shape — it preserves exactly the authorizer-change residual E.3's own row already corrected when it withdrew "can never be set" as one word too strong. The struck option was never the hard-revert standing alone; it was the queue's PAIRED phrase, hard-revert **and amend the constitution**, whose second half PP-D43 forbids. On the exclusion: D-D9 ALREADY blocks collection at `collectAggregateFees:271-276` with `BodenseeYieldCollectionDisabled`, so the surviving gap is the REGISTRATION STAMP at `:616`, `protocolFeeExempt ? 0 : _globalProtocolYieldFeePercentage`. That flag is a Vault-side registration parameter with NO setter anywhere in `src/` or `script/`, so once the global is non-zero der Bodensee inherits a live yield stamp unless the controller pins it by address. Trusting a factory to pass the flag is not a code guarantee.

**THE GATE STAYS, and this reverses the drafting recommendation that preceded this lock.** `04_tokenomics.md` L155 states it in terms: yield fees "are collected permissionlessly; routing into der Bodensee is governance-gated and happens at most once per epoch for any given pool" — which is OQ-20 and OQ-21 exactly, and FINDINGS records that making routing permissionless after the cooldown would be a NEW open question and a break from OQ-20. Dropping `authenticate` from `routeYieldFeeToHook` therefore does not fix E.2; it deletes the mechanism the specification names, and it would make the finding disappear by weakening the spec, which is the anti-shape PP-D43 exists to stop. The canon protects the fee DESTINATION as one of its five extras, and the destination is unchanged either way — what L155 additionally names is the GATE, and that is a `04_tokenomics` clause, so changing it is PP5.4 / PP-D23 territory rather than a silent third bullet inside E.2. **The post-K defect is a REACHABILITY failure, not a mandate:** `authenticate` resolves to `AureumGovernance`, which cannot EXPRESS the call under fixed dispatch, which is the C.2 class. The remedy retargets the CALLER and keeps the gate.

**CLAUSE (v) IS OPEN and no code in this rung may presuppose its answer.** Who becomes `routeYieldFeeToHook`'s reachable governor: the hook's `governanceModule` Safe, which is live today; a typed `YieldFeeRoute` proposal on `AureumGovernance`, which restores expressibility at the cost of a seventh proposal type on a surface F-22 already widened; or leaving the entry gated and completing the skim through `recoverStrandedFees`, which is destination-correct and reachable but is the PB3.10 exception rail rather than the yield route L155 names. RECORDED PREFERENCE, not a lock: keep `routeYieldFeeToHook` as the yield route and make its own caller reachable, leaving recovery as the exception. Until (v) closes, E.3 pins a live 10% skim whose routing leg is still gated to an unreachable principal, so the rung MUST NOT be read as completing the L150 path end to end — collection and the ledger drain become reachable, the route does not.

**What this rung DECLINES, and the first two are corrections to proposals made during its own pre-flight.** A rate-derived floor computed from `IVault.getPoolTokenRates` is REJECTED because it is not a price: `PoolDataLib.getTokenRate:152-162` returns `rateProvider.getRate()` for `WITH_RATE` and `FixedPoint.ONE` for `STANDARD`, and an ERC-4626 `getRate()` is shares-to-underlying, so the ratio `rate(feeToken) / rate(depositToken)` is dimensionally a price ONLY when both tokens share an underlying — which the yield route never does, since it moves LST or ETH or USDS shares through the originating pool onto a CHF rail. Such a floor sits either far below spot, never binding and leaving F-13 open, or far above it, reverting every honest route and stranding the skim again; and because `STANDARD` returns `FixedPoint.ONE` rather than a sentinel, the rate array cannot even detect a missing provider, which needs `TokenInfo.tokenType` from `getPoolTokenInfo`. `MAX_ROUTE_SLIPPAGE_BPS` is REJECTED under PP-D43's fifth obligation: no §xxix clause names it, and a rung that cannot cite canon for a constant stops rather than adjudicating a number. A depth-fraction cap on the input is REJECTED as the wrong quantity — it bounds the route's OWN price impact while a sandwich works by the attacker moving the price first. And inventing a lagged reference is REJECTED as already-settled ground: PB-D9 (ii)(a) records that none exists for arbitrary fee tokens on arbitrary pools, and that `quoteSvZCHF` is constellation-spot averaging that goes circular when the originating pool is the token's only venue.

**CLAUSE (v) RESOLVED at PP4.6j, 2026-08-28 (user-adjudicated): the gate STAYS and is RETARGETED to a scheduled keeper, with the Safe as a standing fallback.** Permissionless is REJECTED for the reason the clause was opened: `04_tokenomics.md` L155 gates routing into der Bodensee, so removing `authenticate` amends the spec rather than fixing E.2. A keeper is the shape that satisfies every constraint at once. It PRESERVES PB-D9 (iii) rather than falsifying it — that disposition is "caller-supplied bounds, governance computes them off-chain, Vault enforces", and a keeper computing `minDepositTokenOut` from a real price source IS that design with a scheduled principal in place of a human one. It satisfies L155 literally, since a governance-seated keeper is a gate and the `_lastRouteBlock` throttle already caps the cadence at one route per pool per epoch (OQ-21). And it is PRECEDENTED rather than novel: PP-D31 already adjudicated "a funded keeper named as a trust assumption" for `advanceTournament` and `updateMultiplier`, and this is the same category with the same disclosure.

**Four typed-domain decisions, settled here because §12 bars materialising them in code first.** (a) `yieldRouteKeeper` is STORAGE, not an immutable — keepers are rotated, compromised and replaced, and an immutable would force a controller redeploy to change one operational address. (b) It is ROTATABLE, never one-shot: a burned keeper slot is a permanently dead route, which is the trap PP3.12 avoided when `admissionAuthority` took the `GaugeRegistry.governance` shape instead of `setGaugeRegistry`'s burn. (c) The rotation authority is the hook's `governanceModule` Safe, NOT `authenticate` — an `authenticate`-gated keeper setter would be circular, since `authenticate` resolving to an inexpressible `AureumGovernance` is the whole defect. The controller reads it through `IAureumFeeRoutingHook`, which gains a `governanceModule()` declaration on the PB-D69 (xiii) precedent, the hook already holding it as a `public` slot at `:111`. (d) The gate admits EITHER the keeper OR that Safe, so a stalled keeper is an inconvenience rather than a stranded skim.

**One scheduling finding that cuts against the obvious design and belongs in the runbook, not just here.** Firing at the END of each epoch is the intuitive schedule and the worst one: a deterministic trigger block telegraphs a large, price-moving swap on a known pool for a known token to every searcher, which is precisely the trade `minDepositTokenOut` is the only defence against. Nothing decays at the epoch edge — once the throttle clears, the route stays available indefinitely until taken — so the keeper SHOULD fire opportunistically inside the eligible window rather than at its boundary, and the jitter costs nothing. The keeper reads `getProtocolFeeAmounts(pool)` and skips pools with no credit, which post-clause-(iii) is a yield-only figure.

**The residual is RELOCATED, not absent, and is recorded as such.** A hostile or compromised keeper can pass a slack bound and let a searcher take the difference; it CANNOT steal, because the recipient is pinned to `FEE_ROUTING_HOOK` and the hook donates into der Bodensee. That fails PP-D41's bar of "capability absent or bounded by code that holds against a hostile holder" — and PP-D41 is NOT the governing test here, because it constrains rows exiting Accepted-risk and E.2 exits **Fix**. The keeper is a named trust assumption in PP-D31's sense, and §xxix's no-treasury clause means it must be funded externally rather than from protocol revenue. **DEFERRED, and this corrects the drafting conversation that preceded this lock:** the carve-out making the non-swap branches permissionless — the same-token donation and the ZCHF-to-svZCHF ERC-4626 deposit, neither of which any swap can sandwich — CANNOT land in PP4.6. L155 gates routing with no branch distinction, so exempting a branch is a spec change belonging to PP5.4 / PP-D23, not a fix rung. It is recorded as the first thing to revisit there.

**E.3's TWO TYPED-DOMAIN AMENDMENTS, user-adjudicated 2026-08-28 at the PP4.6s pre-flight and both written before the code they govern.** (vi) **The hard-reverting yield setters revert a NEW `YieldSkimIsImmutable()`, not `SplitIsImmutable`.** Clause (iv) said "exactly as the swap pair does" and named no error, and reuse is barred by that error's own NatSpec at `:178-181`, which reads "both the global setter and the per-pool override revert with this error" and cites D-D15, OQ-1 and OQ-1a — swap-split provenance throughout. Reusing it would make that sentence false, four setters standing where it claims two, and would assert a 50/50 split for a rate that is a skim rather than a split. The new error cites `10_constitution.md` §xxix L150—151 as its own provenance. (vii) **`withValidYieldFee` at `:144` is DELETED rather than left declared**, its only two call sites being the setters clause (iv) converts, and the swap side is the precedent in both halves: `setGlobalProtocolSwapFeePercentage` is a bare `external pure` revert carrying no modifier, and its pinned value is covered by `test_constructor_pinsGlobalSwapFeePercentageAtCap`, whose sibling E.3 gains. **The ripple was MEASURED at `eccb089` and is far smaller than PP-D44's:** zero production callers of either yield setter in `src/` or `script/`, which is E.3's own premise confirmed rather than assumed; one consumer of the global, `registerPool:623`; `aggregateYieldFeePercentage` and `GlobalProtocolYieldFee` in ONE fork file, `StagePIntegration.t.sol`; and ZERO yield-fee proofs anywhere in `test/formal/` or `formal/`, so unlike PP4.1 nothing there goes semantically false.

**TWO INCIDENTAL-COUPLING FINDINGS the same pre-flight surfaced, both RB-029's class, both reserved for this rung's register step so neither is lost.** FIRST, the Bodensee registration pin clause (iv) adds is protected only by a guard on a DIFFERENT function: `updateProtocolYieldFeePercentage` is permissionless and resyncs any non-override pool to the global, so it would write the skim straight back over the zero, and the only reason it cannot is that `withLatestFees:160` calls `collectAggregateFees`, which D-D9 reverts for `DER_BODENSEE_POOL`. Nothing states that dependency, so E.3 carries a regression pin asserting `updateProtocolYieldFeePercentage(DER_BODENSEE_POOL)` reverts `BodenseeYieldCollectionDisabled`. SECOND, `invariant_allWithdrawalsTargetFeeRoutingHook` was VACUOUS before PP4.6g: its handler calls `withdrawProtocolFees` as itself, one fixed contract address that the authorizer never granted, so every inner call reverted into the `try/catch` and `successfulWithdrawRecipients` stayed empty. Removing the gate is what made that invariant load-bearing, and no test recorded the transition.

**CLOSING RECORD at PP4.6z1, 2026-08-29 — PP4.6 is BUILT and F-57, F-58 and F-59 read `Fixed at PP4.6`.** Commits: the lock and its plan row (`e8a77e1`, `64aa366`); E.4's ledger debit and its ripple (`382b812`, `8f6fad2`, `0cbbe91`, `77672c6`); E.2's two ungated drains, the swap-leg forward and the overload they orphaned (`d1b0d29`, `e885173`, `24c81bf`); clause (v) end to end, the interface getter, the slot, the Safe-gated setter, the keeper-or-Safe gate and its unit coverage (`7c539af`, `145f89f`, `1f9e02e`, `a094831`, `b50b7cf`, `ea7cfed`, `125db39`); E.2's done-criteria in both prongs (`c557397`, `4f51a01`) and the unit repair behind it (`c1c7168`, `eccb089`); the two amendments (`d409be5`); E.3's source in five steps (`92743ee`, `6a7f5ce`, `ab96c91`, `7be6dd8`, `25e9888`); the unit ladder that followed it (`92d6745`, `e52a6f4`, `587c8e3`, `861a03e`, `e37a75a`, `503eeca`); E.3's done-criteria (`12983e5`); the fork route repaired for the new gate (`6e0d6f7`); the ledger (`de68176`, `56afe09`, `6118408`); and the plan (`1959128`, `19d582c`). ONE SEQUENCING DEVIATION, recorded rather than hidden: PP4.5 ran closing-record then Completion Log then §11, and this rung landed the log row first at `PP4.6z`, so the remaining records take `z1` and `z2`. The row's `PP4.6a—PP4.6z` range covers them as it covers `q1` and `t2a`.

**FIVE results not recoverable from the diffs.** FIRST, the Vault's 24-bit aggregate-fee store round-trips `10e16` EXACTLY, because `10e16 / FEE_SCALING_FACTOR` is `1e6` and fits, so pilot pools read the skim to the wei and the fork case asserts equality rather than a bound. SECOND, der Bodensee's zero comes from the ADDRESS pin and nothing else: it registers through `wpf.create` with `protocolFeeExempt` false, so `isOverride` is false too, and an unprotected zero is exactly what `updateProtocolYieldFeePercentage` would resync away — the only thing stopping it is D-D9 guarding `collectAggregateFees` beneath `withLatestFees`, a guard on a DIFFERENT function that nothing states, which is why the RB-029 regression pin exists. THIRD, `test_swapLegFeesReachTheHook`'s all-zero ledger assertion survived the skim going live, because no rate moves inside that test's own block; that was a flagged risk, not a certainty. FOURTH, the controller ends with ZERO `authenticate`-gated functions, verified by a modifier-position grep rather than a keyword count — all six surviving mentions are NatSpec explaining the absence, which is why the `IAuthentication` import fell out of BOTH test files. FIFTH, the fork roster is 27 of 33 files, and the six excluded are excluded for two distinct reasons: three stand up no Vault at all, two bind the live Balancer mainnet Vault whose fee controller is Balancer's rather than Aureum's, and one is Sepolia-scoped per PP-D59.

**What this rung DECLINED, and the first is a standing carry.** Three dead items stay: `withValidSwapFee` and `withValidPoolCreatorFee` in the contract, orphaned by D-D15 and by Pass 4 respectively, and `_mockUpdateAggregateSwapFeePercentage` in the unit fixture, orphaned by the same D-D15 conversion. All three are PRE-AUDIT dead code unrelated to any finding, and PP-D2 requires this branch's diff from `9ec513d` to be exactly the remediation, so sweeping them would put non-remediation noise into the tree the seam-1 re-run consumes. They are a post-gate hygiene item. The queue was likewise NOT edited, and that is a finding rather than an omission: its reproduction column is a deliberate historical record, F.1's and E.6's cells still naming their pre-inversion cases, and all three done-criteria cells already named the cases this rung built and turned green.

**One process cost, and its two halves share a shape: compute the set, never check the suspects.** The fork roster is the loud half. PP4.5's 13-file gate was built from `EmissionDistributor` references; deriving PP4.6's fresh from `AureumProtocolFeeController` gave 27, and `test/fork/AureumFeeRoutingHook.t.sol` — absent from PP4.5's list — came back 13 passed and 2 failed. Inheriting would have shipped it. The quiet half is the same error caught by the same cure: PP4.6u1 deleted a test and orphaned three fixture helpers, which went unnoticed because the rung checked the helpers it suspected; a later pre-flight enumerated EVERY `function _*` in the file and its use count in one pass, which found all three and a fourth that was pre-audit. Both halves also explain the rung's wrong predictions, which were all derivations standing in for measurements: a `git diff --stat` predicted by adding payload sizes, when git reports a MINIMAL EDIT SCRIPT and elides lines common to both sides, wrong by 6 at PP4.6t2a and by 17 at PP4.6v; a `GOVERNANCE_MULTISIG` count reasoned from the code lines of a payload whose COMMENT also named the constant; and a `^## ` baseline asserted as unchanged without ever having been measured. PP13 names this and PP-D47's BASE/OUT/IN table is the cure; it worked wherever it was applied, and every miss came from a token the table was not built for.

## PP-D49 — G.5a and G.5b (PP4.7, rung 7): the phantom proposal, the stale readmission ticket, and a deadline that adjudicates no new number

**PP-D49 (user-adjudicated 2026-08-29): rung 7 is G.5a and G.5b, both in `src/gauge/VaultClassRegistry.sol`, both `governance` unit, and it lands as FIVE changes.** (i) **G.5b:** `vetoProposal` and `finalizeProposal` each gain `if (proposalId >= nextProposalId) revert UnknownProposal(proposalId)` as their FIRST statement. The defect is not merely a missing guard: `AdmissionType.ImplementationAddress` is enum value ZERO, so the zero struct a phantom id reads is a VALID admission type, and `createdBlock` zero makes `block.number <= 0 + VETO_WINDOW_BLOCKS` false for every block past 201,600 — so the window check waves it through and `address(0)` is admitted as a real implementation class. `vetoProposal` has the identical hole and reverts today only incidentally, on `VetoWindowExpired`, which is why the guard goes on BOTH entries rather than the one the reproduction drives. (ii) **G.5a, the finalize re-check:** `finalizeProposal` reverts `ClassAlreadyAdmitted` when `admittedClasses[proposal.admissionValue]` is already true, closing the duplicate-admission case. (iii) **G.5a, the deadline:** `finalizeProposal` additionally requires `block.number <= proposal.createdBlock + 2 * VETO_WINDOW_BLOCKS`, one further window to finalize after the veto window closes. NO new constant is declared, which is the point: PP-D43's fifth obligation stops a rung that cannot cite canon for a number, and this cites G-D19's existing lock instead of adjudicating a fresh figure, exactly as PP-D48 declined `MAX_ROUTE_SLIPPAGE_BPS`. (iv) **G.5a, the revocation stamp:** `revokeVaultClass` records `lastRevokedBlock[admissionValue] = block.number`, and `finalizeProposal` reverts when `proposal.createdBlock <= lastRevokedBlock[proposal.admissionValue]`. (v) **G.5a, the stale type:** `revokeVaultClass` also clears `admissionTypes[admissionValue]`.

**CLAUSE (iv) SUPERSEDES THE QUEUE'S FIX INTENT, and the reason is a defect in that intent rather than a preference.** The row asks for "one live proposal per `admissionValue`", and neither that nor clause (ii)'s re-check closes the sequence the row is named for. Propose, then let the class be admitted, then let governance REVOKE it: at finalize `admittedClasses[value]` is FALSE, so a re-check passes and the stale proposal re-admits the class, bypassing the governance decision that removed it. A uniqueness map does not help either, because the surviving live proposal is precisely the ticket. Only something that invalidates proposals predating a revocation closes it, which is what the stamp does and why it is one mapping rather than proposal bookkeeping. It also leaves honest concurrent proposals legal, which the uniqueness shape would not.

**Three typed-domain settlements, and one existing attestation withdrawn.** The errors are `UnknownProposal(uint256 proposalId)` and `FinalizeDeadlineExpired(uint256 proposalId)`, both new and both carrying the id, matching the file's existing convention at `ProposalAlreadyFinalized` and `VetoWindowExpired`; clause (ii) reuses the declared `ClassAlreadyAdmitted(address)` and clause (iv) needs a third new error, `ProposalPredatesRevocation(uint256 proposalId)`, because reusing either window error would misreport the cause. `lastRevokedBlock` is a `mapping(address => uint256)` in STORAGE, appended after the existing mappings, and zero is its own sentinel: a value never revoked has `lastRevokedBlock` zero and `createdBlock` is always at least one, so the clause-(iv) comparison is false by construction rather than by a guard. **WITHDRAWN:** `revokeVaultClass`'s own NatSpec states that `admissionTypes` is "left stale until a later finalize" per G-D17, and clause (v) makes that false; the NatSpec is corrected in the same rung. Clearing is safe because `admissionType()` has ZERO production consumers — the sole reader of this registry anywhere in `src/` or `script/` is `GaugeEligibility.sol:417`, and it calls `isAdmittedClass`.

**CLOSING RECORD at PP4.7l, 2026-08-30 — PP4.7 is BUILT and F-46 and F-47 read `Fixed at PP4.7`.** Commits: the lock and its plan row (`1b721c5`, `6ebea64`); the declarations, three errors and the `lastRevokedBlock` mapping (`4a7afbd`); G.5b's guard on BOTH entries (`66fe378`); G.5a's finalize gates and revocation stamp (`837b275`); G.5a's type clearing (`74096cd`); the two PoCs inverted to regressions (`f44989b`, `1ee6dee`); the two ledger rows (`f7052c9`, `9023d07`). Gates: **Part-A 1082 passed, 0 failed, 1 skipped of 1083 across 90 suites**, the +4 over PP4.6's 1078 being the two inverted PoCs and nothing else, the suite count unmoved at 90 and the skip still RB-024's `DeploymentArtifactAgreement`; and **25 of 25 fork files green**, the roster computed FRESH for `VaultClassRegistry` per PP-D46 rather than inherited from PP4.6's 27. That roster is four edges, not one: direct references, the transitive `GaugeEligibility.sol:417` call to `isAdmittedClass`, the construction site at `DeployStageG.s.sol:152`, and the full fixture-inheritance closure. Eight files are excluded on stated grounds and a later reader should not re-hunt them — `AureumFeeRoutingHook`, `BroadcastNestingProbe`, `DeployAureumVault`, `DeployStageJ`, `DerBodenseeScriptWitness` and `Sanity` have zero reach; `PB52SepoliaOracleEvidence` is Sepolia-fork and its sole hit is a NatSpec line; and `StageJIntegration` binds `MockGaugeRegistry` from `CCBMocks`, so it never reaches the real eligibility path.

**FIVE results not recoverable from the diffs.** FIRST, **clause (v) is UNOBSERVABLE unless the admitted type is enum one**: `delete` on an `AdmissionType` yields zero, which IS `ImplementationAddress`, so a case that admits `ImplementationAddress` and then revokes cannot distinguish a cleared slot from a stale one. The reproduction got that discriminator for free from the duplicate overwrite that clause (ii) now blocks; the regression had to make its finalized proposal a `FactoryAddress` one to recover it. SECOND, **the stamp comparison is `<=`, so a proposal created in the SAME block as the revocation is invalidated too** — correct rather than an off-by-one, since it closes a front-run inside the revocation block, and it is why the positive control rolls one block before opening its fresh proposal. THIRD, the deadline is STRICT, so exactly `createdBlock + 2 * VETO_WINDOW_BLOCKS` is the last legal block and both sides of that boundary are pinned. FOURTH, **no assertion anywhere in the tree claimed `admissionTypes` survives a revocation**, so clause (v)'s withdrawal of the G-D17 NatSpec inverted nothing; this was checked before the gate rather than assumed, and the three `G-D17` hits under `test/fork/` are about `revokeGauge` on `GaugeRegistry`, a different contract. FIFTH, **the queue needed no rung**: PP4.1's F.1 precedent leaves the `reproduction PoC` cell naming the pre-inversion case as the historical record of what PP3.2 built, and both G.5 done-criteria cells already named cases that now exist, so the supersession is recorded in the ledger rather than by rewriting the queue's fix-intent column.

**One process cost, and it destroyed its own evidence.** In the 25-run fork roster `WK18ThinVenuePumpSim` reported two failures at position 25; run alone immediately afterwards, and again at position 5 of 5 in a fresh shell, it is 6 of 6. **The cause is NOT established and none is asserted here per PB22** — the failure text was destroyed by the loop's own `tail -1`, because forge prints its `Tip: run forge test --rerun` line AFTER the summary, so the single captured line was the tip while the `[FAIL]` rows and the tally scrolled past unread. The generalisation is the register candidate: a compact-summary loop must select what it keeps, filtering on the `FAIL` and `Ran N test suite` line forms, because a fixed tail silently converts a visible failure into an unreadable one and a zero-failure run into an indistinguishable success.

## PP-D50 — C.6 and E.5 (PP4.8, rung 8): the antagonism split by leg, the gauge-status bridge PB-D69 (ix) left open, and a delay that adjudicates no new number

**PP-D50 (user-adjudicated 2026-08-30): rung 8 is C.6 AND E.5 together, and the absorption is forced rather than convenient.** C.6's emission leg and E.5's remedy are ONE mechanism seen from two sides: `recordScore` already opens `if (!_gaugeRegistry.isGaugeApproved(pool)) revert NotApproved(pool)`, and E.5 exists BECAUSE that revert means a revoked pool's score can never be removed, so it keeps drawing while survivors are diluted by a frozen denominator. The queue's C.6 fix intent, a standing re-check in `recordScore`, would therefore have reproduced E.5's defect for a second predicate — the pool stops being re-scored, its last score stays in `totalScore`, and face 1 gets WORSE. Both rows are `emission-stack`, so it is one redeploy either way; rung 13 drops its E.5 item and F-39 and F-60 both close at PP4.8. (ii) **The antagonism resolves BY LEG, not by choosing**: the composition gate snapshots the rail conjunct, the emission leg acts on live state, and neither read is deleted. (iii) **Composition leg** — `Proposal` gains a fifteenth member, `bool railAdmittedAtPropose`, following `newAuthorizer`'s type-scoped precedent, written at `_createProposal` for `CompositionChallenge` and false for every other type; at execute the conjunct is satisfied from the snapshot rather than re-read. ONLY the conjunct is snapshotted — the hook check, the forbidden-token check, factory provenance and the 52% numerator STAY live, because re-checking those is the gate's purpose, and a later reader must not simplify this into snapshotting the whole gate. (iv) That moves `meetsCompositionQualityGate`'s signature, which is G16 scope: the `IGaugeRegistry` inheritor roster goes on the table BEFORE the build, not during it. (v) **Emission leg** — a new PERMISSIONLESS `revokeGaugeIfIneligible(address pool)` on `GaugeRegistry`, since `revokeGauge` at `:178` is `onlyGovernance` and is not reused; E.5's `deregisterScore(pool)`, permissionless and gated `!isGaugeApproved`, then drives `f5Score` and `poolScore` to zero through `_applySignedDelta`, reduce-only, with pre-revocation pending still claimable. (vi) The re-check EXCLUDES the TVL floor outright — a persistence window was considered and REJECTED as re-introducing the grief surface it was meant to close, and canon handles TVL failure by epoch tournament rather than ad-hoc revocation. (vii) A `recoveryPathAdmitted` revocation takes effect only after one full proposal lifecycle plus one block, 223,201, and **NO new constant is adjudicated**: `VOTING_DELAY_BLOCKS`, `VOTING_PERIOD_BLOCKS`, `EXECUTION_TIMELOCK_BLOCKS` and `EXECUTION_GRACE_BLOCKS` are already declared at `AureumGovernance.sol:28-31`, and PP-D43's fifth obligation is met by citing them exactly as PP-D49 (iii) cited G-D19. Those four are PROMOTED from `internal` to `public` and read by `GaugeEligibility` rather than re-derived there, because a second 223,201 in a second contract is precisely RB-026's found-once-never-propagated class; governance is already in this rung's scope for clause (iii). (viii) **Two-step rotation** with the INCOMING authority accepting — a handshake, not PP4.1's arming pattern. PB19 applies and the difference is named: `proposeIncendiaryRegistry` arms a slot, whereas this must stay REPEATABLE per PB-D69 (viii), since a burned authority leaves a dead map no rail-less pool could ever clear, so there is no one-shot, no burn, and a pending proposal stays replaceable. (ix) Rung 8 spans THREE redeploy units — `gauge`, `emission-stack` and `governance` — against PP-D40's one-unit-per-row vocabulary; that is RECORDED rather than relabelled, the queue's `emission-stack` naming the entry contract, and PP4.0's closure prices are re-read before the build so the redeploy cost is known going in.

**FOUR canon citations, read in the clone rather than recalled, and two of them are sharper than the argument that invoked them.** `10_constitution.md:49` says a composition challenge "deprecates the old pool (gauge revoked, emissions cease)" and `:53` says "Deprecation = gauge revoked only", the pool losing "AuMM emissions, CCB multiplier, and Miliarium Registry slot status" — so clause (v) implements a sentence canon already states rather than inventing a policy, and E.5 is a divergence from canon and not merely from taste. `:55` is the specified-pool model: the proposal "must reference the address of an already-deployed candidate pool" and "the 2/3 supermajority vote is binary on that specific pool", which is the SHARPER form of clause (iii)'s justification — a post-vote flag flip does not merely inconvenience the mandate, it annuls a vote canon defines as binary on a pool that has not changed. `:66` locks the two-way enforcement, programmatic checks for what the contract can verify plus semantic review by governance, which is the split clause (iii) preserves by keeping the pool-intrinsic checks live at execute while snapshotting the operator attestation. And **PB-D69 (ix) ANTICIPATED face 1 in terms**: `isGaugeEligible[pool]` is written `true` in `evaluateEligibility` and cleared nowhere in `src/`, a one-way latch, so flipping `recoveryPathAdmitted` to false "fails only a LATER `evaluateEligibility` or `meetsCompositionQualityGate`; an already-Active gauge keeps running until `GaugeChallenge` or `revokeGauge` takes it down". PP-D50 supplies the missing caller. It is the BRIDGE PB-D69 left open, not a reversal of it, and that is now verified from the clause rather than asserted.

**THREE consequences recorded here because they would otherwise surface later as findings.** FIRST, `10_constitution.md:58` states that the moment a gauge is revoked "by composition challenge, gauge challenge, 4-consecutive-disqualified-epoch automatic revocation, or any other path", that pool's AuMT "drops to zero governance weight at that block" — so `revokeGaugeIfIneligible` is ALSO a permissionless governance-weight-zeroing primitive. It is safe under its own predicate, which requires the admission authority to have revoked and then 223,201 blocks to elapse, but a reader meeting the entry cold would reasonably read it as alarming, so the predicate is the thing to state beside it. SECOND, that same line enumerates the 4-consecutive-disqualified-epoch automatic revocation as a live path when PP3.5 measured that it is NOT implemented; `revokeGaugeIfIneligible` is a different predicate and DOES NOT close that gap, and a later reader must not read rung 8 as having done so. THIRD, the `isGaugeEligible` latch stays one-way after this rung: `_checkEligibilityCriteria` is a reverting view and the latch is written by `evaluateEligibility`, so the re-check never touches it, and D.7's certificate-outlives-its-condition finding is untouched and still owned by its own row.

**TWO supersessions, both of this project's own prior text, and both recorded rather than silently overwritten.** PP-D26's emission clause is SUPERSEDED: its Decisions row at `docs/STAGE_P_PRIME_PLAN.md:255` still reads "standing re-check in `recordScore`", which is the defective shape this lock rejects, and that row is amended when this lock lands. The queue's C.6 fix-intent column is likewise superseded and is NOT rewritten — the F-39 ledger row carries the supersession, exactly as F-46 carried PP-D49 (iv)'s. TWO items of documentation debt are ACKNOWLEDGED and deliberately not done here: `docs/STAGE_O_NOTES.md`'s O-D2 execute-time rationale should eventually note the rail-conjunct carve-out, and `recoveryPathAdmitted`, the delay, `deregisterScore` and `revokeGaugeIfIneligible` are absent from published `aumm-site` prose, which is PP5.4's class under PP-D23 rather than a blocker on this rung.

**AMENDMENT (x), user-adjudicated 2026-08-30 at the PP4.8d pre-flight and written before the code it governs: `meetsCompositionQualityGate`'s SIGNATURE DOES NOT MOVE, and clause (iv) is REVISED accordingly.** Clause (iv) anticipated a G16 sweep to PERMIT a signature change; the roster the sweep produced says the correct answer is not to make one. A second parameter `bool railAdmittedSnapshot` on a gate that is `external` on BOTH `IGaugeRegistry` and `IGaugeEligibility` would let any caller pass `true` and take a conjunct-free pass — reopening precisely the back door PB-D69 (x) closed by putting the conjunct inside the gate, so the fix would introduce a hole of the same family as the finding it closes. That governance is the only consumer TODAY does not make it safe: the interface is the security boundary, not the current call set. Instead the conjunct is SPLIT OUT into a new view, `feeRailConjunctSatisfied(address pool)`, returning `rail != address(0) || recoveryPathAdmitted[pool]`. The name is deliberate and `railAdmitted` was REJECTED as misdescribing it: the view is true for any railed pool that was never admitted at all, so a reader grepping for admission would find a function that is mostly not about admission. `meetsCompositionQualityGate` drops the conjunct at `:455` ONLY — `:435` in `_checkEligibilityCriteria` STAYS, that being the activation path clause (iii) does not touch — and `AureumGovernance` reads the view live at `:243` into `Proposal.railAdmittedAtPropose`, then checks the stored bool at `:429` before calling the now conjunct-free gate. PB-D69 (x)'s closure survives because governance is the sole production consumer, VERIFIED rather than assumed: `GaugeRegistry.sol:245` is pure delegation and `AureumGovernance.sol:243` and `:429` are the only call sites in `src/`.

**The G16 roster is THREE KINDS, not one list, and the third kind is the one that bites.** FOUR test mocks are `is IGaugeRegistry` — `test/unit/VotingWeight.t.sol`, `test/unit/EmissionDistributor.t.sol`, `test/fork/mocks/CCBMocks.sol` and `test/unit/CCBMultiplier.t.sol` — so an addition to that interface breaks them at COMPILE time, loudly. ONE is `is IGaugeEligibility`, `MockGaugeEligibility` at `test/unit/GaugeRegistry.t.sol:19`, and behaves the same way. The third is `test/unit/AureumGovernance.t.sol:68`, which declares `meetsCompositionQualityGate` with NO `override` and implements no interface at all: the compiler will not force it to gain the new view, but `AureumGovernance` will CALL that view on it, so it fails at RUNTIME in a test whose subject is unrelated. That mock is named here because a G16 roster built only from `is I<Name>` would have missed it, which is the roster heuristic's own blind spot. One further consequence of leaving the signature alone is worth stating as a benefit rather than an accident: `test/fork/StageKIntegration.t.sol:204` mocks by the literal signature string `meetsCompositionQualityGate(address)`, and `vm.mockCall` does not error on a mismatch, it silently stops intercepting — so a signature change would have degraded that fixture without any failure pointing at the cause.

**ONE mechanism superseded and ONE test that inverts BEHAVIOURALLY rather than by selector.** PB-D69 (x) states that the conjunct reverts INSIDE `meetsCompositionQualityGate`; amendment (x) supersedes that MECHANISM while preserving the POLICY in full — a composition challenge still cannot seat a rail-less, unadmitted pool, the check simply moves to governance, live at propose and from the snapshot at execute. And `test/fork/StagePIntegration.t.sol`'s `test_P1_C6_admissionAuthorityAnnulsPassedCompositionChallenge` is a REPRODUCTION, so its inversion at PP4.8 is revert-to-SUCCESS and not a swap of revert selectors: after the fix beat 4 executes, the slot updates, and a passed two-thirds mandate on a candidate that qualified at propose is no longer voidable by a later flag flip. Its NatSpec at `:1250`, which pins beat 4 as the INNER `NoFeeRailAndNotAdmitted` rather than `CompositionQualityGateFailed`, becomes false and is rewritten rather than re-expected. The StageN ixAetheron pin at `:271`, `:273` and `:284` drives the composition gate and moves with `:455`; `test/fork/StageGIntegration.t.sol:1236` and `:1256` are ACTIVATION sites and are unchanged; the `GaugeEligibility.t` PB-D69 block is sorted into composition and activation cases at build time rather than assumed here.

**AMENDMENT (xi), user-adjudicated 2026-08-30 at the PP4.8g2 pre-flight and written before the code it governs: propose REGAINS the fail-fast enforcement that (x) removed from the gate, and the two sites take DIFFERENT errors.** Amendment (x) moved the conjunct out of `meetsCompositionQualityGate`, which left `proposeCompositionChallenge` with no conjunct enforcement at all — a rail-less unadmitted pool clearing the 52% bar, the hook check and factory provenance would be accepted, its 1,000 svZCHF deposit donated irreversibly, a full vote run, and only `_executeProposal` would fail. That is the back door PB-D69 (x) closed, reopened by the fix, and O-D2's fail-fast-at-propose rule exists precisely against it. So (a) `proposeCompositionChallenge` reads `GAUGE_REGISTRY.feeRailConjunctSatisfied(newPool_)` into a local immediately after the existing gate check, reverts when false, and passes the local into `_createProposal`. (b) The propose-time error is a NEW `CompositionRailConjunctFailed(address pool)` declared on `AureumGovernance`: the contract carries sixteen errors and none fits, and it holds ZERO references to `GaugeEligibility`, knowing only `IGaugeRegistry`, so throwing `NoFeeRailAndNotAdmitted` would import a concrete contract it deliberately does not know AND would borrow an error whose NatSpec describes activation-path semantics. Reusing the sibling `CompositionQualityGateFailed` was REJECTED for the reason PP-D49 declared three new errors rather than reusing window errors: the quality gate passed, so naming it would misreport the cause.

**Clause (c): the execute site takes a DIFFERENT error, `RailConjunctSnapshotFalse(uint256 proposalId)`, and the reason is that the two sites do not check the same thing.** Propose evaluates a LIVE predicate on a pool and a failure means the pool does not qualify now; execute reads a STORED bool and, because propose enforces, a false there means a proposal exists that should have been impossible to create. Those differ in kind rather than in timing, and collapsing them would be actively misleading, since a pool can satisfy the conjunct live — a rail added, or `recoveryPathAdmitted` set — while the snapshot stays false. The check goes BEFORE the conjunct-free `meetsCompositionQualityGate` call in the `CompositionChallenge` branch, so an impossible proposal is diagnosed before the pool is re-qualified. ONE SUB-QUESTION was resolved by reading rather than assumed: `_executeProposal(Proposal storage p)` has NO `proposalId` in scope, so it gains one as a first parameter and its single call site passes the value `execute(uint256 proposalId)` already holds. Keying the error on the pool instead was REJECTED as re-introducing exactly the ambiguity the distinct error exists to remove. The error is UNREACHABLE in a correct deployment and that is the point rather than an objection — if it fires, the revert must read as an invariant violation and not as a routine rejection — so its regression drives `_createProposal` with `false` for a composition type through a harness rather than through the public entry. The five non-composition call sites pass `false`, and only the `CompositionChallenge` execute branch ever reads the field.

**AMENDMENT (xii), user-adjudicated 2026-08-30 at the PP4.8h3 pre-flight and written before the code it governs: clause (vii)'s revocation delay is a SCHEDULED TWO-STEP with a PERMISSIONLESS finalize, and `recoveryPathAdmitted` keeps its shape as a public mapping.** FOUR actions define it. `setRecoveryPathAdmitted(pool, true)` writes the mapping true AND clears any pending stamp, so re-admission cancels a scheduled revocation. `setRecoveryPathAdmitted(pool, false)` does NOT write the mapping at all: it stamps `revocationEffectiveBlock[pool] = block.number + REVOCATION_DELAY_BLOCKS`, and REVERTS `RecoveryPathNotAdmitted(pool)` when the pool is not admitted, because a stamp on an unadmitted pool would leave the finalize step writing false over false and emitting a revocation for something never admitted. Re-stamping while a schedule is already pending is LEGAL and is stated here rather than left implicit: it can only move the effective block later, cancellation is the `true` call, and the caller is the same authority either way. `finalizeRecoveryPathRevocation(pool)` is PERMISSIONLESS, requires a non-zero stamp and `block.number >= revocationEffectiveBlock[pool]`, then writes the mapping false, clears the stamp and emits. And the READERS do not change: `_checkEligibilityCriteria` and `feeRailConjunctSatisfied` keep reading `recoveryPathAdmitted` alone, so no delay logic enters a view. `REVOCATION_DELAY_BLOCKS` is the sum of the four `AureumGovernance` constants promoted to `public` at PP4.8h2 and is READ rather than re-derived, per RB-026.

**TWO shapes were REJECTED and clause (viii) lands in the same amendment.** The LAZY alternative — a view returning admitted-unless-a-matured-revocation-exists — is rejected because it moves both consumers off the public mapping AND re-opens the C.6 annulment class: during the delay window the mapping must stay TRUE so a composition mandate snapshotted at propose is still honourable at execute, which is clause (iii)'s entire purpose. An AUTHORITY-ONLY finalize is rejected because scheduling with no public execution path lets a revocation be recorded and then never executed, leaving the pool's status indefinitely ambiguous — structurally the same failure mode as revoking after a vote passes but before it executes, which is what this rung exists to close. **Clause (viii)** splits `setAdmissionAuthority` into `proposeAdmissionAuthority(newAuthority)`, written by the current authority into `pendingAdmissionAuthority`, and `acceptAdmissionAuthority()`, callable only by that pending address, which must be non-zero, and which writes `admissionAuthority` and clears the pending slot. It stays REPEATABLE with no one-shot and no burn per PB-D69 (viii), since a burned authority leaves a dead map no rail-less pool could ever clear, and its storage is separate from the revocation schedule so the two surfaces cannot interfere.

**AMENDMENT (xiii), user-adjudicated 2026-08-30 at the PP4.8h3h pre-flight: clause (vii)'s compile-time read through the `AureumGovernance` TYPE is INVALID SOLIDITY and is WITHDRAWN.** `public constant` on a CONTRACT produces instance getters at runtime; it does not make the member reachable as `AureumGovernance.VOTING_DELAY_BLOCKS` inside a constant expression, and solc rejects it with error 9582, member not found in `type(contract AureumGovernance)`. Only LIBRARY members work that way, which is why `AureumGovernance.sol:28-31` itself reads `AureumTime.BLOCKS_PER_DAY`. The compiler caught it at PP4.8h3g's build gate, THREE RUNGS after the lock had already rested on it — a G10 miss, since an access pattern is a language-feature claim and G10 exists to say those are compile-probed before a closure lands on them. The replacement: `GaugeEligibility.REVOCATION_DELAY_BLOCKS` becomes the SAME `AureumTime` expansion governance uses for its own four lifecycle constants, plus one block — `BLOCKS_PER_DAY + BLOCKS_PER_EPOCH + 2 * BLOCKS_PER_DAY + BLOCKS_PER_EPOCH + 1`, still 223,201 at canonical figures — and the `AureumGovernance` import is DROPPED for an `AureumTime` one, which incidentally restores the interfaces-and-libraries-only import property this contract held before PP4.8h3a1 and which was flagged as a break when that rung landed.

**RB-026 is discharged by a PIN, and the pin's FORM is load-bearing rather than incidental.** The test asserts that `GaugeEligibility.REVOCATION_DELAY_BLOCKS()` equals the sum of `AureumGovernance`'s four PUBLIC GETTERS plus one. It must NOT re-expand `AureumTime` test-side: a mirror written that way passes unchanged if someone rewrites `EXECUTION_TIMELOCK_BLOCKS` as `3 * BLOCKS_PER_DAY` in governance while the gauge formula sits still, which is precisely the drift RB-026 names and the only failure this pin exists to catch. **PP4.8h2's promotion therefore STAYS and is NOT reverted**: those getters are what the pin reads, so promoting the four served the alignment after all, at runtime rather than at compile time, and returning them to `internal` would force the weaker mirror. h2 is recorded here as vestigial for the purpose it was built for and load-bearing for this one.

**CLOSING RECORD at PP4.8N, 2026-09-01 — PP4.8 is BUILT, F-39 and F-60 read `Fixed at PP4.8`, and rung 8 ran to SIXTY commits from `140aec5` to `9f77ce3`.** The lock and its two plan rows (`140aec5`, `c9c3275`, `3b36917`); amendment (x) and the conjunct split into `feeRailConjunctSatisfied` across both interfaces, both implementations and the five mocks (`91315dd` through `11e84df`); PP14 and its index line (`9a59f3e`, `93aeafa`); the composition leg — `railAdmittedAtPropose` threaded through `_createProposal`, the two (xi) errors, the five non-composition sites threading false, propose's fail-fast and execute's snapshot honour (`231924c` through `b1e0155`); E.5's `deregisterScore` (`59b0997`, `064130f`, `bd7dbae`); the four lifecycle constants promoted to public (`2474516`); amendment (xii)'s scheduled revocation and rotation handshake, thirteen rungs from the slots to the inverted dead-end PoC (`e7f37e8` through `996dc8b`); amendment (xiii)'s `AureumTime` mirror and the RB-026 pin (`9710a7e`, `70bea35`, `a2d9e33`, `bf270aa`, `77bf09a`); the emission leg's `GaugeStillEligible` and `revokeGaugeIfIneligible` (`b97591f`, `7c65efd`); the four fork PoCs inverted (`1d0c680` through `df6b6e1`); the two conjunct overrides the propose-time guard turned out to require (`8857ee1`, `8970bf7`); `deregisterScore` proven (`7082341`); and the two ledger rows (`be25ee0`, `9f77ce3`). Gates: **Part-A 1083 passed, 0 failed, 1 skipped of 1084 across 90 suites**, the skip being RB-024's deferral as throughout this stage, and **32 of 32 fork files green** on a roster computed FRESH per PP-D46 across the three redeploy units, `PB52SepoliaOracleEvidence` excluded on chain grounds as the tree's only Sepolia-fork test.

**FOUR results not recoverable from the diffs.** FIRST, **the conjunct deletion had to be LINE-anchored and a content anchor would have been ambiguous**: before the edit `meetsCompositionQualityGate` and `_checkEligibilityCriteria` opened with EIGHT byte-identical lines, and the edit itself cut that run to two. Anchoring on the shared text could have matched either, and matching the wrong one would have deleted ACTIVATION's copy — the read clause (ii) requires to stay live — leaving the composition gate enforcing a conjunct it was supposed to stop enforcing and emissions enforcing nothing. The two are not near-duplicates by accident: PB3.12 rung c2 landed the same conjunct at both sites under PB-D69, so the hazard is a direct consequence of the fix this rung amends. SECOND, **`revokeGaugeIfIneligible` is deliberately CONCRETE-ONLY and the file has a precedent for it**: it and `advanceTournament` are the only two public functions `GaugeRegistry` declares that `IGaugeRegistry` does not, and `advanceTournament` is the other permissionless keeper entry, so the new trigger joins an existing convention rather than inventing one. Putting it on the interface would oblige all five `is IGaugeRegistry` implementers for no consumer, `AureumGovernance` knowing only the interface and never calling it. It emits the EXISTING `GaugeRevoked` — two emit sites now — so nothing downstream has to watch a second event to see a demotion. THIRD, **the propose-time guard's real blast radius was every fixture that MOCKS the composition gate, and only running the gates could find it**: `test/whitehat/P1_B1.t.sol` and `test/fork/StageKIntegration.t.sol` each mock `meetsCompositionQualityGate` and went red because the guard reads a SECOND view those mocks did not answer for, which no grep over the changed contract would have surfaced. Twelve test files now declare `feeRailConjunctSatisfied`. FOURTH, **D.7 is untouched and stays its own row.** No PP-D50 clause names the eligibility certificate and the scheduled revocation does not reach it: `isGaugeEligible` still has one write site writing `true` and no branch anywhere writing `false`, so a reader must not take C.6's closure as having closed the latch.

**FIVE process costs, and the last of them is one rule failing in three costumes.** FIRST, **the G10 miss cost three rungs**: clause (vii) specified `REVOCATION_DELAY_BLOCKS` as a compile-time read of `AureumGovernance`'s promoted constants, h3a1 built exactly that, and solc rejected it with error 9582 — `public constant` on a CONTRACT produces an instance getter and does NOT make the member reachable through the type in a constant expression, which is a property libraries have and contracts do not. The analogy to the `AureumTime.BLOCKS_PER_DAY` read three lines above felt safe and was not; amendment (xiii) withdrew the clause and h3h landed the `AureumTime` mirror instead. SECOND, **a §8e.1 carrying two payloads silently produced NOTHING, again**: h3a3 asked for two declaration blocks in one rung, twenty-two lines, and returned no error, no partial write and no file change; split into h3a3a and h3a3b it landed first try each. That is PP4.0 (4) recurring, and the verify block remains the only thing that separates it from success. THIRD, **blank separators were stripped twice**, at h3d and j1, which is PB4.3's known Cursor behaviour; both were repaired by blank-only insert rungs verified with `cat -e`, BSD `cat` having no `-A`. FOURTH, **a rung ends at its COMMIT, not at its verify**: h3i2 was verified green, the test gate was run instead of the commit, failures diverted attention, and the uncommitted save was found two rungs later by a `git diff --stat` that disagreed with HEAD — it landed as `bf270aa` naming both rungs. FIFTH, **PP13's rule failed in THREE costumes across the closing beats**, every save correct and every expectation wrong, each costing one reconciliation round: a payload backtick count asserted about a whole line that also had 34 in its untouched head; an em-dash occurrence count asserted through `grep -c`, which counts LINES; and a file-wide header count written with no pre-edit baseline because the string looked new, when PP-D45's record had opened with the same words since rung 2. The instrument for all three is identical and already written down — measure the FILE in the same command that measures the payload, and use `grep -o` piped to `wc -l` whenever the claim is about occurrences rather than lines.
