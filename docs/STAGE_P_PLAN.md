# Stage P — Plan & Sub-Step Roadmap

> **Status:** Stage P OPEN — P0.1 design freeze committed (`6aebebe`, P-D0—P-D11 + four amendments); P0.2 (this plan) in flight. Two workstreams: **(A)** the white-hat back-fill sweep (opens the stage), **(B)** full-system deployment + dual integration validation. Companion to STAGE_P_NOTES.md (the design-decision log); this file is the sub-step sequence + the WH contract×surface matrix + the Completion Log + grep-verifiable checkpoints.
>
> **Last update:** 2026-07-01 — P0 complete (freeze `6aebebe` + plan `6f913ac` + testnet reconciliation `ff32941`/`4696112`); P1 WH-D fee-routing back-fill audited (S2/S5/S6 — F-13 S5 Low Accepted-risk logged; S2/S6 NO FINDING). Branch `stage-p` @ `4696112` (cut from `main` @ f50b4cd = `stage-o-complete`, P-D4). Regression baseline carried from Stage O: 872 unit + 124 fork green. Next: P2 (WH-G pass).
>
> **Mode:** Opus high entry — the white-hat sweep (audit reasoning) + the O-D4 / P-D8 `activateBoost` removal (I13, tagged contract) are §13 Opus-high beats. The §13 table's "P/Q/R extra-high" is the formal-verification character of Stage Q's Act proofs + Stage R; Stage P's optional hevm shakeout (P7) is not that beat. Drops to Sonnet for: PoC-test transcription once a finding's attack path is characterised, stub-template transcription (P8), Completion-Log housekeeping, and the P0.3 doc strike (O-D3-class).
>
> **Audience:** Sagix + any future Claude session resuming Stage P execution. NOTES (STAGE_P_NOTES.md) is the design-rationale source-of-truth; this file is the execution spine + the audit matrix.

---
## Stage scope

Stage P is the first stage where the whole stack runs as one system — the integration boundary every prior stage built toward, and the pre-audit hardening pass before Stage Q's external audit + code freeze. Two workstreams. **(A) White-hat back-fill sweep** (opens the stage, P-D5 / P-D6): the per-stage audit cadence began at the Stage K close, so Stages D / G / H / I / J / L were never audited in isolation — Stage P runs those back-fill passes (WH-D…WH-L), then the four named cross-seams a per-stage pass cannot see, then the INV-2 invariant harness and the O-D4 / P-D8 `activateBoost` removal, then an optional hevm symbolic shakeout. **(B) Full-system deployment** (P-D1 / P-D2 / P-D3): tiered testnet token stubs, network-parameterised deploy scripts (the live testnet target — Sepolia or Hoodi — is a P-bis decision per P-D1), the Balancer V3 substrate redeploy, and dual integration validation — Tenderly mainnet-fork e2e (real literals) is the primary `stage-p-complete` gate; the live testnet broadcast + the `aumm-app` frontend wiring is the deferred P-bis sub-phase. All sweep fixes land fix-forward on `stage-p`; no tagged stage is re-tagged (P-D5, I13-class).

### Surfaces produced at Stage P
| Artifact | Workstream | Sub-step | Purpose |
| --- | --- | --- | --- |
| `docs/FINDINGS.md` + `docs/STAGES_OVERVIEW.md` (testnet) | B | P0.3 | P-D1b Holesky→network-agnostic strike (O-D3-class) |
| `test/whitehat/F-NN_*.t.sol` PoCs | A | P1—P6 | per-finding Foundry PoCs (mirror F-01…F-12) |
| `docs/white_hat/AUREUM_WHITEHAT_OUTPUT.md` rows + roll-up | A | P1—P7 | ledger findings + per-surface coverage attestation |
| `test/invariant/*.t.sol` + `ReferenceEmission.sol` | A | P6 | INV-1/6 + INV-2 (non-circular) + INV-3 (+ INV-4 opt) harness |
| `src/ccb/CCBMultiplier.sol` (edit) | A | P6 | O-D4 / P-D8 `activateBoost` + boost-machinery removal (I13) |
| `test-stubs/*.sol` (Tier A—D) | B | P8 | testnet token stubs + `TestnetTokenRegistry` |
| `script/DeployStageP.s.sol` (+ granular P9 scripts) | B | P9 | full-stack thin orchestrator (P-D25); no Balancer periphery deploy (P-D26 / P-D27) |
| `test/fork/StagePIntegration.t.sol` | B | P10 | Tenderly mainnet-fork e2e (real literals) |
| hevm artifacts (optional) | A | P7 | S5 / S7 / S8-relabeled symbolic shakeout |
| `STAGE_P_PLAN.md` / `CLAUDE.md` §11 / `STAGES_OVERVIEW.md` / ledger | — | P11 | Stage P close + tag `stage-p-complete` |

### Surfaces NOT produced at Stage P
- No live-network broadcast — a live testnet or mainnet `--broadcast` is the deferred P-bis sub-phase (§8b / §8c, P-D3).
- No `aumm-app` frontend wiring — external repo, OQ-18, P-bis.
- No re-audit of the K / L / M / N / O fixes (F-01…F-12 all Fixed) — Stage P back-fills the never-audited pre-K stages only, unless a fix-forward diff reopens a surface (P-D11).
- No Balancer periphery deploy — the canonical Router redeploy is P-bis (P-D26, the deliberately-excluded permit2 dependency); VaultExplorer is not deployed at all (P-D27 — the Vault itself serves the two consumed selectors). No Router source fork either way (D33 / O-D5).
- No pool restoration — 04 ixViatica / 07 ixCambio / the ixCasper waEthwstETH leg stay deferred on the E-D17 bar (O-D6).
- No `aumm-site` canonical-spec edit — the spec names no testnet; the Holesky→network-agnostic strike is repo-side only (P-D1b).
- No Act spec / formal proofs — Stage Q owns the formal-verification budget; hevm (P7) is kept separate (P-D9).

---
## Decisions

Mirror of `STAGE_P_NOTES.md` (P-D0—P-D11, LOCKED at P0.1 from the Stage-P pre-flight Opus beats + the user design-review; freeze committed `6aebebe`). The NOTES file holds the rationale; this table is the roadmap index.

| # | Decision (one-line) | Sub-step |
| --- | --- | --- |
| P-D0 | Two workstreams (A sweep opens, B deploy); gated P-bis; sweep-before-deploy | P0—P11 |
| P-D0b | Tenderly mainnet-fork e2e (real literals) = primary close gate; live testnet + stubs = P-bis | P10 |
| P-D1 | Testnet posture network-agnostic; live target (Sepolia/Hoodi) deferred to P-bis; supersedes OQ-16 | P0.3 / P9 |
| P-D1b | `FINDINGS.md` + `STAGES_OVERVIEW.md` Holesky→network-agnostic strike (O-D3-class) | P0.3 |
| P-D2 | Tiered stubs (A anchors / B ERC4626 / C ERC20 / D wiring); AuMM + RPs deploy real | P8 |
| P-D3 | Broadcast-gated — scripts + Tenderly fork only; no `--broadcast` in Stage P | P9 / P10 |
| P-D4 | Fresh `stage-p` off `main` @ f50b4cd; two-commit PENDING_P close | P0 / P11 |
| P-D5 | WH methodology = process doc; scope = back-fill + cross-seam; fix-forward (I13) | P1—P7 |
| P-D6 | Order WH-D→G→H→I→J→L→cross-seam→INV-2→O-D4 fix→hevm; 4 named seams | P1—P7 |
| P-D7 | INV-1/6 + INV-2 + INV-3 (+ INV-4 opt); drop gov-weight monotonicity | P6 |
| P-D8 | `activateBoost` deprecate-and-remove; impl after WH-H clears CCBMultiplier; I13 | P6 |
| P-D9 | hevm S5 / S7 / S8-relabeled; install §8b at P7; optional, not a close gate | P7 |
| P-D10 | WH PoCs + invariant harness + Tenderly e2e + split-form regression (D35 / D36 / F14) | P1—P10 |
| P-D11 | Back-fill = pre-K stages (D / G / H / I / J / L), not re-audit of K—O | P1—P7 |

---
## White-hat contract × surface matrix (workstream A)

The operational spine of the sweep — one row = one §8e.1 step (process-doc §1: one surface × one contract). Surfaces are the S0—S12 catalog (process-doc §7) filtered to what each contract exposes; skipped surfaces are attested explicitly, never silently omitted (§3). Severity-ordered per §4 (`*.1` is S1 supply / mint wherever a contract can mint). Each step ends with the §9 attestation line; each finding becomes a ledger row + a `test/whitehat/` PoC.

| Step | Surface | Contract(s) | Lens focus |
| --- | --- | --- | --- |
| WH-D.1 | S2 fee conservation | `AureumProtocolFeeController` | 50/50 split immutable; no third destination; creator-fee functions revert; no post-deploy destination setter |
| WH-D.2 | S5 hook hot path | `AureumFeeRoutingHook` | reentrancy via `Vault.unlock` + nested svZCHF swap + one-sided Bodensee add; recursion-guard bypass; spoofed router |
| WH-D.3 | S6 reentrancy | `AureumFeeRoutingHook` | cross-function / read-only reentrancy on the callback path |
| WH-G.1 | S8 access | `GaugeRegistry` | `onlyGovernance` gates; one-shot setters; the `registerGaugeFromComposition` path |
| WH-G.2 | S9 gauge-gaming | `GaugeEligibility` | 52% floor + the $10k-TVL snapshot; wash-liquidity to trip the gauge floor |
| WH-G.3 | S4 4626 rate | `GaugeEligibility` | the O-D2 / F-12 provenance gate (re-verify post fix-forward, P-D11); admitted-class rate reads |
| WH-G.4 | S8 access | `VaultClassRegistry` | F-08 veto-dedup regression; `proposeVaultClass` / `vetoProposal` bond + threshold |
| WH-G.5 | S5 / S6 | `SwapAndDepositToBodensee` | swap-and-deposit reentrancy; rate-read freshness on the deposit leg |
| WH-H.1 | S1 supply / mint | `EmissionDistributor` + `BodenseeBootstrapChannel` + `AuMMMinterRouter` | cap-budget decrement on every mint path; halving-era boundary; no path mints without charging the cap |
| WH-H.2 | S7 TVL-EMA | `EMASampler` + `TVLOracle` + `EfficiencyOracle` | timed / flash deposits skewing the 60-day EMA; observation cadence; the F-05 / F-07 / F-10 EMA-gate lineage |
| WH-H.3 | S3 rounding | `CCBMultiplier` + `CCBScore` + `CCBShare` | rounding direction favours the protocol; the int / uint signed-delta clamp discipline; div-before-mul |
| WH-I.1 | S9 attribution | `EmissionDistributor` recorder + `AureumFeeRoutingHook` dispatch | recorder-clock attribution; the F-09 router-spoof seat; `effectiveQualBlock` top-up / reset |
| WH-I.2 | S8 recorder gate | `EmissionDistributor` + `VotingWeight` | recorder one-shot binding; gauged-only qualification; the `setGovernanceContract` gate |
| WH-J.1 | S8 access | `MiliariumRegistry` | slot-mutation `onlyGovernance`; the `replaceSlot` / `seedFoundingPool` gates |
| WH-J.2 | S9 roster | `MiliariumRegistry` + `TVLOracle` | roster gaming; cross-seam (d) slot-dedup in the TVL aggregate |
| WH-L.1 | S9 economic | `IncendiaryRegistry` | boost-buy gaming; the 5 gates + 5% haircut + 15%-of-epoch cap; FCFS walk-forward |
| WH-L.2 | S4 / S1 | `IncendiaryRegistry` + `EmissionDistributor` boost leg | 60-day-EMA pricing freshness (F-07); cross-seam (a) boost-mint conservation (L-D25) |

Cross-seam steps (P6.1—P6.4) and the INV harness (P6.5) draw their assertions from this matrix; the `AureumVaultFactory` S11 fork-diff + the S10 deployment surfaces are exercised in P9—P10 (workstream B) and rolled up there.

---
## Sub-step roadmap (P0—P11)

> Execution spine. Workstream A (the white-hat sweep) opens the stage and runs P1—P7 in back-fill order (WH-D→G→H→I→J→L per P-D6), then the cross-seam synthesis + invariant harness + O-D4 fix (P6), then the optional hevm shakeout (P7, fixed there per P-D9). Workstream B (deployment) is P9—P10; P11 closes on the real-token gate — P8 (testnet token stubs) is DEFERRED to P-bis per P-D23, off the `stage-p-complete` critical path. Sweep-before-deploy is deliberate (P-D0): the deploy scripts wire contracts the sweep may patch fix-forward. Each WH pass is a sequence of §8e.1 steps drawn from the matrix above — one surface × one contract (process-doc §1), severity-ordered (§4), each ending in a §9 attestation. Every numbered sub-step is one §8e.1 per §8e.

### P0 — Stage entry: freeze + plan + testnet-doc reconciliation
- **P0.1** — `docs/STAGE_P_NOTES.md` design freeze (P-D0—P-D11 + four amendments). ✅ `6aebebe`.
- **P0.2** — `docs/STAGE_P_PLAN.md` — P0.2a reference half (scope + decisions + WH matrix) + P0.2b roadmap + Completion Log. This sub-step.
- **P0.3** — testnet reconciliation (network-agnostic, the P-D1 amendment): P0.3a `STAGE_P_NOTES.md` P-D1 amend + P0.3b this file's testnet-site rewrite (Opus — design + multi-site) → P0.3c `docs/FINDINGS.md` OQ-15 / OQ-16 + P0.3d `docs/STAGES_OVERVIEW.md` Stage P heading + body (Sonnet — Holesky→network-agnostic strike; the live testnet target deferred to P-bis). O-D3-class repo-side docs fix; no `aumm-site` edit.
- Checkpoint: `grep -n Holesky` returns zero residual hits outside the explicit supersession context; the testnet-name grep shows only the two network-agnostic candidate-pair mentions (no single-testnet commitment phrasing survives); the network-agnostic + P-bis-deferral rationale present.

### P1 — WH-D pass (Stage D fee-routing surface, P-D6)
Execute the WH-D.* matrix rows (S2 / S5 / S6) over `AureumFeeRoutingHook` + `AureumProtocolFeeController`. `*.1` is S2 fee conservation (the crown-jewel fee surface — no mint path here). Fix-forward on `stage-p`, no Stage-D re-tag (P-D5).
- Checkpoint: per-step §9 attestation + coverage roll-up row; any finding → `test/whitehat/F-NN_*.t.sol` PoC + ledger row.

### P2 — WH-G pass (Stage G gauge surface, P-D6)
WH-G.* rows (S8 / S9 / S4 / S5—S6) over `GaugeRegistry`, `GaugeEligibility`, `VaultClassRegistry`, `SwapAndDepositToBodensee`. WH-G.3 re-verifies the O-D2 / F-12 provenance gate on the fix-forward diff — the P-D11 reopened-surface case (a Stage-O change to a Stage-G contract).
- Checkpoint: as P1; explicit attestation that the F-12 fix holds.

### P3 — WH-H pass (Stage H emission + Stage-F CCB engine, P-D6)
WH-H.* rows (S1 / S7 / S3). `*.1` is S1 supply / mint — the highest-value miss (the Orchard analog, process-doc §4), spanning `EmissionDistributor` + `BodenseeBootstrapChannel` + `AuMMMinterRouter`. The CCB engine (`EMASampler` … `CCBShare`) carries S7 / S3.
- Checkpoint: as P1; the S1 attestation pasted with the cited mint lines.

### P4 — WH-I pass (AuMT governance spine, P-D6)
WH-I.* rows (S9 attribution + S8 recorder gate). Per I-D14 there is no `AuMT.sol`; the spine is the `EmissionDistributor` recorder + `AureumFeeRoutingHook` dispatch + `VotingWeight`. Not a re-audit of the host contracts' S1 / S2 (covered by P3 / P1, P-D11) — the lens is attribution integrity only.
- Checkpoint: as P1; confirm the F-09 `trustedRouter` seat holds under WH-I.1.

### P5 — WH-J + WH-L passes (P-D6)
- **P5.1** — WH-J (`MiliariumRegistry`): S8 slot-mutation access + S9 roster gaming (WH-J.1 / WH-J.2).
- **P5.2** — WH-L (`IncendiaryRegistry` + the `EmissionDistributor` boost leg, L-D25): S9 economic + S4 / S1 (WH-L.1 / WH-L.2). The formal WL pass — the SPOT-only F-07 review is superseded (P-D6).
- Checkpoint: as P1, per sub-pass.

### P6 — Cross-seam synthesis + invariant harness + O-D4 fix
- **P6.1—P6.4** — the four named cross-seams (process-doc §2; the matrix tail): (a) channel ↔ distributor mint split, (b) hook ↔ recorder attribution, (c) governance ↔ registry `onlyGovernance`, (d) roster ↔ TVLOracle dedup. One §8e.1 per seam.
- **P6.5** — the INV-2 invariant harness (`test/invariant/` + an independent `ReferenceEmission.sol`): INV-1 / INV-6 (≤ 21M across all mint paths), INV-2 (non-circular emission conservation), INV-3 (50/50 fee split), INV-4 (optional vault solvency). Lands after WH-H (P3) + the cross-seams characterise the mint split (P-D7).
- **P6.6** — O-D4 / P-D8: remove `CCBMultiplier.activateBoost` + the boost machinery (`BOOST_FACTOR`, `GAUGE_BOOST_DURATION_BLOCKS`, `boostExpiryBlock`, `BoostAlreadyActive`) + the now-dead `boostExpiryBlock` read branches on the multiplier hot path. Presumed-deprecate per O-D3 unless WH-H surfaced a re-wire requirement. I13 blast-radius sized at this sub-step's pre-flight; fix-forward, no Stage-F re-tag.
- Checkpoint: invariant gate `runs >= 5000` green (process-doc §10); `forge clean && forge build` (F14) after P6.5 + P6.6; split-form regression (D35 / D36) green.

### P7 — hevm symbolic-execution pass (optional, P-D9)
§8b decision: install hevm (Argot Collective) — presented + approved at this sub-step, not pre-committed at the freeze. Targets S5 (the OQ-1 hook), S7 (the CCB / EMA engine), S8-relabeled (the `AureumGovernanceAuthorizer` timelock / emergency window + the `Vault.setAuthorizer` one-shot migration). Optional pre-audit shakeout, NOT a `stage-p-complete` gate; Act stays Stage Q (P-D9). If install is blocked, attest "hevm deferred to Q" and proceed.
- Checkpoint: hevm proof output (or the deferral attestation); no gate on the close.

### P8 — Testnet token stubs (P-D2, workstream B) — **DEFERRED TO P-BIS (P-D23, 2026-07-06)**
Off the `stage-p-complete` critical path — see P-D23. The tier spec below is retained as the P-bis build reference; NAV / fidelity locked in P-D23 (Tier A: fixed 1:1 ERC-4626 NAV; ixEDEL: plain deterministic ERC-20, no DTF re-implementation).
- **P8.1** — Tier A protocol anchors: `svZCHF`, `sUSDS`, `ixEDEL` (NAV / DTF shape), spec-faithful, non-upgradeable, under audit-inheritance discipline.
- **P8.2** — Tier B: one `StubERC4626` template (+ optional waEth-style wrapper variant), N deterministic instances.
- **P8.3** — Tier C: one `StubERC20` template (`*on` equities, BTC wrappers, plain stables, JPYC / tGBP / fBRZ stand-ins).
- **P8.4** — Tier D: `test-stubs/TestnetTokenRegistry.sol` (or a JSON env-map) — pool-config token slot → stub address, mirroring `DeployIxAetheron`'s env injection.
- Out of scope: 04 ixViatica / 07 ixCambio (deferred, O-D6); AuMM + the `src/rate_provider/` RPs deploy REAL, not stubs (P-D2).

### P9 — Deploy scripts + Balancer V3 substrate (workstream B, P-D3 / P-D25)
Granular per-stage deploy scripts for the never-scripted surfaces + the thin `DeployStageP.s.sol` orchestrator — P-D25 (user-adjudicated 2026-07-06): one source of truth per constructor call lives in the granular script; the orchestrator owns only sequence + fail-fast assertions, and the P10 fork fixture inherits it (H13 pattern). Network-parameterised (RPC + chain-id from env); no `--broadcast` (P-D3). The canonical deploy order (stage lettering is NOT the order — M/N binds precede K; `Vault.setAuthorizer` is the final mutation), the `DeployStageH` split rationale, the WH-P6 (b)/(c) new binds, and the assertion set are locked in P-D25 (the Router redeploy + its `setTrustedRouter` orchestrator bind refined to P-bis by P-D26).
- **P9.1** — RESOLVED, no deploy (P-D27): Stage P deploys NO Balancer periphery. The G10 compile-probe ran and produced two findings — a solc Internal Compiler Error building upstream `VaultExplorer` under the pinned via_ir/9999 profile, and independent redundancy: `TVLOracle`'s only two explorer calls (`getPoolTokens` / `getPoolData`) are selector-identical on `IVaultExtension`, so `VAULT_EXPLORER` = `IVaultExplorer(address(aureumVault))` — the established StageH/I/L fixture pattern — is bound at P9.3 where `TVLOracle` deploys. The canonical `Router` redeploy stays DEFERRED to P-bis (P-D26 — the deliberately-excluded permit2 dependency); the P10 e2e drives swaps + liquidity via the D32 (β) `Vault.unlock` pattern and seats a test-side trusted unlock harness for the F-09 recorder path.
- **P9.2** — `script/DeployFeeRoutingHook.s.sol`: the 7-arg hook deploy, pre-compute-aware (D-D21 / D36 — `DeployAureumVault` already stored the predicted hook address in the controller; the hook must land at that address).
- **P9.3** — `script/DeployStageF.s.sol` (TVLOracle → EfficiencyOracle → EMASampler → CCBMultiplier) + the `DeployStageH` split: H gains TVL_ORACLE + EFFICIENCY_ORACLE env keys, keeps channel + distributor only; `test/fork/DeployStageH.t.sol` retrofit — I13 blast radius sized at this sub-step's pre-flight.
- **P9.4** — `script/DeployStageG.s.sol` (SwapAndDeposit → VaultClassRegistry → GaugeEligibility → GaugeRegistry → the `setGaugeRegistry` seal). P-D25 named opens (a)/(b) — the `auMT_` production value + the VCR genesis manifest source — resolve at this sub-step's drafting.
- **P9.5** — `script/DeployStageP.s.sol` thin orchestrator: the `deploy()` delegation chain in P-D25 canonical order + the two new binds (pilot `seedFoundingPool` 01/05/14; `VaultClassRegistry.setGovernanceContract(multisig)`, WH-P6 (c)) + the four fail-fast assertions (GENESIS_BLOCK 4-way equality per P-D14; Miliarium roster completeness; `trustedRouter` unset post-orchestration, WH-P6 (b) / P-D26; post-run `vault.getAuthorizer()`). The `setTrustedRouter` production bind moves to P-bis with the Router; the P10 test seats a trusted unlock harness itself (P-D26).
- **P9.6** — fork validation run (split-form + `--threads 1`, D35 / D36; `forge clean && forge build` first per F14) + the S11 `AureumVaultFactory` differential attestation vs upstream `VaultFactory.sol` + the S10 deployment-surface roll-up in the whitehat ledger.
- Checkpoint: the orchestrator deploys the full stack clean on a local / Tenderly mainnet fork with all four P-D25 assertions passing in-run; the factory differential isolates only the documented diff (S11); split-form regression green.

### P10 — Integration validation (P-D0b, workstream B)
Tenderly mainnet-fork e2e (`test/fork/StagePIntegration.t.sol` or a devnet run) against REAL mainnet literals — the `STAGES_OVERVIEW.md` Stage P testing matrix: the three proposal types, the gauge-activation workflow, fee routing → Bodensee, CCB scoring over a simulated protocol-month, the composition-challenge dry-run, authorizer migration (`Vault.setAuthorizer`), the halving boundary if feasible. Full split-form regression + `forge coverage` (structurally unavailable under the pinned toolchain — P-D42; surfaces-REACHED intent met by execution evidence) + `slither .`.
- Checkpoint: e2e green on the real-state fork; regression count logged; the surfaces-REACHED intent is met by the P-D42 execution-evidence substitution (`forge coverage` structurally unavailable), and `slither .` shows zero high/critical in `src/`.

### P11 — Stage P close
Two-commit PENDING_P (P-D4): the close commit writes `PENDING_P` (the `STAGES_OVERVIEW.md` master row + this Completion Log; `CLAUDE.md` §11 stays hash-free) → a `*-backfill` commit substitutes the real hash → tag `stage-p-complete` at the backfill commit → ff-merge `stage-p` → `main`. The whitehat ledger coverage roll-up is complete before the tag (process-doc §2). P-bis (the live testnet broadcast + the `aumm-app` frontend) is deferred beyond the tag.
- Checkpoint: the ledger carries zero open findings; the tag sits at the backfill commit on `main`; regression count logged.

---
## Completion Log
| Sub-step | Commit | Status |
| --- | --- | --- |
| P0.1 | `6aebebe` | ✅ docs/STAGE_P_NOTES.md — design freeze (P-D0—P-D11; four amendments) |
| P0.2 | `6f913ac` | ✅ docs/STAGE_P_PLAN.md — plan + WH contract×surface matrix + P0—P11 roadmap (P0.2a + P0.2b) |
| P0.3ab | `ff32941` | ✅ STAGE_P_NOTES.md + STAGE_P_PLAN.md — P-D1 testnet posture amended to network-agnostic; live target Sepolia/Hoodi deferred to P-bis |
| P0.3cd | `4696112` | ✅ FINDINGS.md + STAGES_OVERVIEW.md — Holesky→network-agnostic doc strike (P-D1b); FINDINGS supersession-preserved, STAGES_OVERVIEW live-sequence rewritten |
| P1 (WH-D) | `b96ca6c` | ✅ WH-D Stage-D fee-routing back-fill — S2/S5/S6 on AureumFeeRoutingHook + AureumProtocolFeeController; F-13 (S5 Low, Accepted-risk) logged, S2/S6 NO FINDING; ledger coverage roll-up filled |
| P2 (WH-G) | `fef4815` | ✅ WH-G Stage-G gauge-surface back-fill — S8/S9/S4/S5-S6 on GaugeRegistry + GaugeEligibility + VaultClassRegistry + SwapAndDepositToBodensee; F-14 (S5 High — svZCHF-membership settlement-liveness) Fixed via P-D12 fail-safe + sUSDS generalization (`1af89f3`/`1decb70`/`fdb158c`); F-15 (S9 Low — VaultClassRegistry veto live-denominator, F-06 sibling) Accepted-risk → Stage Q; F-08 + F-12 re-verified holding; F-10 favored-cohort tournament dormant observation escalated 2026-07-02 to F-16 (Medium), wired end-to-end F16a-h and now Fixed (ledger backfill `0ce5003`) — see the F-16 findings-table row; coverage roll-up filled |
| P3 (WH-H) | `08616d7` | ✅ WH-H Stage-H emission + Stage-F CCB engine back-fill — S1/S7/S3 across `EmissionDistributor` + `BodenseeBootstrapChannel` + `AuMMMinterRouter` + the CCB engine (`EMASampler`/`TVLOracle`/`EfficiencyOracle`/`CCBMultiplier`/`CCBScore`/`CCBShare`); all three steps **NO FINDING** — WH-H.1 S1 supply/mint conservation (sole `_mint` cap-gated + one-shot minter, exactly 2 `mintFor` sites, LP/Bodensee AP-sum partition byte-identical, halving-era no off-by-one), WH-H.2 S7 TVL-EMA (F-04/F-05/F-10/F-16 gate lineage re-verified; the CCBMultiplier ungated `tvlEMA` reads are bounded — anti-cyclical, sign-only, clamped [0.75,1.25], × gated-EMA in the sole consumer), WH-H.3 S3 rounding (every mul/div rounds down protocol-favorable; F-16 clamp cap-never-exceeded; no div-before-mul); F-16 (S9 Medium, escalated at P2) the sole pass finding, already Fixed; adjacent obs deferred to P6 (`EmissionDistributor` L263 B-leg-anchor doc-nit; `CCBShare` orphaned library — MasterChef superseded the F-6 normalizer); ledger roll-up CLOSED (rows `9ef5471`/`e9bb218`/`08616d7`) |
| P4 (WH-I) | `5d0a41d` | ✅ WH-I AuMT governance-spine back-fill (per I-D14 no `AuMT.sol`) — S9 attribution + S8 recorder-gate lens on `EmissionDistributor` recorder + `AureumFeeRoutingHook` dispatch + `VotingWeight` reader; host-contract S1/S2 covered by P3/P1. **F-17 (S9 High — recorder↔BPT desync) Fixed**: I-D14 transferable-BPT + F-09 symmetric `trustedRouter` allowlist (skips `recordWithdrawal` on non-trusted removal) → phantom `userLP > balanceOf` via non-trusted-router removal or plain BPT transfer, monetizing capital-free emission + honest-LP dilution + skin-less governance + recipient canonical-exit revert; latent until P9 `setTrustedRouter`. P-D18 forfeit-delta downward reconciliation — `_syncDown` settles-then-ratchets to a per-site live-`balanceOf` reference without crystallizing (forfeits the moved delta, stranded per H-D24), wired at `recordDeposit`/`recordWithdrawal` (+ `debit=min(amount,userLP)` liveness clamp)/`claim` + permissionless `syncPosition`; `VotingWeight._positionPower` numerator read-cap `min(userLP,balanceOf)`. S8 recorder gate re-verified holding (F-09 allowlist fail-closed). Commits `392e036`→`a9aafa9`→`5f539a4`→`0598a2f`→`5d0a41d`; PoC `test/whitehat/F17_recorderBptDesync.t.sol` 3/3 + fork faithful-witness test. 885 unit/whitehat + 126 fork green; ledger F-17 row + WH-I roll-up CLOSED |
| P5 (WH-J + WH-L) | `dc0103b` | ✅ WH-J (`MiliariumRegistry` — S8 slot-mutation access + S9 roster gaming, 2 read-lens steps) + WH-L (`IncendiaryRegistry` + `EmissionDistributor` L-D25 boost-delivery leg — S9 economic/S4/S6 + S1/cross-seam-(a) conservation + S3/S7/S8 sweep, 3 read-lens steps, the formal pass superseding the P-D6 SPOT-only F-07 review) — both **NO FINDING**, coverage-roll-up-only (no findings-table rows). WH-J: the only two mutators (`replaceSlot`/`setGovernanceContract`) are `onlyGovernance` exact-equality; membership is exclusive (`PoolAlreadyRegistered` dedup) and every roster consumer (`CCBMultiplier`/`VotingWeight`/`TVLOracle` Leg 2) sums the dense enumeration order-independently, so the `_deregister` swap-remove reorder cannot mis-attribute; the O-D2 composition gate confirmed firing ahead of every governance-routed `replaceSlot`. WH-L: `buyBoost`'s F-04/F-05/F-07 EMA maturity+freshness gate lineage re-verified holding; the 15%-of-epoch shared cap + non-refundable-deposit economics defeat griefing; the boost is confirmed skim-funded not minted — `_placeBoost` writes `epochSkimAllocated`/`epochPoolSkim` by the identical `alloc` so the L-D23 bound `Σ_pools boostIntegral ≤ integratedSkim` holds by construction, and the continuous-phase LP tranche subtracts exactly `integratedSkim` (no double-mint). Commits `4f35e66` (WH-J row) + `dc0103b` (WH-L row). Adjacent obs → P6: a boost bought for a then-gauged pool still delivers post-gauge-revocation (non-refundable, stays skim-funded/cap-bounded, no conservation break); the P5.1 cross-seam caveats (c) governance↔registry + (d) roster↔TVLOracle dedup route to P6.3/P6.4. |
| P6 | `2c0bd31` | ✅ Cross-seam synthesis + INV-2 invariant harness + O-D4/P-D22 `activateBoost` removal (one consolidated row per the P1—P5 pattern). **(a) Cross-seams P6.1—P6.4** (the four P-D6 named seams) — **F-18 (channel↔distributor F-0 bootstrap-rate basis, S1 Low) Fixed**: `BodenseeBootstrapChannel.accrue()` L206 snapshotted `blockEmissionRate(block.number)` (the CALL block) while the distributor `_lpTrancheIntegral` L357 reads it per-integrand-block (H-D30), so a first bootstrap-spanning `accrue()` landing in Era ≥ 1 (~4 yr dormancy, not contract-enforced) prices der Bodensee's mint at the post-halving rate → a halving-factor UNDER-mint of the bootstrap tranche (1/2 in Era 1, 1/32 in Era 5); never over-mints, so INV-1/6 (≤ 21M) is structurally safe with no attacker gain. Fix keys the rate to the clamped integrand upper bound `to` (guaranteed Era 0) per P-D19 (`c162261` + lock `015f280`), PoC `test/whitehat/F18_channelRateBasis.t.sol` 3/3 (`a9e58fc`), ledger row `fb5824e`. Seams (b) hook↔recorder S9 / (c) governance↔registry `onlyGovernance` S8 / (d) roster↔TVLOracle dedup S7 all ✓; two P9 deploy-wiring invariants (`setAuMTContractForPool` before `setTrustedRouter`; `VaultClassRegistry.setGovernanceContract`) + one WK.18 thin-venue extension routed forward; WH-P6 roll-up `9033317`. **(b) INV harness P6.5** — `test/invariant/EmissionConservation.t.sol` (INV-1/2/6) + non-circular `ReferenceEmission.sol` oracle: INV-1/6 (≤ 21M) + INV-2 (≤ schedule) green, INV-2 calibrated to the P-D21 two-cursor bootstrap flooring-dust bound `+ 2·(month10End − GENESIS)` (superadditive integer-floor residue, cap-safe, ≤ ~4.4e-12 AuMM, frozen post-Month-10 — not a break, no F-number); deep-gated `runs≥5000` = 640k calls 3/3 green. Commits `9a0cad2`/`fecb996`/`a7ee36d`/`87ba577`. **(c) activateBoost removal P6.6 (Option B / P-D22)** — removed `CCBMultiplier.activateBoost` + the boost machinery (`BOOST_FACTOR`/`GAUGE_BOOST_DURATION_BLOCKS`/`boostExpiryBlock`/`BoostAlreadyActive`) + the orphaned gauge-registry apparatus (`gaugeRegistry`/`setGaugeRegistry`/`gaugeRegistrySetter`/`OnlyApprovedGauge`/`OnlyGaugeRegistrySetter` + the `IGaugeRegistry` import — sole value-consumer `activateBoost`); constructor 3→2-arg, `getMultiplier` 4→3 regimes, `ICCBMultiplier`/`IGaugeRegistry` NatSpec corrected; test surface removed (24 unit tests + `MockGaugeRegistry` in `CCBMultiplier.t.sol`, the dead `StageGRegistryStageFRegressionTest` + `CCBEngineBoostLifecycleTest` contracts, 6 fork ctor sites → 2-arg); F-8 math suite intact. Commits `0b905f1`→`d3ba70c`→`3606928`→`da2168f`→`023dc21`→`dc2792d`→`2c0bd31`. **P6 checkpoint satisfied**: build ✓, 871/871 unit ✓, invariant `runs≥5000` ✓, split-form fork regression 122/122 ✓ (D35/D36). |
| P7 | — | ✅ hevm symbolic-execution pass DEFERRED to Stage Q (P-D9, user-adjudicated 2026-07-06) — optional non-gating pre-audit shakeout; the §8b hevm install was not undertaken at Stage P. Formal methods consolidate in Stage Q (hevm + the already-Q-scoped Act). No `stage-p-complete` gate impact — the P-D9 checkpoint accepts "hevm proof output OR the deferral attestation", and this row is that attestation. Proceeding to the P8 deployment workstream. |
| P8 | — | ✅ testnet token-stub workstream DEFERRED to P-bis (P-D23, user-adjudicated 2026-07-06, Option B) — off the `stage-p-complete` critical path. The P10 integration e2e runs against REAL mainnet literals on a fork (P-D0b), so no testnet ERC-20 stubs are needed for the tag; the live testnet broadcast + stubs belong to P-bis alongside the `aumm-app` frontend. No gate impact — the P-D23 disposition accepts this deferral attestation in lieu of a stub deliverable, mirroring the P7 row. Deployment workstream proceeds at P9. |
| P9 (deployment) | `21f085e` | ✅ Workstream-B deploy-script authoring + P9.6 lightweight validation (one consolidated row per the P6 pattern; P-D33-revised scope). **P9.1 no-periphery-redeploy** (`ee3843c`/`978282a`, P-D27) — Stage P deploys NO Balancer periphery (the G10 probe hit a solc ICE building upstream `VaultExplorer`, and `TVLOracle`'s two explorer calls are `IVaultExtension`-selector-identical, so `VAULT_EXPLORER` = `IVaultExplorer(address(aureumVault))`; the canonical Router stays DEFERRED to P-bis). **P9.2 DeployFeeRoutingHook** (`4a45639`) — the 7-arg pre-compute-aware hook deploy (D-D21 / D36; lands at the controller's stored predicted address). **P9.3 DeployStageF + DeployStageH split** (`fd7ce18`…`81c6bc4`) — F deploys TVLOracle → EfficiencyOracle → EMASampler → CCBMultiplier; H gains the TVL_ORACLE / EFFICIENCY_ORACLE env keys + keeps channel + distributor; the `DeployStageH.t.sol` I13 retrofit sized at pre-flight. **P9.4 DeployStageG** (`18ababb`…`6fc419e`) — SwapAndDeposit → VaultClassRegistry → GaugeEligibility → GaugeRegistry + the internal seal; P9.4a removed the `auMT_` ctor arg (13-site sweep, per I-D14 no `AuMT.sol`); **P9.4b the P-D31 three-class authority split fixed the composition bug** where P-D30's "every setter = deployer" broke the moment G runs as `deploy(address(g))` under a unified downstream governor — Class A seal-only = deployer, Class B (VCR `votingWeightSetter` / `governanceSetter` + `GaugeRegistry.governance`) = GOVERNANCE_MULTISIG, Class C `donateAuthorizer` = deployer through the seal then handed to GOVERNANCE_MULTISIG in-script; fork-verified 1/1 against real Vault/Bodensee/AuMM state. **P9.5 DeployStageP thin orchestrator** (`18dfad2` P-D32 lock + `0f8635b` the 191-line script) — testable `deploy()` (no governor arg, `address(this)` IS the unified governor), `run()` reverts `ProductionOrchestrationDeferredToPbis()`, the base-layer-governor sentinel, the 12-step chain-order `_orchestrate` (J → F → G → H → setEmissionsRecorder → I → M → N → seedFoundingPool ×3 + setGovernanceContract → L → K), 4 post-conditions. **P9.6 lightweight validation** (P-D33 `2361b90` lock): P9.6.1 split-form regression **869 unit / 123 fork, 0 failed** (F14 `forge clean` first; D35 / D36 `--threads 1`; no commit — validation run); P9.6.2 the **S11 `AureumVaultFactory` differential attestation** (`c26f3a2` — the +27-line delta fully accounted, ZERO divergence beyond the documented Option-F2 diff); P9.6.3 the **S10 deployment-surface roll-up** (`21f085e` — NO NEW FINDING across the four S10 vectors, F-03 re-verified wired + the two WH-P6 S10 invariants closed). **P9 checkpoint (P-D33-revised)**: the committed `DeployStageP.s.sol` compiles + regresses nothing + S11 isolates the documented diff + S10 clean — SATISFIED. The orchestrator's full-stack on-fork deploy exercise (all four assertions passing in-run) + the net-new base-layer-only fixture move to **P10** (the primary `stage-p-complete` gate). |
| P10.1 | `0f6c0c5` | ✅ `test/fork/StagePIntegration.t.sol` — net-new abstract `StagePIntegrationFixture` per the P-D34 lock, in two sub-steps. **P10.1a** (`1797e51`) — the base layer: `DeployStageP` FIRST + the governor-sentinel triple ordering (hook pre-compute → Vault deploy → WPF → AuMM → der Bodensee → hook deploy → `AureumWeightedPoolFactory`), the 4 G-transcribed liquidity helpers (`_initializeBodensee`/`_initializePool` + callbacks). **P10.1b** (`0f6c0c5`) — the 26-pool roster (3 pilots +init transcribed from G L171-197, 5 Majors from M L67-71, 18 Sector-3 + the N-D9 RP prelude from N L87-139), the 34-key env wire (incl. `WEIGHTED_POOL_FACTORY` = awpf, the F-12 provenance bind), and `orchestrator.deploy()`. Verified source-diffed against G/M/N per the CLAUDE.md L329/L330 rule (not a scratchpad oracle); `forge build` green, zero warnings tied to the file. Closes P10.1. |
| P10.2 | `ff01ed0` | ✅ `StagePWiringTest` (`test/fork/StagePIntegration.t.sol`) — the H13 wiring-layer witness of the orchestrator, per P-D34 L461: 5 tests reading the fixture's own handles/arrays (never `vm.env`) — all 15 public handles non-zero, `GENESIS_BLOCK` uniform across the stack, all 26 roster pools gauged + recorder-bound (der Bodensee explicitly excluded), all 14 Stage F/G/H/I/K wire-by-wire handoffs, and the at-rest `governanceModule`/`trustedRouter` state. **First end-to-end exercise of `orchestrator.deploy()` surfaced P-D35** — `OnlyRegistrySetter()` at K wire (8): `TVLOracle.registrySetter` was `msg.sender`-pinned to the nested `DeployStageF` script instance, but K wires as the orchestrator — the two identities coincide in every prior fixture/production path (same broadcaster or same `address(this)`) but split for the first time under the unified-governor orchestrator. **A-lite fix** (`f114ea0` lock, `8766329` fix, `ff01ed0` the now-green wiring test) — `registrySetter = _initialGovernance` (not `msg.sender`); `DeployStageF` passes `GOVERNANCE_MULTISIG` for TVLOracle's initial governance, dropping the now-redundant handoff; `TVLOracle.t.sol` retrofitted with 5 `vm.prank(GOVERNANCE)` calls. Amends K-D8 (ctor signature unchanged). Full split-form regression green: **869/869 unit, 128/128 fork** (the +5 delta over the P9 baseline is `StagePWiringTest`; `DeployStageK.t.sol`'s `test_F03` regression check also holds, confirming the single-governor fixture model is unaffected). |
| P10.3a | `0144e35` | ✅ `test/fork/StagePIntegration.t.sol` — `StagePEndToEndTest` Leg A (`test_legA_claimMintsThroughFullRecorderChain`), leg 1 of the P-D36 7-leg roster: the recorder-claim-mint chain (hook `getSender` dispatch → recorder → `recordScore` → epoch accrual → `mintRouter.mintFor` → AuMM mint) on the orchestrator-deployed 26-pool stack, no `vm.mockCall`/`vm.store` shims (P-D36 policy). The first execution surfaced **F-19** (S7 Low — `TVLOracle._venueRatio` uninitialized-venue liveness: K-D8 Leg 2 fed every registry pool unconditionally into a `getPoolData` read gated by the Vault's `withInitializedPool` modifier, so one registered-but-uninitialized Miliarium pool bricked `tvl()`/`quoteSvZCHF` protocol-wide — the orchestrator's full 26-pool registry paired with the M/N deploy-uninitialized posture for the first time), now **Fixed** (P-D37 — the `isPoolInitialized` guard as `_venueRatio`'s first statement) with PoC `test/whitehat/F19_uninitializedVenueLiveness.t.sol` (4/4). The second execution attempt surfaced **P-D38** (test-tooling lesson, not a protocol finding): `vm.prank(lp)` followed by a chained `orchestrator.emissionDistributor().claim(...)` minted 0 AuMM without reverting — the intermediate public auto-getter is itself a STATICCALL that consumes the single-shot prank, leaving `claim` to run as the test contract (zero stake → the H-D25 `amount==0` early return); fixed by caching the contract handle into a local before the prank. The rule binds Legs B–G, which chain `orchestrator.X()` getters heavily. 4-commit train: `2181cbf` (F-19 fix — `_venueRatio` guard + `MockVaultExplorer` init semantics + PoC) → `42cadda` (ledger F-19 row hash backfill) → `062b522` (Leg A fork witness + the P-D38 handle-cache fix, fork-verified 2/2) → `0144e35` (P-D38 lock in `STAGE_P_NOTES.md`). Full split-form regression: **873/873 unit, 130/130 fork**. |
| P10.3b | `ba21bee` | ✅ `test/fork/StagePIntegration.t.sol` — `StagePEndToEndTest` Legs B + D (P10.3b per the P-D36 decomposition). **Leg B** (`test_legB_swapFeeSweepGrowsBodensee`, `0b903f2`) — the real-swap fee-sweep witness: `_performSwap`/`_performSwapCallback` helpers transcribed from `PilotPools.t.sol` L325-L355 (two-arg `deal` per E10, plain `transfer` — the file carries no SafeERC20 import) drive a sUSDS→svZCHF swap on pilot pool 01 through the D-D21 hook↔controller bake-dynamic path, proving der Bodensee BPT supply grows and zero hook residue on the orchestrator-deployed stack. **Leg D** (`test_legD_ccbMonthWalkEvolvesMultiplier`, `ba21bee`) — the CCB month-walk over `CCBMultiplier.updateMultiplier`'s `BLOCKS_PER_EPOCH` cadence gate + F-8 anti-cyclical `M_i` evolution, permissionless (no `vm.prank`); two epochs witnessed — the cadence-gate `TooEarly` revert (full-encoded per the `CCBMultiplier.t.sol` L174 precedent, since a bare `.selector` exact-matches the whole revert data and fails on an arg-carrying error — fix.1) and the intra-channel step-down (`INITIAL_MULTIPLIER` → 0.95 → 0.90, both clearing the 0.75 floor) off the single-matured-pool EMA landscape. Both legs fork-verified 1/1 individually; full split-form regression re-run deferred to P10-close. |
| P10.3c | `7eb7ae9` | ✅ `test/fork/StagePIntegration.t.sol` — `StagePEndToEndTest` Legs C1/C2/C3, the three `AureumGovernance` proposal-type witnesses (P-D39), sharing the `_seatVoter` (real `_matureStack` + `poke`, no StageO `_qualifyVoter` mock) + `_runProposal` (snapshot→vote→queue→execute) harness landed with Leg C3. **Leg C3** (`test_legC3_feeChangeExecutesThroughMigratedAuthorizer`, `8f9cb15`) — the DYNAMIC authorizer-migration witness: `proposeFeeChange` → vote → queue → execute routes `VAULT.setStaticSwapFeePercentage` as `msg.sender = governance`, succeeding only because the K-wire-9-migrated `AureumGovernanceAuthorizer` grants `GOVERNANCE_CONTRACT` full authority (P10.2 wire (13) asserted only the static `getAuthorizer()` half). Its FIRST execution surfaced **F-20** (S8/S9 Medium — the roster-wide `swapFeeManager: governanceMultisig` EXCLUSIVE Vault role structurally foreclosed the FeeChange execute leg with `SenderNotAllowed`, the bond already donated), Fixed via the F20a-f train (P-D40 lock `3d1bc0e` + Fix A `swapFeeManager: address(0)` `2f4dbb2` + Fix B `proposeFeeChange` executability gate `ed32157` + PoC `d90c16f` 4/4 + doc sweep `3ffd03b`/`dac8c34` + ledger row `69cbce7`); fork-verified 1/1 post-fix. **Leg C2** (`test_legC2_gaugeChallengeRevokesThroughGovernance`, `c4b621b`) — the K-wire-6 revoke witness: all 26 roster pools are slotted so none clears `proposeGaugeChallenge`'s `Active && slotOf == 0` gate, so the precondition manufactures an unslotted Active gauge via governance `seedFoundingPool` (bypasses eligibility + anti-spam, no slot); execute routes the GaugeChallenge branch through `revokeGauge` (Active → Revoked, terminal G-D17). **Leg C1** (`_buildCompositionCandidate` + `test_legC1_compositionChallengeReplacesSlotThroughGovernance`, `7eb7ae9`) — the REAL-candidate dual-gate + slot-replace witness: a real uninitialized `awpf.create` pool `[susds 0.6 WITH_RATE, svZchf 0.4 WITH_RATE]` (no `vm.mockCall`, P-D36) clears both `awpf.create`'s `MIN_ERC4626_WEIGHT` (sum 100%) and `meetsCompositionQualityGate`'s ≥0.52e18 numerator (susds a GaugeGenesisManifest-admitted class at 0.6); execute atomically `revokeGauge(old)` → `replaceSlot(5, candidate)` → `registerGaugeFromComposition(candidate)`, witnessed via slot 5 holding candidate + candidate Active + old (pilot 05) Revoked. Each leg fork-verified 1/1; full split-form regression deferred to P10-close. |
| P10.3d | `587f62d` | ✅ `test/fork/StagePIntegration.t.sol` — `StagePEndToEndTest` Leg E (`test_legE_halvingBoundaryTilesEmissionAcrossEra`, `587f62d`; design lock P-D41 `de97cba`), the halving-boundary witness CLOSING the P-D36 7-leg roster (A/B/C1/C2/C3/D/E, all fork-verified 1/1). **Part 1** — an exact era-edge `blockEmissionRate` read on the orchestrator-deployed AuMM (`GENESIS_RATE` at the last Era-0 block, `>>1` at the first Era-1 block, `>>2` at Era 2, plus the distributor sharing the `GENESIS_BLOCK` halving root) proves the halving magnitude exactly — the `AuMM.t.sol` idiom against the DEPLOYED instance. **Part 2** — mature pilot 01, roll one epoch into Era 1, refresh the EMA (F-05 freshness), `recordScore`, then `claim`, firing `_accrueGlobal`'s era-walk tiling (H-D30 `nthHalvingBlock` boundary split) for the FIRST time across any leg (Legs A/D stay wholly inside Era 0) and asserting a nonzero mint survives the boundary straddle — nonzero not exact-ratio per P-D41 (magnitude is Part 1's job; the liveness rests on continuous-phase α=1e18, the dormant F-16 cap, and the freshness-gated EMA). Fork-verified 1/1. |
| P10-close | `e00eb6f` | ✅ The three P10-close stage gates (P-D42-amended; L149-L150 annotated in this commit). **(1) Split-form regression** (D35/D36; `forge clean` first per F14; `--threads 1`) — **877 unit / 136 fork, 0 failed** across 94 suites (49 unit + 45 fork): the full P-D36 7-leg `StagePEndToEndTest` roster coexists clean with every prior-stage fork suite (validation run, no commit). **(2) `forge coverage`** — STRUCTURALLY UNAVAILABLE under the pinned Balancer-parity toolchain (P-D42 lock `e00eb6f`): `forge coverage --ir-minimum` fails Yul stack-too-deep at COMPILE over the 316-file tree (coverage-mode disables the production `via_ir` + optimizer-9999 profile; `--match-path` cannot dodge a compile-time failure; the overflow sits in the untouchable Vault submodule per §8c); the surfaces-REACHED gate INTENT is met by the execution evidence on record (the 877/136 regression + all 20 F-01…F-20 PoC suites executing their patched paths + the P6.5 640k-call invariant harness + the P10.2 wiring witness + the 7-leg e2e roster), the residual per-branch reachability routed to the Stage-Q hevm remit. **(3) `slither .`** (0.11.4, full-tree S12 — the sweep every prior WH pass deferred to Stage-P) — **ZERO high/critical in `src/`**; the 155 `src/` findings are all benign LOW/MEDIUM/INFORMATIONAL classes matching the WK-R/WN baseline (reentrancy-no-eth/benign/events, `block.number` strict-equalities, FixedPoint divide-before-multiply, unused-return, naming, dead-code), the new P-stage code (F-16/F-17/F-18/F-19/F-20 + `activateBoost` removal) adding only benign reentrancy/strict-equality hits each pre-attested in its finding's own review; the 2 project-wide high-severity results are BOTH upstream Balancer-submodule false-positives (`incorrect-shift-in-assembly` ×8 in `WordCodec.sol` — the `1 << bitLength - 1` precedence FP; `uninitialized-state-variables` in `VaultCommon`/`VaultExtension`/`VaultStorage` — the transient-slot pattern FP), out of scope per §1/§8c (byte-identical audited Vault). P10 checkpoint SATISFIED; P11 (two-commit PENDING_P + tag `stage-p-complete`) follows. |
| P11 | `e25a5dc` | ✅ Stage P close — two-commit PENDING_P (P-D4). This close commit writes the `STAGES_OVERVIEW.md` master row + narrative Completion note (`PENDING_P` placeholder) + this Completion Log row + the `CLAUDE.md` §11 resume-anchor rewrite (Stage P IN PROGRESS → COMPLETE, hash-free); the `*-backfill` commit substitutes the real hash; the `stage-p-complete` tag sits at the backfill commit; then ff-merge `stage-p` → `main`. **Checkpoint SATISFIED** (P-D0b, plan L154): the whitehat ledger carries ZERO open findings (F-01…F-20 = 18 Fixed + F-13 / F-15 Accepted-risk → Stage Q; P11.1 `c42148e` closed the deferred S12 roll-up); the three P10-close gates green (877 unit / 136 fork, 0 failed; `forge coverage` structurally unavailable per P-D42, surfaces-REACHED intent met by execution evidence; `slither .` ZERO high/critical in `src/`, 155 benign findings, the 2 project-wide highs upstream Balancer-submodule FPs out of scope per §1/§8c); the P-D36 7-leg e2e roster (A/B/C1/C2/C3/D/E) all fork-verified 1/1 on the orchestrator-deployed stack. Deferred beyond the tag: P8 testnet stubs + the live testnet broadcast + the `aumm-app` frontend → P-bis (P-D23); the P7 hevm symbolic pass + F-13 / F-15 → Stage Q (P-D9). |
