# Stage G — gauge state machine + auto-gauge eligibility

> **Status:** In progress on `stage-g`. Companion to `docs/STAGE_G_NOTES.md`.
>
> **Audience:** Sagix plus any future Claude session that needs to know what Stage G is and what it produces.
>
> **Why this file exists:** resumption anchor outside chat scrollback — this file is the entry point for Stage G work.

---

## Scope of Stage G

**Goal:** ship the gauge state machine — auto-gauge eligibility, three activation paths (permissionless / composition / founding-pool seeding), the Vault-Class Registry that gates the 52% Quality Gate numerator, and the epoch-boundary efficiency tournament cohort assignment that the Stage H emission distributor consumes. Pure protocol logic; no emissions yet, no AuMT yet, no on-chain governance yet — placeholder + one-shot setter pattern per F-D20–F-D23 for forward dependencies.

**The Solidity files Stage G produces:**

1. **`src/gauge/VaultClassRegistry.sol`** — Frankencoin-inspired proposal-and-veto class admission for ERC-4626 vault classes counted toward the 52% Quality Gate numerator. Three admission fingerprints (`ImplementationAddress`, `FactoryAddress`, `BytecodeHash`) per **G-D9**. Genesis class set seeded via constructor-hardcoded constants (Option a — bytecode-immutable). Bond routes via the OQ-G3 swap-and-one-sided-deposit primitive to der Bodensee. Veto threshold + window + bond magnitude lock at G1.x. Forward deps (`IAuMT`, `governanceContract`) wired via placeholder + one-shot setter mirroring Stage F's F-D20–F-D23 pattern. Companion interface `IVaultClassRegistry.sol` for narrow read surface.

2. **`src/gauge/GaugeEligibility.sol`** — epoch-boundary eligibility evaluator. Reads pool weights via `IBasePool.getNormalizedWeights()`, classifies each token via the ERC-4626 detection method (try/catch `IERC4626.asset()` per **G-D10**), looks up admitted classes in `VaultClassRegistry`, and checks: (i) 52% numerator gate per **G-D8**; (ii) $10K TVL 7-day-SMA floor per **OQ-G2**; (iii) F-10 efficiency tournament cohort assignment per **G-D3** (top-15% favored cohort vs bottom-85% residual); (iv) pool-type whitelist per **G-D6** (fails closed); (v) forbidden-token block (AuMM, AuMT) per **T-I3**. Eligibility latches per epoch and is recomputed at boundaries only. Emits `GaugeEfficiencyDropped` / `GaugeEfficiencyRising` exactly once per pool per boundary crossing per **G-D5**. Companion interface `IGaugeEligibility.sol`. F-10 ranking math may live as a colocated pure helper (inline or `src/gauge/EfficiencyRanking.sol`) per **G-DP3** — physical factoring is a G2 implementation choice, not a protocol fork.

3. **`src/gauge/GaugeRegistry.sol`** — concrete gauge-registration surface implementing the three activation entry points per **G-D1** + **G-D7**: (i) permissionless `activateGauge(pool)` (any caller, full eligibility gate, OQ-G3 anti-spam fee, **no** boost); (ii) `registerGaugeFromComposition(pool)` (`onlyGovernanceContract`, composition-criteria subset, **applies 90-day boost atomically**); (iii) `seedFoundingPool(pool, seedAmount)` (`onlyGovernanceContract`, eligibility bypassed for zero-TVL deploy shim, **applies 90-day boost**). Boost asymmetry enforced structurally — boost-application code path unreachable from `activateGauge`. Concrete registry replaces the placeholder consumed by Stage F's `IGaugeRegistry` mocks via the F-D23 one-shot seal pattern. Reuses or extends Stage F's `IGaugeRegistry.sol`; if extended (richer surface needed), keeps original interface bytecode-stable for F callers.

**The tests Stage G produces:**

- **`test/unit/VaultClassRegistry.t.sol`** — proposal-and-veto state machine, three admission fingerprints, bond routing to der Bodensee via OQ-G3, genesis-seeded class assertion, veto window + threshold guards, `revokeVaultClass` + grandfather policy, placeholder-then-one-shot-setter unreachability of veto + governance-restricted entry points.

- **`test/unit/GaugeEligibility.t.sol`** — 52% numerator computation across mixed token sets (admitted/non-admitted 4626 + plain ERC-20), ERC-4626 detection (try/catch behavior), forbidden-token block (T-I3), pool-type whitelist (T-I4 fast-track absent, G-D6 fails-closed), TVL-floor gate (OQ-G2), F-10 cohort assignment direction (T-T1/T-T2 transition events), epoch-snapshot determinism (T-I5).

- **`test/unit/GaugeRegistry.t.sol`** — three activation paths, T-I1 atomic-boundary check, T-I2 anti-spam non-refund, T-T4 composition path with boost application + atomic substitution, T-R1 founding-pool seeding via Authorizer-Safe deploy shim, gate-failure paths (each immutable criterion).

- **`test/fork/GaugeFlow.t.sol`** — fork integration against Stage E pilot pools + Stage F CCB engine: full permissionless activation path, founding-pool seeding shim, composition substitution, epoch-boundary cohort assignment with synthetic F-10 inputs via a local `MockEfficiencyOracle` per **G-D23 (i)** + OQ-G2 TVL-floor gate fed by Stage F's `MockTVLOracle` (re-imported from `test/fork/mocks/CCBMocks.sol`); reshaped-ABI `GaugeEfficiencyDropped` / `GaugeEfficiencyRising` end-to-end emission verification per **G-D23 (iii)**.

**The directory addition Stage G performs:** Create `src/gauge/` for the three contracts + companion interfaces + any colocated pure-math helpers (e.g. F-10 ranking per **G-DP3**). No other directory reorganization.

---

## Pragma + license note (Stage G)

All Aureum-authored Stage G files use `pragma solidity ^0.8.26` per cursorrules rule 4. License `SPDX-License-Identifier: GPL-3.0-or-later`. Consistent with Stage C / Stage D / Stage E / Stage F Aureum-authored files.

No upstream Balancer V3 inheritance at Stage G (gauge logic is pure Aureum), so there is no `^0.8.24` precedent at this stage like the Stage B `AureumVaultFactory` or Stage E `AureumWeightedPoolFactory`.

---

## Decisions locked in before Stage G starts

These are the answers to the planning-stage questions resolved before this file was written. They are recorded here so implementation doesn't re-litigate them.

The Stage G **design freeze** lives in `docs/STAGE_G_NOTES.md` as `G-D1` through `G-D10`. The PLAN does not duplicate that prose — it points to it. Mid-stage NOTES additions continue as **`G-D11`, `G-D12`, …**; mid-stage implementation findings are numbered **`G10`, `G11`, …** (two-digit) per `CLAUDE.md` §5. The PLAN's own planning-mechanics decisions (sub-step layout, branch model, test invocation, file factoring) use the **`G-DP*`** prefix to avoid collision with the NOTES-owned `G-D*` series.

| ID | Subject | One-line summary | Canonical record |
|----|---------|------------------|------------------|
| **G-D1** | Auto-gauge vs governance-only paths | Permissionless `activateGauge(pool)` once eligibility gate + OQ-G3 anti-spam fee clear; governance composition path via `registerGaugeFromComposition` is the execution leg of an approved composition proposal, not a fourth vote type. | `STAGE_G_NOTES.md` G-D1 |
| **G-D2** | Sandbox fast-track deprecated | Must not appear as callable logic on `GaugeRegistry` / `GaugeEligibility`; `aumm-site` `08_bootstrap.md` §xxi fast-track row obsolete relative to repo truth. | `STAGE_G_NOTES.md` G-D2 |
| **G-D3** | Efficiency tournament semantics | Sort descending by `efficiency_ratio` per `11_formulas.md` F-10; favored cohort = ranks `1 ≤ rank ≤ ceil(0.15 × N)` (top 15%); bottom 85% receives residual CCB flow only. Caps clause defers to G1.x. | `STAGE_G_NOTES.md` G-D3 |
| **G-D4** | Growth-signal doctrine | Miliarium losing top-tier standing post-Month-11 is expected competitive dynamics, not a protocol failure state; `GaugeEfficiencyDropped` feeds bot/aggregator surfaces. | `STAGE_G_NOTES.md` G-D4 |
| **G-D5** | Threshold transition events | `GaugeEfficiencyDropped` / `GaugeEfficiencyRising` ABI reshaped at **G-D23 (iii)** to `(address indexed pool, uint256 indexed epoch, uint256 numeratorSma, uint256 denominatorSma, uint256 efficiencyRatio)`; emitted exactly once per pool per boundary crossing per epoch resolution; `numeratorSma` + `denominatorSma` carry the 3-epoch SMA of F-10 numerator (`swap_fee_revenue + yield_fee_revenue`) + denominator (`emissions_received`) per **OQ-G1** (same unit, dimensionless ratio); `efficiencyRatio = numeratorSma * 1e18 / denominatorSma`. | `STAGE_G_NOTES.md` G-D5 + G-D23 |
| **G-D6** | Pool-type whitelist | At activation, pool factory-derived type must be in allowed set; exact enum locks at G1.x against deployed factories; fails closed. | `STAGE_G_NOTES.md` G-D6 |
| **G-D7** | Three activation paths + per-path boost policy | `activateGauge` (no boost), `registerGaugeFromComposition` (90-day boost), `seedFoundingPool` (90-day boost); boost asymmetry enforced structurally — boost path unreachable from `activateGauge`. `seedFoundingPool` parameter shape locks at G1.x. | `STAGE_G_NOTES.md` G-D7 |
| **G-D8** | Canonical 52% numerator definition | Sum of pool weights assigned to ERC-4626 tokens whose class is admitted in `VaultClassRegistry`; re-evaluated per epoch boundary; admissions/revocations take effect at next boundary. Verbatim from FINDINGS OQ-G4. | `STAGE_G_NOTES.md` G-D8 |
| **G-D9** | Vault-Class Registry mechanism | Frankencoin-inspired proposal + veto + auto-finalize + revocable-with-grandfather; three admission fingerprints (`ImplementationAddress` / `FactoryAddress` / `BytecodeHash`); genesis class set via constructor-hardcoded constants (Option a); bond / veto threshold / window defer to G1.x with non-regressable constraints. | `STAGE_G_NOTES.md` G-D9 |
| **G-D10** | ERC-4626 detection method | Try/catch `IERC4626.asset()`; non-revert ⇒ 4626-claiming, gated by `VaultClassRegistry` admission; revert ⇒ plain ERC-20. Stateless on registry side; T-I3 forbidden-token check runs first. | `STAGE_G_NOTES.md` G-D10 |

Planning-mechanics decisions specific to PLAN authoring (sub-step layout, branch model, test invocation, file factoring, forward-dependency wiring) are captured below as `G-DP*`.

| ID | Decision |
|----|----------|
| **G-DP1** | **Stage G scope:** auto-gauge eligibility (52% numerator gate + TVL floor + F-10 cohort + pool-type whitelist + forbidden-token block + epoch-snapshot determinism), three activation paths (permissionless / composition / founding-pool seeding), Vault-Class Registry (proposal + veto + revoke), threshold transition events, anti-spam fee routing via the OQ-G3 swap-and-one-sided-deposit primitive. **Not in scope:** F-10 numeric caps tunable values (deferred to G1.x within Stage G), Sandbox fast-track (deprecated per **G-D2**), permissionless-veto governance variant (post-Stage-G design question per **G-D8**), AuMT vote-weight resolution (Stage I; placeholder + one-shot setter), `governanceContract` resolution (Stage K; same placeholder pattern), `GaugeChallenge.sol` separate contract (only modeled if permissionless-challenge variant adopted post-G), Incendiary boost (Stage L). |
| **G-DP2** | **Contract layout: 3 separate contracts under `src/gauge/`** — `VaultClassRegistry.sol`, `GaugeEligibility.sol`, `GaugeRegistry.sol`, plus narrow companion interfaces and any colocated pure-math helpers (F-10 ranking). Decomposition rationale: the registry holds class-admission state with its own access-control surface; eligibility holds epoch-boundary read logic with no privileged state; the gauge registry holds activation entry points + boost-application with the three-path access matrix. Three distinct audit surfaces with discrete state and gate models. |
| **G-DP3** | **F-10 efficiency tournament factoring.** F-10 cohort + ranking math ships **as part of `GaugeEligibility.sol`'s G2 surface** (same epoch-boundary latch as TVL floor + 52% gate). Physical factoring (inline within `GaugeEligibility` vs colocated pure library `src/gauge/EfficiencyRanking.sol`) is a **G2 implementation choice** — not a protocol fork — driven by contract size, audit clarity, and any circular-dependency surface that surfaces at G2 entry. No separate `EfficiencyTournament.sol` in scaffold scope. |
| **G-DP4** | **Forward-dependency wiring — placeholder + one-shot setter pattern (mirrors Stage F F-D20–F-D23).** `IAuMT` (Stage I) and `governanceContract` (Stage K) ship as placeholder addresses + one-shot setters in `VaultClassRegistry.sol` and `GaugeRegistry.sol`. Pre-Stage-I + pre-Stage-K, the veto path and governance-restricted entry points (`registerGaugeFromComposition`, `seedFoundingPool`, `revokeVaultClass`) are **structurally unreachable**. Permissionless `activateGauge` against genesis-seeded classes remains usable for testing and integration before either dependency lands. |
| **G-DP5** | **Anti-spam fee routing.** OQ-G3 fee (100 svZCHF/sUSDS equivalent) routes via the shared swap-and-one-sided-deposit primitive to der Bodensee. The same primitive serves the `VaultClassRegistry` proposal bond. Whether the primitive ships fresh in Stage G (likely a colocated helper under `src/gauge/` or `src/fee_router/`), imports from a Stage D/L precursor, or piggybacks on `AureumFeeRoutingHook`'s settlement path is determined at **G-DP10**. Both surfaces depend on this primitive being callable. |
| **G-DP6** | **Test layout.** Per-contract unit-test file under `test/unit/` (`VaultClassRegistry.t.sol`, `GaugeEligibility.t.sol`, `GaugeRegistry.t.sol`); one integration fork-test file under `test/fork/GaugeFlow.t.sol` exercising the Stage D + Stage E pilot-pool deployment chain plus Stage F's `MockTVLOracle` (re-imported from `test/fork/mocks/CCBMocks.sol`) for synthetic TVL feeds — avoids depending on OQ-22's concrete oracle. Test invocation follows the D35 split-form: unit suite via `--no-match-path "test/fork/**"`; fork suite via `--match-path "test/fork/**" --fork-url $URL --threads 1` (per F-D11 / D36). |
| **G-DP7** | **Sub-step layout.** **G0** plan + notes scaffold (G0a NOTES landed at `STAGE_G_NOTES.md`; G0b is this PLAN scaffold). **G1** `VaultClassRegistry.sol` + `IVaultClassRegistry.sol` + unit tests + tunables lock (bond / veto threshold / window / pool-type whitelist enum / OQ-G3 fee primitive resolution per **G-DP10**) — internal sub-rows **G1.1, G1.2, …** rather than a floating top-level G6 per scope-clarification. **G2** `GaugeEligibility.sol` + `IGaugeEligibility.sol` + unit tests, including F-10 ranking + cohort assignment per **G-DP3**. **G3** `GaugeRegistry.sol` + interface (extend or replace Stage F's `IGaugeRegistry.sol`) + unit tests for the three activation paths. **G4** `test/fork/GaugeFlow.t.sol` integration test against pilot pools + Stage F CCB engine. **G5** docs (`STAGES_OVERVIEW.md` Stage G row update + this file's Completion Log + `CLAUDE.md` §11 refresh + FINDINGS cross-refs as needed). **G9** close (FF merge to `main`, tag `stage-g-complete`, push). Final numbering may refine at G1 entry; broad shape locked. |
| **G-DP8** | **Pragma `^0.8.26` + license `GPL-3.0-or-later` for all Stage G files.** No upstream Balancer-V3 inheritance at Stage G (gauge logic is pure Aureum), so no `^0.8.24` precedent at this stage. Compiler is `solc 0.8.26` per `foundry.toml`. License matches Stage C / Stage D / Stage E / Stage F Aureum-authored files. |
| **G-DP9** | **Branch model:** `stage-g` working branch from `main` (created at the post-Stage-F tip — F5.3 CLAUDE.md §11 refresh on `main`); `git push origin stage-g` early per the D9 lesson — don't let origin lag the local branch. Fast-forwards to `main` at G9; `stage-g-complete` lightweight tag applied on `main` at the tip. Preserve `stage-g` on origin as a snapshot marker per the C0 / D0 / E-D8 / F-D14 convention. No PR workflow. |
| **G-DP10** | **OQ-G3 swap-and-one-sided-deposit primitive — sourcing resolved at G1 entry.** The primitive is the shared rail for both the `VaultClassRegistry` proposal bond and the `GaugeRegistry` anti-spam fee. Three live options at G1 entry: (a) ship fresh as a colocated helper under `src/gauge/` (e.g. `src/gauge/SwapAndDepositToBodensee.sol`); (b) import from a Stage D / Stage L precursor if one exists at G1 entry — verified against repo state; (c) extend `AureumFeeRoutingHook`'s settlement path with a non-swap-leg entry point that performs the swap-then-one-sided-deposit and settles. Pre-G1 sub-step resolution required — `VaultClassRegistry.sol` constructor + `proposeVaultClass` shape both depend on the primitive's call signature. |

---

## Mid-stage supersessions

> Decisions resolved during Stage G that revise values or framing locked in by the entry-time decisions above. Each entry preserves the canonical record location in `docs/STAGE_G_NOTES.md` (full reasoning) and `docs/FINDINGS.md` (OQ marker, when applicable). The lock table is left intact as historical context — read these supersessions alongside it.

- **G-DP10 → G-D11 (RESOLVED at G1.0, `STAGE_G_NOTES.md` G-D11):** OQ-G3 swap-and-deposit primitive sourcing — Option A confirmed (fresh helper at `src/gauge/SwapAndDepositToBodensee.sol`); Stage D `AureumFeeRoutingHook` bytecode untouched. α-via-DONATION (`AddLiquidityKind.DONATION`, zero BPT minted) supersedes the BPT-to-recipient framing implicit in G-DP10's option list. Pre-requisite: Bodensee deploy-script `enableDonation` flag flip at `script/DeployDerBodensee.s.sol` line 81 lands as discrete sub-step **G1.1** ahead of `VaultClassRegistry.sol` Solidity; coexists with Stage D's UNBALANCED fee-routing path on a single Bodensee instance. Two-numéraire support (svZCHF + sUSDS) via Bodensee's existing Rate Providers, svZCHF-anchored (**G-D11.eq**). Caller surface gated via placeholder + one-shot setter pattern (F-D20–F-D23 family).

---

## What is explicitly NOT in Stage G

- F-10 numeric caps tunable values (15/10/5 → 1%/0.5%/0.1%) — retain/drop/replace defers to G1.x within Stage G; cohort direction is the Stage G lock per **G-D3**.
- Sandbox fast-track entry point — deprecated per **G-D2**; must not ship as dead code.
- Permissionless-veto governance variant (anyone may revoke with bond, Frankencoin-style open challenge) — post-Stage-G design question per **G-D8**.
- `GaugeChallenge.sol` separate contract — only modeled if a permissionless-challenge variant is later adopted; not in current scope.
- AuMT (LP receipt token, per-pool, governance vote weight) — Stage I; placeholder + one-shot setter at Stage G per **G-DP4**.
- On-chain governance contract (`governanceContract` for `revokeVaultClass`, `registerGaugeFromComposition`, `seedFoundingPool`) — Stage K; same placeholder + one-shot setter pattern per **G-DP4**.
- F-7 emission distribution (per-block AuMM minting and routing) — Stage H.
- F-3 linear blend (Months 11–12 transition) — Stage H.
- F-0 bootstrap channel (one-sided AuMM deposits to der Bodensee Months 0–10) — Stage H.
- Incendiary Boost (priority skim from emission stream pre-CCB) — Stage L.
- `MiliariumRegistry.sol` — Stage J. Stage F injects an `IMiliariumRegistry` placeholder; Stage G continues to consume it via the same shape.
- Concrete TVL oracle implementation — OQ-22 resolution stage; Stage G integration tests use Stage F's `MockTVLOracle`.
- Aureum's own Router — D33 deferred, post-D / re-entry at K or O.
- OQ-20 controller yield-fee entry point + OQ-21 bi-weekly cadence — post-D, tracked.
- Mainnet deployment of the gauge state machine — Stage R.
- `aumm-site` spec amendments (user-side follow-up): `08_bootstrap.md` §xxi Sandbox fast-track row deprecation (precheck C-3); FINDINGS OQ-11 + OQ-10 fee + proposal-types prose (precheck C-1 / C-2); Stage M/N gauge-approvals wording (C-4); Stage P four-types wording (C-5); F-11 vs F-10 efficiency-formula label correction (C-6).

---

## Sub-steps

> Sub-step bodies populate as they are planned. This section's headers are placeholders for G0 through G9 detail.

### G0 — Plan + notes scaffold

G0 lands the Stage G design freeze, planning-mechanics layer, and FINDINGS addendum ahead of any source. Three docs commits:

- **G0a + G0b** — `docs/STAGE_G_NOTES.md` + `docs/STAGE_G_PLAN.md` paired scaffold (commit `61fb085`, 2026-05-05) — design freeze **G-D1** through **G-D10** in NOTES, planning-mechanics **G-DP1** through **G-DP10** in PLAN, test matrix `T-I1`–`T-I5` / `T-T1`–`T-T4` / `T-R1`, efficiency-event ABI (`GaugeEfficiencyDropped` / `GaugeEfficiencyRising`), Vault-Class Registry mechanism lock per **G-D9**, ERC-4626 detection method per **G-D10**, "what is explicitly NOT in Stage G" boundary.
- **G0c** — `docs/FINDINGS.md` Stage G addendum (commits `37d985b` / `b9a30c6` / `dd3d7df`, 2026-05-05) — **OQ-G1** (efficiency metric → F-10 `efficiency_ratio`), **OQ-G2** (TVL floor cadence → epoch-boundary snapshot), **OQ-G3** (anti-spam fee destination → der Bodensee via the swap-and-one-sided-deposit primitive), **OQ-G4** (Vault-Class Registry — Frankencoin-inspired veto, three admission fingerprints, genesis-seeded constants).

- **Cursor prompt:** N/A — G0a/G0b/G0c landed pre-PLAN-body-fill on the `stage-g` branch; this section documents the scaffold completed at stage entry, not future work.

### G1 — VaultClassRegistry + tunables lock

G1 ships **two** contracts under `src/gauge/`: the **OQ-G3** swap-and-one-sided-deposit primitive (`SwapAndDepositToBodensee.sol`, the shared rail for both the registry's proposal bond and the gauge registry's anti-spam fee per **G-D11** + **G-D12**) and the Vault-Class Registry (`VaultClassRegistry.sol` + `IVaultClassRegistry.sol`, the Frankencoin-inspired class-admission state machine per **G-D9**). Plus their unit tests, one fork-only suite for the helper's V3 callback contract, and a NOTES-only tunables lock that pins concrete `proposalBond` / `vetoThreshold` / veto-window-blocks ahead of any source consuming them.

Sub-row layout: G1.0–G1.3-fix landed pre-source (NOTES + Bodensee deploy-script flag + stale-language sweeps); G1.4–G1.8 deliver the helper (scaffold + constructor + callback + unit tests + fork tests); G1.9 locks registry tunables in NOTES; G1.10–G1.16 deliver the registry (interface + scaffold + constructor + propose / veto / revoke + unit tests).

#### G1.0 — Option A + DONATION decision (G-D11 + G-DP10 supersession) ✅ done (commit `5567533`)

OQ-G3 primitive sourcing resolved at **G-DP10 → G-D11** (Option A): fresh helper at `src/gauge/SwapAndDepositToBodensee.sol`; Stage D `AureumFeeRoutingHook` bytecode untouched (`stage-d-complete` audit-inheritance preserved); α-via-DONATION (`AddLiquidityKind.DONATION`, zero BPT minted) supersedes G-DP10's BPT-to-recipient framing; two-numéraire (svZCHF + sUSDS), svZCHF-anchored; caller surface gated via placeholder + one-shot setter pattern (F-D20–F-D23 family).

- **Cursor prompt:** N/A — landed at `5567533`.

#### G1.1 — Bodensee `enableDonation = true` + test fixture updates ✅ done (commit `07b65a6`)

`script/DeployDerBodensee.s.sol` line 81 flag flip from `false` to `true` per **G-D11** pre-requisite. Test-fixture updates in `test/fork/AureumFeeRoutingHook.t.sol`, `test/fork/PilotPools.t.sol`, `test/fork/CCBEngine.t.sol` to keep Stage D's UNBALANCED fee-routing path and Stage G's DONATION anti-spam path coexistent on a single Bodensee instance. V3 supports both add-liquidity kinds when `enableDonation = true` AND `disableUnbalancedLiquidity = false`. Stage D contract bytecode untouched.

- **Cursor prompt:** N/A — landed at `07b65a6`.

#### G1.2 — Helper full spec lock (G-D12) ✅ done (commit `7afca17`)

Resolves G-D11 "Tunables / sub-decisions deferred to G1.x" residual in NOTES. Locks signature (`swapAndDeposit(IERC20, uint256)` + `requiredAmount(IERC20)` view) + payment pattern (caller pre-pushes; helper transfers OWN balance; post-callback balance == 0 assertion) + pay-token allowlist (svZCHF / sUSDS only — every other token reverts `InvalidPayToken`) + strict-equality fee policy (`FEE_SVZCHF = 100e18`, `FEE_SUSDS = 125e18`; underpay/overpay reverts `IncorrectAmount`) + one-shot caller-gate model (placeholder → `setVaultClassRegistry` / `setGaugeRegistry`, irreversible admin burn at second set) + nine-step callback discipline + token-index resolution at constructor + CEI rules + custom-error enumeration. Audit surface fully enumerated for G1.7 (unit) + G1.8 (fork) + Stage Q (formal).

- **Cursor prompt:** N/A — landed at `7afca17`.

#### G1.3 — 90-day boost deprecation (G-D13 + G-D7 / G-D1 / T-T4 inline amendments) ✅ done (commit `7164b13`)

90-day boost deprecated entirely across all three activation paths per **G-D13**. Cold-start emission lift, if any, funded out-of-band via Incendiary Boost (Stage L) — opt-in, user-funded, applicable to any already-gauged pool. **G-D7** boost columns flipped to "no"; "Per-path symmetry — no boost on any path" replaces the earlier asymmetry framing. **G-D1** composition-path bullet drops boost language. **T-T4** test scenario drops "boost clock starts." Companion spec sweep landed at `aumm-site` `08_bootstrap.md`, `09_transitions.md`, `04_tokenomics.md`, `05_miliarium_aureum.md`, `06_miliarium_manifest.md`, `12_aureum_glossary.md`, `14_ux_ui.md`, `17_faq.md` (receipt commit `055d89f`, "SG.AMEND-1"). `CCBMultiplier.sol` boost machinery (OQ-23 `activateBoost`, M_i clamp, F-D24 reset) is **unaffected** — Incendiary Boost (Stage L) reuses it.

- **Cursor prompt:** N/A — landed at `7164b13`.

#### G1.3-fix — Stale fee-language sweep (G-D9 + G-D11) ✅ done (commit `07e3c98`)

Replaces remaining "100 svZCHF/sUSDS" symmetric framing in **G-D9** + **G-D11** with the asymmetric per-token constants from **G-D12** (`FEE_SVZCHF = 100e18`, `FEE_SUSDS = 125e18`). G-D9 `proposalBond` constraint reframed to `proposalBond ≥ FEE_SVZCHF` (= 100e18) since the bond is svZCHF-denominated and must not undercut the cheaper of the two anti-spam fees. G-D11 "Equivalence" subsection notes supersession by G-D12.

- **Cursor prompt:** N/A — landed at `07e3c98`.

#### G1.4 — Helper scaffold (`src/gauge/SwapAndDepositToBodensee.sol`)

Lands storage / immutables / constants / custom-errors / event surface per **G-D12**. Pragma `^0.8.26`; license `GPL-3.0-or-later`. Imports: `IVault` from `lib/balancer-v3-monorepo`, `IERC20` + `SafeERC20` from `lib/openzeppelin-contracts`. Immutables (6 — `internal immutable` per leading-underscore-camelCase convention): `_vault`, `_bodensee`, `_svZchf`, `_sUsds`, `_svZchfIndex`, `_sUsdsIndex`. **G1.5 refactors this minimal constructor** — drops the 2 `Index` ctor params, derives both indices internally via `_vault.getPoolTokens(_bodensee)`, adds `ZeroAddress` guards on the remaining 5 inputs and a `TokenNotInPool` revert when either token is absent; same sub-step lands the two one-shot setters + `onlyAuthorizedCaller` modifier per **G-D12** caller-gate model. `_moduleAdmin` is **not** an immutable per G0.2 disambiguation — it is the constructor input that initializes the public storage slot `moduleAdmin` (G-D12 line 256), the single burnable operational slot zeroed at the second setter call. No immutable mirror. Storage layout per **G-D14** — 3 persistent slots: `moduleAdmin` (public, burnable; cleared atomically at the second setter call), `vaultClassRegistry`, `gaugeRegistry`; 4 EIP-1153 transient slots accessed via Balancer's `TransientStorageHelpers` + `StorageSlotExtension` pattern (solc 0.8.26 lacks the `transient` state-var keyword per G-D14 Toolchain provenance): `bytes32 internal immutable _EXECUTING_SLOT`, `_PENDING_PAY_TOKEN_SLOT`, `_PENDING_AMOUNT_SLOT`, `_ORIGINAL_CALLER_SLOT`, each derived via `calculateSlot("aureum.swapAndDepositToBodensee", "<varName>")`. Constants `FEE_SVZCHF = 100e18`, `FEE_SUSDS = 125e18`. Fourteen custom errors per G-D12 (full parameter signatures locked at G1.4-pre-A, commit `18a7bbf`): `OnlyAuthorizedCaller`, `OnlyVault`, `OnlyModuleAdmin`, `SetterAlreadyCalled`, `ZeroAddress`, `InvalidPayToken`, `ZeroAmount`, `IncorrectAmount`, `BptMintedOnDonation`, `ReserveDeltaMismatch`, `HelperBalanceNonZero`, `ReentrancyGuard`, `TokenNotInPool`, `CallbackPayloadMismatch`. `event FeeRoutedToBodensee(address indexed originalCaller, IERC20 indexed payToken, uint256 amount)`. Constructor body is minimal — takes 7 ctor params (`vault_`, `bodensee_`, `svZchf_`, `sUsds_`, `moduleAdmin_`, `svZchfIndex_`, `sUsdsIndex_`) and assigns the 6 immutables + the `moduleAdmin` storage slot from those params; no zero-address guards, no `getPoolTokens` read, no other functions, no setters, no entry point, no callback (those land at G1.5 / G1.6).

- **Cursor prompt:** Create `src/gauge/SwapAndDepositToBodensee.sol`; declare immutables / storage / constants / custom errors / event per G-D12 (error signatures per the G-D12 table at `18a7bbf`); add a minimal constructor with 7 params (`vault_`, `bodensee_`, `svZchf_`, `sUsds_`, `moduleAdmin_`, `svZchfIndex_`, `sUsdsIndex_`) that assigns the 6 immutables + the `moduleAdmin` storage slot; no zero-address guards, no `getPoolTokens` read, no other functions, no setters, no entry point, no callback.

#### G1.5 — Helper constructor + token-index resolution + one-shot setters (`src/gauge/SwapAndDepositToBodensee.sol`)

Refactors the G1.4 minimal constructor (drops the 2 `Index` ctor params and derives indices internally via `_vault.getPoolTokens(_bodensee)`) and adds `setVaultClassRegistry` + `setGaugeRegistry` + `onlyAuthorizedCaller` modifier per **G-D12** caller-gate model. Constructor: zero-address checks on all params (revert `ZeroAddress`); call `_vault.getPoolTokens(_bodensee)` → locate svZCHF + sUSDS indices; revert `TokenNotInPool` if either is absent; store as `_svZchfIndex` / `_sUsdsIndex` immutables. Set `moduleAdmin = _moduleAdmin`. Decision on `_moduleAdmin == address(0)` rejection vs test-harness acceptance pinned at this sub-step against deploy-script ergonomics (default = reject; if accepted, NOTES note required). Both setters (`setVaultClassRegistry` / `setGaugeRegistry`) gated to `moduleAdmin`, one-shot (revert `SetterAlreadyCalled` if slot already set), zero-input rejected (`ZeroAddress`), atomic admin-burn at second-set per G-D12; each setter emits `VaultClassRegistrySet(registry_)` / `GaugeRegistrySet(registry_)` after the slot-write per the G-D12 event surface (locked at G1.5-pre-B). `onlyAuthorizedCaller` modifier: revert `OnlyAuthorizedCaller(msg.sender)` unless `msg.sender == vaultClassRegistry || msg.sender == gaugeRegistry` — partial-activation correctness is structural since `address(0)` never matches a real `msg.sender`.

- **Cursor prompt:** Refactor the G1.4 minimal constructor (drop the 2 `Index` ctor params; derive indices via `_vault.getPoolTokens(_bodensee)`; add `ZeroAddress` + `TokenNotInPool` guards) and add `setVaultClassRegistry` + `setGaugeRegistry` + `onlyAuthorizedCaller` modifier per G-D12; no entry point, no callback yet.

#### G1.6 — Helper `swapAndDeposit` outer + `_swapAndDepositCallback` (`src/gauge/SwapAndDepositToBodensee.sol`)

Fills the entry point + V3 Vault callback per **G-D12** nine-step callback discipline. `requiredAmount(IERC20 payToken) external view returns (uint256)`: returns `FEE_SVZCHF` for svZCHF, `FEE_SUSDS` for sUSDS, reverts `InvalidPayToken(payToken)` otherwise. `swapAndDeposit(IERC20, uint256) external onlyAuthorizedCaller`: allowlist guard, zero-amount guard, strict-equality fee guard (`if (amount != _requiredAmount(payToken)) revert IncorrectAmount(amount, required);`), reentrancy lock set, payload cache (`_pendingPayToken` / `_pendingAmount` / `_originalCaller = msg.sender`), `_vault.unlock(abi.encodeCall(this._swapAndDepositCallback, (payToken, amount)))`, transient teardown, post-balance assertion `if (payToken.balanceOf(address(this)) != 0) revert HelperBalanceNonZero(...)`. `_swapAndDepositCallback(IERC20, uint256) external`: sender-strict (`OnlyVault`), payload cross-check (`CallbackPayloadMismatch`), `preReserve` snapshot via canonical V3 reserve read (selector locked at this sub-step against `lib/balancer-v3-monorepo` HEAD — candidates `Vault.getReservesOf` / `IVault.getPoolTokenInfo` / `IVault.getCurrentLiveBalances`; same selector reused for `postReserve` to prevent mixed-source drift), `payToken.safeTransfer(address(_vault), amount)` + `_vault.settle(payToken, amount)`, build `maxAmountsIn[3]` with `payToken` slot = `amount` and other slots zero, `_vault.addLiquidity({pool: _bodensee, kind: AddLiquidityKind.DONATION, ...})`, defensive `if (bptOut != 0) revert BptMintedOnDonation(bptOut)`, `postReserve` read + `if (postReserve != preReserve + amount) revert ReserveDeltaMismatch(...)`, `emit FeeRoutedToBodensee(_originalCaller, payToken, amount)` — caller identity sourced from cached outer caller, never from callback `msg.sender`. Transient-storage layout for `_executing` / `_pendingPayToken` / `_pendingAmount` / `_originalCaller` is **locked at G-D14** (all four `transient` per Solidity 0.8.26 + EVM Cancun); G1.6 declares them per the G-D14 lock with no further design surface.

- **Cursor prompt:** Add `requiredAmount` view + `swapAndDeposit` outer + `_swapAndDepositCallback` per G-D12 nine-step callback discipline; resolve V3 reserve-read selector against `lib/balancer-v3-monorepo` HEAD; wire `tload()` / `tstore()` calls to the four `_*_SLOT` constants per **G-D14** (Balancer-pattern via `TransientStorageHelpers` + `StorageSlotExtension`).

#### G1.6-bis — Helper `donate` entry point + authorization mutators (`src/gauge/SwapAndDepositToBodensee.sol`)

Adds the variable-amount `donate` entry point + authorization mutators per **G-D21** (helper two-entry-point split locked at G1.13-pre-D, commit `aafb29b`). The amendment is **outer-only** — `_swapAndDepositCallback`, the four transient slots (`_EXECUTING_SLOT` / `_PENDING_PAY_TOKEN_SLOT` / `_PENDING_AMOUNT_SLOT` / `_ORIGINAL_CALLER_SLOT`), and the V3 unlock-and-DONATION flow per **G-D14** + **G-D18** are untouched; existing `swapAndDeposit` source-level identity is preserved (not touched at this sub-step); the same nine-step callback is reused to honor G-D21's "no callback change" mandate. New storage: `address public donateAuthorizer` (multi-shot — set at construction to the Stage A–K Authorizer Safe per **OQ-10**, mutable post-deploy via `setDonateAuthorizer` per G-D21 authorizer-rotation requirement; one-shot pattern would block legitimate Stage K rotation to `AureumGovernance`); `mapping(address => bool) public authorizedDonators` (runtime-mutable allowlist; empty at construction; populated post-deploy via `addAuthorizedDonator`). Constructor signature change: append `address donateAuthorizer_` as the sixth parameter (constructor goes from 5 to 6 params); revert `ZeroAddress` on miss; assign the storage slot. New entry point `donate(IERC20 payToken, uint256 amount) external onlyAuthorizedDonator`: pay-token allowlist guard (`payToken == _svZchf || payToken == _sUsds`, else revert `InvalidPayToken`); zero-amount guard (revert `ZeroAmount` on miss); reentrancy lock set + payload cache (four transient slots populated identically to `swapAndDeposit`) + `_vault.unlock(abi.encodeCall(this._swapAndDepositCallback, (payToken, amount)))` + transient teardown + post-balance assertion `if (payToken.balanceOf(address(this)) != 0) revert HelperBalanceNonZero(...)` — structurally identical to `swapAndDeposit` minus the magnitude check (no `IncorrectAmount` revert; variable `amount` flows through unchecked at the helper layer per G-D21 trust boundary — caller's bond math is upstream). New `onlyAuthorizedDonator` modifier reverts `OnlyAuthorizedDonator(msg.sender)` unless `authorizedDonators[msg.sender]`; new `onlyDonateAuthorizer` modifier reverts `OnlyDonateAuthorizer(msg.sender)` unless `msg.sender == donateAuthorizer`. Three new mutators each gated to `onlyDonateAuthorizer`: `setDonateAuthorizer(address newAuthorizer) external` — body: `ZeroAddress` check on the new binding, capture `previous = donateAuthorizer`, assign, emit `DonateAuthorizerSet(previous, newAuthorizer)`; `addAuthorizedDonator(address donator) external` — body: `ZeroAddress` check, revert `DonatorAlreadyAuthorized(donator)` if mapping already true, set mapping `true`, emit `AuthorizedDonatorAdded(donator)` — does **not** check `code.length > 0` per G-D21 trust boundary (authorizer review duty); `removeAuthorizedDonator(address donator) external` — body: revert `DonatorNotAuthorized(donator)` if mapping is false, clear mapping, emit `AuthorizedDonatorRemoved(donator)`. Four new errors at the existing error block: `OnlyAuthorizedDonator(address caller)`, `OnlyDonateAuthorizer(address caller)`, `DonatorAlreadyAuthorized(address donator)`, `DonatorNotAuthorized(address donator)`. Three new events: `DonateAuthorizerSet(address indexed previous, address indexed current)`, `AuthorizedDonatorAdded(address indexed donator)`, `AuthorizedDonatorRemoved(address indexed donator)`. The callback's existing `FeeRoutedToBodensee(originalCaller, payToken, amount)` event fires for both `swapAndDeposit` (path #1, anti-spam fee) and `donate` (paths #2–#5, bonds and challenges) calls — the event semantic generalizes to "DONATION routed to Bodensee"; off-chain consumers disambiguate via the indexed `originalCaller` field (`vaultClassRegistry` / `gaugeRegistry` for path #1; authorized donator contract for paths #2–#5). The helper's overall deployed artifact does change at G-D21 (new `donate` function, new storage slots, new errors and events) — the G-D21 source-identity claim is scoped to the source text and review trail of `swapAndDeposit` itself, not whole-contract bytecode. Joint audit checkpoint at this sub-step: `forge build` green; existing G1.7 unit tests must remain green (no `donate`-path coverage at this beat); new `donate`-path coverage lands at **G1.7-bis**.

- **Cursor prompt:** Add the `donate(IERC20, uint256) external onlyAuthorizedDonator` entry point + `donateAuthorizer` storage slot + `authorizedDonators` mapping + three mutators (`setDonateAuthorizer`, `addAuthorizedDonator`, `removeAuthorizedDonator`) + four new errors + three new events to `src/gauge/SwapAndDepositToBodensee.sol` per G-D21; append `address donateAuthorizer_` as the sixth constructor parameter; reuse the existing `_swapAndDepositCallback` and four transient slots verbatim per G-D21 outer-only mandate; do not touch the existing `swapAndDeposit` function source.

#### G1.7 — Helper unit tests (`test/unit/SwapAndDepositToBodensee.t.sol`)

Lands the 14 unit invariants enumerated in **G-D12**: unauthorized caller revert (`OnlyAuthorizedCaller`); pre-setter helper unreachable from any caller; partial-activation correctness (only-registry-set call path; only-gauge-set call path); post-both-set both callers allowed; second-set revert (`SetterAlreadyCalled`); admin-burn verification after second set (`moduleAdmin == address(0)`); invalid pay-token revert (`InvalidPayToken`); zero-amount revert (`ZeroAmount`); svZCHF underpay/overpay revert (`IncorrectAmount`); sUSDS underpay/overpay revert (`IncorrectAmount`); reentrancy guard fires on nested call attempt; non-Vault callback sender revert (`OnlyVault`); callback payload mismatch revert (`CallbackPayloadMismatch`); constructor `TokenNotInPool` revert when given a Bodensee that does not contain svZCHF or sUSDS. Mock fixtures: a `MockVault` with controllable `getPoolTokens` returns, controllable callback re-entry, and revert-injection hooks for the reentrancy / payload-mismatch / non-Vault-sender tests. Pure unit; no mainnet fork.

- **Cursor prompt:** Create `test/unit/SwapAndDepositToBodensee.t.sol` covering the 14 G-D12 unit invariants against a `MockVault`; pure unit (no fork).

#### G1.7-bis — Helper `donate`-path unit tests (`test/unit/SwapAndDepositToBodensee.t.sol`)

Lands the donate-path unit invariants for the **G-D21** outer-only amendment that landed at **G1.6-bis** (commit `fc27f1f`); extends the existing G1.7 test contract in-place (no new file) with seventeen invariants covering the new authorization-mutator surface + the new `donate` entry point. Authorization-mutator surface (eleven invariants): constructor `donateAuthorizer_ = address(0)` reverts `ZeroAddress`; `setDonateAuthorizer` from non-authorizer reverts `OnlyDonateAuthorizer(msg.sender)`; `setDonateAuthorizer(address(0))` reverts `ZeroAddress`; `setDonateAuthorizer` happy path rotates state, emits `DonateAuthorizerSet(previous, current)`, and post-state the old authorizer reverts on subsequent mutator while the new authorizer succeeds; `addAuthorizedDonator` from non-authorizer reverts `OnlyDonateAuthorizer`; `addAuthorizedDonator(address(0))` reverts `ZeroAddress`; `addAuthorizedDonator` idempotency revert `DonatorAlreadyAuthorized(donator)` on a second add; `addAuthorizedDonator` happy path sets `authorizedDonators[d] == true` and emits `AuthorizedDonatorAdded(d)`; `removeAuthorizedDonator` from non-authorizer reverts `OnlyDonateAuthorizer`; `removeAuthorizedDonator` on a not-currently-authorized donator reverts `DonatorNotAuthorized(donator)`; `removeAuthorizedDonator` happy path clears the mapping, emits `AuthorizedDonatorRemoved(d)`, and the removed donator's subsequent `donate` call reverts `OnlyAuthorizedDonator`. `donate` entry-point surface (six invariants): `donate` from a non-authorized caller reverts `OnlyAuthorizedDonator(msg.sender)`; `donate` with a payToken outside `{svZCHF, sUSDS}` reverts `InvalidPayToken`; `donate` with `amount = 0` reverts `ZeroAmount`; `donate` reentrancy guard fires `Reentrancy` on a nested-call attempt routed via the existing `MockVault` callback re-entry rig; `donate` callback-reached at non-fee-equal amount — `donate(svZCHF, 50e18)` from an authorized donator with valid auth + valid token + non-zero amount passes all helper-side gates and reverts `ReserveDeltaMismatch(preReserve + amount, preReserve)` from the static mock, encoded as `abi.encodeWithSelector(ReserveDeltaMismatch.selector, 1_050e18, 1_000e18)` against the setUp svZCHF `balancesRaw[0] = 1_000e18`; `donate` callback-reached at 1 wei — `donate(svZCHF, 1)` from an authorized donator passes the helper-side gates and reverts `ReserveDeltaMismatch(1_000e18 + 1, 1_000e18)` from the static mock, confirming no magnitude gate at the helper layer (no `IncorrectAmount` revert at amounts ≠ `FEE_SVZCHF` / `FEE_SUSDS`, parallel-but-distinct from `swapAndDeposit`'s strict-equality fee — the G-D21 trust-boundary contract divergence is the load-bearing claim per G1.6-bis source at commit `fc27f1f`). Mock fixtures: reuses the existing G1.7 `MockVault` + `MockERC20` infrastructure verbatim — the `_swapAndDepositCallback` is shared between `swapAndDeposit` and `donate` per the **G-D21** outer-only mandate and is already covered by the G1.7 callback invariants (`OnlyVault`, `CallbackPayloadMismatch`, nine-step-callback discipline) — donate-path tests exercise the outer entry point + transient-slot lifecycle + authorization gates only, no callback re-coverage. Full donation semantics — reserve delta movement, post-balance-zero invariant, and `FeeRoutedToBodensee` event emission with `originalCaller` caller-identity preservation — are intentionally deferred to **G1.8** fork tests where Bodensee's real reserve mechanics are exercised against mainnet-fork state; `donate` follows the same unit/fork coverage split as `swapAndDeposit`, which has no unit happy path either. The existing fourteen G1.7 `swapAndDeposit` invariants must remain green at the joint audit checkpoint. Pure unit; no mainnet fork.

- **Cursor prompt:** Extend `test/unit/SwapAndDepositToBodensee.t.sol` with the seventeen donate-path unit invariants per G-D21 (eleven authorization-mutator + six `donate`-entry-point); reuse G1.7's `MockVault` + `MockERC20` fixtures verbatim; do not modify the existing fourteen `swapAndDeposit` tests; pure unit (no fork).

#### G1.8 — Helper fork tests (`test/fork/SwapAndDepositToBodensee.t.sol`)

Lands the four fork invariants enumerated in **G-D12** against real Bodensee on mainnet fork (with `enableDonation = true` per G1.1). Setup: deploy fresh `AureumVault` + `WeightedPoolFactory` + Bodensee per Stage D's `script/DeployDerBodensee.s.sol`, deploy fresh `SwapAndDepositToBodensee`, wire `setVaultClassRegistry` + `setGaugeRegistry` to test-contract addresses (test contract acts as both authorized callers). svZCHF happy path: caller pre-pushes 100e18 svZCHF; calls `swapAndDeposit`; verify `bptOut == 0`, `postReserve - preReserve == 100e18` for svZCHF, helper post-balance == 0, `FeeRoutedToBodensee(test, svZCHF, 100e18)` event emitted with cached caller identity (not Vault). sUSDS happy path: same shape with 125e18 sUSDS. Counterfactual fee-on-transfer: synthetic FoT behaviour injected via `vm.mockCall` on `_vault.getPoolTokenInfo` (freezing pre-state for both pre and post snapshot reads inside the helper's callback) reverts `ReserveDeltaMismatch` — see **G13** for rationale (mocking `payToken.transfer` would trigger a Vault-side `settle` revert before reaching the helper's defensive check). Test invocation per D35 split-form: `--match-path "test/fork/**" --fork-url $MAINNET_RPC_URL -vv --threads 1` per **F-D11** / **D36**.

- **Cursor prompt:** Create `test/fork/SwapAndDepositToBodensee.t.sol` covering the 4 G-D12 fork invariants against real mainnet-fork Bodensee with `enableDonation = true`; D35 split-form invocation.

#### G1.9 — VaultClassRegistry tunables lock (`docs/STAGE_G_NOTES.md`; NOTES-only)

Resolves the **G-D9** "tunables deferred to G1.x" residual ahead of any source consuming them. NOTES-only sub-step — no source, no test, no PLAN edit beyond a cross-link. Three values to pin, recorded as **G-D19** (next available NOTES decision number after G-D18, since G-D15 + G-D16 are reserved by PLAN G2.0 + G3.0 forward-claims):

- `proposalBond` — concrete svZCHF amount; constraint `proposalBond ≥ FEE_SVZCHF` (= 100e18) per G-D9 + G-D12 amendment; class-admission bond is higher-stakes governance and must not undercut the simpler permissionless-activation fee.
- `vetoThreshold` — AuMT-weighted vote-share threshold for successful veto; constraint `vetoThreshold ≤ governanceQuorumThreshold` per G-D9; governance quorum locks at Stage K, so G1.9 picks against the conservative ≤ 20% upper bound or a registry-specific lower fraction.
- Veto-window blocks — bounded `[BLOCKS_PER_EPOCH, 3 × BLOCKS_PER_EPOCH]` per G-D9; minimum gives a governance reaction window, maximum avoids stalling legitimate admissions.

If any value is still open at G1.9 entry, resolution happens in chat (Opus) before the Cursor prompt fires. Mode dispatch per §13 then routes the NOTES write itself to Sonnet (mechanical transcription).

- **Cursor prompt:** Add **G-D19** (tunables lock) to `docs/STAGE_G_NOTES.md` with the three values + rationale + constraints; NOTES-only — no `src/gauge/` edit, no PLAN edit beyond a cross-link entry, no test edit.

#### G1.10 — `IVaultClassRegistry` interface (`src/gauge/IVaultClassRegistry.sol`)

Lands the narrow read surface consumed by `GaugeEligibility` (G2) for class-admission lookups per **G-D8** + **G-D9**. `enum AdmissionType { ImplementationAddress, FactoryAddress, BytecodeHash }` per G-D9 three fingerprints. `function isAdmittedClass(address token) external view returns (bool)` — runtime check `GaugeEligibility` invokes per pool token after ERC-4626 detection per **G-D10**. `function admissionType(address token) external view returns (AdmissionType)` — diagnostic / event surfaces; not on the eligibility hot path. External read-only; mutations live on the concrete `VaultClassRegistry.sol`. Pragma `^0.8.26`; license `GPL-3.0-or-later`.

- **Cursor prompt:** Create `src/gauge/IVaultClassRegistry.sol` with `AdmissionType` enum + `isAdmittedClass` + `admissionType` read-only function surface; narrow read interface.

#### G1.11 — `VaultClassRegistry.sol` scaffold (`src/gauge/VaultClassRegistry.sol`)

Lands storage / immutables / constants (per G1.9 lock) / custom errors / events / struct definitions per **G-D9** + **G-D19** (G1.9 lock). No constructor body, no functions. Pragma `^0.8.26`; license `GPL-3.0-or-later`. Imports: `IVaultClassRegistry`, `SwapAndDepositToBodensee` (for bond routing), `IAuMT` placeholder (Stage I forward dep), governance-contract address placeholder (Stage K forward dep), `IERC20` + `SafeERC20` from OpenZeppelin. Constants from G1.9 NOTES lock: `PROPOSAL_BOND_SVZCHF`, `VETO_THRESHOLD_BPS` (or equivalent shape), `VETO_WINDOW_BLOCKS`. Storage: `mapping(uint256 => VaultClassProposal) public proposals; uint256 public nextProposalId; mapping(address => bool) public admittedClasses; mapping(address => AdmissionType) public admissionTypes;`. Forward-dep placeholder + one-shot setter pattern (F-D20–F-D23): `IAuMT public auMT; address public governanceContract; address public auMTSetter; address public governanceSetter;`. Struct `VaultClassProposal { AdmissionType admissionType; address admissionValue; bytes32 constraintsHash; uint256 createdBlock; uint256 vetoSupport; bool finalized; bool revoked; }`. Events: `VaultClassProposed(uint256 indexed proposalId, address indexed proposer, AdmissionType admissionType, address admissionValue, bytes32 constraintsHash)`, `VaultClassVetoed(uint256 indexed proposalId, address indexed vetoer, uint256 weight)`, `VaultClassFinalized(uint256 indexed proposalId, address indexed admissionValue)`, `VaultClassRevoked(address indexed admissionValue)`. Custom errors: `OnlyGovernance`, `ZeroAddress`, `InvalidAdmissionType`, `ProposalAlreadyFinalized`, `VetoWindowExpired`, `VetoWindowOpen`, `InsufficientVetoWeight`, `ClassAlreadyAdmitted`, `ClassNotAdmitted`, `SetterAlreadyCalled`. (Supplemented at **G1.12-pre-C** with `GenesisClassAdmitted` event + `GenesisLengthMismatch` / `GenesisOverflow` / `DuplicateGenesisToken` errors + `MAX_GENESIS_CLASSES` constant per **G-D20**.)

- **Cursor prompt:** Create `src/gauge/VaultClassRegistry.sol` with type / state / constants / error / event / setter-stub surface per G-D9 + G1.9 NOTES lock; no constructor body, no propose / veto / revoke logic.

#### G1.12-pre-C — `VaultClassRegistry` scaffold supplement (`src/gauge/VaultClassRegistry.sol`)

Sonnet-tier mechanical supplement to the G1.11 scaffold (commit `061d967`) per **G-D20** lock. Adds: one event `GenesisClassAdmitted(address indexed token, AdmissionType admissionType)`; three errors `GenesisLengthMismatch(uint256 tokensLen, uint256 typesLen)`, `GenesisOverflow(uint256 provided, uint256 cap)`, `DuplicateGenesisToken(address token)`; one constant `uint256 public constant MAX_GENESIS_CLASSES = 32` (placed in the constants block alongside `PROPOSAL_BOND_SVZCHF` / `VETO_THRESHOLD_BPS` / `VETO_WINDOW_BLOCKS`). No constructor body, no functions — those land at G1.12. Pure additive surface; nothing in the G1.11 scaffold changes. `forge build` green at sub-step exit.

- **Cursor prompt:** Append the four declarations (one event, three errors, one constant) to the existing `src/gauge/VaultClassRegistry.sol` per **G-D20** scaffold supplement; pure additive; constant lands in the constants block, errors at the end of the existing errors block, event at the end of the existing events block.

#### G1.12 — Constructor + genesis seeding + one-shot setters (`src/gauge/VaultClassRegistry.sol`)

Fills the constructor (genesis class admission via constructor-injected `memory` arrays per **G-D9** Option a-prime + **G-D20** validation discipline) and the one-shot setters for `IAuMT` + `governanceContract`. Constructor signature: `constructor(IERC20 svZCHF_, SwapAndDepositToBodensee helper_, address auMTSetter_, address governanceSetter_, address[] memory genesisTokens, AdmissionType[] memory genesisTypes)`. Zero-address checks on each of the four scalar inputs (revert `ZeroAddress`); assign immutables / setter slots. Genesis loop per **G-D20** validation discipline: revert `GenesisLengthMismatch(genesisTokens.length, genesisTypes.length)` on length disagreement; revert `GenesisOverflow(genesisTokens.length, MAX_GENESIS_CLASSES)` if length exceeds the cap; per-iteration `revert ZeroAddress()` on zero-address token; per-iteration `revert BytecodeHashAdmissionDeferred()` if `genesisTypes[i] == AdmissionType.BytecodeHash` (per G-D20 step 6 / G-D9 Bytecode-hash deferral lock — constructor genesis guard added at G1.13-pre-B; lands at G1.13 source change); revert `DuplicateGenesisToken(genesisTokens[i])` if `admittedClasses[genesisTokens[i]]` already true; set `admittedClasses[genesisTokens[i]] = true` and `admissionTypes[genesisTokens[i]] = genesisTypes[i]`; emit `GenesisClassAdmitted(genesisTokens[i], genesisTypes[i])`. `GenesisClassAdmitted` is distinct from `VaultClassFinalized` per **G-D20** rationale (genesis classes have no `proposalId`; reusing `VaultClassFinalized` would require a synthetic sentinel — avoided by the dedicated event). `setAuMT(address auMT_)`: gated to `auMTSetter` via named error `OnlyAuMTSetter()` (per **G-D9** G1.12-pre-D F-D23 setter-pattern lock; distinct from `OnlyGovernance(address)` which gates `revokeVaultClass` post-setter). Body order: gate → `ZeroAddress()` on the new binding → assign (`auMT = IAuMT(auMT_);`) → clear setter slot (`auMTSetter = address(0);`). `setGovernanceContract(address governanceContract_)`: symmetric, gated to `governanceSetter` via `OnlyGovernanceSetter()`. The setter-slot clear at end of body is the structural seal — subsequent calls revert `OnlyAuMTSetter()` / `OnlyGovernanceSetter()` because the slot is `address(0)` post-clear. `SetterAlreadyCalled` declared in the G1.11 scaffold is **not** the one-shot seal revert under this pattern; structurally unreachable in `setAuMT` / `setGovernanceContract`, declared-but-unused, removal deferred to G5.x. Pre-setter, the veto path (consumes `IAuMT.governanceWeight`) and `revokeVaultClass` (gated to `governanceContract`) are structurally unreachable.

- **Cursor prompt:** Add constructor (memory-array genesis loop per **G-D20** validation discipline) + `setAuMT` + `setGovernanceContract` per **G-D9** Option a-prime + F-D23 setter pattern (gate-revert errors `OnlyAuMTSetter()` / `OnlyGovernanceSetter()` per **G-D9** G1.12-pre-D lock; declared as part of G1.12 alongside the setter implementations); concrete genesis class set passed in by the deploy script / fork-test setUp, not hardcoded.

#### G1.13 — `proposeVaultClass` + bond routing via helper (`src/gauge/VaultClassRegistry.sol`)

Fills the public proposal entry point per **G-D9**. `proposeVaultClass(AdmissionType admissionType, address admissionValue, bytes32 constraintsHash) external returns (uint256 proposalId)`: revert `BytecodeHashAdmissionDeferred()` if `admissionType == AdmissionType.BytecodeHash` (per G-D9 Bytecode-hash deferral lock — on-chain admission semantics deferred until bytes32-keyed storage + `isAdmittedClass(token)` discipline lock; see NOTES); validate `admissionValue != address(0)` (revert `ZeroAddress`); check `!admittedClasses[admissionValue]` (revert `ClassAlreadyAdmitted` if already admitted); pull `PROPOSAL_BOND_SVZCHF` svZCHF from `msg.sender` via `safeTransferFrom`; push to helper via `safeTransfer`; call `helper.donate(svZCHF, PROPOSAL_BOND_SVZCHF)` per **G-D21** variable-amount entry point (G-D12 caller-pre-push pattern; single call, not loop — magnitude validation is upstream in the registry per G-D21 trust boundary, not in the helper); allocate `proposalId = nextProposalId++`; populate the `VaultClassProposal` struct (`createdBlock = block.number`, `vetoSupport = 0`, `finalized = false`, `revoked = false`); emit `VaultClassProposed`. No upfront approval check — tx reverts at `safeTransferFrom` if approval missing. No state mutation of `admittedClasses` — that happens at finalize. Bond is non-refundable (one-way to Bodensee via DONATION). Enum-bound validity for `admissionType` is enforced by Solidity's ABI codec at decode time (`Panic(0x21)` on out-of-range) — no explicit body-level guard needed; see G-D9 enum-bound discipline (NOTES) and the structural-unreachability parallel with `SetterAlreadyCalled`. G1.13 source-file scope (per G-D20 "Subsequent surface added at G1.13" paragraph): (a) declare new error `error BytecodeHashAdmissionDeferred();` in the existing error block; (b) modify constructor genesis loop to insert step 3.b `BytecodeHashAdmissionDeferred()` guard between zero-address check and deduplication check; (c) correct L57 struct Natspec on `admissionValue` field — drop the "sentinel/zero for BytecodeHash (constraints-only path)" claim, replace with a "reserved for future use under BytecodeHash (deferred at G1.13-pre-B)" form. All four source changes (constructor modification + new error + struct Natspec correction + new `proposeVaultClass` function) land in one G1.13 commit; forge build is the joint audit checkpoint.

- **Cursor prompt:** Add `proposeVaultClass` per G-D9 mechanism (bond routing via helper using G-D12 caller-pre-push pattern, proposal struct allocation, event emission, `BytecodeHashAdmissionDeferred()` runtime guard at top of body per G-D9 Bytecode-hash deferral lock); declare new `error BytecodeHashAdmissionDeferred();` in the error block; modify constructor genesis loop to insert step 3.b BytecodeHash guard per G-D20 step 6; correct L57 struct Natspec on `admissionValue` field per G-D20 "Subsequent surface added at G1.13".

#### G1.14 — `vetoProposal` + auto-finalize on window expiry (`src/gauge/VaultClassRegistry.sol`)

Fills `vetoProposal` + `finalizeProposal` per **G-D9** "Veto window" + "Auto-finalize". `vetoProposal(uint256 proposalId) external`: revert `ProposalAlreadyFinalized` if `finalized || revoked`; revert `VetoWindowExpired` if `block.number > proposal.createdBlock + VETO_WINDOW_BLOCKS`; read AuMT-weighted veto weight via `auMT.governanceWeight(msg.sender)` (name locked: `governanceWeight` per `src/token/IAuMT.sol`; pre-`setAuMT`, the placeholder `auMT == IAuMT(address(0))` returns zero weight — vetoes structurally impossible); add to `proposal.vetoSupport`; if `(proposal.vetoSupport * 10_000) / auMT.totalSupply() >= VETO_THRESHOLD_BPS` (where `VETO_THRESHOLD_BPS = 1000` = 10.00% per G-D19 Tunable 2; `auMT.totalSupply()` is the AuMT canonical total supply at veto time; pre-`setAuMT`, `auMT` returns zero weight so this branch is unreachable), set `proposal.finalized = true; proposal.revoked = true;` (proposal is dead, bond stays in Bodensee per the G-D9 "no refund" rule); emit `VaultClassVetoed`. `finalizeProposal(uint256 proposalId) external`: revert `ProposalAlreadyFinalized` if finalized; revert `VetoWindowOpen` if `block.number <= proposal.createdBlock + VETO_WINDOW_BLOCKS`; set `proposal.finalized = true`, `admittedClasses[proposal.admissionValue] = true`, `admissionTypes[proposal.admissionValue] = proposal.admissionType`; emit `VaultClassFinalized`. "Auto-finalize" per G-D9: not a separate timer — the first call to `finalizeProposal` after the veto window expires executes the state transition; permissionless caller. Single-tx state transition matches G-D9's "no explicit two-stage `finalize`-then-`execute`."

- **Cursor prompt:** Add `vetoProposal` (AuMT-weighted veto, threshold finalization, window-expiry guard) + `finalizeProposal` (window-expiry guard, admission-state set, event emission) per G-D9.

#### G1.15 — `revokeVaultClass` + grandfather logic (`src/gauge/VaultClassRegistry.sol`)

Fills `revokeVaultClass(address admissionValue) external onlyGovernance` per **G-D9** "Revocable-with-grandfather." `onlyGovernance` modifier: `if (msg.sender != governanceContract) revert OnlyGovernance(msg.sender);`. Pre-`setGovernanceContract`, the placeholder address rejects all calls — structurally unreachable. Function body: check `admittedClasses[admissionValue]` (revert `ClassNotAdmitted` otherwise); set `admittedClasses[admissionValue] = false`; emit `VaultClassRevoked`. "Grandfather policy" per G-D9: revocation blocks new numerator credit at the next epoch boundary per **G-D8** canonical definition; existing pools holding the now-revoked class lose numerator credit at the boundary; if they fall below 52%, they enter the standard graduated grace period from `08_bootstrap.md` §xxiii. The grace-period machinery itself is a `GaugeRegistry` (G3) concern; `revokeVaultClass` does not touch any pool state.

- **Cursor prompt:** Add `revokeVaultClass` per G-D9 — `onlyGovernance` gate, admission-flag flip, event emission; no pool state mutation (grace-period logic lives at GaugeRegistry G3).

#### G1.15-post — `IVaultClassRegistry` inheritance + view bridges + scaffold NatSpec correction (`src/gauge/VaultClassRegistry.sol`)

Source-only sub-step closing the scaffold-promise gap surfaced at **G15** (NOTES, commit `90111f4`). The G1.11 scaffold's top-of-contract NatSpec at L13–L15 declared that `IVaultClassRegistry` inheritance + view bridges would land at G1.12+; G1.12 / G1.13 / G1.14 / G1.15 each landed only their named scope, leaving the inheritance and bridges undelivered while G2.3 (PLAN L293) hard-requires the typed call `IVaultClassRegistry(vaultClassRegistry).isAdmittedClass(...)`. Four changes land in one commit. (1) Contract declaration becomes `contract VaultClassRegistry is IVaultClassRegistry`. (2) `function isAdmittedClass(address token) external view returns (bool) { return admittedClasses[token]; }` — thin forward to the existing `admittedClasses` storage mapping; satisfies **IVaultClassRegistry** L20 interface surface. (3) `function admissionType(address token) external view returns (IVaultClassRegistry.AdmissionType) { return admissionTypes[token]; }` — thin forward to the existing `admissionTypes` storage mapping; satisfies **IVaultClassRegistry** L29 interface surface. The function name `admissionType` (singular) does not collide with the state variable `admissionTypes` (plural); the auto-generated getter is `admissionTypes(address)`, not `admissionType(address)`, so the interface and storage co-exist without shadowing. (4) Rewrite the L13–L15 NatSpec block to drop the now-stale "land at G1.12+" forward-looking promise; replace with a present-tense statement of file scope ("ships full propose-veto-finalize-revoke surface per G-D9 + G-D19 (G1.9 tunables lock); inherits `IVaultClassRegistry` per G1.10 surface"). View bridges placed in a new "View bridges (`IVaultClassRegistry`)" banner section between the existing one-shot setters block and the public proposal entry point — read-then-write file ordering preserved. `forge build` green at sub-step exit (joint audit checkpoint covers inheritance conformance + bridge compilation + struct-field-vs-function-name disambiguation). G15 default rule (NOTES) applies forward to all close-of-family sub-steps.

- **Cursor prompt:** Add `is IVaultClassRegistry` to contract declaration; add `isAdmittedClass(address)` + `admissionType(address)` view bridges forwarding to existing `admittedClasses` / `admissionTypes` mappings per **G15** (NOTES) resolution; rewrite L13–L15 NatSpec to present-tense ("ships full propose-veto-finalize-revoke surface; inherits `IVaultClassRegistry`"); place bridges in new "View bridges (`IVaultClassRegistry`)" banner between one-shot setters and public proposal entry point.

#### G1.16 — `VaultClassRegistry` unit tests (`test/unit/VaultClassRegistry.t.sol`)

Lands unit invariants per the test matrix in `STAGE_G_NOTES.md` plus governance-restricted entry points. Proposal-and-veto state machine: propose → veto with sufficient weight → finalized-as-revoked; propose → veto with insufficient weight → window expires → finalize → admitted. Three admission fingerprints exercised (`ImplementationAddress` / `FactoryAddress` / `BytecodeHash`). Bond routing to der Bodensee via helper: mock helper to verify `swapAndDeposit(svZCHF, PROPOSAL_BOND_SVZCHF)` was called with correct args. Genesis-seeded class assertion: constructor admits the locked genesis set; `isAdmittedClass(genesisToken) == true` for each. Veto-window guards: `vetoProposal` reverts `VetoWindowExpired` after window; `finalizeProposal` reverts `VetoWindowOpen` before window. `revokeVaultClass`: governance-gated; `admittedClasses[token] = false` after revoke; pre-revoke admittedness asserted. Placeholder-then-one-shot-setter unreachability: pre-`setAuMT`, `vetoProposal` cannot reach finalization (placeholder `IAuMT` returns zero weight); pre-`setGovernanceContract`, `revokeVaultClass` reverts `OnlyGovernance(placeholder)`. Mock fixtures: `MockSwapAndDepositToBodensee`, `MockAuMT` (controllable per-address `governanceWeight` returns), `MockGovernance` (acts as `governanceContract` post-setter). Pure unit; no mainnet fork.

- **Cursor prompt:** Create `test/unit/VaultClassRegistry.t.sol` covering proposal + veto + finalize + revoke + genesis-seed + placeholder-unreachability invariants per G-D9 + G1.9 lock; pure unit (no fork).

### G2 — GaugeEligibility + F-10 ranking

G2 ships `GaugeEligibility.sol` + `IGaugeEligibility.sol` — the epoch-boundary eligibility evaluator. It reads pool weights via `IBasePool.getNormalizedWeights()`, classifies each token via ERC-4626 detection (**G-D10**), checks class admission in `VaultClassRegistry` (**G-D8**), enforces the $10K TVL 7-day-SMA floor (**OQ-G2**), applies the pool-type whitelist (**G-D6**), blocks forbidden tokens (**T-I3**), runs the F-10 efficiency tournament cohort assignment (**G-D3**), and emits threshold transition events (**G-D5**). The contract is primarily read-path logic; the only state written is the per-pool eligibility latch and cohort assignment, updated at epoch boundaries.

Sub-row layout: G2.0 locks pool-type whitelist enum + F-10 caps disposition + $10K TVL floor encoding in NOTES; G2.1–G2.2 deliver the interface + scaffold; G2.3–G2.4 fill the constructor + 52% numerator + eligibility-criteria gates; G2.4-post + G2.5-pre-1 + G2.5-pre-2 land the F-D23 gaugeRegistry setter + IEfficiencyOracle sibling oracle + event ABI reshape + firstTournamentEpoch cold-start grace per **G-D22** + **G-D23**; G2.5 fills the F-10 efficiency tournament + cohort assignment + transition events; G2.6 delivers the public eligibility surface; G2.7 delivers unit tests, decomposed into G2.7a (mocks + fixture + wiring/constructor/setter tests) + G2.7b (`evaluateEligibility` matrix) + G2.7c (`computeEpochSnapshot` matrix) per CCBMultiplier.t.sol F3.4-fix lineage + the **G15** ambiguity-gate concern around large single saves (ABI-stable per-pass Must-match contracts — mock signatures lock at G2.7a + behavior-only configuration at G2.7b/c).

#### G2.0 — GaugeEligibility tunables lock (`docs/STAGE_G_NOTES.md`; NOTES-only)

Resolves the G-D6 "exact pool-type enum locks at G1.x" residual and the G-D3 "caps clause deferred to G1.x" residual ahead of any source consuming them. NOTES-only sub-step — no source, no test. Three decisions to record as **G-D15** (or next available NOTES number):

- **Pool-type whitelist enum** — the set of factory-derived pool types eligible for `activateGauge`. Candidates: Balancer V3 `WeightedPool` (Stage E `WeightedPoolFactory`), `StablePool` if deployed, any additional factory-derived types confirmed at G2.0 entry. Exact enum locked against deployed Stage E factory addresses. Fails-closed per G-D6.
- **F-10 caps clause** — whether to retain the numeric caps (15/10/5 → 1%/0.5%/0.1%) from `11_formulas.md` F-10 as anti-concentration controls within the favored cohort, drop them as unnecessary complexity, or replace with a single flat cap. Decision at sub-step entry in Opus; if retained, exact cap values and application logic lock at G-D15.
- **$10K TVL floor encoding** — OQ-G2 is USD-denominated; concrete encoding is svZCHF-denominated 18-decimal (`TVL_FLOOR_SVZCHF`). Candidate concrete value: `10_000e18` (10 000 svZCHF, treating svZCHF ≈ 1 USD for the anti-spam floor gate — rough magnitude, not oracle-precise equivalence). Confirm at sub-step entry against OQ-G2 + OQ-22 framing.

If any decision is still open at G2.0 entry, resolution happens in chat (Opus) before the Cursor prompt fires. Mode dispatch per §13 routes the NOTES write itself to Sonnet.

- **Cursor prompt:** Add **G-D15** (GaugeEligibility tunables lock) to `docs/STAGE_G_NOTES.md` — pool-type whitelist enum + F-10 caps disposition + `TVL_FLOOR_SVZCHF` encoding with rationale + constraints; NOTES-only — no `src/gauge/` edit, no PLAN edit beyond cross-link entry, no test edit.

#### G2.1 — `IGaugeEligibility` interface (`src/gauge/IGaugeEligibility.sol`)

Lands the narrow read surface consumed by `GaugeRegistry` (G3) for pre-activation eligibility checks. Core functions: `evaluateEligibility(address pool) external returns (bool)` — the state-writing gate that `GaugeRegistry.activateGauge` calls, running all eligibility criteria and writing the per-pool latch; `isEligible(address pool) external view returns (bool)` — latched eligibility state, not re-evaluated on read; `cohortOf(address pool) external view returns (bool favored)` — returns `true` if pool is in the top-15% favored cohort at the last epoch snapshot; `snapshotEpoch() external view returns (uint256)` — the epoch index at which the current eligibility latch was computed per T-I5 epoch-snapshot determinism. Pragma `^0.8.26`; license `GPL-3.0-or-later`.

- **Cursor prompt:** Create `src/gauge/IGaugeEligibility.sol` with `evaluateEligibility`, `isEligible`, `cohortOf`, and `snapshotEpoch` function surface; narrow interface for `GaugeRegistry` consumption.

#### G2.2 — `GaugeEligibility.sol` scaffold (`src/gauge/GaugeEligibility.sol`)

Lands storage / imports / constants / custom errors / events per **G-D5** + **G-D6** + **G-D8** + **G-D15** (G2.0 lock). No constructor body, no functions. Pragma `^0.8.26`; license `GPL-3.0-or-later`. Imports: `IGaugeEligibility`, `IVaultClassRegistry`, `ITVLOracle` (Stage F shape reused), `IBasePool` (Balancer V3 — `getNormalizedWeights`), `IVault` (Balancer V3 — `getPoolTokens`; vault address stored as immutable; exact selector confirmed at G2.3 against submodule HEAD), `IERC4626` + `IERC20` from OpenZeppelin. Constants from G2.0 lock: `TVL_FLOOR_SVZCHF`, `FAVORED_COHORT_BPS = 1500`, `SMOOTHING_EPOCHS = 3`. Immutables: `address public immutable approvedFactory` (G-D15a singleton), `address public vaultClassRegistry`, `address public tvlOracle`, `address public vault`, `address internal _auMM`, `address internal _auMT`. Storage: `mapping(address => bool) public isGaugeEligible; mapping(address => bool) public isFavoredCohort; mapping(address => uint256) public lastSnapshotEpoch; uint256 public currentSnapshotEpoch;`. Events per G-D5 ABI verbatim (`GaugeEfficiencyDropped` / `GaugeEfficiencyRising`). Custom errors: `ZeroAddress`, `ForbiddenToken`, `PoolTypeNotWhitelisted`, `TVLFloorNotMet`, `InsufficientQualityGate`, `EfficiencyDataUnavailable`. **G2.4-post amendment (G-D22):** scaffold-time error / storage list extends post-G2.4 to add `OnlyGaugeRegistry(address caller)` + `OnlyGaugeRegistrySetter()` errors and `address public gaugeRegistry` + `address public gaugeRegistrySetter` storage (both non-immutable per F-D23 circular-deploy-dep); constructor arity extends from 6-arg at G2.3 to 7-arg at G2.4-post (appended `gaugeRegistrySetter_`). **G2.5-pre-2 amendment (G-D23):** scaffold-time import / immutable / event / storage extends post-G2.4-post to add `IEfficiencyOracle` import; `address public immutable efficiencyOracle` (mirrors `tvlOracle` — bound at construction, not post-deploy); events `GaugeEfficiencyDropped` + `GaugeEfficiencyRising` ABI reshaped from `(pool, epoch, tvlSma, efficiencyRatio)` to `(pool, epoch, numeratorSma, denominatorSma, efficiencyRatio)` per **G-D23 (iii)**; `mapping(address => uint256) public firstTournamentEpoch` storage added per **G-D23 (v)**; constructor arity extends from 7-arg at G2.4-post to 8-arg at G2.5-pre-2 (appended `efficiencyOracle_`).

- **Cursor prompt:** Create `src/gauge/GaugeEligibility.sol` with type / state / constants / error / event surface per G-D5 + G-D8 + G-D15; no constructor body, no functions.

#### G2.3 — Constructor + ERC-4626 detection + 52% Quality Gate (`src/gauge/GaugeEligibility.sol`)

Fills the constructor + `_compute52PctNumerator(IERC20[] memory tokens, uint256[] memory weights) internal view returns (uint256)` per **G-D8** + **G-D10**. Constructor: zero-address checks on `_approvedFactory`, `_vaultClassRegistry`, `_tvlOracle`, `_vault`, `_auMM`, `_auMT`; store immutables. `_compute52PctNumerator`: iterate `tokens` + `weights` in lock-step; for each pair: (i) T-I3 forbidden-token check first — `if (address(tokens[i]) == _auMM || address(tokens[i]) == _auMT) revert ForbiddenToken(address(tokens[i]));`; (ii) ERC-4626 detection via `try IERC4626(address(tokens[i])).asset() returns (address) { /* 4626-claiming */ } catch { /* plain ERC-20, no numerator contribution */ }` per G-D10; (iii) if 4626-claiming: `IVaultClassRegistry(vaultClassRegistry).isAdmittedClass(address(tokens[i]))` — admitted → `numerator += weights[i]`; not admitted → no contribution; (iv) return `numerator`. Caller fetches `(tokens, weights)` from `IVault(vault).getPoolTokens(pool)` + `IBasePool(pool).getNormalizedWeights()` before calling.

- **Cursor prompt:** Add constructor (forbidden-token immutables + zero-address checks) + `_compute52PctNumerator` internal (G-D10 try/catch ERC-4626, VaultClassRegistry admission lookup, weight accumulation); no TVL / whitelist logic yet.

#### G2.4 — TVL floor + pool-type whitelist + `_checkEligibilityCriteria` (`src/gauge/GaugeEligibility.sol`)

Fills `_checkEligibilityCriteria(address pool) internal view` — the aggregate binary gate run before tournament ranking. Internally fetches `IERC20[] memory tokens` from `IVault(vault).getPoolTokens(pool)` + `uint256[] memory weights` from `IBasePool(pool).getNormalizedWeights()`. Criterion order — cheapest-first per G-D10 note: (i) forbidden-token — inline `_compute52PctNumerator` call covers T-I3 reverts; (ii) pool-type whitelist — derive factory address via Balancer V3 pool-config read (exact selector locked at G2.4 sub-step entry against `lib/balancer-v3-monorepo` HEAD; candidate: `IVault(vault).getPoolFactoryAddress(pool)` or equivalent call chain); compare to `approvedFactory` (G-D15a singleton); revert `PoolTypeNotWhitelisted(factory)` if `factory != approvedFactory`; (iii) TVL floor — `uint256 tvl = ITVLOracle(tvlOracle).tvl(pool)`; revert `TVLFloorNotMet(tvl, TVL_FLOOR_SVZCHF)` if below; (iv) 52% Quality Gate — `uint256 numerator = _compute52PctNumerator(tokens, weights)`; revert `InsufficientQualityGate(numerator)` if `numerator < 0.52e18`.

- **Cursor prompt:** Add `_checkEligibilityCriteria` per OQ-G2 + G-D6 + G-D10 + G-D15; resolve pool-factory-address selector against `lib/balancer-v3-monorepo` HEAD at sub-step entry; criterion order: forbidden-token → pool-type → TVL floor → 52%-gate.

#### G2.4-post — F-D23 one-shot setter for `gaugeRegistry` + `onlyGaugeRegistry` modifier (`src/gauge/GaugeEligibility.sol`)

Lands the F-D23 plumbing required by **G-D22** (G2.4-post-A NOTES lock) ahead of `computeEpochSnapshot` at G2.5. Five changes in one source-only commit, mirroring `VaultClassRegistry.setAuMT` / `setGovernanceContract` at G1.12 literally: (1) constructor extends from 6-arg to 7-arg — appends `address gaugeRegistrySetter_` after `auMT_`; zero-address check + `gaugeRegistrySetter = gaugeRegistrySetter_` storage assignment; (2) two new storage slots — `address public gaugeRegistry` (post-deploy wiring target — **storage, not immutable**, due to the circular deploy dependency: `GaugeEligibility` is constructed before `GaugeRegistry` since `GaugeRegistry`'s constructor needs `IGaugeEligibility`) + `address public gaugeRegistrySetter` (one-shot setter authority, cleared after first call); (3) two new custom errors — `OnlyGaugeRegistry(address caller)` (gates `computeEpochSnapshot` at G2.5) + `OnlyGaugeRegistrySetter()` (gates `setGaugeRegistry`); (4) `setGaugeRegistry(address gaugeRegistry_) external` — caller must equal `gaugeRegistrySetter` (else revert `OnlyGaugeRegistrySetter`); zero-address check on `gaugeRegistry_`; bind `gaugeRegistry = gaugeRegistry_`; seal slot via `gaugeRegistrySetter = address(0)`; (5) `modifier onlyGaugeRegistry()` — `if (msg.sender != gaugeRegistry) revert OnlyGaugeRegistry(msg.sender);`. Modifier consumed at G2.5 only; no other surface reads `gaugeRegistry`. NatSpec for the new constructor arg + the setter + the modifier per Stage G prose register.

- **Cursor prompt:** Extend constructor to 7-arg (add `gaugeRegistrySetter_`); add `gaugeRegistry` + `gaugeRegistrySetter` storage (not immutable per G-D22 circular-deploy-dep); add `OnlyGaugeRegistry` + `OnlyGaugeRegistrySetter` errors; add `setGaugeRegistry` one-shot setter (mirrors `VaultClassRegistry.setAuMT` literally); add `onlyGaugeRegistry` modifier.

#### G2.5-pre-1 — `IEfficiencyOracle` interface (`src/gauge/IEfficiencyOracle.sol`)

Lands the F-10 efficiency-tournament oracle interface — sibling to `ITVLOracle` per **G-D23 (ii)**. Single function: `efficiencyInputs(address pool) external view returns (uint256 numeratorSma, uint256 denominatorSma)` — returns the 3-epoch SMA-smoothed numerator (`swap_fee_revenue_i + yield_fee_revenue_i`) and denominator (`emissions_received_i`) of F-10 per **OQ-G1** canonical formula. Oracle owns smoothing per **G-D23 (i)**; contract reads pre-smoothed inputs at `computeEpochSnapshot`. **Both quantities expressed in the same unit per F-10 canonical spec — ratio is dimensionless, NatSpec must not lock svZCHF.** Concrete implementation deferred to Stage H emission-distributor wiring (mirrors the OQ-22 carry-forward for `ITVLOracle` per `CLAUDE.md` §11 deferred items). Pragma `^0.8.26`; license `GPL-3.0-or-later`.

- **Cursor prompt:** Create `src/gauge/IEfficiencyOracle.sol` with `efficiencyInputs(address pool) external view returns (uint256 numeratorSma, uint256 denominatorSma)`; NatSpec cites F-10 price-agnostic property (numerator + denominator in same unit, ratio dimensionless) per G-D23 (ii); sibling shape to `ITVLOracle`; concrete implementation deferred to Stage H.

#### G2.5-pre-2 — `IEfficiencyOracle` + `firstTournamentEpoch` + event-ABI reshape wiring (`src/gauge/GaugeEligibility.sol`)

Lands the G2.5 source-side dependencies per **G-D23**, five edits in one source-only commit mirroring the G2.4-post amendment pattern literally: (1) import `IEfficiencyOracle` from `./IEfficiencyOracle.sol`; (2) add `address public immutable efficiencyOracle` (mirrors `tvlOracle` immutable shape — bound at construction, not post-deploy, since `IEfficiencyOracle`'s concrete implementation lives outside the `GaugeEligibility` ↔ `GaugeRegistry` circular dependency); (3) constructor extends from 7-arg at G2.4-post to 8-arg — appends `address efficiencyOracle_` after `gaugeRegistrySetter_`; zero-address check + immutable bind; (4) reshape events `GaugeEfficiencyDropped` + `GaugeEfficiencyRising` from `(address indexed pool, uint256 indexed epoch, uint256 tvlSma, uint256 efficiencyRatio)` to `(address indexed pool, uint256 indexed epoch, uint256 numeratorSma, uint256 denominatorSma, uint256 efficiencyRatio)` per **G-D23 (iii)** — interior-only ABI break (no external consumers at G2.5-pre-2; G-D5 design-table row updated in tandem at G2.5-pre-B-3); (5) add storage `mapping(address => uint256) public firstTournamentEpoch` per **G-D23 (v)** — single-purpose cold-start grace predicate, set once on first ranked appearance in `computeEpochSnapshot` at G2.5, never overloaded with `lastSnapshotEpoch` semantics. NatSpec banner refresh: extend `@dev` to note G2.5-pre wiring; constructor param + immutable + storage NatSpec per Stage G prose register. No function bodies modified — `computeEpochSnapshot` itself lands at G2.5.

- **Cursor prompt:** Extend `src/gauge/GaugeEligibility.sol` per G-D23 — add `IEfficiencyOracle` import; add `address public immutable efficiencyOracle`; constructor 7→8 arg (append `efficiencyOracle_` after `gaugeRegistrySetter_` + zero-address check + immutable bind); reshape `GaugeEfficiencyDropped`/`GaugeEfficiencyRising` event params (`tvlSma` → `numeratorSma` + `denominatorSma`); add `mapping(address => uint256) public firstTournamentEpoch` storage; NatSpec banner + new-symbol NatSpec refresh; no `computeEpochSnapshot` body yet.

#### G2.5 — F-10 efficiency tournament + cohort assignment + transition events (`src/gauge/GaugeEligibility.sol`)

Fills `computeEpochSnapshot(address[] calldata eligiblePools) external` — the epoch-boundary entry point per **G-D3** + **OQ-G1** + **T-I5** + **G-D5** + **G-D22** + **G-D23**. Caller restriction locked at **G-D22** (G2.4-post-A NOTES) + plumbed at G2.4-post: `onlyGaugeRegistry` via the F-D23 one-shot setter — gates against cohort biasing + epoch counter grinding per T-I5 epoch-snapshot determinism. Let `newEpoch = currentSnapshotEpoch + 1`. For each pool in `eligiblePools`: (1) read `(uint256 numeratorSma, uint256 denominatorSma) = IEfficiencyOracle(efficiencyOracle).efficiencyInputs(pool)` per **G-D23 (i)** — the F-10 canonical encoding `(swap_fee_revenue_i + yield_fee_revenue_i) / emissions_received_i` per **OQ-G1**; oracle owns the 3-epoch SMA, contract reads pre-smoothed inputs; (2) zero-handling precedence per **G-D23 (v)**: cold-start grace fires first — if `firstTournamentEpoch[pool] == 0`, set `firstTournamentEpoch[pool] = newEpoch` and skip the pool (no cohort update, no transition event, no revert); else if `(newEpoch - firstTournamentEpoch[pool]) < SMOOTHING_EPOCHS`, skip the pool (still within warmup); else if `denominatorSma == 0`, revert `EfficiencyDataUnavailable(pool)` (post-warmup zero is an oracle-data fault, not a pool fault); else compute `efficiency_ratio = (numeratorSma * 1e18) / denominatorSma` (1e18 fixed-point, dimensionless per F-10 price-agnostic property). Collect the surviving (post-grace) pool / ratio pairs in memory; sort descending by `efficiency_ratio` via in-memory insertion sort per **G-D23 (iv)** with address-ascending tiebreak on equal ratios — locks **T-T3** stable-sort determinism; no storage write during sort. Compute cutoff: `favored_count = (N_ranked * 15 + 99) / 100` where `N_ranked` is the post-grace survivor count (ceiling arithmetic per G-D3). No per-pool emission-share caps applied in Stage G — F-10 caps deferred to Stage H emission distributor per **G-D15b**. For each ranked pool: compare new cohort to previous `isFavoredCohort[pool]`; emit `GaugeEfficiencyDropped(pool, newEpoch, numeratorSma, denominatorSma, efficiencyRatio)` on top-to-bottom crossing (**T-T2**); emit `GaugeEfficiencyRising(pool, newEpoch, numeratorSma, denominatorSma, efficiencyRatio)` on bottom-to-top crossing (**T-T1**) — exactly once per pool per epoch per **G-D5** with reshaped ABI per **G-D23 (iii)**. Update `isFavoredCohort[pool]` + `lastSnapshotEpoch[pool] = newEpoch` for every ranked pool. At function end, increment `currentSnapshotEpoch = newEpoch`.

- **Cursor prompt:** Add `computeEpochSnapshot` per G-D3 + OQ-G1 + T-I5 + G-D5 + G-D22 + G-D23; per-pool read of `(numeratorSma, denominatorSma)` via `IEfficiencyOracle.efficiencyInputs(pool)` per G-D23 (i); zero-handling precedence per G-D23 (v) — cold-start grace via `firstTournamentEpoch[pool]` first (set-once + skip if `(newEpoch - firstTournamentEpoch[pool]) < SMOOTHING_EPOCHS`), then `EfficiencyDataUnavailable(pool)` revert on post-warmup zero `denominatorSma`, then `efficiency_ratio = (numeratorSma * 1e18) / denominatorSma`; in-memory insertion sort over post-grace survivors with address-ascending tiebreak per G-D23 (iv) / T-T3; ceiling 15% cutoff `favored_count = (N_ranked * 15 + 99) / 100`; transition-event emission `(pool, newEpoch, numeratorSma, denominatorSma, efficiencyRatio)` exactly once per crossing per G-D23 (iii); updates `isFavoredCohort` + `lastSnapshotEpoch = newEpoch` + first-time `firstTournamentEpoch`; carries `onlyGaugeRegistry` modifier per G-D22 / G2.4-post; no cap application (F-10 caps deferred to Stage H per G-D15b); increments `currentSnapshotEpoch = newEpoch` at function end.

#### G2.6 — Public eligibility surface (`src/gauge/GaugeEligibility.sol`)

Fills the `IGaugeEligibility` interface implementations: `evaluateEligibility(address pool) external returns (bool)` — calls `_checkEligibilityCriteria(pool)` (reverts on any criteria failure); writes `isGaugeEligible[pool] = true` and `lastSnapshotEpoch[pool] = currentSnapshotEpoch`; returns `true`. `isEligible(address pool) external view returns (bool)` — returns `isGaugeEligible[pool]` latched state. `cohortOf(address pool) external view returns (bool favored)` — returns `isFavoredCohort[pool]`. `snapshotEpoch() external view returns (uint256)` — returns `currentSnapshotEpoch`. T-I5 epoch-snapshot determinism: two reads at the same block return identical latched state — eligibility is written only by `evaluateEligibility` (per-pool first-pass write) and `computeEpochSnapshot` (epoch-boundary write), never on external view reads.

- **Cursor prompt:** Add `evaluateEligibility` + `isEligible` + `cohortOf` + `snapshotEpoch` per IGaugeEligibility interface; `evaluateEligibility` writes latch via `_checkEligibilityCriteria`; view functions return latched state only.

#### G2.7 — `GaugeEligibility` unit tests (`test/unit/GaugeEligibility.t.sol`)

Lands unit invariants per the test matrix in `STAGE_G_NOTES.md`: 52% numerator across mixed token sets (admitted 4626 + non-admitted 4626 + plain ERC-20 combinations); ERC-4626 detection — non-reverting `asset()` triggers class-lookup path, reverting gives plain ERC-20 path; T-I3 forbidden-token block (AuMM / AuMT in any pool token position reverts `ForbiddenToken`); T-I4 fast-track selector absent (`GaugeEligibility` bytecode has no Sandbox fast-track function — ABI-level check); G-D6 pool-type whitelist fails-closed (unrecognized factory reverts `PoolTypeNotWhitelisted`); OQ-G2 TVL-floor gate (below-floor reverts `TVLFloorNotMet`; borderline-floor passes); T-T1 — `GaugeEfficiencyRising` emitted exactly once on bottom-to-top cohort crossing; T-T2 — `GaugeEfficiencyDropped` emitted exactly once on top-to-bottom crossing; T-T3 tie-break — equal `efficiency_ratio` produces stable sort by deterministic tiebreak (address ascending); T-I5 epoch-snapshot determinism — `isEligible` reads at the same block return identical latched state. Mocks: `MockVaultClassRegistry` (controllable `isAdmittedClass`), `MockTVLOracle` (per-pool TVL returns for the OQ-G2 floor gate), `MockEfficiencyOracle` (per-pool `(numeratorSma, denominatorSma)` pair returns for the F-10 tournament per **G-D23 (i)**), `MockBasePoolFactory` (controllable `isPoolFromFactory` for G-D6 whitelist), `MockWeightedPool` (controllable `getNormalizedWeights`). Pure unit; no mainnet fork. **Decomposed into G2.7a/b/c** per CCBMultiplier.t.sol F3.4-fix lineage + the **G15** ambiguity-gate concern around large single saves drifting across "named scope" assertions — three size-bounded passes with ABI-stable per-pass Must-match contracts: mock surfaces lock at G2.7a and are not signature-modified by G2.7b/c (only behavior-configured via the setters). G2.7a closes with `forge build` green and the mock/fixture/wiring/setter/constructor test contract running; G2.7b appends `evaluateEligibility`-path tests against the same mocks; G2.7c appends `computeEpochSnapshot`-path tests against the same mocks. Completion log records three commits under the G2.7 umbrella.

- **Cursor prompt:** No direct Cursor work for the umbrella row — see G2.7a / G2.7b / G2.7c sub-rows below.

#### G2.7a — Mocks + fixture + wiring/constructor/setter tests (`test/unit/GaugeEligibility.t.sol`)

Lands the file scaffold: five inline mock contracts at the top of the file plus the abstract test fixture base + `setUp` that deploys the full wired stack. **Mocks (ABI-stable across G2.7a/b/c — signatures locked here are not modified at G2.7b/c; behavior is configured only via the setters declared here):** `MockVaultClassRegistry` implements `IVaultClassRegistry` with `setAdmittedClass(address token, bool admitted)` setter + `isAdmittedClass(address token) external view returns (bool)` + `admissionType(address token) external view returns (IVaultClassRegistry.AdmissionType)` (defaults to `ImplementationAddress`); `MockTVLOracle` implements `ITVLOracle` with `setTvl(address pool, uint256 value)` setter + `tvl(address pool) external view returns (uint256)`; `MockEfficiencyOracle` implements `IEfficiencyOracle` with `setEfficiencyInputs(address pool, uint256 numeratorSma, uint256 denominatorSma)` setter + `efficiencyInputs(address pool) external view returns (uint256, uint256)`; `MockBasePoolFactory` implements `IBasePoolFactory` with `setPoolFromFactory(address pool, bool isFromFactory)` setter + `isPoolFromFactory(address pool) external view returns (bool)` plus minimal stubs for any other `IBasePoolFactory` selectors required by interface conformance; `MockWeightedPool` implements `IWeightedPool` with `setNormalizedWeights(uint256[] memory weights)` setter + `getNormalizedWeights() external view returns (uint256[] memory)` plus minimal stubs for any inherited interface conformance. `IVault.getPoolTokens` reads are stubbed via `vm.mockCall` per `VaultClassRegistry.t.sol` precedent (no full `IVault` mock needed). **Fixture:** abstract `GaugeEligibilityFixture` base with `auMM` + `auMT` test addresses + mock instances + `GaugeEligibility` instance + `gaugeRegistry` test address (one-shot setter target) declared as storage; `setUp` deploys mocks → deploys `GaugeEligibility` with 8-arg constructor → calls `setGaugeRegistry(gaugeRegistry)` from `gaugeRegistrySetter` to seal the post-deploy wiring slot. **Test contract `GaugeEligibilityWiringTest` extends fixture:** (i) constructor zero-address sweep — 8 reverts, one per arg (`approvedFactory_`, `vaultClassRegistry_`, `tvlOracle_`, `vault_`, `auMM_`, `auMT_`, `gaugeRegistrySetter_`, `efficiencyOracle_`), all assert `ZeroAddress` revert; (ii) `setGaugeRegistry` happy-path — call from `gaugeRegistrySetter`, assert `gaugeRegistry` storage updated + `gaugeRegistrySetter` cleared to `address(0)`; (iii) `setGaugeRegistry` non-setter caller — call from arbitrary address, assert `OnlyGaugeRegistrySetter` revert; (iv) `setGaugeRegistry` zero-address — call from setter with `address(0)`, assert `ZeroAddress` revert; (v) `setGaugeRegistry` post-seal — second call from original setter (now cleared), assert `OnlyGaugeRegistrySetter` revert. `forge build` must pass green after G2.7a save; `forge test --match-contract GaugeEligibilityWiringTest` must pass green; G2.7a tests are the only tests in the file until G2.7b appends.

- **Cursor prompt:** Create `test/unit/GaugeEligibility.t.sol` with five inline mocks (`MockVaultClassRegistry` / `MockTVLOracle` / `MockEfficiencyOracle` / `MockBasePoolFactory` / `MockWeightedPool`) + `GaugeEligibilityFixture` abstract base + `setUp` deploying the full wired stack + `GaugeEligibilityWiringTest` contract covering constructor zero-address sweep (8 reverts) + `setGaugeRegistry` happy-path / non-setter / zero-address / post-seal one-shot semantics; mock signatures locked here are not modified by G2.7b/c.

#### G2.7b — `evaluateEligibility` test matrix (`test/unit/GaugeEligibility.t.sol`)

Appends `GaugeEligibilityEvaluateTest` to the G2.7a-created file (no mock signature changes — only `setAdmittedClass` / `setTvl` / `setPoolFromFactory` / `setNormalizedWeights` state configuration calls): **T-I3** forbidden-token block — AuMM in pool position 0 reverts `ForbiddenToken(auMM)`; AuMT in pool position 1 reverts `ForbiddenToken(auMT)`; precedence over G-D10 4626 detection (forbidden check fires inside `_compute52PctNumerator` before `asset()` probe). **T-I4** fast-track selector absent — ABI-level check on `GaugeEligibility.sol` asserting absence of any Sandbox fast-track function selector (per **G-D6** + STAGE_G_NOTES test matrix **T-I4**). **G-D6** pool-type whitelist — `MockBasePoolFactory.setPoolFromFactory(pool, false)` causes `evaluateEligibility(pool)` to revert `PoolTypeNotWhitelisted(approvedFactory)`. **OQ-G2** TVL floor — `MockTVLOracle.setTvl(pool, 9_999e18)` reverts `TVLFloorNotMet(9_999e18, 10_000e18)`; `setTvl(pool, 10_000e18)` borderline-passes (`tvl >= TVL_FLOOR_SVZCHF`). **G-D10** ERC-4626 detection — plain ERC-20 token (`MockERC20` without `asset()`) hits the empty catch (no numerator contribution); ERC-4626 token (`MockERC4626`) admitted in `MockVaultClassRegistry` contributes its weight; ERC-4626 token non-admitted contributes zero. **52% Quality Gate** — four weighted-pool token sets: (a) all admitted 4626 → numerator = 1e18, passes; (b) admitted weights summing to 0.52e18 → numerator = 0.52e18, passes (borderline); (c) admitted weights summing to 0.51e18 → numerator = 0.51e18, reverts `InsufficientQualityGate(0.51e18)`; (d) all plain ERC-20 → numerator = 0, reverts `InsufficientQualityGate(0)`. **Positive-path latch** — successful `evaluateEligibility(pool)` writes `isGaugeEligible[pool] = true` + `lastSnapshotEpoch[pool] = currentSnapshotEpoch`; `isEligible(pool)` returns `true`. **T-I5** epoch-snapshot determinism — two `isEligible(pool)` reads at the same block return identical latched state (no `vm.roll` between reads). All tests configure mocks via setters declared at G2.7a only; no mock signature changes.

- **Cursor prompt:** Append `GaugeEligibilityEvaluateTest` to `test/unit/GaugeEligibility.t.sol` covering T-I3 forbidden-tokens (AuMM + AuMT positions), T-I4 fast-track selector absent (ABI-level), G-D6 whitelist fails-closed, OQ-G2 TVL floor (below + borderline), G-D10 4626 detection (plain ERC-20 + admitted + non-admitted), 52% gate (1e18 / 0.52e18 borderline / 0.51e18 revert / all-plain revert), positive-path latch (isGaugeEligible + lastSnapshotEpoch writes), T-I5 determinism; configures mocks only via G2.7a setters.

#### G2.7c — `computeEpochSnapshot` test matrix (`test/unit/GaugeEligibility.t.sol`)

Appends `GaugeEligibilitySnapshotTest` to the G2.7b-extended file (no mock signature changes — only `setEfficiencyInputs` state configuration calls and `computeEpochSnapshot` calls from the wired `gaugeRegistry` address): **Caller restriction** — non-`gaugeRegistry` caller reverts `OnlyGaugeRegistry(caller)` (covers EOA, `gaugeRegistrySetter`, arbitrary contract). **Cold-start grace per G-D23 (v)** — first sighting writes `firstTournamentEpoch[pool] = newEpoch` with no event emit, no `isFavoredCohort` mutation, no `EfficiencyDataUnavailable` revert (even with `denominatorSma == 0`). **Warmup window** — for `(newEpoch - firstTournamentEpoch[pool]) < SMOOTHING_EPOCHS`, pool skipped without revert and without event (regardless of `denominatorSma` value). **`EfficiencyDataUnavailable` revert** — post-warmup pool (`newEpoch - firstTournamentEpoch[pool] >= SMOOTHING_EPOCHS`) with `denominatorSma == 0` reverts `EfficiencyDataUnavailable(pool)`. **T-T1 `GaugeEfficiencyRising` emit** — pool not previously in favored cohort + post-warmup + ranked in top `favoredCount` → exactly one `GaugeEfficiencyRising(pool, newEpoch, numeratorSma, denominatorSma, efficiencyRatio)` emit per crossing. **T-T2 `GaugeEfficiencyDropped` emit** — pool previously in favored cohort + post-warmup + ranked outside top `favoredCount` → exactly one `GaugeEfficiencyDropped(pool, newEpoch, numeratorSma, denominatorSma, efficiencyRatio)` emit per crossing. **T-T3 tie-break** — two pools with equal `efficiencyRatio` (e.g. both `numeratorSma = 100e18` + `denominatorSma = 50e18` → ratio `2e18`) but different addresses sort lower-address-first per insertion-sort with address-ascending tiebreak; assert lower-address pool wins the favored slot when cutoff = 1. **Ceiling 15% cutoff** — survey at survivor counts `nRanked ∈ {1, 7, 20, 100}` asserting `favoredCount = (nRanked * 15 + 99) / 100` (i.e. `{1, 2, 3, 15}` respectively). **Monotonic `currentSnapshotEpoch`** — `snapshotEpoch()` increments by 1 per `computeEpochSnapshot` call. **`lastSnapshotEpoch[pool]` update discipline** — every ranked (post-grace) pool gets `lastSnapshotEpoch = newEpoch`; pools still in cold-start or warmup do NOT update `lastSnapshotEpoch`. **No-crossing tournament** — when cohort assignments don't change between epochs, NO transition events emit. Warmup window simulated by repeated `computeEpochSnapshot` calls from `gaugeRegistry` — epoch index is contract-internal counter, not block-based, so no `vm.roll` required. G2.7c closing state: `forge test --match-contract GaugeEligibility` green for all three test contracts (`GaugeEligibilityWiringTest` + `GaugeEligibilityEvaluateTest` + `GaugeEligibilitySnapshotTest`).

- **Cursor prompt:** Append `GaugeEligibilitySnapshotTest` to `test/unit/GaugeEligibility.t.sol` covering `onlyGaugeRegistry` revert, cold-start grace (no-event no-revert first sighting), warmup skip, `EfficiencyDataUnavailable` post-warmup zero-denominator, T-T1 rising emit, T-T2 dropped emit, T-T3 tiebreak (address-ascending), ceiling 15% cutoff over `nRanked ∈ {1, 7, 20, 100}`, monotonic `currentSnapshotEpoch`, `lastSnapshotEpoch` update discipline, no-crossing no-event invariant; configures mocks only via G2.7a setters; epoch advancement via repeated `computeEpochSnapshot` calls (contract-internal counter).

### G3 — GaugeRegistry + three activation paths

G3 ships `GaugeRegistry.sol` + extends `IGaugeRegistry.sol` (from Stage F `src/ccb/`) — the registry and state machine for all three gauge activation paths. Path 1: permissionless `activateGauge` pulls the OQ-G3 anti-spam fee (100 svZCHF, ERC-20 pull) and routes it to der Bodensee via the G-D12 SwapAndDeposit helper before running `GaugeEligibility`; fee is non-refundable on success or failed check per OQ-G3, so eligibility failure does not revert but emits `GaugeActivationFailed` and returns. Path 2: governance-only `registerGaugeFromComposition` registers a composition-replacement pool without eligibility check. Path 3: governance-only `seedFoundingPool` seeds Miliarium genesis pools at deploy with eligibility bypassed per **STAGE_G_PRECHECK_AUTO_GAUGE** C-4. No 90-day boost on any path per **G-D13**.

Sub-row layout: G3.0 locks three design decisions in NOTES (G-D16); G3.1 extends the Stage F `IGaugeRegistry` interface; G3.2–G3.5 fill the registry implementation (with corrective sub-steps **G3.3-fix1** + **G3.3-fix2** immediately following G3.3 to add the `abstract` keyword + `external override` modifiers to the implemented functions and to backfill the `MockGaugeRegistry` interface-expansion stubs per **G-D24**); G3.6 delivers unit tests.

#### G3.0 — GaugeRegistry design decisions (`docs/STAGE_G_NOTES.md`; NOTES-only)

Resolves three G3-scoped design decisions recorded as **G-D16** (or next available NOTES number). NOTES-only sub-step — no source, no test.

- **IGaugeRegistry extend-vs-replace (G-D16a)** — Stage F's `CCBMultiplier.sol` imports `src/ccb/IGaugeRegistry.sol` with a single `isGaugeApproved(address pool) returns (bool)` signature per F-D17. Option A: create a new `IGaugeRegistryV2` in `src/gauge/`; Stage F bytecode unchanged but interface fragments. Option B: extend the existing `src/ccb/IGaugeRegistry.sol` in-place with Stage G functions and enums; Stage F recompiles cleanly since `CCBMultiplier` only calls `isGaugeApproved`. Decision at G3.0 entry — default Option B (extend same file) to avoid interface fragmentation; confirm at Opus entry.
- **`seedFoundingPool` parameter shape (G-D16b)** — Miliarium genesis pools are known at deploy; Vault already holds token/weight metadata. Options: full basket (pool + token addresses + weights — redundant), scalar `address pool` per-call, or batch `address[]`. Decision at G3.0 entry — scalar `address pool` for `seedFoundingPool` plus a batch `seedFoundingPools(address[] calldata)` variant for seeding all 28 Miliarium pools in one governance transaction; confirm at Opus entry.
- **`activateGauge` fee-fail semantics (G-D16c)** — OQ-G3 specifies the anti-spam fee is "non-refundable on success or on any failed criteria check." EVM revert unwinds `safeTransferFrom`, returning the fee. To honor OQ-G3: `activateGauge` must NOT revert on eligibility failure — pull fee first (revert only on transfer failure, which is a precondition, not an eligibility check), route fee to Bodensee via SwapAndDeposit helper using the G-D12 caller-pre-push pattern, then run `gaugeEligibility.evaluateEligibility` in a `try/catch`; on catch or false return emit `GaugeActivationFailed(pool, reason)` and `return` without reverting; on success activate. Confirm at Opus entry.

- **Cursor prompt:** Add **G-D16** (GaugeRegistry design decisions: IGaugeRegistry extend-vs-replace + seedFoundingPool param shape + activateGauge fee-fail semantics per OQ-G3) to `docs/STAGE_G_NOTES.md`; NOTES-only — no `src/gauge/` edit, no PLAN edit beyond a cross-link entry, no test edit.

**G-D17** (Revoked is terminal at Stage G; no permissionless reactivation) binds three sub-steps below. G3.3, G3.4, and G3.5 enforce the invariant tactically — every Revoked-status pool encountered by `activateGauge`, `registerGaugeFromComposition`, or `seedFoundingPool` reverts `AlreadyRevoked(pool)`; no Stage G entrypoint writes Revoked → Active. G3.6 + G4.4 carry the matching negative-path coverage as **T-I6** in the test matrix — three test cases (one per activation path) asserting `AlreadyRevoked` against a Revoked-status pool. Recovery is out of scope for Stage G per **G-D17**; any future-stage Revoked → Active entrypoint must be on a different selector and gated to `onlyGovernanceContract` per the forward rule in `STAGE_G_NOTES.md`.

#### G3.1 — `IGaugeRegistry` interface (extend `src/ccb/IGaugeRegistry.sol`)

Extends the existing Stage F interface per **G-D16a**. Retains: `isGaugeApproved(address pool) external view returns (bool)` (Stage F compat surface; implementation returns `_gaugeStatus[pool] == GaugeStatus.Active`). Adds enums: `enum GaugeStatus { None, Active, Revoked }` and `enum GaugeActivationPath { Permissionless, Composition, Founding }`. Adds functions: `activateGauge(address pool) external` — permissionless, ERC-20 fee pull (not `payable`); `registerGaugeFromComposition(address pool) external` — governance-only; `seedFoundingPool(address pool) external` — governance-only; `seedFoundingPools(address[] calldata pools) external` — governance-only batch; `revokeGauge(address pool) external` — governance-only; `setGovernanceContract(address newGovernance) external` — governance-only handoff for Stage K; `gaugeStatus(address pool) external view returns (GaugeStatus)`. Adds events: `GaugeActivated(address indexed pool, GaugeActivationPath indexed path)`, `GaugeActivationFailed(address indexed pool, bytes reason)`, `GaugeRevoked(address indexed pool)`, `AntiSpamFeeRouted(address indexed payer, uint256 amount)`, `GovernanceTransferred(address indexed oldGovernance, address indexed newGovernance)`. Adds custom errors: `NotGovernance(address caller)`, `AlreadyGauged(address pool)`, `AlreadyRevoked(address pool)`, `NotGauged(address pool)`. Pragma `^0.8.26`; license `GPL-3.0-or-later`.

- **Cursor prompt:** Extend `src/ccb/IGaugeRegistry.sol` with `GaugeStatus` + `GaugeActivationPath` enums, activation functions (`activateGauge`, `registerGaugeFromComposition`, `seedFoundingPool`, `seedFoundingPools`, `revokeGauge`, `setGovernanceContract`), view `gaugeStatus`, events (`GaugeActivated`, `GaugeActivationFailed`, `GaugeRevoked`, `AntiSpamFeeRouted`, `GovernanceTransferred`), and errors; retain `isGaugeApproved` as Stage F compat surface.

#### G3.2 — `GaugeRegistry.sol` scaffold (`src/gauge/GaugeRegistry.sol`)

Lands storage / immutables / constants / imports / modifier surface per **G-D16**. No constructor body, no functions. Pragma `^0.8.26`; license `GPL-3.0-or-later`. Imports: `IGaugeRegistry` (from `src/ccb/IGaugeRegistry.sol`), `IGaugeEligibility` (from `./IGaugeEligibility.sol`; cast at call site only — not used as immutable type per **G-D16d**), `SwapAndDepositToBodensee` (concrete from `./SwapAndDepositToBodensee.sol`; cast at call site only — not used as immutable type per **G-D16d**), `IERC20` + `SafeERC20` from OpenZeppelin. Immutables: `address public immutable svZCHF` (anti-spam fee token; cast to `IERC20(svZCHF)` at call site per **G-D16d**), `address public immutable swapAndDeposit` (Bodensee fee router, G-D12; cast to `SwapAndDepositToBodensee(swapAndDeposit)` at call site per **G-D16d**), `address public immutable gaugeEligibility` (eligibility evaluator binding; cast to `IGaugeEligibility(gaugeEligibility)` at call site per **G-D16d**). Constants: `uint256 public constant ANTI_SPAM_FEE = 100e18` (100 svZCHF at 18 decimals per OQ-G3 / STAGE_G_PRECHECK_AUTO_GAUGE §1). State: `mapping(address => GaugeStatus) private _gaugeStatus`, `address public governanceContract` (NOT immutable — updated via `setGovernanceContract` at Stage K handoff). Modifier: `modifier onlyGovernance() { if (msg.sender != governanceContract) revert NotGovernance(msg.sender); _; }`.

- **Cursor prompt:** Create `src/gauge/GaugeRegistry.sol` with type / state / constants / imports / `onlyGovernance` modifier surface per G-D16; no constructor body, no functions.

#### G3.3 — Constructor + `setGovernanceContract` + `activateGauge` (`src/gauge/GaugeRegistry.sol`)

Constructor: `constructor(address governance, address eligibility_, address swapAndDeposit_, address svZCHF_)` — zero-address checks on all four arguments; store `governanceContract = governance`, `gaugeEligibility = eligibility_`, `swapAndDeposit = swapAndDeposit_`, `svZCHF = svZCHF_` (raw `address` assignments per **G-D16d**; no constructor-time interface casts — casts deferred to per-call-site at G3.3 `activateGauge` + G3.4 view functions).

`setGovernanceContract(address newGovernance) external onlyGovernance`: zero-address check; `address old = governanceContract; governanceContract = newGovernance; emit GovernanceTransferred(old, newGovernance)`. Stage K handoff path.

`activateGauge(address pool) external` — non-reverting on eligibility failure per **G-D16c** + **OQ-G3**: (1) check `_gaugeStatus[pool] == GaugeStatus.None` — revert `AlreadyGauged(pool)` if Active, revert `AlreadyRevoked(pool)` if Revoked; (2) `IERC20(svZCHF).safeTransferFrom(msg.sender, address(this), ANTI_SPAM_FEE)` — reverts on transfer failure (precondition, acceptable revert); (3) `IERC20(svZCHF).safeTransfer(swapAndDeposit, ANTI_SPAM_FEE)` per G-D12 caller-pre-push pattern (`swapAndDeposit` is already `address` per **G-D16d**; no `address(...)` wrap needed); (4) `SwapAndDepositToBodensee(swapAndDeposit).swapAndDeposit(IERC20(svZCHF), ANTI_SPAM_FEE)` — routes fee to Bodensee (call-site casts per **G-D16d**); (5) `emit AntiSpamFeeRouted(msg.sender, ANTI_SPAM_FEE)`; (6) eligibility via try-block: `try IGaugeEligibility(gaugeEligibility).evaluateEligibility(pool) returns (bool ok) { if (!ok) { emit GaugeActivationFailed(pool, ""); return; } } catch (bytes memory reason) { emit GaugeActivationFailed(pool, reason); return; }` (call-site cast per **G-D16d**) — `evaluateEligibility` reverts on failure per G2.6, so the catch branch is the primary failure path; the `!ok` branch is defensive for future evolution; (7) (reached only if eligible): `_gaugeStatus[pool] = GaugeStatus.Active; emit GaugeActivated(pool, GaugeActivationPath.Permissionless)`. **G-D24 schedule:** the G3.3 main commit lands the constructor + function bodies WITHOUT the `abstract` contract-declaration keyword and WITHOUT the `external override` modifier on `setGovernanceContract` / `activateGauge`; both are added at the immediately-following corrective sub-step **G3.3-fix1** in a separate commit (the contract is abstract from G3.2 scaffold onward — only 3 of 8 `IGaugeRegistry` functions are implemented at G3.3 main; the keyword and the `override` modifier on the two implemented functions land in a single source patch per the `src/gauge/GaugeEligibility.sol` L296 / L304 / L309 / L314 precedent).

- **Cursor prompt:** Add constructor (four args, zero-address checks, store governance + eligibility + swapAndDeposit + svZCHF) + `setGovernanceContract` (onlyGovernance, `GovernanceTransferred` emit) + `activateGauge` (fee pull → safeTransfer push → swapAndDeposit call → AntiSpamFeeRouted emit → try/catch eligibility → non-reverting failure path → GaugeActivated emit) per G-D16c + OQ-G3. Per **G-D24**, this main commit does NOT add the `abstract` keyword nor the `external override` modifier — both land at G3.3-fix1 in a separate commit immediately following.

#### G3.4 — `registerGaugeFromComposition` + `revokeGauge` + view functions (`src/gauge/GaugeRegistry.sol`)

`registerGaugeFromComposition(address pool) external override onlyGovernance` (per **G-D24**): revert `AlreadyGauged(pool)` if Active; revert `AlreadyRevoked(pool)` if Revoked; no eligibility check (composition-governance path already vetted the replacement pool; Stage O wire-up hooks this); no anti-spam fee; G-D13 no boost — gauge is Active from activation block; `_gaugeStatus[pool] = GaugeStatus.Active; emit GaugeActivated(pool, GaugeActivationPath.Composition)`.

`revokeGauge(address pool) external override onlyGovernance` (per **G-D24**): revert `NotGauged(pool)` if not Active; `_gaugeStatus[pool] = GaugeStatus.Revoked; emit GaugeRevoked(pool)`. Grace-period machinery (graduated emission reduction post-revocation) is Stage H's concern; revocation at Stage G is a binary state flip only.

`isGaugeApproved(address pool) external view override returns (bool)` (Stage F compat per G-D16a + per **G-D24**): `return _gaugeStatus[pool] == GaugeStatus.Active`.

`gaugeStatus(address pool) external view override returns (GaugeStatus)` (per **G-D24**): `return _gaugeStatus[pool]`.

- **Cursor prompt:** Add `registerGaugeFromComposition` (onlyGovernance, no eligibility check, G-D13 no-boost, Composition path emit) + `revokeGauge` (onlyGovernance, Active-check, Revoked state, GaugeRevoked emit) + `isGaugeApproved` compat view + `gaugeStatus` view per G-D7 + G-D13 + G-D16a. Per **G-D24** Schedule (a), each new function declaration carries the `external override` (or `external view override`) modifier; contract remains `abstract` after G3.4 (still 1 of 8 `IGaugeRegistry` functions unimplemented — G3.5 closes the set).

#### G3.5 — `seedFoundingPool` + `seedFoundingPools` (`src/gauge/GaugeRegistry.sol`)

`seedFoundingPool(address pool) external override onlyGovernance` (per **G-D24**): revert `AlreadyGauged(pool)` if already Active (double-seed is a governance error); revert `AlreadyRevoked(pool)` if Revoked (re-seeding a revoked founding pool is a governance amendment out of scope for Stage G — silent overwrite must not occur); no eligibility check (Miliarium pools bypassed per STAGE_G_PRECHECK_AUTO_GAUGE C-4); no anti-spam fee (governance path); G-D13 no boost — Active from seed block; `_gaugeStatus[pool] = GaugeStatus.Active; emit GaugeActivated(pool, GaugeActivationPath.Founding)`.

`seedFoundingPools(address[] calldata pools) external override onlyGovernance` (per **G-D24**): iterates `pools`, inlining the `seedFoundingPool` logic for each (not an external call to `seedFoundingPool` — avoids reentrancy surface on the governance path); reverts atomically on any individual pool error; enables seeding all 28 Miliarium genesis pools in one governance transaction.

- **Cursor prompt:** Add `seedFoundingPool` (onlyGovernance, `external override`, no eligibility, no fee, Founding path emit, AlreadyRevoked guard) + `seedFoundingPools` batch variant (`external override`, inline loop, atomic revert, 28-pool genesis) per STAGE_G_PRECHECK_AUTO_GAUGE C-4 + G-D13 + G-D16b. Per **G-D24** Schedule (a), this same source change ALSO removes the `abstract` keyword from the contract declaration (`abstract contract GaugeRegistry is IGaugeRegistry` → `contract GaugeRegistry is IGaugeRegistry`) — 8 of 8 `IGaugeRegistry` functions implemented; contract becomes concrete at G3.5 close.

#### G3.6 — `GaugeRegistry` unit tests (`test/unit/GaugeRegistry.t.sol`)

Lands unit invariants per the test matrix in `STAGE_G_NOTES.md` **T-R1** + governance-access gates. Permissionless path (eligibility pass): `MockGaugeEligibility.evaluateEligibility` returns `true`; `MockSvZCHF.safeTransferFrom` + `safeTransfer` recorded; `MockSwapAndDeposit.swapAndDeposit` recorded; assert `GaugeActivated(pool, Permissionless)` emitted, `isGaugeApproved(pool)` == `true`, `gaugeStatus(pool)` == `Active`. Permissionless path (evaluateEligibility reverts): mock eligibility reverts with encoded reason; fee pulled and routed before check (verify `safeTransferFrom` + `safeTransfer` + `swapAndDeposit` all recorded); assert `GaugeActivationFailed(pool, reason)` emitted, NO tx revert, `gaugeStatus(pool)` == `None`. Permissionless path (evaluateEligibility returns false): mock eligibility returns `false`; assert `GaugeActivationFailed(pool, "")` emitted, `gaugeStatus(pool)` == `None`. Fee precondition: mock svZCHF with insufficient allowance; `safeTransferFrom` reverts; assert NO `AntiSpamFeeRouted` event, pool status unchanged. Double-activation: second `activateGauge` on Active pool reverts `AlreadyGauged`; on Revoked pool reverts `AlreadyRevoked`. Composition path: non-governance call reverts `NotGovernance`; governance call activates without touching eligibility mock; `GaugeActivated(pool, Composition)` emitted. Founding path: `seedFoundingPool` governance-only gate; no fee, no eligibility; `GaugeActivated(pool, Founding)`; `seedFoundingPools` activates 3-address test vector atomically (covers 28-pool semantics). Revocation: `revokeGauge` governance-only; Active → Revoked; `GaugeRevoked` emitted; `isGaugeApproved(pool)` == `false`; subsequent `activateGauge` reverts `AlreadyRevoked`. `setGovernanceContract`: old governance transfers; `GovernanceTransferred` emitted; new governance passes `onlyGovernance`; old governance reverts `NotGovernance`. Stage F compat: `isGaugeApproved(pool)` == `(gaugeStatus(pool) == Active)` for all three `GaugeStatus` values. Mock fixtures: `MockGaugeEligibility` (controllable per-pool return + revert injection), `MockSwapAndDeposit` (captures call args), `MockERC20` (controllable allowance/balance for svZCHF). Pure unit; no mainnet fork. **T-I6** Revoked-terminality coverage (per **G-D17**): three negative-path cases assert `activateGauge(revokedPool)`, `registerGaugeFromComposition(revokedPool)`, and `seedFoundingPool(revokedPool)` each revert `AlreadyRevoked(pool)` against a pool whose status was previously flipped to Revoked via `revokeGauge`.

- **Cursor prompt:** Create `test/unit/GaugeRegistry.t.sol` covering T-R1: three activation paths, fee-fail non-revert semantics, fee precondition revert, governance gates, revocation, `setGovernanceContract` handoff, Stage F `isGaugeApproved` compat; pure unit (no fork).

### G4 — Integration fork test

G4 ships `test/fork/StageGIntegration.t.sol` — the cross-contract fork test exercising `VaultClassRegistry` + `GaugeEligibility` + `GaugeRegistry` together against real Bodensee + Stage E pilot pools on mainnet fork. SwapAndDepositToBodensee fork-level invariants are already covered at **G1.8**; G4 covers everything else — registry state machine, eligibility evaluation, all three activation paths, threshold transition events, governance gates — plus a Stage F regression check that the **G-D16a** IGaugeRegistry extension does not break the existing 263-test green baseline. Per **D35** + **D36** + **F-D11**: split-form invocation `--match-path "test/fork/**" --fork-url $MAINNET_RPC_URL -vv --threads 1`.

Sub-row layout: G4.0 stands up the fixture base (deploy chain + one-shot setter wiring + helpers); G4.1 covers the VaultClassRegistry propose-veto-finalize-revoke flow against real Bodensee donation routing; G4.2 covers GaugeEligibility evaluation + epoch-snapshot F-10 tournament against real pilot pools; G4.3 covers GaugeRegistry permissionless activation end-to-end (OQ-G3 fee flow + G-D16c non-revert semantics); G4.4 covers GaugeRegistry governance paths (composition + founding + batch-founding + revoke + handoff) + Stage F regression check.

#### G4.0 — Fork test fixture (`test/fork/StageGIntegration.t.sol`)

Lands the abstract `StageGIntegrationFixture` base extending the Stage E `MiliariumPilotPoolBase` pattern per **F-D11** + **F-D26 (a)**. setUp deploys, in order: fresh `AureumVault` + `WeightedPoolFactory` + Bodensee with `enableDonation = true` per Stage D `script/DeployDerBodensee.s.sol`; `AuMM` + `MockAuMT` from `test/fork/mocks/StageGMocks.sol` per **G-D25c** (concrete `AuMT` deferred to Stage I per the forward-dep convention; mock provides `IAuMT` ABI surface required for `VaultClassRegistry.setAuMT` + veto-weight paths); pilot pools per Stage E `script/pools/DeployIxHelvetia.s.sol` + `script/pools/DeployIxEdelweiss.s.sol` + `script/pools/DeployIxAurebit.s.sol` (three-pilot harness matching `CCBEngineFixture` per **F-D26 (a)** — covers sUSDS-class + ERC-4626 mixed-decimal numerator paths; Stage D `waEthUSDC` fixture is out of scope here); `SwapAndDepositToBodensee` per **G1.4–G1.6**; `VaultClassRegistry` per **G1.11–G1.15** with single-token genesis `[sUSDS]` + `[AdmissionType.ImplementationAddress]` per **G-D25a** (supersedes the broader "genesis class set seeded against deployed pilot tokens per **G1.12**" wording — minimal genesis keeps G4.1 propose-veto-finalize-revoke flow exercising the proposal path on ixEDEL / ixAUR / ixHEL non-sUSDS underlyings); `GaugeEligibility` per **G2.2–G2.6** wired to `VaultClassRegistry` + `MockTVLOracle` from `test/fork/mocks/CCBMocks.sol` (concrete TVL oracle is the OQ-22 carry-forward per `CLAUDE.md` §11 deferred items) + `MockEfficiencyOracle` from `test/fork/mocks/StageGMocks.sol` per **G-D25c** (concrete efficiency oracle deferred to Stage H per **G2.5-pre-1** NatSpec + **G-D23 (ii)**); `GaugeRegistry` per **G3.2–G3.5** wired to `GaugeEligibility` + `SwapAndDepositToBodensee` + svZCHF. One-shot setter wiring (per **F-D20–F-D23** parallel pattern + G-D12 partial-activation): `SwapAndDepositToBodensee.setVaultClassRegistry(address(vaultClassRegistry))` + `SwapAndDepositToBodensee.setGaugeRegistry(address(gaugeRegistry))`; `VaultClassRegistry.setAuMT(address(auMT))` + `setGovernanceContract(address(this))` (test contract acts as governance Safe); `GaugeRegistry`'s governance set in constructor to `address(this)`. Helpers: `_proposeAndFinalizeClass(address token, AdmissionType, bytes32 constraintsHash)` (full propose → `vm.roll(BLOCKS_PER_EPOCH × 3)` veto-window-skip → `finalizeProposal` chain); `_makePoolEligible(address pool, uint256 tvl)` (admission-before-TVL ordering invariant per **G-D25b**: phase 1 — `_proposeAndFinalizeClass` for any unadmitted pool-token contributing to the **G-D8** 52% numerator; phase 2 — TVL oracle slot write); `_approveAndActivate(address caller, address pool, uint256 fee)` (svZCHF approve from `caller` → `activateGauge` call). Test invocation per D35 split-form: `--threads 1` per F-D11 + D36.

- **Cursor prompt:** Create `test/fork/StageGIntegration.t.sol` with `StageGIntegrationFixture` abstract base — full deploy chain (Vault + Bodensee `enableDonation = true` + three Stage E pilots [ixHelvetia + ixEdelweiss + ixAurebit] + helper + VaultClassRegistry single-token genesis `[sUSDS]` per **G-D25a** + GaugeEligibility wired to `StageGMocks.sol` doubles per **G-D25c** + GaugeRegistry), parallel one-shot setter wiring per F-D20–F-D23, and helpers (`_proposeAndFinalizeClass`, `_makePoolEligible` admission-before-TVL ordering per **G-D25b**, `_approveAndActivate`); per F-D11 / F-D26 (a) pattern; D35 split-form invocation.

#### G4.1 — VaultClassRegistry propose-veto-finalize-revoke flow (`test/fork/StageGIntegration.t.sol`)

Adds `StageGVaultClassRegistryTest` extending the fixture. Tests the **G-D9** Frankencoin-veto governance path against real Bodensee donation routing on fork. Happy-path admission: fund a caller with svZCHF; caller approves `PROPOSAL_BOND_SVZCHF` to `VaultClassRegistry`; `proposeVaultClass(ImplementationAddress, sUSDS-class wrapper, constraintsHash)`; assert svZCHF bond pulled from caller via `safeTransferFrom`; assert `SwapAndDepositToBodensee.swapAndDeposit` called with correct args; assert Bodensee svZCHF reserve increased by exactly `PROPOSAL_BOND_SVZCHF` and Bodensee BPT supply unchanged (donation, no BPT mint per G-D11 + G-D12); `vm.roll(block.number + VETO_WINDOW_BLOCKS + 1)` to skip veto window; `finalizeProposal(id)`; assert `admittedClasses[token] == true`, `admissionTypes[token] == AdmissionType.ImplementationAddress`, `VaultClassFinalized(id, token)` event emitted. Veto-success path: propose → mock AuMT holder vetoes with weight ≥ `VETO_THRESHOLD`; assert `proposal.finalized && proposal.revoked`; assert bond stays in Bodensee (no refund to proposer); assert `admittedClasses[token] == false`. Veto-window guards: `vetoProposal(id)` after window expiry reverts `VetoWindowExpired`; `finalizeProposal(id)` before window expiry reverts `VetoWindowOpen`. Revocation path: governance `revokeVaultClass(token)` flips `admittedClasses[token] = false`, emits `VaultClassRevoked(token)`; subsequent `GaugeEligibility.evaluateEligibility` against pool holding the now-revoked class enters grace-period evaluation per G-D8 canonical numerator definition (numerator drops below 52% at next epoch boundary).

- **Cursor prompt:** Add `StageGVaultClassRegistryTest` covering the G-D9 propose-veto-finalize-revoke flow against real Bodensee donation routing — happy-path admission + veto-success + window guards + revocation; assert Bodensee BPT supply unchanged after every donation routing.

#### G4.2 — GaugeEligibility evaluation + F-10 epoch-snapshot tournament (`test/fork/StageGIntegration.t.sol`)

Adds `StageGEligibilityTest` extending the fixture. Tests G2 eligibility evaluation + F-10 tournament against real Stage E pilot pools. Per-pool `evaluateEligibility` happy path: pool with admitted ERC-4626 token at ≥52% weight + above-floor TVL + whitelisted pool type passes; assert `isGaugeEligible[pool] == true`, no revert. Per-pool failure modes: T-I3 forbidden-token (synthetic AuMM-bearing or AuMT-bearing pool reverts `ForbiddenToken`); G-D6 pool-type whitelist (synthetic pool with non-whitelisted factory address — `vm.mockCall` on Vault factory-read or alternative-factory deployment — reverts `PoolTypeNotWhitelisted`); OQ-G2 TVL floor (sub-floor pool reverts `TVLFloorNotMet`); G-D8 52% gate (pool with <52% admitted-class weight reverts `InsufficientQualityGate`). G-D10 ERC-4626 detection paths against real fork state: plain ERC-20 token → `try` catches → no numerator credit; 4626-claiming non-admitted token → `asset()` succeeds, `isAdmittedClass` returns false → no numerator credit; admitted 4626 → full weight credit. Epoch snapshot tournament: `computeEpochSnapshot(eligiblePools)` against ≥4 eligible pools with varying `efficiency_ratio` (manipulate via `MockEfficiencyOracle` per-pool `(numeratorSma, denominatorSma)` writes per **G-D23 (i)**; independent of TVL-floor gate); assert top-15% favored cohort assignment via ceiling arithmetic per G2.5; T-T1 — `GaugeEfficiencyRising` emitted exactly once on bottom-to-top crossing (assert `expectEmit` + revert if duplicate); T-T2 — `GaugeEfficiencyDropped` emitted exactly once on top-to-bottom crossing; T-T3 tie-break — equal `efficiency_ratio` produces stable sort by deterministic tiebreak (address ascending — verify against fixed-address vector); T-I5 epoch-snapshot determinism — two `isEligible(pool)` reads at the same block return identical latched state (per `currentSnapshotEpoch` invariant per G2.6).

- **Cursor prompt:** Add `StageGEligibilityTest` covering per-pool evaluateEligibility happy + four failure modes (T-I3 + G-D6 + OQ-G2 + G-D8) + G-D10 ERC-4626 try/catch detection paths + epoch-snapshot tournament with cohort transitions per G-D3 + G-D5 + T-T1/T-T2/T-T3 + T-I5 against real pilot pools; pool-factory non-whitelist via `vm.mockCall` on Vault factory-read.

#### G4.3 — GaugeRegistry permissionless activation end-to-end (`test/fork/StageGIntegration.t.sol`)

Adds `StageGRegistryPermissionlessTest` extending the fixture. Tests G3.3 permissionless `activateGauge` path with full real-fork fee flow per **OQ-G3** + **G-D16c**. Eligibility-pass path: caller funded with svZCHF (≥ `ANTI_SPAM_FEE`); `svZCHF.approve(address(gaugeRegistry), ANTI_SPAM_FEE)`; record pre-state Bodensee svZCHF reserve + BPT supply; `gaugeRegistry.activateGauge(eligiblePool)`; assert `safeTransferFrom` pulled exactly `ANTI_SPAM_FEE` from caller; assert `safeTransfer` pushed to SwapAndDeposit; assert SwapAndDeposit reached Bodensee — svZCHF reserve delta == `ANTI_SPAM_FEE`, BPT supply unchanged (donation per G-D11 + G-D12); assert `AntiSpamFeeRouted(caller, ANTI_SPAM_FEE)` event; assert `GaugeActivated(pool, GaugeActivationPath.Permissionless)` event; assert `gaugeStatus(pool) == GaugeStatus.Active` and `isGaugeApproved(pool) == true`. Eligibility-fail (non-revert) path per **G-D16c** + **OQ-G3** "non-refundable on any failed criteria check": configure pool to fail TVL floor; caller approves fee; `activateGauge(ineligiblePool)`; assert tx does NOT revert; assert fee STILL pulled and routed to Bodensee (Bodensee svZCHF reserve delta == `ANTI_SPAM_FEE` confirms non-refundable behavior — the spec-intent that fee is forfeited regardless of eligibility outcome); assert `AntiSpamFeeRouted` event; assert `GaugeActivationFailed(pool, reason)` event with non-empty reason bytes (the catch-bytes from `evaluateEligibility` revert); assert `gaugeStatus(pool) == GaugeStatus.None` (no state mutation on failure path). Fee-precondition revert: caller without prior approval → `safeTransferFrom` reverts at step (2) of G3.3; assert NO `AntiSpamFeeRouted` event, NO Bodensee state mutation, `gaugeStatus(pool) == None`. Double-activation guard: second `activateGauge(activePool)` reverts `AlreadyGauged(pool)`; activation on a previously-revoked pool reverts `AlreadyRevoked(pool)`.

- **Cursor prompt:** Add `StageGRegistryPermissionlessTest` covering OQ-G3 anti-spam fee flow end-to-end against real Bodensee — eligibility-pass activation + eligibility-fail non-revert with fee retained in Bodensee + fee-precondition revert (no allowance) + double-activation guards per G-D16c.

#### G4.4 — GaugeRegistry governance paths + revocation + handoff + Stage F regression (`test/fork/StageGIntegration.t.sol`)

Adds `StageGRegistryGovernanceTest` extending the fixture. Tests G3.4 + G3.5 governance-only paths per **G-D7** + **G-D13** + **G-D16a**. `registerGaugeFromComposition`: configure target pool to fail TVL floor (would fail permissionless eligibility); governance call activates without eligibility check + without anti-spam fee (Bodensee state unchanged); assert `GaugeActivated(pool, GaugeActivationPath.Composition)` event; non-governance caller reverts `NotGovernance(caller)`. `seedFoundingPool`: governance call activates a Miliarium-style pool without eligibility + without fee; assert `GaugeActivated(pool, GaugeActivationPath.Founding)`; non-governance caller reverts. `seedFoundingPools` batch (3-pool address vector covering 28-pool semantics): atomic activation; insert one already-Active pool address mid-array; assert entire batch reverts `AlreadyGauged(pool)` with no partial-state leakage (verify all three pool statuses unchanged via `gaugeStatus` reads after revert). `revokeGauge`: governance flips Active → Revoked; emits `GaugeRevoked(pool)`; subsequent `activateGauge(revokedPool)` reverts `AlreadyRevoked(pool)`; `isGaugeApproved(revokedPool) == false`. `setGovernanceContract` (Stage K dry-run): old governance calls `setGovernanceContract(newGovernance)`; assert `GovernanceTransferred(old, new)` event; new governance can call `revokeGauge` etc.; old governance reverts `NotGovernance(old)` post-handoff. **Stage F regression check (per G-D16a)**: query `gaugeRegistry.isGaugeApproved(activePool)` from a Stage F-style consumer (mock-CCBMultiplier reader, or direct read in test) and confirm the IGaugeRegistry extension at G3.1 does not break the legacy single-bool surface; **note** Stage F's `MockGaugeRegistry` in `test/fork/mocks/CCBMocks.sol` will need no-op stubs for the new interface methods — coordinate at G3.1 sub-step entry per the G-D16a Option B trade-off (extend in-place requires Stage F mock update). **T-I6** Revoked-terminality fork coverage (per **G-D17**): after the `revokeGauge` flip above, assert `registerGaugeFromComposition(revokedPool)` reverts `AlreadyRevoked(pool)` and `seedFoundingPool(revokedPool)` reverts `AlreadyRevoked(pool)` — the `activateGauge` path against Revoked is already covered in the permissionless-test sibling.

- **Cursor prompt:** Add `StageGRegistryGovernanceTest` covering G-D7 + G-D13 + G-D16a paths — composition (no fee, no eligibility, ineligible pool succeeds) + founding + batch-founding atomicity + revoke + setGovernanceContract handoff (Stage K dry-run) + Stage F isGaugeApproved regression check; flag MockGaugeRegistry update requirement per G-D16a Option B.

### G5 — Docs

G5 closes Stage G with three documentation sweeps before the FF merge. G5.1 backfills the **Completion Log** in `docs/STAGE_G_PLAN.md` with rows for every G0–G4 sub-step commit landed during execution; mechanical transcription against `git log stage-g ^stage-f-complete --oneline --reverse`. G5.2 updates the cross-stage doc surface — `docs/STAGES_OVERVIEW.md` Stage G row (status flip + 4-contract scope correction since the helper `SwapAndDepositToBodensee` is the 4th contract per **G-D11** + **G-D12**) + `docs/STAGE_G_NOTES.md` status header (Active → Completed) + the `STAGE_G_PLAN.md` "Scope of Stage G" section list (3 → 4 contracts). G5.3 refreshes `CLAUDE.md` §11 with the Stage G completion paragraph: contracts shipped, mid-stage OQ resolutions, G-numbered findings, deferred items, next-stage entry mode.

Sub-row layout: G5.1 — Completion Log catch-up; G5.2 — cross-stage doc sweep (STAGES_OVERVIEW + STAGE_G_NOTES + scope-list fix); G5.3 — CLAUDE.md §11 refresh.

#### G5.1 — Completion Log catch-up (`docs/STAGE_G_PLAN.md`)

Mechanical transcription against `git log stage-g ^stage-f-complete --oneline --reverse`. Each row in the Completion Log table carries: sub-step ref (e.g., G1.1, G2.3, G3.6), commit hash (short, 7 chars), date (YYYY-MM-DD), one-line summary matching the commit subject up to ~80 chars. Drafted by Claude Code at G5.1 execution from the live `git log` output; transcribed into the table by Cursor as a paste-only pass. Per the working agreement, the Completion Log is **the** canonical record of what landed on `stage-g` between branch-point and FF merge — read by any future maintainer trying to reconstruct stage state without re-walking every commit. Style matches Stage F's F5.1 catch-up format (29 rows in F5.1 covering F2.3 → F3.4-fix2 + F4.4 verification closure per `CLAUDE.md` §11 Stage F roll-up). Per §13 mode dispatch — Opus drafts the row prose (each row condenses sub-step intent), Sonnet handles the transcription pass once the prose is locked.

- **Cursor prompt:** Append rows to the Completion Log table in `docs/STAGE_G_PLAN.md` covering every G0–G4 + G5.1–G5.2 commit landed on `stage-g` since `stage-f-complete`; Claude Code drafts row prose from `git log` output at G5.1 execution; Cursor transcribes verbatim — pure mechanical pass.

#### G5.2 — Cross-stage doc sweep (`docs/STAGES_OVERVIEW.md` + `docs/STAGE_G_NOTES.md` + `docs/STAGE_G_PLAN.md`)

Three coordinated edits, each pure paste/replace (no design surface). (i) `docs/STAGES_OVERVIEW.md` Stage G row — flip status indicator from "next" / "in progress" to **complete**; correct contract count from 3 to 4 per the G-D11 / G-D12 helper insertion (`SwapAndDepositToBodensee` is the 4th contract alongside `VaultClassRegistry` + `GaugeEligibility` + `GaugeRegistry`); add tag reference `stage-g-complete`. Exact line edits located by `grep -n "Stage G" docs/STAGES_OVERVIEW.md` at G5.2 execution. (ii) `docs/STAGE_G_NOTES.md` status header — top-of-file `**Status:** Active` → `**Status:** Completed (YYYY-MM-DD, tag stage-g-complete)`; add a "Stage findings" close-out paragraph below the existing G-D table summarizing any G-numbered findings (G10, G11, ... per Stage F convention F10/F11/F12) recorded during execution. (iii) `docs/STAGE_G_PLAN.md` "Scope of Stage G" section — the existing list enumerates 3 contracts; insert `SwapAndDepositToBodensee` as the 4th to match the actual deployed surface — punted from G0.1a per the deferred items list at G0.1a sub-step entry. All three edits are mechanical; Claude Code locates each insertion point via grep at G5.2 execution and Cursor transcribes verbatim.

- **Cursor prompt:** Three coordinated paste-edits — `docs/STAGES_OVERVIEW.md` Stage G row (status flip + 4-contract correction + `stage-g-complete` tag ref) + `docs/STAGE_G_NOTES.md` status header (Active → Completed + Stage findings close-out) + `docs/STAGE_G_PLAN.md` "Scope of Stage G" section (3 → 4 contracts); Claude Code locates each insertion point at G5.2 execution via grep; Cursor transcribes verbatim.

#### G5.3 — `CLAUDE.md` §11 Stage G complete refresh

Replaces the current Stage F completion paragraph in `CLAUDE.md` §11 ("Last update:") with the Stage G completion paragraph. Structure mirrors Stage F's roll-up (per the existing §11 prose authored at F5.3): single dense paragraph capturing — date + tag + commit hash; **contracts shipped** (`SwapAndDepositToBodensee`, `VaultClassRegistry`, `GaugeEligibility`, `GaugeRegistry`) with one-clause description per contract per G-D11/G-D12/G-D9/G-D8/G-D7; **key Stage G design decisions** (G-D1 through G-D16 + any new G-D17+ decisions made during execution); **mid-stage OQ resolutions** cross-link (OQ-G1, OQ-G2, OQ-G3, OQ-G4 — already RESOLVED in `docs/FINDINGS.md`, but cross-link from §11); **G-numbered findings** recorded during execution (G10, G11, ... per F10/F11/F12 convention); **test counts** (split-form unit + fork totals); **deferred items (post-Stage-G carry-forward)** — concrete TVL oracle for OQ-22, MockGaugeRegistry stub-update tracking for any future Stage F + G interface drift per G-D16a Option B trade-off, `aumm-site` spec edits per OQ-G3 + OQ-G4 spec-edits-required appendix; **next-stage entry mode** (Stage H — emission, default Opus extra-high entry per §13 stage-level defaults). Branch-state paragraph: branch position + recent commit stack on `stage-g` + Completion Log pointer. "Stage G position" sub-paragraph: rolling sub-step summary (G1 helper → G2 eligibility → G3 registry → G4 fork integration → G5 close docs → G9 FF merge). Housekeeping bullets section: any new lessons from Stage G execution that warrant a permanent §11 entry alongside D24/D31/D32/D34/D35/D36/E10/F10/F11/F12. Drafted by Claude Code in Opus at G5.3 execution from the post-G5.1 + post-G5.2 state; Cursor transcribes verbatim.

- **Cursor prompt:** Replace the Stage F completion paragraph in `CLAUDE.md` §11 with the Stage G completion paragraph; mirror Stage F structure (date + tag + commit + contracts shipped + design decisions + OQ resolutions + findings + tests + deferred + next-stage entry mode + branch state + position roll-up + housekeeping bullets); Claude Code drafts the full paragraph at G5.3 execution from the post-G5.2 state; Cursor transcribes verbatim.

### G9 — Close

G9 closes Stage G via FF merge of `stage-g` → `main`, lightweight tag `stage-g-complete` at the FF merge HEAD, and push to `origin`. Single sub-row, entirely user-terminal — no Cursor action. The `stage-g` branch is preserved on `origin` as a snapshot per the C0 / D0 / E-D8 / F9 convention. Pre-merge invariants verified by Claude Code in Opus at G9.0 execution before the user-terminal block is drafted.

Sub-row layout: G9.0 — FF merge + tag + push.

#### G9.0 — FF merge `stage-g` → `main` + tag `stage-g-complete` + push (terminal-only)

Pre-merge audit (Opus): Claude Code reads (i) `git log stage-g ^stage-f-complete --oneline` — confirms commit topology covers every Stage G work unit; (ii) tail of latest `forge test` split-form output — unit + fork green per D35 split-form invocation pattern (`--no-match-path "test/fork/**"` for unit + `--match-path "test/fork/**" --fork-url $MAINNET_RPC_URL --threads 1` for fork per F-D11 + D36); (iii) tail of latest `slither` output — clean on the Stage G surface (`src/gauge/` + `src/ccb/IGaugeRegistry.sol`); (iv) `git status -sb` — clean tree, no untracked files; (v) `git diff stage-f-complete..stage-g --stat` — touched-file list confined to `src/gauge/` + `src/ccb/IGaugeRegistry.sol` + `test/unit/` + `test/fork/` + `docs/` + (if any) `script/`; **no `lib/balancer-v3-monorepo/` edits — Vault byte-identity invariant from `CLAUDE.md` §1 "Design philosophy"**. After all five invariants pass, Claude Code drafts the user-terminal command block per §8e Git mutations format: leading `clear` → `git checkout main` → `git merge --ff-only stage-g` (FF expected — no merge commit; if non-FF, abort and diagnose) → `git tag stage-g-complete` (lightweight, no annotation per Stage F convention) → `git log --oneline -5` (verify tag at HEAD) → `git push origin main` → `git push origin stage-g-complete` → `git push origin stage-g` (preserve branch snapshot per F9 convention) → `git status -sb` (final clean check). User executes the block in their own terminal; pastes output back; Claude Code emits the final ✅ Stage G complete summary in chat (re-reads `CLAUDE.md` §11 to confirm the post-G5.3 paragraph reflects the merged HEAD).

- **Cursor prompt:** No Cursor action — G9.0 is terminal-only (FF merge + tag + push). Claude Code performs the five-invariant pre-merge audit at G9.0 entry (commit topology + forge tail + slither tail + git status + Vault-byte-identity diff check), then drafts the user-terminal command block per §8e Git mutations format; user executes in terminal.

---

## Completion Log

| Sub-step | Commit | Date | Summary |
|----------|--------|------|---------|
| RT-02 | 6a6cba5 | 2026-05-05 | docs/STAGE_G_NOTES.md — G-D3 Option A lock (top-15% favored, descending rank, anti-concentration caps) |
| RT-01+RT-04 | 7e19d01 | 2026-05-05 | docs/STAGE_G_NOTES.md — G-D7 three activation paths + per-path boost policy (seedFoundingPool, RT-03 fwd-ref) |
| docs (Stage G pivot prep) | 37d985b | 2026-05-05 | Stage G pivot prep — FINDINGS.md (OQ-G1–G3 addendum + auto-gauge cross-refs) + new docs/STAGE_G_PRECHECK_AUTO_GAUGE.md |
| docs (FINDINGS OQ-G4) | b9a30c6 | 2026-05-05 | FINDINGS.md — OQ-G4 (Vault-Class Registry; ERC-4626 numerator class admission, Frankencoin veto model) |
| docs (FINDINGS intro) | dd3d7df | 2026-05-05 | FINDINGS.md — intro: Stage G addendum indexes OQ-G1–G4 (was OQ-G1–G3) |
| docs (CLAUDE Stage G pivot) | 963a601 | 2026-05-05 | CLAUDE.md — Stage G pivot orientation (§3 tree, §4 ref table, §5 cross-ref codes, §11 next-step) |
| docs (STAGES_OVERVIEW pivot) | ba11236 | 2026-05-05 | STAGES_OVERVIEW.md — Stage G pivot: permissionless gauge activation, OQ-G1–G4 (K/M/R downstream alignment) |
| G0 | 61fb085 | 2026-05-05 | docs/STAGE_G_NOTES.md + docs/STAGE_G_PLAN.md — Stage G scaffold |
| G1.0 | 5567533 | 2026-05-05 | docs/STAGE_G_NOTES.md G-D11 + docs/STAGE_G_PLAN.md G-DP10 supersession (Option A DONATION) |
| G1.1 | 07b65a6 | 2026-05-06 | script/DeployDerBodensee.s.sol + test/fork/{AureumFeeRoutingHook,PilotPools,CCBEngine}.t.sol — enableDonation = true on Bodensee (per G-D11) |
| G1.2 | 7afca17 | 2026-05-06 | docs/STAGE_G_NOTES.md — G-D12 SwapAndDeposit full spec lock + G-D11 equivalence supersession |
| G1.3 | 7164b13 | 2026-05-06 | docs/STAGE_G_NOTES.md — G-D13 (90-day boost deprecation) + G-D7 inline supersession + G-D1 + T-T4 amendments |
| G1.3-fix | 07e3c98 | 2026-05-06 | docs/STAGE_G_NOTES.md — G-D9 + G-D11 stale fee-language sweep (100 svZCHF/sUSDS → asymmetric per G-D12) |
| G0.1b | bca29f8 | 2026-05-07 | docs/STAGE_G_PLAN.md — G2 (GaugeEligibility + F-10 ranking) + G3 (GaugeRegistry + three activation paths) sub-step body fill |
| G0.1c | ea3300f | 2026-05-07 | docs/STAGE_G_PLAN.md — G4 (integration fork test) + G5 (docs sweep) + G9 (close) sub-step body fill |
| G0.2 | 7610a80 | 2026-05-07 | docs/STAGE_G_NOTES.md — G-D12 line 315 _moduleAdmin slot disambiguation (6 immutables + 1 burnable storage; no immutable mirror) |
| docs (CLAUDE §12 ambiguity-gate) | 27bc91d | 2026-05-07 | CLAUDE.md §12 — ambiguity-gate rule for typed domain and state semantics (per 2026-05-07 robustness-first guidance) |
| docs (ROBUSTNESS register skeleton) | 364ebc6 | 2026-05-07 | ROBUSTNESS_BACKPORT_REGISTER.md — Stage B-F retrofit candidate register skeleton (scope + columns + RB-001 seed; rows populated via audit) |
| G0.4 | 4f1f4ec | 2026-05-07 | docs/STAGE_G_PLAN.md — G1.4 body 7→6 immutables + _moduleAdmin storage-only wording (per G0.2 disambiguation) |
| G0.5 | 11c0604 | 2026-05-07 | docs/ROBUSTNESS_BACKPORT_REGISTER.md — RB-002 (CCBMultiplier.emaSampler) + RB-003 (AureumWeightedPoolFactory._poolVersion) + Storage-class hardening class |
| RB-002.0 | 2fe6ef8 | 2026-05-07 | src/ccb/CCBMultiplier.sol — promote emaSampler to public immutable (Storage-class hardening per register RB-002) |
| RB-002.2 | daffa62 | 2026-05-07 | docs/ROBUSTNESS_BACKPORT_REGISTER.md — close RB-002 (emaSampler immutable backport; Stage F regression 263/263 at 2fe6ef8) |
| RB-004.open | a8d995c | 2026-05-07 | docs/ROBUSTNESS_BACKPORT_REGISTER.md — open RB-004 (External-call & type-cast safety; Stage D/E/F test code) + 10th hardening class bullet |
| RB-004.0a | 8bc8e6e | 2026-05-07 | test/fork/{AureumFeeRoutingHook,PilotPools}.t.sol — SafeERC20 migration (full bare-transfer sweep: 6 call sites) |
| RB-004.0b | 67ce558 | 2026-05-07 | test/unit/CCBMultiplier.t.sol — SafeCast helper + typed constants + 16 cast-site removals |
| RB-004.0c | 7bdfbcc | 2026-05-07 | test/unit/CCBMultiplier.t.sol — makeAddr migration for synthetic pool addresses (L449, L621) |
| RB-004.2a | d2cd966 | 2026-05-07 | docs/STAGE_F_NOTES.md — append F13 (F12 test-side extension) + F14 (forge cache stale-list recovery) |
| RB-004.2b | 6a2a0b1 | 2026-05-07 | docs/ROBUSTNESS_BACKPORT_REGISTER.md — close RB-004 (forge-lint 0/21; 263/263 at 7bdfbcc) |
| RB-004.2c | 901d61b | 2026-05-07 | CLAUDE.md §11 — append F13 (test-side type-discipline) + F14 (forge cache stale-list) housekeeping bullets |
| G1.4-pre-A | 18a7bbf | 2026-05-07 | docs/STAGE_G_NOTES.md — G-D12 error signatures typed (14 errors, G1.4-pre-A lock) |
| G1.4-pre-B | 06b6156 | 2026-05-07 | docs/STAGE_G_PLAN.md — G1.4 + G1.5 bodies amended for option (a) constructor split |
| G1.4-pre-C | 038b5ed | 2026-05-07 | docs/STAGE_G_NOTES.md — G-D14 transient-storage lock (resolve G-D12 line 351 deferral) |
| G1.4-pre-D | 0dd41d5 | 2026-05-07 | docs/STAGE_G_PLAN.md + STAGE_G_NOTES.md — post-option-(a)+G-D14 hygiene (storage split, G1.6 repoints) |
| G1.4-pre-E1 | 161c109 | 2026-05-08 | docs/STAGE_G_NOTES.md + STAGE_G_PLAN.md — G-D14 amended to Balancer assembly-helper pattern; G10 finding appended |
| G1.4-pre-E2 | 0b67402 | 2026-05-08 | CLAUDE.md §11 — append G10 bullet (§12 NOTES language-feature compile-probe rule) |
| G1.4 | 2e100cb | 2026-05-08 | src/gauge/SwapAndDepositToBodensee.sol — helper scaffold (errors, event, immutables, storage, transient slot constants, constructor) |
| G1.5-pre-A | e8adf51 | 2026-05-08 | docs/STAGE_G_NOTES.md + STAGE_G_PLAN.md — constant→immutable keyword fix for slot identifiers (G-D14 L383+L406; PLAN L161) + G11 finding appended |
| G1.5-pre-B | 6c209dd | 2026-05-08 | docs/STAGE_G_NOTES.md + STAGE_G_PLAN.md — G-D12 event surface extended to 3 events (VaultClassRegistrySet + GaugeRegistrySet setter events per Stage D precedent) |
| G1.5 | b061ef3 | 2026-05-08 | src/gauge/SwapAndDepositToBodensee.sol — constructor refactor (5-param, ZeroAddress + TokenNotInPool guards, getPoolTokens index derivation) + onlyAuthorizedCaller modifier + setVaultClassRegistry + setGaugeRegistry one-shot setters (G-D12 caller-gate model) |
| G1.5-pre-B | 39b7edd | 2026-05-08 | docs/STAGE_G_NOTES.md — G-D15 (Revoked is terminal; no permissionless reactivation) + T-I6 invariant + recovery-shape coda |
| G1.5-pre-B-fix | 6bad870 | 2026-05-08 | docs/STAGE_G_NOTES.md — G-D15 → G-D17 renumber (Revoked-terminal; resolves PLAN G2.0 code collision) |
| G1.5-pre-B2 | dd25053 | 2026-05-08 | docs/STAGE_G_PLAN.md — G-D17 cross-reference paragraph (§G3 seam) + T-I6 binding to G3.6 + G4.4 |
| G1.6-pre-A | 057773e | 2026-05-08 | docs/STAGE_G_NOTES.md — AddLiquidityParams struct literal L293 + to: address(this) (6-field VaultTypes shape) |
| G1.6-pre-B | 693ed90 | 2026-05-08 | docs/STAGE_G_NOTES.md — G-D18 (V3 reserve-read selector lock: getPoolTokenInfo + balancesRaw[index]; getReservesOf + getCurrentLiveBalances rejected) + L290 cross-reference |
| G1.6 | 342c057 | 2026-05-08 | src/gauge/SwapAndDepositToBodensee.sol — swapAndDeposit + _swapAndDepositCallback (G-D12 nine-step outer + nine-step callback; G-D14 typed-slot transient via using StorageSlotExtension for *; G-D18 single-source getPoolTokenInfo + balancesRaw[idx] in _currentReserve helper; requiredAmount/_requiredAmount + Pick 3 6-field AddLiquidityParams) |
| G1.6-post-A | 5daa2ec | 2026-05-08 | docs/STAGE_G_NOTES.md — G-D14 Implementation pattern correction (using StorageSlotExtension for *; tload/tstore source-attribution from StorageSlotExtension not TransientStorageHelpers) + G12 finding (using-directive chain-rule lesson) |
| G1.7 | 43d04e2 | 2026-05-08 | test/unit/SwapAndDepositToBodensee.t.sol — 19 unit tests covering 14 G-D12 invariants (constructor TokenNotInPool, allowlist/setter/partial-activation, outer-entry guards, callback-side guards via MockVault attack-mode getPoolTokenInfo + corrupt-mode unlock); 19 passed |
| G1.8 | 75f466c | 2026-05-08 | test/fork/SwapAndDepositToBodensee.t.sol — 3 fork tests covering 4 G-D12 fork invariants (svZCHF + sUSDS happy paths verifying bptOut/delta/residue/event; ReserveDeltaMismatch via vm.mockCall on getPoolTokenInfo freezing pre-state); 3 passed in 13.89s |
| G1.8-post-A | 05480d2 | 2026-05-08 | docs/STAGE_G_PLAN.md L185 + docs/STAGE_G_NOTES.md L363 + tail — FoT counterfactual wording amendments (vm.mockCall on _vault.getPoolTokenInfo replacing payToken.transfer) + G13 finding (Foundry vm.expectRevert(bytes4) strict-match rule + frozen-pre-state mock pattern) |
| G1.9 | d6cf7be | 2026-05-08 | docs/STAGE_G_NOTES.md — G-D19 (VaultClassRegistry tunables lock: PROPOSAL_BOND_SVZCHF=1_000e18, VETO_THRESHOLD_BPS=1000, VETO_WINDOW_BLOCKS=201_600 per G-D9 propose-veto-finalize-revoke flow) + STAGE_G_PLAN.md L191/L199/L209 G-D14→G-D19 forward-claim renames |
| G1.10 | 7487a94 | 2026-05-08 | src/gauge/IVaultClassRegistry.sol — narrow read interface (AdmissionType enum {ImplementationAddress, FactoryAddress, BytecodeHash} + isAdmittedClass + admissionType views) per G-D8 + G-D9 + G-D10 |
| G1.11 | 061d967 | 2026-05-08 | src/token/IAuMT.sol + src/gauge/VaultClassRegistry.sol — IAuMT stub (Stage I forward-dep, IERC20 + governanceWeight) + VaultClassRegistry scaffold (5 imports, 2 immutables svZCHF/helper, 3 G-D19 constants PROPOSAL_BOND_SVZCHF/VETO_THRESHOLD_BPS/VETO_WINDOW_BLOCKS, 4 storage proposals/nextProposalId/admittedClasses/admissionTypes, 4 F-D20–F-D23 forward-dep slots auMT/governanceContract/auMTSetter/governanceSetter, 1 struct VaultClassProposal, 4 events, 10 errors; no functions / modifiers / IVaultClassRegistry inheritance — all defer to G1.12) |
| G1.12-pre-A | c3fa8db | 2026-05-08 | docs/STAGE_G_NOTES.md + STAGE_G_PLAN.md — G-D19 Tunable 1 bond lifecycle (non-refundable at propose per G-D9) + G1.14 BPS veto formula + drop stale qualifier |
| G1.12-pre-B | 1f570fa | 2026-05-08 | docs/STAGE_G_NOTES.md + STAGE_G_PLAN.md — G-D9 Option a-prime amendment + G-D20 genesis class set lock (calldata arrays, MAX_GENESIS_CLASSES=32, GenesisClassAdmitted event, 5-step validation discipline) + PLAN G1.11 supplement + G1.12-pre-C paragraph + G1.12 paragraph rewrite |
| G1.12-pre-C | a7288e4 | 2026-05-08 | src/gauge/VaultClassRegistry.sol — G-D20 scaffold supplement (MAX_GENESIS_CLASSES=32 constant, GenesisClassAdmitted event, GenesisLengthMismatch/GenesisOverflow/DuplicateGenesisToken errors); pure additive — no functions, modifiers, or constructor body (all defer to G1.12) |
| G1.12-pre-D | 556c82d | 2026-05-08 | docs/STAGE_G_NOTES.md + STAGE_G_PLAN.md — F-D23 setter-pattern lock (OnlyAuMTSetter/OnlyGovernanceSetter gate-revert errors, body order gate→ZeroAddress→assign→clear, SetterAlreadyCalled declared-but-unused disposition deferred to G5.x) |
| G1.12 | dc5fec2 | 2026-05-08 | src/gauge/VaultClassRegistry.sol — constructor body (genesis loop per G-D20 validation discipline) + setAuMT/setGovernanceContract one-shot setters per G-D9 G1.12-pre-D F-D23 setter-pattern lock + 2 new gate-revert errors (OnlyAuMTSetter/OnlyGovernanceSetter); first executable functions; forge build green; G-D20 calldata→memory correction (solc 0.8.26 constructor-param constraint, Error 6651) deferred to G1.12-post-A NOTES amendment |
| G1.12-post-A | 0f28fcc | 2026-05-09 | docs/STAGE_G_NOTES.md + STAGE_G_PLAN.md — calldata→memory/genesis-arrays sweep + G14 finding lock |
| G1.12-post-B | 6a73227 | 2026-05-09 | CLAUDE.md §11 — G14 housekeeping bullet (solc 0.8.26 calldata constructor-param constraint, G1.12 fix-loop) |
| G1.13-pre-A | 59ac72d | 2026-05-09 | docs/STAGE_G_NOTES.md + STAGE_G_PLAN.md — G-D9 enum-bound discipline lock (InvalidAdmissionType structurally unreachable at ABI decode, parallels SetterAlreadyCalled; G5.x cleanup deferred) + PLAN G1.13 L227 amendment |
| G1.13-pre-B | 4ae5863 | 2026-05-09 | docs/STAGE_G_NOTES.md — G-D9 BytecodeHash deferral lock (Option A, deferral with teeth) + G-D20 step 6 (constructor genesis guard) + Subsequent surface paragraph + sub-step renumbering + test invariant + cross-refs; new error BytecodeHashAdmissionDeferred() declared-but-(currently)-unused parallel with SetterAlreadyCalled / InvalidAdmissionType; G5.x cleanup deferred |
| G1.13-pre-C | c450a0f | 2026-05-09 | docs/STAGE_G_PLAN.md — paragraph amendments locking BytecodeHash deferral guards (G-D9 Option A + G-D20 step 6) into G1.12 + G1.13 implementation spec |
| G1.13-pre-D | aafb29b | 2026-05-09 | docs/STAGE_G_NOTES.md + docs/STAGE_G_PLAN.md — G-D21 helper two-entry-point split lock (swapAndDeposit strict / donate variable; donateAuthorizer + authorizedDonators) + G-D9/G-D12/G-D19 cross-reference reframe + PLAN G1.13 paragraph routing-path correction (helper.swapAndDeposit → helper.donate) |
| G1.6-bis-pre-A | a5a3b93 | 2026-05-09 | docs/STAGE_G_PLAN.md — PLAN G1.6-bis paragraph insertion (helper donate entry point + authorization mutators per G-D21; outer-only amendment, donateAuthorizer multi-shot + authorizedDonators runtime-mutable allowlist; six-line block between G1.6 and G1.7 sections; G1.7-bis pre-A deferred until after G1.6-bis source landing) |
| G1.6-bis | fc27f1f | 2026-05-09 | src/gauge/SwapAndDepositToBodensee.sol — donate entry point + authorization surface per G-D21; test/unit + test/fork arity fix (5→6 constructor args; forge build green) |
| G1.7-bis-pre-A | 87e9b30 | 2026-05-09 | docs/STAGE_G_PLAN.md — PLAN G1.7-bis paragraph insertion (donate-path unit invariants: 11 auth-mutator + 6 donate-entry-point; 17 total; reuses G1.7 MockVault fixtures; pure unit; six-line block between G1.7 and G1.8 sections) |
| G1.7-bis-pre-B | 86de94d | 2026-05-09 | docs/STAGE_G_PLAN.md — G1.7-bis paragraph correction (tests 5–6 revised: callback-reached + ReserveDeltaMismatch instead of mock-infeasible happy path; deferred-to-G1.8 note added; MockVault verbatim confirmed) |
| G1.7-bis | ca222ef | 2026-05-09 | test/unit/SwapAndDepositToBodensee.t.sol — 17 donate-path unit invariants (11 auth-mutator + 6 donate-entry-point; cross-entry-point reentrancy via shared _EXECUTING_SLOT; callback-reached at 50e18 + 1 wei prove no magnitude gate per G-D21; all 36 tests green) |
| G1.13 | 03fdd98 | 2026-05-09 | src/gauge/VaultClassRegistry.sol — proposeVaultClass (bond pull+push→helper.donate per G-D21/G-D12; BytecodeHashAdmissionDeferred guard; struct alloc; event emit) + BytecodeHashAdmissionDeferred error decl + constructor genesis-loop step 3.b BytecodeHash guard (G-D20 step 6) + L55 admissionValue Natspec correction (deferral form per G-D20 subsequent-surface); forge build green |
| G1.14 | 6a1500b | 2026-05-10 | src/gauge/VaultClassRegistry.sol — vetoProposal (AuMT-weighted accumulator, threshold auto-revoke at VETO_THRESHOLD_BPS=1000 per G-D19, emit-always per G-D9) + finalizeProposal (window-expiry-permissioned admission, mutates admittedClasses + admissionTypes, emit VaultClassFinalized); both external + permissionless per G-D9 'Auto-finalize'; consume existing error/event/constant/storage surface; forge build green |
| G15 | 64f91f0 | 2026-05-10 | src/gauge/SwapAndDepositToBodensee.sol — suppress unsafe-typecast lint warnings on constructor uint8(i) index casts via forge-lint:disable-next-line + Bodensee 3-token safety justification; forge build clean; 283/283 unit tests pass |
| G1.15 | c7db1a3 | 2026-05-10 | src/gauge/VaultClassRegistry.sol — onlyGovernance modifier (governanceContract gate; pre-setter seal) + revokeVaultClass (ClassNotAdmitted guard; admission flag flip; VaultClassRevoked emit; no pool-state mutation per G-D8/G-D17 scope; re-admission via propose/finalize recovery path per G-D9); forge build clean |
| G1.15-post-A | 90111f4 | 2026-05-10 | docs/STAGE_G_NOTES.md — G15 finding (scaffold NatSpec interface-promise drift; IVaultClassRegistry inheritance + view bridges promised at G1.12+ but unlanded through G1.15; G1.15-post source-only sub-step + G1.15-post-B PLAN amendment as resolution; default rule extends §12 ambiguity-gate to scaffold-promise tracking) |
| G1.15-post-B | a1cc8d3 | 2026-05-10 | docs/STAGE_G_PLAN.md — insert G1.15-post sub-step row between G1.15 and G1.16 (IVaultClassRegistry inheritance + view bridges + scaffold NatSpec correction; satisfies G2.3 PLAN L293 typed-call dependency; resolution path locked at G15 finding NOTES commit `90111f4`) |
| G1.15-post | 8d9fe88 | 2026-05-10 | src/gauge/VaultClassRegistry.sol — add `is IVaultClassRegistry` + view bridges (isAdmittedClass / admissionType forwarding to admittedClasses / admissionTypes; @inheritdoc + override per G-D9 + G1.10 surface) + rewrite stale NatSpec to present-tense; fix1: rename proposeVaultClass param admissionType → admissionType_ (shadow-clean vs contract-level admissionType(address)); forge build clean; closes G15 scaffold-promise gap |
| G1.15-post-C | fd85c8d | 2026-05-10 | CLAUDE.md §11 — G15 housekeeping bullet (scaffold NatSpec interface-promise drift; `is IVaultClassRegistry` + view bridges promised at G1.12+ unlanded through G1.15; surfaced at G1.16 design pre-flight; resolved at G1.15-post source commit `8d9fe88`; G15 default rule extends §12 ambiguity-gate to scaffold-promise tracking with close-of-family Must match enumeration) |
| G1.16a | 14b9859 | 2026-05-10 | test/unit/VaultClassRegistry.t.sol — scaffold + inline mocks (MockSwapAndDepositToBodensee donate-recorder; MockAuMT IERC20 + governanceWeight) + state vars + setUp wiring (mocks deploy, role addresses, 2-token genesis array [Implementation/Factory], registry construct, one-shot setters fired, proposer minted + approved); forge build clean (purity warnings on IERC20 stubs only); no test_* functions yet (G1.16b/c/d to follow); G1.16 split per §6 baby-step discipline |
| G1.16b | 8632479 | 2026-05-10 | test/unit/VaultClassRegistry.t.sol — append 14 test functions (5 genesis-seed: isAdmittedClass+admissionType for genesisTokenA/B + non-genesis stranger fallthrough; 4 constructor zero-address reverts: svZCHF / helper / auMTSetter / governanceSetter; 5 genesis-array reverts: GenesisLengthMismatch / GenesisOverflow at MAX+1=33 / DuplicateGenesisToken / BytecodeHashAdmissionDeferred / ZeroAddress-at-genesis); fix1: enum-equality assertEq overload miss — cast both sides to uint256 (AdmissionType has no native assertEq overload); fix2: state-mutability narrowings per existing precedent — 4 read-only tests `public view` (AureumAuthorizer.t.sol:16 pattern), 5 MockAuMT IERC20 stubs `external pure` (SwapAndDepositToBodensee.t.sol:91 pattern); testNonGenesisToken_NotAdmitted stays `public` (calls makeAddr); 14/14 pass; forge test warning-free |
| G1.16c | 6b8f2c0 | 2026-05-10 | test/unit/VaultClassRegistry.t.sol — append 2 helpers (_propose / _warpPastWindow) + 5 state-machine tests (testPropose_Success: bond routing 1_000e18 svZCHF proposer→registry→helper + donate call recording + initial proposal state; testVeto_SufficientWeight_KillsProposal: 100_000e18 vetoer crosses 10% threshold of 1M AuMT supply; testVeto_InsufficientWeight_Accumulates: 99_999e18 below threshold accrues vetoSupport without flipping flags; testVeto_Cumulative_CrossesThreshold: two 50_000e18 vetoers; testFinalize_AfterWindow_AdmitsClass: vm.roll past 201_600 blocks then permissionless finalize admits class with correct admissionType); fix1: drop 5 redundant outer vm.prank(proposer) calls (the _propose helper owns the prank; outer prank caused 'cannot overwrite a prank' on every test); 19/19 pass |
| G1.16d | 83fe983 | 2026-05-10 | test/unit/VaultClassRegistry.t.sol — window guards + revoke + setter-seal + placeholder unreachability + event mirrors; 37/37 green |